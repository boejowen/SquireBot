---
phase: 5
plan: 05
type: execute
wave: 5
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
files_modified:
  - docs/_config.yml
  - docs/index.md
  - docs/install.md
  - docs/troubleshooting.md
  - docs/dev.md
  - docs/phase5-distribution.md
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - DOC-01
  - DOC-03
user_setup:
  - service: github-pages
    why: "Publishing the onboarding site at boejowen.github.io/SquireBot"
    env_vars: []
    dashboard_config:
      - task: "Enable GitHub Pages on the repo"
        location: "github.com/boejowen/SquireBot → Settings → Pages → Source: Deploy from a branch → branch: main, folder: /docs → Save"
      - task: "Wait for first build"
        location: "github.com/boejowen/SquireBot/actions — `pages-build-deployment` workflow; ~30-90s; first build URL appears in Settings → Pages"
  - service: screen-recorder
    why: "Recording the SmartScreen walkthrough GIF (D-10)"
    env_vars: []
    dashboard_config:
      - task: "Install ScreenToGif (or LICEcap / ShareX equivalent)"
        location: "https://www.screentogif.com/ (Microsoft Store also available)"
      - task: "Record SmartScreen walkthrough at ≤720p, ≤15fps; export with single-color compression; budget ≤5MB"
        location: "Local dev machine; assets/smartscreen.gif"

tags:
  - docs
  - onboarding
  - distribution
  - smoke

must_haves:
  truths:
    - "GitHub Pages site is live at `https://boejowen.github.io/SquireBot/` and renders the Cayman theme"
    - "`/docs/index.md` is a 1-paragraph landing with `Install →` and `GitHub →` links"
    - "`/docs/install.md` is a chronological 5-step walkthrough (installer → SmartScreen → OAuth → folder picker → first sync) — text-only per 2026-05-11 scope reduction; SmartScreen step describes the dialog elements (`More info` link, `Run anyway` button) in enough detail to navigate without a screenshot"
    - "`/docs/troubleshooting.md` is symptom-keyed (tray red, ErrSchemaTooNew, drive.file propagation delay, system tab visibility recovery via Install Triggers)"
    - "`/docs/dev.md` is a flat link list into existing `docs/oauth-setup.md`, `docs/apps-script-deploy.md`, `docs/build-and-install.md`, `docs/eviction-runbook.md` (from 05-04)"
    - "`README.md` is shrunk to a short pointer per D-12 (1 paragraph + install link + GitHub link + contributor link)"
    - "`REQUIREMENTS.md` traceability row for SEARCH-03 is updated to reflect Path 2 (search sidebar tooltip + view/bank Last Synced columns)"
    - "An end-to-end fake-guildie eviction smoke is documented in this plan's SUMMARY (verifies DOC-02 + the 05-04 + 05-02 chain)"
    - "Distribution report produced at `docs/phase5-distribution.md` with the heartbeat-freshness count; the user authorizes Phase-5-shipped after reviewing the report (no fixed numeric threshold above 1; only a count of ≤1 surfaces an automated smoke-regression warning)"
  artifacts:
    - path: "docs/_config.yml"
      provides: "Jekyll config with remote_theme cayman v0.2.0"
      contains: "remote_theme: pages-themes/cayman@v0.2.0"
    - path: "docs/index.md"
      provides: "Pages landing page"
      contains: "layout: default"
    - path: "docs/install.md"
      provides: "DOC-01 install runbook"
      contains: "Install SquireBot"
    - path: "docs/troubleshooting.md"
      provides: "DOC-01 recovery runbook"
      contains: "Troubleshooting"
    - path: "docs/dev.md"
      provides: "Developer notes link list"
      contains: "Developer notes"
    - path: "docs/assets/smartscreen.gif"
      provides: "DOC-03 SmartScreen walkthrough animation"
      max_size_bytes: 5242880
    - path: "docs/phase5-distribution.md"
      provides: "Phase-5 distribution report (heartbeat-freshness count + report narrative)"
      contains: "Phase 5 distribution report"
    - path: "README.md"
      provides: "Short Pages pointer per D-12"
      contains: "boejowen.github.io/SquireBot"
  key_links:
    - from: "README.md"
      to: "https://boejowen.github.io/SquireBot/install/"
      via: "markdown link"
      pattern: "boejowen\\.github\\.io/SquireBot"
    - from: "docs/install.md"
      to: "docs/troubleshooting.md"
      via: "tray-red recovery cross-link"
      pattern: "troubleshooting"
    - from: "docs/dev.md"
      to: "docs/eviction-runbook.md"
      via: "link list entry"
      pattern: "eviction-runbook"

scope_notes:
  - "This plan contains a HUMAN-IN-THE-LOOP checkpoint for the SmartScreen GIF recording (the user installs the recorder + records the walkthrough themselves per MEMORY.md `feedback_toolchain_installs`)."
  - "**Authorized scope reduction (recorded in CONTEXT.md §43 during plan-revision 2026-05-11):** the literal 12-guildie figure is an UPPER ESTIMATE, not a hard ship gate. Phase 5 ships when a meaningful core of the guild is writing data; the user explicitly authorized 'even a handful' as acceptable. THIS PLAN therefore emits a distribution REPORT, not a GREEN/YELLOW/RED verdict. The only automated sanity check is: if the heartbeat-freshness count is `≤ 1` that's likely a deployment failure (only the dev box is reporting); surface to the user as a likely smoke regression rather than a ship decision. Above 1 → present the report to the user via `checkpoint:human-action` and let them decide whether to mark Phase 5 SHIPPED."
  - "Per CONTEXT scope-changes: installer-driven upgrade UX stays DEFERRED unless it actively blocks distribution. If a guildie cannot self-upgrade, document the workaround (stop process → run installer) in troubleshooting.md AND surface as a v1.0.1 polish item rather than re-opening the structural fix here."
  - "Pages enablement (Settings → Pages → /docs source) is an OWNER action — surfaced via the `user_setup` frontmatter."
  - "Asset sizes: PNG ≤200KB each (use pngcrush or OS image viewer crop); GIF ≤5MB per D-10. If GIF >5MB, fallback to YouTube unlisted per CONTEXT deferred list — but update UI-SPEC §Image format if invoked."
---

<objective>
Close Phase 5 by delivering the DOC-01 + DOC-03 onboarding artifacts, running the DOC-02 end-to-end fake-guildie smoke, and producing the distribution report that the user uses to authorize Phase-5-shipped.

Deliverables (in dependency order within this plan):

1. **Jekyll Pages site under `/docs/`** — 5 markdown files + assets directory. The `remote_theme: pages-themes/cayman@v0.2.0` plugin is on the GitHub Pages allowlist. Source is the standard `/docs` folder.
2. **Static assets** — 6 PNG screenshots + 1 annotated GIF (≤5MB). PNG production is automatable; the GIF requires a human-driven screen-recording session (gated by a `checkpoint:human-action`).
3. **README.md shrink per D-12** — 1-paragraph "what is SquireBot" + 3 links (install, GitHub, dev). Existing long-form content moves to `docs/dev.md` (or referenced via link list).
4. **Owner action: Enable Pages on GitHub** (`checkpoint:human-action` — repo settings change Claude cannot script).
5. **REQUIREMENTS.md traceability update for SEARCH-03 Path 2.**
6. **DOC-02 end-to-end smoke against a fake guildie account** — runs the full eviction → grace → archive chain. Verifies 05-04's commitEviction + 05-02's weeklyEvictionArchive integration.
7. **Distribution report + user-authorized ship gate** — query `_char_owner.owner_email` heartbeat-freshness; write `docs/phase5-distribution.md`; surface via `checkpoint:human-action` for the user to authorize Phase 5 SHIPPED. No automated GREEN/YELLOW/RED logic; the user makes the call. The ONLY automated sanity check is: count ≤ 1 → surface as likely smoke regression.

Closes: DOC-01, DOC-03. Also retires the partial-DOC-02 obligation by running the smoke.

Phase 5 SHIPPED gate: distribution report produced; user reviews the report and authorizes ship via the checkpoint. After 05-05 SUMMARY lands and a milestone retrospective is committed, v1.0 ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-search-onboarding-privacy-polish/05-CONTEXT.md
@.planning/phases/05-search-onboarding-privacy-polish/05-RESEARCH.md
@.planning/phases/05-search-onboarding-privacy-polish/05-UI-SPEC.md
@CLAUDE.md
@docs/oauth-setup.md
@docs/build-and-install.md
@docs/apps-script-deploy.md
@docs/eviction-runbook.md

<interfaces>
**Cayman theme config (verbatim from RESEARCH §Standard Stack — Static Site Stack):**
```yaml
# docs/_config.yml
remote_theme: pages-themes/cayman@v0.2.0
plugins:
  - jekyll-remote-theme
title: SquireBot
description: A small Windows app that streams your EverQuest inventory and spellbook into your guild's shared workbook.
show_downloads: false
```

**Locked H1 + lede copy (verbatim from 05-UI-SPEC §Onboarding site copywriting):**
- `index.md` — H1 `SquireBot`, lede `A small Windows app that streams your EverQuest inventory and spellbook into your guild's shared workbook.`
- `install.md` — H1 `Install SquireBot`, lede `Five steps. Takes about three minutes.`
- `troubleshooting.md` — H1 `Troubleshooting`, lede `Tray icon turned red? Workbook stopped updating? Start here.`
- `dev.md` — H1 `Developer notes`, lede `Stack overview, how to build, how to deploy Apps Script.`

**5-step install structure** (per UI-SPEC §Copywriting — chronological H2-keyed):
1. `## 1. Download` — point to GitHub Releases; latest is v0.4.0 (or whichever tag is current at execution).
2. `## 2. Run the installer` — `.exe` is unsigned; SmartScreen "More info" → "Run anyway"; screenshot pair + the GIF.
3. `## 3. Authorize Google` — OAuth loopback PKCE; expect a browser tab to open; `drive.file` scope; Production consent screen.
4. `## 4. Pick the workbook and EQ folder` — Drive Picker → guild workbook; folder picker → EverQuest install (auto-detected via heuristic, with manual fallback).
5. `## 5. Trigger your first sync` — in EQ, type `/outputfile inventory`; check for `inv:<YourChar>` tab in the workbook within ~30s.

**Symptom-keyed troubleshooting H2s** (per UI-SPEC):
- `## Tray icon is red`
- `## Workbook isn't updating`
- `## SmartScreen warning won't go away`
- `## "ErrSchemaTooNew" in the log`
- `## drive.file permission propagation (~50 minute delay)`  ← cite MEMORY.md note `project_drive_file_propagation_delay`
- `## Hidden tabs (`_meta`, `_char_owner`, etc.) reappeared`  ← run Install Triggers (Pitfall P12 + 05-01)
- `## I need to remove a guildie`  ← link to `docs/eviction-runbook.md`

**Dev page link list** (flat — no content duplication):
- Stack overview → `CLAUDE.md` (root, the canonical doc)
- OAuth setup → `docs/oauth-setup.md`
- Apps Script deploy → `docs/apps-script-deploy.md`
- Build + sideload → `docs/build-and-install.md`
- Eviction runbook → `docs/eviction-runbook.md`
- Phase 4 + 5 SUMMARY files for execution history

**README.md (post-shrink) target structure (per D-12):**
```markdown
# SquireBot

A small Windows app that streams your EverQuest inventory and spellbook into your guild's shared workbook.

**Install:** https://boejowen.github.io/SquireBot/install/
**GitHub:** https://github.com/boejowen/SquireBot
**Developer notes:** https://boejowen.github.io/SquireBot/dev/

---

For full project context, see `CLAUDE.md`.
```
(That's the ENTIRE post-shrink README — no more sections. The existing long-form content goes to `docs/dev.md` or stays in `CLAUDE.md` which is already comprehensive.)

**Asset filenames** (from RESEARCH §Recommended Project Structure):
- `docs/assets/01-installer.png` (installer dialog)
- `docs/assets/02-smartscreen-more-info.png` (SmartScreen with `More info` link visible)
- `docs/assets/03-smartscreen-run-anyway.png` (SmartScreen with `Run anyway` button visible)
- `docs/assets/04-oauth-consent.png` (Google OAuth consent screen)
- `docs/assets/05-folder-picker.png` (EQ folder picker dialog)
- `docs/assets/tray-red.png` (tray icon in error state)
- `docs/assets/smartscreen.gif` (≤5MB annotated GIF; the load-bearing onboarding asset)

**Distribution report query** (run in script editor or via custom function; outputs the counts written into `docs/phase5-distribution.md`):
```typescript
// Read _char_owner.owner_email distinct count + last_seen freshness (last 7 days)
const ss = SpreadsheetApp.getActiveSpreadsheet();
const sheet = ss.getSheetByName('_char_owner');
const values = sheet.getRange(2, 1, sheet.getLastRow() - 1, 13).getValues();
const sevenDaysAgoMs = Date.now() - 7 * 24 * 60 * 60 * 1000;
const distinctActiveEmails = new Set<string>();
const distinctRecentEmails = new Set<string>();
for (const r of values) {
  const isRemoved = r[8] === true || String(r[8]) === 'true';
  if (isRemoved) continue;
  const email = String(r[1] ?? '').trim();
  if (!email) continue;
  distinctActiveEmails.add(email);
  const lastSeenRaw = r[10];
  const lastSeenMs = typeof lastSeenRaw === 'string' ? Date.parse(lastSeenRaw) : (lastSeenRaw instanceof Date ? lastSeenRaw.getTime() : NaN);
  if (Number.isFinite(lastSeenMs) && lastSeenMs > sevenDaysAgoMs) distinctRecentEmails.add(email);
}
console.log(`Distinct active emails: ${distinctActiveEmails.size}`);
console.log(`Distinct emails seen in last 7d: ${distinctRecentEmails.size}`);
// NOTE: no fixed numeric threshold above 1. Above 1 → present to user.
// At exactly 1 (or 0) → likely a deployment failure; surface as smoke regression.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Jekyll Pages site (markdown + config + asset placeholders)</name>
  <files>docs/_config.yml, docs/index.md, docs/install.md, docs/troubleshooting.md, docs/dev.md, docs/assets/.gitkeep, README.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/oauth-setup.md (the existing OAuth runbook to cross-link from install.md and dev.md)
    - docs/build-and-install.md (the existing build runbook; cross-link from dev.md)
    - docs/apps-script-deploy.md (the existing clasp deploy doc; cross-link from dev.md)
    - docs/eviction-runbook.md (written by 05-04; cross-link from dev.md and from troubleshooting.md "I need to remove a guildie" section)
    - README.md (the current long-form content — read in full to confirm what moves to dev.md vs what stays inline in CLAUDE.md)
    - .planning/REQUIREMENTS.md (the traceability table — locate the SEARCH-03 row to update with Path 2 note)
    - .planning/phases/05-search-onboarding-privacy-polish/05-UI-SPEC.md §Onboarding Site Visual Contract (PNG/GIF placement rules, max-width: 100% CSS, no autoplay, no YouTube embed, alt text mandatory)
    - .planning/phases/05-search-onboarding-privacy-polish/05-RESEARCH.md §Pattern 7 (the Jekyll file-by-file targets)
    - .planning/phases/05-search-onboarding-privacy-polish/05-RESEARCH.md §Pitfall P10 (GitHub Pages allowlist — only `jekyll-remote-theme` plugin)
  </read_first>
  <action>
    1. **Create `docs/_config.yml`** with the verbatim YAML from `<interfaces>` (remote_theme cayman v0.2.0, plugin allowlist, title, description, show_downloads: false).

    2. **Create `docs/index.md`** with front-matter `layout: default` and the H1 + lede from `<interfaces>` plus two links:
       ```markdown
       ---
       layout: default
       ---

       # SquireBot

       A small Windows app that streams your EverQuest inventory and spellbook into your guild's shared workbook.

       [Install →]({{ "/install/" | relative_url }}) ·
       [GitHub →](https://github.com/boejowen/SquireBot)
       ```

    3. **Create `docs/install.md`** with the 5-step structure from `<interfaces>`. For each step:
       - `## N. <Title>` header
       - 1-3 sentences of body copy in factual second-person voice (no exclamation marks, no marketing language per UI-SPEC §Copywriting)
       - Inline `![alt](assets/<filename>.png)` reference for steps with screenshots
       - The SmartScreen GIF embedded in Step 2 with alt text exactly `Animated walkthrough of the Windows SmartScreen prompt — click "More info", then "Run anyway".`
       - Cross-link to troubleshooting.md at the bottom: `If you hit a snag, see [Troubleshooting]({{ "/troubleshooting/" | relative_url }}).`

       Include the full lede `Five steps. Takes about three minutes.` directly after the H1.

       Step content guide (use these EXACT facts; pull from CLAUDE.md + ROADMAP.md):
       - Step 1: download `SquireBot-Setup-v0.4.0.exe` (or the current latest tag at execution time — verify via `git tag --sort=-v:refname | head -1`) from `https://github.com/boejowen/SquireBot/releases/latest`. ~12 MB. Per-user installer, no UAC.
       - Step 2: Windows SmartScreen will warn because the binary is unsigned. Click `More info`, then `Run anyway`. The walkthrough GIF shows the exact sequence. (No EV signing — per MEMORY.md `project_smartscreen_ev_no_longer_instant`.) Installer completes in ~5 seconds.
       - Step 3: SquireBot opens your default browser once for Google authorization. Scope is `drive.file` (you'll see "See, edit, create, and delete only the specific Google Drive files you use with this app"). The consent screen is in Production state — refresh tokens do not silently expire.
       - Step 4: SquireBot opens two more browser tabs in sequence. The first is the Google Drive Picker — select your guild's shared workbook. The second (if your EQ folder isn't auto-detected) is a local folder picker — point to your EverQuest install root (e.g. `C:\P99\EverQuest\`).
       - Step 5: In EverQuest, type `/outputfile inventory`. EQ writes `<YourChar>-Inventory.txt` to the configured folder. SquireBot detects the file (fsnotify, 500 ms debounce), parses 5 columns (Location, Name, ID, Count, Slots), and writes to `inv:<YourChar>` in the workbook within ~30 seconds. Then try `/outputfile spellbook` for casters.

    4. **Create `docs/troubleshooting.md`** with the symptom-keyed H2 structure from `<interfaces>`. Each section:
       - `## <Symptom>` header
       - 1-2 sentences of cause description
       - Numbered fix steps (1, 2, 3...)
       - Cross-link where relevant (Install Triggers → SquireBot menu; ErrSchemaTooNew → update watcher; drive.file delay → wait + Reauthorize)

       Use these EXACT facts:
       - **Tray icon is red:** OAuth token failure (most common: `invalid_grant` after 7-day silent expiry on Testing-mode consent screen — but ours is Production so this should be rare). Fix: right-click tray → Reauthorize. If still red, check `%LOCALAPPDATA%\SquireBot\squirebot.log` for the most recent ERROR line.
       - **Workbook isn't updating:** check tray color first. If green, check the log for `WATCH-09 catch-up` messages (watcher rescans on restart per WATCH-09). If watcher is fine, the workbook's Apps Script may need a manual trigger run; open the workbook → SquireBot → Install Triggers.
       - **SmartScreen warning won't go away:** Windows reputation is per-binary-hash. Updates to the .exe re-trigger SmartScreen until reputation builds. Workaround: follow the same `More info → Run anyway` flow. (EV signing removed the instant-rep perk in March 2024 per MEMORY.md note — no avoiding the warning short of SignPath OSS approval.)
       - **"ErrSchemaTooNew" in the log:** the workbook is at a higher schema version than this watcher binary supports. Fix: update the watcher (right-click tray → Check for updates; or manually download the latest installer).
       - **drive.file permission propagation (~50 minute delay):** after a fresh Reauthorize + Picker, write access can take up to ~50 minutes to propagate (cite the probe-goroutine fix from MEMORY.md `project_drive_file_propagation_delay`). The watcher's startup probe waits up to 90 minutes for this. If the tray stays yellow/probe-state for >90 minutes, escalate via the GitHub Issues tracker.
       - **Hidden tabs reappeared:** any editor can right-click → Unhide on a system tab. Fix: from the workbook, open `SquireBot → Install Triggers`. This re-hides all `_`-prefixed tabs (the `hideAllSystemTabs` call from 05-01 is idempotent).
       - **I need to remove a guildie:** see the [Eviction runbook](https://github.com/boejowen/SquireBot/blob/main/docs/eviction-runbook.md). Short version: SquireBot → Evict Guildie → select email → confirm. 30-day grace before archive.

    5. **Create `docs/dev.md`** with the flat link list from `<interfaces>`:
       ```markdown
       ---
       layout: default
       ---

       # Developer notes

       Stack overview, how to build, how to deploy Apps Script.

       ## Canonical docs in this repo
       - [Stack + architecture + conventions](https://github.com/boejowen/SquireBot/blob/main/CLAUDE.md)
       - [OAuth Cloud setup runbook](https://github.com/boejowen/SquireBot/blob/main/docs/oauth-setup.md)
       - [Build + sideload watcher](https://github.com/boejowen/SquireBot/blob/main/docs/build-and-install.md)
       - [Apps Script clasp deploy](https://github.com/boejowen/SquireBot/blob/main/docs/apps-script-deploy.md)
       - [Eviction runbook (DOC-02)](https://github.com/boejowen/SquireBot/blob/main/docs/eviction-runbook.md)

       ## Phase summaries
       - [Phase 1 — End-to-End Thin Slice](https://github.com/boejowen/SquireBot/tree/main/.planning/phases/01-end-to-end-thin-slice)
       - [Phase 2 — Watcher Robustness](https://github.com/boejowen/SquireBot/tree/main/.planning/phases/02-watcher-robustness-schema-lock)
       - [Phase 3 — Apps Script Enrichment](https://github.com/boejowen/SquireBot/tree/main/.planning/phases/03-apps-script-enrichment-foundation)
       - [Phase 4 — Differentiator Features](https://github.com/boejowen/SquireBot/tree/main/.planning/phases/04-differentiator-features)
       - [Phase 5 — Search + Onboarding + Polish](https://github.com/boejowen/SquireBot/tree/main/.planning/phases/05-search-onboarding-privacy-polish)
       ```

    6. **Create `docs/assets/.gitkeep`** as an empty file (so the directory exists in the repo even before Task 2 lands the PNG/GIF binaries).

    7. **Shrink `README.md`** to the post-shrink target from `<interfaces>`. Before stripping, verify that any content NOT mirrored in CLAUDE.md is preserved by appending it to `docs/dev.md` under a new `## Legacy README notes` section. (In practice CLAUDE.md is already the comprehensive doc — most existing README content is duplicative.)

    8. **Update `.planning/REQUIREMENTS.md` traceability table** for SEARCH-03. Locate the row:
       ```
       | SEARCH-03 | Phase 5 | Pending |
       ```
       Change to:
       ```
       | SEARCH-03 | Phase 5 | ✓ Complete (Path 2: search-button cache-freshness tooltip + existing `view`/`bank` `Last Synced` columns — Plan 05-03) |
       ```
       Add a footnote line at the end of the file: `**SEARCH-03 Path 2 note:** per CONTEXT scope-change 2026-05-11, the search sidebar does not show per-row staleness; the existing `view` and `bank` tabs' `Last Synced` columns (Phase 3 VIEW-04 conditional formatting) satisfy the requirement on a complementary surface.`
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const required = ['docs/_config.yml','docs/index.md','docs/install.md','docs/troubleshooting.md','docs/dev.md','docs/assets/.gitkeep','README.md']; let ok = true; for (const f of required) { if (!fs.existsSync(f)) { console.error('MISSING:', f); ok = false; } } process.exit(ok ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nF "remote_theme: pages-themes/cayman@v0.2.0" docs/_config.yml` returns exactly 1 hit
    - `grep -nF "jekyll-remote-theme" docs/_config.yml` returns ≥ 1 hit
    - `grep -nF "title: SquireBot" docs/_config.yml` returns exactly 1 hit
    - `grep -nF "layout: default" docs/index.md` returns ≥ 1 hit
    - `grep -nF "# SquireBot" docs/index.md` returns exactly 1 hit (H1)
    - `grep -nF "boejowen/SquireBot" docs/index.md` returns ≥ 1 hit (GitHub link)
    - `grep -nF "# Install SquireBot" docs/install.md` returns exactly 1 hit
    - `grep -nF "Five steps. Takes about three minutes." docs/install.md` returns exactly 1 hit
    - `grep -nE "^## [12345]\\. " docs/install.md | wc -l` is ≥ 5 (5 numbered steps)
    - `grep -nF "smartscreen.gif" docs/install.md` returns ≥ 1 hit (GIF referenced)
    - `grep -nF "More info" docs/install.md` returns ≥ 1 hit (SmartScreen flow)
    - `grep -nF "/outputfile inventory" docs/install.md` returns ≥ 1 hit (Step 5)
    - `grep -nF "# Troubleshooting" docs/troubleshooting.md` returns exactly 1 hit
    - `grep -nE "^## " docs/troubleshooting.md | wc -l` is ≥ 6 (the 6-7 symptom H2 sections)
    - `grep -nF "Tray icon is red" docs/troubleshooting.md` returns ≥ 1 hit
    - `grep -nF "ErrSchemaTooNew" docs/troubleshooting.md` returns ≥ 1 hit
    - `grep -nF "drive.file" docs/troubleshooting.md` returns ≥ 1 hit
    - `grep -nF "Install Triggers" docs/troubleshooting.md` returns ≥ 1 hit
    - `grep -nF "eviction-runbook" docs/troubleshooting.md` returns ≥ 1 hit
    - `grep -nF "# Developer notes" docs/dev.md` returns exactly 1 hit
    - `grep -nF "eviction-runbook.md" docs/dev.md` returns ≥ 1 hit
    - `grep -nF "oauth-setup.md" docs/dev.md` returns ≥ 1 hit
    - `wc -l README.md` shows ≤ 30 lines (shrunk per D-12)
    - `grep -nF "boejowen.github.io/SquireBot" README.md` returns ≥ 1 hit
    - `grep -nF "SEARCH-03 | Phase 5 | ✓ Complete" .planning/REQUIREMENTS.md` returns ≥ 1 hit (traceability updated)
    - `grep -nF "Path 2" .planning/REQUIREMENTS.md` returns ≥ 1 hit
  </acceptance_criteria>
  <done>5 markdown pages exist with locked copy + cross-links; README shrunk; REQUIREMENTS.md traceability for SEARCH-03 updated; assets directory placeholder exists.</done>
</task>

<!--
SCOPE REDUCTION — 2026-05-11 (mid-execution of plan 05-05, after Task 1 shipped)

Per user direction, the instructional images + SmartScreen GIF are DROPPED. Pages docs are text-only.
The CONTEXT.md Scope Changes section records the rationale next to its peers.

EFFECT ON THIS TASK:
- Task 2 Step A (capture 6 PNGs)       — STRUCK; do not perform
- Task 2 Step B (record SmartScreen GIF) — STRUCK; do not perform
- Task 2 Step C (enable GitHub Pages)    — KEEP; this is the only remaining Task 2 action
- Task 2 Step D (commit assets)          — STRUCK; no assets to commit
- Task 3 Step 0 (asset audit)            — STRUCK; nothing to audit
- `docs/assets/` directory                — removed from the repo

REVISED RESUME-SIGNAL for Task 2 (overrides the `<resume-signal>` block below):
- `pages: live at <URL>` — Pages enabled and the text-only site renders correctly
- `blocked: <reason>`    — Pages enablement is stuck

The Steps A/B/D text is retained below as historical context, NOT as instructions to follow.
-->

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Owner action — enable GitHub Pages (text-only; Steps A/B/D superseded by 2026-05-11 scope reduction)</name>
  <what-built>
    Plan 05-05 Task 1 produced the Pages site source under `/docs/`. After the 2026-05-11 scope reduction, only one operation remains that Claude cannot perform:

    1. **GitHub Pages enablement** — repository Settings → Pages → Source must be flipped to `Deploy from a branch → main + /docs`. This is a repo-owner-only action in the GitHub UI.

    ~~Asset production (6 PNGs + GIF)~~ — DROPPED 2026-05-11; Pages site is text-only.
  </what-built>
  <how-to-verify>
    **Step A — Capture the 6 PNG screenshots (max 1200px wide, ≤200KB each — pngcrush optional but recommended):**
    1. On a clean Windows 11 box (or a VM), download the latest SquireBot installer from `https://github.com/boejowen/SquireBot/releases/latest` and screenshot the installer dialog → save as `docs/assets/01-installer.png`.
    2. When SmartScreen appears, screenshot the initial prompt showing `More info` → save as `docs/assets/02-smartscreen-more-info.png`.
    3. Click `More info`. Screenshot the expanded panel showing `Run anyway` → save as `docs/assets/03-smartscreen-run-anyway.png`.
    4. After install, when the Google OAuth consent screen appears in the browser, screenshot it (showing `drive.file` scope being requested) → save as `docs/assets/04-oauth-consent.png`.
    5. When the EQ folder picker dialog appears (or the auto-detected folder confirmation), screenshot it → save as `docs/assets/05-folder-picker.png`.
    6. Optional but recommended for the troubleshooting page: trigger a tray-red state (e.g., revoke the OAuth grant in `https://myaccount.google.com/permissions`, wait for next watcher poll), screenshot the red tray icon and any context menu → save as `docs/assets/tray-red.png`.

    **Step B — Record the SmartScreen GIF (≤5MB):**
    1. Install ScreenToGif (https://www.screentogif.com/) or equivalent (LICEcap / ShareX).
    2. Start a recording. Re-run the installer on the same machine; capture the SmartScreen sequence: initial warning → click `More info` → expanded panel → click `Run anyway` → installer launches.
    3. Stop recording. Trim to just the SmartScreen interaction (~10-15 seconds ideal).
    4. Add annotations (text callouts pointing at `More info` and `Run anyway`) in ScreenToGif's editor.
    5. Export with single-color-replacement compression at ≤720p, ≤15fps.
    6. Verify the file size: it MUST be ≤ 5,242,880 bytes. Run `ls -la docs/assets/smartscreen.gif` (or `(Get-Item docs/assets/smartscreen.gif).Length` in PowerShell). If >5MB, re-export at lower fps or shorter duration. If consistently >5MB, fallback: upload as YouTube unlisted per CONTEXT D-10 deferred path and update install.md to embed the YouTube link instead (then update UI-SPEC §Image format note accordingly).
    7. Save as `docs/assets/smartscreen.gif`.

    **Step C — Enable GitHub Pages:**
    1. Open `https://github.com/boejowen/SquireBot/settings/pages`.
    2. Under `Source`, select `Deploy from a branch`.
    3. Set `Branch: main` and `Folder: /docs`.
    4. Click `Save`.
    5. Wait ~30-90 seconds for the first `pages-build-deployment` Actions workflow to complete (visible at `https://github.com/boejowen/SquireBot/actions`).
    6. Visit `https://boejowen.github.io/SquireBot/` — the Cayman-themed index page should render with the "SquireBot" H1 and the install link.
    7. Click `Install →` and verify install.md renders, screenshots appear, the GIF plays inline (or shows correctly if static), and links work.

    **Step D — Commit assets:**
    Once all assets are in `docs/assets/` and the Pages site renders correctly, commit. (Plan execution will then proceed to Task 3.)
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `screenshots: done, gif: <SIZE>MB, pages: live at <URL>` — all four assets staged + Pages live, ready to proceed
    - `screenshots: done, gif: YouTube fallback at <URL>` — GIF couldn't fit ≤5MB; YouTube unlisted invoked; install.md update needed (planner notes this triggers a UI-SPEC §Image format note update)
    - `blocked: <reason>` — explain what's stuck (no clean Windows box, install fails, etc.) and we will revise the plan
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Run DOC-02 fake-guildie smoke, produce distribution report, user authorizes Phase 5 SHIPPED</name>
  <what-built>
    Phase 5 has produced all the code (05-01..05-04). This task closes the phase by:

    1. Running a manual end-to-end DOC-02 fake-guildie smoke (verifies the 05-04 sidebar + 05-02 archiver chain on synthetic data).
    2. Querying `_char_owner` for heartbeat-freshness and writing `docs/phase5-distribution.md` (the distribution REPORT — not a verdict).
    3. Surfacing the report to the user via this checkpoint so they can authorize Phase 5 SHIPPED.

    **Per the authorized scope reduction recorded in CONTEXT.md §43 (2026-05-11):** the 12-guildie figure is an UPPER ESTIMATE, not a hard ship gate. The user explicitly authorized 'even a handful' as acceptable. THIS TASK therefore produces a REPORT, not a GREEN/YELLOW/RED automated verdict. The user makes the ship call after reading the report.
  </what-built>
  <how-to-verify>
    **Step 0 — Pre-flight asset audit — STRUCK (2026-05-11 scope reduction).** Pages docs are text-only; there are no PNG/GIF assets to audit. Proceed directly to Step A.

    <!-- Historical Step 0 retained below for project record. DO NOT RUN. -->
    Step 0 (historical) — Pre-flight asset audit (BLOCKING — run before Step A; refuses ship if any asset is missing or the GIF exceeds budget):

    Run this automated check (paste into a shell). It MUST exit 0 before proceeding to the smoke + report + ship authorization. If any assertion fails, return to Task 2 and re-stage the assets — do NOT continue to the user-authorization resume signal.

    ```bash
    node -e "const fs=require('fs'); const pngs=['docs/assets/01-installer.png','docs/assets/02-smartscreen-more-info.png','docs/assets/03-smartscreen-run-anyway.png','docs/assets/04-oauth-consent.png','docs/assets/05-folder-picker.png','docs/assets/tray-red.png']; const gif='docs/assets/smartscreen.gif'; let ok=true; for(const f of pngs){if(!fs.existsSync(f)){console.error('MISSING PNG:',f);ok=false;}} if(!fs.existsSync(gif)){console.error('MISSING GIF:',gif);ok=false;} else {const size=fs.statSync(gif).size; if(size>5242880){console.error('GIF OVER 5MB BUDGET:',size,'bytes');ok=false;} else {console.log('gif size OK:',size,'bytes');}} process.exit(ok?0:1);"
    ```

    Acceptance gates (all MUST hold before ship authorization):
    - `fs.existsSync('docs/assets/01-installer.png')` → true
    - `fs.existsSync('docs/assets/02-smartscreen-more-info.png')` → true
    - `fs.existsSync('docs/assets/03-smartscreen-run-anyway.png')` → true
    - `fs.existsSync('docs/assets/04-oauth-consent.png')` → true
    - `fs.existsSync('docs/assets/05-folder-picker.png')` → true
    - `fs.existsSync('docs/assets/tray-red.png')` → true
    - `fs.existsSync('docs/assets/smartscreen.gif')` → true
    - `fs.statSync('docs/assets/smartscreen.gif').size <= 5_242_880` (5 MB budget; if violated, re-export or YouTube-fallback path)

    If the YouTube-fallback path is invoked in Task 2 (resume signal `screenshots: done, gif: YouTube fallback at <URL>`), this audit RELAXES the GIF assertion: `fs.existsSync('docs/assets/smartscreen.gif')` becomes optional, and `install.md` MUST contain the YouTube URL instead (`grep -nF 'youtube.com' docs/install.md` returns >= 1 hit). Otherwise the GIF assertion is mandatory.

    **Step A — DOC-02 fake-guildie smoke (manual via Apps Script editor on the dev workbook `RiverFAIL VanFRAUD`):**

    1. Seed `_char_owner` with a synthetic guildie: append a row with `char_name='SmokeTestChar', owner_email='fake-guildie@example.com', is_removed=FALSE, last_seen=<now ISO>` and any required defaults for columns 3-8 + 10/12/13. Add a second row for the same email: `char_name='SmokeTestAlt', owner_email='fake-guildie@example.com', is_removed=FALSE`.
    2. Create synthetic `inv:SmokeTestChar` (3 rows) and `spell:SmokeTestAlt` (2 rows) tabs so the archive snapshot has data to capture.
    3. From the workbook: `SquireBot → Evict Guildie…`. Verify:
       - The email `fake-guildie@example.com` appears in the dropdown.
       - Selecting it shows the affected-chars preview with `SmokeTestChar` and `SmokeTestAlt`.
       - The grace-expires date is 30 days from today.
    4. Click `Evict`. Confirm the native browser modal. Verify:
       - Success message renders inline.
       - `_char_owner.is_removed=TRUE` for both rows.
       - `_meta.eviction_log` JSON has a new entry with `{at, email:'fake-guildie@example.com', initiated_by:<your email or 'unknown'>, grace_until, chars:['SmokeTestChar','SmokeTestAlt'], reason:'evicted'}`.
    5. **Simulate grace expiry:** in the script editor, manually edit `_meta.eviction_log` (the cell) to change `grace_until` to a past ISO timestamp (e.g. `'1970-01-01T00:00:00.000Z'`). Save.
    6. From the script editor: open `weeklyEvictionArchive` and click `Run` (executes immediately). Verify:
       - `_archive` tab is created (lazy), hidden, with the 7-column header row.
       - Two `_archive` rows are appended (inv + spell for SmokeTestChar; spell-only for SmokeTestAlt has no inv tab — that's fine, the inv row archives with row_count=0 + empty snapshot, OR no inv row is appended if SmokeTestAlt has neither tab → confirm the moveCharToArchive behavior matches what archive.test.ts asserts).
       - `_meta.archive_log` has 2 entries (one per archived char).
       - `_meta.eviction_log` has the processed entry REMOVED (since both chars succeeded).
       - `inv:SmokeTestChar` is HIDDEN (not deleted). `spell:SmokeTestAlt` is HIDDEN.
       - `_status.last_eviction_archive_run` is the current timestamp; `_status.last_eviction_archive_count = '2'`.
    7. **Un-evict path verification (recovery):** in `_char_owner`, manually flip both rows' `is_removed` to FALSE. Manually unhide `inv:SmokeTestChar` and `spell:SmokeTestAlt`. Verify the data is unchanged from before archive (the `_archive` snapshot remains for audit; the live tabs are restored).
    8. **Cleanup:** delete the two synthetic `_char_owner` rows; delete the `inv:SmokeTestChar` and `spell:SmokeTestAlt` tabs; remove the test entry from `_meta.archive_log`; optionally delete the `_archive` tab if it has no real entries.

    **Step B — Produce the distribution report:**

    1. From the script editor, run the diagnostic query from `<interfaces>` (paste it into a temporary function, log the counts, then delete the function). Capture the two numbers:
       - `distinctActiveEmails.size` — total guildies with at least one non-removed char in `_char_owner`.
       - `distinctRecentEmails.size` — guildies whose `last_seen` is within the last 7 days.
    2. **Automated sanity check (the only one):** if `distinctRecentEmails.size <= 1`, treat as a likely deployment failure (only the dev box is reporting). Surface this to the user in the report's TL;DR section as "⚠ Likely smoke regression — only N guildie(s) writing data; recommend investigating before authorizing ship". If `distinctRecentEmails.size > 1`, the report's TL;DR simply states "N of ~12 guildies writing data in the last 7 days. Ship decision is the user's call." There are NO other automated thresholds.
    3. **Write `docs/phase5-distribution.md`** with this structure:

       ```markdown
       # Phase 5 distribution report

       **Generated:** <ISO date>
       **Source:** `_char_owner` table on workbook `RiverFAIL VanFRAUD`

       ## TL;DR

       - Distinct active guildie emails (non-is_removed): **<N>**
       - Distinct emails with `last_seen` in the last 7 days: **<M>**
       - Upper estimate target (per ROADMAP §93 §5): ~12
       - **<one of the two TL;DR lines from Step B.2 — smoke-regression warning OR normal "ship decision is the user's call">**

       ## Per-email heartbeat freshness

       | owner_email | last_seen (most recent across that email's chars) | within 7d? |
       |-------------|---------------------------------------------------|------------|
       | <email-1>   | <ISO>                                             | yes/no     |
       | <email-2>   | <ISO>                                             | yes/no     |
       | ...         | ...                                               | ...        |

       ## Notable gaps (if any)

       <Free-form narrative: list any expected emails that did NOT appear in active rows, or appear with last_seen older than 7 days. Useful context for the user's ship decision.>

       ## Authorized scope-reduction note

       Per CONTEXT.md §43 (recorded 2026-05-11 during plan revision), the literal "12 guildies" figure is an upper estimate, not a hard ship floor. Phase 5 ships when a meaningful core of the guild is writing data. The user authorizes ship after reviewing this report.
       ```

       Populate every `<...>` placeholder with the actual values. The per-email table is sorted by `last_seen` descending (freshest first).

    **Step C — Surface to the user for ship authorization:**

    Present the report contents inline in the resume signal. The user will reply with one of:
    - `ship: phase-5-shipped` — user authorizes ship; Task continues to Step D
    - `ship: hold, reason: <reason>` — user wants to hold ship; document the reason in this plan's SUMMARY as a blocker
    - `ship: smoke-regression-investigate` — only valid when count ≤ 1; user wants to investigate before deciding

    **Step D — On `ship: phase-5-shipped`: update STATE.md and prepare the SUMMARY:**

    1. Update `.planning/STATE.md`:
       - Bump `status: phase-4-shipped` → `status: phase-5-shipped`
       - Update `completed_phases: 4` → `completed_phases: 5`
       - Update `percent: 90` → `percent: 100`
       - Append to Phase 5 row in the progress table with the smoke outcome + distribution counts
       - Add a "Last Session Summary" entry noting Phase 5 shipped, distribution report produced, user authorized ship, v1.0 milestone tagged

    2. Create `.planning/phases/05-search-onboarding-privacy-polish/05-05-SUMMARY.md` (see `<output>` for required sections).
  </how-to-verify>
  <resume-signal>
    After running Steps A+B+C, paste the contents of `docs/phase5-distribution.md` (or a faithful summary of it) and indicate user's ship decision. Reply with one of:
    - `ship: phase-5-shipped, report at docs/phase5-distribution.md, distinct-recent: <M>, distinct-active: <N>` — ship authorized; proceed to Step D
    - `ship: hold, reason: <reason>` — hold ship; SUMMARY will note the blocker
    - `ship: smoke-regression-investigate, distinct-recent: <M>` — only when M ≤ 1; investigate first
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pages site visitor → GitHub Pages CDN | Public; static content; no auth |
| GitHub Pages build → /docs source | Jekyll runs in safe mode; only allowlisted plugins |
| GIF/PNG assets → repo readers | Public-readable; assets may contain incidental UI text/icons but no secrets |
| User in install flow → SquireBot binary | The binary is unsigned; SmartScreen is the trust gate; documented |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Information Disclosure | Screenshots inadvertently capture guildie emails / character names / personal data | mitigate | Pre-publish check: blur or crop any visible PII. Use a synthetic test character (`SmokeTestChar`) on a fresh install where possible. README note in dev.md about screenshot redaction. |
| T-05-05-02 | Tampering | A third party submits a malicious PR replacing the GIF with malware | mitigate | Repo is single-maintainer; PRs from external contributors require manual review. Pages serves only static content — even a malicious GIF cannot execute code in a browser. |
| T-05-05-03 | DoS | GIF >5MB bloats repo clones for every developer | mitigate | Asset budget enforced (≤5MB GIF, ≤200KB PNG). Verification step in Task 2 measures the file size; YouTube fallback if needed. |
| T-05-05-04 | Spoofing | SmartScreen GIF appears to legitimize an unsigned binary, conditioning users to bypass warnings broadly | accept | Documented limitation. The walkthrough is scoped to SquireBot specifically; copy in install.md does not encourage general "More info → Run anyway" outside the SquireBot context. |
| T-05-05-05 | Repudiation | A guildie disputes that they were ever asked to install | accept | Out-of-band; not a code-side concern. Install instructions are public and reproducible via Pages. |
| T-05-05-06 | Information Disclosure | Pages site reveals project structure that aids attackers | accept | Repo is already public; Pages just makes the docs nicer. Trusted-guild model + public OSS posture. |
</threat_model>

<verification>
**Overall plan check after Task 3:**
- The Pages site is live at `https://boejowen.github.io/SquireBot/` with all 4 markdown pages rendering
- 6 PNGs + 1 GIF live in `docs/assets/`; sizes within budget (PNG ≤200KB each, GIF ≤5MB)
- README.md is ≤30 lines (D-12 compliance)
- REQUIREMENTS.md SEARCH-03 row marked Complete with Path 2 note
- DOC-02 smoke documented in 05-05-SUMMARY
- Distribution report at `docs/phase5-distribution.md` exists with the actual heartbeat-freshness counts
- User authorized ship via the Task 3 checkpoint (STATE.md updated OR blocker captured)
- All 5 Phase 5 plans have committed SUMMARY files
- Tag `phase5-complete` pushed (manual; documented as a follow-up if needed)
- v1.0 milestone gate clear pending retrospective
</verification>

<success_criteria>
- DOC-01 closed: install.md + troubleshooting.md cover all 5 onboarding flows + the tray-red recovery; cross-link to eviction-runbook.md
- DOC-03 closed: 6 PNG screenshots + 1 annotated GIF (≤5MB or YouTube fallback) live in `docs/assets/` and embedded in install.md
- Pages live at `boejowen.github.io/SquireBot`; Cayman theme rendering
- README.md shrunk per D-12; CLAUDE.md remains the canonical project doc
- REQUIREMENTS.md traceability table reflects SEARCH-03 Path 2
- DOC-02 end-to-end smoke executed and documented; eviction → grace → archive chain proven on synthetic data
- Distribution report at `docs/phase5-distribution.md` produced with concrete heartbeat-freshness counts
- User-authorized Phase-5-shipped (or documented hold + reason)
- v1.0 milestone gate: STATE.md reflects final phase status
</success_criteria>

<output>
After completion, create `.planning/phases/05-search-onboarding-privacy-polish/05-05-SUMMARY.md` covering:

- **Pages site:** URL + first-build timestamp + theme used (Cayman) + any deviations from UI-SPEC
- **Assets:** filename + size + alt text for each of the 7 assets; flag the GIF as in-budget or YouTube-fallback
- **README shrink:** before/after line count; what content moved where
- **REQUIREMENTS.md update:** the SEARCH-03 Path 2 note as written
- **Smoke results:** the DOC-02 8-step smoke narrative + outcome (PASS/FAIL/PARTIAL); any Session.getEffectiveUser behavior observed (initiated_by populated correctly or fell back to 'unknown')
- **Distribution report:** path to `docs/phase5-distribution.md` + the concrete numbers `distinctActiveEmails` and `distinctRecentEmails (last 7d)` + the date measured + whether the smoke-regression warning was triggered (count ≤ 1)
- **Ship decision:** the user's authorization signal from Task 3 (shipped / hold / smoke-regression-investigate) + any documented holds or follow-ups
- **Deferred backlog at v1.0:** the carried-forward items (bank-coin permission lock, theme picker UI, sidebar inline-JS tests, installer upgrade UX, self-service eviction, guild_admins allowlist) — explicit list for v1.0.x or v2 planning
- **Next:** tag `phase5-complete`; tag `v1.0.0`; trigger a milestone retrospective; close v1 milestone (only when ship was authorized)

If the user held ship, the SUMMARY also includes the documented reason + a proposed follow-up plan outline (likely installer-upgrade-UX or per-guildie-onboarding) for user review before continuing.
</output>
