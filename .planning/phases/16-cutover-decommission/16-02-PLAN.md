---
phase: 16-cutover-decommission
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [CUTOVER-03]
must_haves:
  truths:
    - "A GitHub Release tagged v2.0.0 exists, is NOT a prerelease, and is GitHub's 'Latest' release"
    - "The v2.0.0 Release carries the re-targeted (Google-free, ~7 MB) squirebot.exe asset plus a latest.json"
    - "The published latest.json reports version 2.0.0 and a binary_url pointing at the v2.0.0 squirebot.exe asset"
    - "A still-running v1.0.2 watcher resolves /releases/latest/download/latest.json to the v2.0.0 manifest (IsNewer(\"1.0.2\",\"2.0.0\")==true)"
  artifacts:
    - path: "(GitHub Release v2.0.0 — external artifact, not a repo file)"
      provides: "the auto-update flip trigger: bare squirebot.exe + latest.json published by release.yml on the v2.0.0 tag"
  key_links:
    - from: "git tag v2.0.0"
      to: ".github/workflows/release.yml"
      via: "the on: push tags 'v*' trigger"
      pattern: "release\\.yml"
    - from: "GitHub Release v2.0.0 latest.json"
      to: "running v1.0.2 watchers (internal/update)"
      via: "minio/selfupdate resolving /releases/latest/download/latest.json"
      pattern: "latest\\.json"
---

<objective>
Publish the re-targeted Phase 13 watcher binary as a clean `v2.0.0` GitHub Release — the literal trigger for the entire guild-wide auto-update flip (CUTOVER-03). RESEARCH VERIFIED (2026-05-31, `gh release list`) that the binary is NOT yet released: the latest published Release is `v1.0.2`, which carries the OLD ~16.99 MB Google-Sheets binary and a `latest.json` pinned to `1.0.2`. Until a non-prerelease tag strictly newer than every guildie's running version is pushed, still-running v1.0.2 watchers fetch v1.0.2's manifest, compute `IsNewer("1.0.2","1.0.2")==false`, and NEVER update.

Pushing a clean `v2.0.0` tag fires the already-re-targeted `.github/workflows/release.yml` (on: push tags `v*`), which builds the bare ~7 MB Google-free `squirebot.exe` + a fresh `latest.json` (version 2.0.0) and publishes them as the new "Latest" Release. `/releases/latest/download/latest.json` then resolves to v2.0.0 for every watcher, and `IsNewer("1.0.2","2.0.0")==true` flips them.

Purpose: this is the #1 plan task of the phase per RESEARCH — the flip rides on this artifact existing. Output: a published, non-prerelease `v2.0.0` Release carrying `squirebot.exe` + `latest.json`.

**`autonomous: false`** — cutting a public Release that auto-updates ~12 real users is hard-to-reverse and outward-facing. The maintainer performs the tag push + verifies the published artifacts. Do NOT plan an agent to push a public Release autonomously.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-cutover-decommission/16-CONTEXT.md
@.planning/phases/16-cutover-decommission/16-RESEARCH.md

<interfaces>
<!-- The flip mechanics, verified end-to-end in RESEARCH. -->

The release workflow (`.github/workflows/release.yml`) is on `push` tags `v*` and was already re-targeted for v2.0 (Google secrets stripped in P13; `binary_url`/`binary_sha256` kept in `latest.json`). ⚠️ It auto-marks ANY tag containing `-` as a PRERELEASE, and GitHub's `/releases/latest/` IGNORES prereleases — so a running watcher would never see a `v2.0.0-rcN` tag. Use a CLEAN `v2.0.0` (Pitfall 4).

The watcher auto-update path (`internal/update/`): `minio/selfupdate` resolves `ManifestURL` = the repo's `/releases/latest/download/latest.json`, parses `{version, binary_url, binary_sha256}`, and applies when `IsNewer(running, manifest)==true`. The 999.22 SemVer compare (shipped in 13-04) correctly ranks a final release above any of its own prereleases and is defensive-false on a corrupt manifest. The shipped watcher default `Version = "0.1.0-dev"` is overridden at release time by the pushed tag.

VERIFIED point-in-time state (2026-05-31 — re-confirm at execution): `gh release list` top entry = `v1.0.2`; `gh release download v1.0.2 -p latest.json -O -` shows `"version":"1.0.2"`. No v2.x release/draft/prerelease/tag exists.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-flight — confirm no v2.x release exists and the tree is release-ready</name>
  <read_first>
    - .planning/phases/16-cutover-decommission/16-RESEARCH.md (Runtime State Inventory → "Build artifacts" row; Pitfall 1 + Pitfall 4)
    - .github/workflows/release.yml (the publish-on-tag pipeline — confirm it still triggers on tags v* and builds squirebot.exe + latest.json)
    - cmd/squirebot/build_constants.go (the Version var the tag overrides)
  </read_first>
  <action>
Before cutting the tag, the maintainer confirms the point-in-time facts RESEARCH captured still hold (the GitHub-Release state is a snapshot; re-verify). Run, from the repo root:

```bash
gh release list                                   # confirm the top entry is still v1.0.2 (NO v2.x)
git tag --list "v2*"                              # confirm no v2.x tag exists locally
gh release download v1.0.2 -p latest.json -O -    # confirm the published manifest still says "version":"1.0.2"
git log --oneline -1                              # confirm HEAD is the intended release commit (the re-targeted, Google-free watcher from P13/P14/P15)
```

Also confirm `.github/workflows/release.yml` is present and triggers on `push` tags `v*` (it was re-targeted in P13). Do NOT push the tag in this task — this is the go/no-go gate. If a v2.x release already exists (someone published in the interim), STOP and reassess (do not double-publish).

NOTE: the watcher binary itself was already built + verified in P13/P14 (57% smaller, Google-free, SemVer auto-update twin in place). This plan publishes that already-finished artifact; it does NOT rebuild or modify watcher code. No repo files change in this plan.
  </action>
  <acceptance_criteria>
    - `gh release list` top entry is `v1.0.2` (no v2.x release present) — or, if a v2.x release IS found, the maintainer halts and reassesses
    - `git tag --list "v2*"` returns empty
    - `gh release download v1.0.2 -p latest.json -O -` shows `"version":"1.0.2"`
    - `.github/workflows/release.yml` exists and is confirmed to run on tag push (`v*`)
    - HEAD is the intended release commit (the Google-free watcher)
  </acceptance_criteria>
  <resume-signal>Type "confirmed — no v2.x release, tree is release-ready" to proceed to the tag push, or "halt" with what you found.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Cut and push the clean v2.0.0 tag (fires release.yml)</name>
  <read_first>
    - .planning/phases/16-cutover-decommission/16-RESEARCH.md (Code Examples → "The flip trigger (ops, not code)")
    - .github/workflows/release.yml
  </read_first>
  <action>
The maintainer cuts a CLEAN, non-prerelease `v2.0.0` tag and pushes it, which fires `release.yml` to build + publish the bare `squirebot.exe` + `latest.json`. From the repo root:

```bash
git tag v2.0.0
git push origin v2.0.0
```

⚠️ Use EXACTLY `v2.0.0` — NOT `v2.0.0-rc1` or any `-`-containing tag (release.yml auto-marks those as prereleases, which `/releases/latest/` ignores — Pitfall 4). Any `vX.Y.Z > 1.0.2` works; `2.0.0` matches the v2.0 milestone framing and is strictly newer than every running v1.0.2 watcher (A3).

Then wait for the GitHub Actions `release.yml` run to complete (watch `gh run watch` or the Actions tab). The workflow builds on windows-latest with Go 1.25 + NSIS; allow it to finish before verifying.

This is irreversible-ish: once published as "Latest", running watchers will begin auto-updating within their next check window. That is the intended flip trigger — but it is why this is a human-gated action, not an autonomous step.
  </action>
  <acceptance_criteria>
    - The `v2.0.0` tag exists on the remote (`git ls-remote --tags origin` shows `refs/tags/v2.0.0`)
    - The `release.yml` Actions run for the `v2.0.0` tag completes successfully (green)
  </acceptance_criteria>
  <resume-signal>Type "pushed — release.yml run is green" once the workflow finishes, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the v2.0.0 Release published the right artifacts (the flip is armed)</name>
  <read_first>
    - .planning/phases/16-cutover-decommission/16-RESEARCH.md (Code Examples → the gh release view / download verification commands; Pitfall 4 isPrerelease check)
  </read_first>
  <action>
The maintainer runs the verification commands below and confirms each post-condition. This is the gate that catches a mis-published Release (especially an accidental prerelease, which silently breaks the flip — Pitfall 4) BEFORE any guildie is told to expect the update. Run from the repo root:

```bash
gh release view v2.0.0 --json tagName,isPrerelease,isLatest,assets
gh release download v2.0.0 -p latest.json -O -
gh release list
```

Confirm the acceptance criteria below against that output. If anything is wrong (e.g. `isPrerelease:true` from a stray `-` in the tag), delete the bad tag + Release and re-push a clean `v2.0.0` (back to Task 2). Do NOT proceed to Plan 03 herding until this passes.
  </action>
  <what-built>
A clean `v2.0.0` GitHub Release, published by `release.yml` on the pushed tag, carrying the re-targeted ~7 MB Google-free `squirebot.exe` + a `latest.json` reporting version 2.0.0. This is the auto-update flip trigger (CUTOVER-03): `/releases/latest/download/latest.json` now resolves to v2.0.0, and any running v1.0.2 watcher computes `IsNewer("1.0.2","2.0.0")==true`.
  </what-built>
  <how-to-verify>
The maintainer verifies the published Release is correct and NOT a prerelease (a stray prerelease silently breaks the flip — Pitfall 4). From the repo root:

```bash
gh release view v2.0.0 --json tagName,isPrerelease,isLatest,assets
#   expect: isPrerelease:false, isLatest:true, assets include squirebot.exe AND latest.json

gh release download v2.0.0 -p latest.json -O -
#   expect: "version":"2.0.0" and a binary_url pointing at the v2.0.0/squirebot.exe asset (+ a binary_sha256)

gh release list
#   expect: v2.0.0 is now the "Latest" entry (above v1.0.2)
```

Sanity on the SemVer flip: the running guild watchers are on `1.0.2`; `IsNewer("1.0.2","2.0.0")` is true, so `/releases/latest/download/latest.json` (which resolves to the newest NON-prerelease release = v2.0.0) will drive their update on the next check. Optionally confirm the `squirebot.exe` asset size is materially smaller than v1.0.2's (~7 MB vs ~16.99 MB — the Google-free binary, SC-2 from P13).

Do NOT herd guildies yet — that is Plan 03 (and depends on the char-meta form being deployed too). This plan only ARMS the flip by publishing the artifact.
  </how-to-verify>
  <acceptance_criteria>
    - `gh release view v2.0.0 --json isPrerelease` shows `isPrerelease: false`
    - `gh release view v2.0.0 --json isLatest` shows `isLatest: true` (it is GitHub's "Latest")
    - the v2.0.0 assets include BOTH `squirebot.exe` and `latest.json`
    - `gh release download v2.0.0 -p latest.json -O -` shows `"version":"2.0.0"` with a `binary_url` pointing at the v2.0.0 squirebot.exe
    - `gh release list` shows v2.0.0 as the Latest entry
  </acceptance_criteria>
  <resume-signal>Type "verified — v2.0.0 is published, non-prerelease, Latest, with squirebot.exe + latest.json@2.0.0" or describe the discrepancy (e.g. accidentally marked prerelease → delete the tag/release and re-push a clean v2.0.0).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer dev box → GitHub (tag push) | The `git push origin v2.0.0` triggers CI to build + publish a public artifact ~12 real users auto-download. |
| GitHub Release CDN → guildie watchers | `latest.json` + `squirebot.exe` cross from the public Release to each watcher's auto-updater. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Tampering | the downloaded binary | mitigate (inherited) | The auto-updater verifies the binary against `binary_sha256` in `latest.json` before applying (the shipped `internal/update` flow); the Release is served over HTTPS from GitHub. No new code here — the verification is the P13 transport. |
| T-16-09 | DoS (self-inflicted: a broken flip) | the published Release | mitigate | Pitfall 4: a `-`-containing tag becomes a prerelease that `/releases/latest/` ignores, silently breaking the flip. The plan mandates a clean `v2.0.0` and Task 3 explicitly asserts `isPrerelease:false` + `isLatest:true` so a mis-publish is caught before any guildie is told to expect the update. |
| T-16-10 | Spoofing | release provenance | accept | The Release is published by the repo's own `release.yml` (GitHub-authenticated maintainer push). No third-party publish path. Standard GitHub-Releases trust model, unchanged from v1.x. |

**Net posture:** this plan publishes an already-built, P13/P14-verified, Google-free binary via the existing, proven `release.yml` + `minio/selfupdate` pipeline. It introduces no new code and no new secret. The only real risk is a mis-tagged prerelease silently no-op-ing the flip — explicitly gated by the Task 3 `isPrerelease:false`/`isLatest:true` checks. No HIGH-severity unmitigated threat.
</threat_model>

<verification>
- `gh release view v2.0.0 --json tagName,isPrerelease,isLatest,assets` → `isPrerelease:false`, `isLatest:true`, assets include `squirebot.exe` + `latest.json`.
- `gh release download v2.0.0 -p latest.json -O -` → `"version":"2.0.0"` + a `binary_url` at the v2.0.0 asset.
- `gh release list` → v2.0.0 is the Latest entry.
- No repo files changed (this plan publishes an artifact; `git status` shows no source diff from this plan).
</verification>

<success_criteria>
- A non-prerelease `v2.0.0` Release is published and is GitHub's "Latest", carrying the re-targeted Google-free `squirebot.exe` + a `latest.json` reporting version 2.0.0.
- `/releases/latest/download/latest.json` resolves to the v2.0.0 manifest, so any running v1.0.2 watcher will compute `IsNewer("1.0.2","2.0.0")==true` and auto-update on its next check — the flip is ARMED (herding happens in Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/16-cutover-decommission/16-02-SUMMARY.md`
</output>
