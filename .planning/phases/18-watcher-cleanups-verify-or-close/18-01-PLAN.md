---
phase: 18-watcher-cleanups-verify-or-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/update/manifest_test.go   # ONLY if D-05's pre-release case is found missing/weak (it is currently present — see Task 1)
autonomous: false                       # Task 3 is a HUMAN-UAT reinstall checkpoint (D-07)
requirements: [WATCH-12, WATCH-13, WATCH-14]
user_setup: []

must_haves:
  truths:
    - "cmd/squirebot/console_windows.go passes gofmt -l clean (SC1)"
    - "A no-console (GUI/Explorer) launch logs at Debug, not Warn, and freeConsole()'s doc matches its implementation (SC2)"
    - "A watcher parked on a pre-release (e.g. 0.4.0-rc1) recognizes a final release as newer and updates to it — proven by a green pre-release→final IsNewer test (SC3)"
    - "The maintainer's own watcher is no longer stuck on 0.4.0-rc1 — it is on the current v2.0.0 release and ingesting again, proven by a fresh last_seen on the backend (SC4)"
  artifacts:
    - path: "internal/update/manifest_test.go"
      provides: "Green pre-release→final IsNewer coverage (TestIsNewer_SemVerPreRelease cases rc-updates-to-final / final-does-not-downgrade-to-rc)"
      contains: "rc-updates-to-final"
    - path: "cmd/squirebot/console_windows.go"
      provides: "gofmt-clean source + Debug-level no-console log + reconciled freeConsole doc/impl"
      contains: "slog.Debug"
    - path: "internal/update/manifest.go"
      provides: "SemVer-pre-release-aware IsNewer (pre-release ranks below its final per SemVer §11)"
      contains: "func IsNewer"
  key_links:
    - from: "internal/update/check.go"
      to: "internal/update/manifest.go IsNewer"
      via: "auto-update version gate"
      pattern: "IsNewer\\("
    - from: "internal/backendsrv/store/replace.go"
      to: "character.watcher_version + character.last_seen columns"
      via: "UPDATE character SET last_seen = ?, watcher_version = ? (the survey + post-reinstall proof source)"
      pattern: "watcher_version"
---

<objective>
Verify-or-close the three carried-forward watcher polish items (WATCH-12/13/14, recorded RESOLVED in v2.0 Plan 13-04) against the live tree, close the one possible hidden gap (D-05's pre-release→final IsNewer test), and remediate the maintainer's own watcher stuck on the broken pre-release `0.4.0-rc1` via a one-time manual reinstall of the current v2.0.0 release.

Purpose: This is the milestone's final close-out. The three code fixes are already shipped — this phase CONFIRMS them (it does NOT re-implement them) and resolves the only real residual: the maintainer's stuck pre-release watcher that the in-place SemVer fix cannot self-heal (a `0.4.0-rc1` watcher runs `0.4.0-rc1`'s own broken pre-SemVer comparison logic — the broken client is the thing that's broken, so a fix shipped in a later release can never reach it). Per D-01, in-place self-heal is explicitly NOT relied upon.

Output: Confirmation evidence for SC1–SC3 (grep/test), at most a one-line test addition if D-05 surfaces a gap (it is not expected to — the case is already present), and a HUMAN-UAT-verified reinstall closing SC4 with a fresh backend `last_seen` as objective proof.

Deciding rule (from 18-CONTEXT.md, applies throughout): "simplest guildie experience + don't rebuild already-shipped work in a close-out phase." HARD CONSTRAINT (milestone-locked): never add Discord OAuth to the watcher; the credential stays a static bearer token; no watcher onboarding change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-watcher-cleanups-verify-or-close/18-CONTEXT.md

# The code being verified (read in the tasks that touch it — see each <read_first>):
@cmd/squirebot/console_windows.go
@internal/update/manifest.go
@internal/update/manifest_test.go

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase during planning. -->
<!-- Use these directly — no further codebase exploration is needed for the verify tasks. -->

From internal/update/manifest.go — the function under WATCH-14 verification:
```go
// IsNewer reports whether the manifest version is strictly newer than current
// under SemVer-with-pre-release ordering. A version WITH a pre-release tail is
// LOWER than the same version WITHOUT one (SemVer §11): a final v2.0.0 IS newer
// than its own "2.0.0-rc1"; a final must NOT downgrade to a manifest rc.
// Returns false defensively on ANY parse failure of either input.
func IsNewer(current, manifest string) bool
```

From internal/update/manifest_test.go — the D-05 coverage that MUST be confirmed present + green
(table-driven, inside TestIsNewer_SemVerPreRelease):
```go
{"rc-updates-to-final", "2.0.0-rc1", "2.0.0", true},          // a parked rc recognizes its final as newer
{"final-does-not-downgrade-to-rc", "2.0.0", "2.0.0-rc1", false},
```
NOTE: D-05 names the `0.4.0-rc1` example specifically, but IsNewer's logic is core-agnostic — the
`2.0.0-rc1`↔`2.0.0` pair proves the identical "pre-release ranks below its final" behaviour that
stranded the `0.4.0-rc1` watcher. This coverage is ALREADY PRESENT (confirmed during planning). Task 1
confirms it green; it only ADDS a case if the assertion is somehow absent/weakened.

From internal/backendsrv/migrations/00001_init.sql — the D-02 survey source (queryable, no new schema):
```sql
CREATE TABLE character (
  id INTEGER PRIMARY KEY, owner_id INTEGER NOT NULL REFERENCES owner(id),
  name TEXT NOT NULL UNIQUE COLLATE NOCASE, ...,
  last_seen TEXT, watcher_version TEXT, ...
);
```
From internal/backendsrv/store/replace.go (lines 116, 176) — proves watcher_version IS persisted per ingest:
```go
tx.ExecContext(ctx, `UPDATE character SET last_seen = ?, watcher_version = ? WHERE id = ?`, ...)
```
From internal/backendsrv/migrations/00005_self_service_linking.sql (Phase 17) — the per-token alive signal:
```sql
ALTER TABLE guild_code ADD COLUMN last_seen TEXT;   -- stamped on each authenticated ingest
```
</interfaces>

# D-02 RESEARCH FLAG — RESOLVED DURING PLANNING:
# watcher_version IS persisted in a queryable column (character.watcher_version, written by
# internal/backendsrv/store/replace.go on every ingest). Therefore the D-02 survey branch is VIABLE:
# Task 2 runs ONE read-only DB query, no new schema/code. (If it had NOT been queryable, the survey
# would have been SKIPPED and only the maintainer remediated — but it IS queryable, so the survey runs.)

# Reinstall mechanics (Task 3): docs/build-and-install.md documents the installer + the Phase 6
# overwrite-running shim (squirebot.exe --quit; NSIS pre-install shim, INST-06). NOTE: that doc's
# step-by-step is the Phase 1 (v0.x, Google-OAuth wizard) runbook — the CURRENT watcher is the v2.0.0
# "Off Google" bearer-token build, so the reinstall target is the v2.0.0 release and there is NO Google
# OAuth wizard. The overwrite-running shim is the still-current painless-reinstall mechanism.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm WATCH-12 / WATCH-13 / WATCH-14-code live state (gofmt, freeConsole, SemVer IsNewer incl. D-05 pre-release→final)</name>
  <files>internal/update/manifest_test.go</files>
  <read_first>
    - cmd/squirebot/console_windows.go (WATCH-12 gofmt subject; WATCH-13 freeConsole doc at lines 22–49 + impl at line 50 + the slog.Debug no-console log at line 57)
    - internal/update/manifest.go (WATCH-14 IsNewer SemVer-pre-release logic, lines 143–185)
    - internal/update/manifest_test.go (the D-05 coverage check — TestIsNewer_SemVerPreRelease at lines 179–209)
  </read_first>
  <action>
    Confirm all three already-shipped fixes are live-correct (D-04: confirm, do NOT re-implement). Run, in order:

    1. WATCH-12 (gofmt): `gofmt -l cmd/squirebot/console_windows.go` — MUST produce NO output (empty = clean). If it lists the file, run `gofmt -w cmd/squirebot/console_windows.go` to fix the residual (this is the only fix WATCH-12 ever needs).

    2. WATCH-13 (freeConsole Debug-not-Warn + doc/impl reconciled): confirm the no-console log line is at Debug level, not Warn:
       `grep -n "slog.Debug" cmd/squirebot/console_windows.go` — MUST match the `FreeConsole returned 0` line (~line 57). Also confirm NO `slog.Warn` exists in the file (`grep -c "slog.Warn" cmd/squirebot/console_windows.go` filtered for non-comment lines MUST be 0). The doc comment (lines 35–49) and the impl (lines 50–61) both state ret==0 is the benign no-console case logged at Debug — confirm they agree (read both, no command). No change expected.

    3. WATCH-14 / D-05 (the one load-bearing verify): confirm `go test ./internal/update/...` passes AND that the pre-release→final coverage is genuinely present. The required assertions (per D-05, generalized — IsNewer is core-agnostic so 2.0.0-rc1↔2.0.0 proves the identical behaviour that stranded 0.4.0-rc1):
         - a pre-release recognizes its final as newer  → IsNewer("…-rc1","…") == true
         - a final does NOT downgrade to an rc          → IsNewer("…","…-rc1") == false
       Confirm via: `grep -n "rc-updates-to-final" internal/update/manifest_test.go` (MUST match) and `grep -n "final-does-not-downgrade-to-rc" internal/update/manifest_test.go` (MUST match). These cases are ALREADY PRESENT (confirmed during planning) — this is a confirm, not an add.

       ONLY IF either grep finds nothing OR the matching case is weakened (e.g. the `false` direction is missing): ADD the minimal missing case(s) to the `TestIsNewer_SemVerPreRelease` table in internal/update/manifest_test.go — exactly:
         `{"rc-updates-to-final", "2.0.0-rc1", "2.0.0", true},`
         `{"final-does-not-downgrade-to-rc", "2.0.0", "2.0.0-rc1", false},`
       Do not add anything beyond closing the surfaced gap (D-06: existing coverage is the guard; do not gold-plate). Then re-run `go test ./internal/update/...` to confirm green.

    Record the gofmt output (empty), the grep hits, and the go test PASS in the SUMMARY as the SC1–SC3 confirmation evidence.
  </action>
  <verify>
    <automated>gofmt -l cmd/squirebot/console_windows.go (no output) AND grep -q "slog.Debug" cmd/squirebot/console_windows.go AND grep -q "rc-updates-to-final" internal/update/manifest_test.go AND grep -q "final-does-not-downgrade-to-rc" internal/update/manifest_test.go AND go test ./internal/update/... passes</automated>
  </verify>
  <done>
    `gofmt -l cmd/squirebot/console_windows.go` is empty (SC1); the no-console log is at slog.Debug with no slog.Warn in the file and the freeConsole doc matches its impl (SC2); `go test ./internal/update/...` passes and the test file contains both the pre-release→final true case (`rc-updates-to-final`) and the no-downgrade false case (`final-does-not-downgrade-to-rc`) (SC3). At most a one-or-two-line test addition was needed (not expected).
  </done>
</task>

<task type="auto">
  <name>Task 2: D-02 stale-version survey — read-only query for any OTHER guildie on a pre-release/stale watcher_version</name>
  <files></files>
  <read_first>
    - internal/backendsrv/ingest/handler.go (lines 119, 234–243 — minWatcherVersion = "2.0.0" floor + watcher_version travelling in the envelope and stored via store.Replace*Tx)
    - internal/backendsrv/ingest/envelope.go (line 30 — WatcherVersion field accepted in the envelope)
    - internal/backendsrv/store/replace.go (lines 116, 176 — UPDATE character SET last_seen, watcher_version: proves it is persisted per-character)
    - internal/backendsrv/migrations/00001_init.sql (lines 8–22 — character table: watcher_version TEXT + last_seen TEXT columns)
    - internal/backendsrv/migrations/00005_self_service_linking.sql (line 23 — guild_code.last_seen, the per-token alive signal)
    - v2-backend-live-and-ops-access.md memory (SSH access to the Hetzner VPS 5.78.232.85 + on-box SQLite DB location) — for HOW to reach the live DB read-only
  </read_first>
  <action>
    D-02 branch RESOLVED at planning time: watcher_version IS queryable (character.watcher_version column, written by replace.go) — so the survey RUNS (it would only have been skipped had it required new schema/code; it does not).

    Run ONE read-only query against the live backend SQLite on the Hetzner VPS (SSH per the v2-backend-live-and-ops-access memory; pipe a here-string to `ssh bash -s` to run sqlite3 read-only — do NOT open an interactive session, do NOT write). The query lists every character whose watcher is on a pre-release (watcher_version contains '-', e.g. an '-rc' tail) OR below the v2.0.0 floor, alongside its owner and last activity:

      SELECT c.name, c.watcher_version, c.last_seen, o.label, o.discord_user_id
      FROM character c JOIN owner o ON o.id = c.owner_id
      WHERE c.is_removed = 0
        AND c.watcher_version IS NOT NULL
        AND (c.watcher_version LIKE '%-%' OR c.watcher_version < '2.0.0')
      ORDER BY c.last_seen DESC;

    (The `< '2.0.0'` lexical compare is a coarse stale-finder for the survey only — it is NOT the auth gate; the real gate is ingest.IsOlder at handler.go:119. The `LIKE '%-%'` catches any pre-release tail including the maintainer's `0.4.0-rc1`.)

    Treat the result as READ-ONLY intelligence. Record the full result set in the SUMMARY. Classify:
      - The maintainer's own stuck `0.4.0-rc1` watcher → already known; folds into Task 3.
      - Any OTHER guildie that appears → add their character/owner to Task 3's reinstall-nudge list (the HUMAN-UAT in Task 3 covers them too).
      - Empty result (other than the maintainer) → state explicitly in the SUMMARY: "no other guildie is silently stuck."

    Do NOT build any telemetry plumbing, dashboard, or schema change (D-02 / deferred note): if a richer version-monitoring surface ever seems warranted, note it as a FUTURE idea in the SUMMARY — do not build it here. This task writes nothing to the DB and changes no code.
  </action>
  <verify>
    <automated>MISSING — manual read-only DB query against the live VPS; verification is the SUMMARY recording the query text + its result set (zero rows beyond the maintainer = "no other guildie stuck"). No code/test artifact is produced by this survey task.</automated>
  </verify>
  <done>
    The read-only survey query has been run against the live backend; its result set is recorded in the SUMMARY; every character on a pre-release/stale watcher_version is either the maintainer (→ Task 3) or explicitly folded into Task 3's reinstall list; and the SUMMARY states whether any OTHER guildie is stuck. No DB writes, no schema/code changes, no telemetry built.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN-UAT — reinstall the maintainer's (and any surveyed-stuck) watcher to v2.0.0 and prove it is uploading again</name>
  <read_first>
    - docs/build-and-install.md (the installer + "Uninstalling"/reinstall flow + the `squirebot.exe --quit` overwrite-running shim; NOTE the step-by-step there is the Phase 1 v0.x Google-OAuth runbook — the current watcher is the v2.0.0 bearer-token "Off Google" build, so there is NO Google wizard and the reinstall target is the v2.0.0 release)
    - Task 2's recorded survey result (the list of stuck characters/owners to remediate)
  </read_first>
  <what-built>
    Tasks 1–2 confirmed the WATCH-12/13/14 code fixes are live and surveyed the backend for stuck watchers. This checkpoint closes WATCH-14's ops residual (SC4): the maintainer's own watcher is stuck on the broken pre-release `0.4.0-rc1` and cannot self-heal (D-01 — its own pre-SemVer comparison logic is the bug), so it needs a one-time manual reinstall of the current v2.0.0 release. The overwrite-running NSIS shim (Phase 6 / INST-06, `squirebot.exe --quit`) makes reinstall-over-a-running-watcher painless.
  </what-built>
  <how-to-verify>
    For the maintainer's watcher (and EACH additional stuck watcher Task 2 surfaced, if any):

    1. Download the current release installer from GitHub Releases: `SquireBot-Setup-2.0.0.exe` (tag `v2.0`/`v2.0.0`) for `github.com/boejowen/SquireBot`. (Or rebuild it locally per docs/build-and-install.md "Building the installer" with `-DAPPVERSION=2.0.0` if a fresh artifact is preferred.)
    2. Run the installer. The Phase 6 overwrite-running shim signals the running `0.4.0-rc1` watcher to quit (`squirebot.exe --quit`) and installs over it — no manual Task-Manager kill needed. Choose "No" to the credential-wipe prompt if asked (the bearer token / config should be preserved so the watcher resumes immediately with no re-link).
    3. Confirm the watcher is now on v2.0.0: the tray "About"/version readout shows `2.0.0`, OR check the installed `DisplayVersion` registry value (`Get-ItemProperty 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Uninstall\SquireBot'` → DisplayVersion = 2.0.0).
    4. Confirm it is uploading again: drop a fresh `/outputfile inventory` (or let the next scheduled write fire), then verify the backend shows a FRESH `last_seen` for that character — objective proof per D-08. Run the read-only check on the VPS (SSH per the v2-backend-live-and-ops-access memory):
         SELECT c.name, c.watcher_version, c.last_seen
         FROM character c WHERE c.name = '<the reinstalled character>';
       SC4 PASSES when `watcher_version` is now `2.0.0` (no `-rc` tail) AND `last_seen` is within the last few minutes (post-reinstall). The companion `guild_code.last_seen` (Phase 17) for that owner's token should likewise be fresh.

    Report PASS only when every stuck watcher from Task 2 (at minimum the maintainer's own) shows watcher_version=2.0.0 + a fresh last_seen. If any watcher still shows a pre-release version or a stale last_seen, report the specific failure.
  </how-to-verify>
  <resume-signal>Type "approved" once each stuck watcher is on v2.0.0 with a fresh backend last_seen, or describe which watcher failed and how.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live backend SQLite (Task 2 survey, Task 3 proof query) | A maintainer SSH session reads existing rows on the VPS. Read-only; no untrusted input crosses into the DB. |
| installer distribution → maintainer PC (Task 3 reinstall) | The existing signed/walked-through v2.0.0 NSIS installer from GitHub Releases is re-run over a running watcher via the existing overwrite shim. No new distribution channel. |
| watcher → ingest API (post-reinstall upload, already-shipped path) | Unchanged from v2.0 — static bearer token over TLS; the version floor gate (ingest.IsOlder vs minWatcherVersion="2.0.0") already guards it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | Task 2/3 read-only survey + proof queries on the live SQLite | mitigate | Queries are strictly SELECT, run via a non-interactive `ssh bash -s` here-string (no interactive shell, no write statements). No token material is read (guild_code stores only token_hash; the survey selects name/version/last_seen/label/discord_user_id, never the plaintext bearer). Results recorded in SUMMARY contain no secrets. |
| T-18-02 | Tampering | Task 3 reinstall binary | mitigate | The reinstall uses the official v2.0.0 GitHub Release artifact (or a local rebuild per docs/build-and-install.md); the auto-updater's mandatory SHA-256 verify (check.go/swap.go) is the standing guard for the bare-binary path. No new/unsigned distribution channel is introduced. |
| T-18-03 | Elevation of Privilege | Watcher auth model | accept | HARD CONSTRAINT honored — NO Discord OAuth added to the watcher; the credential stays a static bearer token. The reinstall preserves the existing token (no re-link). This phase introduces zero new auth surface. |
| T-18-04 | Denial of Service | Overwrite-running reinstall (`--quit` shim) | accept | The shim signals the running watcher to quit then installs over it; a brief (~seconds) ingest gap during reinstall is benign — ingest is full-snapshot-replace, so the next upload fully reconciles. Single-operator, one-time action. |
| T-18-05 | Spoofing | Forged/garbage update manifest reaching IsNewer (WATCH-14 path being verified) | mitigate | Already-shipped + verified in Task 1: IsNewer fails-CLOSED on any unparseable version (TestIsNewer_DefensiveCorruptVersion), and swap.go's SHA-256 verify is the second gate. No new code; this phase confirms the existing mitigation holds. |
</threat_model>

<verification>
Phase-level checks (map 1:1 to the 4 ROADMAP success criteria):

- **SC1 (WATCH-12):** `gofmt -l cmd/squirebot/console_windows.go` produces no output. — Task 1
- **SC2 (WATCH-13):** `cmd/squirebot/console_windows.go` logs the no-console case at `slog.Debug` (line ~57), has no `slog.Warn`, and freeConsole()'s doc matches its impl. — Task 1
- **SC3 (WATCH-14 code):** `go test ./internal/update/...` passes; `internal/update/manifest_test.go` contains both `rc-updates-to-final` (true) and `final-does-not-downgrade-to-rc` (false) cases. — Task 1
- **SC4 (WATCH-14 ops):** Every stuck watcher Task 2 identified (at minimum the maintainer's own `0.4.0-rc1`) is on v2.0.0 and uploading again, proven by a fresh `character.last_seen` + non-pre-release `watcher_version` on the backend. — Tasks 2 + 3

Survey integrity: Task 2's read-only query ran against the live backend with no DB writes and no telemetry/schema/code added.
</verification>

<success_criteria>
Phase 18 is DONE when:
- gofmt clean confirmed on console_windows.go (SC1);
- no-console launch logs at Debug + freeConsole doc/impl reconciled (SC2);
- pre-release→final IsNewer test is green (SC3), with at most a one/two-line test addition if a gap surfaced (none expected);
- the D-02 survey has run read-only and its result is recorded (no other guildie stuck, or each folded into the reinstall);
- the maintainer's watcher (and any other surveyed-stuck watcher) is on v2.0.0 and ingesting again, proven by a fresh backend last_seen (SC4) and confirmed by the HUMAN-UAT approval;
- zero new watcher code, zero schema changes, zero telemetry built, and the no-Discord-OAuth-in-the-watcher constraint upheld.
</success_criteria>

<output>
After completion, create `.planning/phases/18-watcher-cleanups-verify-or-close/18-01-SUMMARY.md`.
Record: the gofmt output, the WATCH-13 grep hits, the `go test ./internal/update/...` result, the full D-02 survey result set (and whether any other guildie was stuck), and the post-reinstall backend last_seen evidence (SC4). Note explicitly whether any code change was needed (expected: none, or at most a one/two-line test addition).
</output>
