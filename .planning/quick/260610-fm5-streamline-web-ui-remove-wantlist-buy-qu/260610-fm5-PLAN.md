---
phase: quick-260610-fm5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # WS1 — reason removal (web)
  - web/src/lib/components/WantAddForm.svelte
  - web/src/lib/columns.ts
  - web/src/lib/components/cells/ReasonCell.svelte   # DELETED
  - web/src/lib/api.ts
  - web/src/lib/wantlist/groupByChar.test.ts
  - web/src/lib/components/WantlistPanel.svelte
  # WS1 — reason removal (Go + migration + tests)
  - internal/backendsrv/webadmin/wantlist.go
  - internal/backendsrv/store/wantlist.go
  - internal/backendsrv/wantmatch/match.go
  - internal/backendsrv/ec/embed.go
  - internal/backendsrv/migrations/00011_wantlist_drop_reason_dedup.sql   # NEW
  - internal/backendsrv/webadmin/wantlist_test.go
  - internal/backendsrv/store/wantlist_test.go
  - internal/backendsrv/wantmatch/match_test.go
  - internal/backendsrv/ec/embed_test.go
  - internal/backendsrv/ec/ec_test.go
  - internal/backendsrv/migrations/migrate_test.go
  - internal/backendsrv/migrations/embed.go   # ONLY if the test-only UpTo helper option is chosen
  # WS2 — extraneous-info strip
  - web/src/lib/components/SearchResults.svelte
  - web/src/lib/components/cells/WantItemCell.svelte
  - web/src/lib/components/NotificationRow.svelte
  - web/src/lib/components/DataGrid.svelte
  - web/src/lib/tooltip/composeNotes.ts
  - web/src/lib/__tests__/composeNotes.test.ts
  # WS3 — placement consistency
  - web/src/lib/components/SiteShell.svelte
  - web/src/routes/+page.svelte
  - web/src/lib/components/NotificationPrefsPanel.svelte
  - web/src/routes/char-meta/+page.svelte
  - web/src/routes/bank-coin/+page.svelte
  - web/src/routes/admin/+page.svelte
  - web/src/routes/my-characters/+page.svelte
  - web/src/lib/components/SettingsMenu.svelte
  - web/src/routes/wantlist/+page.svelte
autonomous: true
requirements: [QUICK-260610-fm5]
commit_docs: false

must_haves:
  truths:
    - "Adding a wantlist item never asks for a buy/quest reason; submit works with item + optional priority/note/character"
    - "Re-adding the same item in the same character scope returns the friendly duplicate error — buy/quest no longer creates a second row"
    - "No member-facing surface renders a raw item ID (view/bank grids, wantlist grids, search results, want add form)"
    - "Notification rows show friendly source labels (EC auction alert / WTS alert / Raid target alert / SquireBot alert) and NOT SENT instead of ERROR"
    - "Nav shows Inventory → Wantlist → Notifications; home scope filter is a My characters/Guild segmented toggle; per-column filters live behind a Filters disclosure"
    - "All verification gates green: web check 0/0, web test, web build, go build/vet/test"
  artifacts:
    - path: "internal/backendsrv/migrations/00011_wantlist_drop_reason_dedup.sql"
      provides: "Dedup-then-reindex migration dropping reason from the wantlist unique key"
      contains: "COALESCE(character_id, -1)"
    - path: "internal/backendsrv/migrations/migrate_test.go"
      provides: "TestMigrate_00011 — index-SQL assert + cross-reason collision + dedupe-data proof"
      contains: "TestMigrate_00011"
    - path: "web/src/lib/components/WantAddForm.svelte"
      provides: "Reason-free add form (no Reason FormField, no reason in addWant body)"
  key_links:
    - from: "internal/backendsrv/store/wantlist.go"
      to: "wantlist_item.reason column (NOT NULL CHECK, retained)"
      via: "AddWantTx INSERT writes the literal 'buy'"
      pattern: "VALUES \\(\\?, \\?, \\?, 'buy'"
    - from: "internal/backendsrv/ec/embed.go"
      to: "wantmatch.Hit.Note"
      via: "whyWanted returns trimmed note or the 'on your wantlist' fallback (never-empty contract)"
      pattern: "on your wantlist"
    - from: "web/src/routes/bank-coin/+page.svelte"
      to: "web/src/routes/+page.svelte"
      via: "'Back to bank' link → /?view=bank; home reads ?view= on mount to seed the active tab"
      pattern: "view=bank"
---

<objective>
Streamline the web UI per the LOCKED decisions in 260610-fm5-CONTEXT.md (a finished 4-agent
audit — do NOT re-audit, do NOT revisit decisions). Three workstreams, one atomic commit each:

1. **WS1** — remove the wantlist buy/quest "Reason" field end-to-end: web UI + Go API/store/
   wantmatch/EC-embed + NEW migration 00011 (dedup data, recreate unique indexes without reason)
   + every affected test.
2. **WS2** — strip extraneous technical info from member-facing UI: six item-ID surfaces, raw
   enum leaks (notification source/delivery), jargon in tooltips and facet labels.
3. **WS3** — clear-win placement consistency: Inventory nav link, home scope-filter restyle,
   Filters disclosure on DataGrid, cross-links, purpose lines, /bank-coin back-link + ?view=
   param, wantlist full-width grids, message-adjacent announce placement.

Purpose: "Make the UI as simple and streamlined as possible; no extraneous info; similar
features in similar places; remove the want/buy designation."

Output: code-complete + all local gates green. Deploy is the ORCHESTRATOR's job — no deploy
steps here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

IMPORTANT — commit_docs is FALSE for this project:
- Commit CODE changes only. Do NOT git-add or commit anything under .planning/ (PLAN/SUMMARY/
  STATE are handled by the orchestrator, not the executor).
- Do NOT deploy. The verify gates are local-only (web check/test/build + go build/vet/test).
- Work directly on master (branching_strategy none). One atomic commit per task,
  conventional-commit style scoped (260610-fm5).
</execution_context>

<context>
@.planning/quick/260610-fm5-streamline-web-ui-remove-wantlist-buy-qu/260610-fm5-CONTEXT.md
@.planning/STATE.md
@./CLAUDE.md

**THE CONTEXT.md `<decisions>` SECTION IS THE AUTHORITATIVE CHANGE MAP.** Every file, field,
and test change is enumerated there (WS1 items 1-16, WS2 items 1-8, WS3 items 1-9). Do not
re-derive the impact set. Line numbers in the map were verified by the audit but files may
have drifted — **match on CONTENT (the quoted identifiers/strings), never on blind line
numbers.** If a mapped item's content cannot be found, grep for the identifier before assuming
it's gone; if genuinely absent, note it in the SUMMARY rather than inventing a change.

**Hard boundaries (from CONTEXT.md):**
- Files under "Verified NOT impacted" and items under "Explicitly KEEP / DEFERRED" — do NOT
  touch (exception: eccursor vestigial-comment cleanup ONLY if trivial).
- Migrations 00001-00010 are SHIPPED — never edited. 00011 is forward-only.
- NO `_meta.schema_version` bump, NO `WatcherMaxSchemaVersion` change (wantlist_item is
  backend-only; the watcher never touches it — 00008/00010 precedent).
- Svelte 5 runes; plain `{}` interpolation only (XSS boundary — the only raw-HTML sink stays
  ItemTooltip). Server-truth pattern: never optimistic-mutate. Go: structured slog, typed
  sentinel errors, withTx(BEGIN IMMEDIATE) — unchanged.
- Claude's discretion (CONTEXT.md): exact wording of purpose lines/cross-links, disclosure
  button styling, minor comment cleanups — follow EQ-theme tokens (--accent, --panel,
  --font-display, 44px min touch targets) and surrounding comment density/idiom.
</context>

<tasks>

<task type="auto">
  <name>Task 1: WS1 — remove wantlist buy/quest reason end-to-end (web + Go + migration 00011 + tests)</name>
  <files>web/src/lib/components/WantAddForm.svelte, web/src/lib/columns.ts, web/src/lib/components/cells/ReasonCell.svelte, web/src/lib/api.ts, web/src/lib/wantlist/groupByChar.test.ts, web/src/lib/components/WantlistPanel.svelte, internal/backendsrv/webadmin/wantlist.go, internal/backendsrv/store/wantlist.go, internal/backendsrv/wantmatch/match.go, internal/backendsrv/ec/embed.go, internal/backendsrv/migrations/00011_wantlist_drop_reason_dedup.sql, internal/backendsrv/webadmin/wantlist_test.go, internal/backendsrv/store/wantlist_test.go, internal/backendsrv/wantmatch/match_test.go, internal/backendsrv/ec/embed_test.go, internal/backendsrv/ec/ec_test.go, internal/backendsrv/migrations/migrate_test.go</files>
  <action>
    Implement CONTEXT.md "WS1 — Reason removal: exact file-by-file change map" items 1-16 in
    full. Work order: migration → Go prod code → Go tests → web → web test fixture.

    **Migration (item: NEW file 00011_wantlist_drop_reason_dedup.sql):** use the EXACT SQL
    given in CONTEXT.md verbatim (goose style mirrors 00006-00010: plain statements,
    `-- +goose Up` / `-- +goose Down` with `SELECT 1;` no-op Down). Load-bearing pins:
    - Dedupe UPDATEs (soft-delete active=0, keep MIN(id)) run BEFORE the index recreation.
    - **The recreated indexes and the dedupe GROUP BYs MUST keep `COALESCE(character_id,-1)`**
      — dropping it deactivates legitimate per-character wants, breaks CWANT-01..06, and fails
      TestMigrate_00010 steps 3/4/7.
    - The `reason` column STAYS (SQLite can't drop a CHECK-referenced column).

    **Go store (CONTEXT items 8-9):** AddWantTx drops the `reason` param BUT the INSERT
    **MUST still supply the literal `'buy'`** for the retained NOT-NULL-CHECK `reason` column:
    `VALUES (?, ?, ?, 'buy', ?, ?, ?, ?)` — **forgetting this = instant 500 on every add.**
    ErrDuplicateWant detection (SQLite extended code 2067) mechanically unchanged. Drop
    `Reason` from WantlistRow/GuildWantRow/Hit structs + every SELECT/Scan listed in the map
    (keep scanHits order: id, discord_user_id, item_id, item_name, note, character_name).

    **Go API (item 7):** addWantReq drops the Reason json field (Decode WITHOUT
    DisallowUnknownFields silently ignores stale clients' "reason" key — no compat shim
    needed). Delete validReasons + its validWant check; keep validPriorities. Update the doc
    comments the map lists.

    **EC embed (item 10):** whyWanted becomes Note-only with the never-empty contract intact:
    trimmed note if present, else `"on your wantlist"` — the 'Why you wanted it' embed field
    stays always-present.

    **Go tests (items 11-16):** apply every listed edit. Highlights: wantlist_test.go drops
    `"reason":"…"` from all postJSON bodies, deletes the bad-reason 400 case, REWRITES
    TestAddWant_Duplicate_409_OtherReason_200 → TestAddWant_Duplicate_409 (same-item re-add
    now 409); store_wantlist_test inverts the 'SAME item, DIFFERENT reason → inserts fine'
    block (second add now returns ErrDuplicateWant; ListOwnWants 2 → 1); raw-SQL seed helpers
    KEEP their `'buy'` literal (column persists). ec_test.go why-wanted expectation
    `"buy — " + note` → just `note`. migrate_test.go: TestMigrate_00006/00010 pass UNCHANGED
    (the COALESCE pin guarantees it — if either fails, the migration is wrong, not the test);
    ADD TestMigrate_00011 with the three sub-asserts from the map — (a) neither recreated
    index's sqlite_master SQL contains 'reason', (b) buy+quest insert pair for same
    (user,item,NULL char) collides on the second insert, (c) dedupe-data proof via UpTo v10 →
    seed cross-reason dups → UpTo v11 → only MIN(id) stays active=1. NewTestDB always migrates
    to HEAD, so (c) requires calling goose directly (goose.SetBaseFS/SetDialect + UpTo) or a
    small test-only UpTo helper in migrations/embed.go — follow whichever migrate_test.go's
    existing style supports best.

    **Web (items 1-6):** WantAddForm loses the reason state/canSubmit term/reset/body
    field/FormField block, duplicate-error copy → "That's already on your wantlist." (drop the
    buy/quest parenthetical); columns.ts drops the ReasonCell import + both reason column
    entries (fix the trailing comma on guildWantlistColumns); DELETE ReasonCell.svelte (sole
    importer verified); api.ts drops `reason` from WantlistRow/GuildWantRow/addWant body type;
    groupByChar.test.ts fixture drops `reason: 'buy',`; WantlistPanel comment-only.

    Do NOT touch the "Verified NOT impacted" list (dm_test, eccursor, bot/scheduler slog
    'reason' fields, the local error-copy `reason` helpers in the admin form components, etc.).

    Commit (code only): `feat(260610-fm5): remove wantlist buy/quest reason end-to-end (web + API + migration 00011)`
  </action>
  <verify>
    <automated>go build ./... && go vet ./internal/backendsrv/... ./cmd/squirebot-server/... && go test ./internal/backendsrv/... && cd web && npm run check && npm test -- --run</automated>
  </verify>
  <done>Reason is gone from the add form, grids, API types, handler, store, wantmatch, and EC embed; 00011 exists with the dedupe-then-reindex SQL (COALESCE pin held, 'buy' literal in INSERT); TestMigrate_00011 added; TestMigrate_00006/00010 pass unchanged; all Go + web gates green; one atomic commit on master, no .planning/ files staged.</done>
</task>

<task type="auto">
  <name>Task 2: WS2 — strip item IDs, raw enums, and jargon from member-facing UI</name>
  <files>web/src/lib/columns.ts, web/src/lib/components/SearchResults.svelte, web/src/lib/components/cells/WantItemCell.svelte, web/src/lib/components/WantAddForm.svelte, web/src/lib/components/NotificationRow.svelte, web/src/lib/components/DataGrid.svelte, web/src/lib/tooltip/composeNotes.ts, web/src/lib/__tests__/composeNotes.test.ts</files>
  <action>
    Implement CONTEXT.md "WS2 — Extraneous-info strip" items 1-8 in full:

    1-2. columns.ts: remove the viewColumns `id` column (bankColumns shares it) and the
       wantlistColumns `item_id` column; update the header doc comments that mention the ID
       column and its global-filter exclusion.
    3-5. Remove every `#{…item_id}` rendering + its now-orphaned style rules: SearchResults
       group span, WantItemCell (KEEP the custom-want chip branch untouched), WantAddForm
       result + staged spans (.result-id/.staged-id styles).
    6. NotificationRow: source fallback → friendly map (`ec_auction` → 'EC auction alert',
       `wts` → 'WTS alert', `raid_target` → 'Raid target alert', unknown → 'SquireBot alert');
       deliveryBadge 'ERROR' → 'NOT SENT' (keep the --status-other treatment). Grep the web
       test suite for 'Alert from ' / 'ERROR' assertions on NotificationRow/inbox and update
       any hits.
    7. DataGrid facet default option `{col.id} (all)` → display header
       `String(col.columnDef.header ?? col.id)`; apply the same substitution to BOTH
       aria-labels on the facet select and the column filter input.
    8. composeNotes.ts 'Quest item: yes (in-game flag)' → 'Quest item'; update the
       composeNotes.test.ts assertions on that string.

    **Explicitly KEEP (locked):** StatusCell OK/MISSING/OTHER vocabulary, LastSyncedCell date
    format, WatcherCodesPanel '#1' ordinals, MonitorAdminPanel channel snowflakes, eviction
    mint-code ops copy, PigParse mention in tooltips. **DEFERRED:** AssignmentAdminPanel
    snowflake→username (needs a server-side join — backlog).

    Note Task 1 already edited columns.ts and WantAddForm.svelte — apply these edits on top of
    that state (e.g. wantlistColumns has already lost its reason entry; doc comments may
    already differ from the audit's line numbers — match on content).

    Commit (code only): `refactor(260610-fm5): strip item IDs and raw enums from member-facing UI`
  </action>
  <verify>
    <automated>cd web && npm run check && npm test -- --run</automated>
  </verify>
  <done>No member-facing grid/search/add-form surface renders an item ID; notification source + delivery labels are friendly; DataGrid facet labels use display headers; tooltip says 'Quest item'; the KEEP list is untouched; web check/test green; one atomic commit, no .planning/ files staged.</done>
</task>

<task type="auto">
  <name>Task 3: WS3 — placement consistency fixes, then run the full verification gates</name>
  <files>web/src/lib/components/SiteShell.svelte, web/src/routes/+page.svelte, web/src/lib/components/DataGrid.svelte, web/src/lib/components/NotificationPrefsPanel.svelte, web/src/routes/char-meta/+page.svelte, web/src/routes/bank-coin/+page.svelte, web/src/routes/admin/+page.svelte, web/src/routes/my-characters/+page.svelte, web/src/lib/components/SettingsMenu.svelte, web/src/routes/wantlist/+page.svelte, web/src/lib/components/WantlistPanel.svelte</files>
  <action>
    Implement CONTEXT.md "WS3 — Placement consistency (clear wins)" items 1-9 in full:

    1. SiteShell: labeled nav link to `/` (label **Inventory**), styled identically to the
       existing nav links, order Inventory → Wantlist → Notifications. Wordmark keeps linking
       home.
    2. Home +page.svelte: restyle the scope filter to the wantlist pattern — segmented
       two-button toggle (`My characters` | `Guild`) + a character `<select>` shown only for
       My characters. Default = Guild (preserves today's all-members default). Reuse the
       wantlist `.seg`/`.seg-btn` styling (duplicating the small style block is acceptable;
       extract shared CSS only if trivial). Presentation-only — same filtering semantics as
       the current merged 'Show' select.
    3. DataGrid: per-column filter row behind a 'Filters' disclosure toggle (default hidden,
       min-height 44px button, aria-expanded). Global filter placeholder 'Filter all
       columns…' → 'Filter this table…'.
    4. NotificationPrefsPanel: one line under the monitor rows — "To mute alerts for a single
       item, use the bell next to it on your wantlist." linking to /wantlist.
    5. Purpose lines (match the existing `.form-purpose` style): /char-meta ("Set class,
       level, and race so gear and spell checks work for that character.") and /bank-coin;
       add an h1 'Admin' above the /admin cards.
    6. Cross-links: /my-characters gains 'Set class & level' → /char-meta; /char-meta gains
       'Claim characters' → /my-characters. SettingsMenu menu item 'Character details' →
       'Set class & level'.
    7. /bank-coin gains 'Back to bank' → `/?view=bank`; home +page.svelte reads the `?view=`
       query param on mount to seed the active tab — validate against the page's ACTUAL tab
       ids, ignore unknown values.
    8. /wantlist route (+ WantlistPanel if needed): intro + WantAddForm stay in the 720px
       card; the filter bar + grids break out to full width (mirror the home layout).
       Markup/CSS only.
    9. WantlistPanel: move the remove/mute announce messages directly above the grid they
       affect (message-adjacent-to-control standardization).

    **DEFERRED (do not do):** folding /char-meta into /my-characters, MyCharactersPanel
    message reshuffle, full toolbar redesign.

    Discretion (exact wording, disclosure styling): follow EQ-theme tokens (--accent, --panel,
    --font-display, 44px min touch targets) and surrounding idiom. Note DataGrid was already
    edited in Task 2 — build on that state.

    Commit (code only): `refactor(260610-fm5): placement consistency — nav, scope filter, grid filters disclosure, cross-links`

    **Then run the FULL verification gate suite from CONTEXT.md <specifics> (all must pass):**
    - `cd web && npm run check` → 0 errors / 0 warnings
    - `cd web && npm test -- --run` → all green (baseline was 303; new total may differ —
      record the new count in the SUMMARY)
    - `cd web && npm run build` → success
    - repo root: `go build ./...` && `go vet ./internal/backendsrv/... ./cmd/squirebot-server/...`
      && `go test ./internal/backendsrv/...` → all green
  </action>
  <verify>
    <automated>cd web && npm run check && npm test -- --run && npm run build && cd .. && go build ./... && go vet ./internal/backendsrv/... ./cmd/squirebot-server/... && go test ./internal/backendsrv/...</automated>
  </verify>
  <done>All 9 WS3 items implemented (deferred items untouched); ALL verification gates green (web check 0/0, web test suite, web build, go build/vet/test); three atomic commits total on master; no .planning/ files committed; NOT deployed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale web clients → POST /api/v1/wantlist | old bundles may still send a "reason" key after deploy |
| live DB → migration 00011 | dedupe UPDATE soft-deletes real user rows; index recreation changes the uniqueness contract |
| ?view= query param → home tab state | user-controlled URL input seeds UI state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fm5-01 | Tampering | 00011 dedupe + reindex | mitigate | Soft-delete only (active=0, never DELETE — alert_log FKs survive); MIN(id) keeper deterministic; COALESCE(character_id,-1) retained in both GROUP BYs and both indexes; TestMigrate_00011(c) proves the data pass; TestMigrate_00010 regression-guards the per-char dedup. |
| T-fm5-02 | DoS | AddWantTx INSERT vs retained NOT NULL CHECK reason column | mitigate | INSERT writes the literal 'buy' (CHECK-satisfying); store test asserts a successful add round-trips; forgetting this = 500 on every add — pinned in Task 1. |
| T-fm5-03 | Tampering | stale clients sending "reason" | accept | json.Decode without DisallowUnknownFields silently drops the key (verified in the audit); no validation gap — the field no longer reaches the store. |
| T-fm5-04 | Tampering/XSS | ?view= param seeding home tab | mitigate | Validate against the page's actual tab-id allowlist on mount; ignore unknown values; param never rendered as HTML (plain {} boundary holds). |
| T-fm5-05 | Information Disclosure | removing IDs/enums from UI | accept | Display-only changes; API payloads unchanged; no new data exposed. KEEP list (admin diagnostics) deliberately retains snowflakes for officers. |
</threat_model>

<verification>
Full gate suite (run at the end of Task 3; Tasks 1-2 run their subsets after each commit):
- `cd web && npm run check` → 0 errors / 0 warnings
- `cd web && npm test -- --run` → all green (record the new total vs the 303 baseline)
- `cd web && npm run build` → success
- repo root: `go build ./...`, `go vet ./internal/backendsrv/... ./cmd/squirebot-server/...`,
  `go test ./internal/backendsrv/...` → all green
- `git log --oneline -3` shows the three atomic conventional commits; `git status` shows no
  staged .planning/ files.
- Browser-smoke gap: node vitest is DOM-blind (P15/P26/P27 precedent) — flag in the SUMMARY
  that the segmented toggle, Filters disclosure, ?view= seeding, and full-width wantlist
  layout need a post-deploy browser smoke (orchestrator handles deploy + smoke).
</verification>

<success_criteria>
- WS1: buy/quest reason is gone from every member-facing surface, the API contract, the store,
  wantmatch, and the EC embed; migration 00011 dedupes then reindexes WITHOUT reason while
  KEEPING COALESCE(character_id,-1); the reason column persists and the store writes 'buy'
  forever; same-item re-add now 409s; TestMigrate_00006/00010 pass unchanged; TestMigrate_00011
  added and green.
- WS2: zero raw item IDs in member-facing UI; friendly notification source/delivery labels;
  display-header facet labels; 'Quest item' tooltip copy; the KEEP/DEFERRED lists untouched.
- WS3: all 9 placement items shipped (Inventory nav, segmented home scope filter defaulting to
  Guild, Filters disclosure, wantlist-mute hint, purpose lines + Admin h1, bidirectional
  char-meta/my-characters cross-links + SettingsMenu rename, bank-coin back-link + validated
  ?view= seeding, full-width wantlist grids, message-adjacent announces).
- Exactly three atomic conventional commits on master; code only (commit_docs=false); NOT
  deployed; "Verified NOT impacted" files untouched.
- All verification gates green.
</success_criteria>

<output>
After completion, create
`.planning/quick/260610-fm5-streamline-web-ui-remove-wantlist-buy-qu/260610-fm5-SUMMARY.md`
(the orchestrator commits it — the executor does NOT git-add .planning/). Record in the
SUMMARY: the new web test count, whether the dedupe pass in 00011 would deactivate any rows on
a reason-duplicate-free DB (expected: zero — but the live DB may differ), which migrate-test
UpTo mechanism was chosen (direct goose vs embed.go helper), and the browser-smoke gap list.
</output>
