---
quick_id: 260602-u7m
slug: church-audit-follow-up-save-report-low-r
date: 2026-06-03
status: planned
---

# Quick Task 260602-u7m: Church of Clean Code audit follow-up

Save the consolidated Church of Clean Code audit report and apply the low-risk
fixes it surfaced. Larger items (test work, dep bump/scan, doc overhaul) are
flagged for a planned phase/backlog rather than done inline.

## Tasks

### Task 1 — Save the audit report (docs)
- **files:** `.planning/CLEAN-CODE-REPORT.md`
- **action:** Persist the consolidated 9-crusade audit report.
- **verify:** File exists with per-pillar verdict table + CRITICAL/WARN/polish sections.
- **done:** Report saved.

### Task 2 — Harden `.gitignore` (code)
- **files:** `.gitignore`
- **action:** Add `.claude/` (untracked local agent state) plus defense-in-depth secret
  patterns: `*.pem`, `*.key`, `.env`, `.env.*`, `!.env.example`, `credentials.json`, `rclone.conf`.
- **verify:** `git status` no longer shows `.claude/`; patterns present in `.gitignore`.
- **done:** Committed atomically.

### Task 3 — Reconcile CLAUDE.md watcher dependency doctrine (code/docs)
- **files:** `CLAUDE.md`
- **action:** Update the watcher Technology Stack line — remove the stale
  `google.golang.org/api/sheets/v4` + `golang.org/x/oauth2` deps (post-v2.0 "Off Google";
  oauth2 is now server-only, the Sheets client is gone). Scope: the dependency doctrine only;
  the broader Sheet-side/architecture/OAuth-scope reconciliation is deferred.
- **verify:** Line 14 no longer lists `google.golang.org/api` or oauth2 as watcher deps.
- **done:** Committed atomically.

### Task 4 — Harmonize naming in enrich/wikiitem.go (code)
- **files:** `internal/backendsrv/enrich/wikiitem.go`
- **action:** Rename `maxSummaryLen` → `maxSummaryLength` (matches sibling `minWikitextLength`).
  Constant + all in-file references and comments. (Contained to wikiitem.go — no cross-file callers.)
- **verify:** `go build ./internal/backendsrv/enrich/...` passes; no `maxSummaryLen` references remain.
- **done:** Committed atomically.

### Task 5 — Record deferred items (docs)
- **files:** `.planning/quick/260602-u7m-church-audit-follow-up-save-report-low-r/260602-u7m-deferred-items.md`
- **action:** Capture the non-quick recommendations for a future planned phase/backlog.
- **done:** Recorded.

## Out of scope (deferred — NOT done in this task)
- C1/C2 test gaps + twin-handler refactor in `runapp.go`
- `govulncheck ./...` run + `fsnotify` v1.7.0→v1.10.1 bump
- Observability M2/L1/L2, Architecture LOW tidy-ups
- Full CLAUDE.md/STACK.md v2.0 reconciliation
