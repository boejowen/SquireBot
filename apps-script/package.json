{
  "name": "squirebot-apps-script",
  "private": true,
  "version": "0.3.0",
  "description": "SquireBot Apps Script side: PigParse + P1999 wiki enrichment, consolidated view + bank tabs, theme registry.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.91",
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
