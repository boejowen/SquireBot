---
phase: 11-backend-foundation-ingest-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - spike/pocketbase/main.go
  - spike/pocketbase/README.md
  - .planning/phases/11-backend-foundation-ingest-api/11-01-SUMMARY.md
  - .planning/phases/11-backend-foundation-ingest-api/11-CONTEXT.md
autonomous: true
requirements: [BACKEND-01, BACKEND-02, BACKEND-03, BACKEND-04]
must_haves:
  truths:
    - "A throwaway PocketBase-as-framework program exercises all four D-01 probes (collections/tables, custom bearer-guarded route doing atomic replace, app.Cron skeleton, arm64 cross-compile) and records PASS/FAIL for each"
    - "A written verdict (adopt PocketBase vs hand-rolled Go fallback) with reasoning lands in 11-01-SUMMARY.md AND as an appended note in 11-CONTEXT.md"
    - "The verdict is unambiguous enough that 11-05 (the HTTP-wiring plan) can pick PocketBase APIs or net/http stdlib without re-deciding"
  artifacts:
    - path: "spike/pocketbase/main.go"
      provides: "Throwaway spike harness exercising the four D-01 probes"
      min_lines: 60
    - path: ".planning/phases/11-backend-foundation-ingest-api/11-01-SUMMARY.md"
      provides: "The spike verdict + per-probe PASS/FAIL + reasoning"
      contains: "VERDICT"
  key_links:
    - from: "11-01-SUMMARY.md"
      to: "11-CONTEXT.md"
      via: "appended verdict note (per D-01)"
      pattern: "VERDICT"
---

<objective>
Run the time-boxed (~1 day) PocketBase-as-framework spike that gates the rest of Phase 11. Exercise the four concrete PASS/FAIL probes from D-01 / RESEARCH.md §"PocketBase Spike Probes" and produce a **verdict**: adopt PocketBase (`pocketbase.New()` as a library) OR fall back to a hand-rolled Go server (`net/http` ServeMux + `modernc.org/sqlite`). The host (Oracle Always Free) and DB (SQLite) decisions stand either way — only the HTTP/cron *wiring shell* (11-05) branches on this verdict.

Purpose: De-risk the single biggest open architectural question before any verdict-dependent code is written. RESEARCH establishes the cgo concern is a non-issue (PocketBase v0.39.0 ships the same `modernc.org/sqlite v1.51.0` driver the fallback would use), so all four probes are expected to PASS on technical grounds — the real decision axis is **leverage** (free admin UI, auto-REST for P14, built-in Discord OAuth2 for P15) **vs. opinion/churn** (pre-1.0 API instability, bypassing PB's auth-record model for opaque guild codes).

Output: A throwaway spike harness under `spike/pocketbase/`, a verdict in `11-01-SUMMARY.md`, and an appended verdict note in `11-CONTEXT.md`. This plan writes NO production backend code — that begins in 11-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-backend-foundation-ingest-api/11-CONTEXT.md
@.planning/phases/11-backend-foundation-ingest-api/11-RESEARCH.md
@.planning/phases/11-backend-foundation-ingest-api/11-PATTERNS.md

<interfaces>
<!-- The spike is throwaway; it does NOT consume production interfaces. The four probes
     map to PocketBase APIs (post-v0.23, current for v0.39) verified in RESEARCH.md: -->

PocketBase-as-framework API surface (RESEARCH.md §"PocketBase Spike Probes"):
```go
app := pocketbase.New()
app.OnServe().BindFunc(func(se *core.ServeEvent) error {
    se.Router.POST("/api/v1/ingest", ingestHandler).
        Bind(&hook.Handler[*core.RequestEvent]{Func: bearerGuard}) // custom guard, NOT apis.RequireAuth()
    return se.Next()
})
app.RunInTransaction(func(txApp core.App) error { /* DELETE + INSERT via txApp.DB().NewQuery(...).Execute() */ })
app.Cron().MustAdd("heartbeat", "*/1 * * * *", func() { /* fires while serving */ })
app.Start()
```
IGNORE any tutorial using `app.Dao()`, `daos.New()`, `:param` routes, or `apis.ActivityLogger()` — all pre-v0.23, removed (RESEARCH.md §State of the Art / Pitfall 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the spike harness and pin the spike-only dependency</name>
  <read_first>
    - .planning/phases/11-backend-foundation-ingest-api/11-RESEARCH.md §"PocketBase Spike Probes" (the four probes + the exact post-v0.23 API code blocks for criteria a–d), §"Standard Stack" → Spike-only table (pin `github.com/pocketbase/pocketbase v0.39.0`), §"Installation (spike path, 11-01 only)"
    - .planning/phases/11-backend-foundation-ingest-api/11-PATTERNS.md §"No Analog Found" → PocketBase bootstrap row, and the "Spike branch (D-01)" header note
    - go.mod (repo is `module github.com/boejowen/SquireBot`, `go 1.25.0`; installed toolchain 1.26.2 — A3)
    - .planning/phases/11-backend-foundation-ingest-api/11-CONTEXT.md §D-01 (the four PASS criteria a–d, verbatim)
  </read_first>
  <action>
    Create `spike/pocketbase/main.go` as a throwaway harness (NOT under `internal/` or `cmd/` — it must be obviously disposable and excluded from the production tree). Pin the dependency exactly:
    ```
    go get github.com/pocketbase/pocketbase@v0.39.0
    ```
    This pulls `modernc.org/sqlite v1.51.0` transitively (no cgo) — verify with `go list -m modernc.org/sqlite` that the version is `v1.51.0`.

    The harness boots `app := pocketbase.New()` and registers (in `app.OnServe().BindFunc`):
    - **Probe (a) — tables:** inside an `OnServe` or a migration hook, run raw DDL via `app.DB().NewQuery("CREATE TABLE IF NOT EXISTS owner (...)").Execute()` for `owner`, `character`, `inventory_item`, `spellbook_entry`, plus one empty dimension table (`item_master`) — proving plain SQL tables coexist with PocketBase's own SQLite file (per A5, plain tables are acceptable for the spike).
    - **Probe (b) — custom-guarded ingest route doing atomic replace:** register `se.Router.POST("/api/v1/ingest", ingestHandler).Bind(&hook.Handler[*core.RequestEvent]{Func: bearerGuard})`. `bearerGuard` reads `e.Request.Header.Get("Authorization")`, strips `Bearer `, SHA-256-hashes, compares against a hardcoded test hash with `crypto/subtle.ConstantTimeCompare`; on miss returns `e.UnauthorizedError("bad token", nil)`; on hit calls `e.Next()`. `ingestHandler` runs `app.RunInTransaction(func(txApp core.App) error { txApp.DB().NewQuery("DELETE FROM inventory_item WHERE character_id = {:cid}").Bind(dbx.Params{"cid": 1}).Execute(); /* INSERT a couple rows */; return nil })`. Do NOT use `apis.RequireAuth()` — guild codes are opaque static tokens, not PB auth records (RESEARCH Anti-Patterns; D-01 criterion b).
    - **Probe (c) — cron skeleton:** `app.Cron().MustAdd("spike-heartbeat", "*/1 * * * *", func() { log.Println("cron fired") })` — a 1-minute job that proves in-process scheduling fires while serving.
    - **Probe (d) — arm64 build:** no code; this probe is the cross-compile in Task 2.

    Add `spike/pocketbase/README.md` stating: "THROWAWAY — PocketBase-as-framework spike for D-01. Delete after the 11-01 verdict is recorded. Not production code." Add the spike harness's `main` package; keep it self-contained (its own `package main`).
  </action>
  <verify>
    <automated>go build ./spike/pocketbase/ 2>&1 | grep -qv "error" && go list -m modernc.org/sqlite</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./spike/pocketbase/` exits 0 (the harness compiles against PocketBase v0.39.0).
    - `go list -m github.com/pocketbase/pocketbase` prints `v0.39.0` (pinned, not floated).
    - `go list -m modernc.org/sqlite` prints `v1.51.0` (the no-cgo driver shared with the fallback — confirms the headline RESEARCH finding).
    - `spike/pocketbase/main.go` contains `pocketbase.New()`, `app.OnServe().BindFunc`, `app.RunInTransaction`, and `app.Cron().MustAdd`.
    - `grep -L 'apis.RequireAuth' spike/pocketbase/main.go` returns the file (i.e. the file does NOT use `apis.RequireAuth` — the guard is custom).
    - `spike/pocketbase/main.go` contains `subtle.ConstantTimeCompare` (the custom bearer guard).
  </acceptance_criteria>
  <done>The spike harness compiles, pins PocketBase v0.39.0, confirms the transitive modernc v1.51.0 (no-cgo) driver, and wires all three runnable probes (a/b/c) with a custom bearer guard rather than PB's JWT auth-record system.</done>
</task>

<task type="auto">
  <name>Task 2: Run the four probes and cross-compile for arm64</name>
  <read_first>
    - .planning/phases/11-backend-foundation-ingest-api/11-RESEARCH.md §"PocketBase Spike Probes" → Criterion (d) probe + §"Code Examples" → "Cross-compile from Windows → linux/arm64" (the exact PowerShell incantation)
    - .planning/phases/11-backend-foundation-ingest-api/11-PATTERNS.md §"Shared Patterns" → "Cross-compile from Windows → linux/arm64" (`.goreleaser.yaml` lines 40-49 `CGO_ENABLED=0` precedent — the watcher targets windows/amd64; the server flips to linux/arm64 with the same CGO_ENABLED=0 discipline)
    - spike/pocketbase/main.go (the harness from Task 1)
  </read_first>
  <action>
    Exercise probes a/b/c locally, then cross-compile for arm64 (probe d):

    1. **Run locally (probes a/b/c):** `go run ./spike/pocketbase/ serve` (PocketBase's default serve subcommand). Confirm: the SQLite file is created with the four+dimension tables (probe a); `curl` the ingest route with no `Authorization` header → 401, with the test bearer token → 200 and the atomic replace runs (probe b); the `*/1 * * * *` cron logs "cron fired" within ~1 minute (probe c). Capture the actual observed behavior (status codes, log lines) for the SUMMARY.
    2. **Cross-compile for arm64 (probe d):** in PowerShell on the Windows dev box:
       ```powershell
       $env:GOOS="linux"; $env:GOARCH="arm64"; $env:CGO_ENABLED="0"
       go build -trimpath -ldflags "-s -w" -o spike-arm64 ./spike/pocketbase
       ```
       A successful build with `CGO_ENABLED=0` for `linux/arm64` PROVES probe (d) at the build level (PocketBase publishes official linux/arm64 binaries; the only SQLite dep is pure-Go modernc, so no C cross-toolchain is needed). Note: running the binary on real ARM hardware is deferred to the on-box smoke (11-06/11-07) — the build success is the spike-level evidence. Reset the env vars afterward (`$env:GOOS=""; $env:GOARCH=""; $env:CGO_ENABLED=""`).

    Record each probe's outcome as PASS / FAIL / PARTIAL with the concrete evidence (observed status codes, the cron log line, the arm64 binary size).
  </action>
  <verify>
    <automated>powershell -Command "$env:GOOS='linux'; $env:GOARCH='arm64'; $env:CGO_ENABLED='0'; go build -trimpath -ldflags '-s -w' -o spike-arm64 ./spike/pocketbase; if (Test-Path spike-arm64) { Write-Output 'ARM64_BUILD_OK' }; $env:GOOS=''; $env:GOARCH=''; $env:CGO_ENABLED=''"</automated>
  </verify>
  <acceptance_criteria>
    - The arm64 cross-compile produces a `spike-arm64` binary from the Windows box with `CGO_ENABLED=0` (probe d PASS at build level) — verify output contains `ARM64_BUILD_OK`.
    - Local run evidence is captured: ingest route returns 401 with no/bad token and 200 with the test token (probe b); the four+dimension tables exist in the spike SQLite file (probe a); the `*/1 * * * *` cron fires within ~1 minute (probe c).
    - Each of probes a/b/c/d has a recorded PASS / FAIL / PARTIAL outcome with concrete evidence (not "looks fine").
  </acceptance_criteria>
  <done>All four probes have been exercised and have a recorded outcome with evidence: a/b/c run locally, d cross-compiles for arm64 from Windows. The technical feasibility question is answered with observed behavior, not assertion.</done>
</task>

<task type="auto">
  <name>Task 3: Write the verdict to 11-01-SUMMARY.md and append the CONTEXT note</name>
  <read_first>
    - .planning/phases/11-backend-foundation-ingest-api/11-RESEARCH.md §"Spike verdict guidance" (the leverage-vs-opinion decision axis: adopt-PB-if vs fall-back-if) + §"PocketBase Spike Probes" intro (criteria a/d pre-validated, b/c where real fit lives) + Pitfall 1 (pre-1.0 churn — adopting PB signs up for periodic migration work)
    - .planning/phases/11-backend-foundation-ingest-api/11-CONTEXT.md §D-01 (verdict MUST be captured in an 11-01 SUMMARY / appended CONTEXT note) + §Decisions header (user delegated all gray areas — the verdict is Claude's recommendation to make, not the user's)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY structure)
    - .planning/phases/11-backend-foundation-ingest-api/11-CONTEXT.md (the file being appended to — read its current tail so the appended note is well-placed)
  </read_first>
  <action>
    Create `.planning/phases/11-backend-foundation-ingest-api/11-01-SUMMARY.md` following the summary template. It MUST include:
    - A **per-probe table**: criterion a/b/c/d → PASS/FAIL/PARTIAL → the concrete evidence from Task 2.
    - A line beginning **`VERDICT:`** stating either `ADOPT PocketBase` or `HAND-ROLLED Go fallback`, with 2–4 sentences of reasoning grounded in the leverage-vs-opinion axis (RESEARCH §"Spike verdict guidance"). If all four probes PASS (expected), the decision is judgment: weigh PB's free admin UI + auto-REST (P14) + built-in Discord OAuth2 (P15, could compress that phase) against pre-1.0 API churn (Pitfall 1: v0.23 was a near-total rewrite; adopting PB means periodic migration work) and the friction of bypassing PB's auth-record model for opaque guild codes. A `HARD BLOCKER` on any probe forces the fallback regardless.
    - An explicit **"What 11-05 branches on"** subsection: if ADOPT → 11-05 wires the route via `app.OnServe().BindFunc` + `e.Router.POST(...).Bind(customGuard)` and the cron via `app.Cron().MustAdd`; if FALLBACK → 11-05 wires `net/http` ServeMux 1.22+ (`"POST /api/v1/ingest"`) + a `time.Ticker`/`robfig/cron` goroutine. Either way the business logic (auth guard, parser, store tx — built in 11-02/03/04) is framework-agnostic and imported unchanged.

    Then **append a note to `11-CONTEXT.md`** (do not rewrite the file — add a new `<spike_verdict>` section at the end, before the trailing `---`/footer) restating the `VERDICT:` line + a one-line pointer to the SUMMARY for the full reasoning. This satisfies D-01's "appended CONTEXT note" requirement so downstream phases (P12/P14/P15) see the verdict in the canonical phase context.

    Note the spike dependency disposition: if FALLBACK, flag that the PocketBase dependency should be removed from `go.mod`/`go.sum` and `spike/pocketbase/` deleted as a cleanup step in 11-05 (don't carry a pre-1.0 dep the production server won't use). If ADOPT, the dep stays and 11-05 promotes the wiring into `cmd/squirebot-server`.
  </action>
  <verify>
    <automated>powershell -Command "if ((Select-String -Path '.planning/phases/11-backend-foundation-ingest-api/11-01-SUMMARY.md' -Pattern 'VERDICT') -and (Select-String -Path '.planning/phases/11-backend-foundation-ingest-api/11-CONTEXT.md' -Pattern 'VERDICT')) { Write-Output 'VERDICT_RECORDED_BOTH' }"</automated>
  </verify>
  <acceptance_criteria>
    - `11-01-SUMMARY.md` exists and contains a line matching `VERDICT:` followed by either `ADOPT` or `HAND-ROLLED`/`FALLBACK`.
    - `11-01-SUMMARY.md` contains a per-probe table covering criteria a, b, c, and d each with a PASS/FAIL/PARTIAL outcome.
    - `11-CONTEXT.md` now contains a `VERDICT` note (appended, not replacing existing content — the original `<domain>`/`<decisions>` sections are still present).
    - `11-01-SUMMARY.md` contains a "What 11-05 branches on" subsection naming the PocketBase APIs OR the net/http stdlib shapes.
    - Verify output contains `VERDICT_RECORDED_BOTH`.
  </acceptance_criteria>
  <done>The spike verdict (adopt vs fallback) with per-probe evidence and downstream-branching guidance is recorded in 11-01-SUMMARY.md and mirrored as a note in 11-CONTEXT.md, satisfying D-01. 11-05 can now wire the HTTP/cron shell without re-deciding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike harness ↔ disk | The spike writes a throwaway SQLite file; no real guild data, no real tokens. |
| (no production boundary) | This plan exercises probes only; the real client→API boundary is introduced in 11-05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.01-01 | Information Disclosure | Spike harness leaks a real guild code or real data | mitigate | Spike uses a hardcoded TEST token hash and synthetic rows only; no `crypto/rand`-minted real code, no real character data. README marks the tree throwaway. |
| T-11.01-02 | Tampering | Pre-1.0 PocketBase dependency drifts and silently changes behavior on a later `go get` | mitigate | Pin `github.com/pocketbase/pocketbase v0.39.0` exactly (Pitfall 1); never float. If the verdict is FALLBACK, 11-05 removes the dep entirely. |
| T-11.01-03 | Elevation | Spike's custom guard accidentally models PB's JWT auth-record system, leaking PB's permission semantics into the opaque-token design | accept | Probe (b) deliberately uses a custom `func(*core.RequestEvent) error` guard with `ConstantTimeCompare`, NOT `apis.RequireAuth()` — documented in the SUMMARY; the production guard (11-04) is independent. Low risk: spike is throwaway. |

This plan introduces no network-exposed production surface; the ASVS L1 controls (V2 token hashing, V5 input validation, V6 crypto, V7 no-token-logging) are exercised for real in 11-04 (auth) and 11-05 (ingest handler), where the full threat register lives.
</threat_model>

<verification>
- `go build ./spike/pocketbase/` compiles against PocketBase v0.39.0.
- `go list -m modernc.org/sqlite` → `v1.51.0` (no-cgo driver confirmed shared with the fallback).
- `CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build` produces an arm64 binary from Windows (probe d).
- All four probes (a/b/c/d) have recorded PASS/FAIL/PARTIAL outcomes with evidence.
- `VERDICT:` line present in BOTH `11-01-SUMMARY.md` and `11-CONTEXT.md`.
</verification>

<success_criteria>
The PocketBase-as-framework spike is complete: the four D-01 probes are exercised with concrete evidence, and an unambiguous verdict (adopt PocketBase vs hand-rolled Go fallback) with reasoning is recorded in `11-01-SUMMARY.md` and appended to `11-CONTEXT.md`. The host (Oracle) and DB (SQLite) decisions are reaffirmed as verdict-independent. 11-02/03/04 (verdict-agnostic business logic) and 11-05 (verdict-dependent HTTP wiring) can now proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-backend-foundation-ingest-api/11-01-SUMMARY.md` (the verdict + per-probe evidence) and append the verdict note to `.planning/phases/11-backend-foundation-ingest-api/11-CONTEXT.md`.
</output>
