---
context: phase
phase: 14-web-frontend
task: null
total_tasks: null
status: phase_complete
last_updated: 2026-05-30T15:40:00Z
---

# Resume Point — Phase 14 COMPLETE; next is Phase 15

> No blocking constraints. Phase 14 is fully closed, deployed, and human-UAT-passed.
> This is a clean between-phases pause.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Static-site redeploy clears live dir before validating new artifact | During the P14 deploy, a local `tar -C web/build` ran from the wrong cwd → empty archive; combined with `rm -rf /var/www/squirebot/*` it 404'd the live site for ~1 min | advisory | When redeploying the static site: build the tarball with ABSOLUTE paths, size-guard it (`if len > 1000`) BEFORE the remote `rm -rf` + extract, and verify `curl https://squirebot.quest` 200 after |
| git-bash ssh can't reach the Windows OpenSSH agent | `ssh-add -l` / `ssh` from the Bash tool fail (`Could not open a connection to your authentication agent`) | advisory | SSH to the VPS ONLY via the PowerShell tool (`ssh.exe` → Windows agent, key `squirebot-hetzner`). Remote multi-line: pipe a single-quoted here-string to `ssh root@5.78.232.85 "tr -d '\r' \| bash -s"` with a throwaway BOM-bait first line |

<current_state>
Phase 14 (Web Frontend) is **fully complete and live**. The guild website is up at https://squirebot.quest (SvelteKit static, served by Caddy on the VPS at the apex) over the Go read API at https://api.squirebot.quest. All gates green: automated verification 6/6, code review (0 critical, WR-01..04 fixed), security 19/19, human UAT 5/5 PASS. Demo data removed — prod is clean (chars=0, views empty), ready for real guildie watcher uploads. We are BETWEEN phases; next is Phase 15.
</current_state>

<completed_work>
- Phase 14 planned (4 plans / 3 waves), executed, all SUMMARYs written
- Code review + auto-fix (WR-01 scheme allow-list, WR-02 global-filter scope, WR-03 onMount, WR-04 branded ApiError)
- Security verification: 19/19 STRIDE threats closed (14-SECURITY.md)
- Deploy: rebuilt linux/amd64 server binary (with readapi routes + apex CORS) → VPS; static build → /var/www/squirebot; Caddy apex block; LE cert auto-issued
- Human UAT 5/5 PASS — the walk found + fixed the WEB-04 wiki-link gap (f12ad9b: links now name-derived at render time)
- Demo character `Demoknight` + `demo-remove-me` code removed
</completed_work>

<remaining_work>
- Phase 15 (Admin Web Forms + Login): Discord OAuth2 login (walls the now-public site to guild members) + officer-only eviction / bank-coin / admin-mgmt write forms. Has no CONTEXT.md yet → start with discuss.
</remaining_work>

<decisions_made>
- Frontend = Caddy-on-VPS at the apex `https://squirebot.quest` (deviation from the planned Cloudflare Pages `app.` subdomain) — lower friction, one control plane, keeps P15 same-origin option open
- Backend `defaultCORSOrigin` → `https://squirebot.quest`; `api.squirebot.quest` unchanged (watcher binaries hardcode it)
- Wiki link derived from item name at render time (per FEATURES.md/WEB-04), not from the (empty-until-enrichment) `item_master.wiki_url`
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — current position (Phase 14 closed; Phase 15 next) + the v2.0 phase plan
2. `.planning/ROADMAP.md` § "Phase 15: Admin Web Forms + Login" — goal, reqs (AUTH-08/09, ADMIN-04/05/06), success criteria
3. `.planning/phases/14-web-frontend/14-CONTEXT.md` § `<deferred>` — the P15 forward-dependencies (Discord gate on read; admin write forms; bank-coin)
4. Memory `v2-backend-live-and-ops-access` — VPS/SSH/ops + the now-live frontend layout

## Infrastructure State
- Backend: LIVE, Hetzner VPS `5.78.232.85`, `https://api.squirebot.quest` (Caddy → loopback :8090), systemd `squirebot-server` active, goose v3. Rollback `.bak` for binary + `/etc/caddy/Caddyfile.bak`.
- Frontend: LIVE, `https://squirebot.quest` (Caddy apex `file_server /var/www/squirebot`, valid LE cert).
- Data: views EMPTY (`chars=0`) — populate naturally as guildies' P13-retargeted watchers upload to `api.squirebot.quest`. One inert disabled `VerifyP13` guild_code remains.
- DNS at Porkbun: apex `squirebot.quest` A → `5.78.232.85`; `api` A → `5.78.232.85`; `*` wildcard CNAME → parking (harmless).

<context>
Phase 14 shipped the full plan→execute→review→secure→deploy→UAT arc this session (15+ commits on master). The website is back for the guild, replacing the Sheet's read UI. Phase 15 is the natural next step (login + admin writes); it needs its own discuss-phase since no CONTEXT.md exists for it yet.
</context>

<next_action>
Start with: `/gsd-discuss-phase 15`
</next_action>
