---
quick_id: 260618-rh1
slug: fix-backend-deploy-web-swap-guard-to-200
date: 2026-06-19
status: complete
---

# Quick Task 260618-rh1: Fix backend-deploy web-swap guard to 200.html

## Objective
`docs/backend-deploy.md` §7.5's web-bundle atomic-swap guard checked `test -f "$NEW/index.html"`, but
the SvelteKit adapter-static SPA build emits `200.html` (no `index.html` — the `/` route is a client
redirect). So the guard aborts with `EXTRACT_FAILED` on a real deploy — hit live during the Phase 32
deploy (2026-06-18). Fix the guard + the build-output comment to reference `200.html` so the P33/P34
web deploys don't fail.

## Tasks
1. `docs/backend-deploy.md` §7.5 — change the extract guard `test -f "$NEW/index.html"` → `test -f "$NEW/200.html"` (load-bearing) and correct the `npm run build` output comment (which listed `index.html`).

## Acceptance
- §7.5 guard checks `$NEW/200.html` (not `index.html`); the build-output comment lists `200.html`
- Any remaining `index.html` text is explanatory only (notes), not a check
- Doc-only change; no code, no behavior change
