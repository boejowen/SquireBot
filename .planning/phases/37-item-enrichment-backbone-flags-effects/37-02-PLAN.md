---
phase: 37-item-enrichment-backbone-flags-effects
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - internal/backendsrv/migrations/00016_item_flags_effects.sql
  - internal/backendsrv/migrations/migrate_test.go
  - internal/backendsrv/store/enrich.go
  - internal/backendsrv/store/enrich_test.go
  - internal/backendsrv/store/backfill.go
  - internal/backendsrv/store/backfill_test.go
  - internal/backendsrv/enrich/jobs/wiki.go
  - internal/backendsrv/enrich/jobs/wiki_test.go
  - cmd/squirebot-server/main.go
autonomous: true
requirements: [ENRICH-12, ENRICH-13]
user_setup: []

must_haves:
  truths:
    - "The four flag booleans + is_clicky/clicky_effect + has_haste/haste_pct + the full flag set are stored as discrete queryable columns on item_master"
    - "An item_master row already enriched under the old (flag-discarding) parse is backfilled from its STORED statsblock TEXT with NO wiki refetch / no network"
    - "The enrich freshness short-circuit re-parses a row that is missing the new fields on the next weekly wiki pass (self-healing)"
    - "A flagless item backfills to flags_json=\"[]\" and is NOT re-written on every subsequent weekly pass (the upsert, backfill, and freshness compare all marshal the SAME canonical flags_json — D-06 idempotency)"
    - "go test ./... and go build ./... are green; the watcher is untouched (WatcherMaxSchemaVersion unchanged)"
  artifacts:
    - path: "internal/backendsrv/migrations/00016_item_flags_effects.sql"
      provides: "the extend-only ADD COLUMN migration for the nine discrete flag/effect columns"
      contains: "ALTER TABLE item_master ADD COLUMN is_clicky"
    - path: "internal/backendsrv/store/enrich.go"
      provides: "ItemMaster struct + upsert + GetItemMasterFreshnessTx all extended for the new columns + the canonical MarshalFlags helper"
      contains: "func MarshalFlags"
    - path: "internal/backendsrv/store/backfill.go"
      provides: "the no-network re-parse-stored-statsblock backfill (D-05)"
      contains: "func BackfillItemFlags"
    - path: "cmd/squirebot-server/main.go"
      provides: "the one-time boot-time backfill call after RunMigrations"
      contains: "BackfillItemFlags"
  key_links:
    - from: "internal/backendsrv/enrich/jobs/wiki.go upsertItemAndQuests"
      to: "store.UpsertItemMasterTx + the extended freshness compare"
      via: "carry the new ParsedWikiItem fields into store.ItemMaster + re-write when the parsed flags differ"
      pattern: "IsClicky|item\\.Flags|parsedSig"
    - from: "cmd/squirebot-server/main.go"
      to: "store.BackfillItemFlags"
      via: "one-time call right after migrations.RunMigrations(db)"
      pattern: "BackfillItemFlags"
    - from: "store.UpsertItemMasterTx + store.BackfillItemFlags + jobs/wiki.go freshness compare"
      to: "store.MarshalFlags"
      via: "ONE canonical flags-array marshal (sorted input, empty → \"[]\") so byte-equality freshness is deterministic across all three write/compare sites (D-06)"
      pattern: "MarshalFlags\\("
---

<objective>
Persist the flag/effect values that Plan 37-01 made the parser surface. Add the goose
migration `00016` (nine discrete columns at the right edge of `item_master`, extend-only),
extend the store's `ItemMaster` struct + upsert SQL to write them, run an IMMEDIATE
no-network backfill that re-parses the ALREADY-STORED `statsblock` TEXT (D-05), extend
`GetItemMasterFreshnessTx` so a row missing the new fields re-parses on the next weekly
pass (D-06), and wire the new fields through the weekly wiki job.

Purpose: close ENRICH-12 + ENRICH-13 at the data layer. After this plan, an enriched item's
LORE/NO-DROP/MAGIC/TEMPORARY flags + Clicky (name) + Haste (%) are individually queryable
columns, every already-enriched row lights up at boot WITHOUT a wiki round-trip, and new/
changed items keep refreshing on the weekly crawl. No UI (P39/P40), no coverage widening (P38).

Output: migration 00016 + store changes + a boot-time backfill + the job wiring, all tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/37-item-enrichment-backbone-flags-effects/37-CONTEXT.md
@.planning/phases/37-item-enrichment-backbone-flags-effects/37-01-SUMMARY.md

# The store path being extended (read in full).
@internal/backendsrv/store/enrich.go
# The job that calls the freshness short-circuit + upsert (the wiring point).
@internal/backendsrv/enrich/jobs/wiki.go
# The ADD-COLUMN + freshness-comparison precedent to MIRROR exactly.
@internal/backendsrv/migrations/00012_item_icon.sql
@internal/backendsrv/migrations/00013_item_statsblock.sql
# The migration test harness shape (mirror TestMigrate_00004 for the new 00016 column test).
@internal/backendsrv/migrations/migrate_test.go

<interfaces>
<!-- Extracted so the executor needs no exploration. -->

The CONFIRMED 37-01 parser fields on enrich.ParsedWikiItem (read 37-01-SUMMARY.md for the
final names; they are):
  IsLore, IsNoDrop, IsMagic, IsTemporary bool
  Flags []string            // the full sorted detected flag set
  IsClicky bool; ClickyEffect string
  HasHaste bool; HastePct int

The store struct to extend (internal/backendsrv/store/enrich.go:58-69):
```go
type ItemMaster struct {
    ItemID, Name, WikiSummary, WikiURL, Slot string-ish...
    IsQuestItem  bool
    WikitextSHA1, LastRefreshed string
    IconID    int    // 00012
    Statsblock string // 00013
}
```

The upsert const to extend (enrich.go:159-166) — currently 10 columns / 10 placeholders:
```go
const itemMasterUpsert = `INSERT INTO item_master
    (item_id, name, wiki_summary, wiki_url, slot, is_quest_item, wikitext_sha1, last_refreshed, icon_id, statsblock)
 VALUES (?,?,?,?,?,?,?,?,?,?)
 ON CONFLICT(item_id) DO UPDATE SET ... icon_id=excluded.icon_id, statsblock=excluded.statsblock`
```

The freshness getter to extend (enrich.go:224-236) — currently returns (sha, iconID, statsblock):
```go
func GetItemMasterFreshnessTx(ctx, tx, itemID) (sha string, iconID int64, statsblock string, err error)
// caller wiki.go:227-237 skips the write iff sha==... && icon==... && statsblock==...
```

The job wiring point (internal/backendsrv/enrich/jobs/wiki.go:220-252, upsertItemAndQuests):
the freshness compare at :231 + the store.ItemMaster literal at :239-250.

The migration column-test precedent to MIRROR (internal/backendsrv/migrations/migrate_test.go) —
the repo convention is one `TestMigrate_000NN_*` per migration that asserts each new column via the
`columnSet`/PRAGMA `table_info` helper (see TestMigrate_00003/00004/00005/00008/00009/00010) plus a
second-Up-is-no-op idempotency check. 00016 currently has NO such test — Task 1 adds it.

The boot sequence (cmd/squirebot-server/main.go:221-226):
```go
if err := migrations.RunMigrations(db); err != nil { ... return 1 }
// <-- the one-time BackfillItemFlags call goes HERE (idempotent, like RunMigrations)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add migration 00016 (nine discrete columns) + its migrate-test + extend the ItemMaster struct, upsert, freshness getter, and the canonical MarshalFlags helper</name>
  <read_first>
    - internal/backendsrv/migrations/00012_item_icon.sql + 00013_item_statsblock.sql (the EXACT ADD-COLUMN comment/shape to mirror — nullable, no DEFAULT, no UNIQUE/PK, the "watcher OFF the read path" note)
    - internal/backendsrv/store/enrich.go (the ItemMaster struct :58-69, itemMasterUpsert :159-166, UpsertItemMasterTx :184-197, GetItemMasterFreshnessTx :216-236)
    - internal/backendsrv/migrations/migrate_test.go (openAtVersion / `columnSet` column-existence assertion helpers AND the TestMigrate_00004 shape to mirror for 00016)
    - .planning/phases/37-item-enrichment-backbone-flags-effects/37-01-SUMMARY.md (the final parser field names)
  </read_first>
  <action>
    Create `internal/backendsrv/migrations/00016_item_flags_effects.sql` mirroring 00012/00013
    (forward-only; 00001-00015 are shipped and NOT edited; one column per ALTER; nullable, no
    DEFAULT, no UNIQUE/PK; a header comment noting the watcher is OFF the read path so
    WatcherMaxSchemaVersion is NOT touched and goose version() is the version of record):
    ```sql
    -- +goose Up
    ALTER TABLE item_master ADD COLUMN is_lore INTEGER;
    ALTER TABLE item_master ADD COLUMN is_no_drop INTEGER;
    ALTER TABLE item_master ADD COLUMN is_magic INTEGER;
    ALTER TABLE item_master ADD COLUMN is_temporary INTEGER;
    ALTER TABLE item_master ADD COLUMN is_clicky INTEGER;
    ALTER TABLE item_master ADD COLUMN clicky_effect TEXT;
    ALTER TABLE item_master ADD COLUMN has_haste INTEGER;
    ALTER TABLE item_master ADD COLUMN haste_pct INTEGER;
    ALTER TABLE item_master ADD COLUMN flags_json TEXT;   -- D-03: the full detected flag SET as a JSON array; no future migration for a new flag

    -- +goose Down
    ALTER TABLE item_master DROP COLUMN is_lore;
    -- ...drop all nine in reverse...
    ```
    Add the matching migration test `TestMigrate_00016_AddsItemFlagsEffects` to
    `internal/backendsrv/migrations/migrate_test.go`, MIRRORING `TestMigrate_00004` exactly:
    open the DB migrated up THROUGH 00016, fetch the `item_master` column set via the existing
    `columnSet`/PRAGMA `table_info` helper, and assert ALL NINE new columns exist
    (`is_lore`, `is_no_drop`, `is_magic`, `is_temporary`, `is_clicky`, `clicky_effect`,
    `has_haste`, `haste_pct`, `flags_json`) — this catches a mistyped column name (e.g.
    `is_nodrop` vs `is_no_drop`) in CI instead of at runtime in the store upsert. Include the
    same second-Up-is-no-op idempotency assertion the sibling TestMigrate_000NN tests use.

    Extend `store.ItemMaster` (enrich.go:58-69) with: `IsLore, IsNoDrop, IsMagic, IsTemporary,
    IsClicky, HasHaste bool`, `ClickyEffect string`, `HastePct int`, `FlagsJSON string` (the
    JSON-encoded flag array — the job marshals enrich.ParsedWikiItem.Flags into this via the
    canonical helper below).

    Add the SINGLE canonical flags-marshal helper `func MarshalFlags(flags []string) string` to
    `store/enrich.go` (exported so the job and backfill share it). CONTRACT (state it in the
    doc-comment): the input is assumed already sorted (37-01's `ParsedWikiItem.Flags` is sorted);
    an EMPTY or nil slice marshals to the literal `"[]"` (NEVER `null`, NEVER `""`); a non-empty
    slice marshals to a deterministic JSON array (`json.Marshal` of the slice). This ONE helper is
    the only place flags_json is produced — it is called by the upsert path (via the job literal),
    the backfill, AND the job's freshness compare, so all three sites byte-compare identical
    strings and a flagless item is written exactly once (D-06 idempotency — without a single
    canonical form, an empty set encoded as `null` in one site and `"[]"` in another would re-write
    the row on every weekly pass forever). Add a unit test `TestMarshalFlags` in `store/enrich_test.go`
    asserting `MarshalFlags(nil) == "[]"`, `MarshalFlags([]string{}) == "[]"`, and
    `MarshalFlags([]string{"LORE ITEM","MAGIC ITEM"})` round-trips to a 2-element JSON array.

    Extend `itemMasterUpsert` (enrich.go:159-166) to 19 columns / 19 placeholders + the matching
    `ON CONFLICT DO UPDATE SET ...=excluded....` for each new column (follow the icon_id/statsblock
    pattern exactly). Extend `UpsertItemMasterTx` (enrich.go:184-197) to bind the new values —
    booleans via the same `0/1` int conversion already used for `quest` (:185-188); ClickyEffect,
    HastePct, FlagsJSON bound directly through `?` placeholders (V5 — UNTRUSTED parsed text is
    NEVER string-concatenated). The caller (the job, Task 3) sets `ItemMaster.FlagsJSON` via
    `store.MarshalFlags(item.Flags)` — the upsert binds that field as-is.
    Extend `GetItemMasterFreshnessTx` (enrich.go:216-236) to ALSO read + return a "parsed-flags
    signature" so a row written before these columns re-parses. Pick the SIMPLEST self-healing
    signal that mirrors the icon_id/statsblock precedent: add `flagsJSON string` to the return
    tuple (read `flags_json` as a NullString) — the caller treats a row whose stored flags_json
    differs from the freshly-parsed flags_json (OR is NULL) as stale. The "freshly-parsed" value
    the caller compares against MUST be produced by `store.MarshalFlags` (the same helper the
    upsert/backfill use) so a flagless item's stored `"[]"` byte-equals the freshly-marshaled
    `"[]"` and is NOT treated as stale. Update the function's doc-comment to extend the
    icon/statsblock rationale: a row written before 00016 has the same SHA-1 + icon + statsblock
    yet NULL flags_json, so it must still re-write to backfill the flag/effect columns (exactly the
    00012-icon argument, one more field).
  </action>
  <verify>
    <automated>cd internal/backendsrv && go build ./... && go test ./migrations/ -run TestMigrate_00016 -count=1 && go test ./store/ -run "TestMarshalFlags" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ADD COLUMN' internal/backendsrv/migrations/00016_item_flags_effects.sql` is 9 (in the Up block).
    - `grep -n 'is_clicky\|clicky_effect\|has_haste\|haste_pct\|flags_json\|is_lore\|is_no_drop\|is_magic\|is_temporary' internal/backendsrv/migrations/00016_item_flags_effects.sql` shows all nine columns.
    - `grep -n 'func TestMigrate_00016_AddsItemFlagsEffects' internal/backendsrv/migrations/migrate_test.go` exists and asserts all nine columns via the `columnSet` helper (mirrors TestMigrate_00004), plus the second-Up no-op idempotency check.
    - `grep -n 'func MarshalFlags' internal/backendsrv/store/enrich.go` exists; `grep -n 'func TestMarshalFlags' internal/backendsrv/store/enrich_test.go` exists and covers the nil/empty→`"[]"` case.
    - `grep -c '?' internal/backendsrv/store/enrich.go` placeholders in itemMasterUpsert VALUES line = 19 (verify by reading the VALUES (...) line — exactly 19 `?`).
    - `grep -n 'flags_json\|FlagsJSON' internal/backendsrv/store/enrich.go` appears in BOTH the upsert and GetItemMasterFreshnessTx.
    - `grep -c 'WatcherMaxSchemaVersion' internal/sheet/client.go` is unchanged from before this plan (the watcher write path does NOT touch these read-only columns — confirm the constant is NOT edited).
    - `go test ./migrations/ -count=1` exit 0 (the 00016 column-existence assertions pass at HEAD).
  </acceptance_criteria>
  <done>Migration 00016 adds the nine columns AND has a TestMigrate_00016 asserting all nine; the store struct/upsert/freshness getter carry them; the canonical MarshalFlags helper (empty→"[]") exists and is unit-tested; the package builds and the migration test is green.</done>
</task>

<task type="auto">
  <name>Task 2: No-network backfill from the stored statsblock (D-05) + boot-time invocation</name>
  <read_first>
    - internal/backendsrv/store/enrich.go (the extended UpsertItemMasterTx + the ItemMaster struct + the canonical store.MarshalFlags helper from Task 1)
    - internal/backendsrv/enrich/wikiitem.go (parseStatsblock + the new flagSet/parseClicky/parseHastePct helpers from 37-01 — the backfill re-uses the SAME derivation on the stored cleaned statsblock; if those helpers are unexported, EXPOSE a single exported pure entrypoint, e.g. enrich.DeriveFlagsAndEffects(statsblock string), and call it from BOTH ParseItempage and the backfill so there is ONE derivation)
    - cmd/squirebot-server/main.go:221-226 (the RunMigrations boot step — the backfill call goes right after)
    - internal/backendsrv/store/testhelper.go (store.NewTestDB — for the backfill test)
  </read_first>
  <action>
    The stored `item_master.statsblock` is the CLEANED newline-separated block (00013), NOT the
    raw `<br>` HTML — AND the `[[wiki-link]]` brackets are ALREADY STRIPPED in the stored form
    (cleanStatsblock ran before storage), so a stored Effect line reads e.g.
    `Effect: Shock of Frost (Click from Inventory)`, NOT the raw `Effect: [[Shock of Frost]] (Click...)`.
    Re-parsing it must use the SAME flag/effect derivation as the parser AND must correctly classify
    + extract the clicky name from this NO-BRACKET cleaned form. To keep ONE source of truth, factor
    the 37-01 derivation into an exported pure helper in `enrich/wikiitem.go` —
    `func DeriveFlagsAndEffects(statsblock string) (isLore, isNoDrop, isMagic, isTemporary, isClicky,
    hasHaste bool, clickyEffect string, hastePct int, flags []string)` (or a small result struct) —
    that splits the cleaned block on NEWLINES (the stored form) the same way parseStatsblock splits
    on `<br>`, classifies flags + reads Effect/Haste lines, and returns the same values. Call it from
    BOTH ParseItempage (replacing the inline derivation added in 37-01) and the backfill. (If 37-01
    already split on `<br>`, generalize the splitter to accept either `<br>` or `\n` so the stored
    cleaned block parses identically — add a test for the newline form.) `parseClicky` must already
    tolerate an effect value with NO `[[...]]` brackets (the bracket-strip is a no-op on the cleaned
    form, then the trailing `(...)` qualifier is cut) — the backfill seed test below proves this.

    Create `internal/backendsrv/store/backfill.go` with:
    `func (s *Store) BackfillItemFlags(ctx context.Context) (scanned, updated int, err error)` —
    a ONE-TIME, no-network, idempotent pass:
      1. SELECT item_id, statsblock, flags_json FROM item_master WHERE statsblock IS NOT NULL
         AND statsblock != '' AND flags_json IS NULL  (only rows not yet backfilled — idempotent;
         a second boot is a no-op once flags_json is populated).
      2. For each row: DeriveFlagsAndEffects(statsblock) → UPDATE item_master SET is_lore=?, ...,
         flags_json=? WHERE item_id=? (parameterized `?` only — V5). Produce flags_json via the
         SAME `store.MarshalFlags(flags)` the upsert uses — so an empty flag set stores `"[]"`
         (NOT NULL, NOT `null`) and byte-matches the freshness compare; the row counts as
         backfilled and is not re-scanned next boot.
      3. slog.Info("item flags backfill", "scanned", scanned, "updated", updated) — counts only,
         never raw content (V7). Do all UPDATEs in one tx (the table is small — held items only,
         pre-P38).
    Wire it in `cmd/squirebot-server/main.go` immediately after `migrations.RunMigrations(db)`
    (around :226): `if scanned, updated, err := store.NewStore(db).BackfillItemFlags(ctx?); err != nil
    { slog.Error("serve: item flags backfill failed", "err", err); ... }` — NON-FATAL is acceptable
    (log + continue; the weekly freshness pass will heal any row the backfill missed) OR fatal like
    RunMigrations — choose log-and-continue so a backfill hiccup never blocks serving. Use
    context.Background() (the signal-tied ctx is created later at :230; the backfill is a fast local
    pass — pass context.Background()).
  </action>
  <verify>
    <automated>cd internal/backendsrv && go build ./... && go test ./store/ -run "Backfill|ItemFlags" -count=1 -v && cd ../.. && go build ./cmd/squirebot-server/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'func .*BackfillItemFlags' internal/backendsrv/store/backfill.go` exists.
    - `grep -n 'func DeriveFlagsAndEffects' internal/backendsrv/enrich/wikiitem.go` exists and is called from BOTH ParseItempage and BackfillItemFlags (`grep -rn 'DeriveFlagsAndEffects' internal/backendsrv/` shows ≥ 2 call sites).
    - `grep -rn 'MarshalFlags(' internal/backendsrv/` shows ≥ 3 call sites (the upsert/job literal, the backfill, and the job freshness compare) — all flags_json values go through the one canonical helper (D-06).
    - `grep -n 'BackfillItemFlags' cmd/squirebot-server/main.go` shows the call after RunMigrations.
    - A test in store/backfill_test.go seeds an item_master row (via UpsertItemMasterTx) whose stored statsblock is the CLEANED, NO-BRACKET form INCLUDING an Effect line — `statsblock="MAGIC ITEM\nSlot: PRIMARY\nEffect: Shock of Frost (Click from Inventory)\nClass: ALL"` — with flags_json left NULL, runs BackfillItemFlags, and asserts the row now has is_magic=1, is_clicky=1, clicky_effect="Shock of Frost" (proving the no-bracket cleaned Effect line classifies AND extracts the clicky name — the exact field SEARCH-04 needs, D-05), and flags_json containing "MAGIC ITEM"; a SECOND BackfillItemFlags call reports updated=0 (idempotent).
    - A second backfill_test case seeds a Haste/no-Effect row (`statsblock="MAGIC ITEM\nSlot: BACK\nHaste: +36%\nClass: ALL"`) and asserts has_haste=1, haste_pct=36, is_clicky=0 after backfill (the haste-positive / clicky-negative path).
    - `go test ./store/ -run "Backfill|ItemFlags" -count=1` exit 0; `go build ./cmd/squirebot-server/` exit 0.
  </acceptance_criteria>
  <done>BackfillItemFlags re-parses stored (cleaned, bracket-stripped) statsblocks with no network — including correctly classifying + naming a CLICKY from the no-bracket Effect line — runs once at boot, is idempotent (empty→"[]" via the shared MarshalFlags), and shares one derivation with the live parser.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the new fields through the weekly wiki job + the self-healing freshness re-parse</name>
  <read_first>
    - internal/backendsrv/enrich/jobs/wiki.go (upsertItemAndQuests :220-272 — the freshness compare :227-237 + the store.ItemMaster literal :239-250)
    - internal/backendsrv/enrich/jobs/wiki_test.go (TestRunWiki_BackfillsStaleIcon :196-237 — the icon-backfill regression to MIRROR for a flags-backfill regression)
    - internal/backendsrv/store/enrich.go (the Task-1 extended GetItemMasterFreshnessTx signature + the canonical store.MarshalFlags helper)
  </read_first>
  <action>
    In `enrich/jobs/wiki.go::upsertItemAndQuests`:
    - The freshness compare (:227-237) now also receives the stored flags_json from the Task-1
      extended GetItemMasterFreshnessTx. Compute the freshly-parsed flags_json by calling
      `store.MarshalFlags(item.Flags)` (the SAME canonical helper the upsert + backfill use — NOT a
      local json.Marshal, so an empty flag set produces the identical `"[]"` and a flagless row is
      NOT re-written every pass; D-06 idempotency) into a local `parsedFlagsJSON`. Change the
      skip-condition to ALSO require `existingFlagsJSON == parsedFlagsJSON` — so a row written
      before 00016 (NULL/"" flags_json) is NOT skipped and re-writes to backfill (the exact
      00012-icon argument, one more field). Update the inline comment to add flags_json to the
      "SHA-1 alone is not sufficient" list.
    - In the store.ItemMaster literal (:239-250), carry the new fields from `item`:
      IsLore: item.IsLore, IsNoDrop: item.IsNoDrop, IsMagic: item.IsMagic, IsTemporary:
      item.IsTemporary, IsClicky: item.IsClicky, ClickyEffect: item.ClickyEffect, HasHaste:
      item.HasHaste, HastePct: item.HastePct, FlagsJSON: parsedFlagsJSON.
    Add a regression test in `wiki_test.go` mirroring TestRunWiki_BackfillsStaleIcon:
    `TestRunWiki_BackfillsStaleFlags` — seed an item_master row with the correct wikitext SHA-1 +
    icon + statsblock but NULL flags_json (simulating a pre-00016 row), clear etag_cache so the
    page re-fetches, run RunWiki against an httptest fetcher serving a flagged item, and assert the
    row's flags_json / is_magic are now populated even though the SHA-1 was unchanged.
  </action>
  <verify>
    <automated>cd internal/backendsrv && go test ./enrich/... ./store/... ./migrations/ -count=1 && cd ../.. && go build ./... && go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'FlagsJSON\|parsedFlagsJSON\|item.IsClicky\|item.HasHaste' internal/backendsrv/enrich/jobs/wiki.go` shows the new fields carried into the store literal AND the freshness compare extended.
    - `grep -n 'MarshalFlags(' internal/backendsrv/enrich/jobs/wiki.go` ≥ 1 — the job computes parsedFlagsJSON via the canonical helper (NOT a local json.Marshal), so the freshness byte-compare is deterministic (D-06).
    - The skip-condition line in upsertItemAndQuests references the stored flags_json (the row is re-written when flags_json differs) — `grep -n 'existingFlags\|parsedFlagsJSON' internal/backendsrv/enrich/jobs/wiki.go` ≥ 1.
    - `grep -n 'func TestRunWiki_BackfillsStaleFlags' internal/backendsrv/enrich/jobs/wiki_test.go` exists.
    - `go test ./... -count=1` exit 0 from the repo root (the whole-module green gate, success criterion 4).
    - `go build ./... ` exit 0.
  </acceptance_criteria>
  <done>The weekly job writes the new fields (flags_json via the one canonical MarshalFlags), a pre-00016 row self-heals on the next pass, a flagless row is idempotent across passes, and the whole module (go test ./...) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed wiki values → SQLite | The flag/effect strings (clicky_effect, flags_json) are UNTRUSTED parsed wiki text written into item_master. |
| stored statsblock → backfill re-parse | The stored statsblock is UNTRUSTED text re-parsed at boot — a malformed block must not panic the backfill. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Tampering (SQL injection) | itemMasterUpsert, BackfillItemFlags UPDATE | mitigate | ALL values bound through `?` placeholders (the existing V5 convention, enrich.go:23-24); parsed names/flags are NEVER string-concatenated into SQL. The migration is STATIC DDL (no interpolation). |
| T-37-05 | Denial of Service / panic | BackfillItemFlags re-parsing stored statsblock | mitigate | Re-uses the SAME bounded DeriveFlagsAndEffects (split-on-separator, no nested-quantifier regex); nil/empty statsblock rows are filtered by the SELECT (`statsblock != ''`); per-row derivation is bounded. A single bad row is logged + skipped, not fatal (mirrors the wiki job's log-and-continue). |
| T-37-06 | Information disclosure | slog in backfill + job | mitigate | slog logs counts/item_id/err ONLY — never raw statsblock/flag content (V7, enrich.go:23-24). |
| T-37-07 | Information disclosure | clicky_effect / flags_json at render | accept (flag for P40) | These columns are stored, not yet rendered; they MUST be escaped at render time (Phase 40 ITEMUI concern). No rendering surface exists this phase — noted, no action. |
</threat_model>

<verification>
- `go build ./... && go vet ./internal/backendsrv/... && go test ./...` all green (the whole-module gate is success criterion 4).
- `grep -c 'WatcherMaxSchemaVersion' internal/sheet/client.go` is unchanged — the watcher write path is NOT touched (read-only additive backend columns; D-06 / CONTEXT carried-forward).
- The migration is purely additive (`ADD COLUMN`, nullable, no DEFAULT) — existing reads are unaffected (00012/00013 precedent) — and TestMigrate_00016 proves all nine columns land.
- The backfill performs ZERO network calls (re-parses stored statsblock TEXT only — D-05) and correctly extracts a clicky name from the no-bracket cleaned Effect line.
- flags_json is produced by ONE canonical store.MarshalFlags (empty→"[]") at the upsert, backfill, and freshness-compare sites — a flagless item is written once, not every pass (D-06).
</verification>

<success_criteria>
- The four flag booleans + is_clicky/clicky_effect + has_haste/haste_pct + flags_json are discrete, individually queryable columns on item_master (ENRICH-12 + ENRICH-13).
- Every already-enriched row is backfilled from its stored statsblock at boot with no wiki refetch (D-05) — including correctly classifying + naming a CLICKY from the cleaned, bracket-stripped Effect line — and the freshness short-circuit re-parses any row missing the new fields on the weekly pass (D-06), without re-writing a flagless row every pass.
- `go test ./...` and `go build ./...` are green; the migration is extend-only (00016) with a TestMigrate_00016 column assertion; the watcher is untouched and no `v*` tag is implied.
</success_criteria>

<output>
After completion, create `.planning/phases/37-item-enrichment-backbone-flags-effects/37-02-SUMMARY.md`
(use the summary template). Record the final column names, the canonical MarshalFlags contract
(empty→"[]"), the backfill idempotency key (flags_json IS NULL), and the freshness signal (flags_json
compare via MarshalFlags) for the downstream P39 facets + P40 outlines.
</output>
</output>
