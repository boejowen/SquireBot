---
phase: 15-admin-web-forms-login
plan: 04
type: execute
wave: 4
depends_on: ["15-02"]
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/__tests__/api.test.ts
  - web/src/lib/auth.ts
  - web/src/lib/__tests__/auth.test.ts
  - web/src/app.css
  - web/src/lib/components/AuthGate.svelte
  - web/src/lib/components/AuthGate.test.ts
  - web/src/lib/components/LoginScreen.svelte
  - web/src/lib/components/NotMemberScreen.svelte
  - web/src/lib/components/SessionIndicator.svelte
  - web/src/lib/components/ConfirmDialog.svelte
  - web/src/lib/components/ConfirmDialog.test.ts
  - web/src/lib/components/SiteShell.svelte
  - web/src/routes/+layout.svelte
autonomous: true
requirements: [AUTH-08, AUTH-09]
must_haves:
  truths:
    - "On load the app resolves the session via the whoami-web endpoint and shows LoginScreen when unauthenticated"
    - "An authenticated non-member sees the NotMemberScreen refusal (AUTH-08) with no views"
    - "An authenticated member sees the full app; the shell shows the Discord avatar+username and a Sign out control"
    - "All API calls send credentials:'include' so the session cookie rides cross-subdomain"
    - "api.ts maps HTTP 401 to a typed Unauthenticated error and 403 to a typed Forbidden error"
    - "A 401 from ANY call (mount or mid-session) drops auth state → LoginScreen; a 403 routes to the matching refusal (NotMember / Officers-only), never a stale authorized view"
    - "The shell renders an Admin nav entry ONLY when the session is an officer"
  artifacts:
    - path: "web/src/lib/api.ts"
      provides: "credentials:'include' on every call + 401→Unauthenticated / 403→Forbidden typed errors carrying the server {error} code"
      contains: "credentials"
    - path: "web/src/lib/auth.ts"
      provides: "Session type, fetchSession (whoami-web), loginUrl/logoutUrl, the session store + the onUnauthenticated/onForbidden handlers AuthGate uses"
    - path: "web/src/lib/components/AuthGate.svelte"
      provides: "load-time gate → LoginScreen / NotMemberScreen / auth-loading / app; catches Unauthenticated/Forbidden mid-session and re-routes"
    - path: "web/src/lib/components/LoginScreen.svelte"
      provides: "Sign in with Discord screen"
      contains: "Sign in with Discord"
    - path: "web/src/lib/components/ConfirmDialog.svelte"
      provides: "shared themed accessible destructive-confirm modal (for 15-05)"
  key_links:
    - from: "web/src/lib/components/AuthGate.svelte"
      to: "web/src/lib/api.ts typed errors"
      via: "catches Unauthenticated/Forbidden and re-routes (server-truth)"
      pattern: "Unauthenticated|Forbidden"
    - from: "AuthGate"
      to: "whoami-web session resolution"
      via: "fetchSession on mount"
      pattern: "whoami-web|fetchSession"
---

<objective>
Turn the public P14 site into a members-only app: a whole-site Discord login gate (D-01) wrapping the shell, the login/not-member/session-indicator surfaces, credentialed API calls so the session cookie rides cross-subdomain, and the shared accessible `ConfirmDialog` the 15-05 forms reuse. Frontend gating is UX only — the API (15-02/15-03) is the real gate; the client treats a 401/403 from the server as authoritative (UI-SPEC "auth state is server-truth") via typed errors. Extends the P14 design system verbatim (tokens, SiteShell, StateBlock, ThemePicker) and adds the `--destructive` token (= per-theme `--status-missing`).

Purpose: AUTH-08/09 on the frontend — the visible login flow + the session identity in the shell, reacting correctly to server 401/403. Pins the `Session` shape + the accessible `ConfirmDialog` that 15-05's three forms consume.
Output: `web/src/lib/api.ts` (typed errors) + `web/src/lib/auth.ts` + the AuthGate/LoginScreen/NotMemberScreen/SessionIndicator/ConfirmDialog components + SiteShell/layout extensions + the `--destructive` token.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-admin-web-forms-login/15-CONTEXT.md
@.planning/phases/15-admin-web-forms-login/15-UI-SPEC.md
@.planning/phases/15-admin-web-forms-login/15-02-SUMMARY.md

<interfaces>
<!-- The backend auth contract (15-02) + the P14 frontend conventions to mirror. Extracted from the codebase. -->

Backend auth endpoints (15-02, on https://api.squirebot.quest — the API origin, NOT the apex site origin):
- GET  /api/v1/auth/login        → 302 to Discord (start the flow; the browser navigates here directly, NOT fetch).
- GET  /api/v1/auth/callback     → backend handles, redirects to the site (`/` or `/?not_member=1`).
- GET  /api/v1/auth/whoami-web   → 200 {authenticated:bool, isMember?:bool, isOfficer?:bool, username?:string, avatar?:string|null, discord_user_id?:string}. authenticated:false for no/expired session.
- POST /api/v1/auth/logout       → 204 (then navigate to `/`).
- The read endpoints (/api/v1/meta, /api/v1/views/*) now require the session cookie (401 without it); officer-only writes (15-03) return 403 for a member session.
- CORS allows the exact site origin + Access-Control-Allow-Credentials:true — so the browser sends the cookie ONLY when the request uses credentials:'include'.

P14 frontend conventions (web/src/) to mirror:
- api.ts (web/src/lib/api.ts) — API_BASE from `env.PUBLIC_API_BASE` ($env/dynamic/public, default https://api.squirebot.quest); getJSON<T>(path, fetchFn=fetch) throws a branded ApiError on non-2xx/transport/malformed-JSON. THIS PLAN edits getJSON's fetch options to add `credentials: 'include'` so every read carries the cookie, and maps status 401→a typed Unauthenticated subclass + 403→a typed Forbidden subclass.
- Components are Svelte 5 runes ($props/$state/$derived/$effect); the project forces runes (svelte.config.js). +layout.svelte owns the [data-theme] + wraps children in SiteShell (bind:theme). +page.svelte fetches all 5 endpoints onMount and renders loading/error/ready via StateBlock.
- StateBlock.svelte (web/src/lib/components/) has kinds empty|view-empty|error|loading|no-results|no-coin — EXTEND with auth-loading + an officers-only refusal kind (or reuse error/empty with copy). Copy from UI-SPEC Copywriting Contract verbatim.
- Tokens (web/src/app.css): per-theme [data-theme] blocks define --bg --panel --text --accent --status-ok --status-missing --status-other --font-display --font-body --weight-display (+ --row-bg/--row-text for heavy). P14 left --destructive UNSET — THIS PLAN adds `--destructive: var(--status-missing)` inside EACH [data-theme] block (incl. :root/velious) so the destructive buttons are theme-consistent (UI-SPEC § Color). Heavy caveat: heavy's --status-missing is dark (#6b1a1a) → the destructive button on Heavy uses accent-colored text, not --bg text (UI-SPEC contrast note).
- Icons: @lucide/svelte (e.g. `import LogIn from '@lucide/svelte/icons/log-in'`). Already a dep. No new UI dependency (UI-SPEC Registry Safety).
- npm scripts: `npm run check` (svelte-check typecheck), `npm run build` (vite build → 200.html), `npm run test:unit -- --run` (vitest).

UI-SPEC copy (verbatim — Copywriting Contract):
- LoginScreen purpose: `Sign in with Discord to see the guild's inventory, gear, and bank.` · footnote: `You must be a member of the guild's Discord server.` · button: `Sign in with Discord` · in-flight: `Redirecting…`
- NotMemberScreen heading: `You're not in the guild` · body: `This site is for members of the guild's Discord server. Join the guild Discord, then sign in again.` · action: `Sign in as someone else`
- Auth-loading: `Checking your access…` · Sign-out control: `Sign out`
- Officers-only refusal (direct nav to /admin as non-officer): heading `Officers only` · body `This area is for guild officers. If you think you should have access, ask an officer to add you.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: auth.ts — session model + credentialed fetch + 401→Unauthenticated / 403→Forbidden typed errors</name>
  <read_first>
    - web/src/lib/api.ts (the API_BASE + getJSON + ApiError pattern to extend with credentials:'include' + the typed 401/403 subclasses)
    - web/src/lib/__tests__/api.test.ts (the injectable fetchFn test seam to mirror)
    - .planning/phases/15-admin-web-forms-login/15-UI-SPEC.md § "Information Architecture & Authorization" + § "Accessibility & Interaction Notes" (the whoami session shape + the server-truth 401→LoginScreen / 403→refusal contract)
    - .planning/phases/15-admin-web-forms-login/15-CONTEXT.md § D-01, D-03, D-05
  </read_first>
  <behavior>
    - Every API call sends credentials:'include' (the session cookie rides cross-subdomain).
    - A 401 response throws Unauthenticated; a 403 response throws Forbidden; both are typed (instanceof-checkable) and carry the server's {error} code so the caller can distinguish not-member vs not-officer.
    - fetchSession parses the whoami-web JSON into a typed Session; a non-2xx or transport error resolves to ANON (fail-safe to logged-out, never throws — the gate treats "can't resolve" as unauthenticated).
    - loginUrl()/logoutUrl() return the API_BASE auth routes.
  </behavior>
  <action>
Edit `web/src/lib/api.ts`: (a) add `credentials: 'include'` to the fetch options inside `getJSON` (every read carries the cookie); (b) define typed error subclasses `class Unauthenticated extends ApiError` (status 401) and `class Forbidden extends ApiError` (status 403), each carrying the parsed `{"error":"<code>"}` value on a `code?` field; in getJSON's non-2xx branch, `if (res.status === 401) throw new Unauthenticated(...)`, `if (res.status === 403) throw new Forbidden(..., code)`, else the existing ApiError. Export both subclasses. Keep the injectable `fetchFn` seam + the ApiError contract intact.

Create `web/src/lib/auth.ts`:
- `export interface Session { authenticated: boolean; isMember: boolean; isOfficer: boolean; username: string; avatar: string | null; discordUserId: string; }`.
- `export const ANON: Session = { authenticated: false, isMember: false, isOfficer: false, username: '', avatar: null, discordUserId: '' };`.
- `export function loginUrl(): string` → `${API_BASE}/api/v1/auth/login`; `export function logoutUrl(): string` → `${API_BASE}/api/v1/auth/logout` (import API_BASE from ./api).
- `export async function fetchSession(fetchFn: typeof fetch = fetch): Promise<Session>` — GET `${API_BASE}/api/v1/auth/whoami-web` with `credentials:'include'`; on a 2xx, map the JSON ({authenticated,isMember,isOfficer,username,avatar,discord_user_id}) into Session (snake→camel; missing → defaults); on ANY error/non-2xx return ANON (fail-safe logged-out; never throw).
- `export async function logout(fetchFn: typeof fetch = fetch): Promise<void>` — POST logoutUrl() with credentials:'include'.
- A small reactive session holder is fine, but keep auth.ts framework-light (pure functions + types) so it's vitest-testable. Optionally add `classifyAuthError(err): 'unauthenticated' | 'not_member' | 'officer_refused' | null` mapping a caught Unauthenticated → 'unauthenticated' and a Forbidden → 'not_member' (if the code indicates not-a-member) else 'officer_refused' — the gate uses this.

Write `web/src/lib/__tests__/api.test.ts` additions: a 401 response throws Unauthenticated; a 403 throws Forbidden (with the body code attached); a 200 returns the parsed body; credentials:'include' is passed.
Write `web/src/lib/__tests__/auth.test.ts` (inject a stub fetch): whoami-web 200 authenticated member → Session with isMember/isOfficer mapped; whoami-web 200 {authenticated:false} → ANON; a 500/thrown fetch → ANON (fail-safe); credentials:'include' is sent; classifyAuthError maps Unauthenticated/Forbidden correctly.
  </action>
  <verify>
    <automated>cd web && npm run test:unit -- --run src/lib/__tests__/api.test.ts src/lib/__tests__/auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "credentials: 'include'" web/src/lib/api.ts` and `grep -q "credentials: 'include'" web/src/lib/auth.ts`
    - B-2: `grep -q "401" web/src/lib/api.ts && grep -q "403" web/src/lib/api.ts && grep -q "Unauthenticated" web/src/lib/api.ts && grep -q "Forbidden" web/src/lib/api.ts` (both auth statuses mapped to the two named typed errors)
    - B-2: `grep -q "Unauthenticated" web/src/lib/__tests__/api.test.ts && grep -q "Forbidden" web/src/lib/__tests__/api.test.ts` (tests assert a 401 throws Unauthenticated and a 403 throws Forbidden)
    - `web/src/lib/auth.ts contains 'whoami-web'` and `grep -q 'export interface Session' web/src/lib/auth.ts && grep -q 'isOfficer' web/src/lib/auth.ts`
    - auth.test.ts proves a failed/500 whoami resolves to ANON (fail-safe) and credentials:'include' is sent
    - `cd web && npm run check` passes (0 errors)
  </acceptance_criteria>
  <done>api.ts sends credentials and throws typed Unauthenticated (401) / Forbidden (403) errors (both exported + tested); auth.ts exposes a typed Session + fail-safe fetchSession + login/logout URLs.</done>
</task>

<task type="auto">
  <name>Task 2: --destructive token + accessible ConfirmDialog (UI-SPEC a11y) + login/not-member/session components</name>
  <read_first>
    - web/src/app.css (the [data-theme] token blocks — add --destructive to each; note heavy's dark --status-missing)
    - web/src/lib/components/StateBlock.svelte (the kind-switch + copy + .retry button styling to mirror for the new screens + the auth-loading/officers-only kinds)
    - web/src/lib/components/ItemTooltip.svelte (the Esc/outside-dismiss + focus pattern to mirror for ConfirmDialog)
    - web/src/lib/components/ThemePicker.svelte (the accent control + 44px target styling to mirror for buttons/menus)
    - .planning/phases/15-admin-web-forms-login/15-UI-SPEC.md § "Login & Auth Surfaces" + "ConfirmDialog" + "Accessibility & Interaction Notes" + "Copywriting Contract" (verbatim strings) + § "Color" (--destructive = --status-missing; Heavy caveat)
  </read_first>
  <behavior>
    - --destructive resolves to each theme's --status-missing (one per [data-theme] block incl. :root/velious).
    - LoginScreen renders the wordmark + the exact purpose line + a "Sign in with Discord" button that navigates to loginUrl() (with a "Redirecting…" in-flight state) + the footnote.
    - NotMemberScreen renders the exact heading/body + a "Sign in as someone else" button (→ loginUrl()).
    - SessionIndicator renders the avatar (Discord CDN url or a user-glyph fallback) + username + a Sign out control (→ logout() then navigate to /); an officer shows a shield badge.
    - ConfirmDialog (UI-SPEC § ConfirmDialog + § Accessibility): role="dialog", aria-modal="true", labelled by its heading; on open focus moves to the CANCEL button (the destructive confirm is NEVER the default-focused element); Esc / backdrop-click / Cancel all dismiss with no side effect; only the destructive button commits; focus is trapped within the dialog and restored to the trigger on close; reduced-motion → instant; color is never the only signal (icon + text).
  </behavior>
  <action>
1) `web/src/app.css`: inside EVERY `[data-theme]` block (the `:root,[data-theme="velious"]` block + vanilla + kunark + minimalist + heavy) add `--destructive: var(--status-missing);`. (One line per block; theme-consistent destructive color per UI-SPEC § Color.)

2) `web/src/lib/components/LoginScreen.svelte`: a centered card (max-width ~420px, `--panel`/`--border`, lg padding). Contents in order: wordmark (Display 28px, `--accent`, `--font-display`) → purpose line (Body 16px): `Sign in with Discord to see the guild's inventory, gear, and bank.` → a primary button (accent fill, `--bg` text, `log-in` lucide glyph, 44px min-height, full width) labeled `Sign in with Discord` that on click sets an in-flight state (label `Redirecting…`, disabled) then `window.location.href = loginUrl()` → a footnote (small, muted): `You must be a member of the guild's Discord server.` Honor prefers-reduced-motion (no entrance animation).

3) `web/src/lib/components/NotMemberScreen.svelte`: same centered card; a `shield-alert` lucide glyph in `--status-missing`; heading (20px) `You're not in the guild`; body (16px) `This site is for members of the guild's Discord server. Join the guild Discord, then sign in again.`; a primary button `Sign in as someone else` → `window.location.href = loginUrl()`.

4) `web/src/lib/components/SessionIndicator.svelte`: props `{ session: Session }`. Right-aligned: the Discord avatar (`<img>` from `https://cdn.discordapp.com/avatars/${discordUserId}/${avatar}.png` when avatar set, else a `user` lucide glyph in a `--panel` circle; `alt` = username) + username (Label 13px); if `session.isOfficer` a `shield` glyph in `--accent` precedes the username with an accessible label "Officer". A Sign out affordance labeled `Sign out` (`log-out` glyph) → `await logout(); window.location.href = '/'`. 44px min target; accent focus ring; menu (if used) dismisses on Esc/outside-click. Escape interpolated username via plain `{}` — never `{@html}`.

5) `web/src/lib/components/ConfirmDialog.svelte` (shared, used by 15-05): props `{ open: boolean; heading: string; body: string; confirmLabel: string; onConfirm: () => void; onCancel: () => void }`. A centered modal over a dimmed backdrop (`color-mix(in srgb, var(--bg) 70%, transparent)`); modal surface `--panel`/`--border`, lg padding, max-width ~440px; heading (20px) + a `triangle-alert` glyph in `--destructive` + body (16px) + an action row: a destructive confirm button (`--destructive` fill, `--bg` text — on Heavy use accent-colored text per the contrast caveat) labeled `confirmLabel`, and a neutral Cancel (ghost, `--text`). Implement the a11y contract exactly: `role="dialog"` + `aria-modal="true"` + `aria-labelledby` the heading id; on open, move focus to the **Cancel** button (the destructive confirm is NEVER default focus); handle `Escape` (→ onCancel), backdrop-click (→ onCancel), and Cancel (→ onCancel); trap focus within the dialog and restore it to the trigger on close. Reduced-motion → instant. All text via plain `{}` (escaped). Color is never the only signal (the triangle-alert icon + the explicit confirmLabel carry the destructive meaning alongside the color).

Write `web/src/lib/components/ConfirmDialog.test.ts` (vitest + @testing-library/svelte): asserts the dialog has role="dialog" + aria-modal="true"; on open the Cancel button is focused (NOT the destructive confirm); pressing Escape fires onCancel; a backdrop click fires onCancel. (A full focus-trap test is a bonus, not required.)
  </action>
  <verify>
    <automated>cd web && npm run test:unit -- --run src/lib/components/ConfirmDialog.test.ts && npm run check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--destructive: var(--status-missing)' web/src/app.css` returns 5 (one per theme block)
    - `web/src/lib/components/LoginScreen.svelte contains 'Sign in with Discord'` and `contains 'You must be a member of the guild'`
    - `web/src/lib/components/NotMemberScreen.svelte contains "You're not in the guild"`
    - W-5: `grep -q 'role="dialog"' web/src/lib/components/ConfirmDialog.svelte && grep -q 'aria-modal="true"' web/src/lib/components/ConfirmDialog.svelte`
    - W-5: `grep -Eq "Escape|keydown" web/src/lib/components/ConfirmDialog.svelte` (Esc dismiss) AND `grep -q "focus" web/src/lib/components/ConfirmDialog.svelte` (Cancel focused on open)
    - W-5: ConfirmDialog.test.ts asserts role + aria-modal + Cancel-focused-on-open + Escape-dismiss (focus-trap component test is a bonus)
    - `! grep -q '@html' web/src/lib/components/SessionIndicator.svelte` (usernames escaped, XSS mitigation)
    - `cd web && npm run check` passes; `npm run build` emits the static site
  </acceptance_criteria>
  <done>The --destructive token + the four auth components + the accessible shared ConfirmDialog (role=dialog, aria-modal, Cancel focused on open, Esc/backdrop dismiss) exist, use the exact copy, and typecheck/build clean.</done>
</task>

<task type="auto">
  <name>Task 3: AuthGate (server-truth 401/403 routing) + wire into the layout + officer-only Admin nav</name>
  <read_first>
    - web/src/routes/+layout.svelte (the SiteShell wrap + [data-theme] ownership to extend with AuthGate)
    - web/src/lib/components/SiteShell.svelte ($bindable theme + the header layout to extend with SessionIndicator + the officer-only Admin nav entry)
    - web/src/routes/+page.svelte (the onMount load + StateBlock loading/error/ready pattern + the existing tab nav to mirror for the Admin entry)
    - web/src/lib/auth.ts (Task 1 — fetchSession, Session, classifyAuthError) + web/src/lib/api.ts (the Unauthenticated/Forbidden typed errors) + the LoginScreen/NotMemberScreen/SessionIndicator (Task 2)
    - .planning/phases/15-admin-web-forms-login/15-UI-SPEC.md § "Information Architecture & Authorization" + § "Accessibility & Interaction Notes" (auth state is server-truth: 401→Login, 403→matching refusal; resolution order; Admin nav only when isOfficer)
  </read_first>
  <behavior>
    - On mount AuthGate resolves the session (fetchSession): while pending → an auth-loading StateBlock (`Checking your access…`); authenticated:false → LoginScreen; authenticated && !isMember → NotMemberScreen; authenticated && isMember → renders the app (children).
    - SERVER-TRUTH (B-2, UI-SPEC): AuthGate catches the typed errors from api.ts on ANY descendant call (mid-session, not just at mount) and re-routes — never leaving the user on a stale authorized view with dead buttons:
      - Unauthenticated (401) → DROP all client auth state and show LoginScreen.
      - Forbidden (403) → show the MATCHING refusal: NotMemberScreen if the code indicates not-a-member, else the Officers-only refusal. Do NOT cache an "I'm an officer" bit past a 403 — trust the server.
    - The session is provided to descendants (context) so SiteShell shows the SessionIndicator + conditionally the Admin nav (only when isOfficer).
    - If the callback bounced back with ?not_member=1, NotMemberScreen shows directly.
  </behavior>
  <action>
1) `web/src/lib/components/AuthGate.svelte`: props `{ children: Snippet }`. Hold `let session = $state<Session | null>(null)` (null = loading) and a `let override = $state<'login'|'not_member'|'officer_refused'|null>(null)`. `onMount(async () => { session = await fetchSession(); })`. Provide both the session and a `guard` helper to descendants via Svelte context (`setContext('session', () => session)` + `setContext('authGuard', (err) => {...})`). The guard helper: given a caught error, if `err instanceof Unauthenticated` → set session=ANON + override='login' (→ LoginScreen); if `err instanceof Forbidden` → set override='not_member' when the code indicates not-a-member else 'officer_refused' (→ NotMemberScreen / Officers-only refusal); never leave the authorized view rendered. Render by precedence: `override==='login' || (session && !session.authenticated)` → `<LoginScreen />`; `override==='not_member' || (session?.authenticated && !session.isMember)` → `<NotMemberScreen />`; `override==='officer_refused'` → `<StateBlock kind="officers-only" />`; `session===null` → `<StateBlock kind="auth-loading" />`; else → `{@render children()}`. Also read `?not_member=1` and treat it as an early NotMember hint.

2) Edit `web/src/routes/+layout.svelte`: wrap the SiteShell+children in `<AuthGate>` (the [data-theme] root stays outermost so the themed shell shows on the login/not-member screens too).

3) Edit `web/src/lib/components/SiteShell.svelte`: read the session from context. In the header (after ThemePicker) render `<SessionIndicator session={session} />` when `session?.authenticated`. Add an "Admin" nav affordance (a link/button to `/admin`, styled like the existing `.tab`) that renders ONLY when `session?.isOfficer` — Layer-1 UX suppression (the server is the real gate). On the pre-auth screens render neither.

4) Add `StateBlock` kind `auth-loading` (copy `Checking your access…`, a spinner like the loading kind) and an `officers-only` kind (heading `Officers only`, body `This area is for guild officers. If you think you should have access, ask an officer to add you.`).

5) Wire the typed-error contract at the existing call site: in `web/src/routes/+page.svelte`'s catch, call the `authGuard` helper from context with the caught error (so a 401 → LoginScreen, a 403 → the matching refusal) instead of always rendering the generic error StateBlock.

Write `web/src/lib/components/AuthGate.test.ts` (vitest + @testing-library/svelte): a 401 (Unauthenticated) passed to the guard routes to LoginScreen; a 403 (Forbidden) with a not-member code routes to NotMemberScreen; a 403 with a not_authorized code routes to the Officers-only refusal (never the stale authorized view).
Keep the existing 60 web tests green (`npm run test:unit -- --run`).
  </action>
  <verify>
    <automated>cd web && npm run check && npm run test:unit -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/components/AuthGate.svelte contains 'LoginScreen'` and `contains 'NotMemberScreen'`
    - `grep -q 'fetchSession' web/src/lib/components/AuthGate.svelte`
    - B-2: `grep -q 'Unauthenticated' web/src/lib/components/AuthGate.svelte && grep -q 'Forbidden' web/src/lib/components/AuthGate.svelte` (gate catches BOTH typed errors) AND `grep -Eq 'NotMember|officers-only|Officers' web/src/lib/components/AuthGate.svelte` (403 → matching refusal)
    - B-2: `grep -Eq 'Unauthenticated|401' web/src/lib/components/AuthGate.test.ts && grep -Eq 'Forbidden|403' web/src/lib/components/AuthGate.test.ts` (test covers both mid-session routes)
    - `grep -q 'AuthGate' web/src/routes/+layout.svelte`
    - `grep -q 'isOfficer' web/src/lib/components/SiteShell.svelte` (officer-only Admin nav) and `grep -q 'SessionIndicator' web/src/lib/components/SiteShell.svelte`
    - `web/src/lib/components/StateBlock.svelte contains 'Checking your access'` and `contains 'Officers only'`
    - `cd web && npm run check` passes; `npm run test:unit -- --run` all green; `npm run build` emits the static site
  </acceptance_criteria>
  <done>The whole site is login-gated: auth-loading → Login/NotMember/app; a mid-session 401 drops auth state → LoginScreen and a 403 routes to NotMemberScreen or the Officers-only refusal (never a stale authorized view); the shell shows the session identity + an officer-only Admin nav.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM ← Discord username/avatar | user-controlled strings rendered in the shell + screens |
| frontend gate vs API gate | the client gate is UX-only; the server (15-02/15-03) is the authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-22 | XSS | interpolated Discord usernames | mitigate | Render usernames via Svelte `{}` (auto-escaped); NO `{@html}` on user/Discord data (the only sanctioned {@html} stays ItemTooltip's pre-escaped composeItemNote, untouched here) |
| T-15-23 | Elevation of privilege | officer-only Admin nav | mitigate (UX) + defer to server | Layer 1: the Admin nav + /admin links render only when session.isOfficer (client suppression); Layer 2 (authoritative): the server re-checks officer status on every admin endpoint (15-03) — the frontend treats a 403 as "not an officer" and collapses the Admin UI, never trusting the hidden nav |
| T-15-24 | Spoofing | session cookie exposure to JS | mitigate (inherited) | The cookie is httpOnly (set by 15-02) — JS cannot read it; the client only ever sends credentials:'include' and reads the whoami-web JSON, never the cookie value |
| T-15-25 | Spoofing / tampering | stale authorized UI after server revocation | mitigate | B-2 (UI-SPEC server-truth): api.ts maps 401→Unauthenticated / 403→Forbidden and AuthGate catches them on any mid-session call and re-routes (401→Login, 403→matching refusal); it never leaves an authorized view rendered after the server says no, and never caches an officer bit past a 403 |
</threat_model>

<verification>
- `cd web && npm run check` passes (0 errors); `npm run test:unit -- --run` all green (incl. api/auth/AuthGate/ConfirmDialog tests); `npm run build` emits index.html + 200.html.
- `grep -q "credentials: 'include'" web/src/lib/api.ts web/src/lib/auth.ts`.
- B-2: api.ts maps 401→Unauthenticated / 403→Forbidden and AuthGate routes both (proven by AuthGate.test.ts).
- W-5: ConfirmDialog has role=dialog + aria-modal + Cancel-focus + Esc (proven by ConfirmDialog.test.ts).
- `grep -c -- '--destructive: var(--status-missing)' web/src/app.css` == 5. No `{@html}` on Discord/user data.
</verification>

<success_criteria>
- The site is login-gated: LoginScreen for the unauthenticated, NotMemberScreen for an authed non-member (AUTH-08), the full app for a member.
- api.ts sends credentials and maps 401→Unauthenticated / 403→Forbidden (both exported + tested); a mid-session 401 drops auth state → LoginScreen and a 403 routes to the matching refusal — never a stale authorized view.
- The shell carries the Discord identity (avatar+username, AUTH-09) + Sign out, and an officer-only Admin nav; usernames are auto-escaped.
- The shared accessible ConfirmDialog + the --destructive token exist for 15-05; check/build/tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-web-forms-login/15-04-SUMMARY.md`
</output>
