---
phase: 21-ec-tunnel-auction-monitor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/backendsrv/notify/dm.go
  - internal/backendsrv/notify/dm_test.go
  - internal/backendsrv/wantmatch/match.go
  - internal/backendsrv/wantmatch/match_test.go
autonomous: true
requirements: [WANT-05]

must_haves:
  truths:
    - "notify can send a discordgo rich embed (D-04) through the EXACT SAME two-gate + dedup/cooldown + alert_log path as the plain-string Send — no gate/dedup logic is duplicated or bypassed"
    - "A nil-embed alert still sends as a plain string (the P20 test-alert path is unbroken)"
    - "wantmatch.ForItem carries the want's saved note (D-05 'why you wanted it') so the embed can echo it"
    - "The real *discordgo.Session still satisfies the extended Sender interface (compile-time assertion holds)"
  artifacts:
    - path: "internal/backendsrv/notify/dm.go"
      provides: "ChannelMessageSendEmbed on Sender + Embed field on Alert + embed branch in the send step"
      contains: "ChannelMessageSendEmbed"
    - path: "internal/backendsrv/wantmatch/match.go"
      provides: "Note *string on Hit + note in the ForItem/ForName SELECT + scanHits Scan"
      contains: "Note"
  key_links:
    - from: "notify.Send embed branch"
      to: "discordgo.Session.ChannelMessageSendEmbed"
      via: "a.Embed != nil => s.ChannelMessageSendEmbed(ch.ID, a.Embed)"
      pattern: "ChannelMessageSendEmbed"
    - from: "wantmatch.ForItem"
      to: "wantlist_item.note"
      via: "SELECT ... , note ... + scanHits sql.NullString → Hit.Note"
      pattern: "note"
---

<objective>
Add the rich-embed send path to the P20 `notify` spine (D-04) and carry the want's saved `note` through `wantmatch` (D-05), so Plan 03's EC producer can compose a polished embed and send it through the SAME gates/dedup/alert_log core that every monitor inherits.

Purpose: D-04 needs an embed, but the shipped `notify.Send` only sends a plain string. The embed MUST NOT be sent directly from the EC job (that would bypass both gates + dedup + the inbox write). This plan extends `notify` once so all three monitors gain the embed path. The `note` extension makes "why you wanted it" possible.

Output: `notify/dm.go` (+ test) with an embed-capable send; `wantmatch/match.go` (+ test) with `Note` on `Hit`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-ec-tunnel-auction-monitor/21-CONTEXT.md
@.planning/phases/21-ec-tunnel-auction-monitor/21-RESEARCH.md
@.planning/phases/21-ec-tunnel-auction-monitor/21-PATTERNS.md
@.planning/phases/20-bot-dm-notification-infrastructure/20-CONTEXT.md

<interfaces>
<!-- VERIFIED contracts to extend. Do NOT re-implement the gate/dedup core. -->

notify/dm.go Sender (dm.go:49-52) — extend with ONE method the real session already has:
  type Sender interface {
    UserChannelCreate(userID string, options ...discordgo.RequestOption) (*discordgo.Channel, error)
    ChannelMessageSend(channelID, content string, options ...discordgo.RequestOption) (*discordgo.Message, error)
    // ADD:
    ChannelMessageSendEmbed(channelID string, embed *discordgo.MessageEmbed, options ...discordgo.RequestOption) (*discordgo.Message, error)
  }
  var _ Sender = (*discordgo.Session)(nil)   // must still compile — restapi.go:1812 satisfies it

notify Alert (dm.go:62-69) — ADD an Embed field:
  type Alert struct { WantID *int64; DiscordUserID string; Source string; ItemID *int64; Body string; Detail *string /* ADD: */ ; Embed *discordgo.MessageEmbed }

notify.Send send step (dm.go:163) currently: s.ChannelMessageSend(ch.ID, a.Body)
  → branch: if a.Embed != nil { s.ChannelMessageSendEmbed(ch.ID, a.Embed) } else { s.ChannelMessageSend(ch.ID, a.Body) }
  The 50007 / error / dm_blocked / recordAttempt handling around it is REUSED VERBATIM (dm.go:149-179). Source "ec_auction" already maps to cooldownEC=22h (dm.go:87) and gates monitor_flag.ec / notify_prefs.ec.

wantmatch Hit (match.go:38-44): { WantID int64; DiscordUserID string; ItemID *int64; ItemName string; Reason string }
  wantlist_item.note column EXISTS (migrations/00006_wantlist.sql:27).
  scanHits (match.go:82-102) is shared by ForItem AND ForName — add the note column to BOTH SELECTs and the single Scan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: notify.SendEmbed path — embed via the SAME gate/dedup/alert_log core</name>
  <read_first>
    - internal/backendsrv/notify/dm.go:46-56 (Sender interface + the compile-time assertion), :62-69 (Alert), :108-197 (the Send core — gates :115-146, dedup :137-146, send :149-172, record :174-197)
    - internal/backendsrv/notify/dm_test.go (the existing fake Sender + the Send gate/dedup/50007 test cases to MIRROR)
    - .planning/phases/21-ec-tunnel-auction-monitor/21-PATTERNS.md §"notify/dm.go" (the extend-in-place instructions + the embed shape)
  </read_first>
  <behavior>
    - Test: an Alert with a non-nil Embed and a real WantID, with both gates ON and no recent alert, calls ChannelMessageSendEmbed (NOT ChannelMessageSend) and records a "sent" alert_log row.
    - Test: an Alert with a nil Embed (Body set) still calls ChannelMessageSend — the P20 plain-string path is unchanged.
    - Test: the embed path respects GATE 1 (monitor_flag off → ErrGatedOff, no send), GATE 2 (pref off → ErrGatedOff), and dedup/cooldown (recent sent within cooldownEC → ErrCooledDown, no send) — mirror the existing Send cases with a non-nil Embed.
    - Test: a 50007 on the embed send marks dm_blocked and returns ErrDMBlocked (mirror the existing Send 50007 case).
  </behavior>
  <action>
    Modify `internal/backendsrv/notify/dm.go`:
      1. Add `ChannelMessageSendEmbed(channelID string, embed *discordgo.MessageEmbed, options ...discordgo.RequestOption) (*discordgo.Message, error)` to the `Sender` interface. The `var _ Sender = (*discordgo.Session)(nil)` assertion must still compile (the real session has this method — discordgo@v0.29.0/restapi.go:1812).
      2. Add `Embed *discordgo.MessageEmbed` to the `Alert` struct.
      3. In the SEND step (dm.go:163), branch the actual message call: when `a.Embed != nil` call `s.ChannelMessageSendEmbed(ch.ID, a.Embed)`, else `s.ChannelMessageSend(ch.ID, a.Body)`. Keep ALL surrounding logic identical — the 50007 isDMBlocked check, the recordAttempt("dm_blocked"/"error"/"sent"), and the V7 slog (never log Body or Embed content). Do NOT add a second function with a duplicate gate/dedup block; the single Send core handles both.

    Update the fake Sender in `dm_test.go` to implement ChannelMessageSendEmbed (record the call so a test can assert which path fired). Add TestSendEmbed cases mirroring the existing Send gate/dedup/50007/sent tests, plus the nil-embed-falls-back-to-string regression.

    Keep the existing `Send` signature unchanged (the EC job calls `Send` with `Embed` populated on the Alert — no separate `SendEmbed` func is required; the Embed field on Alert is the seam, per PATTERNS).
  </action>
  <verify>
    <automated>go test ./internal/backendsrv/notify/ -run "TestSend|TestSendEmbed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'ChannelMessageSendEmbed' internal/backendsrv/notify/dm.go` present (interface + send branch).
    - `grep 'Embed \*discordgo.MessageEmbed' internal/backendsrv/notify/dm.go` present (Alert field).
    - `go test ./internal/backendsrv/notify/` exits 0 (existing Send tests still green + new embed tests).
    - `go build ./...` exits 0 (the `var _ Sender = (*discordgo.Session)(nil)` assertion compiles).
    - There is exactly ONE gate/dedup block in dm.go (`grep -c 'GetMonitorFlags' internal/backendsrv/notify/dm.go` returns 1 — no duplicated gate logic).
  </acceptance_criteria>
  <done>An Embed-bearing Alert routes through the existing gates+dedup+alert_log and sends via ChannelMessageSendEmbed; nil-embed still sends a string; the session assertion compiles; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: carry the want's note through wantmatch (D-05 "why you wanted it")</name>
  <read_first>
    - internal/backendsrv/wantmatch/match.go:38-44 (Hit), :51-61 (ForItem SELECT), :63-78 (ForName SELECT), :82-102 (scanHits — the SHARED scan both callers use)
    - internal/backendsrv/migrations/00006_wantlist.sql:20-34 (the wantlist_item columns incl. note at :27 — nullable)
    - internal/backendsrv/wantmatch/match_test.go (the existing ForItem/ForName tests to extend)
  </read_first>
  <behavior>
    - Test: ForItem returns Hit.Note populated for a want that has a saved note; Hit.Note is nil for a want with a NULL note (note is nullable → *string).
    - Test: ForName likewise carries note (the shared scanHits path serves both — one change covers both).
    - Test: existing ForItem/ForName assertions (active=1, muted=0 exclusion, non-nil slice) still hold.
  </behavior>
  <action>
    Modify `internal/backendsrv/wantmatch/match.go`:
      1. Add `Note *string` to the `Hit` struct (nullable — a want may have no note).
      2. Add `note` to BOTH the `ForItem` and `ForName` SELECT column lists (after `reason`): `SELECT id, discord_user_id, item_id, item_name, reason, note FROM wantlist_item WHERE ...`.
      3. In `scanHits` (the single shared scanner), add a `sql.NullString` for note to the Scan and map `note.Valid` → `&note.String` into `h.Note`. Mirror the existing nullable item_id handling.

    Extend `wantmatch/match_test.go` to assert Note is populated/nil across ForItem and ForName, and that the existing exclusion/non-nil-slice behavior is unchanged.

    This is small and additive (a column already present in the schema). It does NOT touch the EC job (Plan 03 reads Hit.Note).
  </action>
  <verify>
    <automated>go test ./internal/backendsrv/wantmatch/</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Note' internal/backendsrv/wantmatch/match.go` present on the Hit struct.
    - Both SELECTs include `note` (`grep -c 'reason, note' internal/backendsrv/wantmatch/match.go` returns 2).
    - `go test ./internal/backendsrv/wantmatch/` exits 0.
    - `go build ./...` exits 0 (no caller of Hit broke).
  </acceptance_criteria>
  <done>Hit carries a nullable Note populated by both ForItem and ForName via the shared scanHits; existing matcher behavior unchanged; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notify → Discord REST (third-party) | the embed/string DM crosses to Discord |
| third-party auction data → embed fields | price/seller/item text composed by Plan 03 flows into the embed Plan 02 sends |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Elevation/Spoofing (ungated DM) | the new embed send path | mitigate | The embed routes through the EXISTING two-gate (monitor_flag + notify_prefs) + the mute gate (wantmatch muted=0) — exactly one gate block in dm.go; no bypass. The EC job MUST set Alert.Embed and call Send, never call ChannelMessageSendEmbed directly. |
| T-21-06 | DoS (DM spam) | dedup/cooldown | mitigate | The embed path uses the SAME RecentAlertExists + cooldownEC=22h dedup; no second code path that could skip it. |
| T-21-07 | Info disclosure | slog in the send step | mitigate | V7 — the send step logs source/want/status only; never the Body or the Embed content (item names beyond ids stay out of logs). |
</threat_model>

<verification>
- `go test ./internal/backendsrv/notify/ ./internal/backendsrv/wantmatch/` green.
- `go build ./...` exits 0; `go vet ./internal/backendsrv/...` exits 0.
- Exactly one gate/dedup block remains in notify/dm.go (no duplication).
</verification>

<success_criteria>
- An Embed-bearing Alert sends a discordgo rich embed through the existing gates/dedup/alert_log (D-04) with zero duplicated gate logic.
- The plain-string P20 path (nil Embed) is unbroken.
- wantmatch.ForItem/ForName carry the want's note (D-05), nullable-safe.
- The real *discordgo.Session still satisfies Sender.
</success_criteria>

<output>
After completion, create `.planning/phases/21-ec-tunnel-auction-monitor/21-02-SUMMARY.md`
</output>
