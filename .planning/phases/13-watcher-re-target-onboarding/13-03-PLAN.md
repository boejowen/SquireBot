---
phase: 13-watcher-re-target-onboarding
plan: 03
type: execute
wave: 2
depends_on: [13-01, 13-02]
files_modified:
  - internal/app/runapp.go
  - internal/app/runapp_test.go
  - internal/app/migrate.go
  - internal/app/migrate_test.go
  - internal/backend/client.go
  - internal/backend/client_test.go
  - cmd/squirebot/main.go
  - cmd/squirebot/build_constants.go
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/tray/tray.go
  - internal/tray/tray_test.go
  - .github/workflows/release.yml
  - go.mod
  - go.sum
deletes:
  - internal/auth/ (entire package)
  - internal/sheet/ (entire package)
  - internal/scaffold/ (entire package)
  - internal/picker/ (entire package)
  - internal/wizard/ (entire package — EQ-folder logic already relocated to internal/onboarding in Plan 02)
  - internal/heartbeat/ (entire package)
  - internal/app/reauth.go
  - internal/app/reauth_test.go
autonomous: true
requirements: [WATCH-08, WATCH-09, WATCH-10, WATCH-11]
user_setup: []

must_haves:
  truths:
    - "On a file change the watcher re-reads + CP1252-decodes once then POSTs the raw UTF-8 content to the backend (no client-side parse.Parse); the fsnotify 500ms debounce + always-re-read survive verbatim"
    - "The re-targeted binary contains NO Google OAuth/PKCE/Sheets/Drive-Picker code: internal/auth, internal/sheet, internal/scaffold, internal/picker, internal/wizard, internal/heartbeat, and internal/app/reauth.go are gone with no importers; go list -deps shows no oauth2/sheets/google.golang.org/api"
    - "First-run onboarding prompts for the guild code via the native dialog, validates it against GET /api/v1/whoami, stores it in DPAPI, collects the EQ folder, and goes green — no browser OAuth anywhere"
    - "On 401 during normal operation the watcher suspends uploads + re-prompts (no silent retry loop); on 426 it surfaces 'update needed'"
    - "An existing v1.x config migrates on first launch: the stale Google wincred entry is deleted, the dead config fields drop, the migration is idempotent + preserves EQFolders + mtime maps"
    - "No Google client secret is baked into the build (the OAuth ldflags are stripped from build_constants.go + release.yml)"
  artifacts:
    - path: "internal/app/runapp.go"
      provides: "watch→read→POST sink (backend client) + guild-code branch + 401/426 handling; no Sheets/OAuth"
      contains: "backend.New"
    - path: "internal/app/migrate.go"
      provides: "first-launch v1→v2 config + wincred migration (WATCH-11)"
      contains: "func MigrateFromV1"
    - path: "cmd/squirebot/build_constants.go"
      provides: "GUTTED build constants — Version + BackendBaseURL only; no OAuth/Picker vars"
      contains: "BackendBaseURL"
    - path: "internal/config/config.go"
      provides: "Config with SpreadsheetID/GoogleEmail dropped; BackendBaseURL added"
      contains: "BackendBaseURL"
  key_links:
    - from: "internal/app/runapp.go"
      to: "internal/backend.Client.Ingest"
      via: "rewritten inventory/spellbook callbacks POST instead of WriteInventory"
      pattern: "\\.Ingest\\("
    - from: "internal/app/runapp.go"
      to: "internal/credstore + internal/onboarding"
      via: "credstore.Read branch + native prompt on absent code or 401"
      pattern: "credstore\\.|onboarding\\."
    - from: "internal/app/migrate.go"
      to: "stale Google wincred entry"
      via: "delete SquireBot:<GoogleEmail> once on first launch"
      pattern: "Delete"
---

<objective>
The integration plan: re-point the watcher's terminal SINK from Google Sheets to the `internal/backend` client (Plan 02), build the native "paste your guild code" onboarding (Plan 02's dialog + Plan 01's `/whoami` validate), wire the WATCH-11 first-launch auto-update migration (delete the stale Google wincred entry + drop dead config fields), DROP the heartbeat, and THEN DELETE the entire Google stack — `internal/auth`, `internal/sheet`, `internal/scaffold`, `internal/picker`, `internal/wizard`, `internal/heartbeat`, and `internal/app/reauth.go` — leaving the build + tests GREEN and the binary materially smaller (CONTEXT D-1/D-2/D-3/D-6/D-7/D-8/D-10).

Purpose: This is where the live data pipeline is restored end-to-end (watcher → backend → self-populating DB from P12) and the highest-complexity, Google-coupled ~8k LOC is removed. It composes Plan 02's new packages and targets Plan 01's new backend routes, so it runs in Wave 2.
Output: a rewritten `runapp.go` sink + a new `migrate.go` + a gutted `build_constants.go`/`config.go`/tray + the 5 deleted packages + reauth.go gone + the OAuth ldflags stripped from CI.

ORDER (load-bearing — RESEARCH "rewire FIRST, then delete, so the tree never fails to compile"): Task 1 + Task 2 rewire EVERY importer of the deleted packages onto the new client/credstore/onboarding/migration so nothing references `internal/auth|sheet|scaffold|picker|wizard|heartbeat` anymore; Task 3 then deletes them and runs `go mod tidy` with a still-green build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-watcher-re-target-onboarding/13-CONTEXT.md
@.planning/phases/13-watcher-re-target-onboarding/13-02-SUMMARY.md
@.planning/phases/13-watcher-re-target-onboarding/13-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 + Plan 02 (built in Wave 1) + the survivors this plan rewires. -->

From internal/backend (Plan 02 — the new sink):
```go
func New(baseURL string) *Client
func (c *Client) Ingest(ctx, code, character, kind, content, version string) error
var ErrUnauthorized, ErrCrossOwner, ErrVersionTooOld, ErrBadPayload error // classify via errors.Is
```

From internal/credstore (Plan 02 — the new token store):
```go
func Store(code string) error
func Read() (string, error)   // not-found err ⇒ needs onboarding
func Delete() error
```

From internal/onboarding (Plan 02 — the native dialog):
```go
func PromptGuildCode(title, prompt string) (string, error) // ErrCancelled on cancel
func PickEQFolder(title string) (string, error)            // ErrCancelled on cancel
var ErrCancelled, ErrUnsupported error
```

VALIDATION: the watcher does NOT import the server. To validate a code it calls its OWN `GET /api/v1/whoami` via a new `func (c *Client) Validate(ctx, code string) error` added to internal/backend in Task 1 (nil on 200, ErrUnauthorized on 401). Plan 01 built the server route; the watcher hits it with the same http.Client shape.

SURVIVORS this plan keeps + rewires:
```go
// internal/watch — UNTOUCHED pipeline. watch.Run(ctx, folders, onInventory, onSpellbook) error; InventorySuffix/SpellbookSuffix; OnChange func(path string)
// internal/parse — KEEP BOTH: parse.CP1252Reader(io.Reader) io.Reader (watcher decode) + parse.Parse/ParseSpellbook (server uses them — DO NOT call them on the upload path)
// internal/eqfind — Discover() (string,error); ValidateFolder(dir string) error
// internal/config — Config (edit struct); Load()/Save(); pathFn test seam; field drops below
// internal/tray — Controller; SetStatus/SetIconHealth/Show*/Hide*; NewController(Config{...}); Config edits below
// internal/update — UNTOUCHED transport (RunDailyCheck/CheckOnce/Apply). 999.22 fix is Plan 04, NOT here.
// internal/system — SignalShutdown/WaitForShutdown; freeConsole (cmd/squirebot/console_windows.go)
// internal/logging — Setup()
```

The CURRENT (v1) makeOnInventoryChange core to REWRITE (internal/app/runapp.go ~line 428) — KEEP re-stat/mtime/open/skip-empty/mtime-persist; REPLACE parse.Parse+sc.WriteInventory+UpsertCharOwner with read-all+backend.Ingest:
```go
// KEEP: os.Stat → fileMtime; os.Open; if len(bytes.TrimSpace(utf8))==0 { skip }; on success persist cfg.LastKnown*Mtime[char]+cfg.Save()
// REPLACE the middle: utf8Bytes,_ := io.ReadAll(parse.CP1252Reader(f)); backend.Ingest(ctx, code, char, "inventory", string(utf8Bytes), version)
// REMOVE: all ErrPermanentAuth / globalPostReauthPending / runPostReauthProbe / suspendForAuth / globalAuthSuspended Google machinery
```

cmd/squirebot/main.go things to REWIRE:
```go
// DELETE: import internal/auth; bc := auth.BuildConstants{...}; OnChangeWorkbook + OnReauthorize tray callbacks; the --uninstall-wipe-credentials reading cfg.GoogleEmail+auth.DeleteToken (REWIRE to credstore.Delete())
// KEEP verbatim: update.Apply() startup-swap; --quit; freeConsole; logging.Setup; systray.Run; the shutdown listener
// ADD: app.MigrateFromV1(cfg) after config.Load (WATCH-11); pass BackendBaseURL + Version into RunApp
```

release.yml things to STRIP (A5 — verified these exist):
```
// "Materialise oauth-config.json from secret" step → DELETE
// "Load OAuth constants from oauth-config.json (AUTH-03 gate)" step → DELETE (the PRODUCTION gate is Google-specific)
// In "Build bare squirebot.exe": the -X main.OAuthClientID / OAuthClientSecret / PickerAPIKey / GCPProjectNumber ldflags → DELETE; KEEP -X main.Version; ADD -X main.BackendBaseURL=https://api.squirebot.quest (optional); keep -H=windowsgui -s -w
// KEEP: NSIS build, SHA-256, latest.json (binary_url + binary_sha256 MUST stay — the auto-updater needs them), the GitHub Release step
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-target the sink + onboarding flow + config migration (runapp.go rewrite, migrate.go, config.go, Validate)</name>
  <read_first>
    - MODIFY: internal/app/runapp.go (RunApp + runWatcher + makeOnInventoryChange + makeOnSpellbookChange; DELETE buildTokenSourceFromWincred/swappableTS/runPostReauthProbe/applyBootAuthError/suspendForAuth/isPermanentAuthErr/ChangeWorkbook + the OAuth globals); internal/app/runapp_test.go (rewrite cases importing auth/sheet)
    - WRITE NEW: internal/app/migrate.go (+ migrate_test.go) — the WATCH-11 first-launch migration
    - MODIFY: internal/config/config.go (drop SpreadsheetID + GoogleEmail; add BackendBaseURL) + config_test.go
    - MODIFY: internal/backend/client.go (Plan 02) — ADD `func (c *Client) Validate(ctx, code string) error` (GET /api/v1/whoami)
    - MIRROR: the existing makeOnInventoryChange (keep re-stat/mtime/skip-empty/cfg.Save shape — RESEARCH "Code Examples: the rewritten inventory callback core"); RESEARCH "Auto-Update Migration (WATCH-11)" 6-step table for migrate.go
    - CONTRACT to honor: D-1 (sink swap, watcher STOPS calling parse.Parse on upload — sends RAW UTF-8 content); D-8 (decode ONCE via parse.CP1252Reader; do NOT double-decode); D-6 (code from credstore only); D-5 (401 terminal → re-prompt, NO retry loop); D-7 (first-launch migration deletes stale Google wincred + drops dead config fields, idempotent, preserves EQFolders+mtime); RESEARCH Pitfall 4 (migration drops ONLY the Google fields, never EQFolders/mtime).
  </read_first>
  <behavior>
    - makeOnInventoryChange: on a file event it re-stats, captures mtime, opens, reads-all via parse.CP1252Reader, skips if the trimmed body is empty, calls backend.Ingest(ctx, code, char, "inventory", utf8, version), and on success persists cfg.LastKnownInventoryMtime[char]+cfg.Save() — NO parse.Parse call on this path (grep-proven)
    - backend.Ingest → ErrUnauthorized: the watcher suspends + surfaces the re-prompt path (tray red + "Guild code invalid — re-enter…"), does NOT retry, does NOT loop
    - ErrVersionTooOld: tray status "Update needed — SquireBot will auto-update", no retry
    - ErrCrossOwner: logged, skipped (no crash)
    - MigrateFromV1 given a v1 config.json with google_email="x@y.z" + spreadsheet_id="abc" → after migration both keys are gone from the re-read JSON, the stale wincred entry SquireBot:x@y.z is deleted exactly once, and EQFolders + LastKnown*Mtime maps are preserved untouched
    - MigrateFromV1 is idempotent: a second call on an already-migrated config is a no-op and does NOT error
    - Config round-trips without spreadsheet_id/google_email keys and with backend_base_url; a v1 config.json carrying the old keys still Loads (unknown keys ignored)
    - backend.Client.Validate(ctx, goodCode) → nil (200); Validate(ctx, badCode) → ErrUnauthorized (401) [httptest]
  </behavior>
  <action>
**1a — Add Validate to internal/backend (the onboarding probe).** In `internal/backend/client.go` add `func (c *Client) Validate(ctx context.Context, code string) error`: build `GET {base}/api/v1/whoami` with `Authorization: Bearer <code>` + the same UA; `200 → nil`, `401 → ErrUnauthorized`, anything else → a generic error. No retry (validation is one-shot). Add an httptest case to client_test.go.

**1b — Edit internal/config/config.go.** Remove the `SpreadsheetID` and `GoogleEmail` struct fields. ADD `BackendBaseURL string \`json:"backend_base_url,omitempty"\`` (overridable base; hardcoded fallback lives in build_constants.go — RESEARCH A6). Keep everything else (Version, EQFolder/EQFolders, LastKnown*Mtime, LogLevel, PendingUpdateVersion) and the BOM-strip + EQFolder→EQFolders shim. Dropping the two struct fields means old config.json keys are simply ignored on unmarshal — that IS the field-drop. Update config_test.go: assert a v1 JSON with `google_email`+`spreadsheet_id` Loads cleanly (keys ignored), and a round-trip omits them and includes backend_base_url when set.

**1c — Write internal/app/migrate.go (WATCH-11, RESEARCH 6-step table).** `func MigrateFromV1(cfg *config.Config) error`:
- Because 1b removed the struct fields, the migration cannot read the old values from cfg. Re-read the raw config.json once: `raw, err := os.ReadFile(config.Path())` (ignore not-exist → no-op nil), BOM-strip (`bytes.TrimPrefix(raw, []byte{0xEF,0xBB,0xBF})`), `json.Unmarshal` into a local `struct{ GoogleEmail string \`json:"google_email"\`; SpreadsheetID string \`json:"spreadsheet_id"\` }`. If BOTH empty → no-op return nil (already migrated / fresh install — the idempotency sentinel).
- If GoogleEmail != "": delete the OLD wincred target WITHOUT importing internal/auth (it is deleted in Task 3): `cred, err := wincred.GetGenericCredential("SquireBot:"+googleEmail); if err == nil { _ = cred.Delete() }` (import `github.com/danieljoos/wincred` — stays in go.mod via credstore). Ignore not-found. `slog.Info("migrated: stale Google credential removed")` (no secret).
- `cfg.Save()` — the cfg was Loaded by the NEW struct (no Google fields), so Save rewrites config.json without `google_email`/`spreadsheet_id`.
- Idempotent: on a second run the raw read finds both fields empty (dropped by the prior Save) → no-op.
- Write migrate_test.go: redirect the config path to a t.TempDir() via the config package's `pathFn` test seam; write a v1 config.json with both fields + EQFolders + mtime maps; run MigrateFromV1; assert both keys gone from the re-read JSON, EQFolders+mtime preserved, second run a no-op. Guard the wincred delete with `runtime.GOOS` (assert only config-side effects in CI; wincred delete runs on the Windows dev box).

**1d — Rewrite internal/app/runapp.go (the SINK swap — the core change).**
- DELETE the imports of `auth`, `sheet`, `scaffold`, `picker`, `wizard`, `heartbeat`, `golang.org/x/oauth2`, and `net`/`net/http` if now unused. DELETE: `swappableTS`, `buildTokenSourceFromWincred`, `runPostReauthProbe`, `applyBootAuthError`, `suspendForAuth`, `isPermanentAuthErr`, `ChangeWorkbook`, `bootAuthClassification`, and any reference to the OAuth global atomics (`globalAuthSuspended`/`globalReauthTSCh`/`globalPostReauthPending` — declared in reauth.go, deleted in Task 3).
- New `RunApp(ctx context.Context, cfg *config.Config, baseURL, version string, t *tray.Controller)`:
  - `code, err := credstore.Read()`. Not-found ⇒ run onboarding (below). If onboarding yields a valid code, continue; else tray red "Setup needed" + return.
  - If a code is present but no EQ folder is configured (existing folder check), run just the EQ-folder onboarding step.
  - Then `runWatcher(ctx, cfg, baseURL, version, code, t)`.
- New `runOnboarding(ctx, cfg, baseURL, version string, t *tray.Controller) (string, error)`:
  1. `t.SetIconHealth(tray.HealthRed); t.SetStatus("Setup needed — enter your guild code")`.
  2. Loop: `code, err := onboarding.PromptGuildCode("SquireBot Setup", "Paste your guild code (from your guild officer):")`. ErrCancelled ⇒ return "", err (tray stays red; the tray "Enter guild code…" item re-triggers).
  3. Validate: `bc := backend.New(baseURL); if err := bc.Validate(ctx, code); errors.Is(err, backend.ErrUnauthorized) { t.SetStatus("That guild code was rejected — try again"); continue }`. Other error (network) ⇒ surface + return. nil ⇒ `credstore.Store(code)`, break.
  4. EQ folder (if none configured): `path, err := onboarding.PickEQFolder("Pick your EverQuest folder")`; `if eqfind.ValidateFolder(path) != nil` ⇒ re-prompt with the VERBATIM message `"This folder doesn't look like an EverQuest install (no eqgame.exe found). Pick a different folder."` (relocated from wizard/server.go); on valid ⇒ `cfg.EQFolder=path; cfg.EQFolders=[]string{path}; cfg.Save()`.
  5. Return the validated code.
- `runWatcher(ctx, cfg, baseURL, version, code string, t)`: `bc := backend.New(baseURL)`. DELETE the entire `sheet.NewClient`/`ValidateWorkbook`/`ScaffoldSchemaV1`/`SetOnRefresh` block. DELETE the `go heartbeat.Run(...)` line (D-10). KEEP the `go update.RunDailyCheck(...)` goroutine (transport unchanged). KEEP folder resolution + `rescanCatchUp` + `watch.Run`. `t.SetIconHealth(tray.HealthGreen); t.SetStatus("Connected — watching " + strings.Join(folders, ", "))` (drop GoogleEmail). `onInventory := makeOnInventoryChange(ctx, bc, cfg, code, version, t)` (+ spellbook).
- `makeOnInventoryChange(ctx context.Context, bc *backend.Client, cfg *config.Config, code, version string, t *tray.Controller) watch.OnChange`: KEEP re-stat → fileMtime → os.Open → skip-empty → mtime-persist+cfg.Save shape VERBATIM. REPLACE the middle: `utf8Bytes, rerr := io.ReadAll(parse.CP1252Reader(f)); f.Close()` (decode ONCE — D-8); `if len(bytes.TrimSpace(utf8Bytes))==0 { slog.Info("inventory empty; skipping"); return }`; `err := bc.Ingest(ctx, code, charName, "inventory", string(utf8Bytes), version)`; then:
```go
switch {
case errors.Is(err, backend.ErrUnauthorized):
    slog.Warn("upload 401 — guild code invalid", "char", charName)
    t.SetIconHealth(tray.HealthRed); t.SetStatus("Guild code invalid — re-enter via the tray menu")
    return // terminal; NO retry (D-5 / Pitfall 5)
case errors.Is(err, backend.ErrVersionTooOld):
    t.SetStatus("Update needed — SquireBot will auto-update"); return
case errors.Is(err, backend.ErrCrossOwner):
    slog.Warn("cross-owner reject", "char", charName); return
case err != nil:
    slog.Error("upload inventory", "char", charName, "err", err); t.SetStatus("Last upload failed: "+charName); return
}
// success → persist cfg.LastKnownInventoryMtime[char] + cfg.Save() (UNCHANGED)
```
  Mirror for `makeOnSpellbookChange` (kind "spellbook"). DROP UpsertCharOwner entirely (identity is server-derived — D-1).
- Rewrite runapp_test.go: delete/replace auth/sheet-dependent cases. Test the makeOnInventoryChange error-switch against an httptest backend server returning 204/401/426/409: assert 204 persists the mtime; 401 does NOT loop (the handler saw a single request) + sets red; the empty-file path skips with NO request. (Use Plan 02's client_test httptest pattern.) Keep the existing extractCharNameForSuffix tests.
  </action>
  <verify>
    <automated>cd "C:/Users/Virus Canary/Desktop/Claude/SquireBot" && go test ./internal/app/ ./internal/config/ ./internal/backend/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/app/ ./internal/config/ ./internal/backend/ -count=1` exits 0
    - `internal/app/runapp.go` contains `backend.New` AND `.Ingest(` AND `credstore.` AND `onboarding.`
    - `grep -n "parse.Parse" internal/app/runapp.go` finds NOTHING (the watcher no longer parses on the upload path — D-1)
    - `grep -nE "internal/(auth|sheet|scaffold|picker|wizard|heartbeat)" internal/app/runapp.go` finds NOTHING (all rewired)
    - `internal/app/migrate.go` contains `func MigrateFromV1`
    - migrate_test.go asserts EQFolders+mtime preserved AND a second run is a no-op
    - `internal/config/config.go` contains `BackendBaseURL` and (filtering doc comments) the `Config` struct no longer declares `SpreadsheetID` or `GoogleEmail` fields
    - `go vet ./internal/app/ ./internal/config/` exits 0
  </acceptance_criteria>
  <done>The watcher's sink POSTs raw UTF-8 to the backend with no client-side parse; onboarding validates via /whoami + stores to DPAPI + picks the EQ folder natively; the v1→v2 migration drops the Google fields + stale credential idempotently; runapp.go references none of the deleted packages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire cmd/squirebot/main.go + build_constants.go + tray (drop OAuth wiring, add migration + guild-code trigger)</name>
  <read_first>
    - MODIFY: cmd/squirebot/main.go (drop auth.BuildConstants + OAuth tray callbacks; rewire --uninstall-wipe-credentials to credstore.Delete; add MigrateFromV1; new RunApp signature); cmd/squirebot/build_constants.go (gut the OAuth vars); internal/tray/tray.go (drop Change-Workbook/Reauthorize/Open-Workbook-to-Sheets; add Enter-guild-code) + tray_test.go
    - MIRROR: the existing main.go structure (KEEP update.Apply/--quit/freeConsole/logging/systray.Run/shutdown-listener verbatim); the tray Config/MenuPlan/loop pattern
    - CONTRACT to honor: D-2 (strip OAuth ldflags from build_constants.go); D-3 (the tray "Enter guild code…" item triggers the native onboarding, replacing the OAuth/Change-Workbook items); D-7 (MigrateFromV1 called after config.Load, before RunApp). The "Open Workbook" item built a docs.google.com URL — remove it (no Sheet).
  </read_first>
  <behavior>
    - main.go compiles + starts with NO import of internal/auth; --uninstall-wipe-credentials calls credstore.Delete() (not auth.DeleteToken); MigrateFromV1(cfg) runs after config.Load and before app.RunApp
    - The tray menu no longer contains "Change Workbook…", "Reauthorize…", or an "Open Workbook" item opening a Google Sheet URL; it DOES contain "Enter guild code…" whose click triggers onboarding (OnEnterGuildCode)
    - tray MenuPlan() reflects the new item set; tray_test asserts no Change-Workbook/Reauthorize/Open-Workbook labels + Enter-guild-code present
    - build_constants.go exposes only Version (+ BackendBaseURL); the OAuth/Picker vars are gone
  </behavior>
  <action>
**2a — Gut cmd/squirebot/build_constants.go.** Remove `OAuthClientID`, `OAuthClientSecret`, `PickerAPIKey`, `GCPProjectNumber`. KEEP `Version = "0.1.0-dev"`. ADD `BackendBaseURL = "https://api.squirebot.quest"` (the hardcoded HTTPS fallback; release.yml may override via `-X main.BackendBaseURL=...` but the default IS the canonical host — RESEARCH A6/D-5). Rewrite the doc comment: it now documents Version + BackendBaseURL only; delete the OAuth-secret rationale paragraph entirely.

**2b — Rewire cmd/squirebot/main.go.**
- DELETE the `import "github.com/boejowen/SquireBot/internal/auth"` line and the `bc := auth.BuildConstants{...}` block.
- `--uninstall-wipe-credentials`: REWIRE — instead of reading `cfg.GoogleEmail` + `auth.DeleteToken`, call `credstore.Delete()` (delete the new guild-code entry). Drop the email read. Keep the always-exit-0 contract.
- After `cfg, err := config.Load()` (+ its error check), ADD: `if err := app.MigrateFromV1(cfg); err != nil { slog.Warn("v1→v2 migration", "err", err) }` (non-fatal — WATCH-11).
- tray.Config: DELETE `OnChangeWorkbook`, `OnReauthorize`, `SpreadsheetID`. ADD `OnEnterGuildCode: func() { slog.Info("Enter guild code clicked"); go app.RunApp(ctx, cfg, BackendBaseURL, Version, trayCtl) }` (re-running RunApp re-enters onboarding via its credstore.Read branch). Fold the old `OnContinueSetup` intent into this single item (the wizard is gone).
- `go app.RunApp(ctx, cfg, trayCtl)` → `go app.RunApp(ctx, cfg, BackendBaseURL, Version, trayCtl)`.
- The startup `slog.Info("squirebot starting", ...)`: drop the `google_email` + `spreadsheet_id_set` fields; keep version/pid/go_version/log_dir/config_path/icon bytes/eq_folder_set.
- KEEP verbatim: the `update.Apply()` startup-swap block, the `--quit` block, `freeConsole()`, `logging.Setup()`, the named-event shutdown-listener goroutine, `systray.Run(...)`, the final cancel.

**2c — Edit internal/tray/tray.go (+ tray_test.go).**
- Remove the Sheets/OAuth items: drop `LabelChangeWorkbook` + `LabelReauthorize`; REMOVE `LabelOpenWorkbook` + its `mWorkbook` ClickedCh handler (it opened `https://docs.google.com/spreadsheets/d/<id>` — no Sheet) + the `spreadsheetID` field + `SetSpreadsheetID`/`SpreadsheetID()` + the `actSetSpreadsheetID` pending kind.
- ADD `LabelEnterGuildCode = "Enter guild code…"` + an `mEnterGuildCode` item wired to a new `onEnterGuildCode func()` in Config. Collapse the Continue-setup/Reauthorize Show/Hide machinery into this single always-visible item (a re-prompt is always allowed); delete the now-unused Show/HideContinueSetup + Show/HideReauthorize + their pending kinds (and update any caller — runapp.go no longer calls them after Task 1).
- Update `MenuPlan()` to the new ordered set: Open log folder / Check for updates / Enter guild code… / Quit. Update the `Config` struct (drop OnChangeWorkbook/OnReauthorize/OnContinueSetup/SpreadsheetID; add OnEnterGuildCode). Update `OnReady`/`loop`/`drainPending` accordingly (drop the removed menu items + click cases).
- Rewrite tray_test.go: assert MenuPlan() contains `LabelEnterGuildCode` and does NOT contain "Change Workbook…"/"Reauthorize…"/"Open Workbook"; keep the queue-and-replay (OPS-06) tests for the surviving mutators (SetStatus/SetIconHealth).
  </action>
  <verify>
    <automated>cd "C:/Users/Virus Canary/Desktop/Claude/SquireBot" && go test ./internal/tray/ -count=1; go build ./cmd/squirebot</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/tray/ -count=1` exits 0
    - `go build ./cmd/squirebot` exits 0 (the watcher binary still compiles — the deleted packages are not yet removed but are no longer imported by main.go/tray)
    - `grep -n "internal/auth" cmd/squirebot/main.go` finds NOTHING
    - `cmd/squirebot/main.go` contains `credstore.Delete` AND `MigrateFromV1` AND `BackendBaseURL`
    - `cmd/squirebot/build_constants.go` contains `BackendBaseURL` and does NOT contain `OAuthClientSecret`
    - `internal/tray/tray.go` contains `LabelEnterGuildCode` and does NOT contain `LabelChangeWorkbook` or `LabelReauthorize`
  </acceptance_criteria>
  <done>main.go drops all OAuth wiring, runs the v1→v2 migration, and triggers native onboarding from a single "Enter guild code…" tray item; build_constants carries no Google secret; the tray sheds every Sheets/OAuth menu item.</done>
</task>

<task type="auto">
  <name>Task 3: DELETE the Google stack + strip OAuth ldflags + go mod tidy (build/vet/test GREEN)</name>
  <read_first>
    - DELETE: internal/auth/ (entire dir), internal/sheet/ (entire dir), internal/scaffold/ (entire dir), internal/picker/ (entire dir), internal/wizard/ (entire dir), internal/heartbeat/ (entire dir), internal/app/reauth.go, internal/app/reauth_test.go
    - MODIFY: .github/workflows/release.yml (strip the OAuth materialise + AUTH-03 gate steps + the four OAuth ldflags); go.mod + go.sum (via `go mod tidy`)
    - PRECONDITION (Tasks 1+2 done): nothing in the surviving tree imports internal/auth|sheet|scaffold|picker|wizard|heartbeat anymore (the rewire is COMPLETE) — verify the import map BEFORE deleting so the tree never fails to compile (RESEARCH deletion order).
    - CONTRACT to honor: D-2 (the deletion map — these exact packages + reauth.go); WATCH-09 (no Google code, no baked secret, materially smaller binary); D-10 (heartbeat gone — confirmed its only consumer was the Sheet `_status` via sheet.Client.WriteHeartbeat, which dies with internal/sheet); A5 (release.yml carries the OAuth ldflags + the PRODUCTION gate — both verified present).
  </read_first>
  <action>
**3a — Confirm no importers remain (pre-deletion gate).** Run `go list -deps ./cmd/squirebot ./internal/...` (or the grep below) and confirm ZERO references to the six packages from any survivor. If anything still imports them, STOP and fix the rewire (Task 1/2) first — do NOT delete a still-imported package. (The deleted packages' OWN files/tests will still reference each other; that's fine — they're about to be removed wholesale.)

**3b — Delete the packages.** `git rm -r` the six directories + the two reauth files:
- `git rm -r internal/auth internal/sheet internal/scaffold internal/picker internal/wizard internal/heartbeat`
- `git rm internal/app/reauth.go internal/app/reauth_test.go`
(The wizard's EQ-folder logic is NOT lost — it was relocated to `internal/onboarding` in Plan 02; the sqweek dialog + eqfind validation live there now.)

**3c — Strip the OAuth ldflags + gate from .github/workflows/release.yml (A5).**
- DELETE the step `- name: Materialise oauth-config.json from secret` (the whole step + its `env: OAUTH_CONFIG_JSON`).
- DELETE the step `- name: Load OAuth constants from oauth-config.json (AUTH-03 gate)` (id: oauth) — the consent_screen_status PRODUCTION gate is Google-specific and gone.
- In `- name: Build bare squirebot.exe`: remove the `env:` entries for OAUTH_CLIENT_ID/OAUTH_CLIENT_SECRET/PICKER_API_KEY/GCP_PROJECT_NUMBER and the four `-X main.OAuthClientID=... -X main.OAuthClientSecret=... -X main.PickerAPIKey=... -X main.GCPProjectNumber=...` ldflag fragments. KEEP `-H=windowsgui -s -w -X main.Version=${{ steps.ver.outputs.version }}`. Optionally ADD `-X main.BackendBaseURL=https://api.squirebot.quest` (the hardcoded default already covers it, so this is belt-and-braces).
- KEEP everything else: the NSIS install/verify steps, Compute SHA-256, "Write latest.json" (binary_url + binary_sha256 MUST remain — the in-app auto-updater needs them), the upload + GitHub Release steps. (The `phase: 2` / `signed: false` manifest fields can stay; they're informational.)
- Update the workflow's top doc comment + the Release `body:` text to drop the Google/consent-screen framing (no longer ships OAuth; describe it as the re-targeted backend watcher).

**3d — `go mod tidy`.** Run `go mod tidy`. This DROPS the now-unused Google dependency tree: `golang.org/x/oauth2` + `google.golang.org/api` (direct) and their transitive `cloud.google.com/go/auth*`, `google.golang.org/grpc`, `google.golang.org/genproto*`, `github.com/google/s2a-go`, `googleapis/gax-go`, `googleapis/enterprise-certificate-proxy`, etc. (the indirect block in go.mod). `github.com/danieljoos/wincred` STAYS (credstore + migrate.go use it); `golang.org/x/text` STAYS (parse.CP1252Reader); `modernc.org/sqlite` + `goose` STAY (the backend in the same module).

**3e — Verify the tree is green + Google-free.** Run the full build/vet/test (the verification gates below). The materially-smaller-binary signal is the dropped dependency tree (go.mod no longer lists oauth2/google.golang.org/api); Plan 04 does the explicit binary-size + no-secret confirmation.
  </action>
  <verify>
    <automated>cd "C:/Users/Virus Canary/Desktop/Claude/SquireBot" && go build ./... && go vet ./... && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 AND `go vet ./...` exits 0 AND `go test ./... -count=1` exits 0 (whole repo green AFTER deletion + rewire — no compile break, no test regression; the backend tree from P11/P12 is untouched)
    - `go list ./internal/auth 2>&1` contains "no Go files" or "cannot find" or "matched no packages" (the package is gone)
    - the same holds for `go list ./internal/sheet`, `./internal/scaffold`, `./internal/picker`, `./internal/wizard`, `./internal/heartbeat` (all gone)
    - `git status` shows the six directories + reauth.go + reauth_test.go staged as deletions (`git rm`)
    - `go list -deps ./cmd/squirebot` contains NO `golang.org/x/oauth2`, NO `google.golang.org/api`, NO `cloud.google.com/go` (the watcher binary is Google-free; `database/sql/driver` substring-matching "drive" is the known benign exception)
    - `go.mod` no longer lists `golang.org/x/oauth2` or `google.golang.org/api` in the require blocks
    - `grep -n "OAuthClientSecret\|consent_screen_status\|OAUTH_CONFIG_JSON" .github/workflows/release.yml` finds NOTHING (the OAuth ldflags + PRODUCTION gate are stripped)
    - `grep -n "binary_url\|binary_sha256" .github/workflows/release.yml` STILL finds the latest.json manifest fields (the auto-updater contract is preserved)
  </acceptance_criteria>
  <done>The six Google-coupled packages + reauth.go are git-rm'd, the OAuth ldflags + PRODUCTION gate are stripped from CI, go.mod sheds the Google dependency tree, and `go build/vet/test ./...` is green across the whole repo with the auto-updater manifest contract intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| watcher → backend (TLS) | The bearer guild code + raw UTF-8 content cross to the live ingest API |
| guild code at rest (DPAPI) | The plaintext code persists DPAPI-encrypted; the STALE Google refresh token must be purged |
| build/release pipeline | The CI no longer bakes a Google secret into the binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.03-01 | Information Disclosure | Stale v1 OAuth refresh token left in wincred after the re-target | mitigate | MigrateFromV1 deletes the `SquireBot:<email>` entry once on first launch (WATCH-11 / RESEARCH Runtime State Inventory); idempotent — proven by migrate_test. No orphaned Google secret remains on a migrated machine. |
| T-13.03-02 | Information Disclosure | The baked-in Google client secret shipping in the re-targeted binary | mitigate | The OAuth `-ldflags` are stripped from build_constants.go + release.yml; `go mod tidy` drops the oauth2 tree. The grep gate proves `OAuthClientSecret`/`consent_screen_status` are gone from CI. The binary no longer carries a Google secret (WATCH-09). |
| T-13.03-03 | Denial of Service | A 401 during normal operation driving an unbounded re-POST loop | mitigate | The makeOnInventoryChange/Spellbook error switch treats 401 as TERMINAL (suspend + re-prompt, no retry — RESEARCH Pitfall 5); the backend client's bounded retry never fires on 401/409/426. Proven by the "401 single request, no loop" callback test. |
| T-13.03-04 | Tampering | The watcher double-decoding CP1252 and corrupting item names | mitigate | The callback decodes ONCE via parse.CP1252Reader and POSTs the UTF-8 verbatim; parse.Parse is NOT called on the upload path (grep gate); the server feeds content straight in (no re-decode). D-8 / A1 / RESEARCH Pitfall 1. |
| T-13.03-05 | Spoofing / Tampering | A forged backend base URL redirecting uploads | mitigate | The base URL default is the hardcoded HTTPS `https://api.squirebot.quest` (build_constants.go); the config override is documented as an advanced/self-host action and the client (Plan 02) pins HTTPS. The canonical path requires no override. |
| T-13.03-06 | Elevation of Privilege | The deletion removing a survivor the backend needs (internal/parse) | mitigate | internal/parse is explicitly PRESERVED (the backend imports it — RESEARCH Pitfall 2); the deletion targets only the six Google-coupled packages + reauth.go; `go build ./...` green across the whole module proves the backend still compiles. |
| T-13.03-07 | Repudiation | A migration that silently wipes the EQ folder / mtime maps | mitigate | MigrateFromV1 drops ONLY the two Google fields (struct-field removal) + the stale credential; it preserves EQFolders + LastKnown*Mtime (RESEARCH Pitfall 4), proven by migrate_test. No re-pick / re-upload storm. |

This plan is the milestone's largest attack-surface REDUCTION: it deletes the inbound loopback OAuth/picker HTTP listeners, the Drive Picker, the `drive.file` scope, the publicly-baked OAuth client secret, and the refresh-token store. Severity of the residual threats is LOW; none trips block_on:high.
</threat_model>

<verification>
- `go build ./... && go vet ./... && go test ./... -count=1` GREEN across the whole repo AFTER the rewire + deletion (no compile break, no test regression; the P11/P12 backend tree is untouched).
- `go list -deps ./cmd/squirebot` carries no `golang.org/x/oauth2` / `google.golang.org/api` / `cloud.google.com/go` (the watcher binary is Google-free).
- `go list ./internal/{auth,sheet,scaffold,picker,wizard,heartbeat}` all report the package is gone ("no Go files" / "cannot find").
- The release.yml grep gate: no `OAuthClientSecret`/`consent_screen_status`/`OAUTH_CONFIG_JSON`; `binary_url`+`binary_sha256` preserved.
- The whole-repo `grep -nE "internal/(auth|sheet|scaffold|picker|wizard|heartbeat)" --include=*.go` finds NOTHING in any non-deleted file (no dangling importer).
</verification>

<success_criteria>
- The watcher uploads inventory/spellbook to `POST /api/v1/ingest` (raw UTF-8 content, no client-side parse), preserving the 500ms debounce + always-re-read.
- No Google OAuth/PKCE/Sheets/Drive-Picker code remains; no Google secret is baked into the build; the dependency tree sheds the Google packages.
- Native "paste your guild code" onboarding stores the bearer in DPAPI, validates via `/whoami`, and goes green with zero browser OAuth.
- An existing v1.x config migrates once (stale credential deleted, dead fields dropped, EQFolders+mtime preserved, idempotent).
- `go build/vet/test ./...` green; the auto-updater latest.json contract is intact.
</success_criteria>

<output>
After completion, create `.planning/phases/13-watcher-re-target-onboarding/13-03-SUMMARY.md`.
</output>
