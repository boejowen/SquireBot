---
phase: 27-my-characters-inventory-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/myview.ts
  - web/src/lib/__tests__/myview.test.ts
  - web/src/routes/+page.svelte
autonomous: true
requirements: [MYVIEW-01, MYVIEW-02]
must_haves:
  truths:
    - "A member can apply a 'My characters' quick-filter to the consolidated views (inventory/bank/gear/spell) so the grid shows only their assigned characters' rows (MYVIEW-01)."
    - "A member can drill into a single specific assigned character from that same control (MYVIEW-02)."
    - "The default page state is 'All members' (filter OFF) — existing all-members visibility is unchanged; any member can toggle back to all members."
    - "The filter is client-side over the existing single reusable DataGrid — no per-character view tabs are created and no row is server-withheld from another member."
    - "The cross-guild SearchBox still searches the full guild (it is NOT narrowed by the my-characters filter)."
    - "npm run check passes with 0 errors / 0 warnings; npm test passes (the new myview.test.ts included); npm run build succeeds."
  artifacts:
    - path: "web/src/lib/myview.ts"
      provides: "Pure DOM-free filter predicate + my-character-name-set derivation"
      exports: ["myCharNameSet", "applyMyFilter"]
    - path: "web/src/lib/__tests__/myview.test.ts"
      provides: "Node vitest proving the predicate (passthrough / mine-only / drill-down / empty-mine / name-join exactness)"
      contains: "applyMyFilter"
    - path: "web/src/routes/+page.svelte"
      provides: "fetchMyCharacters() in the Promise.all; the single 'My characters' <select> control; $derived filtered arrays feeding the four DataGrid instances; distinct empty-state copy"
      contains: "applyMyFilter"
  key_links:
    - from: "web/src/routes/+page.svelte"
      to: "web/src/lib/myview.ts"
      via: "import { myCharNameSet, applyMyFilter }"
      pattern: "from '\\$lib/myview'"
    - from: "web/src/routes/+page.svelte"
      to: "/api/v1/assignments/mine"
      via: "fetchMyCharacters() added to the existing Promise.all"
      pattern: "fetchMyCharacters"
    - from: "web/src/routes/+page.svelte (the four <DataGrid> instances)"
      to: "applyMyFilter output"
      via: "data={filteredViewRows} (and gear/spell/bank)"
      pattern: "data=\\{filtered"
---

<objective>
Add an ADDITIVE "My characters" quick-filter + single-character drill-down over the existing all-members consolidated views (inventory / bank / gear / spell), satisfying MYVIEW-01 and MYVIEW-02. The filter is purely client-side: it narrows the rows passed to the existing single reusable `DataGrid` and NEVER hides a row from any other member's fetch.

Purpose: A member can quickly see just their assigned characters (or drill into one) without the consolidated all-members views changing for anyone — additive convenience, NOT access control.

Output: a new pure node-testable helper (`web/src/lib/myview.ts`) + its test, and the Svelte wiring in `web/src/routes/+page.svelte` (one `<select>` control + `$derived` filtered arrays feeding the four grids). ZERO backend change, ZERO migration — `fetchMyCharacters()` (`GET /api/v1/assignments/mine`) and all four view payloads are already live (Phase 26 / Phase 14).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-my-characters-inventory-filter/27-RESEARCH.md
@.planning/phases/27-my-characters-inventory-filter/27-PATTERNS.md

<interfaces>
<!-- Contracts extracted from the codebase. Use these directly — no exploration needed. -->

From web/src/lib/api.ts (ALREADY SHIPPED — import, do NOT edit api.ts):
```typescript
export interface MyCharacter {
    character_id: number;
    name: string;            // ← the join key against the view rows' `char` string
    discord_user_id: string;
    assigned_at: number;
    assigned_by: string;
}
/** GET /api/v1/assignments/mine → MyCharacter[] ([] when none). Login-only (RequireSession). */
export function fetchMyCharacters(f?: typeof fetch): Promise<MyCharacter[]>;
```

The three view-row types ALL carry `char: string` and NO character_id; `bank` reuses `ViewRow`:
```typescript
export interface ViewRow      { char: string; slot: string; item: string; /* … */ }   // api.ts:91-108 (bank reuses this)
export interface GearCheckRow { char: string; class: string; tier: string; /* … */ }  // api.ts:110-119
export interface SpellCheckRow{ char: string; class: string; level: number; /* … */ } // api.ts:121-128
```

The pure-helper precedent to mirror EXACTLY (same role, DOM-free, node-tested):
```typescript
// web/src/lib/assignments.ts — header rationale + single-responsibility pure export style
export function partitionClaimable(claimable: ClaimableCharacter[]): { /* … */ } { /* plain filter, no DOM */ }
```

NAME-JOIN FACT (verified this phase — see Task 1 verification): the view builder and the assignment store source the character name from the SAME `character.name` SQLite column with NO transformation:
  - view rows: internal/backendsrv/store/readviews.go:150  → `SELECT c.name, …`
  - "my characters": internal/backendsrv/store/assignment.go:448 → `SELECT a.character_id, c.name, …`
So `MyCharacter.name` ≡ `ViewRow.char` byte-for-byte. An exact-match join is correct; case-insensitive comparison is a cheap defensive belt-and-suspenders (names are stable in-game proper nouns).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure filter helper (myview.ts) + node test (myview.test.ts), and verify the name-join</name>
  <read_first>
    - web/src/lib/assignments.ts (EXACT analog — copy the header rationale + the pure single-responsibility export shape)
    - web/src/lib/__tests__/assignments.test.ts (EXACT analog — copy the factory-fixture + describe/it idiom; use real EQ proper-noun names like 'Slampeach')
    - web/src/lib/api.ts (lines ~91-128 for the *Row types' `char` field; lines ~832-908 for MyCharacter + fetchMyCharacters — do NOT edit api.ts)
    - internal/backendsrv/store/readviews.go (line ~150, the view builder `SELECT c.name`) AND internal/backendsrv/store/assignment.go (line ~448, the my-characters `SELECT … c.name`) — read both to CONFIRM the name-join agreement (Pitfall 1 / Assumption A1) before writing the predicate
  </read_first>
  <behavior>
    - applyMyFilter passthrough: mineOnly=false AND selectedChar=null → returns the input rows unchanged (additive default, MYVIEW-01).
    - applyMyFilter mine-only: mineOnly=true, selectedChar=null → returns only rows whose `char` is in the my-names set.
    - applyMyFilter drill-down dominates: selectedChar='Slampeach' → returns only rows with char==='Slampeach', regardless of mineOnly (MYVIEW-02).
    - applyMyFilter empty mine: mineOnly=true with an empty names set → returns [].
    - name-join exactness: a row whose `char` is NOT in the set is excluded; a row whose `char` IS in the set is included.
    - myCharNameSet: builds a Set of the MyCharacter.name strings.
  </behavior>
  <action>
Create `web/src/lib/myview.ts` mirroring `web/src/lib/assignments.ts` (same header style: "Pure, DOM-free decision helpers … extracted to a plain .ts so they're unit-testable under the node vitest project … the filter is presentation, NOT a security boundary; the server's RequireSession is the authoritative gate" — security V4 negative requirement). Import `import type { MyCharacter } from './api';`. Export EXACTLY these two pure functions:

```typescript
/** The set of the caller's assigned character NAMES — the join key against the
 *  `char` string every view row carries (ViewRow/GearCheckRow/SpellCheckRow source
 *  the SAME character.name column the assignment store does — readviews.go:150 ≡
 *  assignment.go:448 — so exact-match is correct; we also lower-case for a defensive
 *  case-insensitive join). */
export function myCharNameSet(mine: MyCharacter[]): Set<string> {
    return new Set(mine.map((c) => c.name.toLowerCase()));
}

/** Narrow any char-bearing rows to the caller's characters. selectedChar (the
 *  drill-down, MYVIEW-02) DOMINATES: only that char's rows survive. Otherwise
 *  mineOnly=false passes rows through UNCHANGED (additive default, MYVIEW-01) and
 *  mineOnly=true keeps only rows whose char is in mineNames. Pure + DOM-free →
 *  node-testable. Case-insensitive to match myCharNameSet. */
export function applyMyFilter<T extends { char: string }>(
    rows: T[],
    mineNames: Set<string>,
    mineOnly: boolean,
    selectedChar: string | null
): T[] {
    if (selectedChar) {
        const sel = selectedChar.toLowerCase();
        return rows.filter((r) => r.char.toLowerCase() === sel);
    }
    if (!mineOnly) return rows;
    return rows.filter((r) => mineNames.has(r.char.toLowerCase()));
}
```

Then create `web/src/lib/__tests__/myview.test.ts` mirroring `assignments.test.ts`: a `mine(over)` factory returning a `MyCharacter` and a `row(over)` factory returning a `{ char: string }` object; assert every case in <behavior> using real EQ names (e.g. 'Slampeach', 'Findom'). Include one case that proves the drill-down dominates mineOnly and one that proves the empty-mine set yields [].

NAME-JOIN VERIFICATION (Pitfall 1 / A1 — MANDATORY): while reading the two Go SELECTs listed in read_first, confirm both pull `c.name` from the `character` table with no UPPER/LOWER/normalization applied. Record the verification verdict in the SUMMARY (one line: "view builder and assignment store both emit raw character.name — exact join correct; case-insensitive predicate is defensive"). If — contrary to the verified fact — they DIFFER, the case-insensitive predicate above already covers casing drift; flag any deeper normalization mismatch (e.g. trimming, unicode) in the SUMMARY rather than silently shipping.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/__tests__/myview.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/myview.ts` exists and exports `myCharNameSet` and `applyMyFilter` (grep: `grep -E 'export function (myCharNameSet|applyMyFilter)' web/src/lib/myview.ts` returns 2 lines).
    - `web/src/lib/myview.ts` imports the type only: `grep -c "import type { MyCharacter } from './api'" web/src/lib/myview.ts` == 1.
    - `web/src/lib/__tests__/myview.test.ts` exists and references `applyMyFilter` and `myCharNameSet`.
    - `cd web && npx vitest run src/lib/__tests__/myview.test.ts` exits 0 (all cases green).
    - No DOM/Svelte import in either new file: `grep -c "svelte" web/src/lib/myview.ts web/src/lib/__tests__/myview.test.ts` == 0 across both.
    - No raw-HTML sink introduced: `grep -c '{@html}' web/src/lib/myview.ts` == 0.
    - SUMMARY records the name-join verdict (view builder + assignment store both emit raw character.name).
  </acceptance_criteria>
  <done>myview.ts exports the two pure helpers, myview.test.ts proves all six behaviors and exits 0, and the name-join agreement is verified + recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the single 'My characters' &lt;select&gt; control into +page.svelte and feed the grids filtered data</name>
  <read_first>
    - web/src/routes/+page.svelte (the file being edited — the import block :21-33, the $state declarations :85-94, the Promise.all :99-113, the `coinToons = $derived(...)` precedent :151, the SearchBox :172, the view-nav :176-188, the four DataGrid call sites :191-243, the `.tab` / `.record-coin` CSS :248+)
    - web/src/lib/myview.ts (Task 1 output — the helpers being imported)
    - web/src/lib/components/WantlistPanel.svelte (the "derive `data` client-side, feed the grid" precedent — filter upstream of the grid, never fork it)
    - web/src/lib/components/DataGrid.svelte (CONFIRM no edit needed; copy the `.facet` <select> markup :120-131 and its CSS :246-272 for the new control's EQ-theme styling — --accent/--panel/--text/--font-body, 44px touch target, :focus-visible outline)
    - web/src/lib/components/StateBlock.svelte (the existing empty-state kinds — to add or reuse a "none of your characters" affordance)
    - web/src/lib/api.ts (fetchMyCharacters + MyCharacter — already shipped; just import)
  </read_first>
  <action>
Edit ONLY `web/src/routes/+page.svelte` (DataGrid.svelte, columns.ts, api.ts all stay UNTOUCHED — the grid is fed pre-filtered data and must stay view-agnostic; CLAUDE.md consolidated-views LOCK).

1. Imports (:21-33 block): add `fetchMyCharacters, type MyCharacter` to the `$lib/api` import; add a new import `import { myCharNameSet, applyMyFilter } from '$lib/myview';`.

2. State (mirror the `$state` declarations at :85-94): add
   `let myCharacters = $state<MyCharacter[]>([]);`
   `let mineOnly = $state(false);`                  // default OFF — all-members visible (MYVIEW-01, additive)
   `let selectedChar = $state<string | null>(null);`

3. Promise.all (:99-113): add `fetchMyCharacters()` as the 7th element; destructure it (`const [v, g, s, b, m, bt, mc] = await Promise.all([... fetchMyCharacters()])`) and assign `myCharacters = mc;`.

4. Derived filter state (mirror `coinToons = $derived(...)` at :151):
   `let mineNames = $derived(myCharNameSet(myCharacters));`
   `let filteredViewRows  = $derived(applyMyFilter(viewRows,  mineNames, mineOnly, selectedChar));`
   `let filteredGearRows  = $derived(applyMyFilter(gearRows,  mineNames, mineOnly, selectedChar));`
   `let filteredSpellRows = $derived(applyMyFilter(spellRows, mineNames, mineOnly, selectedChar));`
   `let filteredBankRows  = $derived(applyMyFilter(bankRows,  mineNames, mineOnly, selectedChar));`

5. The SINGLE control (per the locked design decision — ONE `<select>` satisfies BOTH MYVIEW-01 and MYVIEW-02). Place it in the view-orchestration layer near the `view-nav` (:176-188), NEVER inside DataGrid. The option set, in order:
   - `<option value="all">All members</option>`  ← DEFAULT (filter OFF)
   - `<option value="mine">My characters</option>`
   - then one `<option>` per assigned character, value = the character name, label = the character name rendered via plain `{}` (Svelte auto-escapes — NEVER `{@html}`; security V5 / repo standing rule), sourced ONLY from `myCharacters` (NOT `meta.characters` — Pitfall 4).
   Wire `onchange` so: value `"all"` → `mineOnly=false; selectedChar=null`; value `"mine"` → `mineOnly=true; selectedChar=null`; any character value → `selectedChar=that name; mineOnly=false`. Give it `aria-label="Filter views by character"` and style it with the `.facet` token set (border `var(--border, var(--accent))`, `background: var(--panel)`, `color: var(--text)`, `font-family: var(--font-body)`, `min-height: 44px`, `:focus-visible { outline: 2px solid var(--accent); }`).
   When `myCharacters.length === 0` (a member who has claimed nothing — Pitfall 5): disable the "My characters" + per-char options (or the whole control) and show a short hint linking to `/my-characters` (e.g. "Claim characters on My characters to filter") so the control is never a dead empty toggle.

6. Feed the grids the FILTERED arrays (:191-243): swap `data={viewRows}` → `data={filteredViewRows}`, `data={gearRows}` → `data={filteredGearRows}`, `data={spellRows}` → `data={filteredSpellRows}`, `data={bankRows}` → `data={filteredBankRows}`. Update each per-view empty guard to test the FILTERED count and render a DISTINCT affordance when the filter is active but empties the grid (Pitfall 3/5) — e.g. when `mineOnly || selectedChar` and the filtered array is empty, show "None of your characters have rows in this view" (a new StateBlock kind or an inline note) rather than the generic "no data" StateBlock, so the member doesn't misread an active filter as missing data.

7. LEAVE the SearchBox at :172 reading the FULL `viewRows` (NOT filtered) — search stays "across the guild" per the locked decision (the filter narrows the GRID only, never the cross-guild SearchBox).

Do NOT add any `?mine=1` query param or a per-caller server filter — that would BE access control and is explicitly out of scope (the API already returns all-members rows to this session; the filter only narrows THIS browser's display).
  </action>
  <verify>
    <automated>cd web && npm run check && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `+page.svelte` imports the helpers: `grep -c "from '\$lib/myview'" web/src/routes/+page.svelte` == 1.
    - `+page.svelte` calls the live read: `grep -c "fetchMyCharacters" web/src/routes/+page.svelte` >= 1 (in the import + the Promise.all).
    - The four grids are fed filtered data: `grep -E "data=\{filtered(View|Gear|Spell|Bank)Rows\}" web/src/routes/+page.svelte` returns 4 lines.
    - Default-off is present: `grep -c "mineOnly = \$state(false)" web/src/routes/+page.svelte` == 1.
    - The drill-down options come from myCharacters, not meta: the per-char `{#each ...}` for the control iterates `myCharacters` (grep the new control block; it must NOT iterate `meta.characters` for the options).
    - No raw-HTML sink: `grep -c '{@html}' web/src/routes/+page.svelte` == 0.
    - SearchBox still reads the FULL rows: `grep -c "rows={viewRows}" web/src/routes/+page.svelte` == 1 (NOT filteredViewRows).
    - No backend/route/migration touched: `git status --porcelain` shows changes ONLY under `web/src/` (no files under `internal/`, `cmd/`, or `*.sql`). If a backend change appears necessary, STOP and flag it (contradicts the research — zero backend change is locked).
    - DataGrid.svelte and columns.ts are unmodified: `git status --porcelain web/src/lib/components/DataGrid.svelte web/src/lib/columns.ts` is empty.
    - `cd web && npm run check` reports 0 errors and 0 warnings; `npm test` passes; `npm run build` succeeds.
  </acceptance_criteria>
  <done>The single 'My characters' <select> (All members / My characters / each assigned char) drives the four grids' filtered data with default-off all-members behavior, the SearchBox stays guild-wide, no backend/grid file is touched, and check(0/0)/test/build are green.</done>
</task>

</tasks>

<browser_smoke_gap>
**FLAGGED — NOT covered by `npm test` (web vitest is node-only, no jsdom, no @testing-library/svelte — `web/vite.config.ts` runs only the `server` project; project memory `web-tests-node-only-blind-to-dom`).** The node test (Task 1) proves the `myview.ts` predicate, but it CANNOT see the Task-2 Svelte wiring: the `<select>` rendering/onchange, the default-off "All members" behavior, the "toggle back to all members" round-trip, the drill-down narrowing the grid, the distinct empty-state copy, and the zero-assigned-characters disabled/hint state are ALL DOM behavior invisible to CI. This exact gap shipped 2 crashing BLOCKERs in P15 and stayed a flagged gap in P26.

Required after execution: run `/gsd-ui-review 27`, then browser-smoke against a DEPLOYED build or a full local stack (NOT `npm run dev` against prod — project memory `web-local-dev-cant-auth-against-prod`: the dev frontend hits the live API and Discord login bounces to prod; a logged-in smoke needs deploy-then-smoke OR a local backend with `SQUIREBOT_COOKIE_INSECURE` + `PUBLIC_API_BASE` + a seeded `sb_session`). Smoke checklist: (1) page loads showing ALL members by default; (2) selecting "My characters" narrows all four grids to the caller's chars only; (3) selecting a single character drills into just that char; (4) selecting "All members" restores the full all-members grid; (5) the cross-guild SearchBox still searches everyone even with the filter on; (6) a member with zero claimed characters sees the disabled/hint affordance, not a dead empty grid; (7) character names in the `<select>` render escaped (no markup injection).
</browser_smoke_gap>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord session cookie → Go read API | Authenticated session (RequireSession) crosses here; the server is the ONLY gate. Both `/api/v1/views/*` and `/api/v1/assignments/mine` already authorize by session, not by per-caller row scoping. |
| Go read API → browser (this phase) | All-members view rows + the caller's own MyCharacter[] arrive in-memory; the new filter runs ENTIRELY in this browser. |

## Load-bearing NEGATIVE security property

This filter is **NOT access control.** The view API already returns the identical all-members rows to every authenticated session; the client filter can only narrow what THIS browser displays and must NEVER be relied on to hide data from any other member. There is nothing to leak that the API did not already authorize for this session. Do NOT add a `?mine=1` param or a per-caller server filter — that would convert a UX convenience into an access-control decision, contradicting the locked consolidated-views rule. (ASVS V4, negative requirement.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | The "My characters" filter framed/relied upon as privacy/access control | mitigate | Document in `myview.ts`'s header (mirroring `assignments.ts`) that the helper is presentation-only and never a security boundary; the server's RequireSession is the gate. The all-members rows are intentionally shown to every session — the filter only narrows local display. No row is server-withheld. |
| T-27-02 | Tampering / XSS | A malicious character name rendered in the drill-down `<select>` option labels or any new note | mitigate | Render every character name via plain `{}` (Svelte auto-escapes). NEVER `{@html}`. Acceptance criterion `grep -c '{@html}' +page.svelte == 0` enforces this (repo standing rule: MyCharactersPanel T-26-16, SettingsMenu T-15-22). |
| T-27-03 | Elevation / Access Control (IDOR) | A member "drilling into" another member's character via the filter control | mitigate | The drill-down `<select>` options are sourced ONLY from `fetchMyCharacters()` (session-scoped, IDOR-safe server-side — Phase 26). The control never offers another member's character. (Viewing another member's single character via the un-narrowed all-members grid is by design, not a leak.) |
| T-27-04 | Spoofing / Identity | Client deciding "which characters are mine" from untrusted client state | mitigate | "Mine" is derived solely from the server's session-scoped `/api/v1/assignments/mine` response, never from a client-supplied identity. No new auth surface is added. |
| T-27-05 | Denial of Service | Filtering large row sets in the browser | accept | ~12 guildies × ~10 chars; row counts are tiny and already fully in memory (the existing Promise.all). A linear `Array.filter` per `$derived` recompute is negligible. No mitigation needed. |
</threat_model>

<verification>
- Phase requirements MYVIEW-01 (quick-filter to my characters, all-members visibility unchanged) and MYVIEW-02 (single-character drill-down) are both delivered by the one `<select>` control + the pure predicate.
- `cd web && npm run check` → 0 errors / 0 warnings.
- `cd web && npm test` → green (includes the new `myview.test.ts`).
- `cd web && npm run build` → succeeds.
- `git status --porcelain` shows changes confined to `web/src/` (no `internal/`, no `cmd/`, no `*.sql`) — confirming ZERO backend change / ZERO migration.
- `DataGrid.svelte` and `columns.ts` are unmodified (consolidated-views LOCK preserved — one reusable grid fed pre-filtered data).
- Browser-smoke + `/gsd-ui-review 27` performed per `<browser_smoke_gap>` (the DOM behavior is NOT covered by `npm test`).
</verification>

<success_criteria>
- A member can switch the consolidated views between "All members" (default), "My characters", and a single assigned character via one EQ-themed `<select>`; the grid narrows accordingly while all-members visibility is unchanged for everyone (MYVIEW-01, MYVIEW-02).
- The filter is client-side over the single reusable DataGrid: no per-character view tabs, no second grid, no server-side row scoping, no row hidden from another member's fetch.
- The cross-guild SearchBox remains guild-wide.
- `npm run check` (0/0), `npm test`, `npm run build` all pass; no backend/migration change.
- The name-join agreement (view builder ≡ assignment store, both raw `character.name`) is verified and recorded; the predicate is case-insensitive-safe regardless.
</success_criteria>

<output>
After completion, create `.planning/phases/27-my-characters-inventory-filter/27-01-SUMMARY.md` (record: the name-join verdict; the exact files changed confined to `web/src/`; that DataGrid/columns/api.ts were untouched; and the explicit browser-smoke + `/gsd-ui-review 27` gap that remains open after green `npm test`).
</output>
