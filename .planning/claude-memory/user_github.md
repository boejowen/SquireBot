---
name: User's GitHub identity
description: User's GitHub username is `boejowen` (NOT `jbowen-mn` despite the @mncivic.com email). Personal GitHub for SquireBot.
type: user
originSessionId: 21f7e633-9f81-410d-aaed-1ec2e411a483
---
User's GitHub username: **`boejowen`**.

Email domain `@mncivic.com` is unrelated to GitHub username — the email is work, the GitHub is personal.

Canonical repo for SquireBot: `https://github.com/boejowen/SquireBot.git` (note mixed-case `SquireBot`).

Go module path should be `github.com/boejowen/SquireBot` (matches GitHub repo casing).

**Why this memory exists:** in the initial Phase 1 session (2026-05-01), Plan 01-01 was executed with `github.com/jbowen-mn/squirebot` based on a guess from the email domain. The user corrected this mid-Phase-1 wrap-up. A rename hotfix landed shortly after. Don't repeat the guess in future sessions; if you need a username and the user hasn't given one explicitly, ask.

Same Google account in use for SquireBot OAuth: `boejowen@gmail.com`. Consistent personal-identity branding.
