---
phase: 30-app-shell-5-tab-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/components/SiteShell.svelte
  - web/src/lib/components/SettingsMenu.svelte
  - web/src/lib/components/ThemePicker.svelte
  - web/src/routes/+layout.svelte
  - web/src/lib/theme/themeContext.ts
  - web/src/routes/+page.svelte
  - web/src/routes/guild-views/+page.svelte
  - web/src/routes/+page.ts
  - web/src/routes/characters/+page.svelte
  - web/src/routes/inventory/+page.svelte
  - web/src/routes/banks/+page.svelte
  - web/src/routes/wantlist/+page.ts
  - web/src/routes/notifications/+page.ts
  - web/src/routes/account/+page.ts
  - web/src/routes/char-meta/+page.ts
  - web/src/routes/my-characters/+page.ts
  - web/src/routes/admin/+page.ts
  - web/src/routes/bank-coin/+page.ts
  - web/src/lib/__tests__/charmeta.test.ts
autonomous: true
requirements: [NAV-01, NAV-04]
must_haves:
  truths:
    - "Five top-level tabs (Characters, Inventory, Banks, Wishlist, Settings, in that order) render on every authenticated route with the current tab marked aria-current=page"
    - "Every old path (/wantlist, /notifications, /account, /char-meta, /my-characters, /admin, /bank-coin) redirects to its new home; / lands on the preserved guild-views inventory surface"
    - "The Characters/Inventory/Banks placeholders each carry a working link to /guild-views so guild-wide lookup is never lost mid-transition"
    - "The header gear SettingsMenu is dissolved to identity + Sign out only; the Theme picker and the 4 nav links are gone from it"
    - "The unread badge renders on the Wishlist tab label (not the header); the single [data-theme] writer in +layout is intact"
  artifacts:
    - path: "web/src/lib/components/SiteShell.svelte"
      provides: "5-tab strip with active indicator + Wishlist-tab unread badge; old header nav removed; theme context provided"
      contains: "tab-strip"
    - path: "web/src/lib/theme/themeContext.ts"
      provides: "THEME_KEY symbol + ThemeContext type for the picker-relocation bridge"
      contains: "THEME_KEY"
    - path: "web/src/routes/guild-views/+page.svelte"
      provides: "the preserved consolidated 4-view home (verbatim move of today's +page.svelte)"
      min_lines: 400
    - path: "web/src/routes/+page.ts"
      provides: "/ redirect to /guild-views"
      contains: "redirect(307, '/guild-views')"
    - path: "web/src/routes/characters/+page.svelte"
      provides: "Characters coming-soon placeholder + /guild-views link"
      contains: "/guild-views"
    - path: "web/src/routes/wantlist/+page.ts"
      provides: "redirect stub to /wishlist"
      contains: "redirect(308, '/wishlist')"
    - path: "web/src/routes/account/+page.ts"
      provides: "redirect stub to /settings"
      contains: "redirect(308, '/settings')"
  key_links:
    - from: "web/src/lib/components/SiteShell.svelte"
      to: "$page.url.pathname"
      via: "isActive() derived from $app/stores page"
      pattern: "\\$page\\.url"
    - from: "web/src/lib/components/SiteShell.svelte"
      to: "$lib/stores/unread"
      via: "unreadCount read rendered on the Wishlist tab <a>"
      pattern: "unreadCount"
    - from: "web/src/routes/+layout.svelte"
      to: "themeContext THEME_KEY"
      via: "setContext bridge so a relocated ThemePicker can mutate theme"
      pattern: "setContext\\(THEME_KEY"
---

<objective>
Build the navigation spine and chrome rework: the persistent 5-tab strip (NAV-01), the
dissolved gear → identity-only affordance + theme-context bridge (D-06), the unread badge
relocated onto the Wishlist tab (NAV-04), all old-path → new-path redirects (D-02), the
preserved `/guild-views` interim inventory surface with `/` landing there (D-03b/D-04), and
the three "coming soon" placeholder tabs (D-03). This plan delivers the routing/chrome
foundation that Plan 02's Wishlist + Settings bodies plug into.

Purpose: NAV-01 (five tabs, active indicator, reachable from any page) + the chrome half of
NAV-04 (badge moves to the Wishlist tab). No tab CONTENT for Characters/Inventory/Banks
(Phases 31/32/33) — only the shell, the placeholders, and the redirects.

Output: Reworked `SiteShell` (tab strip + badge + identity), pruned `SettingsMenu`, a theme
context module, the `/guild-views` verbatim move, `/` + 7 redirect stubs, 3 placeholder
routes, and an updated `charmeta.test.ts` reflecting the new IA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-app-shell-5-tab-navigation/30-CONTEXT.md
@.planning/phases/30-app-shell-5-tab-navigation/30-RESEARCH.md
@.planning/phases/30-app-shell-5-tab-navigation/30-PATTERNS.md
@.planning/phases/30-app-shell-5-tab-navigation/30-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use these directly. -->

From web/src/lib/components/AuthGate.svelte (the context idiom to MIRROR for theme):
```ts
export const SESSION_KEY = Symbol('session');
export type SessionGetter = () => Session | null;
// in the component: setContext(SESSION_KEY, (() => session) satisfies SessionGetter);
```

From web/src/lib/theme/themes.ts (already imported by +layout/SiteShell):
```ts
export function loadTheme(): ThemeKey;
export function applyTheme(theme: ThemeKey, root: HTMLElement | null): void;
export type ThemeKey;             // 'velious'|'vanilla'|'kunark'|'minimalist'|'heavy'
export const THEME_KEYS: ThemeKey[];
```

From web/src/lib/stores/unread.ts (UNCHANGED — do not duplicate):
```ts
export const unreadCount;          // module-level writable<number>
export function refreshUnread(): Promise<void>;
```

From web/src/lib/components/ThemePicker.svelte (current contract — keep it $bindable):
```svelte
let { theme = $bindable() }: { theme: ThemeKey } = $props();
```

From web/src/lib/components/SettingsMenu.svelte <script module> (KEEP these helpers):
```ts
export function avatarUrlFor(session): string | null;   // Discord CDN PNG or null
export function menuKeyAction(key, open): 'close'|'ignore';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme-context bridge + dissolve the gear to identity + Sign out (D-06)</name>
  <read_first>
    - web/src/lib/components/SettingsMenu.svelte (full — analog is itself; prune to a subset)
    - web/src/routes/+layout.svelte (lines 1-48 — the single [data-theme] $effect writer + bind:theme chain to preserve)
    - web/src/lib/components/AuthGate.svelte (lines 1-14, 57-58 — the SESSION_KEY setContext idiom to mirror; PATTERNS.md "Theme-context bridge" §)
    - web/src/lib/components/ThemePicker.svelte (full — stays $bindable; a Settings-side wrapper bridges context→bindable in Plan 02)
    - 30-PATTERNS.md "Theme-context bridge" + "SettingsMenu.svelte (dissolve)" sections
    - 30-RESEARCH.md §5 (theme context option (a)) + Pitfall 3
  </read_first>
  <action>
    Create `web/src/lib/theme/themeContext.ts` exporting a context key + accessor type that
    mirrors AuthGate's SESSION_KEY idiom:
    ```ts
    import type { ThemeKey } from './themes';
    export const THEME_KEY = Symbol('theme');
    export type ThemeContext = { get: () => ThemeKey; set: (v: ThemeKey) => void };
    ```
    In `web/src/routes/+layout.svelte`: KEEP the single `$effect(() => applyTheme(theme, rootEl ?? null))`
    writer (line 24-26) UNCHANGED — it stays the only `[data-theme]` writer (Pitfall 3). Add
    `setContext(THEME_KEY, { get: () => theme, set: (v) => (theme = v) } satisfies ThemeContext)`
    (import setContext from 'svelte' and THEME_KEY/ThemeContext from '$lib/theme/themeContext').
    The `bind:theme` on `<SiteShell>` may stay or be dropped — but the picker no longer rides it
    (it moves to Settings via the context), so SiteShell no longer needs `theme` for the menu.
    Per D-06 keep the `data-theme={theme}` write on `.theme-root` exactly as-is.

    In `web/src/lib/components/SettingsMenu.svelte` — PRUNE to identity + Sign out only (D-06 /
    UI-SPEC §C). KEEP verbatim: the `<script module>` helpers (`avatarUrlFor`, `menuKeyAction`),
    the identity render block (lines 167-179 — the T-15-22 escaped-username `{session.username}`
    via plain `{}` ONLY, never `{@html}`; avatar `alt={session.username}`), the a11y mechanics
    (`aria-haspopup`/`aria-expanded`/`aria-controls`, role=menu/menuitem, Escape→close+restore-focus,
    outside-pointerdown close, route-change close, focus-first-item-on-open — lines 84-145, 162-163),
    and the `signOut()` flow (lines 101-106, 204-213). DELETE: the `ThemePicker` import + its
    `<div class="menu-theme"><ThemePicker bind:theme /></div>` block (lines 65, 181-184), the 4 nav
    links `/account` `/char-meta` `/my-characters` `/admin` + their officer `{#if}` (lines 190-198),
    and the `theme = $bindable()` prop (line 73 → props become `{ session }` only). Swap the trigger
    glyph from the gear `<Settings>` to the avatar+username+`ChevronDown` per UI-SPEC §C: import
    `ChevronDown from '@lucide/svelte/icons/chevron-down'`; the trigger shows `avatarUrl ? <img>`
    else `<UserIcon>`, the escaped `{session.username}`, the officer `<Shield>` if `isOfficer`, and
    the chevron; `aria-label={`Account menu, ${session.username}`}`. Do NOT add a "Settings" menu
    item (one place per concept — the tab strip is the canonical route in). 44px target + tokens +
    `:focus-visible{outline:2px solid var(--accent)}` preserved.
  </action>
  <acceptance_criteria>
    - File `web/src/lib/theme/themeContext.ts` exists and contains `export const THEME_KEY` and `export type ThemeContext`
    - `web/src/routes/+layout.svelte` contains `setContext(THEME_KEY` AND still contains exactly one `applyTheme(` call inside a `$effect`
    - `web/src/lib/components/SettingsMenu.svelte` no longer contains `ThemePicker` (grep -c = 0), no longer contains `href="/account"`, `href="/char-meta"`, `href="/my-characters"`, or `href="/admin"` (each grep -c = 0)
    - `SettingsMenu.svelte` still contains `{session.username}` and `alt={session.username}` and contains NO `{@html}` (grep -c '{@html' = 0)
    - `SettingsMenu.svelte` still contains `signOut` and `aria-haspopup="menu"`
    - `SettingsMenu.svelte` imports `chevron-down`
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep -E "themeContext|SettingsMenu" || echo "no type errors in changed files"</automated>
  </verify>
  <done>Theme context module exists; +layout keeps the single writer + provides the context; SettingsMenu is pruned to identity + Sign out with the T-15-22 escaping intact and no theme/nav links.</done>
</task>

<task type="auto">
  <name>Task 2: 5-tab strip + Wishlist-tab badge in SiteShell (D-01 five real routes, active indicator / NAV-01 / NAV-04 chrome)</name>
  <read_first>
    - web/src/lib/components/SiteShell.svelte (full — lines 64-92 header nav to replace; lines 42-60 badge derivations to keep; lines 145-192 .char-meta-nav + .unread-badge CSS to seed the strip)
    - web/src/routes/+page.svelte (lines 315-327 the .view-nav/.tab active-state markup idiom; lines 407-440 the .tab/.tab.active CSS to copy verbatim)
    - 30-PATTERNS.md "SiteShell.svelte (the 5-tab strip + badge)" + "Unread badge relocation" sections
    - 30-UI-SPEC.md §A/§B/§B2 (frame, tab strip states, badge) + §H (mobile horizontal-scroll) + §Accessibility (1-5)
    - 30-RESEARCH.md §2 (tab structure) + Code Examples (Wishlist tab entry)
  </read_first>
  <action>
    In `web/src/lib/components/SiteShell.svelte`, REMOVE the old header nav (lines 74-90: the
    `Inventory`/`Wantlist`/`Notifications` `.char-meta-nav` links + the `<SettingsMenu bind:theme>`
    mount) and REPLACE the post-auth header structure with: wordmark top-left (keep `<a href="/">`
    line 65 unchanged), the pruned `<SettingsMenu {session} />` (no `bind:theme` now) in
    `.shell-controls` top-right, and a NEW tab strip rendered as a sibling band UNDER the header
    (in the layout, present on every route — UI-SPEC §A), shown only when `session?.authenticated`.

    Tab strip (UI-SPEC §B, RESEARCH §2): use `<a href>` route links (NOT buttons). Keep the
    existing `import { page } from '$app/stores'` (line 12 — repo convention, NOT $app/state) and add:
    ```svelte
    const TABS = [
      { href: '/characters', label: 'Characters' },
      { href: '/inventory',  label: 'Inventory' },
      { href: '/banks',      label: 'Banks' },
      { href: '/wishlist',   label: 'Wishlist' },
      { href: '/settings',   label: 'Settings' }
    ];
    let path = $derived($page.url?.pathname ?? '');
    function isActive(href: string) { return path === href || path.startsWith(href + '/'); }
    ```
    Render `<nav class="tab-strip" aria-label="Primary">` with each tab as
    `<a href={t.href} class="tab" class:active={isActive(t.href)} aria-current={isActive(t.href) ? 'page' : undefined}>{t.label}</a>`,
    in the spec-fixed order Characters · Inventory · Banks · Wishlist · Settings.

    Wishlist tab carries the relocated unread badge (NAV-04). KEEP the existing badge derivations
    (lines 42-60: `count = $derived($unreadCount)`, `badgeText` `9+` cap, the `onMount(refreshUnread)`
    and the route-change `$effect` — all global, stay in SiteShell). RE-LABEL the accessible name to
    Wishlist phrasing. Render the Wishlist tab as:
    ```svelte
    <a href="/wishlist" class="tab notify-tab" class:active={isActive('/wishlist')}
       aria-current={isActive('/wishlist') ? 'page' : undefined}
       aria-label={count > 0 ? `Wishlist, ${count} unread` : 'Wishlist'}>
      Wishlist
      {#if count > 0}<span class="unread-badge" aria-hidden="true">{badgeText}</span>{/if}
    </a>
    ```
    CSS: copy the `.tab`/`.tab.active`/`.tab:focus-visible` rules from `+page.svelte:407-440`
    (44px min-height, 8px/16px padding, 13px display uppercase 0.08em, `border-bottom:2px solid
    transparent`, opacity 0.7; active = accent color + `border-bottom-color: var(--accent)` +
    opacity 1; focus-visible `outline:2px solid var(--accent)` with `outline-offset:-2px`). Keep
    the existing `.unread-badge` CSS (lines 177-192) but render it inline (`margin-left:6px`, not
    absolute) per UI-SPEC §B2. Mobile (UI-SPEC §H): `.tab-strip { display:flex; flex-wrap:nowrap;
    overflow-x:auto; }` below ~520px with `scroll-snap-type:x proximity` + `scroll-snap-align:start`
    on tabs; add a reduced-motion-aware `scrollIntoView({inline:'nearest',block:'nearest',
    behavior: prefersReducedMotion ? 'auto' : 'auto'})` of the active tab on route change. Tokens
    ONLY, no new palette. Remove the now-unused `theme` prop only if nothing else uses it; if
    `bind:theme` was dropped from the layout, drop the `theme = $bindable()` prop here too.
  </action>
  <acceptance_criteria>
    - `web/src/lib/components/SiteShell.svelte` contains `class="tab-strip"` and a `TABS` array with exactly the 5 hrefs `/characters`, `/inventory`, `/banks`, `/wishlist`, `/settings` in that order
    - SiteShell contains `aria-current={isActive(` on the tab links AND `aria-label="Primary"` (or `aria-label='Primary'`) on the `<nav>`
    - SiteShell no longer contains `href="/wantlist"` or `href="/notifications"` (grep -c each = 0) and no longer renders `<a href="/" class="char-meta-nav">Inventory`
    - The Wishlist tab `<a href="/wishlist"` block contains both `{badgeText}` and `aria-label={count > 0 ? `Wishlist`
    - SiteShell still imports `unreadCount` and `refreshUnread` from `$lib/stores/unread` and still calls `refreshUnread()` (badge store NOT duplicated)
    - SiteShell renders `<SettingsMenu` with no `bind:theme` (grep `bind:theme` in SiteShell = 0)
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run check 2>&1 | tail -5</automated>
  </verify>
  <done>The 5-tab strip renders in the post-auth shell with path-derived aria-current; the Wishlist tab hosts the relocated unread badge; the badge store is read, not duplicated; tokens-only styling with 44px + focus-visible.</done>
</task>

<task type="auto">
  <name>Task 3: /guild-views move + / redirect + 7 redirect stubs + 3 placeholders + test migration (D-02/D-03/D-03b/D-04)</name>
  <read_first>
    - web/src/routes/+page.svelte (full — the ~600-line home that MOVES verbatim to /guild-views; note lines 164-170 the ?view= seed, 309 /my-characters link, 386 /bank-coin "Record coin" link)
    - web/src/routes/+page.ts (lines 1-7 — current `prerender = true` to REPLACE with the redirect)
    - web/src/routes/+layout.ts (lines 6-7 — the `export const ssr/prerender` shape the stubs mirror)
    - web/src/routes/account/+page.svelte (lines 13-26, 44-56 — the thin .form-card/.form-title/.form-purpose wrapper the 3 placeholders mirror)
    - web/src/lib/__tests__/charmeta.test.ts (lines 187-259 — the SettingsMenu/SiteShell source-grep tests that BREAK on the dissolve and MUST be migrated to the new IA)
    - 30-PATTERNS.md "Redirect stubs", "guild-views/+page.svelte", "{characters,inventory,banks}/+page.svelte", "Internal-link repoints" sections
    - 30-UI-SPEC.md §E (placeholder body + the D-03b preserved-view link) + Copywriting table (exact placeholder strings)
    - 30-RESEARCH.md §1 (redirect mechanism), §3 (preserved view), Pitfall 2 (don't try/catch the redirect; keep prerender=false), Pitfall 4/5 (preserve lookup + ?view=bank round-trip)
  </read_first>
  <action>
    1. MOVE today's home verbatim: copy the ENTIRE contents of `web/src/routes/+page.svelte` to
    NEW `web/src/routes/guild-views/+page.svelte` UNCHANGED (the 4-view consolidated home +
    SearchBox + My/Guild scope + the `?view=` seed at lines 164-170 — keep intact so
    `/guild-views?view=bank` deep-links work). Add a `<svelte:head><title>SquireBot — guild views
    (classic)</title>` if not already present and label it "Guild views (classic)" in any visible
    heading. Inside this MOVED file, repoint its internal "Record coin" link (was line 386
    `href="/bank-coin"`) and the bank round-trip to `/guild-views?view=bank`, and the "Claim
    characters" link (was line 309 `href="/my-characters"`) to `/settings`. Do NOT delete the
    surface (Deferred list — retired only when 31-33 land).

    2. REPLACE `web/src/routes/+page.ts` entirely (drop `prerender = true`) with the D-04 default
    landing redirect:
    ```ts
    import { redirect } from '@sveltejs/kit';
    export const ssr = false;
    export const prerender = false;
    export function load() { redirect(307, '/guild-views'); }  // 307 temporary — flips to /characters post-Phase-31
    ```
    Then DELETE the old `web/src/routes/+page.svelte` body (it now lives at /guild-views).

    3. Create the 7 redirect stubs, each a `+page.ts` with `ssr=false; prerender=false;` and an
    UNCAUGHT `redirect(308, '<target>')` in `load()` (NEVER wrap in try/catch — Pitfall 2). After
    creating each stub, DELETE the corresponding old `+page.svelte` body:
      - `web/src/routes/wantlist/+page.ts` → `redirect(308, '/wishlist')`   (delete wantlist/+page.svelte)
      - `web/src/routes/notifications/+page.ts` → `redirect(308, '/wishlist')`   (delete notifications/+page.svelte)
      - `web/src/routes/account/+page.ts` → `redirect(308, '/settings')`   (delete account/+page.svelte)
      - `web/src/routes/char-meta/+page.ts` → `redirect(308, '/settings')`   (delete char-meta/+page.svelte)
      - `web/src/routes/my-characters/+page.ts` → `redirect(308, '/settings')`   (delete my-characters/+page.svelte)
      - `web/src/routes/admin/+page.ts` → `redirect(308, '/settings')`   (delete admin/+page.svelte)
      - `web/src/routes/bank-coin/+page.ts` → `redirect(308, '/settings')`   (delete bank-coin/+page.svelte)

    4. Create the 3 placeholder routes (`characters`, `inventory`, `banks`/+page.svelte), each
    mirroring the account/+page.svelte thin `.form-card` wrapper but rendering the UI-SPEC §E
    dashed-border placeholder card (`border:1px dashed var(--border)`, `padding:48px 24px`,
    `text-align:center`): a 20px display Heading (the tab name), a dimmed Body description, and a
    REAL accent-bordered link to `/guild-views` styled like `+page.svelte`'s `.record-coin`
    (min-height 44px, uppercase 13px). Use the EXACT Copywriting strings (UI-SPEC table):
      - Characters: heading "Characters"; body "See what each guildie is carrying — the in-game-style inventory window is coming here soon."; link "View the current inventory →" → `/guild-views`
      - Inventory: heading "Inventory"; body "Look up which characters hold an item, guild-wide. This item-centric view is being built."; link "View the current inventory →" → `/guild-views`
      - Banks: heading "Banks"; body "Browse the guild banks and their total value. This view is being built."; link "View the current bank →" → `/guild-views?view=bank`
    Do NOT add any search bar to these stubs (D-08 — no inert search).

    5. Repoint the remaining stale internal links flagged by RESEARCH "Runtime State Inventory":
    `web/src/lib/components/NotificationPrefsPanel.svelte:166` `href="/wantlist"` → `/wishlist`.
    (char-meta/my-characters cross-links die with their bodies in step 3; SiteShell links handled
    in Task 2.) The wordmark stays `href="/"` (it redirects to /guild-views — fine).

    6. MIGRATE `web/src/lib/__tests__/charmeta.test.ts` to the new IA. The `describe('SettingsMenu.svelte
    source — the /char-meta nav is member-accessible (D-03)')` block (≈lines 223-259) source-greps
    `SettingsMenu.svelte` for `href="/char-meta"`/`href="/admin"` and `SiteShell.svelte` for
    `<SettingsMenu`-under-`session?.authenticated` — these reflect the OLD gear IA and now fail.
    Rewrite them to assert the NEW contract: (a) `SettingsMenu.svelte` no longer contains those nav
    hrefs and is pruned to identity + Sign out (assert it contains `signOut` + `{session.username}`
    and NOT `href="/char-meta"`); (b) the member panels' new home is the Settings page — assert
    `routes/settings/+page.svelte` (created in Plan 02; if not yet present, gate this assertion
    behind file existence OR move it to Plan 02's test scope — prefer asserting against
    `SETTINGS_MENU_SOURCE` not containing the old links here, and leave the Settings-page-composition
    assertions to Plan 02). KEEP the T-15-22 username-escaping assertion if present. Do NOT weaken
    any security assertion — convert "officer-gated /admin link in menu" into "no /admin link in the
    dissolved menu" (the officer gate now lives on the Settings Admin section, asserted in Plan 02).
  </action>
  <acceptance_criteria>
    - `web/src/routes/guild-views/+page.svelte` exists and is ≥400 lines (the moved home body) and contains `URLSearchParams` (the ?view= seed preserved)
    - `web/src/routes/+page.ts` contains `redirect(307, '/guild-views')` and does NOT contain `prerender = true`
    - `web/src/routes/+page.svelte` no longer exists (old home body deleted)
    - All 7 redirect stubs exist with the correct targets: `grep -rl "redirect(308, '/wishlist')" web/src/routes/wantlist web/src/routes/notifications` returns both; `grep -rl "redirect(308, '/settings')" web/src/routes/account web/src/routes/char-meta web/src/routes/my-characters web/src/routes/admin web/src/routes/bank-coin` returns all five
    - No redirect stub contains `try` (grep -c 'try' across the 8 +page.ts = 0) and each contains `prerender = false`
    - The old bodies are gone: `web/src/routes/{wantlist,notifications,account,char-meta,my-characters,admin,bank-coin}/+page.svelte` do NOT exist
    - The 3 placeholders exist; each of `characters/+page.svelte`, `inventory/+page.svelte`, `banks/+page.svelte` contains a link to `/guild-views` and contains NO `<input` or `SearchBox` (no inert search)
    - `web/src/lib/components/NotificationPrefsPanel.svelte` contains `href="/wishlist"` and not `href="/wantlist"`
    - `web/src/lib/__tests__/charmeta.test.ts` no longer asserts `SETTINGS_MENU_SOURCE.indexOf('href="/admin"')` as a positive expectation (the dissolved-menu assertions are inverted) and `npm test` passes
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run check 2>&1 | tail -3 && npm test 2>&1 | tail -8 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>/ redirects to /guild-views; all 7 old paths redirect (308) with bodies deleted; the preserved 4-view home lives at /guild-views with its ?view=bank seed intact; 3 placeholders link to it with no inert search; charmeta.test.ts reflects the dissolved-menu IA; check + test + build all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord identity → DOM | `session.username`/`avatar` are user-controlled (Discord) and rendered in the relocated identity affordance |
| Client route → server API | Tab/redirect changes are client-only; the Go API (RequireSession/RequireOfficer) is the real gate, unchanged |
| Old bookmark URL → redirect target | External bookmarks hit redirect stubs whose targets are static literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering/Elevation (XSS) | SettingsMenu identity block (relocated trigger) | mitigate | Render `session.username` via plain `{}` interpolation ONLY (Svelte auto-escape); avatar `alt` = same escaped name; NO `{@html}` introduced (T-15-22 invariant preserved verbatim — acceptance grep `{@html` = 0) |
| T-30-02 | Information Disclosure | Redirect stubs (D-02) | mitigate | All `redirect()` targets are fixed internal static paths (`/wishlist`, `/settings`, `/guild-views`); NO query/path/user input flows into the location → no open-redirect |
| T-30-03 | Elevation of Privilege | Officer-only surfaces during the rework | accept | This plan removes the officer `/admin` link from the dissolved menu and redirects `/admin` → `/settings`; the officer-gated Admin SECTION is built in Plan 02. No officer-only data is exposed to non-officers in this plan's bundle — the placeholders/redirects/strip carry none. The Go API officer re-check remains the real boundary (unchanged). |
| T-30-04 | Denial of Service (self-inflicted) | `/` apex after dropping `prerender=true` | accept | A2 (RESEARCH): Caddy SPA fallback serves `200.html` for the apex; the `/` redirect is a client-side load. Risk if Caddy only serves `index.html` for `/` → mitigated by the mandatory browser-smoke hard-refresh of the apex (verification below); cheap to restore a minimal prerendered `/` if it 404s. |
</threat_model>

<verification>
- `cd web && npm run check` → 0 errors (svelte-check / tsc).
- `cd web && npm test` → all green (incl. the migrated charmeta.test.ts).
- `cd web && npm run build` → succeeds (SPA bundle emits; the redirect stubs and placeholder routes compile).
- Source greps in the acceptance_criteria above all hold.
- **Browser-smoke is DEFERRED to the end of Plan 02** (the deployed-build smoke covers Plan 01 + 02 together) — see Plan 02's verification. Node vitest is DOM-blind (web-tests-node-only-blind-to-dom): green `npm test` does NOT prove the tabs render or the redirects fire. Do NOT treat green tests as "verified" for this routing rework.
</verification>

<success_criteria>
- Five tabs (Characters · Inventory · Banks · Wishlist · Settings, in order) render on every authenticated route with `aria-current="page"` on the current tab (NAV-01).
- Every old path redirects to its new home; `/` lands on `/guild-views` (D-02/D-04); no bookmark 404s (confirmed in the Plan 02 deployed smoke).
- The 3 placeholders each link to the preserved `/guild-views` lookup (D-03b) and carry no inert search bar (D-08).
- The gear is dissolved to identity + Sign out; the single `[data-theme]` writer is intact; the theme context is provided for Plan 02's relocated picker (D-06).
- The unread badge renders on the Wishlist tab; the badge store is read, not duplicated (NAV-04 chrome).
</success_criteria>

<output>
After completion, create `.planning/phases/30-app-shell-5-tab-navigation/30-01-SUMMARY.md`.
</output>
