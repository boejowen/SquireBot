# Requirements: SquireBot — Milestone v2.5 "Ownership Cleanup" (ARCHIVED)

> **ARCHIVED 2026-06-22** on milestone close. All 4 requirements satisfied + deployed live (prod schema v15) + officer browser-smoke approved. Fresh REQUIREMENTS.md is created with the next milestone via `/gsd-new-milestone`.

**Milestone goal:** Reconcile character-ownership semantics for a guild that shares P99 logins — make guild banks/bots owner-less, and stop eviction from over-deleting shared characters. Backend + a small web change; one schema migration (00015); watcher untouched (no `v*` tag).

**Origin:** Promoted backlog items 999.35 + 999.36, both deferred from quick task `260621-u6j` (which dropped the single-owner write gate). Context: memory `cross-owner-character-misbinding.md`.

## v2.5 Requirements — all COMPLETE

### Ownership (OWN)

- [x] **OWN-01** — An officer can designate any character as a guild bank or bot without first "owning" it; designation is decoupled from upload-ownership (no "claim" step). ✅ Phase 35
- [x] **OWN-02** — A designated guild bank/bot is owner-less (guild-held) and is NOT removed or archived when the member who first uploaded it is evicted. ✅ Phase 35 (sentinel exclusion in the picker + the CR-01 write-path guard)
- [x] **OWN-03** — Evicting a guildie removes only that member's own characters, not shared characters that other guildies also play and upload. ✅ Phase 36 (narrowed cascade + the all-shared-owner web fix; deployed + browser-smoke approved)
- [x] **OWN-04** — Existing designated banks/bots bound to an individual owner (e.g. `Findom` → owner 9) migrate to the owner-less model automatically, with no manual fixup. ✅ Phase 35 (00015 backfill — Findom + Slowscales repointed to the sentinel live)

## Outcomes / changed requirements

- **OWN-03 scope expanded mid-milestone (backend-only → backend + web):** the plan-checker proved that narrowing the eviction preview empties it for an "all-shared owner", which disabled the Evict button so their guild code couldn't be revoked. The user ratified adding a web change (`preserved_shared_count` + `EvictionForm` gating) to deliver the requirement end-to-end. No watcher change; still no `v*` tag.
- All other requirements shipped exactly as scoped.

## Out of Scope (held)

- **Any watcher change** — the single-owner write gate was already relaxed in `260621-u6j` (watcher `v2.1.2`); v2.5 is backend + web only.
- **Re-introducing per-character write ownership / cross-owner rejection** — explicitly reversed; the guild shares characters by design.
- **The v2.2 Track-2 Discord pinger** (invite-gated) — unrelated, still parked.
- **Self-service eviction** (backlog 999.5) — deferred.

## Traceability (final)

| REQ-ID | Phase | Status |
|--------|-------|--------|
| OWN-01 | 35 | Complete (2026-06-22) |
| OWN-02 | 35 | Complete (2026-06-22) |
| OWN-03 | 36 | Complete (2026-06-22) — deployed + browser-smoke approved |
| OWN-04 | 35 | Complete (2026-06-22) — Findom + Slowscales repointed live |

*Created 2026-06-22 for milestone v2.5; archived 2026-06-22 on close. 4/4 requirements satisfied.*
