---
phase: 34-wishlist-rework-per-character-per-slot-upgrades
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified: []
autonomous: false
requirements: [WISH-01, WISH-02, WISH-03, WISH-04, WISH-05, WISH-06, WISH-07]
must_haves:
  truths:
    - "An R2 backup is taken BEFORE the restart (this is a goose-run deploy — 00014 DROPs wantlist_item on the live DB); the restart logs show goose migrating to version 14"
    - "The new backend binary + web bundle are live at squirebot.quest: GET /api/v1/wishlist/{char} is registered (401 without a cookie, not 404); GET /api/v1/wantlist returns 404 (route removed); the web /wishlist tab serves the per-slot wishlist"
    - "NO v* tag is pushed (the watcher is untouched — a v* tag would needlessly fire the watcher release CI)"
    - "A human browser-smokes the deployed /wishlist tab across all 5 EQ themes (the 14-point checklist) and approves"
  artifacts: []
  key_links:
    - from: "deploy restart"
      to: "live SQLite goose_db_version"
      via: "goose.Up applies 00014 → version 14 (R2 backup taken first)"
      pattern: "version: 14"
    - from: "squirebot.quest/wishlist"
      to: "GET /api/v1/wishlist/{char}"
      via: "the deployed web tab consuming the deployed read route"
      pattern: "/api/v1/wishlist"
---

<objective>
Deploy the Phase-34 backend (binary + the goose 00014 migration that runs on restart) and the web bundle to
squirebot.quest, then human-browser-smoke the rendered `/wishlist` tab across all 5 EQ themes to close
WISH-01..07. This is a GOOSE-RUN deploy (unlike P32/P33's no-migration swaps) — 00014 DROPs `wantlist_item`
on the LIVE DB, so the R2 backup MUST be taken BEFORE the restart.

Purpose: The web tab's DOM (the list, the per-slot accordion, the target rows, the ping toggle, the
suggestion add, the remove confirm, the auto-hide, the examine, the cross-wishlist search) is NOT verifiable
by node vitest (DOM-blind) — it MUST be browser-smoked on a deployed build. localhost dev can't auth against
prod, so deploy-then-smoke is the only path.

Output: a deployed backend (schema → v14) + web tab; a human-verify checkpoint approval; NO v* tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-wishlist-rework-per-character-per-slot-upgrades/34-UI-SPEC.md
@.planning/phases/34-wishlist-rework-per-character-per-slot-upgrades/34-01-SUMMARY.md
@.planning/phases/34-wishlist-rework-per-character-per-slot-upgrades/34-02-SUMMARY.md
@.planning/phases/34-wishlist-rework-per-character-per-slot-upgrades/34-03-SUMMARY.md
@docs/backend-deploy.md

<deploy_facts>
<!-- From docs/backend-deploy.md + project memory (v2-backend-live-and-ops-access, watcher-release-versioning). -->
- Backend host: Hetzner VPS 5.78.232.85; live at https://api.squirebot.quest. SSH needs ssh-agent loaded with
  the passphrase-protected id_ed25519; run remote cmds by piping a here-string to `ssh bash -s`.
- Deploy model (backend-deploy.md §D-10): cross-compile (CGO_ENABLED=0, GOOS=linux GOARCH=amd64) → scp the
  binary → cp .bak → install 0755 → systemctl restart. Migrations are EMBEDDED; goose.Up runs on startup
  (idempotent — 00014 applies on the restart). The PRIOR P26/P28 deploys are the proven v9→v10 goose-run path.
- R2 backup: the nightly `squirebot-backup.sh` → R2 `squirebot-backups`; take a MANUAL backup BEFORE this
  restart (rclone on-box → squirebot-YYYY-MM-DD.db.gz). The clean break (00014 DROPs wantlist_item) makes a
  pre-restart snapshot the rollback floor.
- Web deploy (backend-deploy.md §web): `npm run build` (adapter-static) → tarball → scp → extract to
  /var/www/squirebot.new → atomic mv-swap (keep /var/www/squirebot.old for rollback). Do NOT `scp -r` (the
  nested-dir footgun).
- Rollback: `cp /usr/local/bin/squirebot-server.bak → restart` (the old binary runs against v14 — extend-only
  SAFE for the ADDED wishlist_item; BUT the old binary's wantlist routes reference the DROPPED wantlist_item,
  so a true rollback needs the R2 backup restore for a full revert — note this in the SUMMARY). Web: `mv
  /var/www/squirebot.old back`.
- NO v* TAG (watcher-release-versioning + CORRECTION): the watcher is UNTOUCHED; a `vX.Y.Z` tag fires the
  watcher release CI needlessly. This deploy is a plain binary + web swap (+ the goose 00014 run) — same as
  P32/P33 EXCEPT it runs a migration. Do NOT push any tag.
</deploy_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build, R2-backup, deploy backend (goose 00014) + web; verify the live route surface</name>
  <files>(no repo files modified — build + remote deploy only)</files>
  <read_first>
    - docs/backend-deploy.md (the cross-compile + scp + restart steps; §web tarball+atomic-swap; the R2 backup §3)
    - 34-RESEARCH.md Runtime State Inventory (the clean-break DATA migration on the live DB) + Pitfall 1 (no schema gate)
    - The <deploy_facts> block above (host, R2, no-tag, rollback)
  </read_first>
  <action>
    Pre-flight gates (local, MUST be green before touching prod):
    - `go test ./...` — full module green.
    - `go vet ./...` clean; `go build ./...` rc=0.
    - `cd web && npm run check && npm test && npm run build` — 0/0, green, adapter-static build ok.

    Then deploy (the proven P26/P28 goose-run path):
    1. Cross-compile the backend: `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o squirebot-server
       ./cmd/squirebot-server` (a static linux/amd64 ELF).
    2. **R2 BACKUP FIRST (load-bearing — this is a DROP migration):** SSH to the box and take a manual R2
       backup (run the on-box `squirebot-backup.sh` or the documented rclone copy) → confirm
       `squirebot-YYYY-MM-DD.db.gz` lands in `squirebot-backups`. Do NOT proceed without a confirmed backup
       object — 00014 DROPs wantlist_item + rebuilds alert_log; this snapshot is the rollback floor.
    3. scp the binary to /tmp on the box; `cp /usr/local/bin/squirebot-server /usr/local/bin/squirebot-server.bak`
       (keep the old binary); install the new one 0755 to /usr/local/bin/squirebot-server.
    4. `systemctl restart squirebot-server` → tail the logs and CONFIRM `goose: successfully migrated database
       to version: 14` (goose applies 00014 — wishlist_item created, alert_log rebuilt, wantlist_item dropped).
       Confirm the service is `active (running)`, the bot reconnected, the scheduler jobs are up.
       On the box, confirm the schema: `wishlist_item` table present, `wantlist_item` GONE, `goose_db_version`
       top version = 14.
    5. Web: `npm run build` → tarball → scp → extract to /var/www/squirebot.new → atomic mv-swap (keep
       /var/www/squirebot.old). Do NOT `scp -r`.
    6. External verify (GREEN gate before the checkpoint):
       - apex `https://squirebot.quest` → 200; entry JS content-type text/javascript (fresh bundle hash).
       - `GET /api/v1/wishlist/Someone` → 401 WITHOUT a cookie (registered + login-gated, NOT 404).
       - `GET /api/v1/wantlist` → 404 (the route was REMOVED — proves the clean break landed).
       - the api is otherwise healthy (an existing route like `/api/v1/characters` → 401 without a cookie).

    **Do NOT push any git tag.** The watcher is untouched (CORRECTION + watcher-release-versioning). Push
    master first if commits are pending, but a `v*` tag would needlessly fire the watcher release CI.
  </action>
  <verify>
    <automated>go test ./... && (cd web && npm run check && npm run build)</automated>
  </verify>
  <done>
    Pre-flight gates green; R2 backup taken BEFORE the restart; the restart log shows goose → version 14
    (wishlist_item in, wantlist_item gone); the web bundle is swapped live; external checks pass
    (`/api/v1/wishlist/{char}` 401-not-404, `/api/v1/wantlist` 404); NO v* tag pushed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Browser-smoke the live /wishlist tab across all 5 EQ themes (WISH-01..07)</name>
  <what-built>
    The live Phase-34 Wishlist tab at https://squirebot.quest/wishlist — the per-character per-slot upgrade
    wishlist (WISH-01..07) over the deployed backend (schema v14): the viewer-first character list (banks/bots
    excluded), the scoped two-section search, the 21-worn-slot accordion (equipped item + auto-hidden-filtered
    targets + the class+slot Velious suggestions), the ping toggle + EC-hit badge, the reused right-click
    examine, and the server-truth add/remove writes. The /wantlist→/wishlist 308 still redirects; the old
    wantlist surface is gone.
  </what-built>
  <how-to-verify>
    Log in at https://squirebot.quest, open the **Wishlist** tab, and confirm the 14-point browser-smoke
    checklist (34-UI-SPEC §Browser-smoke) — exercise EACH across the relevant themes, and spot-check the WHOLE
    tab under all 5 EQ themes (Velious, Vanilla, Kunark, Minimalist, Heavy):

    1. The character list shows ONLY non-bank/bot characters, viewer's-first-then-A-Z ("you" tag on your own;
       NO bank/bot rows); selecting a character pins its per-character detail.
    2. The detail shows the per-character header (`{name} — Level {n} {race} {class}`) + the per-slot accordion
       with ALL 21 worn slots in paperdoll order (Charm/Power Source omitted), each showing its EQUIPPED item
       (or "Empty" for an empty slot).
    3. Add a target (WISH-03): open a slot, type an item → pick a catalog match → "Add" → it appears in that
       slot (server-truth re-fetch, not optimistic); the custom escape hatch adds an off-catalog target with
       the "won't trigger alerts" chip.
    4. Suggestion add (WISH-04): the slot's suggestions show the COMPLETE Pre-Raid/Group + Raiding lists for the
       character's class+slot, each with PigParse price + Wiki link + last-listed; a no-drop/raid-only
       suggestion shows the "Raid" tag + "Not for sale" (no price); clicking "Add" adds it (re-fetch).
    5. Remove a target (WISH-03, destructive): the Remove button opens "Remove this target?" ConfirmDialog;
       confirming removes it (re-fetch); cancelling leaves it.
    6. Ping toggle (WISH-05): toggling a target's Toggle flips ON/OFF (server-truth re-fetch — the switch
       reflects the persisted state); the ON/OFF word is visible (state never color-only).
    7. EC-hit badge (WISH-05): a target SquireBot has pinged you about shows the "Seen in EC" badge beside it.
       (Coverage-dependent — if no live EC hit exists for one of your targets during the window, this is
       organic; the alert_log→badge path is unit-tested. Note it as not-exercised if so.)
    8. Auto-hide when held (D-02): a target the character currently HAS (it's in their inventory) is HIDDEN
       from the slot's wishlist (not struck-through); removing it from the character makes the target re-appear
       (self-healing compute-on-read) — confirm with a known item.
    9. Examine (WISH-06): hovering/tapping a target item name (and the equipped-item line) shows the reused
       ExaminePanel with the D-08-ordered, D-09-omitted fields (no "null"), the single escaped wiki link.
    10. Search covers ALL wishlists (WISH-07): typing a query toggles the left column to two result groups —
        CHARACTERS + WISHLIST ITEMS. The WISHLIST ITEMS group must surface an item that is on a DIFFERENT
        character's wishlist than the currently-selected one (pick/know an item another guildie has wishlisted
        on a character you have NOT selected, search it, and confirm it appears with that other character's name
        + slot) — the corpus is EVERY non-bank/bot character's wishlist, not just the loaded view. Clicking
        either group's row pins that character's wishlist; the hint reads "Search characters and wishlists.";
        clearing returns to the list; no matches shows `No matches for "{query}"` (escaped).
    11. Owner-scoping: your own characters' wishlists are editable (add/remove/toggle); a guild-mate's wishlist
        (reached via search) renders read-only (no add/remove/toggle controls).
    12. Empty/clean-break (D-01): a freshly-selected character with no targets shows "No targets yet" with the
        accordion + add controls below; a fetch failure shows the error StateBlock + Retry, and Retry
        refetches; no character selected shows "Pick a character".
    13. All 5 themes render correctly — Heavy (#f0d088 on #1a0e05) + Minimalist (#b8915c) contrast spot-check on
        the accent focus ring, the selected-row border, the eyebrows, the wiki link, the EC badge, the Toggle
        ON track, and the --destructive Remove.
    14. Mobile (<640px): the list + detail stack; the target rows reflow keeping name/price/wiki/last-listed/
        Raid/toggle/remove all visible + every control at min-height 44px; the accordion sections remain operable.

    Also confirm: navigating to `/wantlist` 308-redirects to `/wishlist`.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all (exercised) points pass, OR describe the issues (which point, which theme,
    expected vs. actual) so they can be fixed-forward before close.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → live prod (binary + DB migration) | The deploy runs goose 00014 on the LIVE SQLite — a DROP migration (clean break). The R2 backup is the rollback floor; the restart-log goose-version check is the proof the migration landed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-17 | Denial of Service / Data loss | the live 00014 DROP migration | mitigate | An R2 backup is taken + CONFIRMED BEFORE the restart (the DROP migration's rollback floor); the migration is ordered (alert_log rebuilt before wantlist_item dropped) + idempotent (34-01); the restart log is checked for `goose → version: 14`. Rollback = R2 restore (a binary-only rollback leaves the dropped table, so the DB snapshot is the true revert). |
| T-34-18 | Tampering | needless watcher CI trigger | mitigate | NO v* tag is pushed — the watcher is untouched; the deploy is a plain binary + web swap + goose run (watcher-release-versioning). |
| T-34-19 | (verification) | DOM-blind node tests | mitigate | The rendered tab is human-browser-smoked on the DEPLOYED build across all 5 themes (the 14-point checklist, incl. smoke #10's cross-character WISH-07 search) — node vitest is DOM-blind; localhost can't auth against prod, so deploy-then-smoke is the only valid render verification (the P15/P31 precedent). |
</threat_model>

<verification>
- Pre-flight: `go test ./...` green; `cd web && npm run check && npm test && npm run build` green.
- Live: restart log `goose → version: 14`; on-box schema shows wishlist_item present + wantlist_item gone;
  `/api/v1/wishlist/{char}` 401-not-404; `/api/v1/wantlist` 404; apex 200 + fresh JS bundle.
- R2 backup object confirmed BEFORE the restart.
- Human browser-smoke checkpoint: APPROVED across the 5 themes (the 14-point checklist, incl. smoke #10's
  cross-character search proof).
- No v* tag pushed.
</verification>

<success_criteria>
- Backend (schema v14 via goose 00014) + web /wishlist tab deployed live to squirebot.quest.
- R2 backup taken before the DROP migration; restart log confirms goose → version 14.
- Live route surface verified: /api/v1/wishlist/{char} 401-not-404, /api/v1/wantlist 404, /wantlist 308→/wishlist.
- Human browser-smoke APPROVED across all 5 EQ themes — WISH-01..07 closed (incl. the cross-character WISH-07 search).
- NO v* tag pushed (watcher untouched).
</success_criteria>

<output>
After completion, create `.planning/phases/34-wishlist-rework-per-character-per-slot-upgrades/34-04-SUMMARY.md`
</output>
