# Milestone v1.0: Watcher + Workbook + Onboarding (initial release)

**Status:** ✅ SHIPPED 2026-05-11
**Phases:** 1–5
**Total Plans:** 31
**Tag:** `v1.0.0` (semantic version; `phase5-complete` also tagged)
**Audit:** [`v1.0-MILESTONE-AUDIT.md`](v1.0-MILESTONE-AUDIT.md) — status `tech_debt` (documentation-only; no quality blockers)

## Overview

First stable release of SquireBot — a 12-person Project 1999 guild's inventory-sync tooling. Two halves: a Go watcher that runs on each guildie's Windows PC, and an Apps Script TypeScript bundle deployed to a shared Google Sheet. Every guildie installs the watcher; one guildie (the workbook owner) deploys the Apps Script via clasp. Result: every guildie can answer "what does my character still need, and where in the guild is it?" without leaving the spreadsheet.

**Timeline:** 11 days kickoff to ship (2026-04-30 → 2026-05-11). 203 commits. 14k LOC Go (watcher), 11k LOC TypeScript (apps-script). Five phases shipped sequentially; no rework of prior phases required.

## Phases

### Phase 1: End-to-End Thin Slice

**Goal:** A single guildie installs SquireBot, completes OAuth once, points at their EQ folder, runs `/outputfile inventory`, and within seconds sees raw TSV rows appear in `inv:<Char>` of the shared workbook.
**Depends on:** Nothing (first phase)
**Plans:** 8
**Shipped:** v0.1.0 — 2026-05-02

Plans:
- [x] 01-01: Repo skeleton (1abb22a, 4900420, ddb594e)
- [x] 01-02: OAuth Cloud setup (fd4ff3c, 1a98df4 hotfix)
- [x] 01-03: OAuth loopback PKCE (7de3704, 429ec6c, e570943)
- [x] 01-04: EQ watcher + parser (c469d7f, 2824bda, ca1071a)
- [x] 01-05: Sheets writer (7106941, ef2329b, 58493f9)
- [x] 01-06: Drive Picker (0c132f0, d3ccecc, ed410a0)
- [x] 01-07: Tray + wizard + RunApp (7172153, c60b2ec, e04dd06)
- [x] 01-08: NSIS installer + release CI (63c759f, 90508a4)

**Details:**
Validated three of five existential pitfalls before work compounded: (a) Google's `/token` requires `client_secret` for Desktop OAuth clients even with PKCE — contrary to OAuth 2.0 spec but per Google's own docs (locked decision); (b) refresh-token survival via Production consent screen (day-10 routine confirms persistence); (c) `drive.file` scope is non-sensitive — no Google verification audit needed. Azure VM smoke 16/17 PASS; 6 hotfixes landed during smoke (client_secret, ctx-canceled TokenSource, picker views fix, log-folder menu, module rename, oauth-setup docs correction).

### Phase 2: Watcher Robustness + Schema Lock

**Goal:** One guildie can install the watcher and not touch it for six months. Sheet schema frozen at `schema_version=1` with all soft-delete + provenance fields scaffolded — even those not yet exposed by UI — so no later phase forces a destructive migration.
**Depends on:** Phase 1
**Plans:** 10
**Shipped:** v0.2.0 (2026-05-09) + v0.2.1 wizard-handoff hotfix

Plans:
- [x] 02-01: Schema scaffold + canonical-id three-state + Slot→Level rename (SCHEMA-01..08)
- [x] 02-02: Spellbook parser + multi-folder watcher + WATCH-09 catch-up
- [x] 02-03: Sheets retry/backoff + sheet.Client mutex (WATCH-07; closes Pitfall D)
- [x] 02-04: Refresh-token UX — invalid_grant detect + tray red + Reauthorize (AUTH-05)
- [x] 02-05: Heartbeat goroutine — 24h self-reschedule + _status writes (WATCH-08, OPS-05)
- [x] 02-06: Auto-update — startup-swap + latest.json + tray Check for updates (OPS-04)
- [x] 02-07: Autostart HKCU Run key + uninstaller checkbox (INST-04)
- [x] 02-08: goreleaser migration preserving AUTH-03 PRODUCTION gate
- [x] 02-09: SmartScreen walkthrough + SignPath OSS application (INST-05; SignPath approval still pending)
- [x] 02-10: 7-day soak runbook + injection scripts + assertions

**Details:**
7-day soak Days 1/4/6/7 PASS. Verdict: `docs/phase2-complete-verdict.md`. v0.2.1 hotfix fixed wizard handoff bug (BUG-001) where signalDone fired in done.html browser JS instead of immediately after `cfg.Save()` — two regression tests added. Discovered that EV code-signing certificates no longer grant instant SmartScreen reputation (Microsoft removed the perk March 2024) — STACK.md / PITFALLS.md #2 corrected; default became unsigned + walkthrough doc + SignPath OSS application in parallel.

### Phase 3: Apps Script Enrichment Foundation

**Goal:** A watcher upload becomes a tooltipped, priced, wiki-linked row in the consolidated `view` tab within ~30 seconds. PigParse pricing refreshes daily; per-item wiki summaries refresh weekly. The `bank` tab shows the bank toon's inventory with cell-note tooltips.
**Depends on:** Phase 2 (frozen schema; landing tabs producing data)
**Plans:** 4
**Shipped:** v0.3.0 — 2026-05-10

Plans:
- [x] 03-01..04 (per `03-0{1..4}-PLAN.md`)

**Details:**
TypeScript/clasp scaffolding + esbuild bundle pipeline established. Daily PigParse trigger + weekly wiki summary triggers. `politeFetch` helper with identifying User-Agent, ETag/If-Modified-Since, CacheService, exponential backoff, 1s sleep between wiki requests. Consolidated `view` tab with conditional formatting (green ≤7d / orange ≤30d / red >30d Last Synced). `LockService.getDocumentLock().tryLock(30000)` guards aggregate writes. Verification artifact: `03-SMOKE-TEST.md` runbook. ENRICH-09 courtesy emails WAIVED by user 2026-05-09.

### Phase 4: Differentiator Features

**Goal:** Every guildie sees a per-slot "shopping list" of missing Velious gear and a level-aware list of trainable spells they don't yet know. **The reason SquireBot exists.** Shared bank toon's coin balance is editable through a guarded sidebar form.
**Depends on:** Phase 3
**Plans:** 4
**Shipped:** v0.4.0 — 2026-05-11

Plans:
- [x] 04-01..04 (per `04-0{1..4}-PLAN.md`)

**Details:**
Live smoke 6 steps PASS: installer + tray green, version=0.4.0-rc1 in heartbeat log, ErrSchemaTooNew startup gate fires, migrateToV3 + Install Triggers (7 triggers + bank-coin protect), Range.protect warning prompt fires, gear_check populates with OK/MISSING/OTHER status, spell_check populates with 359 rows across 11 caster classes (1,562 spells in _wiki_spells). 3 fix-pack patches landed during smoke (no rc2 needed): bank-coin protection setWarningOnly fix (3c5ea6d), missing on-demand menu items (b9482a6), wiki-spell parser handles all 4 P1999 template variants (9319c6b — SpellRow / RadSpellRow / RadSpellRow2 / SongRow; pre-fix had 5 classes, post-fix all 11). Schema bumped to v3 (added `race` column to `_char_owner` for Iksar tier).

### Phase 5: Search + Onboarding + Privacy Polish

**Goal:** Every guildie running SquireBot can answer "who has Lustrous Russet Coat?" in a sidebar in <2 seconds. Eviction workflow for a guildie leaving the guild is documented and tested. SmartScreen onboarding is non-scary.
**Depends on:** Phase 4
**Plans:** 5
**Shipped:** v1.0.0 — 2026-05-11

Plans:
- [x] 05-01: System tab hide + bank_toon_name protect + weekly schema healthcheck (c85586b, ae57d61, a9562b6)
- [x] 05-02: Archive lib + weeklyStaleCharArchive + weeklyEvictionArchive cron (32f8cfa, 434adf6, ab10732, 4c3b339, 2530de3)
- [x] 05-03: Cross-character search sidebar (bd346c4, 1733264, bdd6774)
- [x] 05-04: Eviction sidebar + DOC-02 runbook (f213bdb, 2e39b3e, 658b4a6)
- [x] 05-05: Jekyll Pages site + README shrink + distribution handoff (df8c2fd + 482f19d + 8d1e332 + b668e75)

**Details:**
Cross-character search HtmlService sidebar (300px, debounced fuzzy search across all `inv:<Char>` tabs, Wagner-Fischer Levenshtein for "Did you mean?" fallback). Lock-guarded eviction workflow with 30-day grace + lazy `_archive` tab + JSON envelope contract between sidebar (`commitEviction`) and weekly archiver (`weeklyEvictionArchive`). Jekyll Pages site live at https://boejowen.github.io/SquireBot/ (Cayman theme, pretty permalinks, 4 pages: index / install / troubleshooting / dev). Mid-phase scope reduction 2026-05-11: instructional images + SmartScreen GIF DROPPED per user direction (text-only walkthrough satisfies the spirit of INST-05 + DOC-03). DOC-02 fake-guildie eviction smoke PASS all 7 checkpoints. Distribution probe: N=1 active writer (expected dev-workbook state; guild rollout is v1.0.1).

---

## Milestone Summary

**Decimal Phases:** None — milestone shipped without any inserted hotfix phases.

**Key Decisions (locked during milestone):**

1. **Stack: Go watcher + Apps Script V8 (TypeScript via clasp + esbuild)** — single-binary Windows install (~12 MB), no runtime to ship. PigParse REST API + MediaWiki API mean no HTML scraping in v1.
2. **OAuth scope = `drive.file` only** — non-sensitive; no Google verification audit. Workbook selected via Drive Picker on first run.
3. **OAuth consent screen flips to Production before first guildie installs** — Testing-mode refresh tokens silently expire every 7 days for non-Workspace users.
4. **Consolidated filterable view tabs (`view`, `gear_check`, `spell_check`, `bank`) — never per-character view tabs** — per-character views would breach Google's 200-tab/workbook hard limit at guild scale (12 × 10 × 5 ≈ 600 tabs). LOCKED.
5. **Schema evolution is extend-only** — add columns at right edge, add tabs, add `_meta` rows. Breaking changes require `schema_version` bump + idempotent migration.
6. **Refresh tokens stored only in Windows Credential Manager via DPAPI (`wincred`)** — never in config file or any other plaintext location.
7. **Watcher writes are full-snapshot replaces per character per file** — atomic clear+write via `spreadsheets.batchUpdate`. Never appends, never row-diffs.
8. **All scraping runs in Apps Script, not in 12 watchers in parallel** — polite citizen of community resources; single source of truth.
9. **Google's `/token` endpoint requires `client_secret` for Desktop OAuth clients even with PKCE** — contrary to OAuth 2.0 spec. The desktop secret is effectively public per Google's own docs and is baked into the binary alongside the client ID. (Phase 1 hard-won lesson, locked 2026-05-01.)
10. **EV code-signing certificates no longer grant instant SmartScreen reputation** — Microsoft removed the perk March 2024. EV ≡ OV on UX. Default became unsigned + walkthrough; SignPath OSS application running in parallel.
11. **Phase 5 D-12 Path A — no `WatcherMaxSchemaVersion` bump for Phase 5** — schema_version held at 3 end-to-end across all 5 Phase-5 plans. Apps-script-only changes.

**Issues Resolved:**

- BUG-001 (v0.2.0 wizard handoff): fixed in v0.2.1 via commit `71f7b76` — wizard `signalDone` now fires in `handleEQFolderConfirm` immediately after `cfg.Save()`.
- drive.file write-access propagation delay (~50 min after Reauthorize+Picker): probe goroutine outside batchMu with 90-min timeout is the shipped fix.
- Wiki-spell parser missed 6 of 11 caster classes (template-variant coverage gap): fix-pack `9319c6b` handles all 4 P1999 template variants; live-verified 1,562 spells.

**Issues Deferred (carried to v1.0.1 / v2):**

- Bank-coin permission lock (only bank-toon-owner can use Set Bank Coin sidebar) — Phase 4 backlog
- Polished theme picker tile UI (6-tile grid per `docs/design/mockups/eq-aesthetic-picker.html`) — Phase 4 backlog
- Sidebar HTML inline-JS unit tests (requires JSDOM setup not currently in vitest) — Phase 4/5 backlog
- Installer-driven upgrade UX (NSIS can't overwrite running .exe; workaround documented in troubleshooting.md) — v1.0.1 candidate
- Self-service eviction (departing guildie quits cleanly without officer action) — v2 candidate
- `_meta.guild_admins` allowlist (eviction sidebar enforces officer-only by code, not by trust) — 05-04 v1.0.x polish
- Extract `SIDEBAR_BODY` constants to `apps-script/src/sidebars/*.html` — uniform deferral across 05-03 + 05-04
- PropertiesService migration for recent-query persistence — 05-03 v1.0.1 candidate
- SignPath Foundation OSS approval — submitted; awaiting review
- v2: Wantlist + Discord pinger (WANT-01..08; prerequisites WANT-06/07 still open)

**Technical Debt Incurred:**

- Phases 3 + 4 missing SUMMARY.md files (8 plans without per-plan summaries; verification lives in STATE.md commentary + 03-SMOKE-TEST.md + Phase 4 live-smoke commentary)
- REQUIREMENTS.md traceability table never kept in sync during execution (62 of 69 rows said "Pending" pre-archive; reconciled in `v1.0-REQUIREMENTS.md` archive)
- No VERIFICATION.md anywhere — project doctrine is live smoke tests instead of `/gsd-verify-work`. Worth an explicit conversation at v1.1 kickoff.
- "56 v1 requirements" summary line in REQUIREMENTS.md was wrong; literal count is 69 (reconciled in archive).

---

*For current project status, see `.planning/ROADMAP.md`.*
