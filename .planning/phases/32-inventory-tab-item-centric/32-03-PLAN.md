---
phase: 32-inventory-tab-item-centric
plan: 03
type: execute
wave: 3
depends_on: [32-01, 32-02]
files_modified: []
autonomous: false
requirements: [ITEM-01, ITEM-02, ITEM-03]

must_haves:
  truths:
    - "The new GET /api/v1/items route is live (the server binary was rebuilt + restarted to register it)"
    - "The new web bundle is live (atomic swap) and the /inventory tab renders the item-centric master-detail"
    - "A pre-deploy R2 backup of the live DB exists (even though no migration runs)"
    - "Browser-smoke confirms the load-bearing interactions on the live build: list, viewer-first search, qty·holders + price + wiki, select→pin→replace, holders table, the /characters?c= deep-link, icon fallback, all 5 themes"
  artifacts:
    - path: ".planning/phases/32-inventory-tab-item-centric/32-03-SUMMARY.md"
      provides: "the deploy + browser-smoke verdict record"
      contains: "browser-smoke"
  key_links:
    - from: "api.squirebot.quest"
      to: "GET /api/v1/items"
      via: "the restarted server binary registers the route (401 without a cookie, not 404)"
      pattern: "/api/v1/items"
    - from: "squirebot.quest/inventory"
      to: "fetchItems → /api/v1/items"
      via: "the deployed web bundle renders the master-detail tab"
      pattern: "inventory"
---

<objective>
Ship the item-centric Inventory tab to production and human-verify it on the LIVE build. This phase adds NO
migration (icon_id/statsblock shipped in P31's 00012/00013, schema v13), so the deploy is a backend BINARY swap
(the server MUST restart to register the new `GET /api/v1/items` route) + a web ATOMIC swap — NOT a goose run.
Take the R2 backup per the established path anyway. Then run the browser-smoke checkpoint: node vitest is
DOM-blind, so the rendered tab + the cross-tab holder deep-link are only verifiable in a real browser on prod.

Purpose: close ITEM-01/02/03 — the list, the viewer-first search, and the master-detail holders drill-down with
the live `/characters?c=` jump are proven against real guild data.
Output: a deployed live tab + the 32-03-SUMMARY.md verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-inventory-tab-item-centric/32-RESEARCH.md
@.planning/phases/32-inventory-tab-item-centric/32-UI-SPEC.md
@.planning/phases/32-inventory-tab-item-centric/32-01-SUMMARY.md
@.planning/phases/32-inventory-tab-item-centric/32-02-SUMMARY.md
@docs/backend-deploy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: full regression gate + build the deploy artifacts</name>
  <read_first>
    - docs/backend-deploy.md (the canonical deploy path — R2 backup → cross-compile linux/amd64 CGO_ENABLED=0 → scp → .bak → install 0755 → systemctl restart → web tarball + atomic swap; the §7.5 web-only swap notes; the rollback steps)
    - .planning/phases/32-inventory-tab-item-centric/32-01-SUMMARY.md + 32-02-SUMMARY.md (confirm both halves landed: the route + the web tab; note any deviations to smoke)
    - MEMORY: v2-backend-live-and-ops-access (SSH via ssh-agent + id_ed25519; remote cmds piped as a here-string to `ssh bash -s`; the on-box layout + R2 backup), web-local-dev-cant-auth-against-prod (why the smoke must be on a deployed build)
  </read_first>
  <action>
    Run the full regression gate BEFORE building any artifact (fail closed if any is red):
    - `go test ./...` (all packages ok, incl. the new compute + readapi tests)
    - `go vet ./internal/backendsrv/{compute,store,readapi}/...` clean
    - `npm --prefix web run check` (0 errors / 0 warnings)
    - `npm --prefix web test` (all green, incl. items.test.ts)
    - `npm --prefix web run build` (adapter-static ok)

    Then build the deploy artifacts per docs/backend-deploy.md:
    - Cross-compile the server binary for linux/amd64 with CGO_ENABLED=0 (the registration of the new route is the
      reason the binary must change + the service must restart — a web-only swap is NOT sufficient this phase).
    - Produce the web bundle tarball from the adapter-static build output.

    Do NOT deploy yet — leave the artifacts staged. The actual on-box swap + restart + R2 backup happens in Task 2
    so the human checkpoint immediately follows a known-good push. If any toolchain is missing (Go/Node), STOP and
    wait for the user (the user installs toolchains themselves — MEMORY feedback_toolchain_installs).
  </action>
  <verify>
    <automated>go test ./... ; npm --prefix web run check ; npm --prefix web test ; npm --prefix web run build</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./...` exits 0
    - `npm --prefix web run check` exits 0 ; `npm --prefix web test` exits 0 ; `npm --prefix web run build` exits 0
    - A linux/amd64 CGO_ENABLED=0 server binary is built and the web bundle tarball is assembled (both present locally)
  </acceptance_criteria>
  <done>All regression gates green; the server binary (with the new route) + the web bundle are built and staged for deploy.</done>
</task>

<task type="auto">
  <name>Task 2: deploy to prod (R2 backup → binary swap + restart → web atomic swap; NO goose run)</name>
  <read_first>
    - docs/backend-deploy.md (the R2 backup command, the binary install + `systemctl restart`, the web tarball + `/var/www/squirebot.new` → atomic `mv` swap keeping `squirebot.old`, the rollback recipe)
    - MEMORY: v2-backend-live-and-ops-access (the exact SSH + here-string-to-`ssh bash -s` pattern; backend at api.squirebot.quest / Hetzner 5.78.232.85; the on-box paths)
  </read_first>
  <action>
    Deploy per docs/backend-deploy.md, in this order:
    1. Take the pre-deploy R2 backup of the live DB (e.g. `squirebot-2026-06-18.db.gz`) — even though NO migration
       runs this phase; it's the standard safety net.
    2. scp the new server binary; `cp` the running binary to `.bak`; install the new one 0755; `systemctl restart`
       the service. Confirm the startup logs are clean (bot connected, listening, scheduler started).
    3. **NO goose run** — there is no new migration. Confirm goose version is unchanged (still schema v13) — do NOT
       expect or apply a migration.
    4. Deploy the web bundle: extract to `/var/www/squirebot.new`, chmod, atomic `mv`-swap (keep `squirebot.old`
       for rollback).
    5. External verify (curl, no browser): apex 200; the entry JS content-type is `text/javascript` (the fresh
       bundle, NOT the blank-screen text/html bug); **`GET /api/v1/items` returns 401 (REGISTERED + login-gated,
       NOT 404)** — this is the proof the new route is live; an existing route (e.g. /api/v1/characters) still 401s
       (backend healthy).

    Record the rollback recipe in the summary: `cp /usr/local/bin/squirebot-server.bak → restart` (the old binary
    runs against schema v13 — extend-only safe, and it simply lacks the new route) + `mv` the web `squirebot.old`
    back. If SSH/ops access fails, STOP and surface it (the user holds the ssh-agent passphrase).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://api.squirebot.quest/api/v1/items</automated>
  </verify>
  <acceptance_criteria>
    - A dated R2 backup of the live DB exists (pre-deploy)
    - `curl https://api.squirebot.quest/api/v1/items` returns HTTP 401 (registered + login-gated, NOT 404)
    - `curl https://squirebot.quest/` returns 200 and the entry JS is served as `text/javascript` (fresh bundle)
    - goose version on-box is unchanged (schema v13 — no migration applied)
  </acceptance_criteria>
  <done>The new route is live (401, not 404) and the new web bundle is swapped in atomically; R2 backup taken; no migration applied; rollback recipe recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: browser-smoke the live Inventory tab (DOM-blind node tests cannot verify this)</name>
  <what-built>
    The item-centric Inventory tab is deployed live at https://squirebot.quest/inventory over the new
    `GET /api/v1/items` route. node vitest is DOM-blind (the P15/P31 trap), so the rendered list, the viewer-first
    ordering, the selection→pin→replace, the holders table, the wiki-icon load + colored-tile fallback, and ESPECIALLY
    the holder deep-link into `/characters?c=` are only verifiable in a real browser on the deployed build (localhost
    cannot auth against prod — cookie Domain=squirebot.quest + apex-only CORS).
  </what-built>
  <how-to-verify>
    Log in at https://squirebot.quest, open the **Inventory** tab, and confirm each (32-UI-SPEC §Build/Verification):
    1. The list renders ONE row per item with the `{summed_qty} · {N} holders` headline (correct numbers), an
       inline PigParse price (a known commodity like Blue Diamond shows a price; an unpriced item OMITS the price
       slot cleanly — no "—"/"null"), and a working "Wiki ↗" link to the P1999 wiki.
    2. At rest, YOUR characters' items are at the top, then A-Z. Typing in the search keeps your matches first;
       a no-match query shows the no-results StateBlock with the escaped query (try a name with `<` or quotes —
       it renders inert, not as HTML).
    3. Selecting an item pins the detail (examine block in the D-08 order, missing fields omitted — no "null");
       selecting a DIFFERENT item REPLACES it (single pinned panel, not a multi-pin board).
    4. The holders table lists Character · Where (slot) · Qty · Last synced, with YOUR characters first.
    5. **Click a holder row → it navigates to /characters?c=<name> and THAT character's inventory window opens**
       (the load-bearing cross-tab jump — verify the window is open on the right character).
    6. A known iconId loads the wiki PNG; a bogus/empty iconId falls back to the colored tile (no broken-image glyph).
    7. Switch through ALL 5 themes (Velious · Vanilla · Kunark · Minimalist · Heavy) — the list + detail render
       correctly; spot-check the accent selected-border + focus ring + qty/holder numbers + wiki link read on
       Heavy (parchment) and Minimalist (low-chroma accent).
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 pass, or describe the issues (each becomes a fix-forward smoke commit, mirroring the P31 close).</resume-signal>
</task>

</tasks>

<verification>
- Regression green before deploy: `go test ./...`, `npm --prefix web run check` 0/0, `npm --prefix web test`, `npm --prefix web run build`.
- Live route proof: `GET /api/v1/items` → 401 (registered, not 404); apex 200 + fresh JS content-type.
- No goose run; schema stays v13; R2 backup taken.
- Human browser-smoke PASS on the 7-point checklist across all 5 themes (the DOM-blind gap — the only place ITEM-01/02/03's rendered behavior is truly verified).
</verification>

<success_criteria>
- The Inventory tab is live and the new route is registered (401, not 404).
- The 7-point browser-smoke passes on the deployed build across all 5 themes — closing ITEM-01 (list w/ qty + price + wiki), ITEM-02 (viewer-first search), and ITEM-03 (master-detail holders + the /characters?c= deep-link).
- Rollback recipe recorded; any smoke issues captured as fix-forward commits (the P31 pattern).
</success_criteria>

<output>
After completion, create `.planning/phases/32-inventory-tab-item-centric/32-03-SUMMARY.md`
</output>
