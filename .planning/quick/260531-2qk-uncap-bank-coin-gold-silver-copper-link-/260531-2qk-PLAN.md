---
quick_id: 260531-2qk
title: Uncap bank-coin gold/silver/copper + link wordmark to home
type: quick
created: 2026-05-31
files_modified:
  - web/src/lib/coin.ts
  - web/src/lib/__tests__/coin.test.ts
  - internal/backendsrv/webadmin/coin.go
  - internal/backendsrv/webadmin/coin_test.go
  - web/src/lib/components/SiteShell.svelte
---

<objective>
Two small post-Phase-15 edits to the (now live) web + admin surface. LOCAL build + test only — the VPS rebuild/redeploy is a separate step the orchestrator runs afterward.

1. **Uncap bank-coin gold/silver/copper.** Today g/s/c are validated to the range 0–999 (the D-11 discretion call: "0–999 vs free integers"). The guild wants larger raw-coin amounts, so switch g/s/c to **non-negative integers with NO upper bound**, exactly matching how **platinum** is already validated (plat is ≥0, no cap). This must change BOTH layers — the frontend `coin.ts` AND the authoritative server-side check in `webadmin/coin.go` (fixing only the frontend would still 400 on the backend).
2. **Link the header wordmark home.** The "SquireBot" wordmark in `SiteShell.svelte` is a plain `<span>` — make it an `<a href="/">` so there's a way back to the main page.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Uncap gold/silver/copper coin values (frontend + backend + tests)</name>
  <files>
    - web/src/lib/coin.ts
    - internal/backendsrv/webadmin/coin.go
    - web/src/lib/__tests__/coin.test.ts
    - internal/backendsrv/webadmin/coin_test.go
  </files>
  <action>
  Read all four files first to see the exact current validation + copy.
  - **web/src/lib/coin.ts**: locate the per-denomination range validation (plat ≥0; gold/silver/copper currently 0–999) and the UI-SPEC error-copy strings that mention "0–999" / "0-999". Remove the ≤999 UPPER bound for gold/silver/copper so they validate identically to platinum: a non-negative integer (≥0), no upper cap. Keep the ≥0 guard and the integer/whole-number guard. Update the now-stale copy (e.g. a "0–999" hint becomes the same wording plat uses — a whole number ≥ 0). Do NOT change platinum.
  - **internal/backendsrv/webadmin/coin.go**: locate the server-side range check that rejects gold/silver/copper > 999 (the authoritative gate) and remove that upper bound, keeping the ≥0 / parse / integer guards. After this, a value like 5000 g/s/c is accepted server-side.
  - **web/src/lib/__tests__/coin.test.ts**: update the range assertions — a gold/silver/copper value of 5000 must now be VALID/accepted; negative and non-integer values must still be rejected; remove or invert any "max 999 rejected" assertion. Keep plat cases as-is.
  - **internal/backendsrv/webadmin/coin_test.go**: same — ensure a >999 g/s/c value (e.g. 5000) is now accepted by the handler, and a negative is still rejected. Update/replace the old 999-cap case.
  </action>
  <verify>
    <automated>cd web && npm run check && npm run test:unit -- --run</automated>
    <automated>go test ./... && go build ./... && GOOS=linux GOARCH=amd64 go build ./cmd/squirebot-server</automated>
  </verify>
  <done>gold/silver/copper accept arbitrary non-negative integers (e.g. 5000) on BOTH the frontend validator and the backend handler; platinum unchanged; negatives + non-integers still rejected; web check 0/0 + all web tests pass + whole Go suite + cross-compile green.</done>
</task>

<task type="auto">
  <name>Task 2: Make the header wordmark a home link</name>
  <files>
    - web/src/lib/components/SiteShell.svelte
  </files>
  <action>
  In SiteShell.svelte the header has `<span class="wordmark">SquireBot</span>`. Change it to a real anchor to the site root: `<a href="/" class="wordmark">SquireBot</a>`. Keep the visual identical — apply the existing `.wordmark` styling to the anchor (add `text-decoration: none; color: var(--accent);` if the anchor would otherwise pick up default link styling, and a `:focus-visible` outline for keyboard accessibility, consistent with the other header controls). Update the `.wordmark` CSS selector if it was element-specific (`span.wordmark`) so it targets the anchor. It must remain keyboard-focusable and look the same as now.
  </action>
  <verify>
    <automated>cd web && npm run check && npm run test:unit -- --run && npm run build</automated>
  </verify>
  <done>The "SquireBot" wordmark renders visually identical but is a clickable, keyboard-focusable link to "/"; svelte-check 0/0; web tests pass; production build emits build/index.html.</done>
</task>

</tasks>

<commit_guidance>
Two atomic commits (commit_docs is false — commit CODE only; the orchestrator commits the PLAN/SUMMARY/STATE docs afterward):
- Task 1 → `fix(260531-2qk): uncap bank-coin gold/silver/copper (frontend + backend)`
- Task 2 → `feat(260531-2qk): link header wordmark to home`
</commit_guidance>

<output>
After completion create `.planning/quick/260531-2qk-uncap-bank-coin-gold-silver-copper-link-/260531-2qk-SUMMARY.md`.
</output>
