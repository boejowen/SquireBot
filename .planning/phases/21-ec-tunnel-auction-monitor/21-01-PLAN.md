---
phase: 21-ec-tunnel-auction-monitor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-ec-tunnel-auction-monitor/21-SPIKE.md
  - internal/backendsrv/enrich/pigdetails.go
  - internal/backendsrv/enrich/pigdetails_test.go
  - internal/backendsrv/enrich/__fixtures__/pigparse-getdetails-fungi.json
  - internal/backendsrv/migrations/00008_ec_cursor.sql
  - internal/backendsrv/migrations/migrate_test.go
  - internal/backendsrv/store/eccursor.go
  - internal/backendsrv/store/eccursor_test.go
autonomous: true
requirements: [WANT-05]

must_haves:
  truths:
    - "The feasibility spike runs against live PigParse, records whether auction timestamps advance + coverage, applies the D-08 threshold rule, and documents the chosen path (getdetails vs lastWTSSeen) in 21-SPIKE.md without a user checkpoint (ROADMAP criterion 1)"
    - "A new getdetails parser turns a PigParse ItemDetail JSON body into []ItemAuctionDetail with u=direction / t=timestamp (the OPPOSITE of getall) and tolerates malformed/nullable records without panicking"
    - "The ec_auction_cursor table exists (migration 00008, idempotent, extend-only) and stores per-item last-seen auction timestamps"
    - "The store layer exposes a first-sight-aware cursor get (absent => ok=false), an advance-only-on-success cursor set, and the DISTINCT active-catalog poll-set query (D-01 reason NOT filtered, D-03 NULL item_id skipped)"
  artifacts:
    - path: "internal/backendsrv/enrich/pigdetails.go"
      provides: "ParseItemDetail([]byte) (ItemDetail, error) + ItemAuctionDetail{U,I,P,T} / ItemDetail{Items,ItemName,Players} types"
      contains: "func ParseItemDetail"
    - path: "internal/backendsrv/migrations/00008_ec_cursor.sql"
      provides: "ec_auction_cursor table (item_id PK, last_seen_t TEXT, updated_at INTEGER)"
      contains: "CREATE TABLE ec_auction_cursor"
    - path: "internal/backendsrv/store/eccursor.go"
      provides: "GetECCursor / SetECCursor / ECPollSet"
      contains: "func (s *Store) GetECCursor"
    - path: ".planning/phases/21-ec-tunnel-auction-monitor/21-SPIKE.md"
      provides: "spike findings: items probed, t advancement Y/N, coverage numbers, path chosen, D-07 coverage caveat"
      min_lines: 20
  key_links:
    - from: "store.ECPollSet"
      to: "wantlist_item"
      via: "SELECT DISTINCT item_id, item_name WHERE active=1 AND item_id IS NOT NULL"
      pattern: "DISTINCT item_id"
    - from: "store.GetECCursor"
      to: "ec_auction_cursor"
      via: "absent row => ok=false (first-sight baseline signal)"
      pattern: "sql.ErrNoRows"
---

<objective>
Lay the EC monitor's data foundation and run the mandatory feasibility spike (the phase's gating first task, ROADMAP criterion 1). This plan delivers everything the producer job (Plan 03) needs from the data side: the spike verdict, the NEW getdetails parser, the cursor migration, and the cursor/poll-set store layer.

Purpose: De-risk the phase up front (spike) and build the genuinely-new pure logic (parser, cursor) before the orchestration wiring lands. Per D-07/D-08 the spike NEVER defers the phase — it applies a stated threshold and proceeds.

Output: `21-SPIKE.md`, `enrich/pigdetails.go` (+ test + fixture), `migrations/00008_ec_cursor.sql` (+ migrate_test extension), `store/eccursor.go` (+ test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-ec-tunnel-auction-monitor/21-CONTEXT.md
@.planning/phases/21-ec-tunnel-auction-monitor/21-RESEARCH.md
@.planning/phases/21-ec-tunnel-auction-monitor/21-PATTERNS.md

<interfaces>
<!-- VERIFIED contracts the executor will build against / mirror. No codebase exploration needed. -->

PigParse getdetails contract [VERIFIED live Swagger 2026-06-05]:
  GET https://pigparse.azurewebsites.net/api/item/getdetails/{itemid}   (id form — RESEARCH Open Q2 recommends id over name: stable, no name-drift)
  GET https://pigparse.azurewebsites.net/api/item/getdetails/{server}/{itemname}  (name form alternative; {server}=1=Blue)
  returns ItemDetail = { items: ItemAuctionDetail[] (nullable), itemName: string (nullable), players: { <string>: string } (nullable) }
  ItemAuctionDetail = { u: int (0=WTS, 1=WTB, 2=BOTH), i: int (itemid), p: int|null (price pp), t: string (RFC3339 date-time) }
  THERE IS NO global "auctions since T" feed — poll-and-diff per item only.

The `t` COLLISION (RESEARCH Pitfall): in the getall feed (enrich.ParseToRows) `t` is a DIRECTION flag; in getdetails `t` is a TIMESTAMP and `u` is the direction. Do NOT reuse ParseToRows.

Cursor-store analog — store/jobstate.go absent-is-zero pattern:
  case qerr == sql.ErrNoRows:  return "", false, nil   // never seen => first-sight baseline
Upsert grain (jobstate.go:87-100): INSERT ... ON CONFLICT(item_id) DO UPDATE SET ... = excluded.*

Poll-set columns (migrations/00006_wantlist.sql): wantlist_item has item_id (nullable), item_name, reason, active.

SSRF-safe URL idiom (enrich/jobs/urls.go:28): hardcoded host constant; escape the item segment with enrich.EncodeURIComponent (NOT url.QueryEscape — over-escapes apostrophes/parens).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PigParse feasibility spike (GATE — ROADMAP criterion 1, D-08 no checkpoint)</name>
  <read_first>
    - .planning/phases/21-ec-tunnel-auction-monitor/21-RESEARCH.md §"The Mandatory Feasibility Spike" (the run steps + the concrete threshold rule)
    - internal/backendsrv/enrich/jobs/urls.go (the hardcoded-host + EncodeURIComponent URL idiom)
    - internal/backendsrv/enrich/politefetch/politefetch.go:80,140 (Fetcher seam + Fetch; FetchResult{OK,Status,Body})
  </read_first>
  <action>
    Run the spike as a throwaway probe (a `go run` one-off under a temporary path, or curl through the polite contract) — do NOT add a permanent spike binary to the tree. Probe 2-4 known hot tunnel items via `GET https://pigparse.azurewebsites.net/api/item/getdetails/1/{itemname}` for a name like "Flowing Black Silk Sash" / "Fungus Covered Scale Tunic" AND the id form `getdetails/{itemid}` for at least one item, so the spike can pin the name-vs-id key choice (RESEARCH Open Q2 — recommend id form).

    Record per item: count of ItemAuctionDetail returned, min/max/distribution of `t`, how many carry `u ∈ {0,2}` (WTS), how many carry a non-null `p`, and whether the `players` map resolves any seller. If a live tunnel window is reachable, poll the same items ≥2 times ≥10 min apart and record whether `max(t)` ADVANCES.

    Apply the stated D-08 threshold rule and pick the path:
      - Adopt per-auction `getdetails` (default) iff BOTH: (a) ≥1 probed item returns ≥1 ItemAuctionDetail with a parseable `t` AND `u ∈ {0,2}`, AND (b) `max(t)` advances between two polls ≥10 min apart (or, if no live window is available, document that advancement is UNCONFIRMED and proceed on the API-shape evidence per D-07 — ship anyway, never defer).
      - Else fall back to the coarser `lastWTSSeen` new-sighting trigger (note this for Plan 03; cursor on Item.lastWTSSeen instead of ItemAuctionDetail.t).

    Write `.planning/phases/21-ec-tunnel-auction-monitor/21-SPIKE.md` documenting: items probed, record counts, `t` distribution, advancement Y/N/UNCONFIRMED, seller-resolution hit-rate, the name-vs-id key decision, the PATH CHOSEN, and the D-07 coverage caveat ("EC alerts depend on someone parsing the tunnel — best-effort, weekend-bursty"). Capture ONE real getdetails JSON response as the parser fixture (next task) at internal/backendsrv/enrich/__fixtures__/pigparse-getdetails-fungi.json (rename to the real item probed). Do NOT log/store the raw `players` map values into committed docs beyond an aggregate hit-rate (V7 — no PII).

    Per D-08 this is yolo/no-checkpoint: apply the rule and proceed to the next task automatically.
  </action>
  <acceptance_criteria>
    - `.planning/phases/21-ec-tunnel-auction-monitor/21-SPIKE.md` exists and contains the literal string "Path chosen:" (or "PATH CHOSEN") and a coverage caveat line.
    - A captured fixture file exists under internal/backendsrv/enrich/__fixtures__/ matching glob `pigparse-getdetails-*.json` and is valid JSON (`node -e "JSON.parse(require('fs').readFileSync(process.argv[1]))" <fixture>` exits 0, or `go run` equivalent).
    - The spike note records which getdetails key form (id vs name) Plan 03 will use.
  </acceptance_criteria>
  <done>21-SPIKE.md documents the path chosen + coverage + key form; a real getdetails fixture is captured; no permanent spike binary left in the tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NEW getdetails parser (enrich/pigdetails.go) — do NOT reuse ParseToRows</name>
  <read_first>
    - internal/backendsrv/enrich/pigparse.go (the SAME-package purity + malformation-tolerance analog — pigparse.go:1-24 doc/purity, :67-100 RawMessage skip loop, :24 malformationTolerancePct=0.01). DIFFERENT JSON schema — copy the discipline, not the shapes.
    - internal/backendsrv/enrich/__fixtures__/pigparse-getdetails-*.json (the fixture captured in Task 1)
    - .planning/phases/21-ec-tunnel-auction-monitor/21-PATTERNS.md §"enrich/pigdetails.go" (the struct + json-tag block)
  </read_first>
  <behavior>
    - Test: a valid getdetails body parses to ItemDetail with all ItemAuctionDetail records in source order; U/I/P/T mapped correctly (P is a *int — nil when JSON `p` is null).
    - Test: a null/missing `items` array yields an empty (non-nil) slice, no error.
    - Test: a body with ≤1% malformed records skips them and logs a slog.Warn with skipped/total counts; >1% malformed returns an error (mirror malformationTolerancePct).
    - Test: a nullable `p` (JSON null) yields P==nil, NOT 0; a present `p` yields the int.
    - Test: a non-object / truncated body returns an error (does not panic).
    - Test (regression on the `t` collision): a record with `u=0` is WTS and its `t` is preserved as the RFC3339 timestamp string (NOT interpreted as a direction).
  </behavior>
  <action>
    Create `internal/backendsrv/enrich/pigdetails.go` in package `enrich` (pure — NO net/http, NO database/sql imports). Define:

      type ItemAuctionDetail struct {
          U int    `json:"u"` // direction: 0=WTS,1=WTB,2=BOTH (NOT the getall `t`)
          I int    `json:"i"` // EQ item ID
          P *int   `json:"p"` // price pp — NULLABLE; nil => "price unknown"/omit (never 0pp)
          T string `json:"t"` // auction timestamp (RFC3339); the DIFF cursor key
      }
      type ItemDetail struct {
          Items    []ItemAuctionDetail `json:"items"`    // nullable in API → normalize to empty slice
          ItemName string             `json:"itemName"`
          Players  map[string]string  `json:"players"`  // seller best-effort (D-05); key→auction undocumented
      }
      func ParseItemDetail(body []byte) (ItemDetail, error)

    Add a package/func doc comment EXPLICITLY calling out the `t` collision (getall: t=direction; getdetails: t=timestamp, u=direction). Mirror pigparse.go's per-record json.RawMessage validate/skip tolerance loop and the >1% → error / ≤1% → skip+slog.Warn("...", "skipped", n, "total", m) discipline, but guard the top-level json.Unmarshal on the OBJECT (ItemDetail), not a top-level array, then iterate Items. NEVER log raw record content or the players map (V7). Normalize a nil Items to a non-nil empty slice.

    Write `enrich/pigdetails_test.go` covering the behaviors above, using the captured fixture for the happy path and small inline literals for the malformed/null/edge cases.
  </action>
  <verify>
    <automated>go test ./internal/backendsrv/enrich/ -run TestParseItemDetail</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'func ParseItemDetail' internal/backendsrv/enrich/pigdetails.go` present.
    - `go test ./internal/backendsrv/enrich/ -run TestParseItemDetail` exits 0.
    - `go vet ./internal/backendsrv/enrich/...` exits 0.
    - pigdetails.go imports NEITHER "net/http" NOR "database/sql" (`grep -E 'net/http|database/sql' internal/backendsrv/enrich/pigdetails.go` returns nothing).
  </acceptance_criteria>
  <done>ParseItemDetail parses the real fixture, tolerates malformed/null records, preserves the timestamp `t`, and is import-pure; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: migration 00008 + cursor/poll-set store (store/eccursor.go)</name>
  <read_first>
    - internal/backendsrv/migrations/00007_notify.sql (the +goose Up/Down header + "00001-00007 SHIPPED and NOT edited" forward-only note — copy this idiom)
    - internal/backendsrv/migrations/migrate_test.go (the existing apply-idempotent test to EXTEND for 00008)
    - internal/backendsrv/store/jobstate.go:36-61,82-100 (GetJobRun absent-is-zero + SetJobRun upsert ON CONFLICT — the cursor analog)
    - internal/backendsrv/wantmatch/match.go:51-61,82-102 (the owner-agnostic SELECT + scanHits non-nil-slice + rows.Err discipline)
    - .planning/phases/21-ec-tunnel-auction-monitor/21-PATTERNS.md §"migrations/00008_ec_cursor.sql" and §"store/eccursor.go"
  </read_first>
  <behavior>
    - Test (TestMigrate, extend): migrations apply cleanly through 00008; `ec_auction_cursor` table exists; re-applying is idempotent (no error, no duplicate).
    - Test (TestECCursor get-absent): GetECCursor on an unseen item_id returns ("", false, nil) — the first-sight baseline signal.
    - Test (TestECCursor set/get round-trip): SetECCursor then GetECCursor returns the stored last_seen_t with ok=true; a second SetECCursor with a later t overwrites (ON CONFLICT upsert).
    - Test (TestECPollSet): given wantlist_item rows — an active buy want with item_id, an active quest want with item_id, an active custom want (item_id NULL), an inactive want, and a DUPLICATE item_id across two users — ECPollSet returns DISTINCT (item_id,item_name) for ONLY the active item_id-bearing rows (custom NULL skipped per D-03; reason NOT filtered per D-01; duplicate item polls once).
  </behavior>
  <action>
    Create `internal/backendsrv/migrations/00008_ec_cursor.sql`, copying the 00007 header idiom (the "00001-00007 SHIPPED and NOT edited" note becomes "00001-00007 SHIPPED..."):

      -- +goose Up
      CREATE TABLE ec_auction_cursor (
        item_id     INTEGER PRIMARY KEY,   -- stable join key; one row per polled item
        last_seen_t TEXT NOT NULL,         -- max(ItemAuctionDetail.t) seen, RFC3339 string
        updated_at  INTEGER NOT NULL       -- unix epoch secs
      );
      -- +goose Down
      SELECT 1;   -- forward-only no-op (mirrors 00004-00007)

    This is backend-only: NO _meta.schema_version bump, NO WatcherMaxSchemaVersion change (the watcher never reads this table — RESEARCH §Project Constraints).

    Extend `internal/backendsrv/migrations/migrate_test.go` to assert 00008 applies and `ec_auction_cursor` exists (idempotent re-apply).

    Create `internal/backendsrv/store/eccursor.go` (package store):
      func (s *Store) GetECCursor(ctx context.Context, itemID int64) (lastT string, ok bool, err error)
        — sql.ErrNoRows => ("", false, nil); wrap other errors with item id context.
      func (s *Store) SetECCursor(ctx context.Context, itemID int64, lastT string, now int64) error
        — INSERT ... ON CONFLICT(item_id) DO UPDATE SET last_seen_t=excluded.last_seen_t, updated_at=excluded.updated_at.
      type ECPollItem struct { ItemID int64; ItemName string }
      func (s *Store) ECPollSet(ctx context.Context) ([]ECPollItem, error)
        — `SELECT DISTINCT item_id, item_name FROM wantlist_item WHERE active = 1 AND item_id IS NOT NULL` (D-01 reason NOT filtered; D-03 NULL skipped); non-nil slice + rows.Err() discipline.

    Write `internal/backendsrv/store/eccursor_test.go` covering the behaviors above (use the existing store test DB setup helper in the store package).
  </action>
  <verify>
    <automated>go test ./internal/backendsrv/migrations/ -run TestMigrate ./internal/backendsrv/store/ -run "TestECCursor|TestECPollSet"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'CREATE TABLE ec_auction_cursor' internal/backendsrv/migrations/00008_ec_cursor.sql` present.
    - `grep 'func (s \*Store) GetECCursor' internal/backendsrv/store/eccursor.go` present.
    - `grep 'DISTINCT item_id' internal/backendsrv/store/eccursor.go` present.
    - `go test ./internal/backendsrv/migrations/ ./internal/backendsrv/store/` exits 0.
    - migrations 00001-00007 are byte-unchanged (`git diff --name-only internal/backendsrv/migrations/` lists ONLY 00008_ec_cursor.sql and migrate_test.go).
  </acceptance_criteria>
  <done>00008 applies idempotently creating ec_auction_cursor; GetECCursor returns absent-is-false; SetECCursor upserts; ECPollSet returns DISTINCT active catalog wants only; 00001-00007 untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend job → PigParse (third-party HTTP) | untrusted/malformed JSON crosses here into ParseItemDetail |
| PigParse item segment → outbound URL | the item id/name placed in the getdetails path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering (SSRF) | getdetails URL build (probe + later job) | mitigate | Hardcoded host/scheme constant (PigparseURL precedent, urls.go:28); item segment escaped with enrich.EncodeURIComponent — item ids/names originate from the catalog/wantlist, never a request body. |
| T-21-02 | DoS (malformed/oversized response) | ParseItemDetail | mitigate | Per-record skip-on-malformed with a ≤1% tolerance (ParseToRows precedent); top-level Unmarshal guarded — no panic on truncated/non-object bodies. politefetch caps the body at ~16 MB (downstream in Plan 03). |
| T-21-03 | Info disclosure (PII) | spike note + parser logs | mitigate | Never commit raw `players` map values into 21-SPIKE.md beyond an aggregate hit-rate; parser logs counts only, never raw record content (V7). |
| T-21-04 | Tampering (null price → 0pp) | ItemAuctionDetail.P | accept→mitigate | `p` typed *int (nil-distinct from 0); downstream renders "price unknown"/omits, never 0pp. |
</threat_model>

<verification>
- `go test ./internal/backendsrv/enrich/ ./internal/backendsrv/migrations/ ./internal/backendsrv/store/` green.
- `go build ./...` exits 0.
- `go vet ./internal/backendsrv/...` exits 0.
- 21-SPIKE.md documents the path chosen + coverage caveat + key form.
</verification>

<success_criteria>
- The spike verdict (ROADMAP criterion 1) is recorded; the phase proceeds (D-07/D-08 — never deferred).
- ParseItemDetail handles the real getdetails shape with the `t`/`u` collision called out, malformed-tolerant, import-pure.
- ec_auction_cursor exists via idempotent 00008 (extend-only; 00001-00007 untouched).
- GetECCursor (first-sight absent), SetECCursor (upsert), ECPollSet (DISTINCT active catalog wants) all tested green.
</success_criteria>

<output>
After completion, create `.planning/phases/21-ec-tunnel-auction-monitor/21-01-SUMMARY.md`
</output>
