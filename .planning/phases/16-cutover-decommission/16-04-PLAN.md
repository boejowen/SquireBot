---
phase: 16-cutover-decommission
plan: 04
type: execute
wave: 3
depends_on: [16-03]
files_modified:
  - docs/decommission-checklist.md
autonomous: false
requirements: [CUTOVER-01, CUTOVER-04]
must_haves:
  truths:
    - "The maintainer has confirmed onboarded guildies are reporting in on the backend — captured as one SQL count line in the checklist (CUTOVER-01, the folded-in reporting-in confirmation, D-05)"
    - "The Google Apps Script enrichment triggers are disabled — no further scheduled PigParse/wiki runs fire on Google's infra (D-10)"
    - "The Google OAuth 2.0 Client ID is retired/deleted — no live Google machinery remains; deleting it immediately revokes any outstanding tokens (D-11)"
    - "The old Google Sheet is abandoned in place — explicitly NO export, NO delete, NO read-only freeze (D-12)"
    - "A committed docs/decommission-checklist.md is the CUTOVER-04 proof artifact: each retired Google asset ticked off, plus the existing P13 code-level Google-freedom proofs referenced (D-13)"
  artifacts:
    - path: "docs/decommission-checklist.md"
      provides: "the CUTOVER-04 proof artifact — every retired Google asset + the reporting-in confirmation + the code-level proofs, eviction-runbook style"
      contains: "decommission"
      min_lines: 30
  key_links:
    - from: "docs/decommission-checklist.md"
      to: "the disabled Apps Script triggers + retired OAuth client"
      via: "checked-off items documenting each console action with its verification"
      pattern: "OAuth|trigger"
    - from: "docs/decommission-checklist.md"
      to: "the P13 code-level Google-freedom proofs"
      via: "referencing go list -deps Google-free + the 57%-smaller binary (D-13)"
      pattern: "go list -deps|Google-free"
---

<objective>
Decommission the live Google machinery (CUTOVER-04) and fold in the CUTOVER-01 reporting-in confirmation (D-05) — the operational endgame of v2.0 "Off Google". Per the CONTEXT reinterpretation this is deliberately LIGHT: kill the two LIVE Google assets (disable the Apps Script enrichment triggers + retire the Google OAuth client), ABANDON the Sheet in place (no export/delete/freeze), and produce a decommission checklist as the proof artifact.

RESEARCH VERIFIED the Apps Script time-driven triggers almost certainly still fire on Google's infrastructure independent of the (dark) watchers — installTriggers.ts created 7 of them (daily PigParse + 3 weekly wiki + onChange/hourly/watchdogs) — double-loading the volunteer-run community wiki + PigParse APIs in parallel with the backend. Disabling them is both teardown AND politeFetch-ethos hygiene (D-10). Retiring the OAuth client is the asset CUTOVER-04 explicitly names (D-11); deleting it immediately revokes any outstanding tokens. The Sheet is frozen-since-2026-05-15 and worthless to the system — deleting it is busywork (D-12).

CUTOVER-04 is satisfied as "no LIVE Google machinery / no Google dependency remains" (D-13): the code-level Google-freedom was already proven in P13 (go list -deps ./cmd/squirebot Google-free; binary 57% smaller; no Google secret). The PROOF ARTIFACT is docs/decommission-checklist.md documenting each retired asset + those existing proofs. CUTOVER-01's "shadow soak" is void (the Sheet is frozen) — it collapses to one reporting-in SQL count line in the same checklist (D-05, Open Question 3).

Purpose: meet the milestone's "Off Google" goal with proof. Output: the two Google assets retired + a committed checklist.

**`autonomous: false`** — the two retirements are maintainer-run Google console actions (outside the repo), and the reporting-in confirmation is a maintainer judgment. The agent may DRAFT docs/decommission-checklist.md, but the console actions + their verification + the reporting-in count are human steps. Do NOT plan an agent to delete Google assets autonomously.

**Depends on:** Plan 03 — the flip must have happened (guildies reporting in) before decommissioning; D-13 satisfaction follows a confirmed-successful cutover.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-cutover-decommission/16-CONTEXT.md
@.planning/phases/16-cutover-decommission/16-RESEARCH.md

<interfaces>
<!-- The decommission steps, cited from Google docs in RESEARCH. -->

The checklist style/precedent: docs/eviction-runbook.md (a markdown checklist — clone the STYLE only, not content). The deploy/ops docs docs/backend-deploy.md + docs/apps-script-deploy.md are reference for the Google-side specifics.

The 7 Apps Script triggers were created by apps-script/src/triggers/installTriggers.ts (daily PigParse + 3 weekly wiki + onChange + hourly view + watchdogs). They are time-driven on Google's schedulers, using the script owner's authorization — INDEPENDENT of the OAuth-blocked watcher writes (Pitfall 3: "watchers can't write to the Sheet" is NOT the same as "the script's triggers stopped"). Disable path (CITED): the per-user Triggers dashboard at script.google.com/home/triggers OR the project editor's Triggers panel, then delete each trigger.

The Google OAuth client retirement (CITED — support.google.com/cloud/answer/15549257): Google Cloud Console, APIs and Services, Credentials/Clients, select the v1.0.2 desktop OAuth 2.0 Client ID, DELETE. Deleting immediately revokes outstanding tokens; the client is restorable for 30 days then permanently gone. The watcher OAuth CODE is already deleted (P13), so the client has no consumer — deletion is safe.

The reporting-in confirmation (CUTOVER-01 / D-05 / Open Question 3): one SQL count line, run on the VPS against the live SQLite DB —
SELECT COUNT(*) FROM character WHERE last_seen IS NOT NULL;  (and/or WHERE last_seen > the-flip-date-epoch)
— captured as a single line in the checklist (N of ~12 guildies reporting in). NOT a separate soak doc.

The P13 code-level Google-freedom proofs to REFERENCE (already established, D-13): go list -deps ./cmd/squirebot is Google-free; the watcher binary is 57% smaller (16.44 MB to 7.07 MB); a 9-pattern byte scan of the binary finds zero Google OAuth/Sheets/secret strings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft docs/decommission-checklist.md (the CUTOVER-04 proof artifact)</name>
  <files>docs/decommission-checklist.md</files>
  <read_first>
    - docs/eviction-runbook.md (the checklist STYLE/precedent to clone — headings, checkbox rhythm, prose tone)
    - .planning/phases/16-cutover-decommission/16-CONTEXT.md (D-10/D-11/D-12/D-13 — the exact decommission decisions + the abandon-in-place stance)
    - .planning/phases/16-cutover-decommission/16-RESEARCH.md (Decommission rows + the cited Google console click-paths + the P13 code-level proofs to reference)
    - apps-script/src/triggers/installTriggers.ts (the 7 triggers being disabled — name them in the checklist)
  </read_first>
  <action>
Create docs/decommission-checklist.md in the style of docs/eviction-runbook.md (a clean markdown checklist with short prose + checkbox items + per-item verification). This is the CUTOVER-04 proof artifact (D-13). The agent DRAFTS it now (the human ticks the live-action boxes in Tasks 2-4). Sections:

1. Header / context — what this documents (the v2.0 "Off Google" decommission), the reinterpretation note (CUTOVER-04 satisfied as "no LIVE Google machinery / no Google dependency remains", D-13), and the date.

2. Pre-decommission gate (CUTOVER-01 reporting-in, D-05) — a single checkbox + a placeholder line for the SQL count:
   - "[ ] Confirmed guildies reporting in on the backend: __ of ~12 — SELECT COUNT(*) FROM character WHERE last_seen IS NOT NULL; (run on the VPS, fill in the count + date)"
   - One sentence: the 1–2 week shadow soak is void (the Sheet is frozen since 2026-05-15); this folded-in confirmation satisfies CUTOVER-01 (D-05). NO separate soak doc.

3. Live Google asset retirement (CUTOVER-04 / D-10, D-11) — checkboxes with the cited click-paths + verifications:
   - "[ ] Disabled the Apps Script enrichment triggers (D-10)" — note the 7 triggers from installTriggers.ts (daily PigParse + 3 weekly wiki + onChange + hourly view + watchdogs); path script.google.com/home/triggers (or the project editor Triggers panel), delete each; verification: the Apps Script Executions dashboard shows no further scheduled refreshPigparse/refreshWikiItems runs. One line on WHY: they fire on Google's infra independent of the dark watchers, double-loading the volunteer wiki/PigParse (politeFetch hygiene).
   - "[ ] Retired the Google OAuth 2.0 Client ID (D-11)" — path: Cloud Console, APIs and Services, Credentials/Clients, the v1.0.2 desktop client, DELETE; note: deletion immediately revokes outstanding tokens, restorable 30 days then permanent; safe because the watcher OAuth code is already deleted (P13) so the client has no consumer.

4. Abandon in place (D-12) — an explicit NON-action section so the audit sees it was deliberate:
   - "[x] Google Sheet: abandoned in place — NO export, NO delete, NO read-only freeze (D-12)." One sentence: the fresh-start (D-01) already made the Sheet worthless to the system; deleting it is busywork. (Pre-checked — it is a deliberate no-op, not a pending action.)

5. Code-level Google-freedom proofs (already established, D-13) — reference, pre-checked:
   - "[x] go list -deps ./cmd/squirebot is Google-free (P13 / 13-03)."
   - "[x] Watcher binary 57% smaller (16.44 MB to 7.07 MB) + zero Google OAuth/Sheets/secret strings on a 9-pattern byte scan (P13 / 13-04)."
   - "[x] Backend handles no Google secret; no Sheets/Drive client in the codebase (P13)."

6. Milestone-close note — one line: with the reporting-in confirmation + both live assets retired + the code-level proofs, CUTOVER-01 and CUTOVER-04 are satisfied and the v2.0 "Off Google" goal is met.

Keep it concise (the eviction-runbook is the length model). Do NOT instruct any Sheet export/delete (D-12). Do NOT add a soak-window deliverable (D-05).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/decommission-checklist.md','utf8'); const need=['Apps Script','OAuth','abandoned in place','go list -deps','last_seen']; const miss=need.filter(m=>!s.includes(m)); if(miss.length){console.error('MISSING:',miss);process.exit(1);} if(s.split(String.fromCharCode(10)).length<30){console.error('too short');process.exit(1);} console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - docs/decommission-checklist.md exists and is at least 30 lines
    - It contains all of: "Apps Script", "OAuth", "abandoned in place", "go list -deps", "last_seen"
    - It contains the CUTOVER-01 reporting-in SQL count line (SELECT COUNT(*) FROM character WHERE last_seen ...) as a single checkbox item (D-05)
    - It contains a pre-checked "[x]" abandon-in-place item for the Sheet (NO export/delete/freeze — D-12)
    - It contains pre-checked "[x]" items referencing the P13 code-level proofs (go list -deps Google-free + the 57%-smaller binary)
    - It does NOT instruct any Sheet export, delete, or read-only freeze
    - It does NOT introduce a separate soak-window deliverable
  </acceptance_criteria>
  <done>docs/decommission-checklist.md exists as the CUTOVER-04 proof artifact in the eviction-runbook style: the reporting-in SQL gate (CUTOVER-01/D-05), the two live-asset retirement items with cited click-paths (D-10/D-11), the deliberate abandon-in-place non-action (D-12), and the referenced P13 code-level proofs (D-13).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Confirm reporting-in + disable the Apps Script enrichment triggers (D-05, D-10)</name>
  <read_first>
    - docs/decommission-checklist.md (the artifact drafted in Task 1 — tick its boxes as you go)
    - .planning/phases/16-cutover-decommission/16-RESEARCH.md (Pitfall 3 — the triggers fire independent of the dark watchers; the cited disable path)
    - apps-script/src/triggers/installTriggers.ts (the 7 triggers to delete)
  </read_first>
  <action>
The maintainer (a) confirms guildies are reporting in (CUTOVER-01/D-05) and (b) disables the Apps Script enrichment triggers (D-10).

1. **Reporting-in confirmation (CUTOVER-01, D-05):** on the VPS, run against the live SQLite DB:
   ```sql
   SELECT COUNT(*) FROM character WHERE last_seen IS NOT NULL;
   ```
   (Optionally scope to since the flip: WHERE last_seen > the-flip-date-epoch.) Fill the count + date into the checklist's reporting-in line. This is a maintainer judgment that onboarded guildies are reporting in and their views look right — it replaces the void shadow soak (the Sheet is frozen). No %-gate; a representative count suffices.

2. **Disable the Apps Script triggers (D-10):** open script.google.com/home/triggers (or the container-bound project's editor, Triggers panel) and DELETE each of the 7 time-driven triggers installTriggers.ts created (daily PigParse + 3 weekly wiki + onChange + hourly view + watchdogs). Verify via the Apps Script Executions dashboard that NO further scheduled refreshPigparse/refreshWikiItems runs appear. This stops the parallel load on the volunteer-run wiki/PigParse (politeFetch hygiene) and is a real teardown step — the triggers fire on Google's schedulers independent of the OAuth-blocked watchers (Pitfall 3).

Tick the corresponding checklist boxes (the reporting-in line + the triggers item).
  </action>
  <acceptance_criteria>
    - The reporting-in line in docs/decommission-checklist.md is filled with a real count (N of ~12) + date
    - All 7 Apps Script triggers are deleted (the Triggers dashboard shows zero remaining triggers for the project)
    - The Apps Script Executions dashboard shows no further scheduled enrichment runs after the deletion
    - The triggers checklist item is ticked
  </acceptance_criteria>
  <resume-signal>Type "done — reporting-in count recorded (N of ~12) and all 7 Apps Script triggers deleted (zero remain)" or describe what you found (e.g. triggers already absent → note it as a no-op verification).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Retire the Google OAuth 2.0 Client ID (D-11)</name>
  <read_first>
    - docs/decommission-checklist.md (tick the OAuth-client item)
    - .planning/phases/16-cutover-decommission/16-RESEARCH.md (the cited OAuth-client delete path + the 30-day-restorable / immediate-token-revoke behavior; the Security Domain residual note)
  </read_first>
  <action>
The maintainer retires the Google OAuth 2.0 Client ID (D-11) — the asset CUTOVER-04 explicitly names. In the Google Cloud Console: APIs and Services, then Credentials/Clients, select the v1.0.2 desktop OAuth 2.0 Client ID, and DELETE it (CITED: support.google.com/cloud/answer/15549257).

This is safe and final-enough:
- The watcher OAuth CODE is already deleted (P13), so the client has NO consumer — nothing breaks.
- Deleting immediately revokes any outstanding tokens (a net security reduction).
- The client is restorable for 30 days then permanently gone (note this residual in the checklist — a deleted-but-restorable window is acceptable since it has no consumer).

(Disabling vs deleting both satisfy "no live Google machinery"; deletion is the cleaner end-state and is the locked intent. If the console only offers disable in your view, disable is an acceptable fallback per the Environment table.)

Tick the OAuth-client checklist item with the action taken (deleted / disabled) + date.
  </action>
  <acceptance_criteria>
    - The v1.0.2 desktop OAuth 2.0 Client ID is deleted (or disabled) in the Cloud Console — confirmed absent/disabled in APIs and Services, Credentials
    - The OAuth-client checklist item in docs/decommission-checklist.md is ticked with the action + date
    - No watcher or backend functionality regressed (expected — the OAuth code was already removed in P13; the client had no consumer)
  </acceptance_criteria>
  <resume-signal>Type "done — Google OAuth client deleted (or disabled), checklist ticked" or describe what you found.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Final decommission sweep — checklist complete, no live Google machinery remains</name>
  <read_first>
    - docs/decommission-checklist.md (the now-completed artifact)
    - .planning/phases/16-cutover-decommission/16-CONTEXT.md (D-12 abandon-in-place + D-13 satisfaction criteria)
  </read_first>
  <action>
The maintainer does the final milestone-close sweep: walk every box in docs/decommission-checklist.md and re-confirm each Google-side post-condition in its console. Concretely:
1. Confirm every actionable box is ticked (reporting-in count filled; triggers item; OAuth-client item) and the abandon-in-place + code-proof items are present as pre-checked non-actions.
2. Re-open script.google.com/home/triggers and confirm ZERO triggers remain for the project.
3. Re-open the Cloud Console (APIs and Services, Credentials) and confirm the OAuth client is absent/disabled.
4. Confirm you did NOT export/delete/freeze the Sheet (D-12 — it stays untouched).
5. Spot-check the live system is unaffected: the site serves and watchers still ingest (the teardown touched nothing the live system depends on).
Approve only when all five hold. The docs commit is handled by the orchestrator per commit_docs.
  </action>
  <what-built>
The CUTOVER-04 decommission is complete: the reporting-in confirmation is recorded (CUTOVER-01/D-05), the 7 Apps Script enrichment triggers are deleted (D-10), the Google OAuth client is retired (D-11), the Sheet is abandoned in place (D-12 — a deliberate non-action), and docs/decommission-checklist.md documents all of it plus the existing P13 code-level Google-freedom proofs (D-13). With these, "no live Google machinery / no Google dependency remains" — the v2.0 "Off Google" goal is met.
  </what-built>
  <how-to-verify>
The maintainer does a final sweep confirming every checklist box is resolved and nothing Google-side is still live:

1. Open docs/decommission-checklist.md and confirm EVERY box is ticked or is an explicit pre-checked non-action:
   - Reporting-in count filled (N of ~12).
   - Apps Script triggers: zero remain (re-check script.google.com/home/triggers).
   - OAuth client: deleted/disabled (re-check Cloud Console Credentials).
   - Sheet: abandoned in place (confirm you did NOT export/delete/freeze it — D-12).
   - The three code-level proofs referenced.
2. Spot-confirm the backend is unaffected: the live site still serves, watchers still ingest (the Google teardown touched nothing the live system depends on — that is the whole point).
3. Confirm there is NO lingering Google dependency: no further Apps Script executions, no OAuth client in use, no Sheets/Drive client in the codebase (P13 proof).

This is the milestone-close gate for v2.0. After approval, the docs change is committed (the orchestrator handles the commit per commit_docs settings) and the phase closes.
  </how-to-verify>
  <acceptance_criteria>
    - Every actionable box in docs/decommission-checklist.md is ticked (reporting-in count, triggers, OAuth client); the abandon-in-place + code-proof items are pre-checked non-actions
    - script.google.com/home/triggers shows zero triggers for the project
    - The OAuth client is absent/disabled in the Cloud Console
    - The Sheet was NOT exported, deleted, or frozen (D-12 honored)
    - The live backend + frontend + watcher ingest are unaffected by the teardown
  </acceptance_criteria>
  <resume-signal>Type "approved — checklist complete, no live Google machinery remains, Off Google goal met" or describe any box still open.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → Google Apps Script console | Deleting time-driven triggers stops server-side automation that was loading external APIs. |
| maintainer → Google Cloud Console | Deleting the OAuth client revokes outstanding tokens and removes a credential. |
| live SQLite DB → checklist | A read-only COUNT(*) query produces the reporting-in confirmation figure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-16 | Information disclosure (residual) | the deleted OAuth client | accept | CITED Google behavior: a deleted OAuth client is restorable for 30 days then permanently gone. Acceptable — the watcher OAuth code is already deleted (P13), so the client has no consumer; deletion immediately revokes any outstanding tokens. The 30-day window is noted in the checklist; no token is usable in it because no client flow exists. |
| T-16-17 | (good-citizen, not STRIDE) | the live Apps Script triggers | mitigate | Deleting the 7 triggers stops the parallel load on the volunteer-run wiki/PigParse APIs the backend now also serves — the politeFetch ethos (D-10). Verified via the Executions dashboard showing no further scheduled runs. |
| T-16-18 | Tampering / availability | the live backend during teardown | accept | The Google teardown touches ONLY Google-side assets the live system no longer depends on (P13 proved code-level Google-freedom). The Task-4 sweep confirms the backend/frontend/ingest are unaffected. No live-system change in this plan beyond a committed docs file. |
| T-16-19 | Repudiation | the decommission record | mitigate | docs/decommission-checklist.md is the committed, dated proof artifact (D-13) — each retired asset ticked with its verification, providing an auditable record of what was decommissioned and when. |

**Net posture:** this plan is a net security REDUCTION — it removes a Google OAuth client (revoking its tokens) and stops server-side automation hitting third-party APIs. The only residual is the 30-day OAuth-client restore window, which is benign (no consumer exists). No new code, no new secret, no HIGH-severity unmitigated threat.
</threat_model>

<verification>
- docs/decommission-checklist.md exists (>=30 lines), contains the reporting-in SQL line + the two retirement items + the abandon-in-place non-action + the P13 code-level proofs (the Task-1 automated grep gate).
- Human: script.google.com/home/triggers shows zero triggers; the OAuth client is deleted/disabled in the Cloud Console; the Sheet was NOT touched (D-12); the live system is unaffected.
- The reporting-in count (N of ~12) is recorded in the checklist (CUTOVER-01 / D-05).
</verification>

<success_criteria>
- The reporting-in confirmation is captured as one SQL count line (CUTOVER-01 satisfied in spirit, D-05).
- The Apps Script enrichment triggers are disabled (no further scheduled runs) and the Google OAuth client is retired (tokens revoked) — no live Google machinery remains (CUTOVER-04 / D-10/D-11).
- The Sheet is abandoned in place (no export/delete/freeze — D-12).
- docs/decommission-checklist.md is the committed CUTOVER-04 proof artifact tying together the retired assets + the P13 code-level proofs (D-13) — the v2.0 "Off Google" goal is met.
</success_criteria>

<output>
After completion, create `.planning/phases/16-cutover-decommission/16-04-SUMMARY.md`
</output>
