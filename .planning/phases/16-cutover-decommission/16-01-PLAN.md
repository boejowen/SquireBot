---
phase: 16-cutover-decommission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/backendsrv/enrich/eqconst.go
  - internal/backendsrv/store/charmeta.go
  - internal/backendsrv/store/readviews.go
  - internal/backendsrv/webadmin/charmeta.go
  - internal/backendsrv/webadmin/charmeta_test.go
  - cmd/squirebot-server/main.go
  - web/src/lib/charmeta.ts
  - web/src/lib/api.ts
  - web/src/lib/components/CharMetaForm.svelte
  - web/src/routes/char-meta/+page.svelte
  - web/src/lib/components/SiteShell.svelte
  - web/src/lib/__tests__/charmeta.test.ts
autonomous: true
requirements: [CUTOVER-02]
must_haves:
  truths:
    - "A signed-in member (no officer role) can open /char-meta, pick an existing character, set its class/level/race/is_bank_toon, and the values persist"
    - "gear_check and spell_check stop showing blank for a character once its class (and level) are set via the form"
    - "A character flagged is_bank_toon via the form appears in the bank view (and becomes selectable in the existing BankCoinForm)"
    - "The server rejects an invalid class, invalid race, or out-of-range level with 400 invalid_input and writes nothing"
    - "The level input is type=text inputmode=numeric (never type=number) so the Svelte-5 number-coercion crash (CR-01) cannot recur"
  artifacts:
    - path: "internal/backendsrv/webadmin/charmeta.go"
      provides: "CharMetaSetHandler (POST) + CharMetaListHandler (GET) — login-only audited write + pick-list"
      contains: "func CharMetaSetHandler"
    - path: "internal/backendsrv/store/charmeta.go"
      provides: "SetCharMetaTx mutator (UPDATE class/level/race/is_bank_toon by id, is_removed=0 scoped) + ErrCharNotFound"
      contains: "func SetCharMetaTx"
    - path: "internal/backendsrv/enrich/eqconst.go"
      provides: "RACES value set (14 abbreviations, ported from eq-constants.ts)"
      contains: "var RACES"
    - path: "web/src/lib/charmeta.ts"
      provides: "pure validation helpers (validateLevel/validateClass/validateRace/charMetaIsValid/charMetaPayload/inputsFromChar/charMetaChanged) cloned from coin.ts"
      contains: "rawToTrimmed"
    - path: "web/src/lib/components/CharMetaForm.svelte"
      provides: "the login-only char-meta form (clone of BankCoinForm)"
      contains: "inputmode=\"numeric\""
    - path: "web/src/routes/char-meta/+page.svelte"
      provides: "member-accessible route page (clone of bank-coin/+page.svelte)"
      contains: "CharMetaForm"
  key_links:
    - from: "web/src/lib/components/CharMetaForm.svelte"
      to: "/api/v1/char/meta"
      via: "saveCharMeta (postJSON) in api.ts"
      pattern: "saveCharMeta"
    - from: "cmd/squirebot-server/main.go"
      to: "webadmin.CharMetaSetHandler"
      via: "mux.Handle under webauth.RequireSession"
      pattern: "char/meta.*RequireSession|RequireSession.*CharMetaSetHandler"
    - from: "internal/backendsrv/webadmin/charmeta.go"
      to: "enrich.CLASSES + enrich.RACES"
      via: "validCharMeta server-side membership check"
      pattern: "slices.Contains\\(enrich\\.(CLASSES|RACES)"
---

<objective>
Build the char-metadata web form + backend write endpoint (CUTOVER-02) — the ONLY code build in Phase 16. It lets any signed-in guild member set `class` / `level` / `race` / `is_bank_toon` on a character that already exists (created by its first watcher upload). This is the fresh-start replacement for the Sheet backfill (D-01/D-02): the columns already exist in the schema, so there is NO migration — just a handler, a store mutator, a value-set port, and a Svelte form.

Purpose: `compute/gearcheck.go` skips characters with no `class` (and keys the Iksar gear tier on `race=="IKS"`); `compute/spellcheck.go` skips characters with no `class` or `level<1`; the bank view filters on `is_bank_toon=1`. Until this form populates those fields, `gear_check`/`spell_check` ship BLANK for every character and no character can be a bank toon. This one piece of "more work" is the phase's center of gravity — without it the Core Value view ("what do I still need") is empty.

Output: a login-only `POST /api/v1/char/meta` + `GET /api/v1/char/meta-list`, a `/char-meta` route page wired into the shell nav, and node-only tests in both repo styles (pure-helper + `.svelte` source-assertion).

NOTE: this plan is `autonomous: true` (a strict clone of shipped, code-reviewed code). The REQUIRED browser smoke of the form — the node-only web suite is structurally DOM-blind to the CR-01 crash (memory: `web-tests-node-only-blind-to-dom`; P15 shipped 165 green tests with 2 crashing browser blockers) — is performed in Plan 16-03 Task 1 against the live deploy (it already calls for "the Plan-01 browser smoke, repeated against production"). One smoke, where it matters most, keeps this plan a clean autonomous build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cutover-decommission/16-CONTEXT.md
@.planning/phases/16-cutover-decommission/16-RESEARCH.md
@.planning/phases/16-cutover-decommission/16-PATTERNS.md

<interfaces>
<!-- The EXACT contracts the executor clones. Extracted from the codebase — no exploration needed. -->

The four char-meta columns ALREADY EXIST (NO migration — D-02). From `internal/backendsrv/migrations/00001_init.sql:12-15`:
```sql
class TEXT,                              -- e.g. "WAR" (set later / by backfill (P16))
level INTEGER,                           -- 1..60 (set later)
race TEXT,                               -- e.g. "IKS" (set later)
is_bank_toon INTEGER NOT NULL DEFAULT 0  -- (set later)
```

Server-side value set already in Go — `internal/backendsrv/enrich/eqconst.go:26`:
```go
var CLASSES = []string{"WAR","CLR","PAL","RNG","SHD","DRU","MNK","BRD","ROG","SHM","NEC","WIZ","MAG","ENC"}
```
RACES is NOT yet in Go. Port verbatim from `apps-script/src/lib/eq-constants.ts:44-47`:
`HUM BAR ERU ELF HIE DEF HEF DWF TRL OGR HFL GNM IKS VAH`

The pick-list / pre-fill source already exists — `internal/backendsrv/store/readviews.go:279` (`CharsWithMeta`) returns, for every `is_removed=0` char ordered by name:
```go
type CharMeta struct { ID int64; Name string; Class string; Level int64; Race string } // NULL→zero-values
// SELECT id, name, class, level, race FROM character WHERE is_removed = 0 ORDER BY name
```
NOTE: `CharsWithMeta` does NOT select `is_bank_toon`. For the form's pre-fill you also need it. Extend the SELECT + `CharMeta` struct by ONE column at the right edge (extend-only) — add `IsBankToon bool` reading a `sql.NullInt64`/`int` → bool — OR add a thin sibling read in `store/charmeta.go`. The planner's recommendation: extend `CharMeta` + `CharsWithMeta` by one right-edge column (the existing `compute` consumers ignore the extra field).

Package-private webadmin helpers the handler REUSES (do NOT re-declare — they live in `officers.go`/`audit.go`):
- `caller(ctx) string` — the acting discord_user_id (`officers.go:58`), AUDIT ONLY
- `nowUnix() int64`, `writeJSON(w, v)`, `writeJSONError(w, status, code)` (`officers.go:37,44`)
- `withTx(ctx, db, fn)` + `AppendAuditTx(ctx, tx, event, actor, detail, now)` (`audit.go:57,88`) — one BEGIN IMMEDIATE tx

The web API contract (`web/src/lib/api.ts`): clone `fetchBankToons`/`saveCoin` (lines 384-395) over the existing `getJSON`/`postJSON` cores (lines 154-269) — they already carry `credentials:'include'`, the 401→`Unauthenticated`/403→`Forbidden(code)`/non-2xx→`ApiError(status,code)` mapping, and the malformed-2xx guard. Mirror the `BankToon` interface (lines 274-281).

The route-wiring block to EXTEND — `cmd/squirebot-server/main.go:328-330`:
```go
// Bank-coin — LOGIN-ONLY (D-12): RequireSession, NOT RequireOfficer.
mux.Handle("GET /api/v1/coin/bank-toons", webauth.RequireSession(db, webadmin.BankToonsHandler(db)))
mux.Handle("POST /api/v1/coin", webauth.RequireSession(db, webadmin.CoinSetHandler(db)))
```
The officer-only block is directly ABOVE (lines 319-326) — char-meta must NOT go there.

Test helpers already in `internal/backendsrv/webadmin/*_test.go` (same package — reuse directly): `withCaller(discordID, next)` (`officers_test.go:32`), `postJSON(t, handler, body)` (`officers_test.go:77`), `decodeErr(t, rec)` (`officers_test.go:258`), `auditCount(t, ctx, db, event)` (`officers_test.go:68`), `seedPlainMember(t, ctx, db, id, name)` (`coin_test.go:72`), `itoa(n)` (`eviction_test.go:391`), `store.NewTestDB(t)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend char-meta endpoint + store mutator + RACES port</name>
  <files>internal/backendsrv/enrich/eqconst.go, internal/backendsrv/store/charmeta.go, internal/backendsrv/store/readviews.go, internal/backendsrv/webadmin/charmeta.go, internal/backendsrv/webadmin/charmeta_test.go, cmd/squirebot-server/main.go</files>
  <read_first>
    - internal/backendsrv/webadmin/coin.go (THE handler template to clone — CoinSetHandler + BankToonsHandler + validCoin; the login-only package doc lines 1-20)
    - internal/backendsrv/store/coin.go (THE store mutator template — SetCoinTx + the ErrNotBankToon fail-closed shape)
    - internal/backendsrv/enrich/eqconst.go (the CLASSES precedent the RACES port follows, same file)
    - internal/backendsrv/webadmin/audit.go (withTx + AppendAuditTx — the audited-tx idiom; do NOT hand-roll the tx)
    - internal/backendsrv/store/readviews.go lines 82-92 + 279-310 (CharMeta struct + CharsWithMeta — the pick-list/pre-fill source to extend by one column)
    - internal/backendsrv/webadmin/coin_test.go (THE test template — TestCoinSet_NonOfficerCanWrite proves the D-03 login-only-write-actually-persists path; clone its shape)
    - internal/backendsrv/webadmin/officers_test.go lines 28-84 (the shared test helpers: withCaller/postJSON/auditCount/seedPlainMember)
    - cmd/squirebot-server/main.go lines 309-331 (the route-wiring block: officer-only block lines 319-326 vs login-only coin block 328-330)
    - apps-script/src/lib/eq-constants.ts lines 44-47 (the RACES source to port verbatim)
  </read_first>
  <action>
Implement the backend write surface for char-meta, cloning the bank-coin trio. Concrete steps:

1. **Port RACES into `internal/backendsrv/enrich/eqconst.go`** (resolves Open Question 1). Add directly after the `CLASSES` slice, following its precedent exactly:
```go
// RACES is the canonical ordered list of the 14 P1999 race abbreviations,
// ported verbatim from eq-constants.ts (`RACES`). Used for char-meta server-side
// validation; the frontend <select> mirrors it. ("IKS" is load-bearing — compute/
// gearcheck.go keys the Iksar gear tier on this exact literal.)
var RACES = []string{
	"HUM", "BAR", "ERU", "ELF", "HIE", "DEF", "HEF", "DWF",
	"TRL", "OGR", "HFL", "GNM", "IKS", "VAH",
}
```

2. **Extend `CharMeta` + `CharsWithMeta` by one right-edge column (`is_bank_toon`)** in `internal/backendsrv/store/readviews.go` (extend-only; the existing `compute.GearCheck`/`SpellCheck` consumers ignore the new field). Add `IsBankToon bool` to the `CharMeta` struct (after `Race`), add `is_bank_toon` to the SELECT's column list at the right edge, and scan it (it is `INTEGER NOT NULL DEFAULT 0`, so scan into an `int` and set `c.IsBankToon = (v == 1)`). This is the GET handler's pick-list + pre-fill source.

3. **Create `internal/backendsrv/store/charmeta.go`** — the mutator (clone `SetCoinTx`'s shape but SIMPLER: no bank-toon gate, since setting `is_bank_toon` IS one of the columns). Parameterized `?` only (V5):
```go
package store

import ("context"; "database/sql"; "errors"; "fmt")

// ErrCharNotFound — the target character does not exist or is removed (RowsAffected==0).
// The handler maps it to 400 invalid_input (fail-closed, mirroring ErrNotBankToon).
var ErrCharNotFound = errors.New("char_not_found")

// SetCharMetaTx writes class/level/race/is_bank_toon onto an EXISTING, non-removed
// character inside the caller's tx. level is *int64 so a blank/unset level stays NULL
// (a NULL level → 0 → spellcheck skips it; the form must not fabricate a 0). is_removed=0
// scoping mirrors CharsWithMeta (the form edits live chars only; D-03 forbids pre-creating).
func SetCharMetaTx(ctx context.Context, tx *sql.Tx, characterID int64, class string, level *int64, race string, isBankToon bool) error {
	bt := 0
	if isBankToon { bt = 1 }
	var levelArg any
	if level != nil { levelArg = *level } // nil → SQL NULL
	res, err := tx.ExecContext(ctx,
		`UPDATE character SET class = ?, level = ?, race = ?, is_bank_toon = ? WHERE id = ? AND is_removed = 0`,
		class, levelArg, race, bt, characterID)
	if err != nil { return fmt.Errorf("set char meta (character_id=%d): %w", characterID, err) }
	n, _ := res.RowsAffected()
	if n == 0 { return ErrCharNotFound }
	return nil
}
```

4. **Create `internal/backendsrv/webadmin/charmeta.go`** — clone `coin.go`. Request struct: `charMetaReq { CharacterID int64; Class string; Level *int64; Race string; IsBankToon bool }` (Level is `*int64` so omitted/blank → NULL). Two handlers:
   - `CharMetaListHandler(db)` (GET) — method-check GET → `store.CharsWithMeta(r.Context())` (it takes a `*store.Store`; construct/obtain it the same way `BankToonsHandler` reaches the store, OR if `CharsWithMeta` is a `*Store` method add a thin `store.CharsForMeta(ctx, db)` free-function wrapper beside `ListBankToons` in `charmeta.go`) → `if list == nil { list = []store.CharMeta{} }` → `writeJSON`. **Read `coin.go:47-64` (`BankToonsHandler`) and mirror exactly how it obtains the store.**
   - `CharMetaSetHandler(db)` (POST) — clone `CoinSetHandler` line-by-line: method-check POST → `json.NewDecoder(r.Body).Decode(&req)` (reject `err != nil || req.CharacterID <= 0` → 400 `invalid_input`) → `validCharMeta(req)` → `writer := caller(ctx)` (AUDIT ONLY, D-03) → `withTx` composing `store.SetCharMetaTx(...)` + `AppendAuditTx(ctx, tx, "char_meta_set", writer, map[string]any{"character_id": req.CharacterID}, now)` → on error: `errors.Is(err, store.ErrCharNotFound)` → 400 `invalid_input`, else `slog.Error` + 500 `internal` → success `writeJSON(w, map[string]any{"character": <name>, ...})` (echo the saved values; a read-back failure is non-fatal — fall back to request values, like coin.go:117-126).
   - `validCharMeta(req charMetaReq) bool`: `slices.Contains(enrich.CLASSES, req.Class)` AND `slices.Contains(enrich.RACES, req.Race)` AND (`req.Level == nil` OR `*req.Level >= 1 && *req.Level <= 60`) → `is_bank_toon` is a JSON bool (decoder-validated). Import `"slices"` + `github.com/boejowen/SquireBot/internal/backendsrv/enrich`.
   - **DECISION (A2, Claude's discretion — documented):** blank/omitted level → NULL (valid); a non-blank level MUST be 1–60. Rationale: a member may know a char's class+race before its level; `spellcheck.go:55` already treats NULL level (→0) as "skip", which is the correct unleveled behavior. Free-text class/race is rejected (Pitfall 5 — store the abbreviation, never a display name).

5. **Register the two routes in `cmd/squirebot-server/main.go`** — add immediately AFTER the coin block (line 330), under `webauth.RequireSession` (login-only, D-03 — NOT RequireOfficer):
```go
// Char-meta — LOGIN-ONLY (D-03): RequireSession. Any signed-in member sets any
// existing character's class/level/race/is_bank_toon (non-sensitive shared data,
// bank-coin precedent). NEVER RequireOfficer.
mux.Handle("GET /api/v1/char/meta-list", webauth.RequireSession(db, webadmin.CharMetaListHandler(db)))
mux.Handle("POST /api/v1/char/meta", webauth.RequireSession(db, webadmin.CharMetaSetHandler(db)))
```

6. **Create `internal/backendsrv/webadmin/charmeta_test.go`** (clone `coin_test.go`). Reuse the package's existing helpers (withCaller/postJSON/decodeErr/auditCount/seedPlainMember/itoa). Tests:
   - `TestCharMetaSet_NonOfficerCanWrite` (the D-03 proof — clone `TestCoinSet_NonOfficerCanWrite`): seed a plain member (no guild_admins row), insert a char, POST `{"character_id":N,"class":"WAR","level":50,"race":"IKS","is_bank_toon":true}`, assert 200 AND read the columns back (class=="WAR", level==50, race=="IKS", is_bank_toon==1) — proven, not just asserted on the response — AND `auditCount("char_meta_set")==1`.
   - `TestCharMetaSet_RejectsBadClass` / `_RejectsBadRace`: `"class":"Warrior"` (display name) and `"race":"Iksar"` → 400 `invalid_input`, nothing written.
   - `TestCharMetaSet_RejectsBadLevel`: `"level":0` and `"level":61` → 400 `invalid_input`; `"level":null` → 200 (blank allowed) with the level column NULL.
   - `TestCharMetaSet_RejectsRemovedOrMissingChar`: a non-existent id (and an `is_removed=1` char) → 400 `invalid_input` (the `ErrCharNotFound` path).
   - `TestCharMetaList_ReturnsExistingChars`: insert two chars, GET → both present with their id/name. (Empty → `[]`, not null.)

NO migration file. NO Sheet read anywhere (D-01). Do NOT add a "create character" path (D-03 — the form edits existing chars only).
  </action>
  <verify>
    <automated>cd internal/backendsrv && go build ./... && go vet ./... && go test ./webadmin/... ./store/... -run "CharMeta" -v</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` and `go vet ./...` from the repo root exit 0
    - `go test ./internal/backendsrv/webadmin/... ./internal/backendsrv/store/...` exits 0 (all CharMeta tests green)
    - `internal/backendsrv/enrich/eqconst.go` contains `var RACES` with all 14 entries including `"IKS"`
    - `internal/backendsrv/store/charmeta.go` contains `func SetCharMetaTx` with the exact SQL `UPDATE character SET class = ?, level = ?, race = ?, is_bank_toon = ? WHERE id = ? AND is_removed = 0` and `var ErrCharNotFound`
    - `internal/backendsrv/webadmin/charmeta.go` contains `func CharMetaSetHandler`, `func CharMetaListHandler`, and a `validCharMeta` that calls `slices.Contains(enrich.CLASSES, ...)` AND `slices.Contains(enrich.RACES, ...)`
    - `internal/backendsrv/webadmin/charmeta.go` does NOT contain `RequireOfficer` or `IsOfficer` (login-only, D-03)
    - `cmd/squirebot-server/main.go` contains `mux.Handle("POST /api/v1/char/meta", webauth.RequireSession(db, webadmin.CharMetaSetHandler(db)))` (RequireSession, not RequireOfficer)
    - `charmeta_test.go` contains `TestCharMetaSet_NonOfficerCanWrite` and it reads the columns back (a `SELECT ... FROM character` after the POST), and asserts `auditCount(... "char_meta_set") == 1`
    - No new file under `internal/backendsrv/migrations/` (grep the dir — unchanged file count)
    - No `Sheet`/`sheets`/`drive`/`oauth` import anywhere in the new Go files
  </acceptance_criteria>
  <done>The login-only char-meta POST + GET endpoints persist class/level/race/is_bank_toon onto existing characters, validate against enrich.CLASSES/RACES + level 1–60 (blank→NULL) server-side, audit every write as "char_meta_set", and are wired under RequireSession beside the coin routes — proven by the cloned coin_test.go-style table tests (including the non-officer-write-persists path).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend char-meta form (pure helpers + api wrappers + form + route + nav)</name>
  <files>web/src/lib/charmeta.ts, web/src/lib/api.ts, web/src/lib/components/CharMetaForm.svelte, web/src/routes/char-meta/+page.svelte, web/src/lib/components/SiteShell.svelte, web/src/lib/__tests__/charmeta.test.ts</files>
  <read_first>
    - web/src/lib/coin.ts (THE pure-helper template — clone the rawToTrimmed CR-01 choke point + validateCoinField + inputsFromToon + coinChanged shapes)
    - web/src/lib/components/BankCoinForm.svelte (THE form template — the onMount→select→prefill→validate→save flow + the CR-01 input markup lines 161-177 + the T-15-28 plain-{} name rule)
    - web/src/routes/bank-coin/+page.svelte (THE route-page template — member-accessible, NO officer getContext check)
    - web/src/lib/components/FormField.svelte (the shared label+control+error wrapper — reuse unchanged)
    - web/src/lib/api.ts lines 154-196 (getJSON), 240-269 (postJSON), 273-295 (BankToon/SaveCoinResult interfaces), 384-395 (fetchBankToons/saveCoin wrappers — the clone targets)
    - web/src/lib/__tests__/coin.test.ts (Style A pure-helper tests — clone the CR-01 number/null/undefined regression block)
    - web/src/lib/__tests__/eviction.test.ts lines 1-40 (Style B .svelte source-assertion via readFileSync — clone the input-type assertion)
    - web/src/lib/components/SiteShell.svelte lines 43-58 (the header `.shell-controls` — the officer-only Admin button {#if session?.isOfficer} vs the member-accessible SessionIndicator {#if session?.authenticated}; the char-meta nav goes under the latter guard)
    - web/src/lib/components/StateBlock.svelte (the loading/error/empty block the form uses)
  </read_first>
  <action>
Build the form as a strict clone of the bank-coin trio. The repo's web tests are node-only and BLIND to DOM behavior — the CR-01 input-type lesson MUST be honored or it will crash invisibly in a browser. Concrete steps:

1. **Create `web/src/lib/charmeta.ts`** (clone `coin.ts`). Copy the `rawToTrimmed(raw: CharMetaRaw)` choke point VERBATIM (the single most important thing — it makes helpers crash-proof regardless of input type). Export:
   - `CLASSES` + `RACES` const string arrays mirroring the Go sets (14+14 abbreviations — UX only; the server is authoritative).
   - `LEVEL_ERROR = 'Enter a level from 1 to 60 (or leave blank).'`
   - `validateLevel(raw): string | undefined` — clone `validateCoinField`: `const t = rawToTrimmed(raw); if (t === '') return undefined;` (blank = unset, valid) `if (!/^\d+$/.test(t)) return LEVEL_ERROR; const n = Number(t); if (Number.isSafeInteger(n) && n >= 1 && n <= 60) return undefined; return LEVEL_ERROR;`
   - `validateClass(v): string | undefined` / `validateRace(v): string | undefined` — `return v === '' || CLASSES.includes(v) ? undefined : '...'` (a `<select>` constrains UX, but validate for the gate; blank class is allowed at the form layer — but see the Save gate below).
   - `CharMetaInputs = { class: string; race: string; level: string; isBankToon: boolean }`.
   - `charMetaIsValid(inputs): boolean` — class non-blank AND race non-blank AND level valid (class/race required to be a meaningful write; level optional).
   - `levelPayload(raw): number | null` — `const t = rawToTrimmed(raw); return t === '' ? null : Number(t)` (blank → null → server stores NULL).
   - `charMetaPayload(inputs)` → `{ class: inputs.class, level: levelPayload(inputs.level), race: inputs.race, is_bank_toon: inputs.isBankToon }` (snake_case, matches the Go `charMetaReq` JSON tags).
   - `inputsFromChar(c: CharMetaItem): CharMetaInputs` (clone `inputsFromToon`; level number→string via a `levelToInput` like `coinToInput`, 0/null → '').
   - `charMetaChanged(inputs, c): boolean` (the Save gate — true when class/race/level/is_bank_toon differs from the loaded char).

2. **Extend `web/src/lib/api.ts`** (clone `fetchBankToons`/`saveCoin`, beside them). Add:
   - `interface CharMetaItem { character_id: number; name: string; class: string; level: number | null; race: string; is_bank_toon: boolean }` (mirror `BankToon`).
   - `interface SaveCharMetaResult { character: string; class: string; level: number | null; race: string; is_bank_toon: boolean }`.
   - `export function fetchCharsForMeta(fetchFn = fetch) { return getJSON<CharMetaItem[]>('/api/v1/char/meta-list', fetchFn); }`
   - `export function saveCharMeta(body: { character_id: number; class: string; level: number | null; race: string; is_bank_toon: boolean }, fetchFn = fetch) { return postJSON<SaveCharMetaResult>('/api/v1/char/meta', body, fetchFn); }`
   Keep the injectable `fetchFn` seam. Do NOT hand-roll fetch — reuse the cores.

3. **Create `web/src/lib/components/CharMetaForm.svelte`** (clone `BankCoinForm.svelte`). Same flow: `getContext<AuthGuard>(AUTH_GUARD_KEY)` → `onMount` `fetchCharsForMeta()` → `<select>` a character → `onSelect` pre-fills from the chosen char via `inputsFromChar` → `$derived` field errors + `canSave = !!selectedChar && charMetaIsValid(inputs) && charMetaChanged(inputs, selectedChar) && !saving` → `onSave` POSTs via `saveCharMeta`, reflects the saved values into the local list so the gate re-disables, sets `successMsg = `Saved details for ${res.character}.`` → a mid-session 401 catches `Unauthenticated` → `authGuard(err)`. Fields wrapped in `FormField`:
   - char `<select>` (the pick-list, like BankCoinForm lines 148-155).
   - class `<select>` sourced from the `CLASSES` array; race `<select>` from `RACES`.
   - **level: the CR-01 input — `type="text" inputmode="numeric" pattern="[0-9]*"`, NEVER `type="number"`.** Copy the input markup + the explanatory comment VERBATIM from `BankCoinForm.svelte:161-177`.
   - `is_bank_toon`: a checkbox (`<input type="checkbox" bind:checked={inputs.isBankToon} />`).
   - NO `ConfirmDialog` (non-destructive — D-12 precedent; resolves Open Question 2 — no confirm even on un-setting is_bank_toon).
   - SECURITY: render the character name ONLY via plain `{}` (Svelte auto-escapes) — NEVER `{@html}` (T-15-28). Copy the security header comment from BankCoinForm lines 14-17.
   Reuse `StateBlock` for loading/error/empty (clone lines 140-146; use `kind="error"` with `onRetry={load}`).

4. **Create `web/src/routes/char-meta/+page.svelte`** (clone `bank-coin/+page.svelte` verbatim, swapping component + title). Member-accessible — NO officer `getContext` check. Title "SquireBot — set character details", heading "Set character details", `<CharMetaForm />` in the `.form-card` (copy the `<style>` block as-is).

5. **Wire the nav in `web/src/lib/components/SiteShell.svelte`** — add a member-accessible affordance to `/char-meta` inside the header `.shell-controls` block (lines 46-57). **There is NO existing `/bank-coin` shell-nav entry to clone — this is a NET-NEW member-facing affordance, so do NOT hunt for a `/bank-coin` analog: it does not exist in the shell (`/bank-coin` is reached from the bank view's per-view "Record coin" link, NOT the shell nav).** The ONLY two affordances currently in `.shell-controls` are (a) the officer-only `Admin` button under `{#if session?.isOfficer}` (lines 47-52, navigates via `goAdmin` → `/admin`) and (b) the member-accessible `SessionIndicator` under `{#if session?.authenticated}` (lines 54-56). Char-meta is login-only / member-accessible (D-03), so place the new entry under a `{#if session?.authenticated}` guard, mirroring the `SessionIndicator` placement — **NOT under the `{#if session?.isOfficer}` `Admin` block.** Officer-gating it would silently breach D-03's member-accessible intent at the nav layer (members couldn't reach the form they're entitled to); leaving it un-guarded would surface it pre-auth. Concretely, add inside `.shell-controls`:
```svelte
{#if session?.authenticated}
  <a href="/char-meta" class="char-meta-nav">Character details</a>
{/if}
```
Reuse/clone the existing `.admin-nav` CSS rules for the new link's styling (rename to `.char-meta-nav`, or share the class) so it matches the chrome — it is a plain navigation link, not an officer marker, so no `goAdmin`-style handler is needed (a bare `<a href>` suffices). Do NOT add a second `<a>`/button inside the existing `session?.isOfficer` block.

6. **Create `web/src/lib/__tests__/charmeta.test.ts`** — BOTH repo styles:
   - **Style A (pure-helper, clone `coin.test.ts`):** drive the `charmeta.ts` helpers. MUST include the CR-01 regression block proving `validateLevel`/`charMetaIsValid` do NOT throw when fed `number`/`null`/`undefined` (the shapes `bind:value` can write back). Plus: `validateLevel('0')`/`'61'` → error; `'1'`/`'60'`/`''` → undefined; `validateClass('WAR')` → undefined, `'Warrior'` → error; `charMetaChanged` true on a class change, false when unchanged.
   - **Style B (.svelte source-assertion, clone `eviction.test.ts:14-40`):** `readFileSync` `CharMetaForm.svelte` and assert: `.toContain('inputmode="numeric"')` AND `.not.toContain('type="number"')` (the CR-01 guard) AND `.toContain('saveCharMeta')` AND that it imports `fetchCharsForMeta`/`saveCharMeta` (the form is wired to the login-only flow). ALSO `readFileSync` `SiteShell.svelte` and assert the `/char-meta` link is NOT officer-gated — i.e. it appears under the `session?.authenticated` guard, not the `session?.isOfficer` block (see the matching acceptance criterion below for the exact assertion shape).
  </action>
  <verify>
    <automated>cd web && npm run check && npx vitest run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run check` reports 0 errors / 0 warnings
    - `npx vitest run` is fully green (the new charmeta tests included)
    - `npm run build` emits `index.html` + `200.html` without error
    - `web/src/lib/charmeta.ts` contains `rawToTrimmed` and `function validateLevel` and exports `CharMetaInputs`
    - `web/src/lib/components/CharMetaForm.svelte` contains `inputmode="numeric"` and does NOT contain `type="number"`
    - `web/src/lib/components/CharMetaForm.svelte` contains `saveCharMeta` and `fetchCharsForMeta` and renders the character name via plain `{` interpolation (no `{@html}` in the file)
    - `web/src/routes/char-meta/+page.svelte` imports and renders `CharMetaForm` and has NO `getContext`/`isOfficer` officer check
    - `web/src/lib/api.ts` contains `export function saveCharMeta` and `export function fetchCharsForMeta` and `interface CharMetaItem`
    - `web/src/lib/components/SiteShell.svelte` contains a reference to `/char-meta`
    - The `/char-meta` nav entry in `SiteShell.svelte` is NOT inside an officer-gated block: it appears under a `session?.authenticated` guard, NOT under `session?.isOfficer`. Verify with a grep that isolates the `/char-meta` line's guard — e.g. `grep -n 'char-meta' web/src/lib/components/SiteShell.svelte` shows the link, and the nearest enclosing `{#if ...}` is `session?.authenticated` (the file must NOT contain `session?.isOfficer` wrapping the `/char-meta` `<a>`). A focused check: `grep -A1 'session?.isOfficer' web/src/lib/components/SiteShell.svelte` does NOT include `char-meta` in the guarded line(s).
    - `web/src/lib/__tests__/charmeta.test.ts` asserts both `inputmode="numeric"` present AND `type="number"` absent in the form source, AND asserts (via `readFileSync` on `SiteShell.svelte`) that the `/char-meta` link is under the `session?.authenticated` guard and is not officer-gated
  </acceptance_criteria>
  <done>The /char-meta route renders a login-only form that pre-fills from an existing character and lets any member set class/race/level/is_bank_toon via saveCharMeta; the level field is the crash-proof text+numeric input; the pure helpers are node-tested (incl. the CR-01 number/null regression) and the form source is asserted to carry the right input type — and the nav surfaces the route for any authenticated member (under session?.authenticated, NOT officer-gated, honoring D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/api/v1/char/meta` (POST) | An authenticated member's request body (class/level/race/is_bank_toon) crosses into a SQL write. Untrusted input. |
| browser → `/api/v1/char/meta-list` (GET) | Returns character names (user-controlled at watcher-upload time) to the form. |
| session cookie (P15) → handler caller identity | `caller(ctx)` resolves the acting discord_user_id from the existing opaque `sb_session` cookie — recorded in the audit row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | `SetCharMetaTx` (store) | mitigate | Parameterized `?` placeholders ONLY — `class`/`level`/`race`/`is_bank_toon` are never string-interpolated into SQL (the store-layer V5 invariant, cloned from coin.go). |
| T-16-02 | Tampering | `validCharMeta` (handler) | mitigate | Server-side re-validation independent of the form's `<select>`: `class ∈ enrich.CLASSES`, `race ∈ enrich.RACES`, `level` blank→NULL or 1–60. Out-of-set → 400 invalid_input, nothing written (T-15-29: never trust the disabled-button UX). Wrong-cased/free-text values are rejected so the gear/spell joins can't be silently corrupted (Pitfall 5). |
| T-16-03 | Tampering / (client) XSS | CharMetaForm success copy | mitigate | The character name (user-controlled) renders ONLY via plain `{}` (Svelte auto-escapes), NEVER `{@html}` (T-15-28, cloned from BankCoinForm). |
| T-16-04 | Elevation of Privilege | route gate | accept (by design) | Char-meta is login-only (`RequireSession`, D-03) — any signed-in member may set any existing character's metadata. This is INTENTIONAL: non-sensitive shared data, trust-rich ~12-person guild, no Discord↔guild-code ownership link exists. NOT a privilege bug. The handler never consults officer status (asserted by the absence of RequireOfficer/IsOfficer + the non-officer-write test). The nav entry is likewise member-accessible (under `session?.authenticated`, NOT `session?.isOfficer`) — gating it to officers would wrongly deny members the form they're entitled to. |
| T-16-05 | Repudiation | audit trail | mitigate | `AppendAuditTx("char_meta_set", caller(ctx), {character_id}, now)` writes an append-only audit row in the SAME tx as the write (commit-together / rollback-together). The acting discord id is captured for accountability. |
| T-16-06 | Spoofing (CSRF) | cookie-authed POST | accept (inherited) | The existing P15 posture: `SameSite=Lax` session cookie + JSON `Content-Type` required + exact-origin CORS (no wildcard). No new surface; unchanged from the shipped coin POST. |
| T-16-07 | DoS / Tampering | request body | mitigate | `json.NewDecoder(r.Body).Decode` + the `CharacterID <= 0` trivial guard; the values are tiny scalars (no large-body surface). A non-existent/removed character_id → ErrCharNotFound → 400 (fail-closed). |

**Net posture:** the char-meta form is a strict clone of the already-security-reviewed, regression-covered bank-coin form (P15 had its 2 node-blind blockers fixed + the WR-05 fail-open→fail-closed fix). No `security_block_on: high` concern is introduced provided the V5 server-side validation + parameterized SQL + plain-`{}` name rendering are honored — all inherited from the template. No HIGH-severity unmitigated threat.
</threat_model>

<verification>
- Backend: `go build ./...` + `go vet ./...` + `go test ./internal/backendsrv/...` all green from the repo root; the new `charmeta_test.go` proves the non-officer-write-persists path, the value-set/level rejections, and the audit row.
- Frontend: `cd web && npm run check` (0/0) + `npx vitest run` (green, incl. the charmeta pure-helper + source-assertion tests) + `npm run build` (emits index.html + 200.html).
- Cross-cut: NO new migration file; NO Sheets/Google import in any new file; the route is registered under `RequireSession` (grep main.go); the form's level input is `type="text" inputmode="numeric"` (grep the .svelte); the `/char-meta` shell-nav entry is under `session?.authenticated`, NOT officer-gated (grep SiteShell.svelte — D-03 member-accessible).
- Live browser smoke: deferred to Plan 16-03 Task 1 (against the live deploy) — the node-only suite cannot see the CR-01 crash, so a human eyeballs the level field + the save round-trip there.
</verification>

<success_criteria>
- A signed-in member can set class/level/race/is_bank_toon on an existing character via `/char-meta`, and the values persist (read back from the DB in tests; confirmed in the Plan 16-03 live browser smoke).
- `gear_check`/`spell_check` go from blank → populated for a character once its class (+ level) are set; an is_bank_toon-flagged char appears in the bank view + the BankCoinForm pick-list.
- Invalid class/race/level is rejected server-side (400 invalid_input) and writes nothing; every write is audited as `char_meta_set`.
- The level input cannot reintroduce the CR-01 crash (type=text inputmode=numeric, asserted in tests + eyeballed in the Plan 16-03 browser smoke).
- The `/char-meta` nav entry is member-accessible (surfaced under `session?.authenticated`, not officer-gated) — D-03 honored at the nav layer.
- No migration was added; no Google dependency was introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/16-cutover-decommission/16-01-SUMMARY.md`
</output>
