---
quick_id: 260531-3ml
title: Eviction restore web UI (close G-1) + Phase 15 deploy wrap-up docs
type: quick
created: 2026-05-31
files_modified:
  - internal/backendsrv/store/eviction.go
  - internal/backendsrv/store/eviction_test.go
  - internal/backendsrv/webadmin/eviction.go
  - internal/backendsrv/webadmin/eviction_test.go
  - cmd/squirebot-server/main.go
  - cmd/squirebot-server/main_test.go
  - web/src/lib/api.ts
  - web/src/lib/components/EvictionForm.svelte
  - web/src/lib/__tests__/eviction.test.ts
  - deploy/squirebot-server.service
  - docs/backend-deploy.md
---

<objective>
Two deliverables (LOCAL build + test only; the restore-UI code needs a VPS redeploy afterward — the orchestrator runs that; the docs need no deploy):

1. **Close G-1: add an eviction-restore web UI.** The backend restore is fully built (`POST /api/v1/admin/eviction/restore` → `RestoreHandler` → `store.RestoreOwnerTx`, re-mints a fresh code) and `api.ts` has `restoreEviction(owner_id)`, but there is **no way to LIST restorable (evicted-within-grace) owners** and **no UI control** — so restore is impossible from the browser. Add the missing list endpoint + surface a Restore section in the EvictionForm.
2. **Phase 15 deploy wrap-up docs** so the next deploy isn't tribal knowledge: wire the auth `EnvironmentFile` into the committed systemd unit + document the Phase 15 auth deploy steps.

Honor the established conventions: parameterized SQL (no interpolation), mirror existing handler/store shapes, officer-only at the route + the in-tx re-check already in RestoreHandler (don't change the auth model), node-only web tests (NO new test toolchain — extract pure helpers + source-inspection like 15-04/15-05/2qk), Svelte auto-escape `{}` on all user/Discord strings (no `{@html}`).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Backend — list restorable owners endpoint</name>
  <files>
    - internal/backendsrv/store/eviction.go
    - internal/backendsrv/store/eviction_test.go
    - internal/backendsrv/webadmin/eviction.go
    - internal/backendsrv/webadmin/eviction_test.go
    - cmd/squirebot-server/main.go
    - cmd/squirebot-server/main_test.go
  </files>
  <read_first>
    - internal/backendsrv/store/eviction.go — mirror `ListEvictableOwners` + the `EvictableOwner` struct EXACTLY (same shape/idiom) for the inverse query.
    - internal/backendsrv/webadmin/eviction.go — mirror `EvictableListHandler` (GET, method-check, writeJSON, nil→[]); the new handler sits beside it.
    - cmd/squirebot-server/main.go § the `GET /api/v1/admin/evictable` route registration — add the sibling route with `webauth.RequireOfficer`.
    - cmd/squirebot-server/main_test.go — the read-gate/route test pattern.
  </read_first>
  <action>
  - store/eviction.go: add `type RestorableOwner struct { OwnerID int64; Label string; CharCount int; GraceUntil int64 }` (JSON tags owner_id/label/char_count/grace_until) and `ListRestorableOwners(ctx, db, now int64) ([]RestorableOwner, error)` — the inverse of ListEvictableOwners: owners with >=1 character that is `is_removed = 1 AND grace_until IS NOT NULL AND grace_until > ? AND archived_at IS NULL` (still in grace, not archived). `GROUP BY o.id, o.label`, `char_count = COUNT(c.id)`, `grace_until = MIN(c.grace_until)` (soonest deadline), `ORDER BY o.label`. Parameterized `?` only.
  - webadmin/eviction.go: add `RestorableListHandler(db) http.HandlerFunc` mirroring EvictableListHandler — GET only, `store.ListRestorableOwners(ctx, db, nowUnix())`, nil→`[]`, writeJSON. (RestoreHandler already exists; do not change it.)
  - main.go: register `mux.Handle("GET /api/v1/admin/restorable", webauth.RequireOfficer(db, webadmin.RestorableListHandler(db)))` next to the evictable route.
  - Tests: store test — seed a live owner, an evicted-in-grace owner, and an archived/past-grace owner; assert ListRestorableOwners returns ONLY the in-grace evicted one. main_test (or a webadmin test) — add `/api/v1/admin/restorable` to the officer-gate coverage (no session → 401, non-officer → 403) consistent with the other admin routes.
  </action>
  <verify>
    <automated>go test ./internal/backendsrv/... ./cmd/squirebot-server/... && go build ./... && GOOS=linux GOARCH=amd64 go build ./cmd/squirebot-server</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'func ListRestorableOwners' internal/backendsrv/store/eviction.go`
    - `grep -q 'func RestorableListHandler' internal/backendsrv/webadmin/eviction.go`
    - `grep -q '/api/v1/admin/restorable' cmd/squirebot-server/main.go` wrapped in `RequireOfficer`
    - store test proves live + archived/past-grace owners are EXCLUDED, in-grace evicted included
    - `go test ./... exits 0`; cross-compile exits 0
  </acceptance_criteria>
  <done>A GET /api/v1/admin/restorable (officer-only) lists evicted-within-grace owners; tested; whole repo builds + cross-compiles + tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — Restore section in EvictionForm</name>
  <files>
    - web/src/lib/api.ts
    - web/src/lib/components/EvictionForm.svelte
    - web/src/lib/__tests__/eviction.test.ts
  </files>
  <read_first>
    - web/src/lib/api.ts § fetchEvictable / restoreEviction / RestoreResult / EvictableOwner — mirror fetchEvictable for the new list; restoreEviction + RestoreResult already exist.
    - web/src/lib/components/EvictionForm.svelte — the existing evict flow, the `route()` server-truth helper (authGuard collapse / owner-floor / 401), ConfirmDialog usage, graceDate.
    - web/src/lib/eviction.ts — graceDate (epoch-seconds→date) + extend with a pure restore-result→message helper for node-only testing.
  </read_first>
  <action>
  - api.ts: add `interface RestorableOwner { owner_id: number; label: string; char_count: number; grace_until: number }` and `fetchRestorable(fetchFn=fetch): Promise<RestorableOwner[]>` → `getJSON('/api/v1/admin/restorable')`, mirroring fetchEvictable.
  - eviction.ts: add a PURE helper `restoreResultMessage(res: RestoreResult): string` mapping the WR-01/WR-02 outcomes to copy — `code_mint_failed` → "Restored {label}, but re-minting the guild code failed — re-issue it on the server with `mint-code`." ; `new_code_issued` → "Restored {label}. A fresh guild code was minted on the SERVER (read it from the server logs / re-run `mint-code`) and hand it to them — it is not shown here." (the code is NOT web-deliverable, WR-02). Keep it pure + node-testable. (Pass the label in as an arg.)
  - EvictionForm.svelte: below the existing evict flow, add a **"Restore evicted guildies"** section: on mount also `fetchRestorable()`; render the restorable owners (label + char_count + `graceDate(grace_until)` deadline). Each row has a **Restore** button → ConfirmDialog (heading "Restore guildie", body: restoring un-removes their characters and re-issues a guild code that you must hand off out-of-band) → `restoreEviction(owner_id)` → on success show `restoreResultMessage(...)`, drop the owner from the restorable list; route errors through the SAME `route()` helper (officers-only collapse / 401 bubble / grace_expired inline "the grace period has expired"). If the restorable list is empty, render nothing (or a quiet empty note) — it is NOT an error. All labels via plain `{}` (T-15-28, no `{@html}`).
  - eviction.test.ts: add node-only cases for `restoreResultMessage` (both new_code_issued and code_mint_failed branches) + a `.svelte` source-inspection assertion that the Restore section calls restoreEviction + uses ConfirmDialog. NO new test dependency.
  </action>
  <verify>
    <automated>cd web && npm run check && npm run test:unit -- --run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'fetchRestorable' web/src/lib/api.ts` and `grep -q 'restoreResultMessage' web/src/lib/eviction.ts`
    - EvictionForm.svelte imports fetchRestorable + restoreEviction + ConfirmDialog and renders a Restore control
    - the success copy makes clear the new code is server-side / not shown in-browser (WR-02); no `{@html}` on owner labels
    - `npm run check` 0 errors/0 warnings; web tests pass; `npm run build` emits build/index.html
  </acceptance_criteria>
  <done>An officer can list evicted-within-grace guildies and Restore one from the web (confirm-before-commit), with copy that correctly says the re-minted code is retrieved server-side; check/tests/build green.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 15 deploy wrap-up docs</name>
  <files>
    - deploy/squirebot-server.service
    - docs/backend-deploy.md
  </files>
  <action>
  - deploy/squirebot-server.service: add `EnvironmentFile=-/etc/squirebot/squirebot.env` under `[Service]` (the leading `-` makes it optional so local/test starts without the file still work — matching what's live on the box). Keep the rest of the unit unchanged.
  - docs/backend-deploy.md: add a new section "## 7. Phase 15 — auth deploy (Discord login + admin forms)" documenting: (a) the root-only `EnvironmentFile` `/etc/squirebot/squirebot.env` (chmod 600) with `DISCORD_CLIENT_ID/SECRET/GUILD_ID`, `DISCORD_REDIRECT_URI=https://api.squirebot.quest/api/v1/auth/callback`, `SQUIREBOT_WEB_ORIGIN=https://squirebot.quest`, `SQUIREBOT_COOKIE_DOMAIN=squirebot.quest` — secret backend-only, never in the repo/bundle/logs; (b) the `EnvironmentFile=` line now in the unit; (c) `00004_web_auth.sql` applies on boot (goose.Up; schema v4); (d) seed the owner-floor once: `squirebot-server set-owner-floor <maintainer-discord-USER-id>`; (e) the frontend bundle deploy = `npm run build` → ship `web/build/` to `/var/www/squirebot` (Caddy `file_server`). Mirror the doc's existing terse, command-first style.
  </action>
  <verify>
    <automated>grep -q 'EnvironmentFile' deploy/squirebot-server.service && grep -q 'set-owner-floor' docs/backend-deploy.md && grep -q 'squirebot.env' docs/backend-deploy.md</automated>
  </verify>
  <done>The committed systemd unit carries the optional EnvironmentFile, and backend-deploy.md documents the full Phase 15 auth deploy (env file + secrets + owner-floor seed + migration + frontend bundle).</done>
</task>

</tasks>

<commit_guidance>
Three atomic commits (commit_docs false — CODE/docs commits as below; the orchestrator handles the PLAN/SUMMARY/STATE docs commit):
- Task 1 → `feat(260531-3ml): GET /api/v1/admin/restorable (list evicted-in-grace owners)`
- Task 2 → `feat(260531-3ml): eviction restore web UI (close G-1)`
- Task 3 → `docs(260531-3ml): document Phase 15 auth deploy + EnvironmentFile in the unit`
</commit_guidance>

<output>
After completion create `.planning/quick/260531-3ml-eviction-restore-web-ui-close-g-1-phase-/260531-3ml-SUMMARY.md`.
</output>
