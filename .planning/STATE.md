---
gsd_state_version: 1.0
milestone: v2.6
milestone_name: — Item Detail & Polish
status: in_progress
last_updated: "2026-06-25T20:00:00.000Z"
last_activity: 2026-06-25
progress:
  total_phases: 6
  completed_phases: 3
  total_plans: 7
  completed_plans: 7
  percent: 50
  note: "PHASE 40 (item display polish; ITEMUI-01/02) DISCUSS + UI-SPEC ✅ GATES DONE 2026-06-25 — 40-CONTEXT.md locks D-01..D-08 (PRIORITY flag outline No-Drop>Lore>Magic on the 62px tiles + the examine name heading, NOT dense list rows; named quest links CLICKABLE via stored quest_items.source_url through safeHttpUrl on DETAIL surfaces only [examine panel + Inventory/Wishlist tooltips], rows keep the yes/no badge; only source='notes_link' rows are real named quests, the in_game_flag/[in-game QUEST flag] pseudo-entry stays the generic badge fallback). 40-UI-SPEC.md APPROVED by gsd-ui-checker 6/6 + full CONTEXT compliance, 0 FLAGs — mandates 3 NEW per-theme flag tokens --flag-nodrop/--flag-lore/--flag-magic added to web/src/app.css + web/src/lib/theme/themes.ts IN LOCKSTEP (parity tests themes.test.ts/themeApply.test.ts must extend), WCAG ≥3:1 non-text contrast per theme (velious/heavy/minimalist lack a usable blue/gold token so reusing --status-* would break Lore/Magic on 3/5 themes); resolved discretion: ::before 2px inset box-shadow flag ring (survives the accent hover/focus border — D-03), a bordered uppercase flag CHIP beside the examine name (not full-heading recolor), comma-joined multi-quest. Pure plumbing: item_master.is_no_drop/is_lore/is_magic (P37 00016) + quest_items.source_url both PRE-EXIST → carry through compute→types→api.ts (the P39 is_clicky/has_haste precedent) + extend QuestLinksByItem SELECT with source_url + attach to the modern InventorySlot/ItemRollup builders (the view.go/bank.go pattern). NO migration; watcher untouched → no v* tag. PLAN-PHASE ✅ COMPLETE 2026-06-25 — pattern-mapper 12/12 analogs (backend flag plumbing = verbatim P39 is_clicky/has_haste copy; quest plumbing reuses the wired questLinksFor path, only source_url SELECT+field is new) → gsd-planner 2 plans (40-01 backend wave 1 [readviews.go QuestLinkRow.SourceURL + 3 item_master flag cols into the InventoryForChar/IconStats joins → compute InventorySlot/ItemRollup JSON + quest_links attach; NO migration] / 40-02 web wave 2 depends_on 40-01, autonomous:false browser-smoke [api.ts mirror + QuestLink.source_url; 3 lockstep flag tokens --flag-nodrop/lore/magic app.css+themes.ts + parity tests; pure priority resolver flags.ts No-Drop>Lore>Magic; PaperdollSlot ::before ring; examine flag-chip + clickable notes_link quest links via safeHttpUrl; composeNotes tooltip links]) → gsd-plan-checker ✅ VERIFICATION PASSED 0-BLOCKER (full ITEMUI-01/02 + D-01..D-08 + UI-SPEC fidelity + no-migration + STRIDE threat models; 2 advisory WARNINGs NON-blocking: 40-02 15-file scope justified [lockstep token edit must land together] + 40-01 flag-seed uses direct db.Exec fallback since seedItemMaster lacks flag params). Planning docs UNCOMMITTED (CONTEXT/UI-SPEC/PATTERNS/2 PLANs untracked + ROADMAP +4/-2; the planner self-caught+restored a ROADMAP clobber — git-diff RE-VERIFIED clean). ▶ NEXT: commit planning docs + /gsd-execute-phase 40 (then deploy backend+web + browser-smoke, the P39 pattern). ─────── v2.6 in progress (3/6 phases complete — 37+38+39 all DEPLOYED LIVE, prod schema v17). PHASE 39 ✅ COMPLETE + DEPLOYED + browser-smoke APPROVED 2026-06-26 (Faceted item search — Clicky/Haste facets + holdings↔catalog scope toggle; SEARCH-04/05/06). Full GSD cycle: UI-SPEC gate APPROVED (gsd-ui-checker 6/6) → RESEARCH (HIGH) → PATTERNS (13/13, extend-in-place) → 2 plans → plan-checker PASSED → executed → verifier 10/10 must-haves PASSED → code-review. Backend (39-01 a3b815d/2d36059/ecd7253): client holdings facet (is_clicky/has_haste on ItemRollup from item_master, SC-4, no catalog-crawl dep) + name-keyed item_master∪catalog_enrichment union on SearchCatalog (joined by lower(trim(name)) NEVER item_id; LEFT JOIN only when a facet is active) + ?clicky/?haste params (2-rune guard+LIMIT+V7 preserved); NO migration. Web (39-02 1f3227e/6202409/a83c2a4): pure facetItems() + api.ts mirror + FacetBar.svelte chips + Inventory .seg Holdings↔Catalog toggle (D-03 lens-not-reset) + catalog-row ExaminePanel/holders-by-name (D-04 held→holders/unheld→not-held-in-the-guild/NO pin) + wishlist add-form facets (D-01 catalog-only). ✅ DEPLOYED LIVE backend+web together (apex 200, JS text/javascript, API 401, schema v17, 0 restarts; R2 backup + .bak/.old rollback kept). ⚠ code-review found 1 BLOCKER BL-01: the catalog facet LEFT JOIN fanned out duplicate rows when a catalog name matched N same-name held item_master rows (item_master.name NOT unique — keyed by item_id) → duplicate Svelte {#each} keys (each_key_duplicate crash) + LIMIT 25 undercount → FIXED-FORWARD bda8289 (wrap the union in GROUP BY norm + MAX(flag); TestSearchCatalog_SameNameHeldNoFanout regression) + backend RE-DEPLOYED (binary swap, schema v17, 0 restarts). MD-01 (catalog debounce survives scope flip) / MD-02 (empty-prices price) / LO-01/02/03 (web UX+cleanup, advisory) → backlog 999.44. Watcher UNTOUCHED → no v* tag; NO migration (schema stays v17). catalog_enrichment unheld-facet coverage backfills on the next Sunday UTC wiki crawl (holdings + held-item facets work now). ⚠ BOTH the `state planned-phase` AND `phase complete` gsd-tools CLOBBERED this STATE frontmatter (overwrote progress with all-time counts + DELETED this note) — CAUGHT via git-diff + git-restored, manual deltas re-applied; the tools got ROADMAP right (Phase 39 [x], table 2/2 Complete) but phase-complete was a NO-OP on REQUIREMENTS so SEARCH-04/05/06 were marked [x] by hand. Prior: PHASE 37 ✅ COMPLETE (flags+effects backbone, migration 00016) + PHASE 38 ✅ COMPLETE (name-keyed catalog_enrichment, migration 00017; D-04 reversed id-keyed→name-keyed) — both deployed 2026-06-25 (schema v15→v17). NEXT: /gsd-plan-phase 40 (item display polish — flag outlines No-Drop red/Lore gold/Magic blue + named quest links on modern tabs; ITEMUI-01/02). Then 41 paper-doll compaction+photo (CHARUI-01/02, migration) → 42 wishlist compaction+sub-Velious tiers (WISHUI-01/02)."
---

> **v2.6 "Item Detail & Polish" — ROADMAP CREATED 2026-06-24 (Phases 37–42; 13/13 requirements mapped).** Source spec `Future Features.txt` (2026-06-24): 1 bug (missing item images), 4 tweaks (wishlist compaction + sub-Velious tiers · paper-doll compaction + per-character photo · named quest links on modern tabs · flag-coded item outlines), 1 feature (faceted search by item type — Clicky/Haste, holdings↔catalog toggle). Shared backbone = re-enabling the dropped stat-block parse (`wikiitem.go` D-8 guard) for flags + effects (Phase 37); full-catalog search/icons require widening enrichment beyond held items (Phase 38). Phase map: **37** ENRICH-12/13 (flags+effects backbone) → **38** ENRICH-14/15 (catalog-wide enrichment + icon coverage/diagnostic) → **39** SEARCH-04/05/06 (Clicky/Haste facets + holdings↔catalog toggle) → **40** ITEMUI-01/02 (flag outlines + named quest links) → **41** CHARUI-01/02 (paper-doll compaction + portrait photo) → **42** WISHUI-01/02 (wishlist compaction + sub-Velious tiers). Dependency chain 37→38→39, then 40·41·42 independent. Backend + web; watcher UNTOUCHED → no `v*` tag (consistent with v2.3/v2.4/v2.5). Research SKIPPED (extends well-understood subsystems; codebase-mapped 2026-06-24). Locked: paper-doll both compact+photo · named quests (no epic model) · Clicky+Haste facets · flags No-Drop red/Lore gold/Magic blue · sub-Velious tiers included. Artifacts written: PROJECT.md Current Milestone + REQUIREMENTS.md (ENRICH-12.. / SEARCH-04.. / ITEMUI-01.. / CHARUI-01.. / WISHUI-01.. + Traceability) + ROADMAP.md (Phases 37–42 appended; all prior history + the 999.x backlog preserved). **Phase 37 ready to plan → `/gsd-plan-phase 37`.**
>
> _Prior milestone:_ **v2.5 "Ownership Cleanup" — SHIPPED + ARCHIVED + CLOSED 2026-06-22 (Phases 35–36; OWN-01..04; prod schema v15).** Promoted backlog 999.35 (owner-less / eviction-safe guild banks & bots → Phase 35: OWN-01/02/04) + 999.36 (shared-character-safe eviction → Phase 36: OWN-03), both deferred from quick `260621-u6j`. Backend + web; migration 00015 (Phase 35); watcher untouched → NO `v*` tag. Phase 35 (sentinel-owner model, schema v15) + Phase 36 (narrowed eviction cascade, no migration) both deployed live + officer browser-smoke approved. Archives `milestones/v2.5-*`; REQUIREMENTS.md removed at close. Backlog follow-ups all cleared via quick `260624-mdp` (999.37/38/39) + already-resolved 999.40.
>
> _Prior milestone:_ **v2.4 "Web UI Revamp — 5-Tab Restructure" — SHIPPED 2026-06-21 (6 phases, 29–34; 27/27 requirements; schema v14).** Reorganized squirebot.quest around five tabs — **Characters · Inventory · Banks · Wishlist · Settings**. Spans backend/data architecture + web; reworked the wantlist into a per-character per-slot wishlist; watcher untouched. **Consolidated-views lock RELAXED** — per-character master-detail drill-down now allowed (CLAUDE.md updated). Migrations 00012 item_icon / 00013 item_statsblock / 00014 wishlist. Milestone audit PASSED.
>
> _Prior:_ **v2.3 SHIPPED + ARCHIVED 2026-06-09** (P26→28 live, schema v10, audit PASSED 14/14; no git tag — watcher unchanged; archived `.planning/milestones/v2.3-*`). **v2.2 Track 2** (Discord pinger WTS/raid, P22-23) still parked on the Raid Alliance invites — independent.

# State: SquireBot

**Initialized:** 2026-04-30
**Last updated:** 2026-06-25 (Phase 40 ✅ PLANNED — discuss (D-01..D-08) + UI-SPEC (gsd-ui-checker 6/6) + plan-phase all done; 2 plans 40-01 backend/40-02 web, gsd-plan-checker VERIFICATION PASSED 0-blocker, 2 advisory WARNINGs non-blocking; NO migration, watcher untouched. Planning docs uncommitted. Prior: ✅ Phase 39 COMPLETE + DEPLOYED, SEARCH-04/05/06, schema v17, BL-01 fixed-forward bda8289. ▶ NEXT: commit planning docs + /gsd-execute-phase 40)

## Project Reference

See: `.planning/PROJECT.md` (updated 2026-06-24 with the v2.6 Current Milestone section)

- **Core value:** Every guildie can answer "what does my character still need, and where in the guild is it?" — delivered via the self-hosted website (squirebot.quest).
- **Current focus:** **Milestone v2.6 "Item Detail & Polish" — IN PROGRESS (3/6 phases complete: 37+38+39 deployed; Phases 37–42; 13/13 requirements mapped).** Turn the half-captured item data into usable intelligence — re-enable the dropped flag/effect parse into discrete columns (P37), widen enrichment to the full PigParse catalog + backfill icons (P38), Clicky/Haste faceted search with a holdings↔catalog scope toggle (P39), color-coded flag outlines + named quest links on the modern tabs (P40), paper-doll compaction + optional per-character photo (P41), wishlist compaction + sub-Velious gear tiers (P42). Backend (`internal/backendsrv`) + web (`web/`); watcher untouched → no `v*` tag. **Next: `/gsd-execute-phase 40` (P40 fully PLANNED 2026-06-25 — discuss D-01..D-08 + UI-SPEC 6/6 + 2 plans, plan-checker PASSED 0-blocker; planning docs uncommitted).** Prod is live at squirebot.quest, schema v17 (37+38 migrations 00016/00017 deployed; P39 no migration). Deploy via Windows ssh-agent service + PowerShell ssh.exe/scp.exe to root@5.78.232.85 (see resume-point memory). v2.2 Track-2 (Discord pinger WTS/raid) still parked on the Raid Alliance invites.
- **Mode:** yolo
- **Granularity:** coarse

## Current Position

Phase: **38 — Catalog-wide enrichment + icon coverage — ✅ COMPLETE + DEPLOYED LIVE 2026-06-25 (name-keyed; D-04 reversed id-keyed → name-keyed; prod schema v15→v17)** (milestone v2.6 — Item Detail & Polish; 2/6 phases complete). Phase 37 ✅ COMPLETE + PUSHED (origin/master through `85c3f03`; migration 00016 NOT deployed). **Phase 38's id-keyed Option A was REPLACED** by the name-keyed re-implementation: migration 00017 `catalog_enrichment(norm_name PK,…)` + `store/catalogenrich.go` + `itemids.go` rework (guard dropped, `EnrichmentRef.Held`, both-stores `CatalogIconCoverage`) + the `wiki.go` `ref.Held` write-branch. Covers all 4,343 catalog items with NO drop; `item_master` + the 5 held readers byte-for-byte unchanged (blast radius ZERO). Verifier 4/4 SC + 2/2 reqs (human_needed = post-deploy seed crawl/browser render only); code-review 0 BLOCKER (WR-01/WR-02 name-keying bug + IN-01 fixed-forward `9735f59`/`c7dae81`; IN-02/IN-03 → 999.43; IN-04 no-action); full `go test ./...` GREEN. Commits `722edbc`→`0e64332` pushed (origin). **✅ DEPLOYED LIVE 2026-06-25** — backend-only binary swap (Windows ssh-agent SERVICE + PowerShell ssh.exe/scp.exe to root@5.78.232.85; R2 backup first; `.bak` kept) → goose applied **00016 + 00017** on boot → prod schema **v15 → v17**; service active, 0 restarts; apex 200 / `/api/v1/*` 401; `catalog_enrichment` seeded EMPTY (populates on the next Sunday UTC wiki crawl); P37 flag backfill ran live (16/953 item_master rows flagged). **Next: `/gsd-plan-phase 39`.** **Phase 39 (Faceted item search; SEARCH-04/05/06) is CONTEXT-GATHERED, now UNGATED** (the 37+38 deploy it depended on is live) — its catalog-scope facets read the name-keyed enrichment this phase produced (meaningful after the Sunday seed crawl; holdings facets work immediately); the catalog↔enrichment facet join is now name-keyed end-to-end (the namespace-bridge hazard 39-CONTEXT flagged is dissolved).

**✅ Phase 38 RE-OPENED + RE-PLAN EXECUTED 2026-06-25 — D-04 storage REVERSED id-keyed Option A → name-keyed (the WHY, retained as history).** Option A shipped first (code-complete, verifier 5/5, code-review 0-blocker; commits `59e7180`/`ac85211`/`d95e7de`/`6d6588f`, on origin/master; NEVER deployed) — it admitted catalog-only rows into `item_master` keyed by their PigParse id behind an `item_id NOT IN (SELECT item_id FROM item_master)` collision guard, on research Assumption A2 that PigParse↔EQ id collisions were "≈0". **The pre-deploy prod probe disproved A2:** live DB `item_master`=953 / `pigparse_price`=4,343 → **60 raw id-collisions / 43 genuinely-unheld catalog items the guard silently DROPS** (e.g. *Cured Silk Gi*, *Ancient Tarnished Breastplate*, *Etched Velium Brawl Stick*) — ~1% of catalog, **no correctness risk** (the guard correctly protects the held rows it collides with) but a permanent coverage hole the icon-less residue can never close. The plan's STOP-at-~20 gate fired; surfaced to the user, who chose **HOLD the deploy + re-plan name-keyed** over shipping the gap. **Binding constraint that was implemented (`38-CONTEXT.md` ⚠ D-04 REVERSED block + `38-DISCUSSION-LOG.md`):** Option B — a separate name-keyed `catalog_enrichment(norm_name TEXT PRIMARY KEY, …)` table joined by normalized name; `item_master` stays **held-only by EQ id** (held-reader blast radius re-verified ZERO); migration **00017**; Phase-39 catalog scope = held(`item_master` by id) ∪ unheld(`catalog_enrichment` by name) COALESCE'd by name; covers **all 4,343, no drop**; the `slog` coverage diagnostic re-points to the name-keyed store. **DONE:** the Option-A code was REPLACED (re-plan + 2-wave execution, verifier 4/4+2/2, code-review 0-BLOCKER with WR-01/WR-02 fixed-forward). ⚠ ROADMAP/STATE git-diffs were re-verified clean after BOTH the re-plan and the execution (the planner had clobbered ROADMAP.md during the FIRST P38 planning — [[feedback_planner_clobbers_uncommitted_docs]]). **The 37+38 deploy (migration 00016 + 00017) is the next step.**

**Phase 39 locked context (2026-06-25):** **D-01** the Clicky/Haste facets + the holdings↔catalog scope toggle live on the **Inventory tab** (`/inventory`, `compute.Items` — the toggle flips its data source between guild holdings and the full `pigparse_price` catalog), AND the facets are ALSO added to the **wishlist add-item catalog search** (catalog-only there — no scope toggle; facets just narrow `SearchCatalog`). **D-02** two independent toggles, **AND-combined** (both on = items that are both clicky and haste; none = all). **D-03** default **Holdings** ("who has one"); flipping the scope toggle **persists** the typed query + active facets (a lens, not a reset). **D-04** a catalog-scope row reuses the **ExaminePanel** (name/flags/effects/price/wiki/last-listed); a held item still shows its **holders** in catalog scope (catalog = superset of holdings), an unheld item reads "not held in the guild"; **NO new pin-to-wishlist action** (deferred). **Likely impl split (Claude's discretion):** holdings facet = client-side over the already-loaded Inventory rollup (add `is_clicky`/`has_haste` to the items payload from `item_master` — keeps SC-4: holdings faceting independent of the catalog crawl); catalog facet = server-side `clicky`/`haste` params on `SearchCatalog`. **Load-bearing research flag:** the catalog↔`item_master` facet join must be by **normalized name** (NOT raw item_id — the `[[pigparse-vs-ingame-item-id-namespaces]]` namespace split; reuse the P29 name bridge / the P38 Option-A keying). Watcher untouched, no `v*` tag. **Deploy note:** catalog-scope facet results only become meaningful in prod after P38's seed crawl populates `is_clicky`/`has_haste` across the catalog (the first weekly wiki run after the P37+P38 deploy); holdings facets work immediately.

**[SUPERSEDED 2026-06-25 — describes the id-keyed Option A build, now being REPLACED by the name-keyed re-plan; see the ⚠ Phase 38 RE-OPENED block above. Retained as history of the first build.]** **Phase 38 ✅ COMPLETE + VERIFIED 2026-06-25 (catalog-wide enrichment + icon coverage; ENRICH-14/15):** executed 1 plan / 2 tasks; verifier PASSED 5/5 goal-backward against real code; regression gate green (full module, 30 pkgs); code-review 0 BLOCKER / 0 HIGH. **D-04 → Option A** (the load-bearing decision): `store.DistinctEnrichmentRefs` (`store/itemids.go`) unions held EQ ids ∪ the full `pigparse_price` catalog, deduped by `lower(trim(name))` (held wins), with TWO guards — held-name exclusion + `item_id NOT IN (SELECT item_id FROM item_master)` (the `ON CONFLICT(item_id)` collision guard so a PigParse id numerically equal to a different item's EQ id can't overwrite it); `runWikiItems` (`enrich/jobs/wiki.go`) swapped to iterate it; ENRICH-15 icons ride the unchanged `GetItemMasterFreshnessTx` short-circuit (NO `BackfillItemIcon` boot pass); the D-03 coverage `slog` line reads TRUE `item_master` state (`store.ItemMasterIconCoverage`, post-MD-01 fix) so the icon-less residue stays visible every weekly run. **Code-review fix-forward (commit `6d6588f`):** MD-01 (steady-state coverage diagnostic) + MD-02 (docstring honesty + held-vs-held test) + LO-01 (double-count) + LO-02 (id-keying assertion) all fixed; NIT-01 dissolved; NIT-02 → backlog 999.42. **NO new migration** (00016 stays last; `item_master` already carries icon[00012]/statsblock[00013]/flags[00016]); watcher untouched; no `v*` tag. Commits `59e7180`/`ac85211`/`d95e7de` + `6d6588f` — **on master, NOT pushed**; **NOT deployed** (rides the next backend deploy alongside P37's 00016; the read-only D-04 prod collision probe [expected ≈0; the exclusion is the safe default regardless] is a pre-deploy checklist item). ⚠ The gsd-planner clobbered ROADMAP.md (gutted 584→2 lines) during P38 planning — CAUGHT via post-run git-diff + `git restore`d (the `[[feedback_planner_clobbers_uncommitted_docs]]` failure mode, recurred 2026-06-25).

**Phase 38 locked context (2026-06-25):** widen enrichment from held-only (`store.DistinctInventoryItemIDs`) to the full PigParse Blue catalog + backfill wiki icons (ENRICH-14/15). **D-01:** seed the full ~4,341 catalog once (one ~70-min paced Sunday run), then the weekly job re-validates the WHOLE catalog via ETag (cheap 304s on unchanged) — justified by `wiki.go`'s "no execution cap" design; runs automatically in the existing weekly job, no new manual trigger. **D-02:** "full catalog" = `pigparse_price` auction catalog ONLY (~4,341 tradeable items); do NOT union never-auctioned no-drop/raid `wiki_gear_tier` items. **D-03:** the ENRICH-15 maintainer coverage diagnostic = a per-run structured `slog` summary line (total/enriched/icon-covered/icon-less + residue names), greppable on the VPS — no new UI surface. **D-04 (DELEGATED to research+planning):** the load-bearing storage/identity risk — `item_master` is keyed by EQ-inventory `item_id` but the PigParse catalog uses a DIFFERENT id namespace (join by normalized name only), so an unheld catalog item has no EQ id to key an `item_master` row by; researcher/planner must resolve the storage model (name-keyed store vs. synthetic id vs. separate table) + the name-dedup rule so SEARCH-06's full-catalog scope shows each item once. Migration footprint TBD (→ 00017 if needed, possibly none). Backend; watcher untouched, no `v*` tag.

**v2.6 milestone opened + roadmapped 2026-06-24.** 13 requirements (ENRICH-12..15, SEARCH-04..06, ITEMUI-01..02, CHARUI-01..02, WISHUI-01..02) mapped 1:1 across Phases 37–42 (see the v2.6 Phase Plan below). The shared backbone is Phase 37 — re-enabling the wiki stat-block parse that `internal/backendsrv/enrich/wikiitem.go` already COMPUTES but the D-8 scope guard DISCARDS (only `is_quest_item` survives today; the full stat block is one TEXT blob in `item_master.statsblock`, migration 00013). P37 adds discrete flag/effect columns (new migration + the `GetItemMasterFreshnessTx` short-circuit updated so existing rows backfill); P38 widens the inventory-driven enrichment gate (`store.DistinctInventoryItemIDs`) to the full ~4,300-row `pigparse_price` Blue catalog (the biggest/riskiest piece — also fixes most missing icons). Dependency chain **37 → 38 → 39**, then **40 · 41 · 42** independent. Held-items faceting (SEARCH-04/05) is sequenced to ship even if the ENRICH-14 catalog crawl runs long. Backend + web; watcher untouched, no `v*` tag.

_The deep per-plan execution narrative below (Phase 36 / 35 / the v2.4 34-xx..31-xx history) is retained as accumulated context — it is HISTORICAL; v2.5 + v2.4 are SHIPPED + archived. v2.6 has no execution history yet._

**Prior milestone close (v2.5, 2026-06-22):**
**36-02 EXECUTED 2026-06-22 (CODE-COMPLETE, then DEPLOYED LIVE)** (commits `b1aab94` + `8c77086`, web-only, all on master): the web half of OWN-03 / the D-06 BLOCKER fix — an ALL-SHARED owner (every live char shared → `characters:[]`) stays evictable so the officer can still revoke their guild code (code-only revoke). (1) mirrored the 36-01 `preserved_shared_count: number` field field-for-field (snake_case) into `web/src/lib/api.ts`'s `EvictionPreview`, + added two PURE node-tested helpers to `web/src/lib/eviction.ts`: `canEvictPreview(preview)` (true iff `characters.length>0 || preserved_shared_count>0` — cascade OR all-shared code-only revoke; false ONLY for a genuine zero-live-chars owner) + `evictPreviewSummary(preview)` (a discriminated `cascade`/`code-only`/`empty` render shape; the `code-only` branch carries the "0 characters removed; {N} shared character(s) preserved; guild code will be revoked." framing) — 6 new node test cases. (2) wired `EvictionForm.svelte` to the helpers: gated `canEvict` on `canEvictPreview(preview)`, switched the preview render block on `evictPreviewSummary(preview).kind` (the all-shared button stays ENABLED, the zero-live button stays DISABLED). NO new `{@html}` sink. Web gates GREEN (`npm run check` 0/0, `npm test` 369 passed, build ok). **DEPLOYED LIVE + officer browser-smoke APPROVED 2026-06-22** (backend binary swap + web atomic-swap, no goose; schema stays v15). See `36-02-SUMMARY.md`.

_Prior (36-01):_
**36-01 EXECUTED 2026-06-22** (commits `652f6d2` + `6969080` + `a1d0861`, backend-only, all on master): the backend half of OWN-03 — narrow the per-owner eviction cascade so evicting a guildie removes ONLY their OWN chars; a SHARED char another guildie uploads survives. **Sharing detection is computed on read from the existing `audit_log` `cross_owner_write` trail (D-01) — NO new table, NO migration (schema stays v15).** Phase 36 is the FIRST reader of `cross_owner_write`. `sharedCharPredicate` package const (the SINGLE source of truth) + narrowed `EvictOwnerTx` cascade (shared chars stay `is_removed=0`); `PreviewEviction` narrowed with the SAME const (preview-set == cascade remove-set — the Phase-35 CR-01 parity lesson) + `CountPreservedShared` + `EvictionPreviewHandler` emits the ADDITIVE snake_case `preserved_shared_count int`; `recentOtherSharerSubquery` second named const (drift-locked surviving-shared repoint). 6 new tests + Phase-35 sentinel tests stay green. Gates GREEN (build/vet/full backendsrv suite). NO migration; watcher + web untouched; NO `v*` tag. See `36-01-SUMMARY.md`.

_Prior (Phase 35):_
**35-01 EXECUTED 2026-06-22** (commits `7a238b8` + `c8305c2` + `4d38389` + fixes `11ebcc6`/`62acc78`/`96ea4ad`, backend-only): Option A = reserved "guild" sentinel owner (id `1000000`, label `guild`). Migration `00015_guild_owner.sql` (schema v15) — `INSERT OR IGNORE` seed + idempotent backfill repointing every `is_bank_toon=1 OR is_guild_bot=1` char to the sentinel (OWN-04, Findom auto-migrates); `store/owner.go` `GuildSentinelOwnerID` + `DesignateCharTx` owner_id repoint (OWN-01/02); `ListEvictableOwners`/`ListRestorableOwners` sentinel exclusion + the OWN-02 survives-eviction proof. **Code-review CR-01 BLOCKER fixed-forward** (`11ebcc6`): the sentinel was guarded in the picker reads but NOT the destructive `EvictOwnerTx`/`RestoreOwnerTx` write path — added `ErrCannotEvictSentinel` + a top-of-function guard mapped to HTTP 403. Verifier PASSED 6/6 + 3/3 req IDs; full backendsrv suite green. See `35-01-SUMMARY.md`.

## v2.6 Phase Plan (created 2026-06-24)

Phases continue from v2.5 (which ran 35–36). Phase dirs 01–36 exist on disk — never reuse them. v2.6 starts at **37**.

**Execution order:** dependency chain **37 → 38 → 39**, then **40 · 41 · 42** (independent of each other once 37/38 land). Phase 37 (the parse backbone) gates Phase 39's facets + Phase 40's flag outlines; Phase 38 (catalog-wide enrichment) gates Phase 39's full-catalog scope. The held-items faceting (SEARCH-04/05) is sequenced so it ships even if the ENRICH-14 catalog crawl runs long.

| Phase | Name | Requirements | Success Criteria | UI |
|-------|------|--------------|------------------|----|
| 37 | Item enrichment backbone — flags + effects | ENRICH-12, ENRICH-13 | 4 | no |
| 38 | Catalog-wide enrichment + icon coverage | ENRICH-14, ENRICH-15 | 4 | no |
| 39 | Faceted item search (Clicky / Haste + scope toggle) | SEARCH-04, SEARCH-05, SEARCH-06 | 4 | yes |
| 40 | Item display polish — flag outlines + named quest links | ITEMUI-01, ITEMUI-02 | 4 | yes |
| 41 | Character paper-doll — compaction + portrait photo | CHARUI-01, CHARUI-02 | 4 | yes |
| 42 | Wishlist polish — compaction + sub-Velious tiers | WISHUI-01, WISHUI-02 | 4 | yes |

**Phase 37 — Item enrichment backbone (flags + effects), FOUNDATIONAL:** re-enable the wiki stat-block parse the Go port discarded (the `wikiitem.go` D-8 scope guard already computes `is_no_drop/is_lore/is_magic/is_temporary` + click-effect/haste, but only `is_quest_item` is kept). Add discrete flag/effect columns via a goose migration + update the enrich freshness short-circuit (`GetItemMasterFreshnessTx` in `store/enrich.go`) so existing rows re-parse + backfill on the next weekly wiki pass. ENRICH-12 (LORE/NO-DROP/MAGIC/TEMPORARY flags) + ENRICH-13 (Clicky + Haste). Gates P39 facets + P40 outlines. Backend; watcher untouched.

**Phase 38 — Catalog-wide enrichment + icon coverage (the biggest/riskiest piece):** widen enrichment beyond held items — today the weekly wiki job iterates `store.DistinctInventoryItemIDs` (`store/itemids.go`), so only items some character HOLDS get a wiki page. ENRICH-14 widens the source set to the full ~4,300-row `pigparse_price` Blue catalog, politefetch-paced. This also fixes most missing icons (`item_master.icon_id` from the wiki `lucy_img_ID`, migration 00012; missing when the page lacks the param OR the item was never enriched). ENRICH-15 = icon backfill + a maintainer-visible coverage diagnostic; the colored-tile fallback (`PaperdollSlot.svelte`) stays for genuinely icon-less items. Backend; watcher untouched.

**Phase 39 — Faceted item search (Clicky / Haste + scope toggle):** search today is `GET /api/v1/items/search` → `store.SearchCatalog` over `pigparse_price` (name/id LIKE only, no facets). SEARCH-04/05 add the Clicky/Haste facets (read P37's parsed effects); SEARCH-06 adds the holdings↔catalog scope toggle (full-catalog facets need P38's coverage). Facets = Clicky + Haste ONLY (locked — not slot/stat/weapon-type). Depends on P37 + P38. Backend + web.

**Phase 40 — Item display polish (flag outlines + named quest links):** ITEMUI-01 = color-coded flag outlines (No-Drop red / Lore gold / Magic blue) on the inventory/bank/paper-doll tiles (`PaperdollSlot.svelte` + list rows; depends on P37's discrete flags). ITEMUI-02 = surface named "used in quest X" links on the modern Characters/Inventory/Wishlist tabs — `quest_items` + `store.QuestLinksByItem` + named `quest_links` already exist (on the LEGACY `guild-views` tab), but the modern `InventorySlot`/`ItemRollup` models carry only a yes/no `is_quest_item` bool; plumb the existing links into those models + the examine panel (compute + `web/src/lib/api.ts` + `examine.ts`). ITEMUI-02 is independent of P37. Backend + web.

**Phase 41 — Character paper-doll (compaction + portrait photo):** `InventoryWindow.svelte` has a ~260px dashed placeholder `.doll` box (a `⚔` emoji) reserved for a portrait; NO per-character image support exists. CHARUI-01 = compact the layout toward the in-game inventory-window idiom (web-only). CHARUI-02 = optional per-character portrait photo — a new image column/table + migration + an upload path + read-API plumbing (the new persisted-data piece of v2.6 alongside P37's flags). Backend + web.

**Phase 42 — Wishlist polish (compaction + sub-Velious tiers):** WISHUI-01 = compact the bespoke wishlist UI (`web/src/routes/wishlist/+page.svelte`) — it already uses the same two-pane pattern as the other tabs, so this is density/visual-consistency work, not an architecture swap. WISHUI-02 = add sub-Velious (Kunark/classic) gear tiers — `wiki_gear_tier` exists but is populated ONLY from two Velious wiki pages (`wikiVeliousGearSources`); add Kunark/classic source pages + tier values (no migration — the table exists). Backend (new wiki source pages) + web (compaction).

## Milestone v2.6 Scope (locked 2026-06-24)

**Goal:** Turn the item data SquireBot already half-captures into usable intelligence — faceted item search (Clicky / Haste), flag-coded outlines, named quest links on the modern tabs, broader icon/tier coverage — and tighten the Characters (paper-doll), Wishlist, and item-display surfaces.

**Locked decisions:**

- **Paper-doll = BOTH** — compact layout (CHARUI-01) AND optional per-character portrait photo upload (CHARUI-02; new image field + migration + upload path).
- **Quest flag = named quests on ALL modern tabs** — plumb the already-harvested `quest_links` into `InventorySlot`/`ItemRollup`; NO epic-specific model (deferred).
- **Search facets = Clicky + Haste ONLY** — not slot / stat / weapon-type; scope toggles holdings ↔ full catalog.
- **Flag → color** — No-Drop red, Lore gold, Magic blue, Temporary gray.
- **Sub-Velious gear tiers INCLUDED** — new Kunark/classic wiki source pages + tier values; NO migration (`wiki_gear_tier` already exists).
- **Sequencing risk** — the full-catalog search toggle (SEARCH-06) requires widening enrichment to all ~4,300 catalog wiki pages (today gated to held items via `DistinctInventoryItemIDs`) — this also fixes most missing icons. Sequence the held-items faceting (SEARCH-04/05) to ship first so it lands even if the catalog crawl (ENRICH-14) runs long.

**Scope:** backend (`internal/backendsrv`) + web (`web/`). The Go **watcher is UNTOUCHED** → **no `v*` tag** (consistent with v2.3/v2.4/v2.5). New goose migrations expected for P37 (discrete flag/effect columns) + P41 (the per-character image reference); P38 widens an existing job (data, no migration); P39/P40/P42 are compute/read/web over existing + the new P37 data (P42 reuses the existing `wiki_gear_tier` table). Resolve the exact migration footprint in per-phase planning.

**Research:** SKIPPED — v2.6 extends well-understood subsystems (the wiki enrichment job, the catalog search, the inventory/examine models, the wishlist UI). Codebase mapped 2026-06-24 (data-availability per requirement confirmed); per-phase CONTEXT/RESEARCH as needed at plan time.

## v2.4 Phase Plan (created 2026-06-17)

Phases continue from v2.3 (which ran 26–28). Phase dirs 01–28 exist on disk — never reuse them. v2.4 starts at **29**.

**Execution order:** strict dependency chain **29 → 30 → 31 → 32 → 33 → 34**. The data foundation (P29) is built FIRST because the Characters inventory window (P31), the Inventory tab (P32), the Banks tab (P33), and the Wishlist (P34) all read the parsed slot taxonomy + name-keyed price/value data. The app shell + 5-tab nav (P30) reframes routing for every tab surface and lands second. The Wishlist (P34) additionally depends on P31's equipped-slot detection + the already-SHIPPED v2.2 EC-monitor + notification spine (reused, not rebuilt). P32 and P33 are independent of each other once 29+30 land.

| Phase | Name | Requirements | Success Criteria | UI |
|-------|------|--------------|------------------|----|
| 29 | Data Foundation — Inventory Parse + Price/Value Aggregation | INV-05, DATA-01, DATA-02 | 4 | no |
| 30 | App Shell + 5-Tab Navigation | NAV-01, NAV-02, NAV-03, NAV-04 | 4 | yes |
| 31 | Characters Tab + In-Game Inventory Window | CHAR-01, CHAR-02, CHAR-03, INV-01, INV-02, INV-03, INV-04 | 4 | yes |
| 32 | Inventory Tab (Item-Centric) | ITEM-01, ITEM-02, ITEM-03 | 3 | yes |
| 33 | Banks Tab + Valuation | BANK-01, BANK-02, BANK-03 | 3 | yes |
| 34 | Wishlist Rework — Per-Character Per-Slot Upgrades | WISH-01..07 | 5 | yes |

**Phase 29 — Data Foundation (backend, no new user surface):** parse the watcher's `Location`/`Slots` inventory columns server-side into a slot taxonomy (EQ paperdoll equipment slots + general inventory + bank) with container (bag) nesting + preserved stack counts (INV-05); join PigParse price + last-listed-for-sale by **normalized name** so gear-tier/wiki rows with NO item_id still resolve a price (DATA-01); aggregate guild-bank item value (summed PigParse) + total platinum from the manual bank-coin entries (DATA-02). Extend-only schema; **watcher untouched**. Unblocks 31/32/33/34.

**Phase 30 — App Shell + 5-Tab Navigation (web, reframes all routing):** the five persistent top tabs (Characters · Inventory · Banks · Wishlist · Settings) with per-tab in-context search (NAV-01/02); Settings consolidates Theme/Notifications/Watcher Codes/Set Class & Level/My Characters/Admin (NAV-03); notifications + unread badge move onto the Wishlist tab (NAV-04). Can land with placeholder tab bodies ahead of the content phases. Existing 5 EQ themes reused unchanged.

**Phase 31 — Characters Tab + In-Game Inventory Window:** the guild character list (viewer's first A-Z, then others, then banks/bots) + viewer-prioritized search (CHAR-01/02); selecting a character opens an in-game-style paperdoll inventory window — equipment slots, general inventory with openable bags (drill-down), bank below, stack counts, real P1999 wiki item icons, click-to-pin right-click examine (stats/price/wiki/last-synced) (CHAR-03, INV-01..04). Reads P29.

**Phase 32 — Inventory Tab (item-centric):** guild-wide item list (name, guild-wide quantity, wiki + PigParse links) with viewer-prioritized search (ITEM-01/02); selecting an item shows holders + slot-on-each + quantity + last-synced as a master-detail drill-down (ITEM-03). Reads P29.

**Phase 33 — Banks Tab + Valuation:** banks-only list (same ordering style), each opening its inventory window (reuses the P31 window) (BANK-01); total PigParse item value across banks + total platinum (DATA-02 aggregation) (BANK-02); per-item bank search (BANK-03).

**Phase 34 — Wishlist Rework (the heaviest single feature):** reworks the v2.2/v2.3 wantlist into a per-character, per-equipment-slot, OPEN-ENDED upgrade list (WISH-01/02/03); per-slot suggestions from the COMPLETE Velious Pre-raid/Grouping + Raiding `_wiki_gear_tier` lists for that class+slot, each with PigParse price + wiki link + last-listed-for-sale, no-drop/raid-only tagged "Raid"/not-for-sale (WISH-04); Discord ping toggle + EC-hit badge reusing the SHIPPED EC-monitor + notification spine (WISH-05); right-click examine + wishlist search (WISH-06/07). Depends on P31 equipped-slot detection + P29 price data + the v2.2 notification/EC spine.

## Milestone v2.4 Scope (locked 2026-06-17)

**Goal:** Reorganize squirebot.quest around five top-level tabs — Characters · Inventory · Banks · Wishlist · Settings — each answering one user question, backed by the new data architecture this requires.

**Locked decisions:**

- **Consolidated-views lock RELAXED** — per-character master-detail drill-down allowed (CLAUDE.md updated); guild-wide consolidated grids remain for the item-centric Inventory tab.
- **Data foundation first** — INV-05 inventory parse + DATA-01/02 joins/aggregation precede every web surface that consumes them.
- **Watcher UNTOUCHED** — it already uploads `Location | Name | ID | Count | Slots`; all new work is backend parsing + web.
- **Gear-tier/wiki rows have NO item_id** — PigParse price + last-listed join by **normalized name** (DATA-01).
- **Pings reuse the SHIPPED spine** — WISH-05 rides the live v2.2 EC-monitor + notification/`alert_log` infrastructure; nothing rebuilt.
- **No re-skin** — the existing 5 EQ themes are reused unchanged (out of scope).
- **v2.2 Track 2 (WTS/raid monitors) stays PARKED** — out of scope for v2.4; independent, invite-gated.

**Scope:** backend (`internal/backendsrv`) + web (`web/`). Likely a new extend-only goose migration for the per-slot wishlist model (next after `00011`); the inventory-parse/valuation work may be compute-only over existing tables. Resolve the migration footprint in per-phase planning.

---

## Prior milestone history (v2.3 — archived; retained as accumulated context)

Phase: 28 — Character-Tagged Wantlist (SHIPPED 2026-06-09 — deployed live v10 + browser-smoke PASS; FINAL v2.3 phase)
BROWSER-SMOKE UAT 2026-06-09: **PASS** — confirmed on prod: add a want tagged to your character (appears in My, grouped by char), account-level (no-character) wants stay valid, the Character group-by filter works, the Guild toggle shows the all-members roll-up with Owner + Character attribution and NO note, the char filter resets on view switch. #6 (live EC embed names the char) NOT exercised — no tagged item hit the tunnel during the window; organic/coverage-dependent (same as P21 live-DM), the embed path is unit-tested + the EC send-path proven live in P20/P21, so it confirms on the first real tagged match. Phase 28 = SHIPPED + UAT-verified. CWANT-01..06 delivered.
Execution: 3/3 plans complete (9 task commits across 3 waves, all on master, all gates green). 28-01 (wave1, fb9a1f6/c49863a/00cf8c7): migration 00010 → schema v10 (nullable character_id ADD COLUMN auto-NULL backfill; both dedup indexes rewritten COALESCE(character_id,-1) — account-level dedup preserved + same-item-two-chars distinct, both tested in TestMigrate_00010; extend-only, 00001-09 untouched) + AddWantTx/ListOwnWants+char + NEW ListGuildWants (joins web_user owner username + LEFT JOIN character, EXCLUDES note) + IsCharAssignedToTx IDOR guard + ErrCharNotAssigned. 28-02 (wave2, 5fa2838/3a5bc5f/1570bda + e96c5d0 summary): AddWantHandler calls IsCharAssignedToTx in-tx before insert → ErrCharNotAssigned 403 (TestAddWant_TaggedUnassignedChar_403 PASS) + NEW ListGuildWantsHandler + GET /api/v1/wantlist/guild (RequireSession, login-gated) + wantmatch.Hit.CharacterName via LEFT JOIN (Hit.DiscordUserID DM target STRUCTURALLY UNCHANGED — TestForItem_DMTargetIsWantOwner_NotCharacterOwner PASS; grep-confirmed no owner_id/character_assignment in DM path) + EC embed display-only "For <char>" field. 28-03 (wave3, e573730/2b987a9/74b7031): api.ts char fields + GuildWantRow(no note) + fetchGuildWants; NEW pure web/src/lib/wantlist/groupByChar.ts + node test (5 cases); WantAddForm char <select> (options from fetchMyCharacters, "(no character)"→null); WantlistPanel My/Guild segmented toggle (Guild lazy-loads roll-up) + group-by-char; guildWantlistColumns (Owner·Character·Priority·Item·Reason, NO Note); no {@html}. Gates: go build/vet/test green (migrations/store/webadmin/wantmatch/ec all ok); web check 0/0 (486 files), npm test 303 passed (incl 5 new groupByChar), build ok. Verifier=human_needed (4/4 success criteria + 6/6 CWANT code-verified; 28-VERIFICATION.md). Fixed stale REQUIREMENTS.md CWANT-02 wording 00009/v9→00010/v10. CODE REVIEW: `/gsd-code-review 28` ran — 28-REVIEW.md, 0 BLOCKER/0 HIGH/2 MEDIUM/3 LOW/2 NIT; the 4 load-bearing controls (COALESCE dedup, IDOR guard, DM-target invariant, guildwide note-privacy) all held under adversarial review. FIXED in be795d5: MD-02 (ListGuildWants ORDER BY add `, w.id DESC` tie-break), MD-01 ("Removed character" label vs phantom #<id>), LOW-02 (reset char filter on My/Guild view switch); gates re-green (go build+store/webadmin test, web check 0/0, test 303, build). DEFERRED to 28-REVIEW.md record: LOW-01 (generic 403 toast), LOW-03 (audit "character_id":null), 2 NITs. **DEPLOYED LIVE v9→v10 2026-06-09 (backend binary + migration, NOT web-only):** R2 backup (squirebot-2026-06-09.db.gz) → cross-compile linux/amd64 CGO_ENABLED=0 (12.9MB) → scp → cp .bak + install 0755 → systemctl restart → **goose.Up applied `OK 00010_character_tagged_wantlist.sql` → schema v10** (verified: goose version 10, wantlist_item.character_id present, bot reconnected guild 1483502186351562925, scheduler 4 jobs, service active) → web tarball+atomic-swap (squirebot.old kept). External verify GREEN: apex 200, entry JS text/javascript (fresh start.5kr44XMn.js), **NEW GET /api/v1/wantlist/guild → 401 (registered+login-gated, not 404)**, assignments/mine 401 (healthy). Rollback: `cp /usr/local/bin/squirebot-server.bak → restart` (old binary runs against v10 — extend-only safe) + `mv /var/www/squirebot.old back`. REMAINING: browser-smoke UAT on prod (8-item checklist in 28-03 <browser_smoke_gap>: add a want tagged to your char → appears in My grouped by char; account-level wants stay; Guild toggle shows all-members roll-up with owner+character attribution, NO note; group-by-char filter; forged-body 403; EC embed names char on a live match) + optional `/gsd-ui-review 28`. **Phase 28 SHIPPED (pending browser-smoke). v2.3 is now FEATURE-COMPLETE (P26+P27+P28 all deployed) — next: browser-smoke → v2.3 milestone close (`/gsd-complete-milestone` or `/gsd-audit-milestone`).**

PRIOR (planned 2026-06-08):
Plan: 3 plans / 3 waves (strict chain 28-01→28-02→28-03), 9 tasks. Gates: plan-directly (no CONTEXT.md), --skip-ui (additive on /wantlist), light research. Flow: research (HIGH) → pattern-map (14 files, all analogs) → planner → plan-checker **VERIFICATION PASSED** (0/0, first iter). LOCKED decisions: NEW migration **00010 → schema v10** (NOT 00009 — that shipped as P26 character_assignment; roadmap text was stale, corrected); `ALTER wantlist_item ADD COLUMN character_id INTEGER REFERENCES character(id)` NULLABLE (auto-NULL backfill, no data-migration); REWRITE the two dedup indexes with `COALESCE(character_id,-1)` so same-item-two-chars works WITHOUT regressing account-level dedup (load-bearing — NULL is DISTINCT in SQLite). CWANT-04=SHOW per-want character + owner (web_user.username) attribution, EXCLUDE private `note` from the NEW guildwide read. CWANT-05=EC embed gains display-only "For <char>" field; `wantmatch.Hit.DiscordUserID` (DM target=owner) STRUCTURALLY UNCHANGED (regression test asserts DM target==want creator even when char owner differs). PRIMARY new security control = IDOR guard `IsCharAssignedToTx` (mirrors charSharedTx) called in AddWantHandler withTx before insert → `ErrCharNotAssigned`→403; tag <select> options only from fetchMyCharacters(). Guildwide surface is NET-NEW (no existing all-members wantlist): ListGuildWants + `GET /api/v1/wantlist/guild` (login-gated) + a My/Guild toggle on /wantlist (P27 pattern). CWANT-06 group-by-char via NEW pure `web/src/lib/groupByChar.ts` (mirrors myview.ts) + node test. 28-01 migration/store/IDOR (wave1) → 28-02 API/embed (wave2) → 28-03 web (wave3). Each plan has STRIDE threat_model (T-28-01..12). Browser-smoke gap flagged (node vitest DOM-blind). NOT YET EXECUTED — 28 ships a LIVE v9→v10 migration (proven P26 deploy path: R2 backup→binary swap→goose.Up→web atomic-swap) — execution+deploy is a deliberate next step. Next: `/gsd-execute-phase 28`.

---

Phase: 27 — My-Characters Inventory Filter (SHIPPED 2026-06-08 — deployed live + browser-smoke PASS)
BROWSER-SMOKE UAT 2026-06-08: **PASS** — confirmed on prod: defaults to All members, "My characters" narrows all four grids, single-char drill-down works, "All members" restores the full grid, SearchBox stays guild-wide with the filter on, names render escaped. (#6 zero-claimed-character hint deferred — needs a no-assignment account; low-risk, logic code-verified.) Phase 27 = SHIPPED + UAT-verified. MYVIEW-01/02 delivered.
Execution: 1/1 plan complete (27-01, 3 commits 1d400bf test-RED → 972da5c feat-GREEN → 9bd86b2 wire +page.svelte; all on master, web/ only). NEW `web/src/lib/myview.ts` (pure `myCharNameSet`+`applyMyFilter`, DOM-free, type-only import, no {@html}) + `myview.test.ts` (9 cases green); `web/src/routes/+page.svelte` adds fetchMyCharacters() to the Promise.all, `mineOnly=$state(false)` (default OFF), one EQ-theme `<select>` (All members/My characters/per-char), four grids fed `data={filtered*Rows}`, SearchBox stays full `viewRows`, zero-claimed disabled/hint. Gates GREEN: npm run check 0/0 (484 files), npm test 296/296 (incl 9 new), npm run build ok. Name-join VERIFIED in source: readviews.go:150 ≡ assignment.go:448 both `SELECT c.name` raw (COLLATE NOCASE is ORDER-BY-only) → MyCharacter.name ≡ ViewRow.char byte-for-byte; predicate also case-insensitive (defensive). CONSOLIDATED-VIEWS LOCK held: DataGrid.svelte + columns.ts + api.ts UNTOUCHED, no per-char tabs, no backend/route/migration (git diff = 3 files all under web/src/), no ?mine=1 server scope (negative security property T-27-01). Verifier=human_needed (3/3 success criteria + MYVIEW-01/02 code-verified; 27-VERIFICATION.md). REMAINING before "verified": browser-smoke UAT on a DEPLOYED build or full local stack (NOT npm run dev vs prod — login bounces) + `/gsd-ui-review 27` — node vitest is DOM-blind (web-tests-node-only-blind-to-dom; same gap as P15/P26). Smoke checklist (8 items) in 27-01-PLAN <browser_smoke_gap> + 27-VERIFICATION. CODE REVIEW: `/gsd-code-review 27` ran — 27-REVIEW.md, 0 BLOCKER/0 HIGH/0 MEDIUM, 2 LOW + 2 NIT; LOW/NIT FIXED in 25c76c1 (IN-01 reset stale drill-down `selectedChar` on refetch; IN-02 clarifying comment that the shared bank-coin summary stays guild-wide by design; IN-03 +2 predicate tests → 298 green; IN-04 noted impossible — character.name is UNIQUE). Gates re-green (check 0/0, test 298, build ok). DEPLOYED LIVE 2026-06-08 (web-only, NO backend restart — zero server change): npm run build → tarball → scp → extract to /var/www/squirebot.new → chmod u=rwX,go=rX → atomic mv-swap (squirebot.old kept for rollback) per docs/backend-deploy.md §7.5. External verify GREEN: apex 200, entry JS content-type text/javascript (fresh bundle start.BPXF6Psz.js; NOT the blank-screen text/html bug), api 401 (backend healthy/untouched). Rollback: `mv /var/www/squirebot /var/www/squirebot.bad && mv /var/www/squirebot.old /var/www/squirebot`. REMAINING: browser-smoke UAT on prod (8-item checklist in 27-01-PLAN <browser_smoke_gap>) + optional `/gsd-ui-review 27` — node vitest is DOM-blind. Next: browser-smoke → `/gsd-plan-phase 28` (Character-Tagged Wantlist — the 00009 character_id touch; final v2.3 phase; stacks on this + P26).

PRIOR (planned 2026-06-08):
Plan: 1 plan / 1 wave / 2 tasks (27-01). Gates chosen: plan-directly (no CONTEXT.md — design fully locked in ROADMAP), **--skip-ui** (additive filter on the existing design system), light research first. Flow ran: research (HIGH conf) → pattern-map (4 files, all exact analogs) → planner → plan-checker **VERIFICATION PASSED** (0 blockers / 0 warnings, first iteration). Key finding: **ZERO backend change, ZERO migration** — purely a `web/` composition of already-shipped parts (consolidated view payloads + P26 `fetchMyCharacters()` / `GET /api/v1/assignments/mine`). 27-01 = NEW pure `web/src/lib/myview.ts` `applyMyFilter` + node test (mirrors assignments.ts), then ONE EQ-theme `<select>` (All members[default-off] / My characters / per-char drill-down) wired into `web/src/routes/+page.svelte` feeding the four grids pre-filtered `data` (WantlistPanel upstream-filter pattern); SearchBox stays guild-wide; DataGrid/columns untouched (consolidated-views LOCK preserved by construction). Locked sub-decisions: single `<select>`, grid-only, join on NAME (verified: readviews.go:150 ≡ assignment.go:448 both `SELECT c.name`, no normalization; predicate also case-insensitive), pure helper. Threat model T-27-01 = the NEGATIVE property (filter is NOT access control — API already serves all-members rows to every session). Browser-smoke gap flagged (node-only vitest is DOM-blind — the P15/P26 trap): post-exec `/gsd-ui-review 27` + deploy-then-smoke (or full local stack) mandatory before verified. Docs: 27-RESEARCH/PATTERNS/01-PLAN written (committed; commit_docs=false otherwise). Next: `/gsd-execute-phase 27`.

---

Phase: 26 — Character Assignment (EXECUTED 2026-06-08 — code-complete; UAT + 2 advisory MEDIUMs pending)
Execution: 3/3 plans complete (10 commits aff1553..c08b4e6, all on master, code only). 26-01 store/migration (00009→v9), 26-02 API (12 routes), 26-03 web UI. Gates: go build/vet/test green; web check(0 warn)/build/test(287) green. Verifier=human_needed (6/6 criteria + 6/6 ASSIGN code-verified; web interactive + live-DB deploy = UAT). Code review=issues_found, 0 BLOCKER/0 HIGH; 2 MEDIUM + 2 NIT FIXED via /gsd-code-review-fix (commits b1852eb/29170a6/55d06d3, ff-merged to master; gates re-green). MD-01: RequestTx now rejects non-contested chars (unassigned/self-held) → 409 not_contested (ErrCharNotContested). MD-02: added ListMyPendingRequests + GET /api/v1/assignments/requests/mine + panel hydration (Request→Cancel survives reload). 3 LOW deferred (out-of-tx assignee probe [FK-backstopped], unused current_assignee UI plumbing, post-approval re-request). SECURITY: /gsd-secure-phase 26 PASSED — 26-SECURITY.md threats_open:0, 18/18 closed (17 mitigate + 1 accept AR-26-07 multiple-guild-banks). Phase 26 = code-complete + verified(human_needed) + review-clean + threat-secure. DEPLOYED LIVE 2026-06-08: R2 backup (squirebot-2026-06-08.db.gz) → backend binary swapped (.bak kept) → goose.Up applied 00009 (schema v8→v9; `OK 00009_character_assignment.sql`; bot reconnected; scheduler 4 jobs) → AUTO-SEED 4/4 eligible chars assigned → web tarball+atomic-swap (squirebot.old kept). External verify GREEN: apex 200, JS content-type canary passes, /my-characters serves 200, whoami-web 200, api healthy. Rollback: cp /usr/local/bin/squirebot-server.bak → restart (old binary runs against v9 DB — extend-only); web mv squirebot.old back. BROWSER-SMOKE UAT 2026-06-08: **PASS** (see 26-HUMAN-UAT.md). Blocks A (member claim/release/re-claim + inline 409)/B (officer assign/reassign/remove/designate)/C (My-characters nav link + de-banked CharMetaForm) all green; live-DB seed verified directly on-box (4/4 assignments, real bank toon Findom preserved). Found 1 MEDIUM UI bug → **backlog 999.33** (commit 9c3022f): designating a char bank/bot clears its character_assignment row so it drops out of ListAllAssignments — the only list AssignmentAdminPanel renders + the host of the designate/reassign controls — AND it's excluded from member claimable, so bank/bot is a UI one-way door (data layer correct/reversible; fix = surface designated chars in the panel). Surfaced live: tester designated all 4 assigned chars → panel emptied → scoped direct-DB recovery (R2 backup first; cleared flags on ids 1/14/15/16 + re-seed assigned_by='migration'; Findom untouched; +assignment_restore audit rows). DEFERRED (tester's discretion): non-officer /admin 403 UI-collapse (needs a 2nd account; server gate already code-verified via TestOfficerEndpoints_NonOfficer_Rejected) + the Request→Cancel (A5)/Approve-Deny (B4) interactive flows (UI trigger can't occur today — claimable=unassigned-only; API test-covered). Optional: /gsd-ui-review 26. Next: /gsd-plan-phase 27 (stacks on committed code). v2.3 Phase 26 SHIPPED + UAT-verified.
PRIOR planning state:
Plan: 3 plans (26-01 store/migration → 26-02 API → 26-03 web), 3 waves, ASSIGN-01..06 covered. RESEARCH.md + PATTERNS.md + 3 PLAN.md written; plan-checker: 0 blockers / 4 warnings (all fixed in a revision pass).
Status: Planning complete. Locked resolutions from research: guild bank SUBSUMES is_bank_toon (officer-only; single-bank invariant RELAXED — multiple banks OK, bank view unchanged, 2-bank test added); guild bot = NEW is_guild_bot column; character_assignment PK on character_id = single-assignee; assignment_request (pending|approved|denied|cancelled) + partial-unique-pending; idempotent INSERT OR IGNORE auto-seed from owner.discord_user_id; goose v9, NO _meta.schema_version cell; owner_id never re-homed; watcher UNTOUCHED. Each plan has a STRIDE threat_model. Browser-smoke gap flagged for /gsd-ui-review (web tests node-only). NOT YET EXECUTED — Phase 26 writes a LIVE-backend schema migration (00009) + API + web; execution + deploy is a deliberate next step. Next: `/gsd-execute-phase 26`.
Last activity: 2026-06-24 — v2.6 "Item Detail & Polish" milestone OPENED + ROADMAPPED (Phases 37–42; 13/13 requirements mapped ENRICH-12/13→P37 · ENRICH-14/15→P38 · SEARCH-04/05/06→P39 · ITEMUI-01/02→P40 · CHARUI-01/02→P41 · WISHUI-01/02→P42; backend + web, watcher untouched, no `v*` tag; Phase 37 ready to plan). Prior: 2026-06-24 — quick task 260624-mdp hardened eviction/ownership guards (3 v2.5 P35/P36 code-review follow-ups — 999.37 preview guard parity, 999.38 repoint skips evicted stewards, 999.39 label bridges exclude the `guild` sentinel) **DEPLOYED LIVE 2026-06-24** (backend binary-swap, no migration, no `v*` tag; goose no-op at v15; fail-closed smoke green; `.bak` kept). 3 atomic fix commits (`71fed2c`, `d98da18`, `470b670`) on master. Web cleanup 999.40 was found already-resolved by quick `260621-px6` (stale backlog entry). **All v2.5/v2.4 code-review backlog follow-ups cleared.** Prior: 2026-06-22 — Phase 36 plan 36-02 web half DEPLOYED LIVE (v2.5 shipped+closed).

## Prior v2.4/v2.5 execution narrative (historical — SHIPPED + archived; retained as accumulated context)

**34-03 EXECUTED 2026-06-19** (commits `ab4a2af` + `18fdd61` + `0e46d65`, web-only, all on master): the SvelteKit Wishlist tab — the user-facing surface that closes WISH-01..07. **api.ts:** the 4 `WishlistView`/`WishlistSlot`/`WishlistTarget`/`WishlistSuggestion` interfaces MIRROR `compute/types.go` field-for-field (snake_case; `price: number | null`, `item_id: number | null`) + `fetchWishlist` (OBJECT generic, char `encodeURIComponent`'d) / `addWishlist` (body carries NO owner — session-derived) / `removeWishlist` / `setWishlistPing`; `searchCatalog` REUSED unchanged for the typed-entry add. **Pure node-tested `web/src/lib/wishlist/wishlist.ts`:** `wishlistBandOf` (two non-bank bands) + `wishlistRoster` (drops every `is_bank_toon || is_guild_bot` row EVEN when `is_mine`, then mine→guild A-Z, a NEW array — WISH-01) + `filterWishlistRoster` (banks/bots-excluded name filter preserving order) + `searchWishlistItems` (the WISH-07 cross-wishlist group-by-item-name over the WHOLE passed-in corpus, listing each (char, slot) — NEVER scopes or fetches; the caller owns the corpus). 15 node cases, RED-verified (module-missing) before GREEN. **`/wishlist/+page.svelte` REWRITTEN** (replaces the P30 WantlistPanel placeholder). Web gates GREEN: `npm run check` 0/0, `npm test` 380 passed (29 files), build ok. DEPLOY = 34-04. See `34-03-SUMMARY.md`.

**34-02 EXECUTED 2026-06-19** (commits `cbbc208` + `58587e1` + `03de8af`, backend-only): the load-bearing `wantmatch` repoint + the owner-scoped wishlist write/read API + the full clean-break test repair so `go test ./...` is GREEN again. Matcher (WISH-05) repointed to `wishlist_item` (`pinged = 1`, INNER `JOIN character`, `note` dropped; DM-target-is-owner invariant T-34-08 preserved + regression-tested). Owner-scoped `webadmin/wishlist.go` (in-tx `IsCharAssignedToTx` 403 / 409 dup / 21-slot 400 / silent IDOR no-op); `readapi/wishlist.go` `GET /api/v1/wishlist/{char}`; main.go 4 wishlist routes registered, 5 wantlist routes removed. Clean-break test repair → `go test ./...` GREEN. NO web/watcher change; NO new migration. See `34-02-SUMMARY.md`.

**34-01 EXECUTED 2026-06-19** (commits `ea0223a` + `6f1eea3` + `00574e9`, backend-only): the wishlist data + transform foundation. Migration 00014_wishlist (schema → v14, the D-01 clean break): new `wishlist_item` table (per char + canonical worn-slot + target; `pinged` DEFAULT 1; two partial-unique dedup indexes), `alert_log` rebuilt (FK repointed to `wishlist_item(id)` before the `wantlist_item` drop), `wantlist_item` + its indexes dropped. `store/wishlist.go` owner-scoped CRUD/ping + `AlertedWishlistIDs`; `store.PriceByName` (full-catalog name bridge); `compute.WishlistFor` + pure `buildWishlistView` (equipped + auto-removal + class+slot suggestions via the slot-vocab bridge). EXPECTED 34-02 hand-off: the 5 retired-wantlist-surface test packages fail on `no such table: wantlist_item` (production code still compiles). See `34-01-SUMMARY.md`.

**33-02 EXECUTED 2026-06-19** (commits `577d503` + `900c4d6` + `cc0a7b8`, web-only): the SvelteKit web half of the Banks tab over the 33-01 `GET /api/v1/banks` route. `api.ts` `BanksView`/`BankRowSummary` interfaces + `fetchBanks()`; pure node-tested `web/src/lib/banks.ts` (`sortBanksAZ` plain A-Z, `bankItemSearch` with the bank-slice qty recompute [Pitfall 3], `sortBankHolders`, `bankByName`) + 11 node cases; the rebuilt `/banks/+page.svelte` (D-02 guild summary band + list/search toggle + D-04 per-bank header + reused InventoryWindow + in-tab holder deep-link). Web gates GREEN (check 0/0 [509 files], test 370/29, build ok). DOM browser-smoke deferred to 33-03. See `33-02-SUMMARY.md`.

**33-01 EXECUTED 2026-06-19** (commits `e7ddfa7` + `bfda028` + `7e43687`, backend-only): the backend half of the Banks tab (BANK-01/02). Resolves the Phase-29 valuation SCOPE bug (Pitfall 1) via Option B: `store.InventoryJoinBanksAndBots` (`is_bank_toon=1 OR is_guild_bot=1`) + `store.ListBankAndBotToons` + `compute.Banks`/`BanksView`/`BankRowSummary` + `GET /api/v1/banks` (RequireSession). Value/platinum asymmetry (item VALUE includes bots; PLATINUM stays bank-toon-gated). NO new migration (schema stays v13). Gates GREEN (full module `go test ./...` ok). See `33-01-SUMMARY.md`.

**32-02 + 32-01 EXECUTED 2026-06-18** (web `e7314bb`/`2d36dbc`; backend `39b6660`/`8bc2cbd`/`446fa2d`): the item-centric Inventory tab. Backend `compute.Items(ctx, store, viewerID)` group-by-normalized-name rollups (one row per item: Σ qty, distinct holder count, is_mine, name-keyed price/wiki/quest, id-correct icon/stats, per-holder slot/qty/last-synced) served at session-gated `GET /api/v1/items`; web `api.ts` ItemRollup/ItemHolder + pure `items.ts` (viewer-first/filter/holder-sort) + the `/inventory` master-detail tab (reused P31 ExaminePanel + holders deep-linking to `/characters?c=`). Deployed live (32-03) + browser-smoke PASS across 5 themes. See `32-0x-SUMMARY.md`.

**Phase 31 CLOSED 2026-06-18** (4 plans + 8 browser-smoke fix commits): the Characters tab + in-game inventory window. 31-01 icon enrich + migration 00012; 31-02 session-gated read routes (`/api/v1/inventory/{char}` + `/api/v1/characters`); 31-03 web Characters tab (viewer-first 3-band list + scoped search + `?c=` selection); 31-04 InventoryWindow (23-slot paperdoll, general+bank grids, inline bag expand, wiki icons + colored-tile fallback, hover/pin examine) + migration 00013_item_statsblock. DEPLOYED LIVE + browser-smoke approved across 5 themes; verifier 4/4 + 7/7 req IDs; code-review 0 BLOCKER/0 HIGH. CHAR-01..03 + INV-01..04. See `31-0x-SUMMARY.md` + `31-VERIFICATION.md`.

Phase 31 UI-SPEC: ✅ APPROVED 2026-06-18 (`31-UI-SPEC.md`; gsd-ui-checker 6/6 PASS). Pins paperdoll geometry (23-slot set per `slotconst.go`), the general+bank grids reusing one renderer, INLINE bag expand, hover-preview+single-pin examine (shared `examineBody`, D-08 order, `{@html}` escaped via `composeItemNote`), colored-tile icon fallback. Window is a generic prop-driven component over `CharacterInventory` (reused by P33, feeds P34).

## v2.3 Phase Plan (created 2026-06-08)

Phases continue from v2.2 (which ran 19–25). Phase dirs 11–25 exist on disk — never reuse them. v2.3 starts at **26**.

**Execution order:** strict dependency chain **26 → 27 → 28**. The character-assignment data layer (P26) is the foundation BOTH the my-characters filter (P27) and the character-tagged wantlist (P28) build on — each needs the authoritative "which characters are mine" answer.

| Phase | Name | Requirements | Success Criteria | UI |
|-------|------|--------------|------------------|----|
| 26 | Character Assignment | ASSIGN-01, ASSIGN-02, ASSIGN-03, ASSIGN-04, ASSIGN-05, ASSIGN-06 | 6 | yes |
| 27 | My-Characters Inventory Filter | MYVIEW-01, MYVIEW-02 | 3 | yes |
| 28 | Character-Tagged Wantlist | CWANT-01, CWANT-02, CWANT-03, CWANT-04, CWANT-05, CWANT-06 | 4 | yes |

**Phase 26 — Character Assignment (foundation):** `00009` goose migration → schema **v9** adding the `character_assignment` layer over the existing `character.owner_id` upload provenance; member self-claim/release + officer assign/reassign API (IDOR-safe, owner/officer-scoped via the ADMIN-04..06 gate, audited); the "My characters" + officer admin-panel UI. **Open sub-decision:** ASSIGN-05 single- vs multi-owner for shared bank toons (resolve in spec/plan; likely many-to-many).

**Phase 27 — My-Characters Inventory Filter:** an ADDITIVE "my characters" quick-filter + single-character drill-down over the existing all-members consolidated views (inventory/bank/gear/spell). Visibility stays all-members — the CLAUDE.md consolidated-views rule is intact; this is NOT access control. Read-only/client-side over the existing DataGrid; no per-character view tabs.

**Phase 28 — Character-Tagged Wantlist:** the wantlist gains an OPTIONAL character dimension — NULLABLE `character_id` on `wantlist_item` (00009 backfills existing wants to NULL, no data loss); character-tagged wants roll up into the guildwide wantlist; per-user filter/group-by-character; the EC-tunnel monitor DM still targets the character's OWNER (keys on `discord_user_id`). **Open sub-decisions:** CWANT-04 guildwide attribution display; CWANT-05 whether the EC embed names the character.

## Milestone v2.3 Scope (locked 2026-06-08)

**Goal:** Associate SquireBot users with specific characters, let them view those characters' inventory, and create character-tagged wantlist items that roll up to the guildwide wantlist.

**Locked decisions:**

- **Assignment = BOTH** — member self-claim (incl. characters under an unlinked/legacy owner) AND officer assign/override/reassign.
- **Inventory = ALL-MEMBERS + FILTER** — keep the all-members consolidated views (CLAUDE.md LOCKED); ADD a "my characters" filter/drill-down. ADDITIVE, not an access-control restriction.
- **Wantlist = CHARACTER-TAGGED** — wants gain an optional character dimension and aggregate into the guildwide wantlist; the EC-monitor DM still targets the OWNER.

**Scope:** backend (`internal/backendsrv`) + web (`web/`). The Go **watcher is untouched** (backend-only schema; no `WatcherMaxSchemaVersion` concern). New `00009` migration → schema **v9** (extend-only, version-stamped, idempotent; `character_id` NULLABLE).

**Open sub-decisions (resolve in spec/plan):** ASSIGN-05 (multi-user shared bank toons), CWANT-04 (guildwide attribution display), CWANT-05 (EC embed names the character).

<details><summary>v2.2 history (parked — Track 2 invite-gated; preserved below)</summary>

Phase: 25 — Linux Watcher — **COMPLETE (3/3 plans)**. v2.1.1 watcher SHIPPED 2026-06-08 (tag pushed; release.yml green; latest.json permalink → 2.1.1; win+linux assets reachable; bundles Linux watcher P25 + eqfind EQLite autodetect quick 260607-vgb). Earlier 2026-06-08: 260607-sdh web gear-menu IA cleanup DEPLOYED to prod via tarball+atomic-swap. v2.2 Track 1 (P19-21) SHIPPED LIVE; Track 2 (22-23 Discord pinger) parked on the 3 Raid Alliance bot invites. Detailed P21/P25 activity logs retained below.
</details>

Phase 25 post-execution (2026-06-06): verified (6/6 criteria code-verified, human_needed — 4 on-machine UATs in 25-HUMAN-UAT.md). Code review found 2 BLOCKERS + 6 warnings — both blockers were install-flow bugs (CR-01: `--status` always exit 0 → install.sh skipped first-run `--setup` so a fresh box sat red; CR-02: per-prompt bufio.Reader dropped piped stdin) — ALL 8 FIXED (25-REVIEW-FIX.md; commits bebe961/b12f013/0ac0bc0/f7f8736/bd197d7/c47efdc/81b4b28), gates re-green. Local snapshot tarball assembled + inspected (static ELF + README + systemd unit + install.sh; `install -Dm755` sets the exec bit on install). HANDOFF: ready for a Linux guildie to smoke-test (run install.sh → --setup → play P99 → confirm upload on squirebot.quest); canonical release tarball builds via release.yml on a tagged release.

Phase 25 activity (25-03, FINAL): 2026-06-06 — 3 task commits (6e31ec3, 1a3c239, 5b09a4f). (1) `internal/update/manifest.go` gained additive `BinaryURLLinux`/`BinarySHA256Linux` (omitempty, new-fields-only — old Windows-only latest.json still parses) + a `Manifest.binaryAsset() (url,sha)` method keyed on `runtime.GOOS`. (2) `packaging/linux/`: systemd USER unit + idempotent POSIX `install.sh` + Linux README. (3) `release.yml`: additive CGO-free linux build + `squirebot-linux-amd64.tar.gz` + both linux assets uploaded — Windows NSIS/installer/`.exe` steps UNTOUCHED. Gates green. Phase 25 COMPLETE (3/3); LNX-01..06 code-complete.

Phase 25 activity (25-02): 2026-06-06 — 3 task commits (09d6d72, 66eac47, 6aa2d16). credstore split (wincred windows / 0600-file other), eqfind WINE-prefix walk, CLI --setup/--status onboarding + RunSetup. Gates green.

Phase 25 activity (25-01): 2026-06-06 — 3 task commits (bb8e214, e84c6c4, 52b96c6). `fyne.io/systray` build-tag-split (tray_windows / tray_other no-op) + main.go systray split + config/logging XDG branch. Gates green.

Track-1 closeout 2026-06-06: cross-compiled linux/amd64 → scp → install → `systemctl restart`; `goose.Up` auto-applied `00008_ec_cursor` (schema v7 → v8). UAT #1 (live EC DM) tracked in `21-HUMAN-UAT.md`. v2.2 Track 1 SHIPPED LIVE; Track 2 invite-gated.

Phase 21 activity: 2026-06-06 — 21-03 (the integration finale, WANT-05): NEW `internal/backendsrv/ec` package `RunMatch` (poll→diff→match→embed→send over PigParse `getdetails/0/{name}`) + `scheduler.ec_auction_match` job. 21-02: discordgo rich-embed send-path + `wantmatch.Hit.Note`. 21-01 (GATING): PigParse feasibility spike (path=getdetails, server=0, NAME key) + migration 00008 + cursor/poll-set store. Phase 21 COMPLETE (3/3).

## v2.2 Phase Plan (created 2026-06-02)

Phases continue from v2.1 (which ended at Phase 18). Phase dirs 11–18 exist on disk — never reuse them. v2.2 starts at **19**.

**Execution order:** 19 → 20 → 21 (Track 1, UNBLOCKED — strict dependency chain) → 22 → 23 (Track 2, INVITE-GATED). 22/23 depend on the Phase 20 spine but are otherwise gated purely on the external Raid Alliance invites.

| Phase | Name | Track | Requirements | Success Criteria | UI |
|-------|------|-------|--------------|------------------|----|
| 19 | Wantlist CRUD | 1 (unblocked) | WANT-01, WANT-02 | 4 | yes |
| 20 | Bot + DM + Notification Infrastructure | 1 (unblocked) | WANT-03, WANT-04, WANT-08 | 5 | no |
| 21 | EC-Tunnel Auction Monitor | 1 (unblocked) | WANT-05 | 4 | no |
| 22 | WTS Cross-Server Monitor | 2 (invite-gated) | WANT-06 | 3 | no |
| 23 | Quest-Target Raid Monitor | 2 (invite-gated) | WANT-07 | 3 | no |

**Phase 19 — Wantlist CRUD:** pure web feature, the product surface — `00006_wantlist.sql` (≥ `wantlist_item`, `alert_log`); `webadmin/wantlist.go` (the `account.go` twin: login-only, IDOR-safe, audited); `web/src/routes/wantlist/` reusing the existing item catalog for add-item search; already-in-bank flag. No Discord yet.

**Phase 20 — Bot + DM + Notification Infrastructure (keystone):** in-process discordgo gateway goroutine (`recover()`-isolated, non-fatal start, reconnect, `DISCORD_BOT_TOKEN` env); `notify` DM open+send + 50007 → `dm_blocked`; `wantmatch` shared matcher; `alert_log` dedup/cooldown; opt-in/prefs + in-site notification inbox; per-monitor enable/disable + `guild_channel` config feature flags. The spine all three monitors ride.

**Phase 21 — EC-Tunnel Auction Monitor:** first real alert. MANDATORY first task = the PigParse feasibility spike. `scheduler.ec_auction_match` poll-and-diff on `ec_auction_cursor` → `wantmatch.ForItem` → `notify`. Advance-cursor-only-after-success; no backlog replay on restart.

**Phase 22 — WTS Cross-Server Monitor (INVITE-GATED):** entry-precondition = 3 Raid Alliance bot invites confirmed in writing + `MESSAGE_CONTENT` enabled. Build/test the name/alias matcher against a fixture corpus first; `bot/wts.go` MESSAGE_CREATE; reuse `notify`/`alert_log`. Stays dark behind feature flag until invites land.

**Phase 23 — Quest-Target Raid Monitor (INVITE-GATED):** entry-precondition = invites + `MESSAGE_CONTENT` AND the curated `quest → NPC` table populated. `bot/raidtarget.go` MESSAGE_CREATE → NPC detect → `quest_target` → existing `quest_items` → `wantmatch.ForItem` → `notify`.

## Milestone v2.2 Scope (locked 2026-06-02)

**Goal:** Guildies maintain a personal wantlist on squirebot.quest and get DMed on Discord when a wanted item appears — at EC-tunnel auction, in cross-server WTS channels, or as a raid-target tied to a wanted item's quest.

**Locked decisions (v2.2 research):**

- **Delivery/reading split** — DM delivery is UNBLOCKED (bot in the guild's own Discord reaches every guildie); only WTS/raid *reading* is invite-gated. Drives Track 1 (P19–21) vs Track 2 (P22–23).
- **In-process bot goroutine, not a separate process** — avoids two SQLite writers; `recover()`-isolated + non-fatal start + `Restart=always`. Lib: `bwmarrin/discordgo` v0.29.0 (CGO-free, the only new dependency).
- **EC = PigParse poll-and-diff** (~10-min cadence; no push feed) — live spike first (Phase 21's first task).
- **One match seam, three sources** — shared `wantmatch` + `notify` + `alert_log` (dedup/cooldown), built once in Phase 20; EC/WTS/raid fan in.
- **50007 (can't-DM) first-class** — in-site notification inbox is table stakes, the fallback that makes the unreliable DM channel acceptable.
- **`MESSAGE_CONTENT`** is a self-serve dev-portal toggle (no Discord audit under 100 servers) — gates Track 2 reading only.
- **HARD CONSTRAINT (carried):** never put a Discord bot or OAuth in the watcher — untouched this milestone.

**New schema:** one goose migration `00006_wantlist.sql` — `wantlist_item`, `alert_log`, `quest_target`, `guild_channel`, `ec_auction_cursor`. `quest_target` adds only the inverse quest → raid-NPC hop; reuses the existing `quest_items` table.

## External prerequisites (gate Track 2 only)

| # | Prerequisite | Gates | Status |
|---|--------------|-------|--------|
| 1 | 3 Raid Alliance Discord bot invites (WTS-channel read + `MESSAGE_CONTENT`) | WANT-06 (P22), WANT-07 (P23) | Un-negotiated (external/human) |
| 2 | Curated `quest → raid-target NPC(s)` lookup populated (seeded from wiki) | WANT-07 (P23) | Not started; can curate in parallel now |

## Deferred Items

Carried forward at v2.1 close (non-blocking):

| Category | Item | Status |
|----------|------|--------|
| ops | Decommission the Azure PAYG test VM (the `0.4.0-rc1` box) to stop billing | ✅ done 2026-06-02 (user decommissioned the VM) |
| signing | 999.9 — SignPath Foundation OSS approval | in flight (lands as a hotfix when approved) |
| uat | P12/P14/P15 HUMAN-UAT live-smoke checklists (from v2.0) | ✅ closed 2026-06-06 — P14 (5/5) + P15 (7/7) were already `complete`; P12's lone Sheet-parity test marked **obsolete** (the Sheet was decommissioned in P16; enrichment is proven live on the prod scheduler) |

## Files of Record

- `.planning/PROJECT.md` — core value, requirements, constraints, Key Decisions (updated 2026-06-24 with the v2.6 milestone).
- `.planning/REQUIREMENTS.md` — v2.6 requirements + Traceability (13/13 mapped to Phases 37–42).
- `.planning/ROADMAP.md` — phase structure (v2.6 Phases 37–42 appended; all prior-milestone history + the 999.x backlog preserved) + Progress tables + Backlog.
- `.planning/MILESTONES.md` — historical record (v1.0 + v1.0.1 + v2.0 + v2.1 + v2.3 + v2.4 + v2.5).
- `.planning/milestones/v2.5-{ROADMAP,REQUIREMENTS}.md` — the v2.5 archive (latest shipped milestone).
- `.planning/config.json` — granularity (coarse), mode (yolo), workflow toggles.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260602-u7m | Church of Clean Code audit follow-up — save report + low-risk fixes (.gitignore, CLAUDE.md dep doctrine, enrich naming) | 2026-06-03 | 922b441 | [260602-u7m-church-audit-follow-up-save-report-low-r](./quick/260602-u7m-church-audit-follow-up-save-report-low-r/) |
| 260607-sdh | Reorganize web top bar into an accessible header settings dropdown (gear menu): theme picker + Watcher-codes + Character-details + officer Admin + identity/Sign-out fold in; header slims to Wantlist + Notifications + gear. Absorbed & deleted SessionIndicator. | 2026-06-08 | bdde646 | [260607-sdh-reorganize-web-top-bar-into-accessible-s](./quick/260607-sdh-reorganize-web-top-bar-into-accessible-s/) |
| 260607-vgb | eqfind autodetect: add EQLite-bundle + system-location coverage (Linux `/opt/...EQLite` direct-hit + `systemCandidateRoots` walk; HOME `~/Desktop/EQLite`; Windows `%USERPROFILE%\Desktop\EQLite` etc.). Fixes a guildie's `/opt/everquest/EQLite` being invisible (Linux scan was WINE-`drive_c`-only). Ships to guildies only via a tagged release (auto-update). | 2026-06-08 | add17e9 | [260607-vgb-add-eqlite-bundle-system-location-covera](./quick/260607-vgb-add-eqlite-bundle-system-location-covera/) |
| 260609-d2o | **999.33 fix** — officer-reversible guild-bank/bot designation. New `ListDesignatedChars` store read + `GET /api/v1/admin/characters/designated` (RequireOfficer) + a "Designated characters (guild bank / bot)" section in `AssignmentAdminPanel` with a "Clear designation" (→ `designateChar(id,'none')`, the existing audited endpoint) button; closes the one-way-door surfaced at the P26 UAT. No migration; watcher untouched. Gates green (go build/vet/test; web check 0/0, test 303, build). **SHIPPED — DEPLOYED LIVE 2026-06-09** (R2 backup → binary swap [.bak kept] → restart [goose no-op at v10] → web atomic-swap; external verify GREEN: apex 200, fresh JS bundle, new GET /api/v1/admin/characters/designated → 401 officer-gated, health 401). | 2026-06-09 | b7b0a48, 2b18d65 | [260609-d2o-officer-undesignate-bank-bot-char](./quick/260609-d2o-officer-undesignate-bank-bot-char/) |
| 260609-er6 | **EC-monitor wrong-server hotfix** (P21 bug, user-reported). The `getdetails` feed was hardcoded to server=**0** = GREEN (the 21-SPIKE "server=0=Blue" finding was WRONG — only proved fresher, not which server); guildies on BLUE got DMs for GREEN sellers. Live probe confirmed **server=1 = live Blue** (Cloak of Flames srv=1 max_t 2026-06-08T23:02, fresher than srv=0; matches `getall/1`=Blue); records carry no per-auction server field → URL segment is the only lever. Changed `ec/urls.go` getDetailsBase `/getdetails/0/`→`/1/` + corrected comments (urls_test/ec.go/pigdetails.go/scheduler.go) + 21-SPIKE correction banner; NO logic change. Gates green (go build/vet/test). **SHIPPED — DEPLOYED LIVE 2026-06-09**: R2 backup → binary swap [.bak kept] → stop → `DELETE FROM ec_auction_cursor` (8→0, re-baseline against Blue, no spurious DMs) → start; logs confirm all 8 items `first-sight baseline` on boot (server=1/Blue), service active, goose 10. Honest expectation: Blue alerts sparser than Green but CORRECT (coverage = someone parsing Blue's EC tunnel, D-07). | 2026-06-09 | de15933 | [260609-er6-ec-poll-blue-not-green](./quick/260609-er6-ec-poll-blue-not-green/) |
| 260610-fm5 | **UI streamline** (user-requested) — (1) wantlist buy/quest **reason removed end-to-end**: add form/grids/API types, Go handler/store/wantmatch/EC-embed ("Why you wanted it" now Note-only), **migration 00011** dedupe-then-reindex (unique key drops reason, KEEPS `COALESCE(character_id,-1)`; reason column persists, store writes 'buy' forever; same-item re-add now 409); (2) **zero raw item IDs** member-facing + friendly notification source/delivery labels + display-header facet labels; (3) 9 placement fixes: Inventory nav link, home scope filter → segmented My characters/Guild toggle, DataGrid Filters disclosure + 'Filter this table…', mute-bell cross-link, purpose lines + Admin h1, char-meta↔my-characters cross-links + menu rename 'Set class & level', /bank-coin 'Back to bank' → `/?view=bank` (allowlist-seeded tab), full-width wantlist grids. Gates green (web check 0/0, **306** tests, build; go build/vet/test, TestMigrate_00011 added, 00006/00010 unchanged). **SHIPPED — DEPLOYED LIVE 2026-06-10**: R2 backup → binary swap [.bak kept] → restart [goose **OK 00011** → v11] → live-index verify (reason-free, COALESCE held; dedupe deactivated **0** rows) → web atomic-swap; external verify GREEN (apex 200, fresh JS `text/javascript`, wantlist/search routes 401-gated). **6-item browser-smoke UAT PASS 2026-06-17 (all 6 items — task fully closed).** Deferred: AssignmentAdminPanel snowflake→username (needs members-list join), /char-meta fold-in. | 2026-06-10 | e082e7c, 9698c57, bf8b31d | [260610-fm5-streamline-web-ui-remove-wantlist-buy-qu](./quick/260610-fm5-streamline-web-ui-remove-wantlist-buy-qu/) |
| 260618-rh1 | Fix `docs/backend-deploy.md` §7.5 deploy-runbook drift found during the Phase 32 deploy — the web-bundle atomic-swap guard checked `index.html`, but the adapter-static SPA build emits `200.html` (no index.html; `/` is a client redirect). Guard now tests `200.html` so the P33/P34 web deploys don't abort with EXTRACT_FAILED. Doc-only; no code change. | 2026-06-19 | 17e50e7 | [260618-rh1-fix-backend-deploy-web-swap-guard-to-200](./quick/260618-rh1-fix-backend-deploy-web-swap-guard-to-200/) |
| 260621-px6 | Clean up the dead wantlist code left by the P34 clean break (v2.4-audit MD-01 follow-up). Removed the full dead subgraph — 5 frontend components (WantAddForm/WantItemCell/WantMuteCell/WantRemoveCell/InGuildCell) + `web/src/lib/wantlist/` (priority.ts/holders.ts + tests) + surgical `columns.ts`/`api.ts`/`api.test.ts` trims; backend `store/wantlist.go` + `webadmin/wantlist.go` (relocated the 2 LIVE shared helpers `sqliteConstraintUnique`/`boolToInt` → new `store/sqliteconstraint.go` FIRST so assignment/guildchannel/wishlist keep compiling). Kept all live code (guild-views view/gear/spell columns, the wishlist/items/banks/notification APIs). Pure dead-code removal — zero behavior change, no deploy, no migration, watcher untouched; all gates green (go build/vet/test; web check 0/0, 362 tests, build). | 2026-06-21 | 5026da6, 1b5bd29 | [260621-px6-clean-up-the-dead-wantlist-code](./quick/260621-px6-clean-up-the-dead-wantlist-code/) |
| 260621-td4 | Linux watcher polish from Kim's Linux debug report. **(#1)** surface cross-owner ingest rejection on the tray — `internal/app/runapp.go` `handleIngestErr` ErrCrossOwner branch now sets `HealthRed` + status (was silent — tray stayed "Connected", which is why Kim's failure was invisible). **(#2)** `squirebot --reconfigure` forces full re-onboarding (code + EQ folder) when already configured — `RunSetup` gains a `force` param; `cmd/squirebot/main.go` adds the `--reconfigure` dispatch (plain `--setup` unchanged). **#3** (config `"version":1`) and **#4** (install-doc UAC/Google "contradictions") investigated → **NOT bugs** (config schema-version is correctly fixed at 1; no real doc contradiction) — recorded in SUMMARY, no change. Gates green (`go build ./...`; `go test ./internal/app/... ./cmd/squirebot/... ./internal/onboarding/...`; existing 409 test still passes). NO `v*` tag (reaches guildies only via a future watcher release). | 2026-06-21 | 20a7d45, c2e3926 | [260621-td4-linux-watcher-polish-surface-cross-owner](./quick/260621-td4-linux-watcher-polish-surface-cross-owner/) |
| 260624-mdp | **Harden eviction/ownership guards** — 3 v2.5 (P35/P36) code-review follow-ups, backend-only, no migration, no `v*` tag. **999.37/WR-01:** `EvictionPreviewHandler` now mirrors the action's guards (403 `cannot_evict_sentinel` + 403 `owner_floor_protected`) so a peer can't enumerate a floor-protected/sentinel roster via the read-only preview. **999.38/WR-02:** `recentOtherSharerSubquery` repoint gains a live-steward `EXISTS` filter so a surviving shared char is never repointed onto an already-evicted steward (drift-lock re-scoped; +`TestEvictOwnerTx_RepointSkipsEvictedSharer`). **999.39/IN-02:** owner-by-label SELECTs in `store/linking.go`, `auth/store.go`, and the `webadmin/eviction.go` floor bridge exclude the reserved `guild` sentinel (id 1000000) by id. Gates green (`go build ./...`, full `go test ./...`). **DEPLOYED LIVE 2026-06-24** (binary-swap, no migration: R2 backup `squirebot-2026-06-25.db.gz` → scp + hash-verify `0a9f2658` → `.bak` + install + restart; boot log `goose no migrations, version 15`, listening pid 545312; external smoke `/admin/eviction/preview`+`/admin/evict`→401 fail-closed, web apex 200). | 2026-06-24 | 71fed2c, d98da18, 470b670 | [260624-mdp-harden-eviction-ownership-guards-999-37-](./quick/260624-mdp-harden-eviction-ownership-guards-999-37-/) |
| 260621-u6j | **Reversed the locked single-owner write gate** (D-07/V4; user-ratified 2026-06-21). The guild shares P99 logins + has owner-less banks, so first-uploader-owns + reject-everyone-else was the root cause of cross-owner rejects (Kim/Aenriel, lern41/Findom). Now ANY valid guild code may upload ANY character: `bindCharacter` returns the existing charID + nil on a cross-owner write (owner_id NEVER overwritten — first uploader stays a non-binding steward) and audits it as `cross_owner_write` (renamed from `cross_owner_reject`); `ErrCharOwnedByAnother` + the handler 409 path removed; the watcher's now-dead cross-owner tray branch (from 260621-td4) pruned; binding.go doctrine comments updated. Backend-only, **NO migration, NO v\* tag**. Gates green (go build/vet/test; the existing 409 watcher test still passes unchanged). **DEPLOYED LIVE 2026-06-22**: R2 backup → binary swap [.bak kept] → restart (goose no-op at v14) → verified active + listening:8090 + external 404 + clean startup log. Unblocks Kim/Aenriel re-uploads, lern41/Findom, and all future shared-char/bank cases at once — no watcher update needed. **Deferred (Q2):** owner-less / eviction-safe guild banks; and the owner-scoped-eviction edge (eviction now also removes shared chars under that owner). | 2026-06-22 | fc6aee5, 2dfd871, 3f11edd | [260621-u6j-allow-any-guild-member-to-upload-any-cha](./quick/260621-u6j-allow-any-guild-member-to-upload-any-cha/) |

---

*State updated: 2026-06-24 for v2.6 "Item Detail & Polish". Roadmap created 2026-06-24 (Phases 37–42; continues numbering from 36). Prior: v1.0 (2026-05-11), v1.0.1 (2026-05-12), v1.0.2 binary (2026-05-13, superseded), v2.0 (2026-05-31), v2.1 (2026-06-02), v2.2 Track 1 (2026-06-06), v2.3 (2026-06-09), v2.4 (2026-06-21), v2.5 (2026-06-22).*
