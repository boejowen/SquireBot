---
phase: 40-item-display-polish-flag-outlines-quest-links
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/theme/themes.ts
  - web/src/app.css
  - web/src/lib/__tests__/themes.test.ts
  - web/src/lib/flags.ts
  - web/src/lib/__tests__/flags.test.ts
  - web/src/lib/examine.ts
  - web/src/lib/__tests__/examine.test.ts
  - web/src/lib/tooltip/composeNotes.ts
  - web/src/lib/__tests__/composeNotes.test.ts
  - web/src/lib/components/PaperdollSlot.svelte
  - web/src/lib/components/ExaminePanel.svelte
  - web/src/lib/components/ItemTooltip.svelte
  - web/src/routes/inventory/+page.svelte
  - web/src/routes/wishlist/+page.svelte
autonomous: false
requirements: [ITEMUI-01, ITEMUI-02]

must_haves:
  truths:
    - "A flagged 62px tile (PaperdollSlot) shows a colored inset ring — No-Drop red, Lore gold, Magic blue — by the priority No-Drop > Lore > Magic; an unflagged tile keeps the existing neutral border."
    - "The flag ring stays visible while a tile is hovered/focused (it rides a ::before inset box-shadow, not border-color which hover flips to accent)."
    - "The flag outline is consistent across every PaperdollSlot surface (paperdoll, general grid, bank grid, bag sub-grids) because the ring rides the component."
    - "The examine panel shows a priority flag chip (NO-DROP / LORE / MAGIC) beside the item name, and lists the named quests an item is used in (Used in: ...) as clickable wiki links (notes_link only), on all three detail surfaces (Characters / Inventory / Wishlist)."
    - "The item tooltip (legacy view/bank grid) renders each named quest as a clickable link via source_url through safeHttpUrl; the generic QUEST ITEM / Quest item badge stays the fallback when only an in_game_flag entry exists."
    - "Three new per-theme flag tokens (--flag-nodrop/--flag-lore/--flag-magic) exist in BOTH app.css and themes.ts in lockstep, with parity tests asserting them; npm run check + npm test + npm run build all pass."
  artifacts:
    - path: "web/src/lib/flags.ts"
      provides: "Pure, node-testable priority flag resolver (flagColorVar + flagChipLabel) shared by PaperdollSlot + examine.ts (No-Drop > Lore > Magic)"
      contains: "is_no_drop"
    - path: "web/src/lib/theme/themes.ts"
      provides: "flagNodrop/flagLore/flagMagic on ThemeTokens + every theme entry"
      contains: "flagNodrop"
    - path: "web/src/app.css"
      provides: "--flag-nodrop/--flag-lore/--flag-magic in all five [data-theme] blocks"
      contains: "--flag-nodrop"
    - path: "web/src/lib/components/PaperdollSlot.svelte"
      provides: "the ::before inset flag ring keyed off --flag-color + the priority derive + class:flagged"
      contains: "--flag-color"
    - path: "web/src/lib/examine.ts"
      provides: "the flagchip + quests ExamineField kinds (priority chip + notes_link-only named quests)"
      contains: "notes_link"
    - path: "web/src/lib/tooltip/composeNotes.ts"
      provides: "clickable quest <a> via safeHttpUrl + QuestLinkForNote.source_url"
      contains: "tooltip-quest-link"
  key_links:
    - from: "web/src/lib/components/PaperdollSlot.svelte"
      to: "var(--flag-nodrop/lore/magic)"
      via: "$derived priority resolver → inline --flag-color → ::before box-shadow"
      pattern: "box-shadow: inset 0 0 0 2px var\\(--flag-color\\)"
    - from: "web/src/lib/examine.ts quests field"
      to: "slot.quest_links filtered to source === 'notes_link'"
      via: "examineFields pushes a 'quests' field with {quest_name, source_url}[]"
      pattern: "source === 'notes_link'"
    - from: "web/src/lib/tooltip/composeNotes.ts"
      to: "source_url through safeHttpUrl"
      via: "each notes_link name → <a href=escapeHtml(safeHttpUrl(source_url))>"
      pattern: "safeHttpUrl\\(l\\.source_url\\)"
    - from: "web/src/routes/inventory/+page.svelte asSlot"
      to: "selectedRow.held?.quest_links"
      via: "thread quest_links + the three flags onto the synthetic InventorySlot"
      pattern: "quest_links: selectedRow\\.held\\?\\.quest_links"
---

<objective>
Web (SvelteKit) half of Phase 40: render ITEMUI-01's flag-coded tile outline + examine flag chip and ITEMUI-02's clickable named quest links, consuming the additive JSON fields the backend (40-01) now ships. Adds three per-theme flag tokens (app.css + themes.ts in lockstep), a pure priority-flag resolver, the examine `flagchip` + `quests` fields, the `PaperdollSlot` `::before` ring, and clickable quest links in the examine panel + the legacy tooltip.

Purpose: make items readable at a glance (color-coded outline by attribute flag) and traceable to their quests (named, clickable "Used in:" links) on the modern tabs — the UI-SPEC contract.
Output: flagged tiles show a colored ring (priority No-Drop>Lore>Magic), the examine shows a flag chip + clickable named quests on Characters/Inventory/Wishlist, the tooltip's quest names become links. A browser-smoke checkpoint verifies the DOM render across all five themes (web vitest is DOM-blind).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-item-display-polish-flag-outlines-quest-links/40-CONTEXT.md
@.planning/phases/40-item-display-polish-flag-outlines-quest-links/40-UI-SPEC.md
@.planning/phases/40-item-display-polish-flag-outlines-quest-links/40-PATTERNS.md
@.planning/phases/40-item-display-polish-flag-outlines-quest-links/40-01-SUMMARY.md

<interfaces>
<!-- The 40-01 backend now ships these new JSON fields. Mirror them in api.ts. -->
<!-- InventorySlot + ItemRollup each gain: is_no_drop, is_lore, is_magic (boolean) + quest_links: QuestLink[] -->
<!-- QuestLink gains source_url: string -->

api.ts — the QuestLink interface (line 86) to extend:
```ts
export interface QuestLink {
	quest_name: string;
	source: 'in_game_flag' | 'notes_link';
	// ← ADD: source_url: string;
}
```
InventorySlot (line 177-197): append after `statsblock` (line 196) — `is_no_drop/is_lore/is_magic: boolean` + `quest_links: QuestLink[]`.
ItemRollup (line 237-253): the is_clicky/has_haste precedent is at lines 250-251 — append the three flags next to them + `quest_links: QuestLink[]`.
ViewRow.quest_links (line 107) is LEFT UNTOUCHED — its QuestLink element now carries source_url for free (legacy views ignore it).

themes.ts — ThemeTokens interface (line 31) + the 5 THEMES entries (line 53-116). Per-theme flag values are PRESCRIPTIVE (UI-SPEC §Color, lines 102-108):
| theme | flagNodrop | flagLore | flagMagic |
| velious | #e88a8a | #e3c46b | #6db3e0 |
| vanilla | #d96b6b | #d4af37 | #6fa0d4 |
| kunark | #d96b6b | #d4a020 | #5f9fd4 |
| minimalist | #d98a8a | #d4b15c | #7fa8d4 |
| heavy | #c0392b | #d4a017 | #3f78c0 |

app.css — the five [data-theme] blocks (line 33-113). --flag-nodrop/--flag-lore/--flag-magic per block, same per-theme hex. `:root` shares the velious block (line 33) so it gets velious values automatically.

PaperdollSlot.svelte — filled tile (line 90-125) + the style block. The hover/focus rule (line 149-158) flips border-color to accent + adds box-shadow — the flag MUST NOT use border-color. `style={`--tile-hue: ${hue};`}` (line 105) is where the inline --flag-color appends. `.slot.filled` is the gate; `.count`/`.bag-marker` corner markers sit inset-1px.

examine.ts — examineFields (line 65) builds the ordered ExamineField[]; ExamineField.kind union (line 23-30). The flags field (line 73-75) is the precedent for a conditional uppercase label. wikiHref (line 117) is the URL-derivation precedent. The order is name → flags → stats → notes → price → wiki → lastsynced; insert the flagchip after name, the quests field AFTER wiki and BEFORE lastsynced.

ExaminePanel.svelte — the {#each fields as f (f.kind)} switch (line 54-76); .ex-wiki {@html} sink (line 60-62); .ex-flags style (line 112-120); .ex-wiki :global(.tooltip-wiki-link) style (line 167-172). safeHttpUrl + wikiUrlFor already imported.

composeNotes.ts — QuestLinkForNote (line 42-45), the notes_link block (line 154-161), safeHttpUrl (line 69-72), escapeHtml (line 53-60). MAX_QUEST_LINKS_IN_NOTE=5 (line 47).

ItemTooltip.svelte — the .tooltip-popover :global(.tooltip-wiki-link) style (line 166-171) is the rule to mirror for .tooltip-quest-link. ItemCell passes row.quest_links (already ViewRow, now with source_url) — no call-site wiring change.

inventory/+page.svelte — asSlot $derived (line 164-185) constructs a synthetic InventorySlot from selectedRow.held (an ItemRollup). When InventorySlot gains the 4 new fields, this literal MUST set them or TS errors.
wishlist/+page.svelte — asSlot() function (line 276-299) constructs a synthetic InventorySlot from {name, price, wiki_url} (no rollup behind it). Same — must set the 4 new fields (flags false, quest_links []).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts contract mirror + three per-theme flag tokens (themes.ts + app.css lockstep) + parity tests</name>
  <files>web/src/lib/api.ts, web/src/lib/theme/themes.ts, web/src/app.css, web/src/lib/__tests__/themes.test.ts</files>
  <read_first>
    - web/src/lib/api.ts (the file being modified) — lines 79-108 (PriceDetail, QuestLink, ViewRow), 177-197 (InventorySlot), 237-253 (ItemRollup)
    - web/src/lib/theme/themes.ts (the file being modified) — full file (ThemeTokens line 31-51, the 5 THEMES entries 53-116)
    - web/src/app.css (the file being modified) — lines 26-113 (the header token-list comment + the 5 [data-theme] blocks)
    - web/src/lib/__tests__/themes.test.ts (the test being extended) — full file (REQUIRED_TOKENS line 16-27, the "all required tokens" loop line 52-59, the velious spot-check line 69-75)
    - .planning/phases/40-item-display-polish-flag-outlines-quest-links/40-UI-SPEC.md § Color (lines 84-110 — the per-theme flag-token table + the lockstep mandate)
    - .planning/phases/40-item-display-polish-flag-outlines-quest-links/40-PATTERNS.md § "themes.ts + app.css" and § "themes.test.ts"
  </read_first>
  <action>
    (A) **api.ts.** Add `source_url: string;` to the `QuestLink` interface (line 86-89, after `source`). To `InventorySlot` (line 177-197): append after `statsblock` (line 196) — `is_no_drop: boolean;`, `is_lore: boolean;`, `is_magic: boolean;`, `quest_links: QuestLink[];` (each with a one-line doc comment: `// item_master flag (00016); ITEMUI-01 tile outline` / `// notes_link named quests (ITEMUI-02)`). To `ItemRollup` (line 237-253): append after `has_haste` (line 251) — the same three booleans + `quest_links: QuestLink[];`. Leave `ViewRow.quest_links` (line 107) untouched.

    (B) **themes.ts.** Add three keys to the `ThemeTokens` interface (after `statusOther`, line 38): `flagNodrop: string;`, `flagLore: string;`, `flagMagic: string;` (with a `/** Phase 40 flag-outline tokens — No-Drop>Lore>Magic; WCAG ≥3:1 vs --panel. */` doc). Add the three values to EACH of the 5 `THEMES` entries using the PRESCRIPTIVE per-theme hex from the table in `<interfaces>` (velious `#e88a8a`/`#e3c46b`/`#6db3e0`, vanilla `#d96b6b`/`#d4af37`/`#6fa0d4`, kunark `#d96b6b`/`#d4a020`/`#5f9fd4`, minimalist `#d98a8a`/`#d4b15c`/`#7fa8d4`, heavy `#c0392b`/`#d4a017`/`#3f78c0`).

    (C) **app.css.** Add three custom props to EACH of the 5 `[data-theme]` blocks (velious/`:root` shared block line 33, vanilla 51, kunark 65, minimalist 79, heavy 93): `--flag-nodrop`, `--flag-lore`, `--flag-magic` with the SAME per-theme hex as themes.ts (lockstep — the values MUST match byte-for-byte). Update the header token-list comment (line 28-30) to append `--flag-nodrop --flag-lore --flag-magic`.

    (D) **themes.test.ts.** Add `'flagNodrop', 'flagLore', 'flagMagic'` to `REQUIRED_TOKENS` (line 16-27) — this makes the existing "all required tokens for every theme" parity loop (line 52-59) assert the new keys on all 5 themes. Update the test NAME at line 52 from "all 10 required tokens" to the new count (13) or de-hardcode the number. Add a velious spot-check it-block (mirror line 69-75) asserting `THEMES.velious.flagNodrop === '#e88a8a'`, `flagLore === '#e3c46b'`, `flagMagic === '#6db3e0'`. (themeApply.test.ts asserts the apply/persist wiring, NOT token shape — it needs NO change.)
  </action>
  <verify>
    <automated>cd web && npm test -- --run themes && npm run check 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'source_url' web/src/lib/api.ts` shows it on the QuestLink interface.
    - `grep -c 'quest_links: QuestLink\[\]' web/src/lib/api.ts` == 2 (InventorySlot + ItemRollup).
    - `grep -c 'flagNodrop' web/src/lib/theme/themes.ts` == 6 (1 interface + 5 themes).
    - `grep -c -- '--flag-nodrop' web/src/app.css` == 5 (one per [data-theme] block).
    - themes.test.ts REQUIRED_TOKENS contains `'flagNodrop'`, `'flagLore'`, `'flagMagic'` (grep each).
    - `cd web && npm test -- --run themes` passes (the parity loop now covers the 3 new tokens × 5 themes).
    - `cd web && npm run check` exits 0 (no TS errors — note: the synthetic asSlot literals are fixed in Task 3; if check fails ONLY on those two files, that is expected until Task 3 lands — run check again after Task 3).
  </acceptance_criteria>
  <done>api.ts mirrors the backend's new fields; the three flag tokens exist in both themes.ts and app.css with matching per-theme hex; the parity test asserts them and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Pure flag-priority resolver + examine.ts flagchip/quests fields + composeNotes clickable quest links (all node-tested)</name>
  <files>web/src/lib/flags.ts, web/src/lib/__tests__/flags.test.ts, web/src/lib/examine.ts, web/src/lib/__tests__/examine.test.ts, web/src/lib/tooltip/composeNotes.ts, web/src/lib/__tests__/composeNotes.test.ts</files>
  <read_first>
    - web/src/lib/examine.ts (the file being modified) — full file (the ExamineField kind union line 22-34, examineFields line 65-110, wikiHref line 117-123)
    - web/src/lib/tooltip/composeNotes.ts (the file being modified) — full file (QuestLinkForNote line 42-45, safeHttpUrl line 69-72, escapeHtml line 53-60, the notes_link block line 154-161)
    - web/src/lib/items.ts (the S-4 pure-helper precedent — facetItems, a node-tested pure flag predicate) — read the facetItems shape
    - web/src/lib/__tests__/examine.test.ts + composeNotes.test.ts (the tests being extended) — read the existing assertion shape (how an InventorySlot / QuestLinkForNote fixture is built; composeNotes' XSS-escaping assertions are the security precedent)
    - .planning/phases/40-item-display-polish-flag-outlines-quest-links/40-UI-SPEC.md § "Priority resolution (D-01)" (line 112-123), § "ITEMUI-01 Flag tint on the examine name heading" (line 196-206), § "ITEMUI-02 Named quest links" (line 208-226), § Copywriting (the exact strings, line 144-155)
    - .planning/phases/40-item-display-polish-flag-outlines-quest-links/40-PATTERNS.md § S-3 (safeHttpUrl link discipline), § S-4 (pure helper), § "examine.ts", § "composeNotes.ts + ItemTooltip"
  </read_first>
  <action>
    (A) **NEW web/src/lib/flags.ts — the pure priority resolver (S-4).** Export two pure functions keyed off the three flag booleans, in the LOCKED priority order No-Drop > Lore > Magic:
    ```ts
    // The three flag fields are present on InventorySlot (and ItemRollup); accept a minimal shape.
    export interface FlagFlags { is_no_drop?: boolean; is_lore?: boolean; is_magic?: boolean; }
    /** The priority CSS var string for the flag ring/chip color, '' when no flag (D-01). */
    export function flagColorVar(f: FlagFlags | null | undefined): string {
      if (!f) return '';
      if (f.is_no_drop) return 'var(--flag-nodrop)';
      if (f.is_lore)    return 'var(--flag-lore)';
      if (f.is_magic)   return 'var(--flag-magic)';
      return '';
    }
    /** The priority chip label NO-DROP/LORE/MAGIC, '' when no flag. */
    export function flagChipLabel(f: FlagFlags | null | undefined): string {
      if (!f) return '';
      if (f.is_no_drop) return 'NO-DROP';
      if (f.is_lore)    return 'LORE';
      if (f.is_magic)   return 'MAGIC';
      return '';
    }
    ```
    **NEW flags.test.ts** — assert: No-Drop wins over Lore+Magic (returns `var(--flag-nodrop)` / `NO-DROP`); Lore wins over Magic when no No-Drop; Magic when only Magic; `''` when none / null / undefined. Mirror the items.test.ts table-test shape.

    (B) **examine.ts.** Extend the `kind` union (line 22-30) with `'flagchip'` and `'quests'`. Extend `ExamineField` with an optional `quests?: { quest_name: string; source_url: string }[]` (the `href?` optional at line 33 is the precedent for a kind-specific optional). In `examineFields`: import `flagColorVar`, `flagChipLabel` from `./flags`. (1) **Flag chip** — after the name push (line 69), compute `const chip = flagChipLabel(slot);` and `if (chip) fields.push({ kind: 'flagchip', text: chip });` (the priority chip; the existing QUEST ITEM flags field at line 73-75 stays unchanged — additive). (2) **Named quests** — AFTER the wiki push (line 100) and BEFORE the lastsynced push (line 104), add: `const named = (slot.quest_links ?? []).filter((q) => q.source === 'notes_link'); if (named.length > 0) fields.push({ kind: 'quests', text: 'Used in:', quests: named.map((q) => ({ quest_name: q.quest_name, source_url: q.source_url })) });` — the `notes_link`-only filter (D-06; NEVER the in_game_flag `[in-game QUEST flag]` pseudo-name) + the graceful-omission guard (D-09: no field when zero named quests).

    **examine.test.ts** — assert: a slot with `is_no_drop` produces a `flagchip` field with text `NO-DROP`; a slot with `quest_links` containing a `notes_link` entry produces a `quests` field positioned after `wiki` and before `lastsynced`, carrying `{quest_name, source_url}`; a slot whose quest_links are ALL `in_game_flag` produces NO `quests` field (graceful omission, D-06/D-09). Mirror the existing order/omission assertions.

    (C) **composeNotes.ts.** Add `source_url: string;` to `QuestLinkForNote` (line 42-45). In the notes_link block (line 154-161): change the name-mapping to emit a clickable `<a>` via S-3 when a URL is present, plain escaped text otherwise:
    ```ts
    const names = noteLinks.map((l) => {
      const url = safeHttpUrl(l.source_url);
      const name = escapeHtml(l.quest_name);
      return url
        ? `<a class="tooltip-quest-link" href="${escapeHtml(url)}" target="_blank" rel="noopener">${name}</a>`
        : name; // plain escaped text when no/blocked URL (D-05 fallback)
    }).join(', ');
    parts.push(`<p class="tooltip-quests">Used in quests: ${names}</p>`);
    ```
    The `notes_link`-only filter, the `MAX_QUEST_LINKS_IN_NOTE=5` cap, the `Used in quests:` label, and the `escapeHtml` discipline are UNCHANGED.

    **composeNotes.test.ts** — extend the existing tests: a notes_link quest WITH a valid http(s) source_url renders an `<a class="tooltip-quest-link" href=...>`; a notes_link quest with a `javascript:`/blank source_url renders the name as PLAIN escaped text (no `<a>`) — the safeHttpUrl scheme allow-list (the security assertion, mirroring the existing wiki-link XSS test); a malicious quest_name like `<img onerror=...>` is escaped to inert text inside the link. The in_game_flag-only case still emits `Quest item` (unchanged).
  </action>
  <verify>
    <automated>cd web && npm test -- --run "flags|examine|composeNotes" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/flags.ts` exists and exports `flagColorVar` + `flagChipLabel` (grep both).
    - flags.test.ts asserts No-Drop > Lore > Magic priority (grep `var(--flag-nodrop)` AND `NO-DROP` in the test).
    - examine.ts kind union contains `'flagchip'` and `'quests'` (grep both); examineFields contains `source === 'notes_link'` (grep).
    - examine.test.ts asserts a `quests` field is omitted for in_game_flag-only links (grep `in_game_flag` in the test).
    - composeNotes.ts QuestLinkForNote contains `source_url` (grep); the notes_link block contains `safeHttpUrl(l.source_url)` and `tooltip-quest-link` (grep both).
    - composeNotes.test.ts asserts a blocked/blank source_url renders NO `<a>` (the safeHttpUrl fallback assertion — grep `tooltip-quest-link` in the test).
    - `cd web && npm test -- --run "flags|examine|composeNotes"` passes.
  </acceptance_criteria>
  <done>The pure flag resolver + the examine flagchip/quests fields + the clickable composeNotes quest links all exist and are node-tested (priority order, notes_link filter, safeHttpUrl fallback, XSS escaping); web vitest is green for these logic units. The DOM render is verified in Task 3's checkpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Component renders — PaperdollSlot ring, ExaminePanel flagchip+quests branches, ItemTooltip link style, synthetic asSlot builders</name>
  <files>web/src/lib/components/PaperdollSlot.svelte, web/src/lib/components/ExaminePanel.svelte, web/src/lib/components/ItemTooltip.svelte, web/src/routes/inventory/+page.svelte, web/src/routes/wishlist/+page.svelte</files>
  <read_first>
    - web/src/lib/components/PaperdollSlot.svelte (the file being modified) — full file (the $derived block 45-74, the filled button + style attr 90-106, the .slot/.slot.filled:hover/.count/.bag-marker styles 134-226)
    - web/src/lib/components/ExaminePanel.svelte (the file being modified) — full file (the {#each} switch 54-76, the .ex-flags style 112-120, the .ex-wiki :global link style 164-172, the safeHttpUrl/wikiUrlFor imports)
    - web/src/lib/components/ItemTooltip.svelte (the file being modified) — the .tooltip-popover :global(.tooltip-wiki-link) style (line 166-171)
    - web/src/routes/inventory/+page.svelte (the file being modified) — the asSlot $derived (line 156-185) constructing a synthetic InventorySlot from selectedRow.held
    - web/src/routes/wishlist/+page.svelte (the file being modified) — the asSlot() function (line 272-299) constructing a synthetic InventorySlot
    - web/src/lib/flags.ts (created in Task 2 — flagColorVar) and web/src/lib/examine.ts (the flagchip/quests fields from Task 2)
    - .planning/phases/40-item-display-polish-flag-outlines-quest-links/40-UI-SPEC.md § "ITEMUI-01 Flag outline on the 62px tile" (the prescriptive ::before CSS, lines 161-194), § "Flag tint on the examine name heading" (the chip treatment, 196-206), § "ITEMUI-02 Named quest links" (the examine + tooltip render, 208-226)
    - .planning/phases/40-item-display-polish-flag-outlines-quest-links/40-PATTERNS.md § "PaperdollSlot.svelte" (the derive + ::before excerpt), § "ExaminePanel.svelte", § "composeNotes.ts + ItemTooltip"
  </read_first>
  <action>
    (A) **PaperdollSlot.svelte (ITEMUI-01 ring, D-01/D-03).** Import `flagColorVar` from `$lib/flags`. Add a derive next to `hue` (line 59): `let flagColor = $derived(filled && slot ? flagColorVar(slot) : '');`. On the filled `<button>` (line 91-106): add `class:flagged={flagColor !== ''}` and change the inline style to append the var conditionally: `style={`--tile-hue: ${hue};${flagColor ? ` --flag-color: ${flagColor};` : ''}`}`. In `<style>`, add the ring rule (the UI-SPEC prescriptive CSS, lines 172-184) — it MUST NOT touch border-color:
    ```css
    .slot.filled.flagged::before {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: 3px;                       /* match .slot */
      box-shadow: inset 0 0 0 2px var(--flag-color);
      pointer-events: none;                      /* never steal hover/click */
      z-index: 1;                                /* above .ico, below .count/.bag-marker */
    }
    ```
    Leave the resting `.slot` border (line 139), the `.slot.filled:hover/:focus-visible` accent rules (149-158), and the reduced-motion block (218-226) UNCHANGED — the ring coexists with the accent border/box-shadow (D-03). The ring rides the component so it inherits to every InventoryWindow grid (paperdoll/general/bank/bags) for free — no call-site change.

    (B) **ExaminePanel.svelte (the flagchip + quests branches).** Add two branches to the `{#each fields as f (f.kind)}` switch (line 54-76). After the `name` branch, add `{:else if f.kind === 'flagchip'}` rendering a bordered transparent chip colored by `--flag-color` (the priority value — derive it once in the script from `slot` via `flagColorVar(slot)` and pass inline): `<span class="ex-flagchip" style={`--flag-color: ${flagChipColor}`}>{f.text}</span>`. For `quests`, add `{:else if f.kind === 'quests'}` rendering `Used in: ` (static label) + the comma-joined `<a>`s. Use the NATIVE Svelte path (S-3 option 2 — no `{@html}` needed): `{#each f.quests as q, i}{#if i > 0}, {/if}{#if safeHttpUrl(q.source_url)}<a class="tooltip-wiki-link" href={safeHttpUrl(q.source_url)} target="_blank" rel="noopener">{q.quest_name}</a>{:else}{q.quest_name}{/if}{/each}` — Svelte auto-escapes `{q.quest_name}`; the href passes `safeHttpUrl` FIRST (D-05). Add styles: `.ex-flagchip` (mirror `.ex-flags` line 112-120 but `color: var(--flag-color)`, a `1px solid var(--flag-color)` border, `border-radius: 3px`, transparent fill, `padding: 1px 6px`, `display: inline-block`) and reuse the existing `.ex-wiki :global(.tooltip-wiki-link)` look for the quest `<a>` (add an `.ex-quests` wrapper + a `.ex-quests a { color: var(--accent); border-bottom: 1px solid var(--accent); text-decoration: none; }` rule mirroring line 167-172). Add `.ex-flagchip`/`.ex-quests` to the overflow-wrap group if the run-on guard (line 121-132) applies.

    (C) **ItemTooltip.svelte (the link style).** Add a `.tooltip-popover :global(.tooltip-quest-link)` rule mirroring `.tooltip-wiki-link` (line 166-171): `color: var(--accent); border-bottom: 1px solid var(--accent); text-decoration: none;`. No script/prop change — `composeItemNote` (Task 2) already emits the `<a class="tooltip-quest-link">`; ItemCell already passes `row.quest_links` (ViewRow, now with source_url). Confirm `QuestLinkForNote` carrying `source_url` is satisfied by the ViewRow QuestLink shape (it is — source_url is now on the api.ts QuestLink).

    (D) **The two synthetic asSlot builders (TS-required by the new InventorySlot fields).** In `inventory/+page.svelte` asSlot (line 164-185): add to the object literal `is_no_drop: selectedRow.held?.is_no_drop ?? false, is_lore: selectedRow.held?.is_lore ?? false, is_magic: selectedRow.held?.is_magic ?? false, quest_links: selectedRow.held?.quest_links ?? []` — so the Inventory-tab examine surfaces the held item's flags + named quests (D-04). In `wishlist/+page.svelte` asSlot() (line 281-298): add `is_no_drop: false, is_lore: false, is_magic: false, quest_links: []` — wishlist targets/suggestions carry no rollup, so safe defaults satisfy the type (the examine simply omits the chip + quests, D-09). These edits also clear any `npm run check` errors Task 1 deferred.
  </action>
  <verify>
    <automated>cd web && npm run check && npm test -- --run && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PaperdollSlot.svelte contains `class:flagged` AND `box-shadow: inset 0 0 0 2px var(--flag-color)` (grep both); it does NOT add `--flag-color` to any `border-color` rule (grep `border-color` shows only the existing accent hover/focus rules).
    - ExaminePanel.svelte contains `f.kind === 'flagchip'` AND `f.kind === 'quests'` AND `safeHttpUrl(q.source_url)` (grep all three).
    - ItemTooltip.svelte contains `:global(.tooltip-quest-link)` (grep).
    - inventory/+page.svelte asSlot literal contains `quest_links: selectedRow.held?.quest_links` (grep); wishlist/+page.svelte asSlot contains `quest_links: []` (grep).
    - `cd web && npm run check` exits 0 (the synthetic-slot literals now satisfy the extended InventorySlot type).
    - `cd web && npm test -- --run` passes (full web suite green).
    - `cd web && npm run build` exits 0.
  </acceptance_criteria>
  <done>The flag ring renders on flagged PaperdollSlot tiles (coexisting with hover/focus accent), the examine panel shows the flag chip + clickable named quests, the tooltip styles the quest links, and both synthetic asSlot builders carry the new fields. npm check/test/build all green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Browser-smoke the flag outlines + named quest links across themes (web vitest is DOM-blind)</name>
  <what-built>
    The flag-coded tile outline (ITEMUI-01) + the examine flag chip + the clickable named quest links (ITEMUI-02). web vitest is DOM-blind (no @testing-library/svelte) — the priority resolver, the notes_link filter, and the safeHttpUrl fallback are node-tested (Task 2), but the actual DOM render of the ::before ring, the chip, and the quest <a>s is a browser-only gap (the web-tests-node-only discipline). Deploy-then-smoke-on-prod OR a full local stack (local backend + seeded session) per the established web-smoke memory — localhost dev cannot auth against prod.
  </what-built>
  <how-to-verify>
    1. Deploy backend (40-01) + web (40-02) together to prod (Windows ssh-agent SERVICE key + PowerShell ssh.exe/scp.exe to root@5.78.232.85; R2 backup first; web §7.5 atomic swap; NO migration this phase — goose is a no-op at the existing schema version; verify apex 200, JS text/javascript, API 401-not-404). NOTE: catalog/quest data already exists in prod; the P37 flags backfill already ran live (16/953 item_master rows flagged 2026-06-25), so flagged items + named quests are present to smoke immediately.
    2. **ITEMUI-01 tile outline:** Open the Characters tab, select a character holding a No-Drop item (e.g. most epic/raid gear) — confirm its 62px tile shows a RED inset ring; a Lore item a GOLD ring; a Magic item a BLUE ring; an item with none keeps the neutral border. Confirm priority: an item that is BOTH No-Drop and Magic shows RED (No-Drop wins). Confirm the ring stays visible on HOVER and on keyboard FOCUS (the accent border/box-shadow appears AROUND it — both visible, D-03). Confirm the ring does not clip the stack-count or bag (⊞) corner glyphs.
    3. **Consistency (SC-2):** Confirm the same item reads the same way in the general inventory grid, an opened bag sub-grid, and the Banks tab grid.
    4. **Theme sweep:** Cycle all five themes (Settings → Theme: velious/vanilla/kunark/minimalist/heavy) and confirm each flag color is clearly distinguishable against the tile and clears the eye-test for ≥3:1 contrast (heavy's dark tiles especially).
    5. **Examine flag chip:** With a flagged item pinned in the examine panel, confirm a bordered uppercase chip (NO-DROP / LORE / MAGIC) appears beside the item name in the priority color, AND the full stat block below still lists all flags.
    6. **ITEMUI-02 named quests — examine (all 3 tabs):** Pin a known quest item (one with a real notes_link quest — e.g. a Coldain Ring turn-in item) on the Characters tab → confirm a `Used in: <quest name>` row with the quest name as a clickable accent link that opens the P1999 wiki page in a new tab. Repeat on the Inventory tab (held item) and the Wishlist tab examine. Confirm an item that is a quest item with ONLY the in-game flag (no named quest) shows the generic QUEST ITEM badge and NO `Used in:` row (never the literal `[in-game QUEST flag]`).
    7. **ITEMUI-02 named quests — tooltip (legacy view/bank grid):** On a surface using the item tooltip (the legacy view/bank ItemCell), hover a quest item with a named quest → confirm `Used in quests: <name>` where the name is a clickable link; confirm the generic `Quest item` line still shows for in_game_flag-only items.
  </how-to-verify>
  <resume-signal>Type "approved" if all flag colors, the chip, the hover/focus coexistence, the theme sweep, and the clickable named quests render correctly on all three tabs + the tooltip; otherwise describe the issue (theme, surface, item).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server JSON → DOM render | `quest_name` (wiki-/user-sourced string) + `source_url` (wiki-harvested URL) cross into the page on the examine panel + the tooltip. These are the XSS/HTML-injection surface of this phase. |
| theme attribute → CSS var | the new `--flag-*` tokens resolve per `[data-theme]`; no user string reaches CSS (the resolver returns only the fixed literal var names `var(--flag-nodrop|lore|magic)`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | Injection (XSS via quest name) | `examine.ts`/`ExaminePanel` + `composeNotes.ts`/`ItemTooltip` quest names | mitigate | Two render paths, both escape: the examine `quests` field renders quest names via NATIVE Svelte `{q.quest_name}` interpolation (auto-escaped — no `{@html}`); the tooltip path renders via `escapeHtml(quest_name)` inside the single sanctioned `composeItemNote` `{@html}` sink. A malicious `<img onerror=...>` quest name renders as inert text on both. composeNotes.test.ts asserts this (the existing XSS test precedent). ASVS L1 V5.3 output-encoding satisfied. |
| T-40-05 | Injection (XSS via href / scheme abuse) | the `source_url` on every quest `<a href>` | mitigate | EVERY quest href passes `safeHttpUrl` (the http/https scheme allow-list — blocks `javascript:`/`data:`/`vbscript:`/protocol-relative) BEFORE render, identical to the existing wiki-link guard. The examine native path uses `href={safeHttpUrl(q.source_url)}`; the tooltip path uses `escapeHtml(safeHttpUrl(l.source_url))`. A rejected scheme → plain text, no `<a>` (D-05 fallback). All quest `<a>`s carry `rel="noopener" target="_blank"` (reverse-tabnabbing). flags.test.ts/composeNotes.test.ts assert the fallback. |
| T-40-06 | Tampering (CSS-var injection via flag color) | the inline `--flag-color` on PaperdollSlot / the examine chip | mitigate | The component sets `--flag-color` ONLY to one of three fixed literal `var(--flag-*)` strings returned by the pure `flagColorVar` resolver — never an item-derived or user string. No untrusted value reaches a `style=` sink. The flag booleans driving the choice are server-derived integers (T-40-03 in 40-01). |
| T-40-07 | Spoofing (misleading No-Drop signal) | a tile that should show No-Drop showing nothing | accept | A pre-00016/un-backfilled item resolves all flags false → neutral tile (no ring). This is graceful degradation, not a security failure — the examine stat block still shows the wiki flags as text (WCAG 1.4.1 redundant encoding), and the worst case is a missing visual hint, never a wrong-color one (priority is deterministic). |
</threat_model>

<verification>
- `cd web && npm run check` (0 TS errors), `npm test -- --run` (full suite green incl. the new flags/examine/composeNotes/themes tests), `npm run build` (0 errors).
- The new flag tokens are in lockstep: `app.css` and `themes.ts` carry the same per-theme hex (the parity test asserts shape; the hex match is a code-review check).
- The flag ring uses `::before` box-shadow, NOT border-color (so hover/focus accent coexists — D-03).
- Every quest href passes `safeHttpUrl`; every quest name is escaped (native Svelte or escapeHtml).
- The browser-smoke checkpoint (Task 4) confirms the DOM render across all five themes + all three detail surfaces + the tooltip — the node-blind gap.
</verification>

<success_criteria>
- SC-1: flagged inventory/bank/paperdoll tiles show a color-coded outline (No-Drop red / Lore gold / Magic blue; none = neutral border).
- SC-2: the outline is consistent across every PaperdollSlot tile surface.
- SC-3: the modern Characters / Inventory / Wishlist examine surfaces show the named quests an item is used in (clickable), not just the yes/no badge.
- SC-4: named quest links surface in the examine panel AND the list/grid tooltip, drawing on the already-harvested `quest_links` (no new pipeline); the watcher is untouched; `go test ./...` (40-01) + `npm test`/`npm run build` (40-02) green.
</success_criteria>

<output>
After completion, create `.planning/phases/40-item-display-polish-flag-outlines-quest-links/40-02-SUMMARY.md`
</output>
