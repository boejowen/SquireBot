---
phase: 31-characters-tab-in-game-inventory-window
plan: 04
type: execute
wave: 4
depends_on: ["31-03"]
files_modified:
  - web/src/lib/examine.ts
  - web/src/lib/__tests__/examine.test.ts
  - web/src/lib/components/PaperdollSlot.svelte
  - web/src/lib/components/ExaminePanel.svelte
  - web/src/lib/components/InventoryWindow.svelte
  - web/src/routes/characters/+page.svelte
autonomous: false
requirements: [INV-01, INV-02, INV-03, INV-04]
user_setup: []
must_haves:
  truths:
    - "Selecting a character renders an in-game-style window: 23-slot paperdoll + general grid + bank grid below, stacked slots show count"
    - "Item icons load from wiki Item_<iconId>.png with a colored-tile fallback when the icon is missing or fails"
    - "A general/bank bag (Slots>0) expands INLINE to reveal Children that behave like the grid (no modal)"
    - "Hovering an item shows a transient examine preview; clicking pins the full examine (single, replace-on-click) in the D-08 order; missing fields are omitted"
    - "Deployed to prod + browser-smoked: list order, search priority, window render, hover/pin, inline bag, stacks, icon+fallback, examine omission, no-inventory state, all 5 themes"
  artifacts:
    - path: "web/src/lib/components/InventoryWindow.svelte"
      provides: "generic prop-driven window over CharacterInventory (reused by P33)"
      min_lines: 60
    - path: "web/src/lib/components/PaperdollSlot.svelte"
      provides: "one slot tile: icon + onerror colored-tile fallback + count + bag marker"
    - path: "web/src/lib/examine.ts"
      provides: "pure examine field-omission + D-08 ordering (node-testable)"
      contains: "examineFields"
  key_links:
    - from: "web/src/routes/characters/+page.svelte"
      to: "web/src/lib/components/InventoryWindow.svelte"
      via: "fetchInventory(selected) → <InventoryWindow inventory={inv}>"
      pattern: "InventoryWindow"
    - from: "web/src/lib/components/PaperdollSlot.svelte"
      to: "wiki.project1999.com/images/Item_<iconId>.png"
      via: "<img onerror> colored-tile fallback"
      pattern: "Item_\\$\\{|onerror"
    - from: "web/src/lib/components/ExaminePanel.svelte"
      to: "composeItemNote/escapeHtml"
      via: "the single sanctioned {@html} sink"
      pattern: "composeItemNote|escapeHtml"
---

<objective>
Render the in-game inventory window (INV-01..04): the 23-slot paperdoll, the general + bank grids
(one renderer, D-05), the INLINE bag drill-down (D-04, NOT a modal), real wiki icons with the
colored-tile fallback (INV-04/D-02), and the hover-preview + click-to-pin examine (INV-02/D-06/D-07)
in the locked D-08 order with graceful field omission (D-09). Then DEPLOY to prod and BROWSER-SMOKE
— node vitest is DOM-blind, so the window's correctness is only provable in a real browser.

Purpose: This is the visible payoff of the phase and the component Phase 33 reuses per bank toon +
Phase 34's equipped-slot source. It must be a generic, prop-driven component over
`CharacterInventory`, not a Characters-tab-only widget.

Output: a pure `examine.ts` (D-08 ordering + omission, node-tested), `PaperdollSlot.svelte`,
`ExaminePanel.svelte`, `InventoryWindow.svelte`, the window wired into the `/characters` page, and
a deploy + browser-smoke checkpoint.

NOTE: this plan has a human-verify checkpoint (the browser-smoke) — `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-characters-tab-in-game-inventory-window/31-UI-SPEC.md
@.planning/phases/31-characters-tab-in-game-inventory-window/31-PATTERNS.md
@.planning/phases/31-characters-tab-in-game-inventory-window/31-03-SUMMARY.md
@web/src/lib/components/ItemTooltip.svelte
@web/src/lib/tooltip/composeNotes.ts
@docs/backend-deploy.md

<interfaces>
<!-- Extracted from the codebase — use directly. -->

The ONE sanctioned {@html} sink (web/src/lib/tooltip/composeNotes.ts, verified):
```ts
export function escapeHtml(s: string): string  // & < > " ' → entities
export function safeHttpUrl(url: string): string  // returns the url only if absolute http(s), else ''
export function wikiUrlFor(itemName: string): string  // https://wiki.project1999.com/<Item_Name>
export function composeItemNote(itemName, wikiUrl, summary, prices, questLinks): string  // escaped HTML body
```

The hover/tap/Esc/outside-dismiss popover mechanics to reuse (web/src/lib/components/ItemTooltip.svelte:49-111):
```ts
let open = $state(false);
$effect(() => {
  if (!open) return;
  function onPointerDown(e) { if (triggerEl && !triggerEl.contains(e.target)) hide(); }
  function onKey(e) { if (e.key === 'Escape') hide(); }
  window.addEventListener('pointerdown', onPointerDown, true);
  window.addEventListener('keydown', onKey);
  return () => { /* remove both */ };
});
```

The TS contract (from Plan 31-03 api.ts):
```ts
InventorySlot { location, category, canonical_slot, item, id, count, slots, price, last_listed,
                wiki_url, wiki_summary, is_quest_item, prices, children, icon_id }
CharacterInventory { char, last_seen, equipment[], general[], bank[] }
```

The 23 canonical equipment slots (internal/backendsrv/compute/slotconst.go — authoritative, NOT 21):
Charm, Head, Face, Ear1, Ear2, Neck, Shoulders, Arms, Back, Wrist1, Wrist2, Range, Hands,
Primary, Secondary, Finger1, Finger2, Chest, Legs, Feet, Waist, Power, Ammo.

The colored-tile fallback (31-RESEARCH §"Code Examples" / 31-UI-SPEC §Color, LOCKED D-02):
```svelte
<img src={`https://wiki.project1999.com/images/Item_${slot.icon_id}.png`} alt=""
     style="image-rendering: pixelated; object-fit: contain;"
     onerror={(e) => (e.currentTarget.style.display = 'none')} />
<!-- icon_id == 0 (or null): skip the <img>; render the deterministic hsl(hue 45% 30%)→hsl(hue+40 40% 18%) gradient tile keyed on item name/id. -->
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pure examine.ts (D-08 order + D-09 omission) + PaperdollSlot + ExaminePanel</name>
  <files>web/src/lib/examine.ts, web/src/lib/__tests__/examine.test.ts, web/src/lib/components/PaperdollSlot.svelte, web/src/lib/components/ExaminePanel.svelte</files>
  <read_first>
    - Files being modified: NEW. PaperdollSlot/ExaminePanel mirror web/src/lib/components/ItemTooltip.svelte (the popover mechanics + the single {@html} sink + theme-token styling at :113-182) and web/src/lib/tooltip/composeNotes.ts (escapeHtml/safeHttpUrl/wikiUrlFor/composeItemNote). examine.ts mirrors web/src/lib/myview.ts (pure, DOM-free, node-testable).
    - Closest analog (PATTERNS.md): ExaminePanel ← ItemTooltip + composeNotes; PaperdollSlot ← the §E slot-tile contract + the D-02 colored-tile fallback; examine.ts ← myview.ts (pure helper precedent).
    - Source of truth: 31-UI-SPEC.md §E (slot tile geometry: 62px, icon inset, count badge top-right 13px, bag marker top-left accent, empty-slot label, filled-slot hover/focus), §G (examine interaction + the LOCKED D-08 content/order list of 12 items), §Color (colored-tile fallback, accent reserved list), §I (a11y: slot=button, aria-label, focus-visible, reduced motion); 31-RESEARCH.md Pitfall 2 (last_synced = CharacterInventory.last_seen, NOT slot.last_listed).
  </read_first>
  <action>
    1. **`web/src/lib/examine.ts`** (pure, DOM-free, node-testable) — compute the D-08-ordered, D-09-omitted examine field list from an `InventorySlot` + the char `last_seen`. Return an ordered array of `{ label, value }` (or typed variants) so the panel renders deterministically and the test asserts the ORDER + OMISSION without a DOM:
    ```ts
    import type { InventorySlot } from './api';
    export interface ExamineField { kind: 'name'|'flags'|'slot'|'dmgdly'|'ac'|'stats'|'wtsize'|'classrace'|'price'|'wiki'|'lastsynced'; text: string; href?: string; }
    /** Build the examine fields in the LOCKED D-08 order, omitting any field with no data (D-09).
     *  Always includes the name. `last_seen` is the per-CHARACTER value (NOT slot.last_listed). */
    export function examineFields(slot: InventorySlot, charLastSeen: string): ExamineField[] { ... }
    ```
    The D-08 order (omit a field entirely when its source is empty — never render blank/"null"):
    1 name (always) · 2 flags (from wiki_summary/is_quest_item-derived; omit if none) · 3 slot/skill (canonical_slot) · 4 DMG/DLY (weapons only; omit otherwise) · 5 AC (omit if none) · 6 stats (omit if none) · 7 wt/size (omit if none) · 8 class/race (omit if none) · 9 PigParse price (`price` → "PigParse: {price}", omit when `price === null`) · 10 wiki link (wiki_url || wikiUrlFor(item)) · 11 last-synced ("Last synced: {charLastSeen}", omit when "").
    Parse what's available from `wiki_summary` pragmatically — if the stored summary doesn't expose discrete AC/stat fields, render the summary as the "stats" block and omit the structured rows (D-09: show what's known, never fabricate). Keep it pure — no DOM, no fetch.

    2. **`web/src/lib/__tests__/examine.test.ts`** (node vitest): assert (a) name is ALWAYS first and present even for a bare slot; (b) a slot with `price === null` omits the price field entirely (no "null", no empty row); (c) `charLastSeen === ''` omits last-synced; (d) the relative ORDER of present fields matches D-08; (e) last-synced uses the passed `charLastSeen`, NOT `slot.last_listed` (pass distinct values and assert the right one wins).

    3. **`web/src/lib/components/PaperdollSlot.svelte`** (§E) — a prop-driven tile (`slot: InventorySlot`, `label?: string` for empty equipment slots, `onpin`, `onopen` callbacks). 62px×62px, `background: var(--panel)`, `border: 1px solid var(--border)`, `border-radius: 3px`, `overflow: hidden`, relative. Filled slot = `<button type="button" aria-label="{item}, count {count}">`; empty equipment slot = a non-interactive element showing the slot LABEL (13px Label uppercase dimmed) — NOT focusable. Icon: inner `.ico` inset ~6–8px holding the `<img Item_${icon_id}.png>` with `image-rendering: pixelated; object-fit: contain` over a deterministic `hsl()` colored-gradient under-layer keyed on `item`/`id`; `onerror` hides the `<img>` so the tile shows through; when `icon_id === 0` skip the `<img>` entirely (D-02). Count badge top-right, 13px tabular-nums, shown only when `count > 1`. Bag marker (`⊞` or `Package` glyph) top-left in `var(--accent)` when `slots > 0` + `aria-expanded` bound to the open state. Filled-slot hover/focus → `border-color: var(--accent)` + 1px accent box-shadow + `transform: scale(1.04)` (honors `prefers-reduced-motion`). `:focus-visible outline: 2px solid var(--accent)`. Tokens ONLY; the ONLY non-token color is the locked colored-tile `hsl()` fallback.

    4. **`web/src/lib/components/ExaminePanel.svelte`** (§G/§D-08) — the pinned panel: `border: 1px solid var(--border)`, `border-radius: 6px`, `background: var(--panel)`, `padding: 16px`, `position: sticky`, `top: ~60px`, `min-height: 200px`. Props: `slot: InventorySlot | null`, `charLastSeen: string`. When `slot === null` render the dimmed empty prompt `"Click an item to see its details — name, stats, PigParse price, wiki link, last-synced."`. When set, render the `examineFields(slot, charLastSeen)` rows in order. **The ONE sanctioned {@html} sink:** any HTML body MUST go through `composeItemNote`/`escapeHtml`; the wiki href via `safeHttpUrl(slot.wiki_url || wikiUrlFor(slot.item))`. Item names render via plain `{}` (auto-escaped) or the escaped composer — NEVER a raw `{@html}` of any wiki/price/name string. Name in `var(--accent)` Heading; flags in `var(--status-other)`; the highlighted-stats line in `var(--status-ok)`; price `font-variant-numeric: tabular-nums`; wiki link accent + 1px accent underline; footer dimmed.
  </action>
  <verify>
    <automated>cd web && npm test -- examine && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/examine.ts` exports `examineFields`; imports a TYPE-only from `./api` (no .svelte import).
    - `cd web && npm test -- examine` exits 0; the name-always-first, price-omit-on-null, last-synced-uses-charLastSeen-not-last_listed cases pass.
    - `web/src/lib/components/ExaminePanel.svelte` contains `composeItemNote` OR `escapeHtml` and `safeHttpUrl`; grep finds NO `{@html}` of a raw price/name/wiki string (the only `{@html}` is the composed escaped body).
    - `web/src/lib/components/PaperdollSlot.svelte` contains `Item_${` and `onerror` and `aria-label` and `var(--accent)`; grep finds no literal hex color outside the locked `hsl()` fallback + comments.
    - `cd web && npm run check` → 0 errors for the new files.
  </acceptance_criteria>
  <done>The pure examine helper is node-tested green; the slot tile (icon+fallback+count+bag marker, a11y) and the pinned examine panel (D-08 order, single escaped {@html} sink) exist on theme tokens.</done>
</task>

<task type="auto">
  <name>Task 2: InventoryWindow (paperdoll + grids + inline bag expand + hover/pin) wired into /characters</name>
  <files>web/src/lib/components/InventoryWindow.svelte, web/src/routes/characters/+page.svelte</files>
  <read_first>
    - Files being modified: NEW InventoryWindow.svelte; web/src/routes/characters/+page.svelte (the window-slot region from Plan 31-03 to replace with `<InventoryWindow>`). Reuse web/src/lib/components/ItemTooltip.svelte (hover/tap/Esc/outside-dismiss for the transient preview) + the PaperdollSlot/ExaminePanel from Task 1.
    - Closest analog (PATTERNS.md): InventoryWindow ← ItemTooltip mechanics + StateBlock states; the §D/§E/§F/§G/§H layout is fully specified in 31-UI-SPEC.
    - Source of truth: 31-UI-SPEC.md §D (window structure top-to-bottom, ONE scroll, no Inventory/Bank toggle), §E (paperdoll 3-column arrangement + the bottom WORN row + the 23-slot placement), §F (general + bank grids reuse ONE renderer; INLINE bag expand — NOT a modal — with the bag sub-header + aria-expanded + re-click collapse), §G (hover preview + click-to-pin single replace-on-click; a bag click OPENS not pins), §H (responsive two-pane → stack), §J/§K (window loading/error + no-inventory state); 31-RESEARCH.md Pitfall 5 (inline expand is the biggest delta from the sketch modal — NO #bagscrim) + Pitfall 6 (render all 23 slots).
  </read_first>
  <action>
    1. **`web/src/lib/components/InventoryWindow.svelte`** — a GENERIC, prop-driven component: `inventory: CharacterInventory` (NO Characters-tab-only assumptions — Phase 33 reuses it per bank toon). Layout (§D, one continuous scroll, master/detail left+right where the right column is the `ExaminePanel`):
       - **Char-head:** `{inventory.char}` (Display 24px accent) + a dimmed meta line if available.
       - **Paperdoll (§E):** a 3-column grid (left 2-wide / center `.doll` figure / right 2-wide), `gap: 24px`. Render ALL 23 equipment slots from `inventory.equipment` placed per §E (left: Ear1,Head,Face,Ear2,Neck,Shoulders,Arms,Back; right: Wrist1,Wrist2,Hands,Finger1,Finger2,Chest,Legs,Feet; bottom WORN row: Charm,Primary,Secondary,Range,Ammo,Waist,Power). Match each canonical slot to its `equipment[]` entry by `canonical_slot`; render an EMPTY `PaperdollSlot` (with the slot label) for any of the 23 not present (D-11 keeps empty slots). The center `.doll` is a dashed-border panel, min-height ~260px, silhouette glyph + a dimmed `{race} {class}` line (no fabricated AC/ATK — show only if the data exposes it).
       - **GENERAL INVENTORY (§F):** an accent eyebrow + a grid of `PaperdollSlot` tiles (`repeat(auto-fill, 62px)`, gap 8px) over `inventory.general`.
       - **BANK — STORED ITEMS (§F/D-05):** the SAME grid renderer over `inventory.bank`, directly below general (NO toggle).
       - **WORN eyebrow** "WORN — WEAPONS & MISC" above the bottom equipment row (may fold into the paperdoll).
       - **Hint line** (dimmed): "Click any item to pin its details on the right. Click a ⊞ bag to open it. Item icons load from the P1999 wiki."
    2. **Inline bag expand (§F / D-04 — NOT a modal):** `let openBags = $state<Set<string>>(new Set())` keyed by slot `location`. Clicking a container tile (`slots > 0`) toggles its location in the set; when open, render an INLINE indented `--panel` sub-region immediately below the grid row: a Label-13px sub-header `"{item} — {usedChildren} of {slots} slots"`, then a sub-grid of `PaperdollSlot` over `slot.children` (same renderer). `aria-expanded` on the bag tile; re-click collapses. Empty bag → a dimmed "Empty" line. NO modal, NO `#bagscrim`, NO scrim element (acceptance greps for its absence). A container click OPENS the bag and does NOT pin; a non-container filled click PINS.
    3. **Examine hover/pin (§G):** `let pinned = $state<InventorySlot | null>(null)`. Hovering a filled non-container tile shows a transient preview (reuse `ItemTooltip` mechanics OR a lightweight popover sharing the `examineFields` body) — `pointer-events: none`, dismissed on mouse-out/Esc/outside. Clicking a filled non-container tile sets `pinned = slot` (single, replace-on-click — D-07) and the right-column `<ExaminePanel slot={pinned} charLastSeen={inventory.last_seen} />` updates. On touch, tap pins (no hover). Pass `inventory.last_seen` as the examine "Last synced" (NOT a per-slot value — Pitfall 2).
    4. **Responsive (§H):** desktop two-pane (grids left, `~290px` sticky `ExaminePanel` right, `grid 1fr 290px gap 24px`); ≤900px the panel drops below (single column); <640px tiles wrap, gutters 16px (inherited). Slot tiles stay 62px across breakpoints.
    5. **Wire into `web/src/routes/characters/+page.svelte`:** replace the Plan-31-03 window-slot region. When `selected !== null`, fetch the inventory in a window-scoped state machine: `let invStatus = $state<'loading'|'error'|'ready'>('loading')`, `let inv = $state<CharacterInventory|null>(null)`; an effect/handler on `selected` calls `fetchInventory(selected)` (own loading/error inside the window column: `StateBlock kind="loading"` while fetching, `kind="error"` + Retry on failure). When ready: if `inv` has zero equipment+general+bank items → render the §K no-inventory block ("No inventory synced yet" / "{name} hasn't uploaded inventory yet…"); else `<InventoryWindow inventory={inv} />`. Keep the §K "Pick a character" prompt for `selected === null`. Route 401/403 from `fetchInventory` to the AuthGate guard (same as the roster load).
    All colors/fonts via theme tokens; the ONLY non-token color is the locked colored-tile fallback. NO new `{@html}` (the examine body's escaped sink lives in ExaminePanel).
  </action>
  <verify>
    <automated>cd web && npm run check && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/components/InventoryWindow.svelte` contains `PaperdollSlot`, `ExaminePanel`, all three section eyebrows (`GENERAL INVENTORY`, `BANK — STORED ITEMS`, `WORN — WEAPONS & MISC`), and `aria-expanded`; grep finds NO `bagscrim`/`scrim`/`modal` (inline expand, Pitfall 5).
    - grep confirms all 23 canonical slot names are referenced in InventoryWindow (the paperdoll arrangement renders the full set — Pitfall 6).
    - `web/src/routes/characters/+page.svelte` contains `InventoryWindow` and `fetchInventory(` and the `No inventory synced yet` block and passes `inventory.last_seen` (not a per-slot last_listed) to the examine.
    - grep finds NO `{@html}` added to InventoryWindow.svelte or +page.svelte (the only escaped sink is ExaminePanel).
    - `cd web && npm run check` → 0 errors; `npm test` passes; `npm run build` succeeds.
  </acceptance_criteria>
  <done>The window renders the 23-slot paperdoll + general + bank grids (one renderer), inline bag expand (no modal), hover/pin examine (single, D-08), and is wired into /characters with per-character loading/error/no-inventory states; check/test/build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy to prod + browser-smoke the window (node vitest is DOM-blind)</name>
  <read_first>
    - docs/backend-deploy.md (§7.5 web-only atomic-swap — the P27/P30 web-deploy path) AND the backend binary+migration deploy path (P28/P26 — R2 backup → binary swap → goose.Up → web swap), because THIS phase ships BOTH a new backend binary (the two readapi routes + RosterFor + icon enrichment) AND migration 00012 AND the web bundle. This is a binary+migration+web deploy, NOT web-only.
    - Memory: web-tests-node-only-blind-to-dom (green vitest ≠ works in browser) + web-local-dev-cant-auth-against-prod (localhost login bounces to prod — must deploy then smoke, or run a full local stack).
    - Source of truth: 31-UI-SPEC.md §Build/Verification Notes (the exact smoke checklist) + 31-RESEARCH.md Pitfall 1.
  </read_first>
  <what-built>
    Tasks 1–2 built the inventory window (paperdoll, grids, inline bag expand, icons + colored-tile
    fallback, hover-preview + click-to-pin examine) over the new session-gated read-API + icon
    enrichment. Node vitest covers ONLY the pure helpers (roster.ts, examine.ts) — the entire window
    DOM (render, hover/pin, inline expand, remote icon load + onerror fallback, master-detail
    selection, 5-theme rendering) is invisible to node tests and MUST be browser-smoked on a deployed
    build. The executor first deploys (backend binary + migration 00012 + web bundle) to prod per
    docs/backend-deploy.md, then runs the checklist below.
  </what-built>
  <how-to-verify>
    The executor deploys, then verifies + presents results. Steps:
    1. **Deploy (backend+migration+web):** R2 backup → cross-compile `GOOS=linux GOARCH=amd64 CGO_ENABLED=0` → scp + swap the binary (keep `.bak`) → restart → confirm `goose.Up` applied `00012_item_icon` (schema version advances; `item_master.icon_id` present) → web build → tarball → atomic-swap (`squirebot.old` kept). External sanity: apex 200, entry JS `text/javascript`, `GET /api/v1/characters` → 401 unauthenticated (proves the RequireSession gate, T-31-05), `GET /api/v1/inventory/Test` → 401 unauthenticated.
    2. **Browser-smoke on squirebot.quest (signed in), the UI-SPEC §Build-Notes checklist:**
       - the character list orders viewer's chars → guild → banks/bots; a band with zero members shows no header.
       - the scoped search filters by name and the viewer's matches rank first; no-results shows the StateBlock copy.
       - selecting a character (row click) renders the window; the URL gains `?c=<name>`; deep-linking `?c=<name>` pre-selects.
       - desktop hover shows the transient examine preview; clicking PINS it; clicking another item REPLACES the pin (single panel).
       - a ⊞ bag expands INLINE (no modal/scrim) revealing its contents that behave like the grid; re-click collapses; an empty bag shows "Empty".
       - stacked slots show their count (>1); empty equipment slots show the slot label.
       - a known iconId (e.g. Cloak of Flames 658) loads the wiki PNG; a bogus/absent iconId falls back to the colored tile (no broken-image glyph).
       - the examine omits missing fields (no "null"/blank rows); "Last synced" shows the character upload time (not a price-listing date).
       - a character with no synced inventory shows "No inventory synced yet" (not a crash/blank).
       - the window + list render correctly under ALL 5 themes (Velious/Vanilla/Kunark + a Heavy + Minimalist contrast spot-check: the accent focus ring + slot-hover border read).
    3. Capture pass/fail per item in `31-04-SUMMARY.md` + a browser-smoke record (the P28/P30 precedent). Any FAIL → fix-forward (or roll back per docs/backend-deploy.md: `cp …squirebot-server.bak` restart + `mv squirebot.old` back) before declaring the phase verified.
  </how-to-verify>
  <resume-signal>Type "approved" once the deploy + browser-smoke checklist passes (or describe the failures to fix).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read API → window DOM | Guildie-named items/characters + stored wiki summaries render into the examine. |
| wiki CDN → `<img>` | The icon image loads client-side from `wiki.project1999.com` (a content image, not proxied). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-14 | Tampering (Stored XSS) | examine HTML body | mitigate (BLOCKING) | The examine body is the ONLY `{@html}` in this phase and MUST route through `composeItemNote`/`escapeHtml` + `safeHttpUrl` (the live T-14.04-01 gate). Item/character names + wiki summaries are guildie-controlled; a new un-audited `{@html}` of any wiki/price/name string is a HIGH-severity XSS reopening — acceptance greps for its absence in InventoryWindow/PaperdollSlot/+page and confirms ExaminePanel's only `{@html}` is the escaped composed body. |
| T-31-15 | Tampering | icon `<img>` URL | mitigate | `icon_id` is an INTEGER (Plan 31-01 `parseIconID`); the URL is `Item_${int}.png` — no guildie string in the path. A bad/absent id → the colored-tile fallback. No arbitrary-URL injection into `<img src>`. |
| T-31-16 | Tampering | wiki link href | mitigate | The wiki href is `safeHttpUrl(wiki_url || wikiUrlFor(item))` — http(s) scheme allow-list blocks `javascript:`/`data:`/relative before any href (composeNotes.ts existing control). |
| T-31-17 | Info Disclosure | window data fetch | accept | `fetchInventory` rides `getJSON` (credentialed, typed 401/403 → AuthGate); the API gate (RequireSession, Plan 31-02) is authoritative; the window renders only what the session is authorized to read (guild-wide consolidated model). |
| T-31-18 | DoS | deep bag nesting render | accept | The data foundation flattens grandchildren to top-level (Phase 29) and bags are one level deep — the inline expand cannot recurse unboundedly. |
</threat_model>

<verification>
- `cd web && npm run check` → 0 errors; `npm test` green (incl. examine.ts); `npm run build` succeeds.
- Deployed to prod (backend binary + migration 00012 + web); `/api/v1/characters` returns 401 unauthenticated (gate proven).
- Browser-smoke checklist PASS on squirebot.quest across all 5 themes (the human-verify checkpoint).
</verification>

<success_criteria>
- Selecting a character renders the in-game-style window: 23-slot paperdoll + general + bank grids, stacked counts shown.
- Item icons load from the wiki; a missing/failed icon falls back to the colored tile (window never blocked on icon coverage).
- A bag expands INLINE (no modal) to reveal its contents that behave like the grid.
- Hover previews + click-to-pin examine (single, replace-on-click) in the D-08 order; missing fields omitted; "Last synced" is the per-character upload time.
- Deployed + browser-smoked across all 5 themes; the examine has exactly one escaped {@html} sink.
</success_criteria>

<output>
After completion, create `.planning/phases/31-characters-tab-in-game-inventory-window/31-04-SUMMARY.md`
</output>
