---
phase: 20-bot-dm-notification-infrastructure
plan: 05
type: execute
wave: 5
depends_on: [20-04]
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/components/MonitorAdminPanel.svelte
  - web/src/routes/admin/+page.svelte
  - web/src/lib/components/cells/WantMuteCell.svelte
  - web/src/lib/columns.ts
  - web/src/lib/components/WantlistPanel.svelte
  - web/src/lib/components/MonitorAdminPanel.test.ts
  - web/src/lib/api.test.ts
autonomous: false
requirements: [WANT-08, WANT-03]
must_haves:
  truths:
    - "An officer sees a new 'Monitors' section on /admin with three guild-wide kill-switch toggles (EC default ON; WTS + raid default OFF/dark)"
    - "An officer can register a source channel (server label + channel id + monitor type) writing a guild_channel row, and remove one via a ConfirmDialog (the one destructive action)"
    - "The officer 'Send me a test alert' button DMs the clicking officer + logs to their inbox, with the three feedback states (sent / 50007-blocked / bot-down) - the WANT-03 end-to-end proof (D-10)"
    - "Each /wantlist grid row gains a bell/bell-off mute toggle ('Alerts' column); clicking toggles wantlist_item.muted server-side then re-reads (never optimistic); a custom want shows a disabled bell"
    - "The api.ts WantlistRow interface gains `muted: boolean` deterministically (the backend ListOwnWants already returns it from Plan 01 - BLOCKER-3 closed; no conditional 'add if missing' here)"
    - "The /admin Monitors section inherits the existing two-layer officer authorization; non-officers never see it"
    - "Plan 05 APPENDS its officer-monitor api.ts wrappers BELOW Plan 04's notify block - it does NOT overwrite that block"
  artifacts:
    - path: "web/src/lib/components/MonitorAdminPanel.svelte"
      provides: "the officer Monitors UI: kill-switches + add-channel form + channel list + remove + test-alert"
      contains: "Send me a test alert"
    - path: "web/src/lib/components/cells/WantMuteCell.svelte"
      provides: "the per-row bell/bell-off mute toggle on /wantlist"
      contains: "bell-off"
  key_links:
    - from: "web/src/routes/admin/+page.svelte"
      to: "MonitorAdminPanel"
      via: "a third .form-card section in .admin-area"
      pattern: "MonitorAdminPanel"
    - from: "web/src/lib/components/cells/WantMuteCell.svelte"
      to: "/api/v1/wantlist/mute"
      via: "onMute callback -> muteWant() in WantlistPanel (server-truth reload)"
      pattern: "onMute"
    - from: "web/src/lib/components/MonitorAdminPanel.svelte"
      to: "/api/v1/admin/monitors/*"
      via: "the officer monitor api.ts wrappers, classifyAdminError-routed"
      pattern: "classifyAdminError"
---

<objective>
Build the officer `/admin` Monitors section (guild-wide kill-switches + add-channel form + channel list + remove + the D-10 test-alert button) and the per-want mute bell on the existing `/wantlist` grid. Reuses the Toggle + api.ts core from Plan 04. Ends in a mandatory blocking browser-smoke.

Purpose: WANT-08 (officer enable/disable each monitor + register source channels - ship dark, flip on with no rebuild) and WANT-03's frontend proof (the "Send me a test alert" button - the bot pulse). The mute bell completes D-09's UI.

Output: the MonitorAdminPanel + the /admin page extension + the WantMuteCell + the columns.ts + WantlistPanel wiring + the officer monitor api.ts wrappers + the deterministic `WantlistRow.muted` TS-interface field. The phase's final browser-smoke (the bot pulse is the keystone proof). This plan is FRONTEND-ONLY - the backend `muted` read path (ListOwnWants + WantlistRow struct + JSON) was completed in Plan 01; this plan touches NO Go files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-bot-dm-notification-infrastructure/20-UI-SPEC.md
@.planning/phases/20-bot-dm-notification-infrastructure/20-PATTERNS.md
@.planning/phases/20-bot-dm-notification-infrastructure/20-03-SUMMARY.md
@.planning/phases/20-bot-dm-notification-infrastructure/20-04-SUMMARY.md

<interfaces>
<!-- From Plan 03 (officer JSON contracts) + Plan 04 (Toggle + api.ts core) + Plan 01 (muted read path). -->

Backend JSON contracts (from Plan 03):
- GET  /api/v1/admin/monitors               -> { flags: {ec,wts,raid:boolean}, channels: GuildChannel[] }
- POST /api/v1/admin/monitors/flag          <- { monitor, enabled } -> ok
- POST /api/v1/admin/monitors/channel       <- { label, channel_id, monitor } -> the row (409 {"error":"duplicate"})
- POST /api/v1/admin/monitors/channel/remove<- { channel_id, monitor } -> { removed }
- POST /api/v1/admin/monitors/test          <- {} -> { status:"sent" } | {"error":"dm_blocked"} | {"error":"bot_unavailable"}
  where GuildChannel = { id, channel_id, label, monitor, enabled, created_at }; monitor in 'ec_auction'|'wts'|'raid_target'

From Plan 01 (the mute read path - ALREADY shipped):
- store.ListOwnWants returns `muted` (the SELECT + WantlistRow.Muted struct field), so GET /api/v1/wantlist's JSON each row carries `"muted": <bool>`. The TS `WantlistRow` interface in api.ts must be extended to match (see Task 2) - this is a DETERMINISTIC interface edit, NOT a conditional "add it if missing". No Go file is touched in this plan.

From Plan 04 (reuse):
- web/src/lib/components/Toggle.svelte - `{ on, label, onToggle, disabled? }`
- web/src/lib/api.ts getJSON/postJSON cores + classifyAdminError (~:517-532) + the `// --- Notifications (20-04) ---` block (muteWant() etc.) - Plan 05 APPENDS its `// --- Monitors (20-05) ---` block BELOW that, never editing it.

From web/src/lib/components/BankCoinForm.svelte (~:1-60) - the officer-form lifecycle (onMount->fetch->FormField form->save) to clone for the add-channel form.
From web/src/lib/components/WatcherCodesPanel.svelte (~:159-192, :320-330) - the revoke + ConfirmDialog destructive pattern for remove-channel.
From web/src/routes/admin/+page.svelte (~:35-49) - the .admin-area .form-card stack + the two-layer officers-only auth that already wraps it.
From web/src/lib/components/cells/WantRemoveCell.svelte - the cell shape ({row, onRemove, busy} + the 44px icon-button CSS) to clone for WantMuteCell.
From web/src/lib/columns.ts (~:252-260) - the `remove` column + the renderComponent(WantRemoveCell, {row, onRemove, busy}) FlexRender path + the factory args (onRemove/removeBusy threaded at ~:197-201).
From web/src/lib/components/WantlistPanel.svelte - the load->mutate->server-truth-reload lifecycle that owns onRemove (clone for onMute).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: officer monitor api.ts wrappers + MonitorAdminPanel + /admin section</name>
  <read_first>
    - web/src/lib/api.ts:517-532 (classifyAdminError) + the /wantlist + Plan-04 `// --- Notifications (20-04) ---` blocks (the wrapper idiom; APPEND below, do NOT edit the Plan-04 block)
    - web/src/lib/components/BankCoinForm.svelte (the officer-form onMount->fetch->FormField->save lifecycle)
    - web/src/lib/components/WatcherCodesPanel.svelte:159-192,320-330 (revoke + ConfirmDialog)
    - web/src/lib/components/ConfirmDialog.svelte (the confirm-before-commit contract; Cancel default-focused)
    - web/src/routes/admin/+page.svelte:35-49 (the .admin-area .form-card stack + two-layer auth)
    - 20-UI-SPEC.md section Monitors Section (the three sub-blocks: kill-switches / add-channel / channels list+remove / test-alert + the three test toasts) + section Copywriting (all copy verbatim)
  </read_first>
  <action>
    EXTEND `web/src/lib/api.ts` - APPEND a `// --- Monitors (20-05 / WANT-08) ---` block BELOW the Plan-04 Notifications block (cookie-credentialed, errors routed through classifyAdminError by the caller). Do NOT modify or remove the Plan-04 block:
    ```ts
    export interface GuildChannel { id: number; channel_id: string; label: string; monitor: 'ec_auction'|'wts'|'raid_target'; enabled: boolean; created_at: number; }
    export interface MonitorState { flags: { ec: boolean; wts: boolean; raid: boolean }; channels: GuildChannel[]; }
    export function fetchMonitors(f = fetch) { return getJSON<MonitorState>('/api/v1/admin/monitors', f); }
    export function setMonitorFlag(monitor: string, enabled: boolean, f = fetch) { return postJSON<unknown>('/api/v1/admin/monitors/flag', { monitor, enabled }, f); }
    export function addGuildChannel(body: { label: string; channel_id: string; monitor: string }, f = fetch) { return postJSON<GuildChannel>('/api/v1/admin/monitors/channel', body, f); }
    export function removeGuildChannel(channel_id: string, monitor: string, f = fetch) { return postJSON<{ removed: boolean }>('/api/v1/admin/monitors/channel/remove', { channel_id, monitor }, f); }
    export function sendTestAlert(f = fetch) { return postJSON<{ status?: string; error?: string }>('/api/v1/admin/monitors/test', {}, f); }
    ```

    Create `web/src/lib/components/MonitorAdminPanel.svelte` cloning the BankCoinForm officer-form lifecycle ($state phase machine, onMount->fetchMonitors, authGuard via classifyAdminError). Three lg-gapped sub-blocks:
    1. **Kill-switches:** three Toggle rows (EC / WTS / raid) bound to flags; onToggle -> setMonitorFlag(monitor, !on) then re-fetch (server-truth). Labels + descriptions verbatim; the "Ships dark - enable once the bot is invited to the source channels." muted note on WTS/raid.
    2. **Add-channel form:** three FormFields (Server label text / Channel ID `inputmode="numeric" pattern="[0-9]*"` / Monitor type `<select>` of EC-tunnel auctions / WTS cross-server / Raid targets) + a `.primary` "Add channel" (plus glyph) button disabled until all three valid; on submit -> addGuildChannel() -> re-fetch; the 409 duplicate routes a friendly error; polite aria-live success "Added <label> to <monitor> monitoring."
    3. **Registered channels list + remove:** each row = label + muted `#<channel-id>` + a monitor-type chip + a destructive Remove (.revoke-btn treatment, trash-2) gated through ConfirmDialog (the ONE destructive action; Cancel default-focused) -> removeGuildChannel() -> re-fetch. Empty list -> the quiet note "No channels registered yet. EC-tunnel monitoring uses PigParse and needs no channel; WTS and raid monitors need a channel here."
    4. **Test-alert:** a `.primary` "Send me a test alert" (send glyph) -> sendTestAlert(); map the response to the three aria-live lines: status==="sent" -> --status-ok "Test alert sent - check your Discord DMs (and your Notifications inbox)."; error==="dm_blocked" -> --status-missing "We couldn't DM you - Discord is blocking messages from the server. It's logged in your Notifications inbox; turn on server DMs in Discord to receive alerts."; error==="bot_unavailable" (or any other) -> "Couldn't send the test alert - the bot may be offline. Nothing was sent; try again shortly." (all copy verbatim). XSS: the officer-entered label renders via {} only.

    EXTEND `web/src/routes/admin/+page.svelte`: append a third `<section class="form-card"><h2 class="form-title">Monitors</h2><MonitorAdminPanel /></section>` to the existing .admin-area stack (it inherits the two-layer officers-only auth that already wraps .admin-area).

    Create `web/src/lib/components/MonitorAdminPanel.test.ts` (vitest, DOM-free): extract + test the test-alert response->message mapping as a pure function (sent/dm_blocked/bot_unavailable -> the three copy strings) and the add-channel validity predicate (label non-blank AND channel_id numeric non-empty AND monitor set). EXTEND `web/src/lib/api.test.ts` for the new monitor wrappers (URL/method/body; no owner field) - APPEND assertions; do NOT remove Plan 04's notify-wrapper assertions in the same file.
  </action>
  <acceptance_criteria>
    - `grep -c "export function fetchMonitors\|export function setMonitorFlag\|export function addGuildChannel\|export function removeGuildChannel\|export function sendTestAlert" web/src/lib/api.ts` returns 5
    - `grep -c "export function fetchPrefs" web/src/lib/api.ts` returns 1 (the Plan-04 block survived - not overwritten)
    - `grep -c "Send me a test alert" web/src/lib/components/MonitorAdminPanel.svelte` returns 1
    - `grep -c "Ships dark" web/src/lib/components/MonitorAdminPanel.svelte` returns 1
    - `grep -c "ConfirmDialog" web/src/lib/components/MonitorAdminPanel.svelte` returns 1 or more (remove-channel confirm)
    - `grep -c "dm_blocked\|bot_unavailable" web/src/lib/components/MonitorAdminPanel.svelte` returns 2 or more (the three test toasts)
    - `grep -c "@html" web/src/lib/components/MonitorAdminPanel.svelte` returns 0 (officer label via {} only)
    - `grep -c "MonitorAdminPanel" web/src/routes/admin/+page.svelte` returns 1 or more
    - `cd web && npm run check && npx vitest run` passes
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run check && npx vitest run</automated>
  </verify>
  <done>The /admin Monitors section has the three kill-switches (EC on / WTS+raid dark), the add-channel form, the channel list with ConfirmDialog remove, and the test-alert with its three feedback states; officer-gated; XSS-safe; the Plan-04 api.ts notify block is preserved (appended, not overwritten) - typecheck + vitest green.</done>
</task>

<task type="auto">
  <name>Task 2: WantMuteCell + columns.ts "Alerts" column + WantlistPanel onMute wiring + WantlistRow.muted TS field</name>
  <read_first>
    - web/src/lib/components/cells/WantRemoveCell.svelte (the {row, onRemove, busy} cell shape + the 44px icon-button + :focus-visible CSS to clone)
    - web/src/lib/columns.ts:197-201 (the factory args onRemove/removeBusy) + :252-260 (the remove column's renderComponent path + the no-sort/no-filter flags)
    - web/src/lib/components/WantlistPanel.svelte (the load->mutate->server-truth-reload lifecycle that owns onRemove + removeBusy - clone for onMute + muteBusy)
    - web/src/lib/api.ts muteWant() (added in Plan 04) + the `WantlistRow` interface (to extend with `muted`)
    - 20-UI-SPEC.md section Per-Want Mute (the bell/bell-off glyph rule, the aria-labels, the custom-want disabled bell, the no-row-dim rule)
  </read_first>
  <action>
    EXTEND `web/src/lib/api.ts` - add `muted: boolean` to the `WantlistRow` interface. This is DETERMINISTIC: Plan 01 already added `muted` to `store.ListOwnWants` + the `WantlistRow` Go struct + its JSON tag, so GET /api/v1/wantlist returns `muted` on every row. The TS interface MUST mirror it so the mute bell can read `row.muted`. (No Go file is touched here - the backend side is already done. If, contrary to Plan 01's SUMMARY, the field is absent from the API response, STOP and flag it - do NOT silently re-add the backend column; that was Plan 01's job and a missing field means Plan 01 regressed.)

    Create `web/src/lib/components/cells/WantMuteCell.svelte` cloning WantRemoveCell: `$props()` of `{ row, onMute, busy }`. An icon button swapping `bell` (unmuted, --text, aria-label="Alerts on for <item> - click to mute") vs `bell-off` (muted, --status-other, aria-label="Alerts muted for <item> - click to unmute") - the glyph carries state (color not the only signal). 44px target + :focus-visible CSS (cloned). A custom want (row.item_id === null) renders a DISABLED muted bell-off with title "Custom wants never trigger alerts." (column stays aligned). onclick -> onMute(row). Coerce `row.muted` to a strict bool for the glyph choice (never render on a truthy-non-bool - the P15 coercion class).

    EXTEND `web/src/lib/columns.ts`: add an "Alerts" column to the wantlistColumns factory BEFORE the `remove` column, mounted via `renderComponent(WantMuteCell, { row: ctx.row.original, onMute, busy: muteBusy })`. Thread `onMute` + `muteBusy` through the factory args exactly like `onRemove`/`removeBusy`. Set `enableSorting:false, enableColumnFilter:false, enableGlobalFilter:false` + `header: 'Alerts'`.

    EXTEND `web/src/lib/components/WantlistPanel.svelte`: add the onMute handler + muteBusy state mirroring onRemove/removeBusy. onMute(row) -> set muteBusy -> `muteWant(row.id, !row.muted)` -> re-read the wantlist via the existing load() (server-truth reload, NEVER optimistic) -> clear muteBusy; a polite aria-live "Muted alerts for <item>." / "Unmuted alerts for <item>.". Pass onMute + muteBusy into the wantlistColumns factory call.
  </action>
  <acceptance_criteria>
    - `grep -c "bell-off" web/src/lib/components/cells/WantMuteCell.svelte` returns 1 or more
    - `grep -c "Custom wants never trigger alerts" web/src/lib/components/cells/WantMuteCell.svelte` returns 1
    - `grep -c "WantMuteCell" web/src/lib/columns.ts` returns 2 or more (import + renderComponent)
    - `grep -c "header: 'Alerts'\|header: \"Alerts\"" web/src/lib/columns.ts` returns 1
    - `grep -c "muteWant\|onMute" web/src/lib/components/WantlistPanel.svelte` returns 2 or more
    - `grep -c "muted" web/src/lib/api.ts` returns 1 or more (WantlistRow.muted TS field)
    - `cd web && npm run check` passes
    - `cd web && npx vitest run` passes
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run check && npx vitest run</automated>
  </verify>
  <done>The /wantlist grid has an "Alerts" mute column; the bell/bell-off glyph carries mute state; clicking toggles muted server-side then re-reads (never optimistic, no row-dim); custom wants show a disabled bell; row.muted (from the Plan-01 backend reader) flows through the TS interface to the cell - typecheck + vitest green. No Go files touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The officer /admin Monitors section (kill-switches + add-channel + channel list + the test-alert button) and the per-want mute bell on /wantlist. web/ vitest is DOM-blind - this is the phase's keystone proof (the bot pulse) and MUST be smoked in a browser against the live bot.</what-built>
  <how-to-verify>
    PRECONDITION: the live Discord bot must be connected (the Plan-03 checkpoint's "bot-live" path - DISCORD_BOT_TOKEN set, bot invited to the guild's own server, `bot connected` in the logs). If the bot was deferred ("defer-live"), steps 4-5 will show bot_unavailable - that's expected; verify the UI states instead.

    Signed in as an OFFICER, on /admin:
    1. The new **Monitors** section appears with three kill-switch toggles: EC-tunnel auctions ON, WTS cross-server + Raid targets OFF (dark) with the "Ships dark" note. Toggle one -> it persists across reload (server-truth). A non-officer (or signed-out) cannot reach this section.
    2. Add a channel (label + a numeric channel id + a monitor type) -> it appears in the registered list. Re-adding the same channel+monitor -> a friendly duplicate error.
    3. Remove a channel -> the ConfirmDialog opens (Cancel default-focused); confirm -> it's removed.
    4. Click **Send me a test alert** -> you receive a Discord DM from the bot AND a DELIVERED row appears in YOUR /notifications inbox; the success toast shows. (This is the WANT-03 end-to-end proof - gateway-up + DM-send + inbox-log.)
    5. If your own Discord DMs are off -> the button shows the CAN'T-DM toast and the row logs as CAN'T DM in your inbox (the 50007 path).
    On /wantlist:
    6. Each row has an "Alerts" bell. Click it -> it flips to bell-off (muted) + a polite announce; reload -> persisted. A custom (free-text) want shows a disabled bell with the "Custom wants never trigger alerts." title. A muted row is NOT dimmed (only the bell changes).
  </how-to-verify>
  <resume-signal>Type "approved" once the test-alert DMs you + logs to the inbox and the mute bell persists (or "approved-deferred" if the bot is on the defer-live path and only the UI states were verified), or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /admin/monitors/* | Officer-cookie'd requests; the server re-checks officer status in-tx (Plan 03) |
| officer-entered label/channel-id -> DOM | Officer free-text rendered in the channel list |
| officer -> test-alert button | DM-spam vector; must only DM the caller |
| browser -> /wantlist/mute | The mute call sends only the want id (no owner) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-21 | Elevation | /admin Monitors section visibility | mitigate | Inherits the existing two-layer officer auth (Layer-1 officers-only StateBlock for direct-nav non-officers + Layer-2 the server RequireOfficer + in-tx re-check from Plan 03); the section is unreachable to non-officers |
| T-20-22 | Spoofing/abuse | Send-me-a-test-alert button | mitigate | The server sends ONLY to the calling officer (caller(ctx)); the UI cannot target another user; officer-gated |
| T-20-23 | Tampering (XSS) | channel label render | mitigate | The officer-entered label renders via Svelte {} auto-escape only - never {@html} |
| T-20-24 | Elevation/IDOR | /wantlist mute | mitigate | muteWant sends only {id, muted}; the server scopes to the session caller; cross-owner is a silent no-op (Plan 01/03) |
| T-20-25 | Tampering | client-side channel-id validation | accept | The numeric inputmode/pattern is UX defense-in-depth only; the server re-validates `^[0-9]+$` (Plan 03) - the client check is not trusted |
</threat_model>

<verification>
- `cd web && npm run check` clean ; `cd web && npx vitest run` green ; `cd web && npm run build` succeeds.
- The Plan-04 api.ts notify block is still present (appended-not-overwritten) and the WantlistRow.muted TS field is present.
- NO Go file is in `git diff --stat` for this plan (the backend muted read path was Plan 01's job).
- The blocking browser-smoke is approved - the test-alert DMs the officer + logs to the inbox (the keystone WANT-03 proof), and the mute bell persists.
</verification>

<success_criteria>
- WANT-08: officers enable/disable each monitor (guild-wide kill-switches; EC on, WTS+raid dark) and register/remove source channels - no rebuild to flip a monitor on.
- WANT-03 (frontend): the "Send me a test alert" button proves the bot CAN DM a guildie end-to-end (DM + inbox log) and exercises the 50007 path.
- D-09: the per-want mute bell toggles wantlist_item.muted (server-truth, no row-dim, custom-want disabled); the bell reads row.muted via the deterministic TS-interface field.
</success_criteria>

<output>
After completion, create `.planning/phases/20-bot-dm-notification-infrastructure/20-05-SUMMARY.md`
</output>
