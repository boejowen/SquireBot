---
phase: 33-banks-tab-valuation
plan: 03
type: execute
wave: 3
depends_on: ["33-01", "33-02"]
files_modified:
  - docs/backend-deploy.md
autonomous: false
requirements: [BANK-01, BANK-02, BANK-03]
user_setup: []
must_haves:
  truths:
    - "The new GET /api/v1/banks route is live on api.squirebot.quest (registered = 401 without a cookie, not 404)"
    - "The rebuilt Banks tab is live on squirebot.quest/banks (the P30 placeholder is gone)"
    - "A human has browser-smoked the rendered tab across all 5 EQ themes and approved it"
    - "An R2 backup was taken before the swap (rollback path exists); NO goose migration was run"
  artifacts:
    - path: "docs/backend-deploy.md"
      provides: "any deploy-runbook delta discovered during this deploy (only if a step changed)"
      contains: "banks"
  key_links:
    - from: "api.squirebot.quest"
      to: "/api/v1/banks"
      via: "the new binary registers the route (restart required)"
      pattern: "api/v1/banks"
    - from: "squirebot.quest/banks"
      to: "the rebuilt page bundle"
      via: "web atomic-swap"
      pattern: "banks"
---

<objective>
Deploy the Plan 33-01 backend (the new `GET /api/v1/banks` route — the server MUST restart to register it) + the Plan 33-02 web bundle (atomic-swap) to the live Hetzner stack, then run the human browser-smoke checkpoint across all 5 EQ themes. This is the ONLY way the rendered Banks tab is verified — node vitest is DOM-blind, and `npm run dev` cannot auth against prod (cookie Domain=squirebot.quest + apex-only CORS).

Purpose: close BANK-01/02/03 — the data + math + route + UI are code-complete after 33-01/02, but "verified" requires the live deploy + a human confirming the summary numbers, the A-Z list, the per-bank header, the item-search→holder→bank-window deep-link, and the value/platinum correctness on a real build.

Output: a live `/api/v1/banks` route + a live `/banks` tab + a human-approved browser-smoke. NO goose migration (schema stays v13 — bank/bot designation + bank-coin shipped v2.3; icon/statsblock P31). The deploy is a backend-binary swap + a web atomic-swap, with an R2 backup taken anyway (per `docs/backend-deploy.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/backend-deploy.md
@.planning/phases/33-banks-tab-valuation/33-UI-SPEC.md
@.planning/phases/33-banks-tab-valuation/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy gate + R2 backup (NO goose)</name>
  <files>(no source files — deploy-prep only)</files>
  <read_first>
    - docs/backend-deploy.md (the full deploy runbook — the R2 backup step, the cross-compile flags `GOOS=linux/amd64 CGO_ENABLED=0`, the binary scp/.bak/install/`systemctl restart` sequence, the web tarball + atomic mv-swap + `squirebot.old` rollback)
    - 33-RESEARCH.md §"Environment Availability" + §"Verification Approach" Deploy note (NO migration — backend-binary swap to register the route + web atomic-swap; R2 backup anyway)
    - the SSH access note (ssh-agent loaded with the passphrase-protected `id_ed25519`; run remote cmds by piping a here-string to `ssh bash -s`)
  </read_first>
  <action>
**Re-run the full gate chain locally first** (the deploy must not ship red code):
- `go build ./...` rc=0, `go vet ./internal/backendsrv/...` clean, `go test ./...` all packages ok (incl. the new `banks` store/compute/readapi tests AND the unchanged `inventory_test.go` BankValuation tests — the legacy bank-toons-only branch must still be green).
- `npm --prefix web run check` 0/0, `npm --prefix web test` green, `npm --prefix web run build` ok (adapter-static).

**Confirm NO migration is part of this deploy:** verify no new `00014_*.sql` exists under the migrations dir (this phase added none). The deploy is a binary swap + web swap — `goose.Up` will be a no-op (schema already v13). Do NOT run any manual goose command.

**Take the R2 backup** of the live SQLite DB per `docs/backend-deploy.md` (the `squirebot-<date>.db.gz` step) BEFORE touching the box — even without a migration, this is the rollback safety net. Confirm the backup object exists in R2.

**Cross-compile the server binary** for linux/amd64 (`GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build`) per the runbook flags. The new binary is what registers `GET /api/v1/banks`.
  </action>
  <verify>
    <automated>cd "C:\Users\Virus Canary\Desktop\Claude\SquireBot" && go build ./... && cd web && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0 and `go test ./...` reports all packages ok (run + eyeball before proceeding)
    - `npm --prefix web run check` exits 0 and `npm --prefix web run build` exits 0
    - No new `00014_*.sql` exists under the migrations dir (this phase adds no migration)
    - An R2 backup object `squirebot-<today>.db.gz` exists (confirmed before any box change)
    - A linux/amd64 server binary is built (the artifact that will register the route)
  </acceptance_criteria>
  <done>Gates green, no migration present, R2 backup taken, the linux binary built — safe to deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy — backend binary swap (restart) + web atomic-swap + external verify</name>
  <files>(no source files — live deploy)</files>
  <read_first>
    - docs/backend-deploy.md (the binary install steps: scp → `cp` the running binary to `.bak` → install 0755 → `systemctl restart squirebot-server` → confirm `bot connected` + `listening` + `scheduler started` in the startup logs; the web steps: build tarball → scp → extract to `/var/www/squirebot.new` → chmod → atomic `mv`-swap keeping `/var/www/squirebot.old` for rollback)
    - 33-RESEARCH.md §"Verification Approach" Deploy note (the route MUST restart to register; external verify = apex 200 + entry JS content-type + the NEW `/api/v1/banks` → 401, registered not 404)
  </read_first>
  <action>
**Deploy the backend** per `docs/backend-deploy.md`: scp the new linux binary to the box, keep a `.bak` of the running binary (rollback), install 0755, and `systemctl restart squirebot-server` (REQUIRED — the route only registers on restart). Confirm the startup logs are clean: `bot connected` (the guild), `listening 127.0.0.1:8090`, `scheduler started ... jobs:N`. `goose.Up` runs on boot and is a NO-OP (schema already v13) — confirm it reports the current version without applying anything new.

**Deploy the web** per the runbook: the `npm run build` output → tarball → scp → extract to `/var/www/squirebot.new` → chmod → atomic `mv`-swap (keep `/var/www/squirebot.old` for rollback).

**External verify (GREEN gate before the human smoke):**
- apex `https://squirebot.quest` → 200; the entry JS is served `content-type: text/javascript` (a fresh bundle hash, NOT the blank-screen `text/html` bug).
- **`https://api.squirebot.quest/api/v1/banks` → 401** (registered + login-gated — NOT 404; a 404 means the binary didn't swap/restart, a 200 means the gate is broken).
- `https://squirebot.quest/banks` → 200 (the route serves the rebuilt page, not the placeholder).

**Rollback path (document in the SUMMARY; do not execute unless needed):** `cp` the `.bak` binary back + `systemctl restart` (the old binary runs against the unchanged v13 DB — extend-only safe); `mv /var/www/squirebot.old` back for the web.
  </action>
  <verify>
    <automated>curl -s -o NUL -w "%{http_code}" https://api.squirebot.quest/api/v1/banks</automated>
  </verify>
  <acceptance_criteria>
    - `https://api.squirebot.quest/api/v1/banks` returns HTTP 401 (registered + login-gated; NOT 404, NOT 200)
    - `https://squirebot.quest/banks` returns HTTP 200 (the rebuilt page serves)
    - apex `https://squirebot.quest` returns 200 and the entry JS content-type is `text/javascript` (fresh bundle, no blank-screen regression)
    - the server startup logs show `bot connected` + `listening` + `scheduler started`, and `goose` reports the existing v13 version (no new migration applied)
    - the `.bak` binary + `/var/www/squirebot.old` exist on the box (rollback ready)
  </acceptance_criteria>
  <done>The new route is live (401 not 404), the rebuilt /banks page serves, the server is healthy, no migration applied, and the rollback artifacts exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser-smoke the rendered Banks tab across all 5 EQ themes</name>
  <what-built>
    The live Banks tab at squirebot.quest/banks: a guild-wide valuation summary header (D-02), an A-Z bank/bot list (D-01) with per-bank item counts, a per-bank value/plat header above the reused P31 inventory window (D-04), and an item-search toggle scoped to bank holders whose holder rows pin that bank's window in-tab (D-03). Plus the new `GET /api/v1/banks` route, deployed and login-gated. Node vitest is DOM-blind — only a human on the live build can confirm the rendered tab.
  </what-built>
  <how-to-verify>
Sign in at https://squirebot.quest, open the **Banks** tab, and confirm each of the following (this is the 33-UI-SPEC §"Browser-smoke checklist", 1:1):

1. **Valuation summary header** renders at the top: eyebrow "GUILD BANKS" + `{value} pp · {platinum} plat`; the numbers are CORRECT (cross-check against what the bank windows hold + the bank-coin entries) and accent-emphasized; `0 pp` / `0 plat` render cleanly when there's no priced item / no recorded coin (never "null"/"—").
2. **Bank list** shows ONLY guild-bank/bot characters, **A-Z**, each row = name + `{n} items` + the correct tag(s); NO per-row value/platinum. A non-bank character never appears.
3. **Select a bank** → the per-bank detail header pins (`{bank} — {value} pp · {plat} plat`, the per-bank slice NOT the guild total) ABOVE the reused inventory window; the window renders that bank's paperdoll/general/bank grids + bags + examine with real wiki icons + colored-tile fallback; selecting another bank REPLACES header + window.
4. **A bank with zero inventory** still lists (`0 items`) and selecting it shows its per-bank header (coin still counted) + the window's "no inventory synced yet" state — no crash/blank. **A bank whose coin was never recorded shows "not recorded" / "— plat", NOT "0 plat".**
5. **Type a query** → the left column TOGGLES to item-search: results show matched bank items, each with which bank(s) hold it + qty/slot + last-synced; the hint reads "Search items held by the guild banks."; the qty/holder count reflects the **bank slice** (NOT a guild-wide number — e.g. an item held 40× guild-wide but 3× across 2 banks reads "3 · 2 holders"); clearing the query returns to the A-Z bank list.
6. **The deep-link:** clicking an item-search **holder** pins THAT bank's window in the right detail column — stays in the Banks tab (no route change); header + window correct for the holder's bank.
7. **Item-search no matches** shows `No matches for "{query}"` (escaped); a normal query is never blank-broken.
8. **No bank selected** shows the "Pick a bank" prompt (not an error); a fetch failure shows the error block + Retry, and Retry refetches.
9. **All 5 EQ themes** render correctly (switch theme in Settings): Velious, Vanilla, Kunark, Minimalist, Heavy — spot-check the accent focus ring, the selected-row border, and every accent NUMBER (summary value/platinum, row item-count, per-bank value/platinum, result qty) on **Heavy** (`#f0d088` on `#1a0e05`) and **Minimalist** (`#b8915c`).
10. **Mobile (<640px, or a narrow window):** the summary header stacks its totals (both visible); the list + detail stack; the reused window collapses its inner examine; item-search holder rows reflow keeping all four facts.

If anything is wrong, describe it — the executor fixes forward (deploy → re-smoke) the same way Phases 31/32 closed.
  </how-to-verify>
  <resume-signal>Type "approved" once all 10 points pass across the 5 themes, or describe the issues to fix-forward.</resume-signal>
</task>

</tasks>

<verification>
- External: `/api/v1/banks` → 401 (registered, login-gated), `/banks` → 200, apex healthy, server logs clean, no migration applied.
- Human: the 10-point UI-SPEC browser-smoke checklist passes across all 5 EQ themes (the load-bearing verification — node vitest is DOM-blind).
- Regression intact: the pre-deploy gate chain (`go test ./...` + web check/test/build) was green before the swap.
- Rollback artifacts (`.bak` binary + `/var/www/squirebot.old`) exist on the box.
</verification>

<success_criteria>
- BANK-01/02/03 are LIVE and human-verified on the deployed build across all 5 themes.
- The new route registered (401 not 404); the rebuilt /banks page serves (placeholder gone).
- Value includes bots; platinum bank-toon-gated; bank-slice search qty correct; nil plat reads "not recorded".
- No migration ran; R2 backup + rollback path in place.
</success_criteria>

<output>
After completion, create `.planning/phases/33-banks-tab-valuation/33-03-SUMMARY.md`
</output>
