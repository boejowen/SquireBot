---
phase: 11-backend-foundation-ingest-api
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - go.mod
  - go.sum
  - cmd/squirebot-server/main.go
  - internal/backendsrv/ingest/handler.go
  - internal/backendsrv/ingest/envelope.go
  - internal/backendsrv/ingest/handler_test.go
  - internal/backendsrv/ingest/envelope_test.go
  - internal/backendsrv/scheduler/scheduler.go
  - internal/backendsrv/scheduler/scheduler_test.go
  - internal/backendsrv/logging/logging.go
autonomous: true
requirements: [BACKEND-01, BACKEND-03, BACKEND-04]
must_haves:
  truths:
    - "A single Go binary (cmd/squirebot-server) runs goose.Up on startup, serves POST /api/v1/ingest, and registers the in-process scheduler skeleton (no real jobs)"
    - "POST /api/v1/ingest with a valid Bearer token decodes the JSON envelope, parses content, binds the character, and atomically replaces its rows; the row is then queryable"
    - "A request with a missing/malformed/unknown bearer token returns 401 and writes nothing (the store is never touched)"
    - "Malformed JSON / missing fields / unknown kind -> 4xx; an oversized body is rejected via http.MaxBytesReader"
    - "The bind+replace runs through 11-03's *sql.Tx-based bindCharacter/ReplaceInventory/ReplaceSpellbook in ONE transaction (no second, test-uncovered SQL path for the atomic-replace + cross-owner-reject logic)"
    - "main.go dispatches serve | mint-code | revoke-code subcommands and exits early for the CLI ones"
  artifacts:
    - path: "cmd/squirebot-server/main.go"
      provides: "Entrypoint: serve/mint-code/revoke-code dispatch, goose.Up on startup, scheduler skeleton registration, server start"
      contains: "RunMigrations"
    - path: "internal/backendsrv/ingest/handler.go"
      provides: "POST /api/v1/ingest: bearer guard -> envelope decode -> parse -> bind -> atomic replace; 401 writes nothing; 409 on cross-owner"
      contains: "MaxBytesReader"
    - path: "internal/backendsrv/scheduler/scheduler.go"
      provides: "In-process scheduler skeleton (no real jobs until P12)"
      min_lines: 20
  key_links:
    - from: "internal/backendsrv/ingest/handler.go"
      to: "internal/backendsrv/auth (resolveToken) + store (bindCharacter, ReplaceInventory/ReplaceSpellbook)"
      via: "guard first (401 on fail, before any store call) -> bind -> replace in one *sql.Tx (the SAME 11-03 functions)"
      pattern: "resolveToken"
    - from: "cmd/squirebot-server/main.go"
      to: "internal/backendsrv/migrations (RunMigrations) + store.Open + scheduler"
      via: "goose.Up on startup; mint/revoke dispatch; serve"
      pattern: "RunMigrations"
---

<objective>
Wire the backend into a runnable single binary - the **verdict-DEPENDENT** HTTP/hosting shell. Build: the `POST /api/v1/ingest` handler (bearer-guard -> JSON envelope decode + validation + body cap -> parse `content` -> first-sighting bind -> atomic replace, all the pieces from 11-02/03/04 composed), the `cmd/squirebot-server/main.go` entrypoint (`serve | mint-code | revoke-code` dispatch, `goose.Up` on startup, scheduler skeleton registration, server start), and the in-process scheduler skeleton (no real jobs until P12). **This plan branches on the 11-01 verdict** (read it first): if ADOPT PocketBase, the route binds via `app.OnServe().BindFunc` + `e.Router.POST(...).Bind(customGuard)` and the cron via `app.Cron().MustAdd`; if FALLBACK, the route is `net/http` ServeMux 1.22+ (`"POST /api/v1/ingest"`) and the cron is a `time.Ticker`/`robfig/cron` goroutine. Either way the imported business logic (auth guard, parser, store tx) is unchanged.

Crucially, the load-bearing atomic-replace + cross-owner-reject logic is OWNED by 11-03 (`bindCharacter`/`ReplaceInventory`/`ReplaceSpellbook`, all `*sql.Tx`-based) and is NOT re-implemented here in either framework branch - both branches call the identical 11-03 functions over ONE `*sql.Tx`, so 11-03's store tests cover the single code path.

Purpose: BACKEND-03's HTTP delivery + BACKEND-04's 401-writes-nothing wiring + BACKEND-01's "single Go binary + in-process scheduler" half (the deploy/TLS/systemd half is 11-06). Composes the verdict-agnostic packages into the one network surface this milestone introduces.

Output: `cmd/squirebot-server/main.go`, `internal/backendsrv/ingest/`, `internal/backendsrv/scheduler/`, and a Linux-appropriate slog setup, all tested with `httptest` against the 11-02 temp-DB helper. Pure Go - runs in CI on the Windows dev box; the actual on-box run is 11-06/11-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-backend-foundation-ingest-api/11-CONTEXT.md
@.planning/phases/11-backend-foundation-ingest-api/11-RESEARCH.md
@.planning/phases/11-backend-foundation-ingest-api/11-PATTERNS.md
@.planning/phases/11-backend-foundation-ingest-api/11-01-SUMMARY.md

<interfaces>
<!-- THE VERDICT: read 11-01-SUMMARY.md FIRST. The VERDICT: line decides route+cron wiring. -->

<!-- Contracts CONSUMED (built in 11-02/03/04) -->
internal/backendsrv/store/db.go:    func Open(dbPath string) (*sql.DB, error)
internal/backendsrv/store/replace.go:  type Store; (s *Store) ReplaceInventory(ctx, charID, rows, uploadedAt, watcherVer) error; ReplaceSpellbook(...)
internal/backendsrv/store/binding.go:  var ErrCharOwnedByAnother; bindCharacter(ctx, tx, charName, tokenOwnerID) (charID int64, err error)  // *sql.Tx-based
internal/backendsrv/auth/guard.go:  type Auth; (a *Auth) resolveToken(ctx, authHeader) (ownerID int64, ok bool)
internal/backendsrv/auth/mint.go:   func MintCode(db, ownerLabel) (string, error); func RevokeCode(db, idOrLabel) error
internal/backendsrv/migrations/embed.go: func RunMigrations(db *sql.DB) error
internal/parse:  func Parse(io.Reader) ([][]string, error); func ParseSpellbook(io.Reader) ([][]string, error) // UTF-8 content (A1)

<!-- The ingest envelope (D-04) -->
type Envelope struct {
  Character      string `json:"character"`
  Kind           string `json:"kind"`            // "inventory" | "spellbook"
  Content        string `json:"content"`         // RAW /outputfile text, UTF-8 (A1)
  WatcherVersion string `json:"watcher_version"` // accepted now; version-gate reject is P13
}

<!-- Contracts this plan CREATES -->
internal/backendsrv/ingest/handler.go:  the POST /api/v1/ingest handler (framework form per the verdict)
internal/backendsrv/scheduler/scheduler.go:  func Register(...) // skeleton; no real jobs (P12 fills in)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the verdict + add the ingest envelope decode/validation + Linux slog</name>
  <read_first>
    - .planning/phases/11-backend-foundation-ingest-api/11-01-SUMMARY.md (the VERDICT: line + the "What 11-05 branches on" subsection - this decides whether Task 2/3 wire PocketBase APIs or net/http; if the verdict is FALLBACK, also remove the spike's PocketBase dep from go.mod and delete spike/pocketbase/ as a cleanup step here)
    - .planning/phases/11-backend-foundation-ingest-api/11-RESEARCH.md section "Security Domain" V5 (validate JSON envelope: required fields, kind enum; cap content size - reject >1 MB via http.MaxBytesReader; a maxed char is <50 KB) + section "Architecture Patterns" diagram steps [2] decode envelope and [3] parse content + Assumption A1 (content is UTF-8)
    - .planning/phases/11-backend-foundation-ingest-api/11-PATTERNS.md section "Shared Patterns -> Structured logging" (reuse the slog JSON-handler shape logger.go:47-51, NOT the Windows LOCALAPPDATA path; server logs to stdout/journald) + "cmd/squirebot-server/main.go" row (the watcher's logging.Setup writes to %LOCALAPPDATA% - the server needs a Linux stdout handler)
    - internal/logging/logger.go (lines 47-53 the slog.NewJSONHandler shape to reuse; lines 25-30 the Windows path to NOT reuse)
    - .planning/phases/11-backend-foundation-ingest-api/11-CONTEXT.md D-04 (the envelope shape verbatim: character/kind/content/watcher_version; char name from the watcher filename, not the file body)
  </read_first>
  <action>
    First, READ `11-01-SUMMARY.md` and record the VERDICT (ADOPT PocketBase vs FALLBACK) in this plan's SUMMARY - Tasks 2 and 3 wire accordingly. If the verdict is FALLBACK: run `go mod edit -droprequire github.com/pocketbase/pocketbase` then `go mod tidy`, and delete the `spike/pocketbase/` tree (do not ship a pre-1.0 dep the production server does not use). If ADOPT: keep the dep pinned at v0.39.0.

    Create `internal/backendsrv/logging/logging.go` with a `Setup() *slog.Logger` that builds the JSON handler from `internal/logging/logger.go` lines 47-51 (`slog.NewJSONHandler(os.Stdout, &slog.HandlerOptions{Level: slog.LevelInfo, AddSource: true})`) writing to **stdout** (systemd/journald captures it on the box) - NOT the watcher's lumberjack-to-LOCALAPPDATA setup (that is Windows-only). Call `slog.SetDefault`.

    Create `internal/backendsrv/ingest/envelope.go` with the `Envelope` struct (D-04 shape verbatim) and a `DecodeAndValidate(r io.Reader) (Envelope, error)` that: `json.Decode`s the body; validates `Character` non-empty, `Kind` is exactly `"inventory"` or `"spellbook"` (enum - reject anything else), `Content` present (empty content is a valid no-op snapshot - allow it; the parser returns (nil,nil)); returns a typed error for each failure so the handler can map to the right 4xx. The body-size cap (`http.MaxBytesReader`, ~1 MB) is applied in the handler (Task 2) before decode, but document the cap rationale here (V5: a maxed char is <50 KB; reject >1 MB). NEVER log raw `content` or the bearer token (V7).

    Write `internal/backendsrv/ingest/envelope_test.go` (table test):
    - valid inventory envelope -> ok; valid spellbook envelope -> ok.
    - missing `character` -> error; empty `kind` -> error; `kind:"badkind"` -> error (enum reject); malformed JSON -> error.
    - empty `content` with valid character+kind -> ok (no-op snapshot is allowed).
  </action>
  <verify>
    <automated>go test ./internal/backendsrv/ingest/... -run "TestDecodeAndValidate|TestEnvelope" -v && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - The VERDICT from 11-01-SUMMARY is recorded in this plan's SUMMARY and drives Tasks 2/3 (PocketBase vs net/http).
    - If FALLBACK: `go list -m github.com/pocketbase/pocketbase` returns nothing/error (dep dropped) AND `spike/pocketbase/` is deleted. If ADOPT: the dep is pinned at v0.39.0.
    - `internal/backendsrv/logging/logging.go` builds a `slog.NewJSONHandler` writing to `os.Stdout` (NOT lumberjack/LOCALAPPDATA).
    - `envelope.go` contains the `Envelope` struct with `character`/`kind`/`content`/`watcher_version` json tags and a kind enum check rejecting anything other than `inventory`/`spellbook`.
    - `go test ./internal/backendsrv/ingest/...` exits 0; the envelope table test covers valid/missing-field/bad-kind/malformed-JSON/empty-content cases.
    - `go build ./...` exits 0.
  </acceptance_criteria>
  <done>The 11-01 verdict is resolved and recorded (and the spike dep removed if FALLBACK), the server has a Linux stdout slog handler, and the ingest envelope decode+validation (required fields, kind enum, empty-content-ok) is implemented and tested. The validation surface (V5) is in place for the handler to compose.</done>
</task>

<task type="auto">
  <name>Task 2: The POST /api/v1/ingest handler (guard -> decode -> parse -> bind -> atomic replace; 401 writes nothing) reusing 11-03's *sql.Tx functions</name>
  <read_first>
    - .planning/phases/11-backend-foundation-ingest-api/11-01-SUMMARY.md (the VERDICT - decides the handler's framework form: PocketBase custom-guarded route vs net/http ServeMux handler)
    - .planning/phases/11-backend-foundation-ingest-api/11-RESEARCH.md section "Architecture Patterns" diagram (the full request flow: [1] bearerGuard -> 401 writes nothing; [2] decode envelope; [3] parse content; [4] resolve owner + first-sighting bind, cross-owner -> 409 + audit; [5] RunInTransaction DELETE+INSERT) + section "PocketBase Spike Probes" Criterion (b) (the PocketBase route+guard+RunInTransaction shape, IF ADOPT) + section "Don't Hand-Roll" (net/http ServeMux 1.22+ row, IF FALLBACK)
    - .planning/phases/11-backend-foundation-ingest-api/11-PATTERNS.md section "No Analog Found -> ingest/handler.go" (NO repo analog - net/http ServeMux is new; cite RESEARCH Architecture diagram; the repo's only HTTP servers are transient OAuth-loopback/picker, not a routed API)
    - internal/backendsrv/auth/guard.go (resolveToken from 11-04), internal/backendsrv/store/binding.go (bindCharacter + ErrCharOwnedByAnother from 11-03 - takes *sql.Tx), internal/backendsrv/store/replace.go (Replace* from 11-03 - the Store type wraps *sql.DB and runs its OWN BeginTx; see the tx-sharing note below), internal/parse (Parse/ParseSpellbook - UTF-8 content), internal/backendsrv/ingest/envelope.go (DecodeAndValidate from Task 1)
  </read_first>
  <action>
    Create `internal/backendsrv/ingest/handler.go` implementing `POST /api/v1/ingest` in the form the VERDICT dictates. The flow (RESEARCH Architecture diagram) is identical either way:
    1. **Body cap:** wrap the body in `http.MaxBytesReader(w, r.Body, 1<<20)` (~1 MB) BEFORE decode (V5).
    2. **Bearer guard FIRST:** call `auth.resolveToken(ctx, r.Header.Get("Authorization"))`; if `!ok` return **401 and RETURN IMMEDIATELY** - do NOT touch the store (the "401 writes nothing" guarantee - BACKEND-04 / V2). This MUST be before any DB write.
    3. **Decode + validate** the envelope (`DecodeAndValidate`); on failure return the mapped 4xx (400 malformed JSON / 422 bad kind / etc.).
    4. **Parse** `content` via `parse.Parse` (kind=inventory) or `parse.ParseSpellbook` (kind=spellbook) - feeding `strings.NewReader(env.Content)` (UTF-8, A1 - no charmap).
    5. **One transaction** (BEGIN IMMEDIATE): `bindCharacter(ctx, tx, env.Character, ownerID)` (first-sighting bind); if `errors.Is(err, ErrCharOwnedByAnother)` return **409** with a clear message (cross-owner reject already audited in binding.go); then run the replace for the resolved `charID` within the SAME tx; commit. On success return **200/204**.

    **SINGLE-TRANSACTION REUSE (WARNING-3 fix - the load-bearing constraint):** the atomic-replace + cross-owner-reject logic LIVES in 11-03's `*sql.Tx`-based `bindCharacter` / `ReplaceInventory` / `ReplaceSpellbook`. This handler MUST reuse those exact functions over ONE `*sql.Tx` - it must NOT re-implement the DELETE-all-then-INSERT or the owner-bind SQL in either framework branch (a second, test-uncovered SQL path is the failure mode being prevented). Concretely:
      - **PREFERRED SHAPE (use this whichever the verdict): PocketBase is ONLY the HTTP/cron shell; the shared `store` package owns the one `*sql.DB` and the transaction.** Both framework branches obtain the `*sql.DB` from 11-02's `store.Open`, call `db.BeginTx(ctx, nil)` (the 11-02 DSN gives BEGIN IMMEDIATE), then call 11-03's `bindCharacter(ctx, tx, ...)` and the replace over that SAME `tx`, then `tx.Commit()`. To run the replace inside the caller's tx (11-03's `Store.ReplaceInventory` opens its own `BeginTx`), either (a) extend 11-03's store with a thin `*sql.Tx`-taking variant the public method delegates to (e.g. `replaceInventoryTx(ctx, tx, charID, rows, ...)`), so BOTH the standalone `Store.ReplaceInventory` (used by 11-03's tests) and this handler call the identical tx-body; OR (b) if 11-03 already exposes a tx-taking form, call it directly. Do NOT duplicate the SQL inline. The bind and the replace MUST share ONE tx so a cross-owner reject (409) rolls back cleanly and the replace sees the just-bound `charID`.
      - **PocketBase wiring (IF ADOPT):** bind the route via `se.Router.POST("/api/v1/ingest", ingestHandler).Bind(&hook.Handler[*core.RequestEvent]{Func: bearerGuard})` inside `app.OnServe().BindFunc`. For the transaction, do NOT use `txApp.DB().NewQuery(...)` to re-implement bind+replace SQL - instead drive the SAME `store` `*sql.DB`/`*sql.Tx` path described above from inside the handler (PB is just the routing+guard shell here). ONLY if a PB-native SQL path is genuinely unavoidable, the 11-03 store tests MUST also be run against that PB path (so the single tested-path guarantee holds); prefer the shared-store shape to avoid that.
      - **net/http wiring (IF FALLBACK):** register `mux.HandleFunc("POST /api/v1/ingest", handler)` on a `net/http.ServeMux` (Go 1.22+ method+pattern routing); the handler uses the shared `store` `*sql.DB`.BeginTx + 11-03's tx functions directly (this is the natural shape).
    Either way: NEVER log raw `content` or the token (V7); use parameterized SQL only (inherited unchanged from 11-03/04 - no new SQL is authored in this handler).

    Write `internal/backendsrv/ingest/handler_test.go` using `httptest.NewRequest`/`httptest.NewRecorder` + `store.NewTestDB` + `MintCode`:
    - `TestIngest_ValidInventory_ReplacesRows`: mint a code, POST a 3-row inventory envelope with `Authorization: Bearer <code>`; assert 200 AND 3 inventory_item rows for that character are queryable back out (BACKEND-03 round-trip).
    - `TestIngest_NoAuthHeader_401_WritesNothing`: POST with NO Authorization header; assert 401 AND `SELECT COUNT(*) FROM inventory_item` is unchanged (0) - the store was never touched.
    - `TestIngest_UnknownToken_401`: POST with a random bearer token; assert 401 and no rows written.
    - `TestIngest_ShrinkingSnapshot_DropsRows`: POST 3 rows then POST 1 row (same char+code); assert 1 row remains.
    - `TestIngest_CrossOwner_409`: mint two codes (owner A, owner B); POST a char under A, then POST the same char name under B; assert 409 and A's rows are untouched.
    - `TestIngest_BadKind_4xx` and `TestIngest_MalformedJSON_4xx`: assert the mapped 4xx and no rows written.
    - `TestIngest_OversizedBody_413or400`: POST a >1 MB body; assert it is rejected (MaxBytesReader) and no rows written.
  </action>
  <verify>
    <automated>go test ./internal/backendsrv/ingest/... -run "TestIngest" -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/backendsrv/ingest/...` exits 0.
    - `handler.go` contains `MaxBytesReader` (body cap before decode) and `resolveToken` is called BEFORE any store/replace call.
    - `handler.go` calls 11-03's `bindCharacter` and the Replace* logic over a SINGLE `*sql.Tx` (one `BeginTx`/`Commit` around both); a `grep` confirms `handler.go` contains NO inline `DELETE FROM inventory_item` / `INSERT INTO inventory_item` / `INSERT INTO character` SQL (the bind+replace SQL is reused from 11-03, not re-authored). If a PB-native SQL path was unavoidable, 11-03's store tests are wired to also exercise it.
    - `TestIngest_ValidInventory_ReplacesRows` passes (200 + the row is queryable back out - BACKEND-03 round-trip).
    - `TestIngest_NoAuthHeader_401_WritesNothing` passes (a POST with no Authorization header returns 401 and inventory_item row count is unchanged - BACKEND-04).
    - `TestIngest_ShrinkingSnapshot_DropsRows` passes (shrinking snapshot drops rows).
    - `TestIngest_CrossOwner_409` passes (cross-owner -> 409, original owner's rows untouched).
    - `TestIngest_BadKind_4xx`, `TestIngest_MalformedJSON_4xx`, `TestIngest_OversizedBody_413or400` all pass with no rows written.
    - `grep` confirms no raw `content` or Authorization value is passed to slog in handler.go.
  </acceptance_criteria>
  <done>POST /api/v1/ingest composes the verdict-agnostic pieces: guard-first (401 writes nothing), body cap, envelope validation, UTF-8 parse, first-sighting bind (409 on cross-owner), and atomic replace in one transaction - reusing 11-03's *sql.Tx-based bindCharacter/Replace* (no second SQL path), with the round-trip (valid upload -> row queryable, shrinking snapshot drops rows) proven via httptest. BACKEND-03 delivery + BACKEND-04 401-writes-nothing are satisfied at the test tier in the framework form the 11-01 verdict dictates.</done>
</task>

<task type="auto">
  <name>Task 3: cmd/squirebot-server entrypoint (serve/mint/revoke dispatch, goose.Up on startup) + scheduler skeleton</name>
  <read_first>
    - .planning/phases/11-backend-foundation-ingest-api/11-01-SUMMARY.md (the VERDICT - decides whether main wires pocketbase.New()+app.Start() or http.ListenAndServe on a ServeMux, and whether the scheduler skeleton uses app.Cron().MustAdd or a time.Ticker goroutine)
    - cmd/squirebot/main.go (lines 26-76 the os.Args[1] subcommand dispatch shape: sniff os.Args[1] for --uninstall-wipe-credentials / --quit, run, exit early; lines 39-55 / 69-76 the early-exit pattern) - mirror this for serve | mint-code | revoke-code
    - .planning/phases/11-backend-foundation-ingest-api/11-RESEARCH.md section "Recommended Project Structure" (cmd/squirebot-server/main.go: flag parse serve|mint-code|revoke-code; goose.Up; http.ListenAndServe) + section "Pattern 4" (goose.Up on startup) + section "Code Examples -> systemd unit" (the binary is invoked as `squirebot-server serve --addr 127.0.0.1:8090 --db /var/lib/squirebot/squirebot.db` - match these flags) + section "PocketBase Spike Probes" Criterion (c) (app.Cron().MustAdd skeleton, IF ADOPT)
    - .planning/phases/11-backend-foundation-ingest-api/11-PATTERNS.md section "cmd/squirebot-server/main.go" (role-match os.Args dispatch; reuse the logging bootstrap but Linux stdout handler not LOCALAPPDATA) + section "internal/backendsrv/scheduler/" (partial analog internal/heartbeat/heartbeat.go ticker-goroutine precedent; RESEARCH Don't-Hand-Roll says don't BUILD a scheduler - skeleton only, no real jobs until P12)
    - internal/heartbeat/heartbeat.go (lines 1-59 the ticker-goroutine precedent: Interval const, realSleep with select on ctx.Done() - the fallback scheduler skeleton mirrors this ergonomics)
    - internal/backendsrv/ingest/handler.go (Task 2), internal/backendsrv/migrations/embed.go (RunMigrations from 11-02), internal/backendsrv/store/db.go (Open from 11-02), internal/backendsrv/auth/mint.go (MintCode/RevokeCode from 11-04)
  </read_first>
  <action>
    Create `internal/backendsrv/scheduler/scheduler.go` - the in-process scheduler SKELETON only (BACKEND-01 "in-process scheduler"; NO real jobs until P12). IF ADOPT PocketBase: a `Register(app *pocketbase.PocketBase)` that calls `app.Cron().MustAdd("heartbeat", "0 * * * *", func(){ slog.Info("scheduler heartbeat") })` (a harmless hourly no-op proving the cron fires). IF FALLBACK: a `Start(ctx context.Context)` launching one `time.Ticker` goroutine (mirroring `internal/heartbeat/heartbeat.go`'s `select { case <-ctx.Done(): return; case <-ticker.C: slog.Info("scheduler heartbeat") }` ergonomics) that logs a heartbeat and returns cleanly on ctx cancel - no PigParse/wiki jobs (those are P12). Per RESEARCH Don't-Hand-Roll, do NOT build a general scheduler; this is a placeholder P12 fills in. Keep a single exported entrypoint matching the verdict (`Register` for ADOPT / `Start` for FALLBACK).

    Create `cmd/squirebot-server/main.go` mirroring the watcher's `cmd/squirebot/main.go` os.Args dispatch. Sniff `os.Args[1]`:
    - **`mint-code`** (with `--owner <label>`): `store.Open(dbPath)`, `RunMigrations(db)` (so the schema exists), `code, err := auth.MintCode(db, label)`, print the plaintext code to stdout ONCE (D-05), `os.Exit(0)`. Exits BEFORE starting the server (mirror the watcher's `--uninstall-wipe-credentials`/`--quit` early-exit at cmd/squirebot/main.go:39-55).
    - **`revoke-code`** (with the id/label arg): `store.Open(dbPath)`, `RunMigrations(db)`, `auth.RevokeCode(db, idOrLabel)`, print confirmation, `os.Exit(0)`.
    - **otherwise (`serve`, the default):** parse `--addr` (default `127.0.0.1:8090`) and `--db` (default `/var/lib/squirebot/squirebot.db`) flags (via a `flag.FlagSet` for the serve subcommand) to MATCH the systemd unit invocation in RESEARCH (`serve --addr 127.0.0.1:8090 --db ...`). Then: `logging.Setup()` (Task 1's Linux stdout handler); `db, _ := store.Open(dbPath)`; `RunMigrations(db)` (goose.Up on startup - D-10; idempotent, no-ops on an up-to-date DB); register the scheduler skeleton; start the server:
      - **IF ADOPT:** `app := pocketbase.New()`; register the Task-2 ingest route in `app.OnServe().BindFunc`; `scheduler.Register(app)`; `app.Start()`.
      - **IF FALLBACK:** build a `mux := http.NewServeMux()`; register the Task-2 handler on `"POST /api/v1/ingest"`; launch `scheduler.Start(ctx)` (ctx cancelled on SIGINT/SIGTERM via `signal.NotifyContext`); `http.ListenAndServe(addr, mux)`.
    The server binds to loopback (`127.0.0.1:8090`) only - Caddy fronts 443 (11-06). NEVER bake any Google secret (CLAUDE.md "Off Google" - no Google deps in the backend); the server has no OAuth, no Sheets, no `-X main.OAuthClientID` ldflags. Prefer refactoring the body into a testable `run(args []string) int` that `main` calls with `os.Exit(run(os.Args[1:]))`, so the mint dispatch is unit-testable.

    Write `internal/backendsrv/scheduler/scheduler_test.go`: assert the skeleton registers/starts without error and (FALLBACK) the ticker goroutine exits on ctx cancel (start with a short-deadline ctx, assert `Start` returns); (ADOPT) `app.Cron()` has the heartbeat entry registered after `Register`. A minimal smoke test is sufficient (no real jobs to test). For `main.go`, the build-level check is the primary gate (the package compiles and `cmd/squirebot-server` cross-compiles - exercised in 11-06); if `main` was refactored to `run([]string) int`, add a `TestRun_MintDispatch` invoking the mint path against a `store.NewTestDB`-style temp DB and asserting a non-empty code is printed and exit code 0.
  </action>
  <verify>
    <automated>go test ./internal/backendsrv/scheduler/... -v && go build ./cmd/squirebot-server/ && powershell -Command "if (Select-String -Path 'cmd/squirebot-server/main.go' -Pattern 'RunMigrations') { Write-Output 'GOOSE_ON_STARTUP_OK' }"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./cmd/squirebot-server/` exits 0 (the single binary compiles).
    - `cmd/squirebot-server/main.go` contains `RunMigrations` (goose.Up on startup - D-10); verify output contains `GOOSE_ON_STARTUP_OK`.
    - `main.go` dispatches `mint-code` and `revoke-code` subcommands with early `os.Exit(0)` before starting the server (mirrors the watcher's os.Args pattern).
    - `main.go` parses `--addr` (default `127.0.0.1:8090`) and `--db` flags matching the RESEARCH systemd invocation; the server binds loopback only.
    - `internal/backendsrv/scheduler/scheduler.go` registers a skeleton (no real PigParse/wiki jobs - P12) via a single verdict-matched entrypoint (`Register`/`Start`); `go test ./internal/backendsrv/scheduler/...` exits 0.
    - `grep` confirms `cmd/squirebot-server/main.go` contains NO Google/OAuth/Sheets references and no `-X main.OAuthClientID`-style secret baking.
  </acceptance_criteria>
  <done>A single Go binary (`cmd/squirebot-server`) dispatches serve/mint-code/revoke-code, runs goose.Up on startup, binds POST /api/v1/ingest on loopback, and registers an in-process scheduler skeleton with no real jobs - in the framework form the 11-01 verdict dictates, with no Google dependency anywhere. BACKEND-01's "single binary + in-process scheduler" half is satisfied (the deploy/TLS/systemd half is 11-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet/client -> POST /api/v1/ingest | The one network-exposed write surface this milestone introduces. Untrusted JSON envelope + bearer token cross here; the body is capped, the token is guard-checked first, and validation precedes any DB write. |
| loopback (127.0.0.1:8090) -> Caddy | The Go server binds loopback only; Caddy (11-06) terminates TLS and reverse-proxies. The server never exposes a public port directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.05-01 | Spoofing / Elevation | Unauthenticated write reaches the store | mitigate | `resolveToken` is called FIRST; `!ok` returns 401 and RETURNS before any store call (401-writes-nothing, BACKEND-04 / V2). `TestIngest_NoAuthHeader_401_WritesNothing` proves the row count is unchanged. |
| T-11.05-02 | Denial of Service | Oversized/malicious payload exhausts memory | mitigate | `http.MaxBytesReader(w, r.Body, 1<<20)` caps the body at ~1 MB before decode (V5; a maxed char is <50 KB). `TestIngest_OversizedBody` proves rejection. |
| T-11.05-03 | Tampering | Malformed/unknown-kind envelope causes a bad write | mitigate | `DecodeAndValidate` enforces required fields + the kind enum before parse; bad input -> 4xx, no write (V5). |
| T-11.05-04 | Tampering / Elevation | Cross-owner upload overwrites another guildie's character | mitigate | bindCharacter inside the tx returns ErrCharOwnedByAnother -> handler returns 409 and the tx rolls back; the reject is audited (D-07 / V4). `TestIngest_CrossOwner_409` proves it. |
| T-11.05-05 | Information Disclosure | Raw `content`, character data, or the bearer token leaks into logs | mitigate | slog logs operation + status + char name only; NEVER the raw `content` or Authorization value (V7). grep-checked in acceptance criteria. |
| T-11.05-06 | Information Disclosure | A debug/health route leaks data on the only-authenticated surface | mitigate | The only registered route in P11 is the authenticated ingest; no unauthenticated debug/health route that returns data (RESEARCH V-table "Unauthenticated reads N/A in P11"). If a health endpoint is added it returns a static 200 with no data. |
| T-11.05-07 | Tampering | A Google secret or OAuth path is accidentally baked into the backend | mitigate | CLAUDE.md "Off Google" - the server has no Google/OAuth/Sheets deps; acceptance criteria grep-check forbids them and the OAuthClientID ldflag in main.go. |
| T-11.05-08 | Tampering | A second, test-uncovered bind+replace SQL path diverges from 11-03's audited logic | mitigate | The handler reuses 11-03's `*sql.Tx`-based bindCharacter/Replace* over ONE tx; no inline DELETE/INSERT SQL is authored here (grep-checked). 11-03's store tests remain the single coverage for that logic. |

ASVS L1 coverage in this plan: V2 (401-writes-nothing wiring completed - guard before store), V5 (body cap + envelope validation + kind enum), V7 (no raw content/token logging). V4 (cross-owner 409) and V6 (token crypto) are inherited from 11-03/11-04 and composed here. TLS (V6) is Caddy's job in 11-06.
</threat_model>

<verification>
- `go test ./internal/backendsrv/ingest/... ./internal/backendsrv/scheduler/...` exits 0.
- `go build ./cmd/squirebot-server/` exits 0 (single binary).
- Round-trip: valid Bearer + inventory POST -> 200 + row queryable; shrinking snapshot drops rows.
- 401-writes-nothing: missing/unknown token -> 401, store untouched.
- 4xx: malformed JSON / bad kind / oversized body rejected with no write; cross-owner -> 409.
- The bind+replace runs through 11-03's *sql.Tx functions over ONE tx; no inline bind/replace SQL in handler.go (single tested path).
- goose.Up runs on startup; mint/revoke dispatch; loopback-only bind; no Google deps.
- The 11-01 verdict drove the route/cron framework form; if FALLBACK, the spike PocketBase dep was removed.
</verification>

<success_criteria>
The backend is a runnable single binary that satisfies BACKEND-03's HTTP delivery and BACKEND-04's 401-writes-nothing wiring and BACKEND-01's "single Go binary + in-process scheduler" half: POST /api/v1/ingest composes guard-first auth, body cap, envelope validation, UTF-8 parse, first-sighting bind (409 on cross-owner), and atomic replace in one transaction (proven round-trip via httptest), reusing 11-03's *sql.Tx-based bind+replace functions so there is ONE tested code path; `cmd/squirebot-server` dispatches serve/mint-code/revoke-code and runs goose.Up on startup; the scheduler skeleton registers no real jobs. All wiring is in the framework form the 11-01 verdict dictates, with no Google dependency. The on-box deploy/TLS/systemd/firewall half is 11-06.
</success_criteria>

<output>
After completion, create `.planning/phases/11-backend-foundation-ingest-api/11-05-SUMMARY.md`. Record the resolved VERDICT (PocketBase vs fallback), which framework form the route/cron took, and how the bind+replace tx-reuse was wired (shared-store *sql.Tx vs PB-native path), for 11-06's deploy plan.
</output>
