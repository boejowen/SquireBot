---
gsd_state_version: 1.0
milestone: v2.0
milestone_name: — "Off Google" — Website Frontend
status: executing
stopped_at: Phase 16 (Cutover + Decommission) — 16-01 code build COMPLETE + locally verified; 16-02/03/04 human runbook remain
last_updated: "2026-05-31T18:05:00.000Z"
last_activity: 2026-05-31
progress:
  total_phases: 6
  completed_phases: 5
  total_plans: 29
  completed_plans: 26
  percent: 90
---

# State: SquireBot

**Initialized:** 2026-04-30
**Last updated:** 2026-05-28 (v2.0 "Off Google" Website Frontend ROADMAP created — Phases 11–16)

## Project Reference

See: `.planning/PROJECT.md` (updated 2026-05-28 with v2.0 milestone scope)

- **Core value:** Every guildie can answer "what does my character still need, and where in the guild is it?" — now delivered via a self-hosted website instead of the Google Sheet.
- **Current focus:** Phase 15 — Admin Web Forms + Login
- **Mode:** yolo
- **Granularity:** coarse

## Current Position

Phase: 16 (Cutover + Decommission) — executing. **16-01 (char-meta web form + backend write endpoint, CUTOVER-02) CODE-COMPLETE + LOCALLY VERIFIED** (the ONLY code build in Phase 16). 16-02/03/04 are human-gated ops runbooks (mint ~12 codes, publish the v2.0.0 binary + flip + the live browser smoke, disable Apps Script triggers + retire the Google OAuth client + decommission checklist).
Plan: 1 of (16-01 code build) complete — 16-02/03/04 are operational, not code plans
**Deploy progress (2026-05-31):** 16-03 Task 1 DONE — char-meta backend+frontend deployed live to the Hetzner VPS / squirebot.quest (orchestrator drove the ssh deploy via the user's loaded ssh-agent). The deferred 16-01 browser smoke PASSED (CR-01 level input validated live; server-side persistence confirmed class=WAR/level=50/race=HFL/bank=1 on a seeded throwaway char, since deleted). Found+fixed a deploy perm bug: `sudo cp -r` left `_app/` dirs `drwx------` (root umask) → caddy (the `caddy` user) couldn't traverse → Caddy served 200.html for every JS asset → blank white screen. Fix = `sudo chmod -R a+rX /var/www/squirebot`; now documented in docs/backend-deploy.md §7.5. DB chars=0 (clean). Leftover PRE-EXISTING demo rows remain (owner `demo-remove-me` + disabled code `VerifyP13` from P13) — pending user OK to delete. Remaining 16-03 = mint ~12 codes + DM/herd (human-only, NOT agent-run: mint prints plaintext). Then Stage 4 = publish v2.0.0 (human).
**Resume:** after the human runbook (16-02 publish v2.0.0 Release → 16-03 deploy+mint+herd → flip), re-run `/gsd-execute-phase 16`. It will skip 16-01 (has SUMMARY), draft 16-04's `docs/decommission-checklist.md` (auto Task 1), present the final decommission gates (disable Apps Script triggers + retire OAuth client), then run phase verification + milestone close. Pre-flight verified 2026-05-31: no v2.x tag/release exists; v1.0.2 is still GitHub "Latest"; clear to cut `v2.0.0`.

### Phase 16 Plan 01 outcome (2026-05-31, /gsd-execute-phase 16)

- **16-01 (char-meta form + endpoint, CUTOVER-02) done — code-complete, locally verified.** Both tasks TDD (RED test commit -> GREEN feat commit). The fresh-start replacement for the Sheet backfill (D-01/D-02): the `class/level/race/is_bank_toon` columns already existed in `00001_init.sql`, so NO migration — just a handler, a store mutator, the RACES value-set port, a Svelte form, and node-only tests in both repo styles.
- **Backend:** `POST /api/v1/char/meta` + `GET /api/v1/char/meta-list`, login-only under `webauth.RequireSession` (D-03 — NEVER `RequireOfficer`/`IsOfficer`; the non-officer-write-persists path is proven by `TestCharMetaSet_NonOfficerCanWrite` reading the columns back). `validCharMeta` re-validates `class ∈ enrich.CLASSES` / `race ∈ enrich.RACES` / level blank->NULL or 1–60 server-side (T-15-29/Pitfall 5); one BEGIN IMMEDIATE tx composes `store.SetCharMetaTx` (param `?` only, `is_removed=0`-scoped, `ErrCharNotFound` fail-closed) + the `char_meta_set` audit row. `enrich.RACES` ported (IKS load-bearing); `CharMeta`/`CharsWithMeta` extended by `is_bank_toon` at the right edge (extend-only) + snake_case JSON tags. Commits `64b9f9d`(test)/`9f6407b`(feat).
- **Frontend:** `/char-meta` route + `CharMetaForm.svelte` (a strict clone of the code-reviewed `BankCoinForm`) with the **CR-01-safe `type="text" inputmode="numeric"` level input** (NEVER number-typed — the node suite is DOM-blind to that crash), class/race `<select>`s, `is_bank_toon` checkbox, plain-`{}` name render (T-16-03, no `{@html}`), NO ConfirmDialog (D-12). Pure helpers in `charmeta.ts` (rawToTrimmed choke point); `fetchCharsForMeta`/`saveCharMeta` over the existing credentialed cores. `/char-meta` shell-nav link under `{#if session?.authenticated}` (D-03 — member-accessible, NOT the officer block). Commits `80fbb23`(test)/`8fbbce3`(feat).
- **Verification:** Go `build`/`vet` clean from repo root + full `go test ./internal/backendsrv/...` green (no regression from the CharMeta change). Web `npm run check` 0/0, `npx vitest run` 200/200 (charmeta.test.ts: +28, incl. the CR-01 number/null regression + the Style-B source assertions for the input type and the member-accessible nav), `npm run build` emits index.html + 200.html. One self-inflicted deviation (Style-B assertions tripped by the form's own copied comments containing `type="number"`/`isOfficer` literals) auto-fixed before the GREEN commit. Summary: `.planning/phases/16-cutover-decommission/16-01-SUMMARY.md`.
- **Deferred (by design):** the REQUIRED browser smoke of the level field + save round-trip is performed in **Plan 16-03 Task 1 against the live deploy** (the node-only suite cannot see the CR-01 crash). No live deploy / mint / release / Google-console action in this run — those are 16-02/03/04.

### Phase 15 execution-run outcome (2026-05-31, /gsd-execute-phase 15)

- **Executed** all 5 plans sequentially (one plan per wave, on master, no worktrees). 21 task commits + 10 fix commits.
- **15-02 checkpoint** resolved "build-only" per directive — built + httptest-faked; live login smoke deferred.
- **Code review (standard, 37 source files):** 2 BLOCKER + 7 WARNING + 5 INFO. User chose "fix everything" → 13/14 fixed (IN-01 was a no-op), 10 atomic `fix(15)` commits. The 2 BLOCKERs (BankCoinForm number-input crash; eviction epoch-seconds date) were invisible to the node-only suite — each now carries a node-only regression. WR-05 owner-floor guard flipped fail-open→fail-closed. Artifacts: 15-REVIEW.md + 15-REVIEW-FIX.md (uncommitted, commit_docs:false).
- **Verification:** human_needed, 5/5 must-haves verified against live source (no gaps). human_needed is solely the deploy/visual smokes. 15-VERIFICATION.md written.
- **Suites at close:** Go build + linux/amd64 cross-compile + vet + `go test ./...` all green; web `npm run check` 0/0, 172/172 vitest, `npm run build` OK.
- **Deferred to deploy (a deliberate separate step):** systemd `DISCORD_*` secrets, `00004` on the live DB, `set-owner-floor` seed, and the 7 live/visual smokes in 15-HUMAN-UAT.md.

### Phase 15 execution directives (user-set 2026-05-30, honor on /gsd-execute-phase 15)

- **15-02 checkpoint = answer "build-only".** Build + unit-test the Discord OAuth2 / session / CORS code against httptest fakes; DEFER the live end-to-end login smoke. (User HAS the Discord app provisioned — CLIENT_ID/SECRET/GUILD_ID in hand + redirect URI `https://api.squirebot.quest/api/v1/auth/callback` registered — but the live values are NOT entered during this run; they go on the box at deploy time only.)
- **Scope = build + verify LOCALLY only.** Execute all 5 plans, run Go + web test suites, commit per plan. NO live deploy this run — do NOT drop the binary on the VPS, do NOT run 00004 on the live DB, do NOT touch systemd. Deploy is a separate deliberate step (this is the first auth/destructive surface).
- **Secret handling (at deploy, later):** the 4 DISCORD_* vars go in the squirebot-server **systemd** unit (Environment= / root-only EnvironmentFile=, chmod 600). Secret never enters the repo, the static bundle, or chat.
- **Owner-floor seed (at deploy, later):** run `squirebot-server set-owner-floor <maintainer-discord-USER-id>` once on the box.

Status: 15-05 complete — PHASE 15 CODE-COMPLETE (5/5) (the three authenticated write forms — build+verify local, 3/3 tasks; the visible write surface now composes the 15-03 endpoints + the 15-04 login gate). `web/src/lib/api.ts` gains `postJSON<T>` [credentialed POST, the SAME typed 401→Unauthenticated/403→Forbidden(code) mapping as getJSON, the server {error} code on EVERY non-2xx so 400 invalid_input/not_bank_toon is branchable] + the 9 typed admin/coin wrappers [fetchBankToons/saveCoin/fetchOfficers/addOfficer/removeOfficer/fetchEvictable/previewEviction/evict/restoreEviction] + their response interfaces + the PURE `classifyAdminError` [the forms' server-truth router]. Pure helpers (node-tested, 15-04 philosophy): `web/src/lib/coin.ts` [D-11 range validation plat≥0 / gold-silver-copper 0–999 + the exact UI-SPEC error copy + the Save gate + the hasRecordedCoin surfacing predicate], `web/src/lib/admin.ts` [showRemoveButton owner-floor suppression + idempotent result copy + the inline owner-floor/lock-busy strings]. `FormField.svelte` [shared label+control+inline-error rhythm]. **BankCoinForm** [ADMIN-05, login-only — NO officer guard, D-12; pre-filling, range-validated, no ConfirmDialog; recorded coin SURFACES in the bank view replacing the P14 null placeholder]. **EvictionForm** [ADMIN-04, officer-only; preview cascade + 30-day grace + the D-10 guild-code-revoke consequence callout → ConfirmDialog (confirm `Evict <guildie>`) → evict; 403 not_authorized → authGuard collapse to Officers-only, owner_floor_protected → inline]. **AdminMgmtForm** [ADMIN-06, officer-only; `Current officers (N):` list + `(owner)` floor annotation + Remove suppressed for a peer + promote-by-pick (D-07) + Remove via ConfirmDialog; exact owner-floor/lock-busy/not-authorized error routing]. `/admin` [officer-gated Layer-1 refusal + the two form cards; server is the real gate — a 403 from any call collapses the UI to Officers-only, T-15-26] + `/bank-coin` [member-accessible]. Tests stay node-only [no DOM/@testing-library; user installs toolchains themselves] via extracted pure helpers + the helper-sourced copy — 165/165 web tests green [+44: 19 adminApi + 18 coin + 7 adminHelpers], `npm run check` 0/0, `npm run build` emits index.html+200.html; ADMIN-04/05/06 satisfied; commits 9687237/dfd802c/8581941. **Live deploy + the deferred login/evict/coin smokes remain a separate deliberate step** (per the Phase 15 directives — build-only this run). Next: Phase 16 (Cutover + Decommission) after the Phase 15 live deploy.
Last activity: 2026-05-31

### v2.0 Phase Plan (2026-05-28)

Coverage: 26/26 v2.0 requirements mapped to exactly one phase. No orphans, no duplicates. Full success criteria in `.planning/ROADMAP.md` § Phase Details.

| Phase | Name | Requirements | Stack | Depends on | Status |
|-------|------|--------------|-------|-----------|--------|
| 11 | Backend Foundation + Ingest API | BACKEND-01, BACKEND-02, BACKEND-03, BACKEND-04, BACKEND-06 | Go + SQLite + goose + Caddy (Hetzner Cloud VPS, US, amd64) | — | ✅ Complete (7/7 — LIVE at https://api.squirebot.quest; ship-gate passed, nightly R2 backup + drilled restore) |
| 12 | Enrichment Job Migration | ENRICH-10, ENRICH-11 | Go in-process scheduler (PigParse + wiki parsers ported) | P11 | ✅ Complete (5/5; 12-01 schema/store SQL + 12-02 the 4 pure parsers + 12-03 politeFetch + 12-04 the 2 jobs + 12-05 scheduler/wiring done — `RunPigparse` (D-9 WTS filter, D-4 truncation-guard-as-LOG, 304-skip) + `RunWiki` (single uninterrupted run, 1s sleep, SHA-1 short-circuit, gear full-replace, log-but-continue); 12-05 db-backed Job registry: `pigparse_daily` (>=24h) + `wiki_weekly` (Sunday UTC) with immediate-check-on-startup + advance-always job_run cursor + per-job sync.Mutex, `run-job pigparse|wiki` D-7 entrypoint; zero inline SQL; ENRICH-10/11 proven end-to-end) |
| 13 | Watcher Re-Target + Onboarding | WATCH-08, WATCH-09, WATCH-10, WATCH-11 | Go watcher (`internal/backend` HTTP client; OAuth/Sheets/Picker deleted) | P11 | ✅ Complete (4/4 — sink re-pointed to backend; Google stack deleted; native guild-code onboarding; binary 57% smaller + Google-secret-free; SemVer pre-release auto-update twin in place) |
| 14 | Web Frontend | BACKEND-05, WEB-01, WEB-02, WEB-03, WEB-04, WEB-05 | SvelteKit static + `@tanstack/table-core` (local adapter; svelte-table is Svelte-4-only) + Tailwind v4; Go read API | P11 (read API) + P12 (data) | ✅ Complete 2026-05-30 (4/4 — human_needed: 6/6 must-haves code-verified, WEB-02 Go parity green, 60 web tests; code-review 0 Critical, WR-01 fixed; deploy + 5 visual UAT pending in 14-HUMAN-UAT.md) |
| 15 | Admin Web Forms + Login | AUTH-08, AUTH-09, ADMIN-04, ADMIN-05, ADMIN-06 | Discord OAuth2 login; web forms | P14 + P11 | 🔄 Code-complete (5/5 — 15-01 schema+store + 15-02 OAuth2 login/session/CORS-creds/set-owner-floor CLI + 15-03 backend write surface + 15-04 frontend login gate + 15-05 the three write forms done; AUTH-08/09 + ADMIN-04/05/06 satisfied; backend: `internal/backendsrv/webadmin` re-authorizes INSIDE the write tx [WR-04 TOCTOU close] + audit_log + DAILY eviction-archive job, 9 routes wired; frontend: AuthGate whole-site gate [server-truth] + BankCoinForm [login-only, surfaces in the bank view, D-12] + EvictionForm [officer-only, preview+consequence+ConfirmDialog] + AdminMgmtForm [officer-only, list+promote-by-pick+remove+owner-floor] + /admin [a 403 collapses to Officers-only, T-15-26], 165/165 web tests green. **Local build+verify only — live deploy + login/evict/coin smokes are the deferred separate step.** Next: Phase 15 live deploy, then Phase 16) |
| 16 | Cutover + Decommission | CUTOVER-01, CUTOVER-02, CUTOVER-03, CUTOVER-04 | shadow soak + backfill + coordinated self-update flip | P13 + P14 + P15 + P12 | Not started |

**Sequencing rationale (FRONT-LOAD THE INGEST PATH):**

1. **P11 first** because the ~12 guildies are dark on the Sheet during the build (Google walled off their watchers); P11 stands up the authenticated ingest endpoint so data has somewhere to go. No dependencies — it is the foundation.
2. **P12 + P13 next** (both depend only on P11): P12 makes the new DB self-populate its dimension data; P13 re-points the watcher at the ingest API and ships it via the auto-updater — together these restore the live data pipeline end-to-end before any polished UI exists.
3. **P14** (depends on P11 read API + P12 data) rebuilds the visible product — the 4 views, search, tooltips, theming.
4. **P15** (depends on P14 + P11) adds Discord login + the officer-only write forms; must exist before the Sheet's admin sidebars can retire.
5. **P16 last** (depends on P13 + P14 + P15 + P12) — shadow soak, human-data backfill, one coordinated watcher flip, then decommission Sheet + Apps Script + Google OAuth client. Meets the "Off Google" goal.

**Reverted to the research-recommended Hetzner VPS; SQLite retained (not Postgres):** SCOPE.md + the 4 findings docs recommended a Hetzner VPS + PostgreSQL. v2.0 initially overrode the host to Oracle Cloud Always Free for $0, then **reverted to the research-recommended Hetzner Cloud VPS (US, amd64)** on 2026-05-29 — while **keeping SQLite + `goose`** (not the research's Postgres). DDL ports with `CITEXT`→`TEXT COLLATE NOCASE` + identity-syntax changes; `pg_trgm` search → SQLite FTS5 / `LIKE`.

**Schema-evolution change:** the `_meta.schema_version` ↔ `WatcherMaxSchemaVersion` handshake retires in favour of forward-only `goose` DB migrations + an explicit API version (`/api/v1/...`). The watcher's Sheets schema gate is removed in P13.

**Open decision surfaced to P11 (not a separate phase):** optional 1-day **PocketBase** spike at the start of P11 (open-source single Go binary = SQLite + auth + REST + admin UI, self-hosted on the same Hetzner VPS) could compress P11 **and** P15 by ~5–8 days. Evaluate before committing to a hand-rolled Go server; verdict captured in the P11 CONTEXT.

### Superseded: 999.19 Google brand verification

Google REJECTED brand verification 2026-05-15 ("home page not registered to you" — `github.io` can't satisfy it), walling off all ~12 guildie watchers. **v2.0 "Off Google" supersedes this** by removing Google entirely rather than renting a domain to placate it. The guild stays dark on the sheet during the v2.0 build (P11 ingest is front-loaded to restore data flow first). Full incident trail at `.planning/debug/v1-0-2-oauth-invalid-client-incident.md`. Anti-pattern note for future Google-OAuth incidents: check OAuth consent screen Verification status FIRST when seeing `Access blocked / Error 400: invalid_request`; the multiple-secrets / loopback-IP / redirect-URI angles are red herrings.

**Shipped to date:**

| Milestone | Phases | Plans | Tag | Date |
|-----------|--------|-------|-----|------|
| v1.0 — Watcher + Workbook + Onboarding | 5 (Phases 1–5) | 31 | `v1.0.0` | 2026-05-11 |
| v1.0.1 — Installer + Permissions Hardening | 3 (Phases 6–8) | 12 | `v1.0.1` (binary; Phase 6 ship gate) | 2026-05-12 |
| v1.0.2 — Robustness Polish (binary; milestone close superseded by v2.0) | 2 (Phases 9–10) | 8 | `v1.0.2` (binary) | 2026-05-13 |

> v1.0.2 binary shipped but its milestone close was superseded by the v2.0 pivot (the Sheet it targeted is being replaced). Phase directories 09 + 10 exist on disk; v2.0 continues at **Phase 11** (never reuses 9 or 10). v1.0.2 was never written to MILESTONES.md.

## Performance Metrics

| Metric | Value |
|--------|-------|
| Total milestones shipped | 2 fully closed (v1.0, v1.0.1) + 1 binary-only (v1.0.2, close superseded) |
| Total phases shipped | 10 (1–10) |
| Total plans shipped | 51 (31 + 12 + 8) |
| Total commits since init | 266+ (203 v1.0 + 63 v1.0.1 + v1.0.2 work) |
| Watcher LOC (Go) | shrank materially in 13-03 — the ~8k-LOC Google stack (internal/auth/sheet/scaffold/picker/wizard/heartbeat + reauth.go, 41 files) deleted, ~1k re-target LOC added; go.mod sheds the oauth2/google-api dependency tree. **13-04 SC-2 byte measure: the re-targeted `squirebot.exe` is 7,408,640 B (7.07 MB) vs the pre-deletion 17,242,112 B (16.44 MB) = 57% smaller; 0 Google strings in the binary; `go list -deps ./cmd/squirebot` Google-free.** |
| Apps-script LOC (TypeScript) | 13,266 (to be decommissioned in P16) |
| Vitest tests | 336/336 (apps-script; pre-v2.0) + **165/165 (web/ — P14: searchIndex 17 / composeNotes 15 / themes 11 / themeApply 6 / tableAdapter 8 / columns; 15-04: api 11 / auth 19 / ConfirmDialog 16 / AuthGate 10; 15-05: adminApi 19 / coin 18 / adminHelpers 7)** |
| Active blockers | 0 |
| Phase 14 progress | 14-01 (~73 min) + 14-02 (~27 min) + 14-03 (~11 min) + 14-04 (~30 min) complete — ALL 4 PLANS DONE (phase verification + code-review pending) |
| Phase 15 progress | 15-01 (~13 min) + 15-02 (~14 min) + 15-03 (~16 min) + 15-04 (~15 min) + 15-05 (~17 min) complete — schema+store + OAuth2/session + backend write surface + frontend login gate + the three write forms (5/5 plans, CODE-COMPLETE); local build+verify only (live deploy + smokes deferred) |

## Accumulated Context

### Decisions Log

All locked decisions live in `PROJECT.md` Key Decisions table and the per-milestone archive ROADMAPs. v1.0 contributed 11 PROJECT-level decisions; v1.0.1 contributed 5 more.

**v2.0 milestone-open decisions (locked, see PROJECT.md Current Milestone):**

- **Off Google entirely** — replace the Sheet (UI + data store), not placate Google's brand verification.
- **Backend = Hetzner Cloud VPS (US, amd64) + Caddy + SQLite + `goose`** (host switched Oracle Always Free → Hetzner on 2026-05-29 — reverted to the research-recommended Hetzner VPS, SQLite retained not Postgres; see Phase 11 CONTEXT D-12). Cost ~$55/yr VPS + ~$12/yr domain ≈ $67/yr (the "$0 backend" premise is retired).
- **Backend server = HAND-ROLLED Go (`net/http` ServeMux + `modernc.org/sqlite` + `goose`), NOT PocketBase** (D-01 spike verdict, Plan 11-01, 2026-05-29). All four spike probes passed, but the opaque-token auth + plain-SQL-table design bypasses PocketBase's leverage points; the pre-1.0 churn tax isn't worth it. P15 Discord OAuth2 (AUTH-08) is consequently hand-rolled `golang.org/x/oauth2`, not PB's built-in provider.
- **Frontend = SvelteKit static (adapter-static) + TanStack Table + Tailwind** on Cloudflare/GitHub Pages.
- **Watcher↔backend auth = per-guildie opaque bearer token** ("guild code"), hashed server-side, stored client-side in DPAPI wincred; character ownership derived server-side from the credential (`_char_owner`/`UpsertCharOwner` deleted).
- **Website login = Discord OAuth2** gated on guild Discord membership (gate-free; pre-pays AUTH-09). "Sign in with Google" rejected outright (re-introduces the gate).
- **Schema evolution = forward-only `goose` migrations + API version**; retire `_meta.schema_version`/`WatcherMaxSchemaVersion`.
- **Cutover = hybrid shadow-mode** (never writes to the Sheet; self-healing inventory + enrichment).
- **Encoding contract A1 = UTF-8 `content` (locked, Plan 11-03, 2026-05-29):** the shared `internal/parse` is now UTF-8/`io.Reader`-based — the CP1252→UTF-8 decode lives in ONE place (`parse.CP1252Reader`) on the caller's disk-read side. The watcher (incl. P13) wraps; the backend ingest path (UTF-8 JSON `content`) does NOT. **P13 must not double-decode.** The v1 watcher's two runapp.go call sites already wrap in `CP1252Reader` as of 11-03 (zero behavior change off disk). RESEARCH "Encoding Note" Open Question 1 → resolution 1/2.
- **v2 access-control tightening (D-07, Plan 11-03):** v1 first-write-wins warned-and-allowed a cross-owner character; v2 REJECTS (`store.ErrCharOwnedByAnother`) + writes an append-only `audit_log` row, owner_id never overwritten — because the backend (not 12 racing watchers) owns the write.
- **Bearer-token auth = stdlib-only, hash-only-at-rest (BACKEND-04, Plan 11-04):** `auth.MintCode` mints a 32-byte `crypto/rand` token → base64url plaintext shown ONCE (stdout + return), persists ONLY `sha256(plaintext)` in `guild_code.token_hash` (mirrors the watcher's `internal/auth/store.go` DPAPI hash-only discipline). `auth.RevokeCode` sets `disabled_at` (idempotent, label-or-id). `(*auth.Auth).resolveToken` SHA-256-hashes the presented `Bearer` value and `crypto/subtle.ConstantTimeCompare`s it against active (`disabled_at IS NULL`) rows; missing/malformed/unknown/revoked → `(0,false)` (11-05 maps to 401-writes-nothing). NO PocketBase `apis.RequireAuth`/JWT — opaque static tokens via a custom guard. The auth API is verdict-agnostic (pure `crypto/*` + `database/sql`), imported UNCHANGED by 11-05. Added an exported `auth.New(db)` constructor (Rule 2) so the separate 11-05 package can build the guard while keeping `db` unexported.
- **The 2 enrichment jobs compose Wave-1 over one tx with ZERO inline SQL (ENRICH-10/11, Plan 12-04, 2026-05-30):** New `internal/backendsrv/enrich/jobs` package — `RunPigparse(ctx,db,fetch)` (daily) + `RunWiki(ctx,db,fetch)` (weekly) — each composes `politefetch.Fetch` → `enrich.Parse*` → `store.*Tx` over one tx, mirroring `ingest/handler.go::bindAndReplace`; grep-confirmed **0** INSERT/DELETE/UPDATE/ALTER in the production job files (single-tested-SQL-path holds). **RunPigparse:** GetETag → Fetch → **304 skips parse+write entirely** (Pitfall 6, never wipes good rows with an empty body) → ParseToRows → **D-9 WTS (t=0) filter as ONE isolated in-place step** (4,333 of the fixture's 7,240 rows; item 19450 keeps its t=0 price current_avg=239/blue_volume=30/direction="0", not the t=1 0/2) → **D-4 truncation guard = slog.Warn then PROCEED** (the per-item upsert leaves unmentioned rows intact, never clobbers wholesale; no `return` after the warn) → `UpsertPigparsePricesTx` over one tx → SetETag + SetJobRun "ok" (stashes `rows=N` for the next run's truncation baseline). **RunWiki:** ONE uninterrupted pass (NO 6-min resumable machinery — D-5): items+quest_items → per-class spells → 2-page gear-tier full replace; a **1s ctx-aware sleep runs before every wiki fetch** (SC-4, `wikiSleepFn` seam mirroring update/check.go, no-op in tests, no bare `time.Sleep`); a **per-item SHA-1 short-circuit** (`GetItemMasterSHA1Tx`) skips unchanged item_master upserts; a **per-resource 304 skips** that page; the **gear full-replace fires ONLY with the complete combined set** (a failed/304 page skips the replace rather than wipe with a partial set — Pitfall 1, faithful to the TS partial-failure abort); a **single bad page is logged-and-skipped** (the run never aborts). DROPPED (D-5/D-8): the cursor/checkpoint, monitorCellCount, weeklySchemaHealthcheck, LockService, PropertiesService, and the gear/spell view rebuilds (grep-confirmed 0 in production job files). Also `store.DistinctInventoryItemIDs` (dedup-by-id `(item_id,name)` inventory union excluding 0/NULL — the wiki items pass iterates it) + `store.GetJobRunDetail` (read `last_detail` for the truncation baseline, single SQL path). **2 deviations auto-fixed:** Rule 1 — the union dedups by `item_id` ALONE (GROUP BY item_id, MIN(name)), not `(item_id,name)`, so a same-id-different-name item isn't fetched twice (politeness regression; matches the Sheet's first-name-wins); Rule 3 — added `store.GetJobRunDetail` so the truncation guard reads its baseline without inline job SQL. **`go build`/`go vet` clean; `go test ./internal/backendsrv/...` 0 fail; whole-repo `go test ./...` = 28 pkgs, 0 fail (no v1 regression).** 2 atomic feat commits (`9c8120c`, `a79b37b`). Wave-3 12-05 (scheduler) registers `RunPigparse`+`RunWiki`, reads `GetJobRun` for due-checks (D-10), a per-job `sync.Mutex` replaces LockService, and `squirebot-server run-job pigparse|wiki` calls these two with `politefetch.Fetch` for the D-7 parity check.
- **The 4 pure enrichment parsers + eq-constants ported to Go, byte-parity-proven (ENRICH-10/11, Plan 12-02, 2026-05-30):** New `internal/backendsrv/enrich` package — `ParseToRows` (PigParse), `ParseItempage` (+`sha1Hex`), `ParseClassPage`, `ParseGearTierPage`, and the `eqconst.go` lookup tables — transliterated 1:1 from `apps-script/src/lib/*`. **Pure** (no `net/http`, no `database/sql`, no `os` beyond test fixture reads; purity grep clean across all non-test files). **SHA-1 byte-parity** is the strongest D-7 signal: `sha1Hex` = `crypto/sha1` + `hex.EncodeToString` with the TS signed-byte fix-up DROPPED (Go bytes are unsigned) → byte-identical lowercase hex (`sha1Hex("test") == a94a8fe5…`; cloth-cap wikitext `== 5ed737f5…`). **Exact byte-parity** cross-checked by running the TS parsers in Node against the same fixtures: PigParse 7240 rows (item 19450 retained twice — no t=0/t=1 dedup in the parser, deferred to the job per D-9); NEC 171 spells/23 levels, PAL 66/15, Warrior 0; Pre-Raid 577 gear rows/14 classes/4 Iksar, Raiding 606/14/0; item Summary/Slot/quest-links identical (incl. the `…` ellipsis). **D-8 scope guard honored:** `ParsedWikiItem` surfaces ONLY the 6 Sheet-persisted fields (ItemName/Summary/WikiURL/Slot/IsQuestItem/WikitextSHA1) — ac/weight/effect/classes/is_no_drop never surfaced; `PigparseRow` omits T90/A90/Tc/Ta. **`NormalizeSpellName = lower(trim)` by design** (the store's `replace.go:169` join key, NOT the TS alphanumeric-strip variant) so the P14 spellbook↔wiki join matches — spell_name values stay byte-identical. **2 auto-fixes:** Rule 1 — `pageNameToSlug` reimplements JS `encodeURIComponent` (Go's `url.PathEscape` over-escapes apostrophes → `Lord_Nagafen's_Lair` parity); Rule 3 — `.gitattributes` pins `enrich/testdata/* binary` so autocrlf can't flip the wikitext SHA-1 on checkout. **RE2 has no lookahead** → `splitMultiStat` + `extractListItems` reimplemented as manual scans (the latter handles unclosed `<li>`). 28 enrich tests pass; `go build ./...`/`go vet ./...` clean; full `go test ./...` green (zero regression). 3 atomic feat commits (`012d082`, `1a2fc15`, `d519f37`). Wave-2 12-04 (jobs) composes these parsers with the 12-01 `store.*Tx` methods (output structs already line up); 12-03 (politeFetch) is independent.
- **Enrichment schema + store SQL layer = the single tested SQL path for all 5 dimension tables (ENRICH-10/11 foundation, Plan 12-01, 2026-05-29):** Forward-only `00003_enrich_columns.sql` adds the 8 price-history columns `pigparse_price` was short (`t30,a30,t60,a60,t6m,a6m,ty,ay`; one column per `ALTER TABLE ADD COLUMN` — SQLite forbids multi-column adds) + `CREATE TABLE job_run` (scheduler last-run cursor) + `CREATE TABLE etag_cache` (politeFetch 304 state); `t90`/`a90` OMITTED for Sheet parity (buildRow never wrote them, D-7); 00001/00002 byte-unchanged; auto-included by `//go:embed *.sql` (embed.go untouched); `goose.Up` applies cleanly + idempotently (assert-proven via `PRAGMA table_info` + `sqlite_master`). `store/enrich.go` is the single tested SQL path (11-05 WARNING-3 extended): per-table conflict strategy locked by D-12 — `UpsertPigparsePricesTx`/`UpsertItemMasterTx` per-item `ON CONFLICT(item_id)` (D-4 graceful degradation: a truncated response updates what it got, leaves the rest), `UpsertWikiSpellsForClassTx` per-class DELETE+INSERT, `ReplaceWikiGearTierTx` FULL-TABLE replace (the declared `UNIQUE(...,item_id)` never fires because item_id is always NULL → a per-row upsert would duplicate every Sunday, Pitfall 1; proven non-duplicating by the N-not-2N test), `ReplaceQuestItemsForIDTx` per-item-id DELETE+INSERT; `GetItemMasterSHA1Tx` (returns "" when absent) for the wiki job's SHA-1 short-circuit. `store/jobstate.go`: `GetJobRun`/`SetJobRun` (absent/NULL last_run_at → zero time + ok=false = the "never-run ⇒ due-on-startup" signal; upsert, one row per job, advance-always-even-on-error) + `GetETag`/`SetETag` (upsert; 304 leaves the row untouched). Store-local input structs keep the dependency one-directional (jobs → store, never store → enrich). `?` placeholders only (V5); slog logs counts/ids/err only (V7); grep-confirmed zero enrichment SQL leaked into `scheduler/` or `ingest/`. FULL `go test ./...` green (24 pkgs, 0 fail — no v1 regression); `go build`/`go vet` clean. No rule-triggered deviations (2 minor doc-vs-literal-grep notes only). Wave-2 (jobs + politefetch + scheduler flesh-out) unblocked: they compose these `*Tx` methods over one tx and read/write the `job_run`/`etag_cache` cursors.
- **Ingest HTTP shell = HAND-ROLLED net/http, single tested SQL path (BACKEND-01/03/04 wiring, Plan 11-05):** `POST /api/v1/ingest` (`ingest.Handler`) composes the pieces in load-bearing order — `http.MaxBytesReader(1<<20)` (V5) → `auth.ResolveToken` FIRST (`!ok` → 401 + RETURN before any store call; 401-writes-nothing is structural) → `DecodeAndValidate` (typed sentinels → 400/422) → `parse.Parse`/`ParseSpellbook` on UTF-8 `content` (A1, no CP1252) → `store.BindCharacter` + `store.Replace*Tx` in ONE `db.BeginTx` → 204; cross-owner → 409 with the `audit_log` row COMMITTED (the only write on that path; Rule 1 fix — a blanket rollback discarded the audit trace). **Single-tested-SQL-path constraint (WARNING-3):** the handler authors NO inline DELETE/INSERT SQL — exported `store.ReplaceInventoryTx`/`ReplaceSpellbookTx` (the public `Store.Replace*` now delegate to them) + `store.BindCharacter` + `auth.ResolveToken` thin wrappers (Rule 2) keep 11-03/11-04's tests as the single coverage. `cmd/squirebot-server` is one binary: `run([]string) int` dispatches `mint-code`/`revoke-code`/`serve`; `serve` runs `goose.Up` on startup (D-10), binds loopback `127.0.0.1:8090` (Caddy fronts 443), registers a `time.Ticker` scheduler skeleton (no real jobs — P12), graceful-shuts-down on SIGINT/SIGTERM. NO Google/OAuth/Sheets dep anywhere (`go list -deps` = 0). **PocketBase dep + `spike/` tree REMOVED + `go mod tidy` (0 pocketbase refs).** Server logs JSON to stdout (journald), not lumberjack/LOCALAPPDATA. Static `linux/amd64` cross-compile verified (ELF). 3 deviations auto-fixed (1 missing-critical cross-package exports, 2 bugs: cross-owner audit durability + revoke flag ordering).
- **Watcher polish + ship-prep nits + SC-2 confirmation (WATCH-09/11, Plan 13-04, 2026-05-30 — PHASE 13 COMPLETE):** The three deferred ride-along nits landed + the milestone's SC-2 "materially smaller, no Google secret" claim was MEASURED. **999.20 + 999.21 (`cmd/squirebot/console_windows.go`):** `gofmt -w` re-aligned the `var (kernel32…procFreeConsole…)` block; `freeConsole()` now logs `ret==0` at `slog.Debug` and returns `nil` (was `slog.Warn`+`fmt.Errorf` — the spurious detach Warn on every GUI/Explorer launch is gone), with the doc reconciled to "Returns nil unconditionally … a no-console launch is the common benign case … logged at Debug" (stale "log at Warn on failure" sentence removed) and the now-unused `"fmt"` import dropped. **999.22 (`internal/update/manifest.go`):** `parseVersion` now returns `(core [3]int, pre string, ok bool)` — strips leading `v`, splits the pre-release tail on the FIRST `-`, requires 3 numeric core parts (defensive `ok=false` preserved); `IsNewer` compares MAJOR.MINOR.PATCH then SemVer §11 pre-release precedence on a core tie (a pre-release ranks BELOW its final — an rc updates to the final, a final never downgrades to an rc; `rcN` vs `rcM` lexical), with the defensive contract preserved (corrupt manifest OR unparseable current → `false`, never update on doubt). **One version-compare doctrine, PER SIDE:** this watcher-side `IsNewer` is a DELIBERATE separate copy of 13-01's server-side `ingest.IsOlder` — same DIRECTION (pre-release below final), no `internal/backendsrv` import (grep-confirmed the only `backendsrv` token in `internal/update/` is the doctrine doc-comment). Watcher fails-CLOSED on the UPDATE decision (never update on doubt); the server fails-CLOSED on the GATE (always reject on doubt) — opposite mechanics, same "doubt is safe" spirit. **SC-2 CONFIRMED (read-only, no production code change):** built the pre-deletion commit `d12fbaf` (13-02 complete, full Google stack) in a detached worktree with identical ldflags = **16.44 MB**, vs the post-deletion re-targeted binary = **7.07 MB** → **9.38 MB / 57% smaller** (apples-to-apples, corroborating the documented ~17 MB v1 figure); a 9-pattern byte-accurate ISO-8859-1 scan of the binary (`oauth2.googleapis.com`/`accounts.google.com`/`client_secret`/`spreadsheets.googleapis.com`/`sheets.googleapis.com`/`googleapis.com/auth`/`GOCSPX-`/`golang.org/x/oauth2`/`google.golang.org/api`) = **0 matches** (not even the package-path strings — the tree is genuinely gone); `go list -deps ./cmd/squirebot` = **0** packages containing "google" (and `database/sql/driver`'s "drive" substring absent too). **De-risks the P16 coordinated self-update flip (CUTOVER-03):** a final `v2.x.0` tag is now correctly seen as newer by every watcher including any parked on a pre-release, and a stray `-rcN` manifest can never make a final watcher downgrade. **Whole-repo `go build`/`vet`/`test ./...` green (22 pkg, 0 fail); `gofmt -l` clean on all three touched files.** **No deviations** (the 3 target files matched the plan's `<interfaces>`; the SemVer rework mirrored the proven `ingest.IsOlder`; all 3 SC-2 sub-checks passed first try — no Plan-03 regression to re-open). 3 atomic commits (`c930fc2` fix, `e758fb0` test RED, `3e8e53b` feat GREEN). **Path note:** the orchestrator prompt + CONTEXT D-9 said `internal/system/console_windows.go`, but the actual file (and the plan frontmatter) is `cmd/squirebot/console_windows.go` — `internal/system/` exists but has no such file; the plan frontmatter was authoritative. **PHASE 13 COMPLETE (4/4).** Next: Phase 14 (Web Frontend).
- **web/ frontend foundation + the 3 pure-logic ports (WEB-03/04/05, Plan 14-02, 2026-05-30):** Stood up the repo's FIRST frontend — a greenfield `web/` SvelteKit static SPA scaffolded non-interactively (`npx sv create web --template minimal --types ts --add vitest=usages:unit --add tailwindcss=plugins:none --add sveltekit-adapter=adapter:static`). **Foundation:** `svelte.config.js` adapter-static `fallback:'200.html'` (SPA, NO `kit.paths.base` — deploy LOCKED to Cloudflare Pages at the ROOT subdomain `app.squirebot.quest`, which is also Plan 03's CORS allow-origin); `vite.config.ts` `@tailwindcss/vite` (Tailwind v4 CSS-first — ZERO `tailwind.config`/`postcss.config`); `app.css` `@import "tailwindcss"` + 6 self-hosted `@fontsource` woff2 faces (Off-Google — grep-clean of googleapis/gstatic in BOTH `src/` and `build/`; 31 woff2 bundled); `app.html` `<meta robots noindex>` + `static/robots.txt Disallow: /` (D-05); `+layout.ts` `ssr=false; prerender=false`; `+page.ts` `prerender=true` (emit `index.html` shell — SAFE under ssr=false, no build-time fetch). **`@tanstack/table-core` installed (NOT `@tanstack/svelte-table`, the Svelte-4 trap — Pitfall 1); `@lucide/svelte` (NOT deprecated `lucide-svelte`).** **searchIndex.ts** (WEB-03): `levenshtein` ported VERBATIM (Wagner-Fischer, the oracle) + `didYouMean` with the **999.28 fix** (`if (!query||!query.trim()) return []` first line) + `groupAndSort`/`COLLAPSE_THRESHOLD=5` + a new pure `searchRows` in-memory engine (substring→group→didYouMean-on-no-match) replacing the Sheet-coupled `runSearch`; ALL Apps Script I/O dropped; **999.30 fixed path (a)** — the formerly-skipped Test 4 un-skipped, asserts `toEqual([])` (whole-string Levenshtein 'clok'→'cloak of confusion' >=13 is correct; the v1 assertion was arithmetically wrong). **composeNotes.ts** (WEB-04, the HIGH-severity XSS item T-14.02-01): rewritten plain-text→escaped rich HTML; `escapeHtml()` (ampersand-first, ASVS V5) on EVERY interpolated value (item/quest names, summary, wiki URL in href); wiki `<a rel="noopener" target="_blank">`; direction TEXT `"0"`=WTS/`"1"`=WTB/`"2"`=BOTH (BOTH row surfaces as both ask+buy); a vitest assertion PROVES `<img src=x onerror=alert(1)>` renders `&lt;img` not a live tag. **themes.ts** (WEB-05): 5-theme (`velious/vanilla/kunark/minimalist/heavy`) CSS-custom-property registry, `sheets-default` DROPPED, `DEFAULT_THEME='velious'`, token VALUES from the UI-SPEC Theme Catalog (transitively satisfies CLAUDE.md's eq-aesthetic-theme.md derivation), minimalist `weightDisplay=400`/others 600, heavy carries inverting `rowBg`/`rowText` parchment tokens; `resolveTheme` whitelists localStorage to the 5-key enum (no injection); `app.css` emits a `[data-theme]` block per theme + `:root` velious fallback + heavy `.eq-row`/`.eq-header` parchment CSS + `prefers-reduced-motion`. **`npm run build` exits 0 (emits `build/index.html`+`build/200.html`); `npm run check` 0/0; `npx vitest run` 43/43** (searchIndex 17 / composeNotes 15 / themes 11). 4 atomic feat commits (`2ac5357`/`30123e4`/`2952b2a`/`269b069`). **4 deviations auto-fixed:** Rule 3 — added `+page.ts prerender=true` so `index.html` emits without flipping the layout's `prerender=false`; Rule 3 — added `@types/node` devDep (silences the svelte-kit-generated `node` types-lib svelte-check warning); Rule 1 — reworded comments so the literal acceptance greps (`.skip`/`CacheService|…`/`sheets-default`) return 0; Rule 1 — removed the scaffold's `vitest-examples` demo. Wave-2 14-04 imports `searchRows`/`composeItemNote`/`THEMES` + builds the `DataGrid` over the installed `table-core` with a local Svelte-5 adapter.
- **Read API compute/data half = Go reimpl of the 4 v1 view builders + 8 store read methods, parity test-proven (BACKEND-05/WEB-02, Plan 14-01, 2026-05-30):** New `internal/backendsrv/store/readviews.go` (8 parameterized `(*Store)` read methods mirroring `itemids.go`: `InventoryJoin(bankOnly)`, `QuestLinksByItem`, `WikiGearTiers`, `WikiSpells`, `CharsWithMeta`, `InventoryByChar`, `SpellbookNormalizedByChar`, `CharFreshness`; store-local result structs so the dep runs compute→store; `?`-placeholders only with the bankOnly scoping a FIXED-STRING WHERE branch — grep-confirmed 0 string-concat SQL) + new `internal/backendsrv/compute` package reimplementing `buildView`/`buildBank`/`buildGearCheck`/`buildSpellCheck` over the live store (D-01/D-02). **WEB-02 parity is test-provable:** the v1 `buildGearCheck.test.ts` + `buildSpellCheck.test.ts` seed-arrays + expected `(Char,Tier,Slot,Have,Recommended,Status)`/`(Char,Level,Spell,Status)` tuples were translated into Go table-tests over `store.NewTestDB` and assert the same output (RESEARCH approach 1). **`gearcheck.go`** reuses `enrich.WIKI_SLOT_TO_INV_SLOTS` (NOT re-typed), the load-bearing OK→OTHER→MISSING branch order, `EqualFold` name match, tiers Pre-Raid+Raiding always / Iksar iff race==IKS, sort Char→tierRank(1/2/3)→slot→recommended. **`spellcheck.go`** joins on the materialized `normalized_name` (lower(trim) — no recompute, both tables store it identically), level<=char gate, skip classless/level<1, sort Char→level→spell. **`view.go`** ports `pickPrice` with the Pitfall-6 fix: `pigparse_price.direction` is **TEXT** ("0"=WTS/"1"=WTB/"2"=BOTH — confirmed from the P12 job source `enrich/pigparse.go:42` = `strconv.Itoa(t)`), compared via `directionWTS`/`directionWTB` consts; `Price` is `*float64` (nil when neither a30>0); plain wiki URL (no `=HYPERLINK`); enrichment inline (D-03: wiki_summary, is_quest_item, prices[], quest_links[]). **`bank.go`** = bankOnly join; **`Coin` is `*CoinTotals` ALWAYS nil in P14** (no fabricated 0pp — ADMIN-05 fills it in P15; the struct is defined so the JSON shape is stable). **FIXED cross-plan JSON contract (documented in `compute/types.go` header):** snake_case tags `char/slot/item/id/count/wiki_url/price/last_synced/wiki_summary/is_quest_item/prices/quest_links`, `PriceDetail{direction(string),a30,t30}`, `QuestLink{quest_name,source}`, `BankView{rows,coin}`, `GearCheckRow`/`SpellCheckRow` likewise — Plan 03 encodes these, Plans 02/04 consume them. **NO HTTP/handlers here** (Plan 03 composes these). **`go test ./internal/backendsrv/...` green (store+compute incl. all parity tests); `go vet`/`go build ./...` exit 0; whole-repo `go test ./...` green (no v1 regression).** 3 atomic feat commits (`0dc3b35`, `bbe681e`, `f0efa8d`). No behavioral deviations (2 view.go comments reworded so the `grep HYPERLINK==0` acceptance check passes; gear/spell tests seed normalized_name as lower(trim) — the DB contract — not the v1 vitest alphanumeric-strip variant, status tuples unchanged). Wave-2 14-03 wires `compute.View/Bank/GearCheck/SpellCheck` + `store.CharFreshness` behind `GET /api/v1/views/*` + `/meta` (mirror whoami.go, drop the guard per D-04).
- **Frontend integration capstone = DataGrid×4 + ItemTooltip + search + [data-theme] SiteShell, wired to the read API (WEB-01/03/04/05 + BACKEND-05 consumption, Plan 14-04, 2026-05-30 — PHASE 14 CODE-COMPLETE):** The visible product. `web/src/lib/api.ts` = 5 typed fetch wrappers (`fetchView/GearCheck/SpellCheck/Bank/Meta`) over `PUBLIC_API_BASE` (default `https://api.squirebot.quest`, trailing-slash-trimmed) decoding the 14-03 pinned snake_case contract, `ApiError` on non-2xx → the error StateBlock. **The local table-core adapter (Pitfall 1 — NOT `@tanstack/svelte-table`):** the reactive `createSvelteTable` needs `$state` so it lives in `createSvelteTable.svelte.ts` (getter-preserving `mergeObjects` Proxy + `setOptions` re-pipe so the caller's reactive `data`/`columns`/`state` getters survive); `createSvelteTable.ts` is the runes-free public entry re-exporting it + owning `resolveUpdater` (the Pitfall-2 `typeof u==='function'?u(prev):u` unwrap) so plain `.ts` (columns.ts, tests) import cleanly. **DataGrid.svelte** = the ONE reusable grid instantiated 4× (never per-character — CLAUDE.md LOCKED): sticky leading Char col (`position:sticky;left:0`) + sticky header (`top:0`) PURE CSS, global filter + per-column filters with a faceted `<select>` for `meta.filter==='facet'` cols (Status/Tier/Class via `getFacetedUniqueValues`), multi-sort with an accent caret, zebra/hover via `color-mix` accent alpha, NO pagination (fixed-height scroll region), 8×16 cell padding, `tabular-nums`. **columns.ts** = the 4 exact-order `ColumnDef[]` (Char leads each) — view/bank `Char·Slot·Item·ID·Count·Wiki·Price·Last Synced`, gear `Char·Class·Tier·Slot·Have·Recommended·Status`, spell `Char·Class·Level·Spell·Status` — Item/Recommended cells wrap ItemTooltip, Wiki = external `<a rel=noopener>`, Price = `<n>pp` tabular, Last Synced = freshness dot (<7d/<30d/≥30d), Status = StatusCell; a custom Tier `sortingFn` maps Pre-Raid/Raiding/Iksar→1/2/3. **ItemTooltip.svelte** (WEB-04/D-08) = hover (pointer) + tap/click (touch) popover, dismiss on outside-tap + Esc, keyboard-openable on focus, 44px target; body = `{@html composeItemNote(...)}` — the SOLE `{@html}` in the app, safe because 14-02's composer fully escapes every value (grep-confirmed `{@html}` confined to ItemTooltip); wiki anchors `rel="noopener"`. **SearchBox/SearchResults** (WEB-03/D-03/D-09) = maps fetched view rows→`SearchResultRow`, runs in-memory `searchRows`; holders surfaced as `↳ <Char>: <Location>, count <n>`, >5 auto-collapse, a single clickable inline `Did you mean <suggestion>?` accent link on no exact match RE-RUNS the search; the echoed query uses plain `{}` interpolation (auto-escaped, T-14.04-02 — never the raw-HTML directive). **StatusCell/StatusLegend** = OK/MISSING/OTHER (gear) + KNOWN/MISSING (spell) word badges over an 8% pill + the swatch+word legend near those grids (color never the only signal). **StateBlock** = the exact UI-SPEC empty/view-empty/error(+Retry)/loading-skeleton/no-coin copy. **Theme (WEB-05/D-06):** `+layout.svelte` owns `theme $state` (loadTheme seed → velious default), an `$effect` calls the NEW `themes.applyTheme(key, root)` (resolveTheme whitelist → velious fallback, write the single `[data-theme]` attribute + persist, null-root no-op — T-14.04-04) on every change; SiteShell carries the wordmark + ThemePicker (simple `<select>`, fancy tiles deferred) + the required CC-BY-SA footer; `+page.svelte` parallel-fetches the 4 views + meta, holds loading/error/ready, renders a 4-tab switch over the reusable DataGrid (each with its columns + multi-key default sort) + SearchBox over the view rows; bank shows `Coin: not yet recorded` (coin null in P14 — never a fabricated zero, grep `0pp`==0) and treats an empty bank as empty-state not error (P16 sets is_bank_toon). **TDD on the new theme-apply wiring:** RED `205d0c9` (applyTheme test fails) → GREEN `1cc2264`; the 2 loadTheme regression cases stayed green (14-02 shipped them). **`npm run check` 0/0; `npm run build` emits index.html+200.html (noindex preserved); `npx vitest run` 57/57** (43 from 14-02 + 6 themeApply + 8 tableAdapter); `@tanstack/svelte-table` absent; `npm run preview` boots at HTTP 200 (local-deploy proof). **3 deviations auto-fixed:** Rule 1 — the first plain-`.ts` adapter wasn't reactive (`getCoreRowModel is not a function`; header-click no-op — the exact Pitfall-2 failure), caught by a new adapter logic test and fixed with the runes `.svelte.ts` adapter (`6ca160c`); Rule 3 — ItemTooltip + the view/bank cell components built in Task 1 (columns.ts hard-depends on them) to keep commits self-contained; Rule 1 — reworded comments so the literal greps (`@tanstack/svelte-table`, the raw-HTML directive token outside ItemTooltip, `0pp` in +page) return 0. 6 atomic commits (`5896bc7` Task1 / `a8b5219` Task2 / `205d0c9` RED / `6ca160c` adapter-fix / `1cc2264` GREEN / `ef3ee70` Task3). **PHASE 14 CODE-COMPLETE (4/4 plans executed); phase verification + code-review pending before close.** Operational follow-ups (mirroring P11's manual posture): publish `web/build/` to Cloudflare Pages at `app.squirebot.quest` + verify Caddy doesn't duplicate the CORS header (Pitfall 5).
- **Phase 15 schema + store foundation = `00004_web_auth.sql` + 4 store files, v1 admin.ts/eviction SEMANTICS ported into Go/SQL (AUTH-08/09 + ADMIN-04/05/06, Plan 15-01, 2026-05-31):** The hard prerequisite for all of Phase 15, built local-only (no deploy/migration/systemd per the user's locked scope). **Migration `00004_web_auth.sql`** (forward-only; 00001/02/03 byte-unchanged; auto-embedded): 4 new tables — `web_user` (Discord snowflake PK + username/avatar/first_seen/last_login, the AUTH-09 identity captured per login), `web_session` (opaque session stored ONLY as `session_hash` = sha256 hex — T-15-01, plaintext lives only in the cookie; rolling 30-day expiry), `guild_admins` (officer allowlist keyed by Discord ID, ports v1's `_meta.guild_admins` JSON to real rows, FK→web_user ON DELETE CASCADE), `app_config` (singleton key/value; the CLI-seeded owner-floor lives under key `owner_floor_discord_id` — D-08) — plus 6 new `character` columns (nullable `plat/gold/silver/copper` for ADMIN-05/D-11 manual bank coin + `grace_until`/`archived_at` for ADMIN-04/D-10 eviction grace+archive) and 3 GENERIC `audit_log` columns (`actor`/`detail`/`at` — D-06: REUSE/EXTEND the existing ingest-specific audit_log, do NOT invent a parallel log; the officer/coin/eviction web-write events 15-03 appends use these). New-table timestamps are INTEGER epoch seconds (self-contained, store controls read+write); the existing TEXT `datetime('now')` columns (incl. `guild_code.disabled_at`) keep their type. **Four parameterized store files (package `store`, `?` placeholders only, %w-wrapped errors):** `websession.go` (GenerateSessionID via crypto/rand+hex, HashSession via sha256, UpsertWebUser idempotent on discord_user_id with first_seen immutable, CreateSession hash-only, ResolveSession FAIL-CLOSED → ErrSessionExpired/ErrSessionNotFound, TouchSession rolling-window bump, DeleteSession, GetWebUser; `SessionTTLSeconds=2592000`); `admins.go` (ports admin.ts VERBATIM: fail-closed IsOfficer, ListOfficers/ListPromotableUsers, GetOwnerFloor, SetOwnerFloor [seeds a placeholder web_user when the floor hasn't logged in so the FK holds + makes the floor the bootstrap officer — replaces v1's onOpen/getOwner bootstrap], AddOfficerTx/RemoveOfficerTx that re-check caller IsOfficer as their FIRST in-tx SELECT [authorize-under-transaction, closes the v1 WR-04 TOCTOU window], owner-floor protection [ErrOwnerFloorProtected before any write when target==floor && caller!=floor], self-removal orphan rule [floor pointer NOT cleared]; error strings `not_authorized`/`owner_floor_protected` match v1); `eviction.go` (EvictOwnerTx cascades is_removed=1 + grace_until across all the owner's live chars AND sets guild_code.disabled_at to revoke their code in ONE tx — D-10; RestoreOwnerTx reverses during grace [skips archived]; ArchiveExpiredEvictions idempotent past-grace via the archived_at-IS-NULL guard; `EvictionGraceSeconds=2592000`); `coin.go` (SetCoinTx bank-toon-gated → ErrNotBankToon for a non-bank-toon/missing target — T-15-04; coin round-trips as `*int64` so an entered 0 is distinguishable from unset; ListBankToons/GetCoin pre-fill source). **Resolved Claude's-discretion items:** guild_admins is its own table (not an is_admin column); owner-floor is an app_config singleton row (not a column); coin is `*int64`; eviction archive is a lazy/scheduled UPDATE (job-vs-lazy wiring deferred to 15-03). **`go build ./...` + `GOOS=linux GOARCH=amd64 go build ./cmd/squirebot-server` (cross-compile) + `go vet ./internal/backendsrv/...` + full `go test ./...` all green** (the cross-compile artifact `/squirebot-server` is gitignored + was removed). **1 Rule-3 deviation:** renamed the new commit-on-success test helper `withTx`→`commitTx` (the `store` package already has a rollback-only `withTx` in enrich_test.go) across the 3 new test files — test-only, zero production impact. 3 atomic feat commits (`abb92a0` migration, `78d7e24` websession, `4b39e40` admins/eviction/coin). **Deploy-deferred (per the user's locked scope, NOT done this run):** the 4 `DISCORD_*` systemd vars + `squirebot-server set-owner-floor <discord-id>` on the box + goose applying 00004 on restart. 15-02 (Discord OAuth2 + session gate + set-owner-floor CLI) composes UpsertWebUser/CreateSession/ResolveSession + SetOwnerFloor; 15-03 (write surface) composes the *Tx mutators under an audited tx using the new audit_log columns.
- **Phase 15 backend write surface = the 3 authenticated write forms' Go backend, authorize-under-tx + audit_log + DAILY archive (ADMIN-04/05/06, Plan 15-03, 2026-05-30):** New `internal/backendsrv/webadmin` package composing 15-01's `*Tx` store mutators + 15-02's `webauth` gates, built local-only (no deploy). **`audit.go`:** `AppendAuditTx` writes an append-only `audit_log` row over the 00004 generic cols (`at,event,detail,actor`) — D-06 REUSE, the ingest-specific cols stay NULL; never UPDATE/DELETE (grep-verified). `withTx` = the shared `BEGIN IMMEDIATE` (via the `_txlock=immediate` DSN) tx-composer every mutator uses so the store write + its audit row land atomically. **`officers.go` (ADMIN-06):** `OfficersListHandler`/`OfficerAddHandler`/`OfficerRemoveHandler` — RequireOfficer at the route, and `store.AddOfficerTx`/`RemoveOfficerTx` re-check the caller's officer status as their FIRST in-tx SELECT (authorize-under-transaction = WR-04 TOCTOU close); idempotent (`{added:false}`/`{removed:false}`), owner-floor protected (peer→403 `owner_floor_protected`), audit only on a real mutation. **`eviction.go` (ADMIN-04, D-09/D-10):** `EvictHandler` re-checks officer in-tx via the NEW exported `store.IsOfficerTx` (EvictOwnerTx/RestoreOwnerTx don't self-authorize, unlike the officer mutators) then `EvictOwnerTx` cascades is_removed + revokes the guild code + 30d grace in one tx; `RestoreHandler` reverses during grace then re-MINTS a fresh guild code (`auth.MintCode`, post-commit; the burned code stays disabled, response `new_code_issued:true` not the plaintext), refusing 409 `grace_expired` once archived (W-2); owner-floor DATA protection resolves the floor's owner via `owner.label == floor web_user.username` (the documented bridge absent an owner↔discord FK), peer-blocked but floor may act on its own data; preview/evictable list handlers too. **`coin.go` (ADMIN-05, D-12/B-1):** `BankToonsHandler`/`CoinSetHandler` — LOGIN-only (RequireSession at the route; the handler NEVER checks officer status — proven by a comment-stripped `grep -Ec 'RequireOfficer|IsOfficer'`==0 AND `TestCoinSet_NonOfficerCanWrite` which writes as a plain member and reads the columns back), server-side range validation (plat≥0, g/s/c 0–999 → 400 `invalid_input`), bank-toon-gated (`not_bank_toon`), audited. **`scheduler.go`:** appended `eviction_archive` to the registry — DAILY cadence (reuses `duePigparse`, NOT the weekly predicate — W-3), runs `ArchiveExpiredEvictions` (archived_at-IS-NULL guarded → idempotent, proven by `TestArchiveJob_Idempotent`). **`main.go`:** 9 routes wired — 7 officer-only (RequireOfficer) + 2 login-only (RequireSession); `main_test.go::TestWriteRoutes_Gates` proves admin/evict→403 for a member session + 401 anon, coin→401 anon + member admitted (D-12). Error codes match v1 + the UI-SPEC routing exactly (`not_authorized`/`owner_floor_protected`/`not_bank_toon`/`invalid_input`/`grace_expired`). **3 deviations auto-fixed:** Rule 3 — exported `store.IsOfficerTx` (delegates to the private `isOfficerTx`) for the eviction handler's in-tx re-check; Rule 3 — exported `webauth.WithUser` (the ctx setter the gates already used) so cross-package handler tests inject the acting identity; Rule 1 — the route-gate test minted its member session with a fixed PAST `now` (already-expired vs the real-clock ResolveSession) → switched to `time.Now()`. One comment-hygiene reword (scheduler.go says "the weekly one" not the literal `dueWiki`) so the daily-cadence grep gate reads 0. **`go build ./...` + `GOOS=linux GOARCH=amd64 go build ./cmd/squirebot-server` + `go vet ./...` + full `go test ./...` all green** (the stray repo-root build binaries are gitignored + were removed). 3 atomic feat commits (`433f971` officers/audit, `000ac6b` eviction/archive, `78a2bfe` coin/routes). **Deploy-deferred (NOT this run):** the `DISCORD_*` systemd vars + `set-owner-floor` on the box + goose applying 00004; THEN the live smokes (Discord sign-in, evict→restore→re-mint, member coin write). 15-04/15-05 (frontend) consume the 9-route write contract + the pinned response shapes + error codes (fetch with `credentials:'include'`, route 401→Login / 403→refusal per D-01).
- **Watcher re-targeted to the backend + the entire Google stack DELETED (WATCH-08/09/10/11, Plan 13-03, 2026-05-30):** The watcher's terminal SINK is re-pointed from Sheets `batchUpdate` to `backend.Ingest` — `internal/app/runapp.go`'s `makeOn{Inventory,Spellbook}Change` now `io.ReadAll(parse.CP1252Reader(f))` (decode ONCE — A1/D-8) → POST the RAW UTF-8 `content`; the watcher NO LONGER calls `parse.Parse` on the upload path (grep-proven absent) — the SERVER parses (D-1). 401 is TERMINAL (re-prompt + tray red, no retry loop — Pitfall 5); 426 → "update needed"; 409 → logged. The fsnotify 500ms debounce + always-re-read + `rescanCatchUp` survive verbatim. Native onboarding (D-3): `runOnboarding` loops `onboarding.PromptGuildCode` → `backend.Validate(GET /api/v1/whoami)` → `credstore.Store` (DPAPI) → `onboarding.PickEQFolder` → `eqfind.ValidateFolder` (verbatim re-prompt) → `cfg.Save`, ZERO browser/loopback. WATCH-11 first-launch migration (`app.MigrateFromV1`, D-7): raw-reads config.json, deletes the stale `SquireBot:<google-email>` wincred entry, `cfg.Save()`s the new struct (drops `google_email`/`spreadsheet_id`); idempotent (both-keys-absent sentinel); preserves EQFolders+mtime (Pitfall 4). `config.go` dropped `SpreadsheetID`/`GoogleEmail`, added `BackendBaseURL` (config override else the `build_constants.go` `https://api.squirebot.quest` default). `tray.go` shed every Sheets/OAuth item (Open-Workbook/Change-Workbook/Reauthorize/Continue-setup + SpreadsheetID + Show/Hide machinery) for a single always-visible "Enter guild code…" (re-runs RunApp → re-enters onboarding). **THEN DELETED** `internal/{auth,sheet,scaffold,picker,wizard,heartbeat}` (41 files) + `reauth.go`/`reauth_test.go` — the wizard's EQ-folder logic already relocated to `internal/onboarding` in 13-02; `internal/parse` PRESERVED (the backend imports it, Pitfall 2); heartbeat dropped (D-10, its only consumer was `sheet.Client.WriteHeartbeat`). `go mod tidy` shed the ENTIRE Google dependency tree (oauth2, google.golang.org/api, cloud.google.com, grpc, genproto, s2a-go, gax-go, enterprise-certificate); wincred/x/text/sqlite/goose/sqweek retained. `build_constants.go` GUTTED to `Version`+`BackendBaseURL` (no Google secret) and `.github/workflows/release.yml` stripped of the four OAuth `-X` ldflags + the `OAUTH_CONFIG_JSON` materialize step + the `consent_screen_status` PRODUCTION gate (`binary_url`/`binary_sha256` PRESERVED — the auto-updater contract). **Verification:** whole-repo `go build`/`vet`/`test ./...` green (22 pkg, 0 broken — the P11/P12 backend tree untouched, the rewire-then-delete order meant the tree never failed to compile); `go list ./internal/{auth,sheet,…}` all "directory not found"; `go list -deps ./cmd/squirebot` carries NO oauth2/google-api/cloud (and not even `database/sql/driver`). **1 Rule 3 auto-fix:** `reauth.go`/`reauth_test.go` removal pulled into Task 1 (they consumed the deleted `buildTokenSourceFromWincred`; leaving them would break the Task-1 compile) — both were already in the plan's `deletes:` frontmatter, so only the timing moved. 3 atomic commits (`81172b1`/`e66d9ec`/`d2e8025`). 13-04 (polish) ships the 999.20/21/22 nits + confirms the SC-2 materially-smaller-binary byte measure.

### Open TODOs

- **(P11 PocketBase spike — RESOLVED 2026-05-29, Plan 11-01; cleanup DONE 11-05)** Verdict = **HAND-ROLLED Go fallback** (reject PocketBase). All four D-01 probes passed technically, but the locked design bypasses PB's auth-record model (opaque guild-code tokens via a custom `crypto/subtle` guard) and its collection model (plain SQL tables), so PB's admin-UI/auto-REST/Discord-OAuth2 leverage is unused — not worth a 22.9 MB pre-1.0 framework + migration tax. **11-05 wired `net/http` ServeMux + `time.Ticker` and REMOVED the pocketbase dep + `spike/` tree (`go mod tidy` clean, 0 pocketbase refs). P15 Discord OAuth2 is now hand-rolled `golang.org/x/oauth2`.** Verdict in `11-01-SUMMARY.md` + appended to `11-CONTEXT.md`.
- **(A-record for `api.squirebot.quest` — blocked on Hetzner VPS provisioning)** Domain `squirebot.quest` registered at Porkbun (2026-05-29); apex/`www` reserved for the P14 frontend. Remaining: add DNS A-record `api` → the Hetzner VPS public IPv4 once the instance is provisioned (P11 Wave 5 / 11-06 Task 2), so Caddy can issue the TLS cert via HTTP-01. Tracked: `.planning/todos/pending/2026-05-29-add-api-squirebot-quest-a-record-after-oracle-provision.md`.
- **(Port-relevant backlog into P14 — RESOLVED 2026-05-30, Plan 14-02)** 999.28 (`didYouMean('')` empty-query) FIXED via the `if (!query||!query.trim()) return []` first-line guard; 999.30 (`didYouMean` Levenshtein contract) FIXED via path (a) — whole-string Wagner-Fischer is the correct algorithm, the formerly-skipped Test 4 is un-skipped and asserts `toEqual([])`. Both proven in `web/src/lib/__tests__/searchIndex.test.ts` (no `.skip` remains).
- **(Watcher gofmt/console nits into P13)** 999.20 (`console_windows.go` gofmt) + 999.21 (`freeConsole()` doc-vs-impl) + 999.22 (SemVer-aware auto-update comparison — load-bearing for the P16 coordinated flip) ride along with the watcher re-target.
- **(SignPath OSS, separate track, 999.9)** still in flight; orthogonal to the backend swap; lands as a hotfix when the Foundation review completes.

### Active Blockers

None. Roadmap created; Phase 11 ready to plan.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260531-2qk | Uncap bank-coin gold/silver/copper (free non-negative integers, both layers) + link header wordmark to home | 2026-05-31 | bcfd353 | [260531-2qk](./quick/260531-2qk-uncap-bank-coin-gold-silver-copper-link-/) |
| 260531-3ml | Eviction restore web UI (close G-1: new GET /api/v1/admin/restorable + EvictionForm Restore section) + Phase 15 deploy wrap-up docs | 2026-05-31 | 2b2f263 | [260531-3ml](./quick/260531-3ml-eviction-restore-web-ui-close-g-1-phase-/) |

> 260531-2qk + 260531-3ml both DEPLOYED LIVE 2026-05-31 (binary + bundle, sha-verified; rollback at /usr/local/bin/squirebot-server.bak + /var/www/squirebot.prev). 3ml closes UAT gap G-1. Demo data (DemoGuildie/Demoknight) cleaned from prod — real-data-only again.

## Session Continuity

### Last Session Summary

**2026-05-31 (session resumed via /gsd-resume-work — Phase 15 confirmed SHIPPED + LIVE; proceeding to discuss Phase 16):** Restored context. **Reconciled a body↔frontmatter drift:** the frontmatter (`completed_phases: 5`, `last_activity` = Phase 15 deployed + UAT-verified) and git history are AUTHORITATIVE — Phase 15 is done, deployed, and human-verified (all 7 UAT smokes PASS, commit `c748b4b`); quick tasks 260531-2qk + 260531-3ml shipped + redeployed live (sha-verified); the eviction-restore UI closed UAT gap G-1; demo data scrubbed from prod (real-data-only). **The dated entries BELOW this one stop at Plan 15-01 and predate 15-02…15-05 + deploy — treat them as historical, not current.** Only **Phase 16 (Cutover + Decommission)** remains in v2.0; it has no phase dir / CONTEXT yet. Cleaned two stale checkpoint artifacts (HANDOFF.json pointing at the already-done discuss-phase-15; a `.continue-here.md` in the completed Phase 14). No pending todos; every PLAN has a SUMMARY (no incomplete executions). **Real-world gate to confirm at P16 kickoff:** are the ~12 guildies' re-targeted (P13) watchers actually uploading yet? — the shadow-soak's premise. **Stopped at:** Phase 16 (Cutover + Decommission) context gathered

**2026-05-31 (Plan 15-01 executed — Phase 15 schema + store foundation: `00004_web_auth.sql` + websession/admins/eviction/coin store methods; AUTH-08/09 + ADMIN-04/05/06; LOCAL build+verify only, Wave 1):** The hard prerequisite for the whole phase, built in 3 tasks (Tasks 1+2 TDD RED→GREEN, Task 3 impl+tests grouped) across 3 atomic feat commits (~13 min). **Task 1 (`abb92a0`):** forward-only `00004_web_auth.sql` — `web_user`/`web_session`(hash-only, T-15-01)/`guild_admins`(Discord-ID-keyed)/`app_config`(owner-floor singleton) tables + 4 nullable coin columns + `grace_until`/`archived_at` + GENERIC `audit_log` `actor`/`detail`/`at` columns (D-06 reuse-not-duplicate); extended migrate_test.go (fresh-DB Up over 00001→00004 + idempotent re-run + PRAGMA column assertions). **Task 2 (`78d7e24`):** `websession.go` — GenerateSessionID(crypto/rand+hex)/HashSession(sha256)/UpsertWebUser(idempotent, first_seen immutable)/CreateSession(hash-only)/ResolveSession(FAIL-CLOSED ErrSessionExpired/ErrSessionNotFound)/TouchSession(rolling 30d)/DeleteSession/GetWebUser; `SessionTTLSeconds=2592000`; the websession_test PROVES the plaintext id is never stored. **Task 3 (`4b39e40`):** `admins.go`+`eviction.go`+`coin.go` — ports v1 admin.ts (fail-closed IsOfficer, idempotent Add/RemoveOfficerTx with authorize-under-transaction [WR-04 first-in-tx-SELECT re-check], owner-floor protection [ErrOwnerFloorProtected before any write], self-removal orphan rule; SetOwnerFloor seeds a placeholder web_user + bootstrap officer) + eviction (EvictOwnerTx cascades is_removed+grace_until AND revokes guild_code.disabled_at in one tx [D-10], RestoreOwnerTx reverses during grace, ArchiveExpiredEvictions idempotent) + coin (SetCoinTx bank-toon-gated → ErrNotBankToon [T-15-04], `*int64` so entered-0 ≠ unset). **`go build ./...` + `GOOS=linux GOARCH=amd64 go build ./cmd/squirebot-server` + `go vet ./internal/backendsrv/...` + full `go test ./...` all green** (cross-compile artifact gitignored+removed). **1 Rule-3 deviation:** renamed the new commit-on-success test helper `withTx`→`commitTx` (collision with enrich_test.go's rollback-only `withTx`) across the 3 test files — test-only. **Scope honored:** NO deploy, NO live-DB migration, NO systemd touch (user's locked build-only scope); the 4 `DISCORD_*` systemd vars + `set-owner-floor` seed are deploy-time steps deferred to a later deliberate run. **Phase 15 now 1/5.** Next: 15-02 (Discord OAuth2 + opaque session + CORS-creds + `set-owner-floor` CLI) — its checkpoint answer is pre-locked to "build-only" per the Phase 15 execution directives (the user has the Discord app provisioned but the live values go on the box at deploy only).

**2026-05-30 (Plan 14-04 executed — frontend integration capstone: DataGrid×4 + ItemTooltip + search + [data-theme] SiteShell wired to the read API; WEB-01/03/04/05 + BACKEND-05 consumption; PHASE 14 CODE-COMPLETE 4/4):** The visible product, in 3 tasks across 6 atomic commits (Task 1 + 2 `auto`; Task 3 `tdd`). **Task 1 (`5896bc7`):** `api.ts` (5 typed wrappers over `PUBLIC_API_BASE`, snake_case row types, `ApiError`) + the local table-core adapter + `FlexRender`/`renderComponent` + `DataGrid` (sticky Char+header pure-CSS, faceted `<select>` filters, multi-sort caret, zebra, no pagination, tabular-nums) + the 4 exact-order `columns.ts` defs (custom Tier sortingFn) + StatusCell + the view/bank/gear cell components + ItemTooltip (built here because columns.ts depends on it). **Task 2 (`a8b5219`):** SearchBox (maps view rows→SearchResultRow, runs `searchRows`) + SearchResults (holders surfaced, >5 collapse, clickable inline did-you-mean re-run, query auto-escaped) + StateBlock (exact UI-SPEC empty/error+Retry/loading/no-coin copy). **Task 3 (TDD):** RED `205d0c9` (applyTheme `[data-theme]`+persist test fails — `applyTheme is not a function`, 4 cases; the 2 loadTheme regression cases stayed green by design) → **Rule-1 fix `6ca160c`** (the first plain-`.ts` adapter wasn't reactive — `getCoreRowModel is not a function`, header-click no-op, the exact Pitfall-2 failure; replaced with the verified shadcn-svelte runes adapter in `createSvelteTable.svelte.ts`, `createSvelteTable.ts` the runes-free re-export entry; proven by a new `tableAdapter.test.ts` — 8 cases: asc/desc sort + per-column + global filter + column resolution) → GREEN `1cc2264` (implement `applyTheme` — resolveTheme whitelist → velious fallback, single `[data-theme]` write + persist, null-root no-op, T-14.04-04) → wiring `ef3ee70` (`+layout` owns theme state + the `applyTheme` `$effect` + SiteShell; SiteShell wordmark+ThemePicker+CC-BY-SA footer; ThemePicker simple `<select>` over the 5 keys; StatusLegend; `+page` parallel-fetches the 4 views+meta, loading/error/empty states, the 4-tab DataGrid switch + SearchBox, bank `Coin: not yet recorded` never a fabricated zero, empty bank = empty-state not error). **`{@html}` confined to ItemTooltip on the escaped composeItemNote (T-14.04-01); search query plain-interpolated (T-14.04-02); wiki anchors `rel=noopener` (T-14.04-03).** **`npm run check` 0/0; `npm run build` emits index.html+200.html (noindex preserved); `npx vitest run` 57/57** (43 from 14-02 + 6 themeApply + 8 tableAdapter); `@tanstack/svelte-table` absent; `npm run preview` served the SPA shell at HTTP 200. **3 deviations auto-fixed** (1 bug = the non-reactive adapter; 1 blocking = ItemTooltip/cells built in Task 1; 1 hygiene = literal-grep comment rewordings). **PHASE 14 CODE-COMPLETE (4/4 plans).** Next: phase verification + code-review (the orchestrator), then Phase 15 (Discord login + admin write forms). Operational follow-up: publish `web/build/` to Cloudflare Pages at `app.squirebot.quest` + verify Caddy doesn't duplicate the CORS header (Pitfall 5).

**2026-05-30 (Plan 14-02 executed — web/ SvelteKit foundation + the 3 pure-logic ports; WEB-03/04/05; Phase 14 now 2/4):** Stood up the repo's FIRST frontend in 4 tasks (Task 1 scaffold `auto`; Tasks 2-4 TDD ports; 4 atomic feat commits + metadata). **Task 1 (`2ac5357`):** scaffolded `web/` non-interactively (`npx sv create web --template minimal --types ts --add vitest=usages:unit --add tailwindcss=plugins:none --add sveltekit-adapter=adapter:static`) → adapter-static SPA `fallback:'200.html'` (no `kit.paths.base` — deploy LOCKED to Cloudflare Pages at the ROOT subdomain `app.squirebot.quest`, Plan 03's CORS origin); Tailwind v4 CSS-first (`@tailwindcss/vite`, ZERO config files); `app.css` `@import "tailwindcss"` + 6 self-hosted `@fontsource` woff2 (grep-clean of Google Fonts in `src/`+`build/`; 31 woff2 bundled); `app.html` noindex + `robots.txt Disallow:/` (D-05); `+layout.ts` ssr=false/prerender=false; `+page.ts` prerender=true (emit index.html shell); `@tanstack/table-core` installed (NOT the Svelte-4 `@tanstack/svelte-table` trap — Pitfall 1), `@lucide/svelte` (NOT deprecated `lucide-svelte`). **Task 2 (`30123e4`) searchIndex.ts** (WEB-03): `levenshtein` VERBATIM + `didYouMean` 999.28 guard + `groupAndSort`/`COLLAPSE_THRESHOLD=5` + new pure `searchRows` in-memory engine; ALL Apps Script I/O dropped; **999.30 path (a)** — formerly-skipped Test 4 un-skipped → `toEqual([])`. **Task 3 (`2952b2a`) composeNotes.ts** (WEB-04, the HIGH-severity XSS item T-14.02-01): plain-text→escaped rich HTML; `escapeHtml()` (ampersand-first, ASVS V5) on EVERY interpolated value + the wiki URL in the href; `rel="noopener"`; direction TEXT `"0"`/`"1"`/`"2"` (BOTH→ask+buy); a vitest assertion PROVES `<img src=x onerror=…>` renders `&lt;img` not a live tag. **Task 4 (`269b069`) themes.ts** (WEB-05): 5-theme (`velious` default) CSS-custom-property registry, `sheets-default` DROPPED, values from the UI-SPEC Theme Catalog, minimalist weight 400/others 600, heavy inverting parchment tokens, `resolveTheme` enum-whitelist; `app.css` `[data-theme]` block per theme + `:root` velious fallback + heavy `.eq-row`/`.eq-header` + `prefers-reduced-motion`. **`npm run build` 0 (emits index.html+200.html); `npm run check` 0/0; `npx vitest run` 43/43** (17+15+11). **4 deviations auto-fixed:** Rule 3 `+page.ts prerender=true` (emit index.html without flipping the layout's prerender=false); Rule 3 `@types/node` devDep (svelte-check clean); Rule 1 comment rewordings so the literal `.skip`/`CacheService|…`/`sheets-default` greps return 0; Rule 1 removed the scaffold `vitest-examples` demo. **999.28 + 999.30 backlog RESOLVED.** Wave-2: 14-03 (read handlers + CORS, set allow-origin `app.squirebot.quest`) + 14-04 (Svelte client — imports `searchRows`/`composeItemNote`/`THEMES`, builds the `DataGrid` over the installed `table-core` with a local Svelte-5 adapter) remain.

**2026-05-30 (Plan 13-04 executed — polish + ship-prep: 999.20/21/22 nits + SC-2 binary-smaller/no-secret proof; WATCH-09/11; PHASE 13 COMPLETE 4/4):** The last plan of Phase 13 — the three ride-along watcher nits + the milestone's SC-2 confirmation. 3 atomic commits (Task 1 `auto`, Task 2 TDD RED→GREEN, Task 3 read-only confirmation = no commit); whole-repo `go build`/`vet`/`test ./...` green (22 pkg). **Task 1 (`c930fc2`):** `cmd/squirebot/console_windows.go` — `gofmt -w` re-aligned the `var (kernel32…procFreeConsole…)` block (999.20, the extra space before `=` is gone, `gofmt -l` now empty); `freeConsole()` ret==0 now logs `slog.Debug("FreeConsole returned 0 (likely no console attached); continuing")` + `return nil` instead of `slog.Warn`+`fmt.Errorf` (999.21 — kills the spurious detach Warn on every GUI/Explorer launch); doc reconciled to "Returns nil unconditionally / Debug-not-Warn / a no-console launch is the common benign case" (stale Warn-on-failure sentence removed); dropped the now-unused `"fmt"` import. **Task 2 (`e758fb0` RED → `3e8e53b` GREEN):** `internal/update/manifest.go` `IsNewer`/`parseVersion` made SemVer-pre-release-aware (999.22) — `parseVersion(v) (core [3]int, pre string, ok bool)` strips leading `v`, splits the pre-release tail on the FIRST `-`, requires exactly 3 numeric core parts (defensive `ok=false` preserved); `IsNewer` compares MAJOR.MINOR.PATCH then SemVer §11 pre-release precedence on a core tie (a pre-release ranks BELOW its final: `IsNewer("2.0.0-rc1","2.0.0")==true`, `IsNewer("2.0.0","2.0.0-rc1")==false`; `rcN`/`rcM` lexical); the defensive contract preserved — a corrupt manifest (`IsNewer("2.0.0","garbage")==false`) OR an unparseable current version yields `false` (never update on doubt). RED asserted the new pre-release cases fail against the strict 3-part compare (it did, exactly — `2.0.0-rc1→2.0.0` returned false); GREEN landed the SemVer impl mirroring 13-01's `ingest.IsOlder` DIRECTION as a DELIBERATE separate copy (no `internal/backendsrv` import — grep-confirmed the only `backendsrv` token in `internal/update/` is the doctrine doc-comment); the "one version-compare doctrine, per side" rule + the watcher-fails-closed-on-the-update-decision vs server-fails-closed-on-the-gate inversion are documented in the file header. `TestIsNewer_SemVerPreRelease` (11 cases) + `TestIsNewer_DefensiveCorruptVersion` (8 cases) added; all pre-existing `IsNewer`/`Fetch`/`Manifest` tests kept passing. **Task 3 (read-only SC-2 confirmation, no commit):** built the pre-deletion commit `d12fbaf` (13-02 complete, full Google stack) in a detached `git worktree` with identical ldflags (`-H=windowsgui -s -w -X main.Version=2.0.0-dev`) = **17,242,112 bytes (16.44 MB)**, vs the post-deletion re-targeted binary = **7,408,640 bytes (7.07 MB)** → **9,833,472 bytes / 9.38 MB / 57% smaller** (apples-to-apples; corroborates the documented ~17 MB v1 baseline); a 9-pattern byte-accurate ISO-8859-1 whole-blob substring scan of the binary (`oauth2.googleapis.com`, `accounts.google.com`, `client_secret`, `spreadsheets.googleapis.com`, `sheets.googleapis.com`, `googleapis.com/auth`, `GOCSPX-`, `golang.org/x/oauth2`, `google.golang.org/api`) = **0 matches total** (not even the linked-package path strings — the Google tree is genuinely gone, not dead code); `go list -deps ./cmd/squirebot` = **0** packages whose import path contains "google" (and the benign `database/sql/driver` "drive" substring is absent too — this watcher binary doesn't even pull `database/sql`). The detached worktree was removed + the gitignored `dist/` artifacts pruned afterward (no `git clean`/blanket reset — destructive-git prohibition honored). **No deviations** — the 3 target files matched the plan's `<interfaces>` snapshots, the SemVer rework mirrored the already-proven `ingest.IsOlder`, and all 3 SC-2 sub-checks passed on the first build (no Plan-03 regression to re-open). **Path correction recorded:** the orchestrator prompt + CONTEXT D-9 referenced `internal/system/console_windows.go`, but the actual file is `cmd/squirebot/console_windows.go` (the plan frontmatter was right; `internal/system/` is a real package but has no `console_windows.go`). **WATCH-09 + WATCH-11 confirmed complete** (13-04 supplies the SC-2 binary-size byte measure for WATCH-09 + the 999.22 P16-flip de-risk for WATCH-11; both already `[x]` in REQUIREMENTS.md with 13-04 credited in their traceability rows). **PHASE 13 COMPLETE (4/4).** Next: Phase 14 (Web Frontend — SvelteKit static + the read API; depends on P11 read API + P12 data). The P16 coordinated self-update flip is de-risked: the watcher's `IsNewer` and the server's `ingest.IsOlder` now agree on pre-release ranking.

**2026-05-30 (Plan 13-03 executed — re-target integration: watcher SINK→backend, native onboarding, WATCH-11 migration, DELETE the ~8k-LOC Google stack; WATCH-08/09/10/11 COMPLETE, Wave 2):** The highest-stakes plan of the milestone — the rewire-then-delete integration that restores the live data pipeline end-to-end and removes all Google coupling. 3 atomic commits (Tasks 1-2 TDD RED→GREEN, Task 3 `auto`); whole-repo `go build`/`vet`/`test ./...` green at every commit (the tree NEVER failed to compile). **Task 1 (`81172b1`):** rewrote `internal/app/runapp.go` — the SINK swap (D-1/D-8): `makeOnInventoryChange`/`makeOnSpellbookChange` now re-stat → `os.Open` → `io.ReadAll(parse.CP1252Reader(f))` (decode ONCE) → skip-if-`bytes.TrimSpace`-empty → `backend.Ingest(ctx, code, char, kind, string(utf8Bytes), version)`; the watcher NO LONGER calls `parse.Parse` on the upload path (grep-proven absent) — it POSTs raw UTF-8, the server parses. Error switch: 401→tray-red+"re-enter" TERMINAL (no retry, Pitfall 5), 426→"update needed", 409→logged, other→warn; on 204 persist mtime+`cfg.Save()`. The fsnotify debounce + `rescanCatchUp` are untouched. New `runOnboarding` (native `PromptGuildCode`→`backend.Validate(/whoami)`→`credstore.Store`→`PickEQFolder`→`eqfind.ValidateFolder` verbatim re-prompt→`cfg.Save`, zero browser/loopback, D-3). New `internal/app/migrate.go` `MigrateFromV1` (WATCH-11): raw-reads config.json, deletes the stale `SquireBot:<google-email>` wincred entry, `cfg.Save()`s the new struct (drops `google_email`/`spreadsheet_id`); idempotent (both-keys-absent sentinel); preserves EQFolders+mtime (Pitfall 4). `config.go` dropped `SpreadsheetID`/`GoogleEmail`, added `BackendBaseURL`. `backend.Client.Validate` (GET /whoami: 200→nil, 401→ErrUnauthorized) + `SetBackoffForTest` seam. **1 Rule 3 auto-fix:** `reauth.go`/`reauth_test.go` (OAuth-specific, consumed the deleted `buildTokenSourceFromWincred`) were `git rm`'d in Task 1 not Task 3 — leaving them would have left `internal/app` non-compiling at the Task-1 boundary; both were already in the plan's `deletes:` frontmatter, so only the timing moved (faithful rewire-then-delete). **Task 2 (`e66d9ec`):** `cmd/squirebot/main.go` dropped `internal/auth`+`auth.BuildConstants`, routed `--uninstall-wipe-credentials`→`credstore.Delete()`, added `MigrateFromV1` after `config.Load`, new `RunApp(ctx, cfg, baseURL, Version, trayCtl)` (baseURL = `cfg.BackendBaseURL` override else the build_constants default). `build_constants.go` GUTTED to `Version`+`BackendBaseURL` (no Google secret). `tray.go` shed Open-Workbook/Change-Workbook/Reauthorize/Continue-setup + the Show/Hide machinery + SpreadsheetID; added a single always-visible "Enter guild code…" (`OnEnterGuildCode`→re-runs RunApp→re-enters onboarding). MenuPlan = Open log folder / Check for updates / Enter guild code… / Quit. **Task 3 (`d2e8025`):** `git rm -r` of `internal/{auth,sheet,scaffold,picker,wizard,heartbeat}` (41 files; the wizard's EQ-folder logic already relocated to `internal/onboarding` in 13-02; `internal/parse` PRESERVED — the backend imports it; heartbeat dropped per D-10, its only consumer was `sheet.Client.WriteHeartbeat`). `go mod tidy` dropped the entire Google dependency tree (oauth2, google.golang.org/api, cloud.google.com, grpc, genproto, s2a-go, gax-go, enterprise-certificate); wincred/x/text/sqlite/goose/sqweek retained. `.github/workflows/release.yml` stripped of the four OAuth `-X` ldflags + the `OAUTH_CONFIG_JSON` materialize step + the `consent_screen_status` PRODUCTION gate; `-X main.Version`+`-X main.BackendBaseURL` stamped; `binary_url`/`binary_sha256` preserved (auto-updater contract); doc/body re-framed as the backend watcher. **Verification:** `go build`/`vet`/`test ./...` all green (22 pkg, 0 broken — P11/P12 backend untouched); `go list ./internal/{auth,sheet,scaffold,picker,wizard,heartbeat}` all "directory not found"; `go list -deps ./cmd/squirebot` carries NO oauth2/google-api/cloud (and not even `database/sql/driver` — the watcher binary is fully Google-free); release.yml grep clean of OAuthClientSecret/consent_screen_status/OAUTH_CONFIG_JSON with binary_url/binary_sha256 intact. **Known-stale (non-blocking):** 8 provenance doc-comments in the SURVIVING backend files still say "mirrors internal/auth|sheet|heartbeat…" (design lineage, not a live dep) — left as-is rather than churn P11/P12 code. **WATCH-08/09/10/11 marked COMPLETE** (13-04 confirms the SC-2 binary-size byte measure + ships the 999.22 watcher-side SemVer twin). Phase 13 now 3/4; only 13-04 (polish + ship-prep) remains.

**2026-05-30 (Plan 13-02 executed — watcher foundation: `internal/backend` ingest client + `internal/credstore` DPAPI + native Win32 dialog; WATCH-08/09/10 watcher half, Wave 1):** Built the 3 composable watcher-side building blocks the re-target wires (CONTEXT D-5/D-6/D-3), each independently testable with ZERO overlap with the 13-03 deletion (so it shipped Wave-1, parallel to 13-01's backend additions). 5 atomic commits (Tasks 1-2 TDD RED→GREEN, Task 3 `auto`). **Task 1 (`bc3c526` RED → `8f81a66` GREEN):** `internal/backend/client.go` — `Client.Ingest(ctx, code, char, kind, content, version)` POSTs `Envelope{character,kind,content,watcher_version}` to `{base}/api/v1/ingest` with `Authorization: Bearer <code>` + `Content-Type: application/json` + a `SquireBot/<ver> (+github)` User-Agent (version in BOTH the body + the UA so the 426 gate reads either). `classify()` is the D-5 map EXACTLY: `204→nil`, `401→ErrUnauthorized`, `409→ErrCrossOwner`, `426→ErrVersionTooOld`, `400/422→ErrBadPayload`, `>=500→errRetryable`, surprise-status→terminal-generic. Bounded ctx-aware retry `[1s,2s,4s]` (3 attempts) on `errRetryable` ONLY — terminal errors return immediately, NEVER retry (Pitfall 5). Content POSTed verbatim (no client-side CP1252 decode — A1); code/content never logged (V7). Decoupled from `internal/backendsrv/*` (mirrors the Envelope + UA by hand — `go list -deps` clean of backendsrv). httptest client_test.go covers every behavior: 204 request-shape, each terminal status asserts exactly 1 request, 500→204 asserts exactly 2, 500-always asserts the bounded cap + a non-terminal error, network-failure retryable, UTF-8 curly-apostrophe byte fidelity, no-secret-in-logs. **Task 2 (`f4db9f0` RED → `2dd7818` GREEN):** `internal/credstore/store.go` — the salvaged DPAPI wincred survivor of `internal/auth/store.go`: `Store`/`Read`/`Delete` under the single fixed target `SquireBot:guild-code` (no email key — v2 identity is server-derived; D-6/A4), `PersistLocalMachine` (DPAPI at rest). Stores the PLAINTEXT code (the watcher presents it as the Bearer value; the server stores only the hash); never config.json (AUTH-04 carries over — grep-proven only the SECURITY doc mentions config), never logged. The real DPAPI round-trip test PASSES on the Windows dev box (Store→Read equal→Delete→Read not-found; skips off-Windows). **Task 3 (`3665db2`):** `internal/onboarding` — `PromptGuildCode` builds an in-memory `DLGTEMPLATE` (static prompt + single-line EDIT + OK/Cancel) and runs it modally via `user32!DialogBoxIndirectParamW` with a `windows.NewLazySystemDLL`-resolved DLL-preload-safe proc (resolving open_for_planner #1: the dialog manager gives Tab/Enter→OK/Esc→Cancel + a self-contained modal pump for free vs. a hand-rolled `CreateWindowEx` message loop). `PickEQFolder` is the verbatim relocation of `wizard/folderpicker_dialog.go` (sqweek), mapping `dialog.Cancelled`→`ErrCancelled`. `//go:build !windows` stubs return `ErrUnsupported` so `go build/test ./...` stays green off-Windows; `dialog_test.go` asserts the error contract + skips the interactive modal on Windows (validated by the human ship-gate). **ZERO browser, ZERO loopback listener — the `net.Listen|net/http|http.Server` grep over `internal/onboarding/` returns NOTHING** (RESEARCH Pitfall 3; the deletion's spirit honored before the deletion lands). **`go build ./...` + `go vet ./...` + full `go test ./...` all green (zero v1 watcher / backend regression); all 3 new packages clean of `oauth2`/`google.golang.org/api`; `internal/backend` clean of `backendsrv`.** **1 Rule 1 auto-fix:** the first cut of the Win32 callback passed the `*dialogState` Go pointer as `dwInitParam` and reconstructed it via `(*dialogState)(unsafe.Pointer(lParam))` — a GC-unsafe `uintptr`→pointer round-trip `go vet` flagged (the GC can't track an integer-as-pointer across the syscall/callback boundary). Fixed by passing an integer TOKEN as `dwInitParam`, pinning the live state in a mutex-guarded `byToken` map (GC-reachable), claiming it by token on `WM_INITDIALOG` and re-keying by HWND — no `uintptr`→pointer conversion anywhere; `go vet ./...` clean. Build stays green with BOTH the old Google stack and the new packages present (this plan deleted nothing) — 13-03's deletion is a bounded rewire-then-delete. WATCH-08/09/10 remain the watcher half-built (left UNMARKED until 13-03 wires these into `runapp`/`main` + deletes the stack). Phase 13 now 2/4. 13-03 wires `backend.Ingest`+`credstore.Read`+`onboarding.Prompt*` into the re-target.

**2026-05-30 (Plan 13-01 executed — backend additions: `GET /api/v1/whoami` + the `426` min-watcher-version gate + SemVer-aware `ingest.IsOlder`; WATCH-08/09/10 backend half):** Built the ~50-LOC backend half of Phase 13 (CONTEXT D-4) in 3 TDD `auto` tasks (6 atomic commits: 3 test RED + 3 feat GREEN, + metadata) so it ships in ONE VPS redeploy bundled with the P12 DEPLOY-PENDING binary. **Task 1 (`7d3e061` RED → `54b9972` GREEN):** `internal/backendsrv/ingest/version.go` — `IsOlder(present, floor)`, the single backend version-compare truth: strips a leading `v`, compares `MAJOR.MINOR.PATCH`, then SemVer §11 pre-release precedence (a pre-release sorts BELOW its final; tails compared lexically — sufficient for our `rcN`/`betaN` scheme). **Asymmetric failure handling (the load-bearing inversion vs. the watcher's defensive `IsNewer`):** fail-CLOSED on a bad/empty PRESENT version (`IsOlder("","2.0.0")==true`, `IsOlder("garbage",…)==true` — a forged/blank version cannot pass the gate), fail-OPEN on a bad FLOOR const (our misconfig never rejects a real client). Documented the "one truth PER SIDE" doctrine in the header (this + the watcher-side 999.22 twin in Plan 04 are deliberate separate copies; no client↔server internal imports). 20-case `TestIsOlder` table. **Task 2 (`5e35bc0` RED → `b520790` GREEN):** `internal/backendsrv/ingest/whoami.go` — `WhoamiHandler`/`NewWhoami`, the authed, side-effect-free validation endpoint reusing `auth.ResolveToken` VERBATIM → `200 {owner_id, owner_label}` on a valid active code, `401` on missing/unknown/revoked, `405` on non-GET; one inline read-only `SELECT label FROM owner WHERE id=?` (the plan's documented exception — pure read, the only SQL in the file; a scan failure degrades to an empty label, never a 500); never logs the bearer token (V7). Registered `mux.Handle("GET /api/v1/whoami", …)` next to the ingest route in `cmd/squirebot-server/main.go`. `whoami_test.go` (httptest, `ingest_test`) asserts the row-count-unchanged 200, the revoked-401, the 405, and a no-token-in-logs capture. **Task 3 (`2eb3efa` RED → `7256e63` GREEN):** `internal/backendsrv/ingest/handler.go` — `const minWatcherVersion = "2.0.0"` + a `[2.5]` gate `if env.WatcherVersion != "" && IsOlder(env.WatcherVersion, minWatcherVersion)` → `426 Upgrade Required` with the watcher-facing message ("Your SquireBot is too old for this server; it will auto-update shortly."), placed AFTER `DecodeAndValidate` (malformed bodies 400 first) and BEFORE parse/bind/store (a 426 writes nothing), AFTER the bearer guard (a missing token is 401, never 426). **The empty-version allow is the ONE intentional exception** to `IsOlder`'s fail-closed-on-empty rule (forward-compat with the "accepted now" envelope contract). The pre-existing 11-05 ingest suite (bodies carry `watcher_version:"2.0.0"`) stays green; 5 new gate cases (426-writes-nothing, floor/newer/empty→204, too-old-no-auth→401). **`go build ./...` + `go vet ./...` + full `go test ./...` all green (zero v1 watcher regression); `go list -deps ./cmd/squirebot-server` clean of oauth2/sheets/google-api/cloud/pocketbase/genproto; static `CGO_ENABLED=0 GOOS=linux GOARCH=amd64` build produces a statically-linked x86-64 ELF** (the deploy artifact). **No Rule 1/2/3 deviations** (the contract files matched the plan's `<interfaces>` exactly; the `2.0.0` floor kept every pre-existing test green untouched). **1 out-of-scope discovery logged, NOT fixed** (SCOPE BOUNDARY): `main.go`'s `const defaultAddr` has a pre-existing `gofmt -l` comment-alignment nit (present in `aabf9b8`, unrelated to the route edit) → `deferred-items.md`. **Requirements WATCH-08/09/10 are PARTIALLY advanced (backend half only) — left UNMARKED;** the watcher re-target/onboarding/DPAPI work (13-02/03) must land before they're complete. Phase 13 now 1/4; the DEPLOY-PENDING bundle (P12 + 13-01) is build-ready. 13-02 (watcher foundation) can now build its `internal/backend` client against the real `/whoami` validate target + the real 426 contract.

**2026-05-30 (Plan 12-05 executed — scheduler flesh-out + run-job CLI; ENRICH-10/11 proven end-to-end — PHASE 12 COMPLETE):** Fleshed the 11-05 no-op `time.Ticker` skeleton into a real db-backed job registry and wired it into the server, in 2 TDD `auto` tasks (2 atomic feat commits + metadata). **Task 1 (`b08dbea`):** rewrote `internal/backendsrv/scheduler/scheduler.go` — `const checkInterval = 10*time.Minute` (the 1h `HeartbeatInterval` constant fully removed); `type Job struct{Name; Due func(last,now) bool; Run func(ctx) error; mu sync.Mutex}`; cadence predicates `duePigparse` (`now-last>=24h`) + `dueWiki` (`now.Weekday()==Sunday && last.Before(startOfSundayUTC(now))`) + the `startOfSundayUTC` helper; `Start(ctx, db)` builds the 2-job registry (`pigparse_daily`→`jobs.RunPigparse`, `wiki_weekly`→`jobs.RunWiki`, both with `politefetch.Fetch`) and spawns `go run(...)`; `run(ctx, db, registry)` loads each cursor via `store.GetJobRun` (NULL→zero→due), does an **immediate check pass before the ticker loop** (due-on-startup-if-missed), then the ticker loop with the `ctx.Done()` shutdown branch **kept verbatim** (the tested contract); `runJob` does `mu.TryLock()` (skip-not-queue overlap, the LockService replacement) → `Run` → `SetJobRun` **after, advance-always even on error (A2)** so a failing fetch retries next window not every tick. **Cursor ownership = scheduler is authoritative** (documented in a `runJob` comment): the jobs' internal `SetJobRun` (richer detail) is a harmless idempotent overwrite; the load-bearing invariant is that `last_run_at` advances after every attempt. 6 new tests (`TestDuePigparse`, `TestDueWiki`, `TestStartOfSundayUTC`, `TestRunJob_PersistsCursorAndPreventsOverlap`, `TestRunJob_AdvancesCursorOnError`, `TestRun_ImmediateCheckRunsDueJob`) + the 2 pre-existing ctx-cancel tests updated to the new `(ctx, db)` signatures (assertions preserved). **Task 2 (`2caede6`):** `cmd/squirebot-server/main.go` — added `case "run-job": return runJobCmd(args[1:])` to the dispatch + `func runJobCmd` (split flags/positionals, **exactly-one** job name `pigparse|wiki` else exit 2, `openMigratedDB`+`logging.Setup`+`signal.NotifyContext` so Ctrl-C aborts a long wiki run, dispatch to `jobs.RunPigparse`/`jobs.RunWiki` with `politefetch.Fetch`) — the D-7 parity-check entrypoint paralleling the Sheet's "Refresh Now" menu, no HTTP surface; replaced the no-op `scheduler.Start(ctx)` with the real `scheduler.Start(ctx, db)`; added the `enrich/jobs` + `enrich/politefetch` imports. 4 new arg-handling tests (`TestRun_RunJob_BadName/_MissingName/_ExtraPositional/_NameAroundFlag`, no live fetch — the live success paths are covered by 12-04's httptest job tests + the manual D-7 parity check). **Off-cloud-suite invariant holds:** `go list -deps ./cmd/squirebot-server` clean of oauth2/sheets/google/pocketbase (only stdlib `database/sql/driver` substring-matches "drive"); the whole-file brand grep on main.go = 0. **`go build ./...` + `go vet ./...` clean; whole-repo `go test ./...` = 25 pkg, 0 broken (no v1 watcher regression); static `CGO_ENABLED=0 GOOS=linux GOARCH=amd64` build produces an 11.5 MB statically-linked x86-64 ELF** (the deploy artifact now carries scheduler + jobs + 00003 migration). **No rule-triggered deviations** — 2 cosmetic doc-comment rewordings (the `HeartbeatInterval` doc mention reworded so the constant-name grep returns 0; four brand-substring comment lines reworded so the Off-cloud grep returns 0; both zero-behavior) + 1 `go vet` lock-copy guard removed pre-commit. **PHASE 12 IS COMPLETE (5/5).** Deploy/soak (drop binary + restart; goose.Up applies 00003; immediate check pass runs due jobs within seconds; `run-job` for the D-7 parity check) is the P12 rollout. Phase 13 (watcher re-target) is the next plannable phase.

**2026-05-30 (Plan 12-04 executed — the 2 enrichment jobs: daily PigParse + weekly wiki single-run; ENRICH-10/11):** Built the composition layer where fetch → parse → upsert comes together, in 2 TDD `auto` tasks (2 atomic feat commits + metadata) into a new `internal/backendsrv/enrich/jobs` package. **Task 1 (`9c8120c`):** `store.DistinctInventoryItemIDs`+`ItemRef` (dedup-by-id `(item_id,name)` inventory union excluding 0/NULL — the wiki items pass iterates it; Rule 1 fix: GROUP BY item_id + MIN(name), NOT `SELECT DISTINCT item_id,name`, so a same-id-different-name item isn't fetched twice — matches the Sheet's first-name-wins union); `store.GetJobRunDetail` (read `last_detail` for the truncation baseline on the single SQL path — Rule 3, 12-01's GetJobRun returns status not detail); `jobs/urls.go` (hardcoded `PigparseURL`+`WikiAPIBase` constants = SSRF-safe + `wikiParseURL` builder, redirects=1, query-escaped underscored title, doubles as the etag_cache key); `jobs/pigparse.go` `RunPigparse` (GetETag → Fetch → **304 skips parse+write** Pitfall 6 → ParseToRows → **D-9 WTS t=0 filter** as one isolated in-place step → **D-4 truncation-guard-as-LOG** then PROCEED, no return after the warn → `UpsertPigparsePricesTx` over one tx → SetETag+SetJobRun, stashes `rows=N`). 4 tests: WTS-only (4,333 rows, item 19450 keeps t=0 price 239/30/"0"), 304-skips-write (sentinel untouched), truncation-logs-but-writes, fetch-error-records-error. **Task 2 (`a79b37b`):** `jobs/wiki.go` `RunWiki` — ONE uninterrupted pass (NO 6-min cursor — D-5): items+quest_items → per-class spells (display name via inverted CLASS_DISPLAY_TO_ABBREV) → 2-page gear-tier full replace; **1s ctx-aware `wikiSleepFn` sleep before every wiki fetch** (SC-4, no-op in tests, no bare time.Sleep); **per-item SHA-1 short-circuit** via `GetItemMasterSHA1Tx`; **per-resource 304-skip**; **gear full-replace ONLY with the complete combined set** (a failed/304 page skips it — Pitfall 1); **log-but-continue** on a bad page (never aborts). 5 tests over an httptest fixture server: all-4-tables-populated (+Iksar +summary/sha1), SHA-1-short-circuit (no row churn on identical re-run), 304-skips-resource (PAL 304 absent while NEC 200 populates), gear-full-replace-no-duplicates (N not 2N), one-bad-page-no-abort (Pearl 500 absent, others populate). Reworded the wiki.go/urls.go/pigparse.go doc comments so the `<verification>` machinery grep (CURSOR|monitorCellCount|weeklySchemaHealthcheck|buildSpellCheck|buildGearCheck|LockService|PropertiesService) returns **0** across the production job files (no behavior change; the deleted machinery was never ported). **`go build`/`go vet` clean; `go test ./internal/backendsrv/...` 0 fail; whole-repo `go test ./...` = 28 pkgs, 0 fail (no v1 watcher regression); production-job-file inline-SQL grep = 0.** **2 deviations auto-fixed** (Rule 1 dedup-by-id, Rule 3 GetJobRunDetail) + cosmetic doc rewordings. Wave-3 12-05 (scheduler + wiring) is the only remaining Phase-12 plan.

**2026-05-30 (Plan 12-02 executed — 4 pure enrichment parsers + eq-constants ported to Go; ENRICH-10/11):** Ported the I/O-free heart of both enrichment jobs in 3 TDD `auto` tasks (3 atomic feat commits + metadata) into a new `internal/backendsrv/enrich` package. **Task 1 (`012d082`):** copied all 12 `apps-script/src/__fixtures__/*.json` byte-identical into `enrich/testdata/` (pinned `binary` in `.gitattributes` mirroring the `internal/parse/testdata/*` precedent — a CRLF rewrite would flip the wikitext SHA-1 on checkout); `eqconst.go` (CLASSES/CLASS_DISPLAY_TO_ABBREV/WIKI_SLOT_TO_INV_SLOTS, dependency-free); `pigparse.go` `ParseToRows`+`PigparseRow` (port of `parseToRows`/`isValidRow`/`coerceRow` with the 1% malformation tolerance via per-row `json.RawMessage` typeof-checks; surfaces only Sheet-persisted price fields, omits T90/A90/Tc/Ta; keeps both t=0/t=1 rows — dedup deferred to the job, D-9). **Task 2 (`1a2fc15`):** `wikiitem.go` `ParseItempage`+`ParsedWikiItem`+`WikiQuestItemLink`+`sha1Hex` (full {{Itempage}} parser; surfaces ONLY the 6 persisted fields per D-8; `sha1Hex` = `crypto/sha1`+hex, NO signed-byte fix-up → byte-identical lowercase hex; `pageNameToSlug` reimplements JS `encodeURIComponent` because `url.PathEscape` over-escapes apostrophes — Rule 1 fix caught by `TestPageNameToSlug`). **Task 3 (`d519f37`):** `wikispell.go` `ParseClassPage`+`WikiSpellRow`+`NormalizeSpellName` (SpellRow/RadSpellRow/RadSpellRow2 header pass + Bard SongRow inline-level fallback; `NormalizeSpellName = lower(trim)` = the store join key, NOT the TS variant — deliberate per D-12) + `wikigear.go` `ParseGearTierPage`+`WikiGearTierRow`+`Tier` consts (lookahead-free `extractListItems` since RE2 has no lookahead; Iksar re-tag on Pre-Raid; `ItemID` always nil). **Byte-parity proven** by running the TS parsers in Node (esbuild+vm+Utilities SHA-1 shim) and diffing exact output: PigParse 7240 rows; NEC 171/PAL 66/Warrior 0 spells; Pre-Raid 577/Raiding 606 gear rows, 14 classes, 4/0 Iksar; cloth-cap SHA-1 `5ed737f5…`; Summaries + quest-links identical incl. the `…` ellipsis (throwaway scripts/dump-tests removed pre-commit). **28 enrich tests pass; `go build ./...`/`go vet ./...` clean; FULL `go test ./...` green** (every watcher + backend pkg, no regression); no `go.mod` change (stdlib only). **2 deviations auto-fixed** (Rule 1 encodeURIComponent, Rule 3 .gitattributes binary) + 3 cosmetic doc-comment rewordings so the plan's purity/`signed-byte` acceptance greps return 0. Wave-2 12-04 (jobs) + Wave-1 12-03 (politeFetch) unblocked.

**2026-05-29 (Plan 12-01 executed — 00003 enrich migration + store/enrich.go + store/jobstate.go; ENRICH-10/11 foundation):** Landed the hard prerequisite for all of Phase 12 in 3 TDD `auto` tasks (3 atomic feat commits + metadata). **Task 1 (`5cd347b`):** `internal/backendsrv/migrations/00003_enrich_columns.sql` — 8 single-column `ALTER TABLE pigparse_price ADD COLUMN` (t30,a30,t60,a60,t6m,a6m,ty,ay) + `CREATE TABLE job_run` (scheduler cursor) + `CREATE TABLE etag_cache` (politeFetch 304 state); `t90`/`a90` omitted for Sheet parity (D-7); 00001/00002 byte-unchanged; embed.go untouched (auto-included). `TestMigrate_00003_AddsEnrichColumnsAndTables` asserts the 8 cols via `PRAGMA table_info` + both tables via `sqlite_master` + idempotent re-run. **Task 2 (`f96e96d`):** `store/enrich.go` — the 5 dimension write methods with the D-12 per-table strategy: `UpsertPigparsePricesTx`/`UpsertItemMasterTx` per-item `ON CONFLICT(item_id)` (D-4: truncated response updates what it got, leaves the rest), `UpsertWikiSpellsForClassTx` per-class DELETE+INSERT (normalized_name = lower(trim) — the P14 join key), `ReplaceWikiGearTierTx` FULL-TABLE replace (UNIQUE-on-NULL is broken → per-row upsert would duplicate every Sunday, Pitfall 1; proven N-not-2N), `ReplaceQuestItemsForIDTx` per-item-id DELETE+INSERT; `GetItemMasterSHA1Tx` ("" when absent) for the wiki SHA-1 short-circuit. Symmetric `Store.X`/`XTx` split mirrors replace.go; store-local input structs (no store→enrich import cycle); `?` placeholders only. `enrich_test.go`: ON CONFLICT updates-in-place + partial-reupsert-leaves-rest, SHA-1 getter present/absent, per-class spell drop + other class untouched, gear-tier 2nd identical replace == N, quest per-id scoped replace. **Task 3 (`97409ba`):** `store/jobstate.go` — `GetJobRun`/`SetJobRun` (absent/NULL → zero time + ok=false = "never-run ⇒ due-on-startup"; upsert one row per job; advance-always-even-on-error) + `GetETag`/`SetETag` (upsert; 304 leaves row untouched). `jobstate_test.go`: empty-table-not-due, RFC3339 round-trip via `.Equal` + upsert count==1 (job_run + etag). **Single-tested-SQL-path holds:** grep-confirmed zero enrichment SQL in `scheduler/` or `ingest/`. **FULL `go test ./...` green** (24 pkgs, 0 fail — no v1 watcher regression); `go build ./...`/`go vet ./...` clean; no `go.mod` change (stdlib + existing modernc/goose only). **No rule-triggered deviations** — 2 minor notes where the plan's prose grep COUNTS didn't match its own verbatim SQL/comment style (00003 Down uses `DROP COLUMN` not `ADD COLUMN`; jobstate doc-comments cite the `ON CONFLICT` clause alongside the SQL), neither a behavior divergence. Wave-2 (12-04 jobs / 12-05 scheduler) + Wave-1 12-02 parsers / 12-03 politeFetch unblocked — they compose these `*Tx` methods over one tx and read/write the `job_run`/`etag_cache` cursors.

**2026-05-29 (Plan 11-05 executed — POST /api/v1/ingest + cmd/squirebot-server + scheduler skeleton; BACKEND-01/03/04 wiring):** Wired the backend into a runnable single binary in 3 tasks (4 atomic commits: 1 chore + 3 feat) + metadata. **Verdict resolved (FALLBACK from 11-01):** dropped `pocketbase` + `dbx` from go.mod, deleted `spike/pocketbase/`, `go mod tidy` → 0 pocketbase refs in go.mod/go.sum; all wiring is HAND-ROLLED `net/http`. **Task 1:** `internal/backendsrv/logging.Setup()` (JSON slog to os.Stdout/journald, NOT lumberjack/LOCALAPPDATA) + `ingest.Envelope` (D-04) + `DecodeAndValidate` (required-field + kind-enum, typed sentinels `ErrMalformedJSON`/`ErrMissingCharacter`/`ErrInvalidKind`; empty content = no-op; unknown fields ignored). **Task 2:** `ingest.Handler`/`New` + `ServeHTTP` — ordered flow `MaxBytesReader(1<<20)` → `auth.ResolveToken` FIRST (`!ok`→401+RETURN before any store call) → `DecodeAndValidate` (4xx) → `parse.Parse`/`ParseSpellbook` (UTF-8, no CP1252) → `store.BindCharacter` + `store.Replace*Tx` in ONE `db.BeginTx` → 204; cross-owner→409 (audit row COMMITTED). **Single-tested-SQL-path (WARNING-3):** added exported `auth.(*Auth).ResolveToken`, `store.BindCharacter`, `store.ReplaceInventoryTx`/`ReplaceSpellbookTx` (public `Store.Replace*` now delegate) — handler authors NO inline SQL (grep-confirmed). 11 httptest cases: valid inv/spellbook rows queryable, 401-writes-nothing (no-header/unknown/revoked), shrinking 3→1 drops rows, cross-owner 409+audit+untouched, bad-kind/malformed/oversized 4xx no-write, empty-content no-op. **Task 3:** `scheduler.Start(ctx)` time.Ticker skeleton (no jobs, returns on cancel) + `cmd/squirebot-server/main.go` testable `run([]string) int` dispatching mint/revoke/serve; serve = logging.Setup + store.Open + RunMigrations (goose.Up on startup, D-10) + scheduler + ServeMux `POST /api/v1/ingest` on loopback + graceful shutdown. NO Google/OAuth/Sheets dep (`go list -deps`=0). **3 deviations auto-fixed:** Rule 2 (cross-package exported wrappers — 11-03/04 internals are unexported; anticipated by 11-04); Rule 1 (cross-owner 409 must COMMIT to persist the audit row, a blanket rollback discarded it — caught by the plan's own `TestIngest_CrossOwner_409`); Rule 1 (revoke-code flag parsing was position-dependent — `splitFlagsAndPositionals` fix). **FULL `go test ./...` green** (every watcher + backend pkg, no v1 regression); `go build`/`go vet` clean; **`CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build ./cmd/squirebot-server` produces a static ELF** (the deploy binary 11-06 ships). BACKEND-01 (single-binary+scheduler half) / BACKEND-03 (delivery) / BACKEND-04 (401-writes-nothing) satisfied at the build/test tier; 11-06 (deploy) + 11-07 (backup) remain.

**2026-05-29 (Plan 11-04 executed — bearer-token auth: mint/revoke CLI + resolveToken guard, BACKEND-04):** Built the project's first authenticated network surface in two TDD tasks (RED→GREEN each; 4 atomic commits + metadata). New package `internal/backendsrv/auth/` (stdlib crypto only — no new third-party deps). **Task 1 (mint/revoke):** `MintCode(db, ownerLabel)` = 32-byte `crypto/rand` → `base64.RawURLEncoding` plaintext shown ONCE via `fmt.Printf` (stdout) + returned, stores ONLY `sha256(plaintext)` in `guild_code.token_hash` (hash-only at rest, mirrors `internal/auth/pkce.go` crypto shape + `internal/auth/store.go` "secret never leaves as plaintext" discipline); `upsertOwner` SELECT-then-INSERTs by label (owner.label has no UNIQUE → reuse, no duplicate owners); `RevokeCode(db, idOrLabel)` sets `disabled_at WHERE (label OR id) AND disabled_at IS NULL` (idempotent, D-09). **Task 2 (guard):** `Auth{db}` + `New(db)` + `resolveToken(ctx, authHeader) (ownerID, ok)` strips `Bearer ` (or fails), `sha256.Sum256`-hashes the presented code, queries active rows (`disabled_at IS NULL`), `subtle.ConstantTimeCompare`s each (timing-safe, RESEARCH Pattern 3); missing/malformed/unknown/revoked/wrong-scheme → `(0,false)`. No PocketBase `apis.RequireAuth` (opaque static tokens); the token is hashed before any DB use (no SQL interpolation); never logged (V7 — `auth_reject` slog records carry only a reason). 5 mint/revoke tests + 3 guard tests (7-case table + minting-owner + AST-parsed structural check) all pass. **`go build ./...`, `go vet ./...` clean; `go test ./internal/backendsrv/...` exit 0; full `go test ./...` exit 0 (no v1 watcher regression).** **2 deviations (auto-fixed):** Rule 2 — added the exported `auth.New(db)` constructor (the plan's `<interfaces>` gave `Auth{db unexported}` + `resolveToken` but no constructor; 11-05 is a separate package); Rule 1 — the structural no-PocketBase test initially `strings.Contains`-matched the guard's own doc comment (which names `apis.RequireAuth` to explain it's avoided), rewrote it to AST-parse imports for any `pocketbase` path. No `go.mod` change (stdlib only). All 6 STRIDE `mitigate` threats + ASVS V2/V6/V7 covered at the test tier. 11-05 imports the auth API UNCHANGED and wires the 401-writes-nothing path.

**2026-05-29 (Plan 11-03 executed — UTF-8 parser refactor + atomic replace tx + first-sighting bind, BACKEND-03):** Built the heart of BACKEND-03 in three TDD tasks (RED→GREEN each; 6 atomic commits). **Task 1 (encoding A1):** dropped `charmap.Windows1252` from the shared `Parse`/`ParseSpellbook` (they now treat their `io.Reader` as already UTF-8) and relocated the decode into ONE exported helper `parse.CP1252Reader`; wrapped the v1 watcher's two production call sites (`internal/app/runapp.go` inventory + spellbook) in `parse.CP1252Reader` (zero behavior change off disk); re-homed the 4 CP1252-dependent tests into `reader_test.go` + added `TestParse_UTF8Content_NoDoubleDecode`. **Task 2 (atomic replace):** `store.Store`/`NewStore` + `ReplaceInventory`/`ReplaceSpellbook` = one `BEGIN IMMEDIATE…DELETE-all…INSERT…UPDATE…COMMIT` (RESEARCH Pattern 1); shrinking snapshot drops rows (BACKEND-03); integers persist as real INTEGER via `strconv.Atoi` (Sheets `StringValue` hack DROPPED); `row_ordinal`=line order; spellbook `normalized_name`=`lower(trim(name))`; proven atomic on error (FK-violation rollback, neighbour rows untouched). **Task 3 (binding):** `bindCharacter(ctx, *sql.Tx, name, ownerID)` + `ErrCharOwnedByAnother` — first sighting binds to the uploading owner, same-owner re-bind is a no-op, a DIFFERENT owner is REJECTED + `audit_log` row, owner_id never overwritten (v2 tightens v1's warn-and-allow, D-07/V4); single indexed `SELECT … WHERE name` (UNIQUE COLLATE NOCASE); new forward-only `00002_audit.sql` (00001 untouched; goose idempotent at version 2). **FULL `go test ./...` green** (every watcher AND backend package — no v1 regression); `go build`/`go vet` clean; no `go.mod` change. **No deviations.** `bindCharacter`/`Replace*` take a `*sql.Tx`/`*sql.DB` so 11-05 composes bind + replace in ONE tx behind `POST /api/v1/ingest`.

**2026-05-29 (Plan 11-02 executed — goose schema + modernc DB-open + NewTestDB, BACKEND-02):** Built the verdict-agnostic SQLite persistence foundation. Created `internal/backendsrv/migrations/00001_init.sql` (forward-only goose migration — all ten D-13 tables: `owner`/`character` split with FK + `name UNIQUE COLLATE NOCASE`, `inventory_item`/`spellbook_entry` with `ON DELETE CASCADE`, `guild_code`, and the five EMPTY dimension tables, copied verbatim from RESEARCH §"Migration SQL Sketch") + `embed.go` (`//go:embed *.sql` + `goose.SetDialect("sqlite3")` + `goose.Up`, idempotent). Created `store/db.go` (`Open`/`DSN` on the modernc `"sqlite"` driver with the RESEARCH Pattern 5 DSN — WAL/busy_timeout(5000)/foreign_keys(ON)/synchronous(NORMAL)/_txlock=immediate — + `SetMaxOpenConns(1)` single-writer) and `store/testhelper.go` (`NewTestDB(t)` shared temp-DB fixture: `Open` + `goose.Up`, in a non-`_test.go` file so 11-03/04/05 can import it). Six tests pass (foreign_keys=1 on a fresh conn; all 10 tables created; goose.Up idempotent on re-run; 5 dimension tables empty); `go build ./...`, `go vet ./...` clean; `CGO_ENABLED=0` builds on host + `linux/amd64` (pure-Go modernc, no cgo). Task 2 followed TDD RED→GREEN. 3 atomic commits (`c262816` feat migration, `3117a1a` test RED, `165582a` feat GREEN). **No deviations.** `go mod tidy` promoted `modernc.org/sqlite` + `goose/v3` to direct deps (and reclassified the pre-existing spike's `pocketbase` dep to direct — removed in 11-05 as planned, not touched here). Wave-3 (11-03 store tx / 11-04 auth store / 11-05 ingest+HTTP) unblocked.

**2026-05-29 (Plan 11-01 executed — PocketBase spike):** Ran the time-boxed D-01 PocketBase-as-framework spike (the gating plan of Phase 11). Pinned `github.com/pocketbase/pocketbase@v0.39.0`, confirming it pulls the same no-cgo `modernc.org/sqlite v1.51.0` the hand-rolled fallback uses (the headline RESEARCH finding, now empirical). A throwaway harness (`spike/pocketbase/main.go`) exercised all four probes with recorded PASS evidence: (a) 5 raw-DDL tables coexist with PB system tables; (b) custom `crypto/subtle` bearer guard → 401 no/bad token, 200 valid, with idempotent DELETE+INSERT atomic replace in `RunInTransaction`; (c) `*/1 * * * *` cron fired at the minute boundary while serving; (d) `CGO_ENABLED=0 GOOS=linux GOARCH=amd64` produced a 22.9 MB static ELF x86-64 binary from Windows. **VERDICT = HAND-ROLLED Go fallback** (reject PocketBase): the locked design bypasses PB's auth-record + collection models, so its admin-UI/auto-REST/Discord-OAuth2 leverage is unused — not worth a pre-1.0 framework + migration tax. Verdict recorded in `11-01-SUMMARY.md` and appended to `11-CONTEXT.md`. 3 atomic commits (`b894008` feat, `adeaead` chore, `b797c64` docs). 11-02 (verdict-agnostic) and 11-05 (now `net/http` ServeMux + `time.Ticker`; removes the PB dep + spike tree) unblocked.

**2026-05-28 (v2.0 ROADMAP created):** Roadmapper transformed the 26 v2.0 requirements into a 6-phase structure (Phases 11–16), accepting REQUIREMENTS.md's provisional A–F mapping unchanged (no concrete coverage problem found). Coverage validated 26/26 (P11=5, P12=2, P13=4, P14=6, P15=5, P16=4); no orphans, no duplicates. Dependencies set: P11 has none; P12→P11; P13→P11; P14→P11+P12; P15→P14+P11; P16→P13+P14+P15+P12. Each phase got 2–5 observable success criteria. Honored the locked stack (Oracle Always Free + SQLite + goose; SvelteKit; Discord OAuth2; bearer token) over the research docs' Hetzner+Postgres recommendation, with an explicit "locked overrides research" note in ROADMAP. Front-loaded the ingest path (P11 + P13 restore data flow before the polished P14 frontend). Surfaced the optional PocketBase spike as a P11 decision note. Wrote `.planning/ROADMAP.md` (v2.0 section + Phase Details + progress tables + backlog re-annotated for Sheet-mooting), finalized `.planning/REQUIREMENTS.md` traceability (Phase column finalized), and reset this STATE.md to the v2.0 phase plan (replacing the stale v1.0.2 phase-plan content). Phase numbering starts at 11 (Phase dirs 09+10 exist on disk from the superseded v1.0.2 binary; never reuse 9/10). *(2026-05-29: backend host later switched Oracle → Hetzner — see Phase 11 CONTEXT D-12; the "$0 backend / ~$12/yr total" premise this entry recorded is retired in favour of ~$67/yr.)*

**2026-05-13 (v1.0.2 binary shipped):** Phases 9 + 10 shipped as binary tag `v1.0.2`; HUMAN-UAT was blocked on 999.19 (Google brand verification). Milestone close subsequently superseded by the v2.0 "Off Google" pivot — the Sheet it targeted is being replaced, so a Google-OAuth-dependent UAT close became moot.

### Files of Record

- `.planning/PROJECT.md` — core value, requirements (v1 Validated + v2.0 Active), constraints, Key Decisions (updated 2026-05-28 with v2.0 milestone).
- `.planning/REQUIREMENTS.md` — v2.0 requirements (26 across 7 categories) + finalized traceability (Phases 11–16).
- `.planning/ROADMAP.md` — v2.0 Phases 11–16 (full Phase Details + success criteria) + collapsed v1.0/v1.0.1/v1.0.2 + Backlog (updated 2026-05-28).
- `.planning/MILESTONES.md` — historical record (v1.0 + v1.0.1; v1.0.2 not recorded — close superseded).
- `.planning/explorations/website-milestone/SCOPE.md` — authoritative v2.0 research (A–F phase plan, dependencies, effort, open decisions).
- `.planning/explorations/website-milestone/{01-backend-hosting,02-frontend-stack,03-watcher-auth,04-data-enrichment-migration}.md` — 4 findings docs.
- `.planning/RETROSPECTIVE.md` — living retrospective (v1.0 + v1.0.1 + Cross-Milestone Trends).
- `.planning/STATE.md` — this file.
- `.planning/config.json` — granularity (coarse), mode (yolo), workflow toggles.

### Next Action

**Phase 14 is EXECUTING (3/4 plans; 14-01 + 14-02 + 14-03 done 2026-05-30; 0 blockers).** Phases 11/12/13 COMPLETE. Next (and last P14) plan = **14-04 (Svelte client)** — wires the now-live read API into the visible product.

1. **`/gsd-execute-phase 14`** (continues at plan 14-04 — Svelte client, Wave 3, depends 14-02+14-03): API client + a local Svelte-5 `@tanstack/table-core` adapter (the package is already installed) + the one reusable `DataGrid` (sticky Char+header, faceted filters, multi-sort) ×4, the hover/tap `ItemTooltip` (wraps the already-escaped `composeItemNote`), `SearchBox`/`SearchResults` (`searchRows` + inline "did you mean?"), `[data-theme]` SiteShell+ThemePicker (`THEMES`/`loadTheme`/`saveTheme`). The home `+page.svelte` is still the scaffold placeholder — 14-04 replaces it. **Wire `lib/api.ts` to the 5 endpoints 14-03 pinned** (see 14-03-SUMMARY.md "THE PINNED ENDPOINT JSON CONTRACT"): every view endpoint returns a JSON array (`[]` when empty, never null); `/views/bank` → `{rows,coin:null}`; `/meta` → `{characters:[{name,last_seen}]}`; `price` nullable; `prices[].direction` a string `"0"`/`"1"`/`"2"`; CORS origin `https://app.squirebot.quest`.
   - **14-03 done:** `internal/backendsrv/readapi/` — 5 public read handlers (one `ViewsHandler` dispatched by view name + `MetaHandler`) mirroring `whoami.go` minus the bearer guard (D-04 public), zero writes, V7 slog; stdlib CORS (exact origin, no wildcard, OPTIONS→204) wrapping the whole mux via a `-cors-origin` flag; httptest-proven; `go test ./internal/backendsrv/...` + static linux/amd64 cross-compile + `go vet ./...` green. The JSON contract is PINNED for 14-04. The read API is BUILT (deploys with the next on-box redeploy — item 2).
   - **14-02 done:** the `web/` SvelteKit SPA scaffolds/builds/type-checks (adapter-static, Tailwind v4, self-hosted fonts, noindex); the 3 pure modules are ported with provable parity + both carried bugs fixed + the HIGH-severity tooltip XSS mitigation test-proven; 43/43 vitest green.

2. **Deploy (P12 + P13-01 bundled, on-box ops) — NOW UNBLOCKED:** the P12 enrichment binary is DEPLOY-PENDING and 13-01's `/whoami` + `426` gate are BUILT into `main`, shipping in the SAME redeploy. Build stamped — `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -ldflags "-s -w -X github.com/boejowen/SquireBot/internal/backendsrv/buildinfo.Version=<tag>" -o squirebot-server ./cmd/squirebot-server` (static x86-64 ELF verified) — drop on the Hetzner VPS (`https://api.squirebot.quest`, 5.78.232.85) + restart systemd; `goose.Up` applies 00003. Then `run-job pigparse|wiki` + the SC-4 Sheet-parity spot-check (`12-HUMAN-UAT.md`). Can deploy now that 13-01 has landed (one redeploy ships both). No new systemd timer (scheduler is in-process).

**Encoding caution (A1):** the re-targeted watcher decodes CP1252→UTF-8 ONCE via `parse.CP1252Reader`, POSTs UTF-8 `content`; the server does NOT re-decode.

**Phases 11 + 12 + 13 all COMPLETE** (live backend at `https://api.squirebot.quest`; the DB self-populates dimension data on cadence; the watcher is re-targeted + Google-free + ship-prepped). 3 of 6 v2.0 phases done; next is Phase 14 (Web Frontend). The re-targeted watcher binary is verified materially smaller (57%) + Google-secret-free (13-04 SC-2 proof) and the auto-update compare is pre-release-safe.

---

*State initialized: 2026-04-30. v1.0 COMPLETE: 2026-05-11. v1.0.1 COMPLETE: 2026-05-12. v1.0.2 binary shipped: 2026-05-13 (close superseded). v2.0 OPENED + ROADMAP created: 2026-05-28 (Phases 11–16).*
