---
phase: 34-wishlist-rework-per-character-per-slot-upgrades
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/wishlist/wishlist.ts
  - web/src/lib/wishlist/__tests__/wishlist.test.ts
  - web/src/routes/wishlist/+page.svelte
  - web/src/lib/components/WantlistPanel.svelte
  - web/src/lib/wantlist/groupByChar.ts
autonomous: true
requirements: [WISH-01, WISH-02, WISH-03, WISH-04, WISH-05, WISH-06, WISH-07]
must_haves:
  truths:
    - "The /wishlist tab shows a viewer-first character list EXCLUDING banks/bots (WISH-01), a scoped search that toggles to two result groups CHARACTERS + WISHLIST ITEMS where the WISHLIST ITEMS corpus is EVERY non-bank/bot character's wishlist (WISH-07 — lazily fetchWishlist(char) for each not-yet-loaded char on first search, cache, feed all to searchWishlistItems; NO scope-to-loaded-view escape hatch), and selecting a character pins a per-slot accordion of the 21 worn slots with the equipped item per slot (WISH-02)"
    - "Each slot's wishlist-target rows render name + price + Wiki link + last-listed + 'Raid' tag + ping Toggle + EC-hit badge + the reused ExaminePanel (WISH-03/04/05/06); auto-hidden targets never appear (D-02, server-computed)"
    - "Add (typed or from suggestion), remove (ConfirmDialog), and ping-toggle are server-truth: each awaits the POST then re-fetches fetchWishlist(char) — never optimistic; a non-owned character's wishlist renders read-only"
    - "Only WantlistPanel.svelte + web/src/lib/wantlist/groupByChar.ts (+ test) + the old /wishlist page body are deleted; web/src/lib/wantlist/priority.ts + holders.ts are KEPT (live consumers in columns.ts/guild-views/InGuildCell/WantAddForm); /wishlist no longer mounts WantlistPanel; the /wantlist→/wishlist 308 redirect (already shipped P30) is unchanged"
  artifacts:
    - path: "web/src/lib/wishlist/wishlist.ts"
      provides: "pure node-tested helpers: viewer-first no-banks/bots filter, cross-wishlist search grouping"
      contains: "export function"
    - path: "web/src/routes/wishlist/+page.svelte"
      provides: "the per-character per-slot master-detail wishlist tab"
      contains: "fetchWishlist"
    - path: "web/src/lib/api.ts"
      provides: "WishlistView/WishlistTarget interfaces + fetchWishlist/addWishlist/removeWishlist/setWishlistPing"
      contains: "fetchWishlist"
  key_links:
    - from: "web/src/routes/wishlist/+page.svelte"
      to: "/api/v1/wishlist/{char}"
      via: "fetchWishlist(char) on character selection + lazy per-char fetch for the WISH-07 corpus"
      pattern: "fetchWishlist"
    - from: "web/src/routes/wishlist/+page.svelte"
      to: "ExaminePanel.svelte"
      via: "the reused examine seam (a representative InventorySlot-shaped object, WISH-06)"
      pattern: "ExaminePanel"
    - from: "web/src/routes/wishlist/+page.svelte"
      to: "addWishlist / removeWishlist / setWishlistPing"
      via: "server-truth writes (await POST then re-fetch)"
      pattern: "await fetchWishlist"
---

<objective>
Build the `/wishlist` web tab: the viewer-first character list (banks/bots excluded), the WISH-07 two-section
scoped search (CHARACTERS + WISHLIST ITEMS, the items corpus = EVERY non-bank/bot character's wishlist), the
per-character per-slot accordion (equipped item + auto-hidden-filtered targets + the class+slot suggestions +
ping toggle + EC-hit badge + the reused ExaminePanel), and the server-truth add/remove/ping writes. Add the
`api.ts` interfaces + fetch/mutation wrappers + a pure node-tested `wishlist.ts` helper. Delete the
superseded `WantlistPanel.svelte` + `web/src/lib/wantlist/groupByChar.ts` (D-01/D-03) — KEEP `priority.ts` +
`holders.ts` (live consumers).

Purpose: This is the user-facing surface that closes WISH-01..07. It mirrors the approved P31/P32
master-detail shape verbatim (the 34-UI-SPEC mandates it) so Wishlist feels identical to Characters/Inventory.

Output: `api.ts` (append), `web/src/lib/wishlist/wishlist.ts` + test, `web/src/routes/wishlist/+page.svelte`
(rewrite), deleted `WantlistPanel.svelte` + `web/src/lib/wantlist/groupByChar.ts`. NO backend/watcher change.
NO deploy (that's 34-04). The rendered DOM is browser-smoked in 34-04 (node vitest is DOM-blind).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-wishlist-rework-per-character-per-slot-upgrades/34-CONTEXT.md
@.planning/phases/34-wishlist-rework-per-character-per-slot-upgrades/34-RESEARCH.md
@.planning/phases/34-wishlist-rework-per-character-per-slot-upgrades/34-PATTERNS.md
@.planning/phases/34-wishlist-rework-per-character-per-slot-upgrades/34-UI-SPEC.md
@.planning/phases/34-wishlist-rework-per-character-per-slot-upgrades/34-02-SUMMARY.md

<interfaces>
<!-- The exact web contracts. Extracted from the codebase + the 34-01/34-02 backend shapes. -->

The backend JSON contract (mirror the Go WishlistView field-for-field, snake_case — from 34-01 types.go):
  WishlistView { char: string; slots: WishlistSlot[] }
  WishlistSlot { slot: string; equipped: string; targets: WishlistTarget[]; suggestions: WishlistSuggestion[] }
  WishlistTarget { id: number; item_id: number | null; item_name: string; pinged: boolean;
                   pinged_hit: boolean; price: number | null; last_listed: string; wiki_url: string }
  WishlistSuggestion { item_name: string; is_raid: boolean; price: number | null; last_listed: string; wiki_url: string }

The routes (from 34-02):
  GET  /api/v1/wishlist/{char}      → WishlistView
  POST /api/v1/wishlist             → { character_id, slot, item_id, item_name } body (NO owner — session-derived)
  POST /api/v1/wishlist/remove      → { id } → { removed: boolean }
  POST /api/v1/wishlist/ping        → { id, pinged } → { pinged: boolean }
  GET  /api/v1/characters           → RosterCharacter[] (REUSED — filter banks/bots client-side, WISH-01)

The api.ts analogs (web/src/lib/api.ts):
  - getJSON<T>(path, f) / postJSON<T>(path, body, f) — the private credentialed wrappers (used by fetchOwnWants
    line 776, addWant 816, removeWant 836).
  - RosterCharacter interface (the /characters row: name/level/race/class/is_mine/is_bank_toon/is_guild_bot/last_seen).
  - fetchCharacters() (the /characters wrapper); fetchMyCharacters() (the assigned-char list for the add char id).
  - searchCatalog(q) (line 811 — REUSE UNCHANGED for the typed-entry add over /api/v1/items/search).
  - InventorySlot interface (the ExaminePanel prop shape — item/icon_id/statsblock/wiki_summary/is_quest_item/
    price/prices/wiki_url/location/category/canonical_slot/id/count/slots/children).
  - Unauthenticated / Forbidden typed errors (the AuthGate guard inputs).

The web master-detail analogs (mirror these 1:1):
  - web/src/routes/inventory/+page.svelte — the $state status machine, onMount one-shot load, ?i= URL seam via
    history.replaceState, the AuthGate guard (401/403 → authGuard), the asSlot ExaminePanel reuse seam
    (lines 98-120: a representative InventorySlot with category:'general', charLastSeen="").
  - web/src/routes/characters/+page.svelte — the bespoke selectable <button aria-pressed> row list + the
    winStatus per-selection fetch + stale-response guard + the ?c= deep-link.
  - web/src/lib/roster.ts — bandOf/viewerFirst/filterRoster (lines 26-51). For WISH-01, EXCLUDE banks/bots.

The reused components (mount as-is — from web/src/lib/components/):
  ExaminePanel.svelte (props slot?: InventorySlot | null, charLastSeen=''), StateBlock.svelte,
  cells/LastSyncedCell.svelte, ConfirmDialog.svelte, Toggle.svelte. The add-form DEBOUNCE idiom to CLONE
  (not import): WantAddForm.svelte script (DEBOUNCE_MS=250 line 35, searchSeq out-of-order guard line 41,
  pickedItem/customLabel staging lines 45-46, onAdded re-fetch callback). NOTE: WantAddForm.svelte itself
  imports noteRuneCount from $lib/wantlist/priority — that file is KEPT (Task 3), so cloning the idiom is fine.

The 21 worn slots (UI-SPEC §D, the InventoryWindow taxonomy):
  LEFT:  Head, Face, Ear1, Ear2, Neck, Shoulders, Arms, Back
  RIGHT: Wrist1, Wrist2, Hands, Finger1, Finger2, Chest, Legs, Feet, Waist
  WORN:  Primary, Secondary, Range, Ammo
  (Note: the SERVER already returns view.slots in this order — the page renders them as-given; no client
   re-ordering needed. The client only filters the character LIST.)

The files to DELETE after rewire (Pitfall 7 — DELETE ONLY THESE): web/src/lib/components/WantlistPanel.svelte +
web/src/lib/wantlist/groupByChar.ts (+ its .test.ts). KEEP web/src/lib/wantlist/priority.ts (priorityRank +
noteRuneCount) and holders.ts (type Holder + holdersFor) — they have LIVE consumers: columns.ts:29-30 imports
BOTH (priorityRank + type Holder), and columns.ts feeds the live guild-views/+page.svelte + InGuildCell.svelte;
WantAddForm.svelte:27 imports noteRuneCount. Deleting the whole dir breaks `npm run check && npm run build`.
The /wishlist placeholder currently mounts WantlistPanel (web/src/routes/wishlist/+page.svelte lines 28, 38-46)
— that mount goes away. The /wantlist→/wishlist 308 (web/src/routes/wantlist/+page.ts) is ALREADY shipped
(P30) — verify-only, NO edit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: api.ts interfaces + fetch/mutation wrappers; pure wishlist.ts helper + node test</name>
  <files>web/src/lib/api.ts, web/src/lib/wishlist/wishlist.ts, web/src/lib/wishlist/__tests__/wishlist.test.ts</files>
  <read_first>
    - web/src/lib/api.ts:740-838 (the wantlist interfaces + fetchOwnWants/addWant/removeWant/searchCatalog wrappers)
    - web/src/lib/roster.ts (bandOf/viewerFirst/filterRoster — the viewer-first sort to adapt, MINUS banks/bots)
    - web/src/lib/items.ts + its __tests__ (the grouping-helper + node-test precedent)
    - 34-RESEARCH.md WISH-01/WISH-07 rows + Pattern 4 (auto-removal is SERVER-side, not here)
    - 34-PATTERNS.md §web/src/lib/wishlist/*.ts + §api.ts (the wrappers to mirror)
    - 34-UI-SPEC.md §C (the two-section WISH-07 search contract)
  </read_first>
  <behavior>
    - Test 1 (WISH-01 list): wishlistRoster(chars) returns viewer-first (mine → guild, A-Z within band) with
      EVERY is_bank_toon || is_guild_bot row EXCLUDED (even a viewer-owned bank toon — the cleanest WISH-01
      predicate per UI-SPEC §B); filterWishlistRoster(chars, query) preserves that order among name matches,
      empty query → full set, no match → [].
    - Test 2 (WISH-07 item search): searchWishlistItems(loadedWishlists, query) groups matched targets across
      ALL passed-in (non-bank/bot) characters' wishlists by item name, each result naming which character(s) +
      slot(s) wishlist it; a match on a DIFFERENT character than any "selected" one is found (the corpus is the
      whole passed-in set, not one char); empty query → []; no match → []. (Pure over a passed-in shape — no fetch.)
  </behavior>
  <action>
    Append to `web/src/lib/api.ts` (mirror the wantlist wrappers at 776-838):
    - The interfaces (snake_case, field-for-field with the Go WishlistView from 34-01):
      `WishlistView`, `WishlistSlot`, `WishlistTarget`, `WishlistSuggestion` (see <interfaces> for the exact
      field list + types; price `number | null`, the nullable discipline).
    - `export function fetchWishlist(char: string, f = fetch): Promise<WishlistView> {
        return getJSON<WishlistView>('/api/v1/wishlist/' + encodeURIComponent(char), f); }` (char encoded —
      guildie-controlled, T-32-08 precedent). Note this is an OBJECT generic (like fetchBanks), not the array generic.
    - `export function addWishlist(body: { character_id: number; slot: string; item_id: number | null;
        item_name: string }, f = fetch): Promise<WishlistTarget> { return postJSON('/api/v1/wishlist', body, f); }`
      (body carries NO owner — session-derived, D-02).
    - `export function removeWishlist(id: number, f = fetch): Promise<{ removed: boolean }> {
        return postJSON('/api/v1/wishlist/remove', { id }, f); }`.
    - `export function setWishlistPing(id: number, pinged: boolean, f = fetch): Promise<{ pinged: boolean }> {
        return postJSON('/api/v1/wishlist/ping', { id, pinged }, f); }`.
    REUSE the existing `searchCatalog` (line 811) UNCHANGED for the typed-entry add. Do NOT redeclare
    RosterCharacter/InventorySlot — import/reuse them.

    Create `web/src/lib/wishlist/wishlist.ts` (pure, DOM-free — the roster.ts/items.ts precedent; type-only
    imports; the header comment explains the node-vitest constraint):
    - `export function wishlistBandOf(c: RosterCharacter): 'mine' | 'guild'` — WISH-01 excludes banks/bots,
      so this is only the two non-bank bands (callers PRE-filter banks/bots out).
    - `export function wishlistRoster(rows: RosterCharacter[]): RosterCharacter[]` — filter OUT every
      is_bank_toon || is_guild_bot row (WISH-01, even if is_mine), then sort mine → guild, A-Z within band.
      Returns a NEW array.
    - `export function filterWishlistRoster(rows: RosterCharacter[], query: string): RosterCharacter[]` — the
      filterRoster analog over wishlistRoster (case-insensitive name match preserving order; ''→full; no match→[]).
    - `export function searchWishlistItems(wishlists: { char: string; slots: { slot: string; targets:
        { item_name: string }[] }[] }[], query: string): { item_name: string; where: { char: string; slot:
        string }[] }[]` — the WISH-07 cross-wishlist grouping: over ALL passed-in (non-bank/bot) characters'
      wishlists, collect every target whose item_name matches the query, group by item_name (case-insensitive),
      and list the (char, slot) holdings. The caller passes the FULL corpus (every non-bank/bot char's wishlist
      — see Task 2's lazy-fetch); this helper does NOT scope or fetch. Empty/no-match → []. Pure + immutable.

    Create `web/src/lib/wishlist/__tests__/wishlist.test.ts` covering Behaviors 1-2 (mirror roster.test.ts /
    items.test.ts node cases — banks/bots-excluded ordering + the cross-wishlist grouping over a multi-char
    corpus where the matching item is on a NON-selected character).
  </action>
  <verify>
    <automated>cd web && npm run check && npm test -- wishlist</automated>
  </verify>
  <done>
    api.ts carries the 4 WishlistView interfaces + fetchWishlist/addWishlist/removeWishlist/setWishlistPing;
    wishlist.ts exports the banks/bots-excluded viewer-first filter + the WISH-07 multi-char search; `npm run
    check` 0 errors/0 warnings; `npm test` passes incl. the new wishlist cases (incl. the cross-char match).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite /wishlist/+page.svelte — the per-character per-slot master-detail wishlist tab</name>
  <files>web/src/routes/wishlist/+page.svelte</files>
  <read_first>
    - web/src/routes/wishlist/+page.svelte (the CURRENT P30 placeholder — what to replace; KEEP the Notifications region, NAV-04)
    - web/src/routes/inventory/+page.svelte (the master-detail shape: $state machine, onMount, ?i= seam,
      AuthGate guard, the asSlot ExaminePanel reuse seam lines 98-120)
    - web/src/routes/characters/+page.svelte (the bespoke selectable row list + winStatus per-selection fetch + stale guard)
    - web/src/lib/components/WantAddForm.svelte:1-95 (the debounce/seq-guard idiom to CLONE for the typed-entry add)
    - web/src/lib/components/WantlistPanel.svelte (the server-truth onAdded/doRemove/onMute re-fetch discipline + the ConfirmDialog remove)
    - 34-UI-SPEC.md §A-§J (the full layout/states/copy contract — the executor builds to this) + the Browser-smoke checklist
    - 34-PATTERNS.md §web/src/routes/wishlist/+page.svelte (the reuse list + the worn-slot constants + the server-truth discipline)
  </read_first>
  <action>
    Rewrite `web/src/routes/wishlist/+page.svelte` to the UI-SPEC contract. KEEP the P30 Notifications region
    (NotificationPrefsPanel + NotificationInbox — NAV-04; do NOT import/mutate the badge store, P30 §4 gotcha).
    REPLACE the WantlistPanel mount with the per-character per-slot master-detail. Use ONLY EQ theme tokens;
    render all names via plain {} (Svelte auto-escape) — NO new {@html} sink (the only raw-HTML is inside the
    reused ExaminePanel). Concrete structure:

    - Imports: onMount + getContext; Search (lucide); StateBlock, ExaminePanel, LastSyncedCell, ConfirmDialog,
      Toggle (from $lib/components/...); AUTH_GUARD_KEY/AuthGuard; from $lib/api: Unauthenticated, Forbidden,
      fetchCharacters, fetchWishlist, addWishlist, removeWishlist, setWishlistPing, searchCatalog, type
      RosterCharacter, type WishlistView, type WishlistTarget, type WishlistSuggestion, type InventorySlot;
      from $lib/wishlist/wishlist: wishlistRoster, filterWishlistRoster, searchWishlistItems.
    - State ($state): `pageStatus: 'loading'|'error'|'ready'`, `chars: RosterCharacter[]`, `query: string`,
      `selected: string | null` (the pinned char name), `winStatus: 'idle'|'loading'|'error'|'ready'`,
      `view: WishlistView | null` (the selected char's wishlist), `examineSlot: InventorySlot | null` (the
      pinned examine target), and a `wishlistCache: Record<string, WishlistView>` (the WISH-07 corpus cache —
      keyed by char name; populated lazily, see below).
    - onMount: read `?c=<char>` (pre-select); one-shot `chars = await fetchCharacters()` → 401/403 → authGuard,
      else pageStatus='error'. The character list shown = `filterWishlistRoster(chars, query)` (WISH-01 +
      banks/bots excluded; search preserves order).
    - LEFT column = the search bar (top, the /inventory .search visual verbatim; placeholder "Search
      characters and wishlists…", hint "Search characters and wishlists.") + the list:
        * Empty query → the viewer-first character list (bespoke `<button type=button aria-pressed>` rows,
          /characters .row treatment: name + accent "you" tag on is_mine + dimmed `Level {n} {race} {class}`
          meta dropping unknown tokens, never "null"; selected = 3px accent left-border + accent name).
          Two bands (YOUR CHARACTERS / GUILD) OR a single viewer-first run (UI-SPEC §B allows either; pick
          the two-band labels for /characters parity).
        * Non-empty query → two labelled result groups (UI-SPEC §C):
          - CHARACTERS group = matched non-bank/bot chars (filterWishlistRoster(chars, query)), each a row
            calling select(char).
          - WISHLIST ITEMS group = `searchWishlistItems(allWishlists, query)` where `allWishlists` is the FULL
            corpus of EVERY non-bank/bot character's wishlist (WISH-07 — NOT scoped to the loaded/selected
            view). Build the corpus by LAZY fetch + cache: on the FIRST non-empty query (or via an
            `ensureCorpus()` helper), for every char in `wishlistRoster(chars)` whose name is NOT already a
            key in `wishlistCache`, `await fetchWishlist(char)` and store it in `wishlistCache` (a 401/403 →
            authGuard; a per-char fetch error → skip that char + record it, do NOT abort the whole corpus).
            Then feed `Object.values(wishlistCache)` to `searchWishlistItems`. Show a small "searching all
            wishlists…" affordance while the corpus is still loading (the WISHLIST ITEMS group resolves once
            the fetches settle). Each WISHLIST ITEMS result names which character(s) + slot(s) wishlist the
            item; clicking a result pins THAT character's wishlist (select(result-char)). There is NO
            scope-to-loaded-view fallback — the corpus is always all non-bank/bot characters' wishlists.
            (Selecting a char also populates `wishlistCache[char]` from the `view` it already fetched, so
            re-searching does not re-fetch the already-loaded char.)
        * No matches → StateBlock kind="no-results" → `No matches for "{query}"` (query via {} — escaped).
    - select(char): set selected, reflect `?c=<char>` via history.replaceState + encodeURIComponent; set
      winStatus='loading'; `view = await fetchWishlist(char)` with a stale-response guard (ignore a response
      for a char that's no longer `selected` — the /characters winStatus pattern); on success ALSO store
      `wishlistCache[char] = view` (feeds the WISH-07 corpus without a re-fetch); 401/403 → authGuard, else
      winStatus='error'. Clear examineSlot on a new selection.
    - RIGHT column (the detail) when a char is selected:
        * "Pick a character" prompt (StateBlock-style centered) until selected (UI-SPEC §J).
        * winStatus loading/error → in-detail StateBlock (+ Retry refetches fetchWishlist(selected)).
        * The per-character header: char NAME (Heading 20px accent) + dimmed `Level {n} {race} {class}` meta
          (find the row in `chars` for the meta).
        * If view.slots all have zero targets → "No targets yet" block, with the accordion STILL rendered below
          (D-01 first-run; UI-SPEC §I).
        * A single pinned `<ExaminePanel slot={examineSlot} charLastSeen="" />` at the top/side of the detail
          (the reuse seam; build a representative InventorySlot per target/equipped via the asSlot idiom —
          item=name, price/prices/wiki_url copied, category:'general', the list-irrelevant fields zero/empty,
          charLastSeen=""). Hovering/tapping a target item name or the equipped-item line sets examineSlot.
        * The per-slot accordion: iterate `view.slots` (server-ordered) — each section:
            - slot header = the slot name as an accent Label eyebrow; OPTIONALLY a collapsible <button
              aria-expanded min-height:44px> (recommended: collapsible, collapsed by default EXCEPT slots with
              ≥1 target — UI-SPEC §D).
            - equipped-item line = "EQUIPPED" dimmed eyebrow + `{slot.equipped || 'Empty'}` (dimmed when empty);
              examine-able.
            - target rows (slot.targets — already auto-removal-filtered server-side, D-02): each a target row
              (UI-SPEC §E): name (examine on hover/tap) + `{price}pp` (--status-other tabular-nums, OMITTED
              when null) + "Wiki ↗" (accent, stopPropagation) when wiki_url + LastSyncedCell for last_listed
              (when present) + the ping `<Toggle>` (server-truth: setWishlistPing(id, !pinged) → await →
              re-fetch fetchWishlist(selected)) + the "Seen in EC" badge when pinged_hit + a "Remove"
              `--destructive` button → ConfirmDialog → removeWishlist(id) → await → re-fetch. On a NON-owned
              character (the row's char is not is_mine), render targets READ-ONLY (no toggle/remove/add).
            - the add control (OWNED chars only, UI-SPEC §D/§F/§G): the cloned WantAddForm debounced
              searchCatalog typed-entry (pick a catalog item → addWishlist({character_id, slot, item_id,
              item_name}) → await → re-fetch; or the custom escape hatch item_id:null → addWishlist) +
              the suggestion picker: slot.suggestions under two tier eyebrows (PRE-RAID / GROUP, RAIDING — split
              by is_raid OR render flat with the per-row "Raid" tag, UI-SPEC §F), each suggestion a row with
              name + price (or "Not for sale" when is_raid/no price) + Wiki link + last-listed + a "Raid" tag
              when is_raid + an "Add" button → addWishlist({character_id, slot, item_id:null, item_name}) →
              await → re-fetch. The character_id for the add = the selected char's id (from the chars row —
              ensure fetchCharacters carries id, OR look it up via fetchMyCharacters; if RosterCharacter lacks
              an id, use fetchMyCharacters to map the selected char name → its character_id for the add body).
        * After any add/remove, update `wishlistCache[selected]` to the re-fetched `view` (keeps the WISH-07
          corpus consistent for the next search).
    - Server-truth discipline (load-bearing, T-19-16): EVERY mutation awaits the POST then re-fetches the
      authoritative wishlist (`view = await fetchWishlist(selected)`) — NEVER optimistic-mutate. Polite
      aria-live announces on add/remove/ping (the v2.2 idiom).
    - All 44px touch targets, focus-visible 2px accent outline, theme-token-only colors, names via {} (no
      {@html}). Reduced-motion honored (global rule).

    Keep the Notifications region BELOW the two-pane (or per the planner's §A discretion) — the per-character
    wishlist is the PRIMARY content; the notifications inbox/badge stay reachable on this tab (NAV-04).
  </action>
  <verify>
    <automated>cd web && npm run check && npm run build</automated>
  </verify>
  <done>
    /wishlist/+page.svelte renders the viewer-first char list (banks/bots excluded) + scoped search whose
    WISHLIST ITEMS group covers EVERY non-bank/bot character's wishlist (lazy fetch + cache, no scope-to-loaded
    escape hatch) + the per-slot accordion (equipped + targets + suggestions + ping toggle + EC badge + reused
    ExaminePanel) + server-truth add/remove/ping; KEEPS the Notifications region; no new {@html} sink; `npm run
    check` 0/0, `npm run build` ok. (DOM render verified in 34-04 browser-smoke — node vitest is DOM-blind.)
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete the superseded WantlistPanel + groupByChar (KEEP priority.ts + holders.ts)</name>
  <files>web/src/lib/components/WantlistPanel.svelte, web/src/lib/wantlist/groupByChar.ts</files>
  <read_first>
    - web/src/lib/wantlist/ (the directory — confirm the file set incl. .test.ts before deleting)
    - web/src/lib/columns.ts:29-30 (CONFIRM it imports priorityRank from $lib/wantlist/priority + type Holder
      from $lib/wantlist/holders — these are LIVE; do NOT delete those two files)
    - web/src/lib/components/cells/InGuildCell.svelte:18 (CONFIRM it imports type Holder — live consumer)
    - web/src/lib/components/WantAddForm.svelte:27 (CONFIRM it imports noteRuneCount from $lib/wantlist/priority)
    - web/src/routes/wantlist/+page.ts (CONFIRM it is still the 308 redirect — verify-only, NO edit; D-03 already shipped P30)
    - 34-RESEARCH.md Pitfall 7 + Runtime State Inventory (the dead-web-component cleanup — DELETE-ONLY scope)
    - 34-PATTERNS.md §WantlistPanel.svelte + wantlist/*.ts (DELETE WantlistPanel + groupByChar only)
  </read_first>
  <action>
    AFTER Task 2 (the /wishlist body no longer mounts WantlistPanel), delete ONLY the dead wantlist web surface
    whose sole consumer was WantlistPanel (Pitfall 7 — a dangling fetchOwnWants/groupByChar import would break
    the build):
    - Delete `web/src/lib/components/WantlistPanel.svelte` (no longer mounted after Task 2).
    - Delete `web/src/lib/wantlist/groupByChar.ts` and its `__tests__/*.test.ts` (its ONLY importer was
      WantlistPanel.svelte:27 — `groupByChar`/`ACCOUNT_LEVEL`/`CharSelection`). Use `git rm` so the deletion is
      tracked.
    - **DO NOT DELETE** `web/src/lib/wantlist/priority.ts` or `web/src/lib/wantlist/holders.ts`. Both have LIVE
      consumers and deleting them breaks `npm run check && npm run build`:
        * `priority.ts` (`priorityRank`) is imported by `web/src/lib/columns.ts:29` (which feeds the live
          consolidated `guild-views/+page.svelte`); `priority.ts` (`noteRuneCount`) is imported by
          `web/src/lib/components/WantAddForm.svelte:27`.
        * `holders.ts` (`type Holder`) is imported by `web/src/lib/columns.ts:30` and
          `web/src/lib/components/cells/InGuildCell.svelte:18`.
      Leave both files (and their tests) UNTOUCHED.
    - Grep the web tree for any remaining import of `WantlistPanel`, `groupByChar`, `ACCOUNT_LEVEL` (from
      groupByChar), or `fetchOwnWants` mounted via WantlistPanel — if any non-test file still imports
      WantlistPanel or groupByChar, STOP and resolve it (the only expected importer was the /wishlist body,
      removed in Task 2). Confirm `$lib/wantlist/priority` and `$lib/wantlist/holders` imports STILL resolve
      (the files are kept).
    - VERIFY-ONLY (no edit): `web/src/routes/wantlist/+page.ts` still does `redirect(308, '/wishlist')` — the
      D-03 redirect shipped in P30; confirm it's intact.
    - Leave the api.ts wantlist wrappers (fetchOwnWants/addWant/removeWant/fetchGuildWants/searchCatalog) — note
      searchCatalog is REUSED by the new wishlist add (Task 2) and fetchGuildWants/the GuildWantRow/WantlistRow
      interfaces are consumed by columns.ts/guild-views. Removing the now-unused ones is OPTIONAL cleanup; if
      you remove any, grep that nothing imports it first and KEEP searchCatalog + anything columns.ts uses.
      Record what you removed in the SUMMARY.
  </action>
  <verify>
    <automated>cd web && npm run check && npm test && npm run build</automated>
  </verify>
  <done>
    WantlistPanel.svelte + web/src/lib/wantlist/groupByChar.ts (+ test) are deleted (git rm);
    web/src/lib/wantlist/priority.ts + holders.ts are KEPT (live consumers still resolve); no remaining import
    references WantlistPanel/groupByChar (grep clean); the /wantlist→/wishlist 308 is intact (unedited);
    `npm run check` 0/0, `npm test` green, `npm run build` ok.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser DOM → write API | Add/remove/ping POST from the tab; the owner is server-derived (the page sends no owner); a non-owned character's wishlist renders read-only (UI guard, NOT the security boundary — the server re-authorizes). |
| guildie-controlled strings → DOM | Character/item/slot names render via plain {} (Svelte auto-escape); the only {@html} is the reused ExaminePanel's escaped composeItemNote. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-13 | Tampering (XSS) | the /wishlist tab | mitigate | Every character/item/slot name + the search query renders via plain {} (Svelte auto-escapes); this phase adds NO new {@html} sink — the ONLY raw-HTML is the reused ExaminePanel's composeItemNote (escaped + scheme-allow-listed). Any char name in a ?c= deep-link is encodeURIComponent'd. Covered-by-design (the P31/P32/P33 single-sink rule). |
| T-34-14 | Elevation (the UI is not the gate) | read-only non-owned wishlist | accept | The read-only render for a non-owned character is UX, NOT access control — the server re-authorizes every write (IsCharAssignedToTx → 403, 34-02 T-34-07). A forged client write is rejected server-side regardless of the UI; the read API serves the same wishlists to every session (the WISH-07 browse leg — every non-bank/bot char's wishlist is readable by design). The NEGATIVE property (the filter/read-only is presentation-only). |
| T-34-15 | Tampering | optimistic mutation drift | mitigate | Every add/remove/ping AWAITs the POST then re-fetches the authoritative wishlist (the v2.2 server-truth discipline, T-19-16) — never optimistic-mutate, so the UI can't drift from the persisted state; the WISH-07 corpus cache is refreshed from the re-fetched view. |
| T-34-16 | Denial of Service | dangling dead-import build break | mitigate | WantlistPanel + groupByChar.ts are deleted AFTER the /wishlist body stops mounting them, with a grep sweep for residual imports (Pitfall 7); priority.ts + holders.ts are KEPT so their live consumers (columns.ts/guild-views/InGuildCell/WantAddForm) still resolve — no dangling import in either direction. |
</threat_model>

<verification>
- `cd web && npm run check` — 0 errors / 0 warnings (the whole web tree, incl. the deleted-file sweep + the kept-file resolves).
- `cd web && npm test` — green, incl. the new wishlist.ts node cases (banks/bots-excluded viewer-first + WISH-07 cross-char grouping).
- `cd web && npm run build` — adapter-static build ok (no dangling import from the deletions; priority.ts/holders.ts still resolve).
- Grep: no remaining `WantlistPanel` / `groupByChar` import in web/src; `$lib/wantlist/priority` + `$lib/wantlist/holders` still imported by columns.ts/InGuildCell/WantAddForm; the /wantlist 308 redirect intact.
- DEFERRED to 34-04 (node vitest is DOM-blind): the list/search/accordion/target-row/ping/suggestion/
  remove/examine/auto-hide DOM render — browser-smoked on the deployed build across the 5 EQ themes (incl.
  smoke #10: search finds an item on a DIFFERENT character's wishlist than the selected one).
</verification>

<success_criteria>
- /wishlist tab: viewer-first char list (banks/bots excluded), WISH-07 search whose items corpus = every
  non-bank/bot character's wishlist (lazy-fetch + cache, no scope-down), per-slot accordion (equipped +
  targets + suggestions + ping toggle + EC badge + reused ExaminePanel), server-truth add/remove/ping.
- api.ts: WishlistView interfaces + fetchWishlist/addWishlist/removeWishlist/setWishlistPing; searchCatalog reused.
- wishlist.ts: banks/bots-excluded viewer-first filter + WISH-07 cross-wishlist search, node-tested.
- WantlistPanel.svelte + web/src/lib/wantlist/groupByChar.ts deleted; priority.ts + holders.ts KEPT; /wantlist 308 intact; web gates green.
</success_criteria>

<output>
After completion, create `.planning/phases/34-wishlist-rework-per-character-per-slot-upgrades/34-03-SUMMARY.md`
</output>
