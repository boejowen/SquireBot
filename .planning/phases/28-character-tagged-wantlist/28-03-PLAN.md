---
phase: 28-character-tagged-wantlist
plan: 03
type: execute
wave: 3
depends_on: [28-02]
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/wantlist/groupByChar.ts
  - web/src/lib/wantlist/groupByChar.test.ts
  - web/src/lib/components/WantAddForm.svelte
  - web/src/lib/components/WantlistPanel.svelte
autonomous: true
requirements: [CWANT-01, CWANT-03, CWANT-04, CWANT-06]
must_haves:
  truths:
    - "The add-want form offers an OPTIONAL character <select> sourced from fetchMyCharacters(); default '(no character)' = account-level"
    - "The /wantlist page has a My/Guild toggle; Guild shows all-members wants with owner + character attribution"
    - "A member can group/filter THEIR OWN wantlist by character via a <select>"
    - "groupByChar is a pure DOM-free helper with a co-located node test"
  artifacts:
    - path: "web/src/lib/wantlist/groupByChar.ts"
      provides: "pure character group/filter helper"
      contains: "export function groupByChar"
    - path: "web/src/lib/wantlist/groupByChar.test.ts"
      provides: "node test for the helper"
      contains: "groupByChar"
    - path: "web/src/lib/api.ts"
      provides: "WantlistRow +char fields, addWant +character_id, NEW fetchGuildWants + GuildWantRow"
      contains: "fetchGuildWants"
  key_links:
    - from: "WantAddForm.svelte"
      to: "fetchMyCharacters()"
      via: "character <select> options"
      pattern: "fetchMyCharacters"
    - from: "WantlistPanel.svelte"
      to: "fetchGuildWants / groupByChar"
      via: "My/Guild toggle + group-by-char select"
      pattern: "fetchGuildWants|groupByChar"
---

<objective>
Surface the character-tagged wantlist in the web UI: the optional tag `<select>` on add, the My/Guild toggle showing the guildwide roll-up with attribution, and a per-user group/filter-by-character control — all client-side over the API shapes Plan 02 ships.

Purpose: CWANT-01 (tag on add), CWANT-03/04 (guildwide display with owner + character attribution), CWANT-06 (group/filter own list by character). Mirrors the P27 client-filter precedent (DOM-free helper + node test + a `<select>`).

Output: extended `api.ts` (WantlistRow + addWant body + new GuildWantRow/fetchGuildWants), the new pure `groupByChar.ts` + node test, the character `<select>` in WantAddForm, and the My/Guild toggle + group-by-char control in WantlistPanel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-character-tagged-wantlist/28-RESEARCH.md
@.planning/phases/28-character-tagged-wantlist/28-PATTERNS.md
@.planning/phases/28-character-tagged-wantlist/28-02-PLAN.md

<interfaces>
<!-- Backend contracts from Plan 02 that this plan consumes (login-gated routes). -->

API routes (Plan 02):
- POST /api/v1/wantlist  body now accepts optional `character_id: number | null`
- GET  /api/v1/wantlist          → WantlistRow[] (now each row has character_id + character_name)
- GET  /api/v1/wantlist/guild    → GuildWantRow[] (owner + character_name, NO note)

Existing my-characters source (P26 — REUSE, do not invent a new one):
```ts
// web/src/lib/api.ts:832 / :906
export interface MyCharacter { /* ...; name: string; character_id: number; ... */ }
export function fetchMyCharacters(f?: typeof fetch): Promise<MyCharacter[]>;
```

Existing wantlist client shapes to extend (web/src/lib/api.ts):
```ts
export interface WantlistRow { /* item_id: number|null; item_name; reason; priority; note: string|null; ... */ }
export function fetchOwnWants(f?: typeof fetch): Promise<WantlistRow[]>;  // GET /api/v1/wantlist
export function addWant(body, f?): Promise<WantlistRow>;                   // POST /api/v1/wantlist
```

The P27 pure-helper precedent to clone (web/src/lib/myview.ts):
```ts
export function applyMyFilter<T extends { char: string }>(
  rows: T[], mineNames: Set<string>, mineOnly: boolean, selectedChar: string | null): T[]
```
</interfaces>

<browser_smoke_gap>
node vitest is DOM-BLIND (no @testing-library/svelte — toolchain-install rule; the P15/P26/P27 trap). The pure `groupByChar.ts` is node-tested, but the `<select>` rendering/onchange in WantAddForm + WantlistPanel and the guildwide display CANNOT be proven by `npm test`. After execution, browser-smoke on a DEPLOYED build (or a full local stack — `npm run dev` bounces login against prod). Smoke checklist:
1. Add-want form shows a character <select> populated from my assigned characters + a "(no character)" default.
2. Adding a want with a character tag succeeds; adding with "(no character)" succeeds (account-level).
3. Tagging a character NOT mine is impossible from the UI (select only lists mine) AND the server rejects a forged body with 403 (negative test).
4. The /wantlist My/Guild toggle switches between own list and the all-members guildwide list.
5. The guildwide list shows owner (username) + character name per want; account-level wants show no character.
6. The guildwide list NEVER shows a note column.
7. Group/filter own wantlist by character via the <select> narrows to the chosen character (and an "all" option restores).
8. Character + owner names render escaped (no HTML injection) — never {@html}.
</browser_smoke_gap>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: api.ts — char fields on WantlistRow + addWant body + GuildWantRow/fetchGuildWants</name>
  <files>web/src/lib/api.ts</files>
  <read_first>
    - web/src/lib/api.ts around lines 580-616 (WantlistRow interface with item_id/note nullable fields; addWant body + postJSON; fetchOwnWants:606 + getJSON to clone for fetchGuildWants) AND lines 832-906 (MyCharacter interface + fetchMyCharacters — the tag-select source to REUSE)
    - internal/backendsrv/store/wantlist.go (Plan 01: the Go GuildWantRow fields to mirror in TS — item_id, item_name, reason, priority, discord_user_id, owner, character_id, character_name, NO note)
  </read_first>
  <behavior>
    - Type-level only (no node test needed beyond `npm run check`): WantlistRow gains character_id + character_name; GuildWantRow exists with owner + character_name and NO note; addWant body accepts optional character_id; fetchGuildWants targets /api/v1/wantlist/guild
  </behavior>
  <action>
In `web/src/lib/api.ts`:

1. Add to `WantlistRow`: `character_id: number | null;` and `character_name: string | null;` (mirror the existing `item_id: number | null` / `note: string | null`).

2. Extend the `addWant` body type to include `character_id?: number | null;` (optional). Pass it straight through to `postJSON('/api/v1/wantlist', body, f)`.

3. Add a NEW `GuildWantRow` interface mirroring the Go `GuildWantRow` from Plan 01: `id: number; item_id: number | null; item_name: string; reason: 'buy' | 'quest'; priority: 'low' | 'med' | 'high'; discord_user_id: string; owner: string; character_id: number | null; character_name: string | null;` — NO `note` field.

4. Add `export function fetchGuildWants(f: typeof fetch = fetch): Promise<GuildWantRow[]> { return getJSON<GuildWantRow[]>('/api/v1/wantlist/guild', f); }` — cloning `fetchOwnWants`.

Do NOT add a new my-characters fetch — `fetchMyCharacters` (:906) already exists; reuse it.
  </action>
  <verify>
    <automated>cd web && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fetchGuildWants" web/src/lib/api.ts` matches; it calls `getJSON<GuildWantRow[]>('/api/v1/wantlist/guild'` 
    - The `GuildWantRow` interface has an `owner` field and a `character_name` field and NO `note` field (grep the interface block for `note` → no match)
    - `WantlistRow` contains `character_id` and `character_name`
    - The `addWant` body type contains `character_id`
    - `cd web && npm run check` reports 0 errors / 0 warnings
  </acceptance_criteria>
  <done>The client types + fetchers match the Plan-02 API shapes; addWant carries the optional tag; fetchGuildWants reads the guildwide route; no note on GuildWantRow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: groupByChar.ts pure helper + node test</name>
  <files>web/src/lib/wantlist/groupByChar.ts, web/src/lib/wantlist/groupByChar.test.ts</files>
  <read_first>
    - web/src/lib/myview.ts (the full file: the DOM-free pure-helper doctrine header — WHY a plain .ts not .svelte, the LOAD-BEARING "presentation NOT a security boundary" note, `import type` only; the applyMyFilter filter signature :36-48 to mirror)
    - web/src/lib/__tests__/myview.test.ts (the node-test shape — 9 cases — to clone) OR web/src/lib/wantlist/holders.test.ts / priority.test.ts (the co-located wantlist test precedent)
    - web/src/lib/api.ts (WantlistRow shape — the helper operates over WantlistRow[] keyed on character_id / character_name)
  </read_first>
  <behavior>
    - Test: groupByChar(rows, null) returns ALL rows (no filter / "all")
    - Test: groupByChar(rows, charId) returns only rows whose character_id === charId
    - Test: groupByChar with a charId that matches no rows returns []
    - Test: a special sentinel for account-level (character_id === null) — e.g. groupByChar(rows, ACCOUNT_LEVEL) returns only untagged rows (decide the sentinel; document it)
    - Test: the helper does NOT mutate its input array
  </behavior>
  <action>
Create `web/src/lib/wantlist/groupByChar.ts` as a pure, DOM-free helper cloning the `myview.ts` doctrine header (presentation only, NOT a security boundary; `import type { WantlistRow } from '$lib/api'`).

Signature (mirror `applyMyFilter`'s filter-by-selection shape):
```ts
// selected: null ⇒ all; a number ⇒ that character_id; a sentinel ⇒ account-level (untagged)
export function groupByChar(rows: WantlistRow[], selected: number | null): WantlistRow[] {
    if (selected === null) return rows;
    return rows.filter((r) => r.character_id === selected);
}
```
If a distinct "account-level only" filter is desired beyond `null`, add an explicit exported sentinel/overload and document it in the header. Keep it pure — no `$state`, no DOM, no side effects.

Co-locate `groupByChar.test.ts` covering the five `<behavior>` cases (the holders.test.ts / priority.test.ts precedent in the same dir).
  </action>
  <verify>
    <automated>cd web && npm test -- groupByChar</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/wantlist/groupByChar.ts` exists with `export function groupByChar`
    - The file imports `WantlistRow` via `import type` (grep: `import type`) and contains NO `{@html}`, NO `$state`, NO DOM API
    - `web/src/lib/wantlist/groupByChar.test.ts` exists and exercises the all / by-char / no-match / non-mutation cases
    - `cd web && npm test -- groupByChar` passes
    - `cd web && npm run check` 0/0
  </acceptance_criteria>
  <done>groupByChar is a pure node-testable filter over WantlistRow[]; its node test is green; it is DOM-free and not a security boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WantAddForm character select + WantlistPanel My/Guild toggle & group-by-char</name>
  <files>web/src/lib/components/WantAddForm.svelte, web/src/lib/components/WantlistPanel.svelte</files>
  <read_first>
    - web/src/lib/components/WantAddForm.svelte (self: the Reason/Priority `<select>` block :207-222 to clone for the character tag; submit:111-126 threading body into addWant :118-124; the $state/$derived/$props runes :22-53; the XSS header lines 8-13 — plain {} only, never {@html})
    - web/src/lib/components/WantlistPanel.svelte (self: the onMount load → server-truth-reload lifecycle :92-117 with Promise.all([fetchOwnWants(), fetchView()]); the $derived memo feeding the grid :69-86; wantlistColumns import :24; the NEVER-optimistic-mutate / always-re-fetch discipline :106-117)
    - web/src/lib/myview.ts + the P27 +page.svelte My/All toggle precedent (the $state-driven <select> filter control pattern to mirror for My/Guild + group-by-char)
    - web/src/lib/api.ts (Task 1: fetchGuildWants, GuildWantRow, fetchMyCharacters, addWant +character_id) and web/src/lib/wantlist/groupByChar.ts (Task 2)
  </read_first>
  <behavior>
    - (Browser-smoke — node vitest is DOM-blind; see <browser_smoke_gap>) WantAddForm shows a character <select> from fetchMyCharacters() with a "(no character)" default → character_id null
    - WantAddForm submit threads the picked character_id (or null) into addWant
    - WantlistPanel My/Guild toggle switches row source (fetchOwnWants vs fetchGuildWants) + columns (guild adds owner + character)
    - WantlistPanel group-by-char <select> (My view) filters via groupByChar; "all" restores
    - All character/owner names render via plain {} (no {@html})
  </behavior>
  <action>
In `WantAddForm.svelte`:
- Load the caller's characters in `onMount`/`$effect` via `fetchMyCharacters()` into a `$state<MyCharacter[]>`.
- Add an OPTIONAL character `<select>` cloning the Reason/Priority markup (FormField + `.field` select). First option `(no character)` with `value=""` → `character_id: null`; the rest map each MyCharacter (value = character_id, label = name).
- In `submit`, add `character_id: pickedCharId` (number or null) to the `addWant({...})` body.
- Render character names via plain `{}` (never `{@html}`).

In `WantlistPanel.svelte`:
- Add a My/Guild `$state` toggle (a `<select>` or segmented control, mirroring the P27 My/All control). My → `fetchOwnWants()` rows; Guild → `fetchGuildWants()` rows (lazy/parallel load per the existing Promise.all lifecycle; ALWAYS re-fetch, never optimistic-mutate).
- For the My view, add a group-by-char `<select>` populated from the distinct character_id/name on the own-wants rows (or from fetchMyCharacters) with an "All characters" default; feed the grid through `groupByChar(ownWants, selected)` via a `$derived` memo.
- For the Guild view, feed the grid the GuildWantRow[] and show owner + character attribution columns (extend the `wantlistColumns` shape with an owner column + a character column for the guild grid; account-level wants render blank character).
- All owner/character names render via plain `{}`.

Keep the consolidated-views lock intact: this is one DataGrid with a client-side toggle/filter, NOT a new tab or a per-character view.
  </action>
  <verify>
    <automated>cd web && npm run check && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fetchMyCharacters" web/src/lib/components/WantAddForm.svelte` matches; the form has a character `<select>` and threads `character_id` into the addWant body (grep `character_id`)
    - `grep -n "fetchGuildWants" web/src/lib/components/WantlistPanel.svelte` matches; `grep -n "groupByChar" web/src/lib/components/WantlistPanel.svelte` matches
    - Neither component contains `{@html}` (grep both files → no match)
    - The guild grid surfaces an owner column and a character column (grep for the owner/character_name field usage)
    - `cd web && npm run check` 0/0; `npm test` passes (incl. the groupByChar test); `npm run build` succeeds
    - BROWSER-SMOKE GAP flagged: the <select> rendering + toggle + guildwide display require a deployed-build smoke per <browser_smoke_gap> (node vitest is DOM-blind) — NOT satisfiable by npm test alone
  </acceptance_criteria>
  <done>The add-form offers the optional character tag from the caller's own characters, the /wantlist page toggles My/Guild (guild shows owner + character attribution, no note), and the member can group/filter their own list by character — all client-side, names escaped, one DataGrid. Browser-smoke gap flagged for post-exec verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | The character `<select>` lists ONLY the caller's characters (UX); the SERVER (Plan 02 IsCharAssignedToTx) is the actual authorization — the select is not the gate |
| rendered names → DOM | character/owner names are external/in-game/wiki text; rendered via Svelte `{}` auto-escape only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-10 | Tampering (XSS) | character_name / owner render | mitigate | Plain `{}` auto-escape in both components; NEVER `{@html}`. Acceptance greps for absence of `{@html}`. |
| T-28-11 | Elevation (IDOR) | character `<select>` | accept (UX) / mitigate (server) | The select sourcing options from fetchMyCharacters is UX convenience, NOT the control. The real gate is `IsCharAssignedToTx` server-side (Plan 02). A forged body is rejected 403 — flagged in the browser-smoke negative test. |
| T-28-12 | Information Disclosure | guildwide grid | mitigate | The grid renders GuildWantRow which carries NO note (Plan 01/02); the client cannot surface a field the API does not send. |
</threat_model>

<verification>
- `cd web && npm run check` reports 0 errors / 0 warnings
- `cd web && npm test` passes (incl. the new groupByChar test)
- `cd web && npm run build` succeeds
- grep confirms no `{@html}` in either edited component
- BROWSER-SMOKE GAP: the interactive `<select>`/toggle/guildwide display must be smoked on a deployed build (or full local stack) per <browser_smoke_gap> before the phase is "verified" — node vitest is DOM-blind (the P15/P26/P27 trap)
</verification>

<success_criteria>
- The add-want form offers an optional character tag from the caller's own characters (CWANT-01 UI).
- The /wantlist My/Guild toggle shows the all-members guildwide wantlist with owner + character attribution and no note (CWANT-03/04 UI).
- A member can group/filter their own wantlist by character (CWANT-06).
- All web gates green; the browser-smoke gap is explicitly flagged for post-exec verification.
</success_criteria>

<output>
After completion, create `.planning/phases/28-character-tagged-wantlist/28-03-SUMMARY.md`.
</output>
