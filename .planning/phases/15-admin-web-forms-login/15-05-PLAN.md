---
phase: 15-admin-web-forms-login
plan: 05
type: execute
wave: 5
depends_on: ["15-03", "15-04"]
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/__tests__/adminApi.test.ts
  - web/src/lib/components/FormField.svelte
  - web/src/lib/components/BankCoinForm.svelte
  - web/src/lib/components/EvictionForm.svelte
  - web/src/lib/components/AdminMgmtForm.svelte
  - web/src/routes/admin/+page.svelte
  - web/src/routes/bank-coin/+page.svelte
  - web/src/routes/+page.svelte
autonomous: true
requirements: [ADMIN-04, ADMIN-05, ADMIN-06]
must_haves:
  truths:
    - "Any authenticated member can record bank coin for an is_bank_toon character; plat/gold/silver/copper persist and surface in the bank view"
    - "Eviction is reachable only via the officer-only Admin area; it previews the character cascade + 30-day grace + guild-code-revoke consequence and requires an explicit confirm"
    - "Officer management lists officers (owner-floor flagged), promotes a logged-in user by pick, and removes with confirm + owner-floor protection"
    - "A non-officer who navigates directly to /admin sees an Officers-only refusal, not the forms"
    - "A 403 from any officer endpoint collapses the admin UI to the Officers-only refusal (the 'you're no longer an officer' path), not a generic form-error; destructive actions use the themed ConfirmDialog"
  artifacts:
    - path: "web/src/lib/components/BankCoinForm.svelte"
      provides: "bank-coin entry (login-only, range-validated, pre-fill)"
      contains: "Save coin"
    - path: "web/src/lib/components/EvictionForm.svelte"
      provides: "officer eviction with preview + ConfirmDialog; 403 → Officers-only refusal"
      contains: "Evict guildie"
    - path: "web/src/lib/components/AdminMgmtForm.svelte"
      provides: "officer list + promote-by-pick + remove with confirm; 403 → Officers-only refusal"
    - path: "web/src/routes/admin/+page.svelte"
      provides: "officer-only Admin area (eviction + manage officers); /admin direct-nav refusal for non-officers"
  key_links:
    - from: "EvictionForm / AdminMgmtForm"
      to: "ConfirmDialog (from 15-04) before any destructive commit"
      via: "confirm-before-commit"
      pattern: "ConfirmDialog"
    - from: "EvictionForm / AdminMgmtForm"
      to: "the 15-04 AuthGate Officers-only refusal on a 403 (Forbidden)"
      via: "server-truth collapse, not a generic form-error"
      pattern: "Forbidden"
---

<objective>
Build the three authenticated web forms (UI-SPEC binding contract): BankCoinForm (ADMIN-05, any authenticated member), EvictionForm (ADMIN-04, officer-only), AdminMgmtForm (ADMIN-06, officer-only). Reuse the P14 design system + the 15-04 ConfirmDialog/Session/StateBlock; use the exact UI-SPEC copy + the v1 error codes the backend returns; honor the two-layer authorization IA (frontend gating is UX; the API is the gate — a 403 collapses the admin UI to the Officers-only refusal). The bank-coin value then surfaces in the existing bank view, replacing P14's null/0 placeholder.

Purpose: ADMIN-04/05/06 — the visible write surface. Composes the 15-03 backend endpoints + the 15-04 gate into the three forms.
Output: typed admin/coin API wrappers + FormField + the three form components + the /admin and /bank-coin routes + the bank-view coin surfacing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-admin-web-forms-login/15-CONTEXT.md
@.planning/phases/15-admin-web-forms-login/15-UI-SPEC.md
@.planning/phases/15-admin-web-forms-login/15-03-SUMMARY.md
@.planning/phases/15-admin-web-forms-login/15-04-SUMMARY.md

<interfaces>
<!-- The backend write endpoints (15-03) + the 15-04 frontend primitives to compose. -->

Backend write endpoints (15-03, on API_BASE = https://api.squirebot.quest; all need credentials:'include'):
- GET  /api/v1/coin/bank-toons   (login-only) → [{character_id, name, plat, gold, silver, copper}] (null coin where unset). [] if none.
- POST /api/v1/coin              (login-only) body {character_id, plat, gold, silver, copper} → {character, coin:{plat,gold,silver,copper}}. Errors: 400 {"error":"invalid_input"}, 400 {"error":"not_bank_toon"}, 401.
- GET  /api/v1/admin/officers    (officer-only) → {officers:[{discord_user_id,username,avatar,is_floor}], promotable:[{discord_user_id,username,avatar}]}. 403 {"error":"not_authorized"} for a member.
- POST /api/v1/admin/officers/add    (officer-only) body {discord_user_id} → {added:bool, username}. Errors: 403 not_authorized.
- POST /api/v1/admin/officers/remove (officer-only) body {discord_user_id} → {removed:bool, username}. Errors: 403 not_authorized | owner_floor_protected.
- GET  /api/v1/admin/evictable   (officer-only) → [{owner_id, label, char_count}].
- GET  /api/v1/admin/eviction/preview?owner_id=N (officer-only) → {owner_id, characters:[name], grace_until}.
- POST /api/v1/admin/evict       (officer-only) body {owner_id} → {removed_count, grace_until}. Errors: 403 not_authorized | owner_floor_protected.
- POST /api/v1/admin/eviction/restore (officer-only) body {owner_id} → {restored_count}. (Re-mints the guild code per D-10.)

From 15-04 (web/src/lib/):
- api.ts getJSON sends credentials:'include' + maps 401→Unauthenticated / 403→Forbidden typed errors (Forbidden carries the server {error} code). ADD a postJSON<T>(path, body, fetchFn) sibling that also sends credentials:'include' + JSON content-type + throws the SAME typed errors (so a 403 owner_floor_protected surfaces as Forbidden with code='owner_floor_protected').
- auth.ts Session ({authenticated,isMember,isOfficer,...}); the session + an `authGuard(err)` helper are in Svelte context (AuthGate setContext('session',...) + setContext('authGuard',...)). Calling authGuard(err) with a Forbidden/Unauthenticated routes to the matching refusal (the server-truth collapse).
- ConfirmDialog.svelte props {open, heading, body, confirmLabel, onConfirm, onCancel} — the accessible destructive modal (role=dialog, aria-modal, Cancel-focused, Esc/backdrop dismiss).
- StateBlock kinds: error|loading|empty|view-empty|no-coin|no-results|auth-loading|officers-only (15-04). EXTEND with the form lifecycle kinds (or render inline): form-saving|form-success|form-error + the picker-empty states (no-bank-toons|no-evictable-guildies|no-promotable-users) — copy from UI-SPEC verbatim.
- The bank view (+page.svelte) currently renders `<StateBlock kind="no-coin" />` for the bank tab (coin null in P14). THIS PLAN surfaces real coin from the bank-toons data once present.

P14 form/control styling (mirror): native `<select>` + `<input>` styled like ThemePicker's select (--panel bg, 1px --border, 4-6px radius, 16px/--font-body, 44px min-height, accent focus ring). Each form in a --panel card, lg padding, Heading-20px title, right-aligned action row, primary button accent fill / --bg text (like StateBlock .retry). Inline error text in --status-missing, aria-live="polite". SvelteKit routes are folders under web/src/routes/ with a +page.svelte (SPA, ssr=false already global).

UI-SPEC copy (verbatim — Copywriting Contract):
- Bank-coin: title context `Record bank coin`; field labels `Platinum`/`Gold`/`Silver`/`Copper`; char select label `Bank character` placeholder `Choose a bank character…`; invalid gold/silver/copper `Enter 0–999.`; invalid platinum `Enter a whole number (0 or more).`; saving `Saving…`; success `Coin saved for <character>.`; error `Couldn't save the coin. <reason> No changes were saved.`; empty heading `No bank characters yet` body `No character is marked as a bank toon yet. Once one is, you can record its coin here.`; primary `Save coin`.
- Eviction: guildie select label `Guildie` placeholder `Choose a guildie…`; preview heading `Characters affected (<n>):`; grace line `Grace expires: <date> (30 days from today).`; consequence callout `Evicting also revokes this guildie's guild code right away, so their watcher stops uploading. Their data auto-archives after 30 days. Reversible during the grace period — you can restore them and re-issue a code.`; empty cascade `No characters found for this guildie.`; owner-floor block `Owner-floor protected — a peer officer can't evict the maintainer's data.`; success `Marked <n> character(s) as removed and revoked the guild code. Grace until <date>.`; error `Eviction failed: <reason>. No changes were written.`; primary `Evict guildie`; dialog confirm `Evict <guildie>`.
- Admin mgmt: list heading `Current officers (<n>):`; owner annotation `(owner)` tooltip `This is the maintainer. The owner-floor lockout prevents anyone else from removing them.`; promote picker label `Promote a member` placeholder `Choose a member who's signed in…`; no-promotable heading `No one to promote yet` body `Only members who've signed in at least once can be promoted. Once another member signs in, they'll show up here.`; add result `Officer added: <username>.`/`Already an officer: <username>.`; remove result `Officer removed: <username>.`/`Not in the list: <username>.`; remove confirm dialog `Remove <username> as an officer? They'll no longer be able to evict guildies or manage officers. Reversible — you can re-add them here.`; error routing — owner-floor `Owner-floor protected — only the maintainer can remove themselves. No changes were written.` · not-authorized `You're no longer an officer. Please reload.` · lock-busy `Another officer action is in flight. Please retry. No changes were written.`; primary `Add officer`; remove `Remove`; dialog confirm `Remove <username>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin/coin API wrappers (postJSON, typed 401/403) + FormField</name>
  <read_first>
    - web/src/lib/api.ts (the getJSON + ApiError + Unauthenticated/Forbidden typed errors + credentials:'include' from 15-04 — add the postJSON sibling)
    - web/src/lib/__tests__/api.test.ts (the injectable fetchFn test seam to mirror)
    - .planning/phases/15-admin-web-forms-login/15-UI-SPEC.md § "Form Contracts" (the shared field rhythm for FormField)
    - .planning/phases/15-admin-web-forms-login/15-03-SUMMARY.md (the exact endpoint paths + error codes)
  </read_first>
  <behavior>
    - postJSON sends credentials:'include' + Content-Type: application/json + JSON.stringify(body); on a 401 it throws Unauthenticated, on a 403 it throws Forbidden (with .code = the parsed {"error":"<code>"} string, e.g. owner_floor_protected), on other non-2xx an ApiError; on 2xx it parses the JSON.
    - The typed wrappers (fetchBankToons, saveCoin, fetchOfficers, addOfficer, removeOfficer, fetchEvictable, previewEviction, evict, restore) call the right path + method.
  </behavior>
  <action>
Edit `web/src/lib/api.ts`:
- Ensure `ApiError`/`Forbidden` carry the server's `{"error":"<code>"}` value on a `code?` field (added in 15-04) so callers can branch on owner_floor_protected vs not_authorized vs invalid_input vs not_bank_toon.
- Add `async function postJSON<T>(path, body, fetchFn = fetch): Promise<T>` — `fetchFn(\`${API_BASE}${path}\`, { method:'POST', credentials:'include', headers:{'Content-Type':'application/json', Accept:'application/json'}, body: JSON.stringify(body) })`; mirror getJSON's status mapping: 401 → throw Unauthenticated, 403 → throw Forbidden(code), other non-2xx → ApiError, transport failure → ApiError(status 0); on a 2xx parse the JSON (wrap a malformed body as ApiError like getJSON does).
- Add typed wrappers (snake_case bodies/results matching 15-03):
  - `fetchBankToons(fetchFn?)` → GET /api/v1/coin/bank-toons.
  - `saveCoin(body:{character_id,plat,gold,silver,copper}, fetchFn?)` → POST /api/v1/coin.
  - `fetchOfficers(fetchFn?)` → GET /api/v1/admin/officers.
  - `addOfficer(discord_user_id, fetchFn?)` → POST /api/v1/admin/officers/add.
  - `removeOfficer(discord_user_id, fetchFn?)` → POST /api/v1/admin/officers/remove.
  - `fetchEvictable(fetchFn?)` → GET /api/v1/admin/evictable.
  - `previewEviction(owner_id, fetchFn?)` → GET /api/v1/admin/eviction/preview?owner_id=N.
  - `evict(owner_id, fetchFn?)` → POST /api/v1/admin/evict.
  - `restoreEviction(owner_id, fetchFn?)` → POST /api/v1/admin/eviction/restore.
  Add the matching response/row TS interfaces (BankToon, OfficersResponse, EvictableOwner, EvictionPreview, EvictResult, ...).

Create `web/src/lib/components/FormField.svelte` (shared): props `{ label: string; error?: string; children: Snippet; id?: string }` — renders `<label for={id}>` (Label 13px/600 display) above the control slot, with inline `{error}` text below (Body 16px, `--status-missing`, `aria-live="polite"`) when set. FormField standardizes the label+error rhythm.

Write `web/src/lib/__tests__/adminApi.test.ts` (inject stub fetch): saveCoin posts credentials:'include' + JSON body; a 400 {"error":"not_bank_toon"} → ApiError with code='not_bank_toon'; removeOfficer 403 {"error":"owner_floor_protected"} → Forbidden with code='owner_floor_protected'; a 403 not_authorized → Forbidden code='not_authorized'; a 200 → parsed result.
  </action>
  <verify>
    <automated>cd web && npm run test:unit -- --run src/lib/__tests__/adminApi.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'function postJSON' web/src/lib/api.ts && grep -q "credentials: 'include'" web/src/lib/api.ts`
    - `grep -q "Forbidden" web/src/lib/api.ts && grep -q "code" web/src/lib/api.ts` (postJSON throws the typed Forbidden carrying the server error code)
    - `web/src/lib/api.ts contains '/api/v1/admin/evict'` and `contains '/api/v1/coin'`
    - B-2 cross-ref: adminApi.test.ts proves a 403 owner_floor_protected AND a 403 not_authorized each surface as a Forbidden with the matching .code
    - `cd web && npm run check` passes
  </acceptance_criteria>
  <done>postJSON + the typed admin/coin wrappers carry credentials + throw the typed Unauthenticated/Forbidden (with the server error code); FormField standardizes the field rhythm.</done>
</task>

<task type="auto">
  <name>Task 2: BankCoinForm + the /bank-coin route + bank-view coin surfacing</name>
  <read_first>
    - web/src/lib/api.ts (Task 1 — fetchBankToons, saveCoin, the BankToon type)
    - web/src/lib/components/ThemePicker.svelte (the native select styling to mirror) + web/src/lib/components/StateBlock.svelte (form-saving/success/error + no-bank-toons + the no-coin kind it replaces)
    - web/src/routes/+page.svelte (the bank tab's current `<StateBlock kind="no-coin" />` — to be replaced by real coin once present; + the load/StateBlock pattern)
    - .planning/phases/15-admin-web-forms-login/15-UI-SPEC.md § "BankCoinForm" + Copywriting (verbatim labels/validation/success/error/empty)
    - .planning/phases/15-admin-web-forms-login/15-CONTEXT.md § D-11, D-12
  </read_first>
  <behavior>
    - The form lists is_bank_toon characters in a select; choosing one pre-fills the four coin inputs with current values.
    - Validation: platinum ≥ 0 integer; gold/silver/copper in 0..999; the Save button is disabled until valid AND at least one value differs from the loaded value; invalid fields show the exact inline error copy.
    - Save posts via saveCoin; in-flight shows `Saving…`; success shows `Coin saved for <character>.` and keeps the select; a not_bank_toon/invalid_input/other error shows the error copy.
    - No bank toons → the no-bank-toons empty state (not the form). No confirm dialog (non-destructive, D-12 member-accessible).
    - The bank view surfaces recorded coin (replacing the null/0 placeholder).
  </behavior>
  <action>
1) `web/src/lib/components/BankCoinForm.svelte`: onMount `fetchBankToons()`. If `[]` → `<StateBlock kind="no-bank-toons" />` (add the kind: heading `No bank characters yet`, body per copy). Else: a `<select>` (label `Bank character`, placeholder `Choose a bank character…`) of the toons; on change, pre-fill four `<input type="number" inputmode="numeric">` (labels `Platinum`/`Gold`/`Silver`/`Copper`, tabular-nums) from the chosen toon's current coin (null → empty). Validation (D-11): platinum `>=0` integer (else `Enter a whole number (0 or more).`); gold/silver/copper integer in [0,999] (else `Enter 0–999.`); `Save coin` disabled until all valid AND ≥1 changed. On submit: `saveCoin({character_id, plat, gold, silver, copper})`; in-flight button `Saving…` disabled; success → inline `Coin saved for <name>.` (keep the select); failure → `Couldn't save the coin. <reason> No changes were saved.` Use FormField for label+error rhythm. Use plain `{}` for all interpolation. NOT officer-gated (RequireSession on the backend; this route is reachable by any member).

2) `web/src/routes/bank-coin/+page.svelte`: render a page title `Record bank coin` + `<BankCoinForm />`. Reachable by any authenticated member (the AuthGate already gates login; no officer check). The view-nav (15-04 shell) links here; also add a "Record coin" affordance near the bank view (step 3).

3) Edit `web/src/routes/+page.svelte` bank tab: surface recorded coin. Since the read bank view's `coin` is still null in the public read API (15-03 didn't change the read bank endpoint's coin field), surface coin by ALSO calling `fetchBankToons()` when the bank tab is shown: render a small "Bank coin" summary (per bank character: plat/gold/silver/copper) above/below the bank grid, replacing `<StateBlock kind="no-coin" />` when at least one toon has recorded coin; keep the `no-coin` affordance only when no coin is recorded anywhere. Add a "Record coin" link/button (to /bank-coin) in the bank panel toolbar (UI-SPEC IA: a control on/near the bank view). Keep it minimal — the authoritative entry form is BankCoinForm; this is the surfacing + the entry affordance.
  </action>
  <verify>
    <automated>cd web && npm run check && npm run build && npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/components/BankCoinForm.svelte contains 'Save coin'`
    - `web/src/lib/components/BankCoinForm.svelte contains 'Enter 0–999.'` and `contains 'Choose a bank character'`
    - BankCoinForm enforces gold/silver/copper <=999 and plat>=0 (range check present in the validation)
    - D-12: BankCoinForm is NOT behind an officer guard (no isOfficer/status==='officer' check guarding the form or the /bank-coin route)
    - `web/src/routes/bank-coin/+page.svelte` exists and renders BankCoinForm
    - `grep -q 'fetchBankToons' web/src/routes/+page.svelte` (bank view surfaces recorded coin)
    - `! grep -q '@html' web/src/lib/components/BankCoinForm.svelte`
    - `cd web && npm run check` passes; `npm run build` emits the site; `npm run test:unit -- --run` green
  </acceptance_criteria>
  <done>Bank-coin entry is a member-accessible, range-validated, pre-filling form; recorded coin surfaces in the bank view; no confirm dialog.</done>
</task>

<task type="auto">
  <name>Task 3: EvictionForm + AdminMgmtForm + officer-only /admin (403 → Officers-only refusal)</name>
  <read_first>
    - apps-script/src/triggers/showEvictionSidebar.ts (the v1 preview→confirm→commit UX + the result message shapes — the behavioral oracle)
    - apps-script/src/triggers/showAdminMgmtSidebar.ts (the v1 officer-list + promote + remove UX + the (owner) annotation + showRemove suppression)
    - web/src/lib/components/ConfirmDialog.svelte (15-04 — the destructive modal these forms open) + StateBlock (officers-only refusal kind from 15-04) + ThemePicker (select styling)
    - web/src/lib/api.ts (Task 1 — fetchEvictable/previewEviction/evict/restore + fetchOfficers/addOfficer/removeOfficer + the Forbidden typed error + .code) + web/src/lib/components/AuthGate.svelte (15-04 — the authGuard context helper that collapses to the Officers-only refusal on a 403)
    - web/src/lib/auth.ts (Session from context — isOfficer)
    - .planning/phases/15-admin-web-forms-login/15-UI-SPEC.md § "EvictionForm" + "AdminMgmtForm" + "Information Architecture & Authorization" + "Accessibility & Interaction Notes" (server-truth) + Copywriting (verbatim)
    - .planning/phases/15-admin-web-forms-login/15-CONTEXT.md § D-06, D-07, D-08, D-09, D-10
  </read_first>
  <behavior>
    - /admin is reachable only when session.isOfficer; a non-officer navigating directly sees the Officers-only StateBlock refusal (not the forms).
    - SERVER-TRUTH (B-2 cross-ref, UI-SPEC): a 403 (Forbidden) from ANY officer call collapses the admin UI to the Officers-only refusal (the "You're no longer an officer." path) — NOT a generic form-error. This reuses the 15-04 authGuard / officers-only refusal mechanism; do not cache an officer bit past a 403. owner_floor_protected → the inline floor message; a 401 (Unauthenticated) propagates to AuthGate → LoginScreen.
    - EvictionForm: pick a guildie → preview (characters affected (N) + grace date + the guild-code-revoke consequence callout, with the restore/re-issue copy) → `Evict guildie` opens ConfirmDialog (confirm `Evict <guildie>`) → on confirm, evict; success/error use the exact copy.
    - AdminMgmtForm: list officers ((owner) flagged + Remove suppressed on the floor row for a peer) → promote-by-pick (`Add officer`) → Remove opens ConfirmDialog (confirm `Remove <username>`); idempotent results + the owner_floor_protected/lock_busy error routing use the exact copy; no-promotable → the empty state.
  </behavior>
  <action>
1) `web/src/lib/components/EvictionForm.svelte`: read `authGuard` from context. onMount `fetchEvictable()`. A `<select>` (label `Guildie`, placeholder `Choose a guildie…`) of evictable owners (label by owner.label + char_count). On select → `previewEviction(owner_id)` → render the preview block: heading `Characters affected (<n>):` + the character list (subtle `--status-missing` tint) + grace line `Grace expires: <date> (30 days from today).` + a consequence callout (`triangle-alert` glyph) with the exact consequence copy (includes "Reversible during the grace period — you can restore them and re-issue a code."). Empty cascade → `No characters found for this guildie.` + Evict disabled. The `Evict guildie` primary button opens `<ConfirmDialog open heading="Evict guildie" body={...count+revoke+grace reversibility...} confirmLabel={\`Evict ${label}\`} onConfirm={doEvict} onCancel={...} />`. doEvict → `evict(owner_id)`; success → `Marked <n> character(s) as removed and revoked the guild code. Grace until <date>.`; CRITICAL (B-2 cross-ref): wrap every admin call in `try/catch` — on `Forbidden` call `authGuard(err)` so the view collapses to the Officers-only refusal (the "you're no longer an officer" path), EXCEPT when `err.code==='owner_floor_protected'` → show the inline owner-floor block copy; other errors → `Eviction failed: <reason>. No changes were written.`; let `Unauthenticated` bubble (AuthGate → LoginScreen). All interpolation via `{}`.

2) `web/src/lib/components/AdminMgmtForm.svelte`: read `authGuard` from context. onMount `fetchOfficers()`. Render `Current officers (<n>):` + a list: each row = avatar+username; the `is_floor` row annotated `(owner)` with the tooltip copy; a `Remove` button per row EXCEPT the floor row when the caller is a peer (suppress when `is_floor && session.discordUserId !== floorId`; if unknown, suppress on is_floor for safety + rely on the server's owner_floor_protected 403). Promote: a `<select>` (label `Promote a member`, placeholder `Choose a member who's signed in…`) of `promotable`; `Add officer` → `addOfficer(id)` → success `Officer added: <username>.` / idempotent `Already an officer: <username>.` If `promotable` is empty → `<StateBlock kind="no-promotable-users" />`. Remove → `<ConfirmDialog>` (confirm `Remove <username>`, body the exact remove-confirm copy) → on confirm `removeOfficer(id)` → success `Officer removed: <username>.`. CRITICAL (B-2 cross-ref): on a caught `Forbidden` with code `not_authorized` → call `authGuard(err)` to collapse the whole admin UI to the Officers-only refusal (NOT a generic inline error); map code `owner_floor_protected` → `Owner-floor protected — only the maintainer can remove themselves. No changes were written.`; code `lock_busy` → `Another officer action is in flight. Please retry. No changes were written.` Re-fetch the list after a successful add/remove. All interpolation via `{}` (escaped).

3) `web/src/routes/admin/+page.svelte`: read the session from context. If `!session?.isOfficer` → `<StateBlock kind="officers-only" />` (the refusal; Layer-1 UX — the server is still the gate). Else render a two-section Admin area: "Evict guildie" → `<EvictionForm />` and "Manage officers" → `<AdminMgmtForm />`, each titled (Heading 20px). The forms themselves collapse to the Officers-only refusal via authGuard on any 403 (server-truth override, UI-SPEC).

Write `web/src/lib/__tests__/adminApi.test.ts` additions (or a focused component test): assert that a 403 from an admin call is the Forbidden type the forms route on, and that the forms' catch path calls authGuard / drives the officers-only refusal (not a generic error) — at minimum a unit test over the error-handling helper proving Forbidden(not_authorized) → the officers-only route and Forbidden(owner_floor_protected) → the inline floor message. (A full component test is a bonus.)

Run `npm run check` + `npm run build` + `npm run test:unit -- --run` (the existing 60 tests + the new auth/adminApi tests stay green).
  </action>
  <verify>
    <automated>cd web && npm run check && npm run build && npm run test:unit -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/lib/components/EvictionForm.svelte contains 'Evict guildie'` and `contains 'Characters affected'` and `contains 'revokes this guildie'` (the D-10 consequence callout)
    - `grep -q 'ConfirmDialog' web/src/lib/components/EvictionForm.svelte && grep -q 'ConfirmDialog' web/src/lib/components/AdminMgmtForm.svelte` (confirm-before-commit on both destructive actions)
    - B-2 cross-ref: `grep -q 'Forbidden' web/src/lib/components/EvictionForm.svelte && grep -q 'Forbidden' web/src/lib/components/AdminMgmtForm.svelte` (forms handle the 403 typed error) AND `grep -Eiq 'officers-only|officers only|no longer an officer' web/src/lib/components/AdminMgmtForm.svelte web/src/lib/components/EvictionForm.svelte` (403 collapses to the officers-only refusal, not a generic error)
    - B-2 cross-ref: `grep -Eq 'Forbidden|403|officers-only|not_authorized' web/src/lib/__tests__/adminApi.test.ts` (test covers the 403→officers-only path)
    - `web/src/lib/components/AdminMgmtForm.svelte contains 'Current officers'` and `contains '(owner)'` and `contains 'owner_floor_protected'`
    - `web/src/routes/admin/+page.svelte contains 'officers-only'` and `grep -q 'isOfficer' web/src/routes/admin/+page.svelte`
    - `! grep -rq '@html' web/src/lib/components/EvictionForm.svelte web/src/lib/components/AdminMgmtForm.svelte`
    - `cd web && npm run check` passes; `npm run build` emits the site; `npm run test:unit -- --run` green
  </acceptance_criteria>
  <done>Eviction + officer-management forms exist, port the v1 UX with confirm-before-commit + owner-floor protection + the exact error routing; /admin is officer-gated client-side, and a 403 from any admin call collapses the form to the Officers-only refusal (not a generic error) while a 401 routes to LoginScreen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated browser → write forms | destructive (eviction, officer-remove) + mutating (coin, officer-add) requests cross here |
| frontend gate vs API gate | the /admin client gate is UX-only; the server (15-03) is the authority (re-checks officer inside the tx) |
| DOM ← guildie labels / usernames / error reasons | user/Discord-controlled strings rendered in previews + results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-26 | Elevation of privilege | /admin reachable by a non-officer | mitigate (UX) + server-authoritative | Layer 1: /admin renders the officers-only refusal when !session.isOfficer (client suppression); Layer 2: every officer endpoint (15-03) re-checks server-side and 403s — the form treats a 403 as "no longer an officer" and collapses the UI to the Officers-only refusal (B-2 cross-ref, via authGuard); the hidden nav is never trusted |
| T-15-27 | Tampering | destructive action without confirm | mitigate | Eviction + officer-remove route through the accessible ConfirmDialog (confirm-before-commit, default focus Cancel); a misclick cannot fire the destructive call; the consequence + reversibility are stated in the dialog body |
| T-15-28 | XSS | guildie labels / usernames / error reasons | mitigate | All interpolation via Svelte `{}` (auto-escaped); NO `{@html}` in any form (grep-enforced); ports v1's escapeHtml discipline |
| T-15-29 | Tampering | client-side coin validation bypass | accept (defense-in-depth only) | The client range-validates (plat>=0, gold/silver/copper 0–999) for UX; the SERVER (15-03) is authoritative (re-validates → 400 invalid_input / not_bank_toon). The disabled Save button is UX, never the gate. |
| T-15-30 | Elevation of privilege | removing the owner-floor via the UI | mitigate | The Remove button is suppressed on the floor row for a peer (Layer 1); the server rejects owner_floor_protected (Layer 2) and the form surfaces the exact protection message — color is never the only signal (text + disabled/absent button) |
</threat_model>

<verification>
- `cd web && npm run check` passes (0 errors); `npm run build` emits index.html + 200.html; `npm run test:unit -- --run` all green.
- `grep -rq 'ConfirmDialog' web/src/lib/components/EvictionForm.svelte web/src/lib/components/AdminMgmtForm.svelte` (confirm-before-commit on both).
- B-2 cross-ref: a 403 from an admin endpoint collapses the form to the Officers-only refusal (Forbidden → authGuard), not a generic error.
- `! grep -rq '@html' web/src/lib/components/BankCoinForm.svelte web/src/lib/components/EvictionForm.svelte web/src/lib/components/AdminMgmtForm.svelte`.
</verification>

<success_criteria>
- Bank-coin: any authenticated member records plat/gold/silver/copper for an is_bank_toon char; it persists + surfaces in the bank view (ADMIN-05/D-11/D-12).
- Eviction: officer-only, preview + 30-day grace + guild-code-revoke consequence + confirm-before-commit + owner-floor protection (ADMIN-04/D-09/D-10).
- Officer mgmt: officer-only, list + promote-by-pick + remove-with-confirm + owner-floor protection + the exact error routing (ADMIN-06/D-06/D-07/D-08).
- A non-officer cannot reach the forms (refusal); a 403 collapses the admin UI to the Officers-only refusal (not a generic error); a 401 routes to LoginScreen; usernames are escaped; check/build/tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-web-forms-login/15-05-SUMMARY.md`
</output>
