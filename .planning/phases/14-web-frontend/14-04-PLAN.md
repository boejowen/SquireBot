---
phase: 14-web-frontend
plan: 04
type: execute
wave: 3
depends_on: [14-02, 14-03]
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/table/createSvelteTable.ts
  - web/src/lib/table/FlexRender.svelte
  - web/src/lib/components/DataGrid.svelte
  - web/src/lib/components/StatusCell.svelte
  - web/src/lib/components/ItemTooltip.svelte
  - web/src/lib/components/SearchBox.svelte
  - web/src/lib/components/SearchResults.svelte
  - web/src/lib/components/ThemePicker.svelte
  - web/src/lib/components/StateBlock.svelte
  - web/src/lib/columns.ts
  - web/src/routes/+layout.svelte
  - web/src/routes/+page.svelte
autonomous: true
requirements: [WEB-01, WEB-03, WEB-04, WEB-05, BACKEND-05]
must_haves:
  truths:
    - "A guildie opens the site and sees all four views (view, gear_check, spell_check, bank) each with a leading sticky Char column, filterable and sortable on every column"
    - "gear_check shows OK/MISSING/OTHER and spell_check shows KNOWN/MISSING as colored text badges from the read API"
    - "Cross-character search returns holders (char + location) and shows a clickable 'Did you mean?' suggestion on no exact match"
    - "Every inventory row exposes a hover/tap rich-HTML tooltip (wiki summary + price + quest) with a working wiki link, and the active EQ theme applies site-wide via [data-theme]"
  artifacts:
    - path: "web/src/lib/components/DataGrid.svelte"
      provides: "the one reusable filterable/sortable grid (sticky Char + header), instantiated 4x"
      contains: "position: sticky"
    - path: "web/src/lib/table/createSvelteTable.ts"
      provides: "local Svelte-5 adapter over @tanstack/table-core (NOT svelte-table)"
      contains: "table-core"
    - path: "web/src/lib/api.ts"
      provides: "fetch wrappers for /api/v1/views/* + /meta against PUBLIC_API_BASE"
      contains: "/api/v1/views/"
    - path: "web/src/routes/+layout.svelte"
      provides: "SiteShell — [data-theme] root, theme picker, view nav, footer attribution"
      contains: "data-theme"
  key_links:
    - from: "web/src/lib/components/DataGrid.svelte"
      to: "@tanstack/table-core"
      via: "local createSvelteTable adapter"
      pattern: "createSvelteTable"
    - from: "web/src/lib/components/ItemTooltip.svelte"
      to: "web/src/lib/tooltip/composeNotes.ts"
      via: "{@html} on the ESCAPED composeNotes output only"
      pattern: "composeItemNote"
    - from: "web/src/routes/+page.svelte"
      to: "web/src/lib/api.ts"
      via: "fetch the 4 view payloads + meta"
      pattern: "fetchView\\|api\\."
---

<objective>
Build the visible product: wire Plan 02's ported logic + theme system and Plan 03's read API into the actual SvelteKit UI. One reusable `DataGrid` (over `@tanstack/table-core` + a local Svelte-5 adapter — NOT `@tanstack/svelte-table`, which is Svelte-4-only) instantiated 4× with the exact v1 column orders; the cross-character search box with inline "did you mean?"; the hover/tap rich-HTML item tooltip; and the site shell carrying the `[data-theme]` attribute + theme picker + footer attribution.

Purpose: WEB-01 (the 4 consolidated grids, leading Char column, filter+sort), WEB-03 (search UI over Plan 02's in-memory engine), WEB-04 (tooltip popover + wiki link), WEB-05 (EQ theme applied site-wide) — and the consumption side of BACKEND-05 (the client fetches the read endpoints Plan 03 pinned).

Output: the wired `web/` app — `api.ts` + the TanStack adapter + 8 components + the column defs + `+layout.svelte`/`+page.svelte`, building green and rendering the four views against the read API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-web-frontend/14-CONTEXT.md
@.planning/phases/14-web-frontend/14-RESEARCH.md
@.planning/phases/14-web-frontend/14-PATTERNS.md
@.planning/phases/14-web-frontend/14-UI-SPEC.md

<interfaces>
<!-- From Plan 02 (ported logic + theme — already in web/src/lib/): -->
search/searchIndex.ts:  export function searchRows(query, rows): { groups: SearchResultGroup[]; suggestions: string[] }; didYouMean; levenshtein
tooltip/composeNotes.ts: export function composeItemNote(itemName, wikiUrl, summary, prices, questLinks): string   // ESCAPED HTML
theme/themes.ts:        THEME_KEYS, DEFAULT_THEME='velious', THEMES, resolveTheme, loadTheme, saveTheme

<!-- From Plan 03 (the PINNED JSON contract — read .planning/phases/14-03-SUMMARY.md for the EXACT field names): -->
GET /api/v1/views/view        -> ViewRow[]        (Char, Slot, Item, ID, Count, WikiURL, Price, LastSynced + tooltip fields: WikiSummary, IsQuestItem, Prices[], QuestLinks[])
GET /api/v1/views/gear_check  -> GearCheckRow[]   (Char, Class, Tier, Slot, Have, Recommended, Status)
GET /api/v1/views/spell_check -> SpellCheckRow[]  (Char, Class, Level, Spell, Status)
GET /api/v1/views/bank        -> { rows: ViewRow[]; coin: null }
GET /api/v1/meta              -> { characters: [{ name, last_seen }] }
<!-- Field JSON tags are snake_case per Plan 01/03 SUMMARY — use the SUMMARY's exact names. -->

<!-- CRITICAL grid package (RESEARCH Pitfall 1 — OVERRIDES the UI-SPEC literal which named @tanstack/svelte-table): -->
<!-- @tanstack/table-core 8.21.x (installed by Plan 02) + a LOCAL ~40-line createSvelteTable/FlexRender adapter (the shadcn-svelte idiom, RESEARCH Pattern 3). -->
<!-- Sticky Char col + sticky header = pure CSS. Faceted Status/Tier/Class filter = getFacetedRowModel + getFacetedUniqueValues. -->

<!-- RESEARCH Pattern 3 (the verified working Svelte 5 adapter shape — getCoreRowModel/getSortedRowModel/getFilteredRowModel, -->
<!-- $state-backed sorting/columnFilters/globalFilter, onXChange unwrapping the updater fn — Pitfall 2). -->

<!-- API base: PUBLIC_API_BASE env -> https://api.squirebot.quest (the API origin; distinct from the static origin app.squirebot.quest). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client + local TanStack adapter (createSvelteTable/FlexRender over table-core) + reusable DataGrid + StatusCell</name>
  <files>web/src/lib/api.ts, web/src/lib/table/createSvelteTable.ts, web/src/lib/table/FlexRender.svelte, web/src/lib/components/DataGrid.svelte, web/src/lib/components/StatusCell.svelte, web/src/lib/columns.ts</files>
  <read_first>
    - .planning/phases/14-03-SUMMARY.md (Plan 03 — the EXACT JSON field names + the bank `{rows,coin}` + meta `{characters}` shapes the client decodes)
    - .planning/phases/14-web-frontend/14-RESEARCH.md § "Pattern 3: Local Svelte-5 TanStack adapter" (the verified createSvelteTable shape: getCoreRowModel/getSortedRowModel/getFilteredRowModel, $state sorting/columnFilters/globalFilter, enableMultiSort, the FlexRender header/cell pattern) + Pitfall 1 (table-core NOT svelte-table) + Pitfall 2 (unwrap onXChange updater fns)
    - .planning/phases/14-web-frontend/14-UI-SPEC.md § "Grid Contract" (universal grid behavior + the exact per-view columns/secondary-sort/special-cells table) + § Color (status tokens, accent reserved list) + § Typography (tabular-nums for price/count)
    - .planning/phases/14-web-frontend/14-PATTERNS.md § "No Analog Found" (DataGrid/StatusCell/table adapter are greenfield — use RESEARCH Pattern 3, no in-repo analog)
  </read_first>
  <action>
**`api.ts`** — typed fetch wrappers using `PUBLIC_API_BASE` (SvelteKit `$env/dynamic/public` or `import.meta.env.PUBLIC_API_BASE`), default `https://api.squirebot.quest`. Functions: `fetchView()`, `fetchGearCheck()`, `fetchSpellCheck()`, `fetchBank()` (returns `{rows,coin}`), `fetchMeta()`. Each does `fetch(`${base}/api/v1/...`)`, throws a typed error on non-2xx (StateBlock renders the error state), returns the decoded JSON typed to the Plan-03 row shapes. Define the TS row interfaces here (or in a `types.ts`) matching the SUMMARY's snake_case field names.

**`table/createSvelteTable.ts`** — the local Svelte-5 adapter over `@tanstack/table-core` (RESEARCH Pattern 3, the shadcn-svelte idiom; ~40 lines). Wrap `createTable` from `@tanstack/table-core`, feeding it `$state`-backed `sorting`/`columnFilters`/`globalFilter` via getter properties, with each `onXChange` unwrapping the updater: `(u) => (state = typeof u === 'function' ? u(state) : u)` (Pitfall 2 — without this, clicking a header does nothing). Wire `getCoreRowModel`, `getSortedRowModel`, `getFilteredRowModel`, `getFacetedRowModel`, `getFacetedUniqueValues`, `enableMultiSort: true`. Do NOT import `@tanstack/svelte-table`.

**`table/FlexRender.svelte`** — renders a columnDef header/cell that may be a string or a component/function, given the TanStack `context` (RESEARCH Pattern 3). Used for both header cells and body cells.

**`columns.ts`** — the four `ColumnDef[]` arrays with the EXACT v1 column orders + secondary sorts (UI-SPEC Grid Contract table):
- `view`/`bank`: Char · Slot · Item · ID · Count · Wiki · Price · Last Synced. `Item` cell = accent link + tooltip trigger (renders ItemTooltip — Task 2); `Wiki` cell = external-link `<a href>` (lucide external-link icon, rel="noopener", target=_blank); `Price` = `<n>pp` with `font-variant-numeric: tabular-nums`; `Last Synced` = freshness dot/tint (<7d ok / <30d other-amber / ≥30d missing). Default sort Char asc, secondary item asc then location.
- `gear_check`: Char · Class · Tier · Slot · Have · Recommended · Status. `Status` cell = StatusCell (OK/MISSING/OTHER); `Recommended` = tooltip trigger. Secondary sort tier-rank (Pre-Raid→Raiding→Iksar) then slot then recommended — implement a custom sort fn for the Tier column mapping the three tier strings to 1/2/3.
- `spell_check`: Char · Class · Level · Spell · Status. `Status` = StatusCell (KNOWN/MISSING). Secondary sort level asc then spell asc.
Mark Status/Tier/Class columns as faceted-select filter columns; all others text-contains.

**`DataGrid.svelte`** — the ONE reusable grid (instantiated 4× — never per-character tabs, CLAUDE.md LOCKED). `let { data, columns } = $props()`. Build the table via `createSvelteTable`. Render: a toolbar (global filter input + per-column filter inputs; faceted `<select>` for Status/Tier/Class populated from `getFacetedUniqueValues`); then a scrollable table region under a sticky header. **Sticky Char column** = `position: sticky; left: 0` on the first `th`/`td`; **sticky header** = `position: sticky; top: 0` on the header row (pure CSS — TanStack is headless). Sort on header click (asc→desc→none) with an accent caret (lucide). Zebra striping via `:nth-child` at ~4-5% accent alpha; row hover same tint. No pagination (scrolls within a fixed-height region). Cells use 8px×16px padding (UI-SPEC Spacing). All colors via the `--*` CSS vars (theme-driven). Horizontal scroll allowed on narrow viewports with Char kept sticky (UI-SPEC Responsive).

**`StatusCell.svelte`** — renders the status word (OK/MISSING/OTHER/KNOWN) at Label type (13px/600) in the matching `--status-*` color with a ~8% tinted background pill. The literal word is always present (color is never the only signal — a11y). `let { status } = $props()`.
  </action>
  <verify>
    <automated>cd web && npm run check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0 and `npm run check` (svelte-check) passes.
    - `grep -rn "@tanstack/svelte-table" web/src/` returns 0 (the Svelte-4 trap is NOT used — Pitfall 1).
    - `grep -n "@tanstack/table-core" web/src/lib/table/createSvelteTable.ts` matches.
    - `grep -n "typeof u === 'function'" web/src/lib/table/createSvelteTable.ts` matches (Pitfall 2 updater unwrap).
    - `grep -n "position: sticky" web/src/lib/components/DataGrid.svelte` matches at least twice (sticky Char col + sticky header).
    - `grep -n "/api/v1/views/" web/src/lib/api.ts` matches all four view paths AND `grep -n "/api/v1/meta" web/src/lib/api.ts` matches.
    - `grep -n "tabular-nums" web/src/lib/components/DataGrid.svelte` matches (price/count alignment).
    - `web/src/lib/columns.ts` defines exactly 4 column-def arrays whose first column id is the Char column in each.
    - `grep -n "PUBLIC_API_BASE" web/src/lib/api.ts` matches.
  </acceptance_criteria>
  <done>The API client, the local table-core adapter (not svelte-table), the reusable DataGrid (sticky Char + header, faceted filters, multi-sort, no pagination), StatusCell, and the 4 exact-order column defs exist, type-check, and build.</done>
</task>

<task type="auto">
  <name>Task 2: ItemTooltip (hover/tap popover) + SearchBox/SearchResults (with inline "did you mean?") + StateBlock</name>
  <files>web/src/lib/components/ItemTooltip.svelte, web/src/lib/components/SearchBox.svelte, web/src/lib/components/SearchResults.svelte, web/src/lib/components/StateBlock.svelte</files>
  <read_first>
    - web/src/lib/tooltip/composeNotes.ts (Plan 02 — `composeItemNote` returns ESCAPED HTML; the ONLY value the tooltip `{@html}`s)
    - web/src/lib/search/searchIndex.ts (Plan 02 — `searchRows(query, rows)` returns `{groups, suggestions}`; groups carry holders; COLLAPSE_THRESHOLD collapses >5)
    - .planning/phases/14-web-frontend/14-UI-SPEC.md § "Item Tooltip Contract" (hover on pointer + tap/click on touch; dismiss outside-tap/Esc; content order; the Safety clause) + § "Search Contract" (results = item name + per-holder `↳ <Char>: <Location>, count <n>`; >5 collapse; "Did you mean <suggestion>?" accent link only on no exact match; clicking re-runs) + § "Copywriting Contract" (the exact empty/error/loading/no-results copy)
    - .planning/phases/14-web-frontend/14-RESEARCH.md § "Security Domain" (no `{@html}` on un-escaped content — only composeNotes output, which Plan 02 escaped; the search query echoed in "No matches for <query>" relies on Svelte's default `{}` escaping, NOT `{@html}`)
  </read_first>
  <action>
**`ItemTooltip.svelte`** — a popover anchored to its trigger (the Item/Recommended cell or a search result name). `let { itemName, wikiUrl, summary, prices, questLinks } = $props()`. Open on **hover (pointer devices)** via `onmouseenter`/`onmouseleave` AND on **tap/click (touch)** — use a pointer-type check or just support both (pointerenter + click toggling). Dismiss on **outside-tap and Esc** (a window listener while open). The popover body is `{@html composeItemNote(itemName, wikiUrl, summary, prices, questLinks)}` — this is the ONLY `{@html}` allowed, and only because Plan 02's composeNotes fully escapes every interpolated string (T-14.02-01). Surface = `--panel`, 16px padding, max-width ~360px, `--border`, subtle shadow, fast fade honoring `prefers-reduced-motion`. Keyboard: openable via focus on the trigger, dismissable with Esc. 44px min touch target on the trigger.

**`SearchBox.svelte`** — `let { rows } = $props()` (the `view` payload). A search input (placeholder `Search items across the guild…`, lucide search icon, accent focus ring). On input, call `searchRows(query, rows)` → bind `{groups, suggestions}`. Renders `<SearchResults>` with the result. Case-insensitive; data is tiny so it's synchronous + <2s trivially (WEB-03).

**`SearchResults.svelte`** — `let { groups, suggestions, query, onSuggest } = $props()`. Per group: the item name (accent link + ItemTooltip trigger) + id de-emphasized, then one line per holder `↳ <Char>: <Location>, count <n>` (surfaces which char/location holds it — the core value). Groups with `collapsed` (>5 holders) render collapsed with an expand affordance. When `groups.length === 0`: render the no-results StateBlock — heading `No matches for "<query>"` (the query interpolated via plain `{query}` — Svelte escapes it by default; do NOT `{@html}` it); if `suggestions.length > 0` show the body `Did you mean <suggestion>?` where `<suggestion>` (suggestions[0]) is an accent link calling `onSuggest(suggestions[0])` (re-runs search — D-09); else body `Try a shorter or different spelling.`

**`StateBlock.svelte`** — `let { kind, ... } = $props()` rendering the shared empty/error/loading copy from the UI-SPEC Copywriting Contract: empty (no data) `No characters yet` + body; per-view-empty `Nothing to show here` + the view-name-substituted body; error `Couldn't load the data` + body + a **Retry** button (`onRetry` prop re-fires the fetch); loading a brief skeleton/`Loading…`. Use the exact UI-SPEC strings.
  </action>
  <verify>
    <automated>cd web && npm run check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0; `npm run check` passes.
    - `grep -n "composeItemNote" web/src/lib/components/ItemTooltip.svelte` matches AND it is the only `{@html}` target — `grep -c "@html" web/src/lib/components/` shows `{@html}` ONLY in ItemTooltip.svelte (the search query is rendered via plain `{query}`, never `{@html}`).
    - `grep -n "searchRows" web/src/lib/components/SearchBox.svelte` matches.
    - `grep -n "Did you mean" web/src/lib/components/SearchResults.svelte` matches AND `grep -n "onSuggest\|re-run" web/src/lib/components/SearchResults.svelte` shows clicking re-runs the search (D-09).
    - `grep -n "Search items across the guild" web/src/lib/components/SearchBox.svelte` matches (UI-SPEC copy).
    - `grep -n "Couldn't load the data\|No characters yet\|Coin: not yet recorded\|Nothing to show here" web/src/lib/components/StateBlock.svelte` matches the UI-SPEC copy.
    - `grep -n "keydown\|Escape\|Esc" web/src/lib/components/ItemTooltip.svelte` shows Esc dismissal.
    - `grep -rn "{query}" web/src/lib/components/SearchResults.svelte` shows the query is interpolated (escaped by default), NOT `{@html query}`.
  </acceptance_criteria>
  <done>The tooltip (hover/tap, Esc/outside dismiss, escaped-HTML body), the search box + results (holders surfaced, >5 collapse, inline clickable "did you mean?"), and the shared StateBlock all exist with the exact UI-SPEC copy, and `{@html}` is confined to the escaped composeNotes output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SiteShell (+layout) with theme picker + nav + footer, and +page wiring the 4 views + search to the API</name>
  <files>web/src/routes/+layout.svelte, web/src/routes/+page.svelte, web/src/lib/components/SiteShell.svelte, web/src/lib/components/ThemePicker.svelte</files>
  <read_first>
    - web/src/lib/theme/themes.ts (Plan 02 — THEME_KEYS, DEFAULT_THEME='velious', loadTheme/saveTheme/resolveTheme; the [data-theme] CSS blocks are already in app.css)
    - web/src/lib/api.ts + web/src/lib/components/DataGrid.svelte + SearchBox.svelte + StateBlock.svelte (Tasks 1-2 — what +page wires together)
    - web/src/lib/columns.ts (Task 1 — the 4 column-def arrays)
    - .planning/phases/14-web-frontend/14-UI-SPEC.md § Design System (component inventory — SiteShell/ThemePicker) + § Theme Catalog (single `[data-theme]` write + localStorage; velious default; prefers-reduced-motion) + § Typography (wordmark Display 28px) + § Copywriting (footer attribution string) + § Grid Contract (status legend near gear/spell grids)
    - .planning/phases/14-web-frontend/14-CONTEXT.md § deferred (theme picker = simple dropdown/swatch; fancy preview tiles deferred)
  </read_first>
  <behavior>
    - On load with no stored theme, the shell root carries `data-theme="velious"` (DEFAULT_THEME); with a stored valid key, it carries that key.
    - Choosing a theme writes `localStorage.theme` and flips the single `[data-theme]` attribute — no rebuild, no per-component re-theming.
    - The four views are reachable via nav/tabs; each renders a DataGrid over its API payload + its column def.
    - bank renders the `Coin: not yet recorded` affordance (coin is null in P14), never a fabricated `0 pp`.
    - A failed fetch renders the error StateBlock with a working Retry; while fetching, the loading state.
  </behavior>
  <action>
**`+layout.svelte`** (or a `SiteShell.svelte` it renders) — the shell with the `[data-theme]` attribute on the root element. On mount: `let theme = $state(loadTheme())` (resolves to velious if unstored); set `data-theme={theme}` on the root `<div>`. Header: wordmark (Display 28px, `--font-display`), the `ThemePicker`, and the view nav/tabs (active tab in accent). `<main>` renders the page. Footer: the required attribution `Item & class data from the Project 1999 Wiki (CC-BY-SA) and PigParse.` (UI-SPEC Copywriting). Honor `prefers-reduced-motion` for theme transitions.

**`ThemePicker.svelte`** — a simple `<select>` (or swatch row) over `THEME_KEYS` (fancy live-preview tiles are deferred — CONTEXT). On change: `saveTheme(key)` + update the shell's `theme` state (which flips `[data-theme]`). The selected theme indicator uses accent. 44px touch targets. `let { theme = $bindable() } = $props()` (or an `onThemeChange` callback) so the shell owns the attribute.

**`+page.svelte`** — the four views + search. On mount, fetch all four payloads + meta via `api.ts` (parallel `Promise.all`); hold loading/error/data state. Render a tabbed/nav switch over the four `DataGrid` instances (each fed its payload + the matching `columns.ts` def). Above/near the views, mount `<SearchBox rows={viewPayload} />` (search runs over the `view` rows in memory — D-03). Near the `gear_check` and `spell_check` grids, show the compact **status legend** (OK/MISSING/OTHER swatches + words — UI-SPEC). For `bank`: if `coin === null` render the `Coin: not yet recorded` affordance (StateBlock copy) above/with the grid — never `0 pp`. On fetch failure → `<StateBlock kind="error" onRetry={refetch} />`; while loading → loading state; if a view's payload is empty → the per-view-empty StateBlock with the view's friendly name. The bank view may be legitimately empty until the P16 backfill sets `is_bank_toon` — render the empty state, not an error (RESEARCH Open-Q4 / A7).

Add a tiny vitest for the theme-apply wiring if feasible (e.g. a unit test that `loadTheme()` with a mocked empty localStorage returns 'velious' and with 'kunark' returns 'kunark') — reuse Plan 02's themes test harness; this keeps the velious-default + persistence contract regression-guarded. (Component DOM testing is optional; the build + check + the logic test are the gate.)
  </action>
  <verify>
    <automated>cd web && npm run check && npm run build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0 (emits `build/index.html` + `build/200.html`); `npm run check` passes; `npx vitest run` (all suites incl. Plan 02's) green.
    - `grep -n "data-theme" web/src/routes/+layout.svelte` (or SiteShell.svelte) matches — the single attribute carrying the theme.
    - `grep -n "loadTheme\|DEFAULT_THEME\|saveTheme" web/src/routes/+layout.svelte web/src/lib/components/ThemePicker.svelte` shows the velious-default + localStorage persistence wiring.
    - `grep -n "fetchView\|fetchGearCheck\|fetchSpellCheck\|fetchBank\|fetchMeta\|api\." web/src/routes/+page.svelte` shows all four views + meta fetched.
    - `grep -n "DataGrid" web/src/routes/+page.svelte` shows the grid instantiated for each view (one reusable component, 4 instances — never per-character tabs).
    - `grep -n "Coin: not yet recorded" web/src/routes/+page.svelte` (or via StateBlock) matches AND `grep -c "0 pp\|0pp" web/src/routes/+page.svelte` returns 0 (no fabricated coin).
    - `grep -n "CC-BY-SA" web/src/routes/+layout.svelte` (or SiteShell) matches (required footer attribution).
    - `grep -n "Retry\|onRetry" web/src/routes/+page.svelte` shows the error-state retry path.
  </acceptance_criteria>
  <done>The site shell applies the active EQ theme site-wide via a single [data-theme] write + localStorage (velious default), the four views render via the reusable DataGrid against the read API, search is wired over the in-memory view rows, bank shows the no-coin affordance, and the error/loading/empty states use the UI-SPEC copy — the app builds green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| read-API JSON → tooltip `{@html}` | The one place raw HTML is injected into the DOM. Mitigated by confining `{@html}` to Plan 02's already-escaped composeNotes output. |
| search query → "No matches for <query>" | User-typed string echoed in the UI — relies on Svelte's default `{}` escaping (never `{@html}`). |
| localStorage theme → [data-theme] | Constrained to the 5-key enum by resolveTheme (Plan 02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.04-01 | Tampering / Elevation (stored XSS) | ItemTooltip `{@html}` | mitigate (HIGH — block gate) | `{@html}` is confined to `composeItemNote(...)` output, which Plan 02's composeNotes fully escapes (T-14.02-01). Grep-verified `{@html}` appears ONLY in ItemTooltip.svelte. No other component injects raw HTML. |
| T-14.04-02 | Tampering (reflected XSS) | SearchResults "No matches for <query>" | mitigate | The query is rendered via Svelte's default `{query}` interpolation (auto-escaped), never `{@html query}` — grep-verified. |
| T-14.04-03 | Tampering (tab-nabbing) | Wiki `<a>` (grid Wiki cell + tooltip) | mitigate | `rel="noopener"` + `target="_blank"` on every external wiki anchor (tooltip inherits Plan 02's; the grid Wiki cell sets it explicitly in columns.ts). |
| T-14.04-04 | Spoofing (theme injection) | ThemePicker / loadTheme | mitigate | The `[data-theme]` value comes only from `resolveTheme` (Plan 02 enum-whitelist); an arbitrary localStorage value falls back to velious — no attribute/CSS injection. |
| T-14.04-05 | Info disclosure (data public by design) | the rendered site | accept (time-boxed) | Inherits D-04: the site is intentionally unauthenticated in P14 to unblock the guild; P15's AUTH-08 Discord login gates it. D-05 noindex (Plan 02) keeps it off search engines. Stated accepted risk, not oversight. |
</threat_model>

<verification>
- `cd web && npm run build` exits 0 (static SPA); `npm run check` passes; `npx vitest run` green.
- All four views render via the one reusable DataGrid (sticky Char + header, filter+sort) — never per-character tabs.
- gear/spell status badges render; search surfaces holders + inline "did you mean?"; tooltip is hover/tap with a wiki link; theme applies site-wide.
- `{@html}` confined to escaped composeNotes; search query auto-escaped; wiki links `rel="noopener"`.
- bank shows "Coin: not yet recorded" (no fabricated zeros); error/loading/empty states present.
</verification>

<success_criteria>
- WEB-01: the four consolidated views render with a leading sticky Char column, filterable + sortable on every column (one reusable grid, 4 instances).
- WEB-02 (presentation): gear OK/MISSING/OTHER + spell KNOWN/MISSING render as colored badges from the read API (compute is Plan 01).
- WEB-03: cross-character search returns holders <2s with a clickable inline "did you mean?" suggestion.
- WEB-04: per-row hover/tap rich-HTML tooltip (summary + price + quest) + a working wiki link.
- WEB-05: the active EQ theme applies site-wide via a single [data-theme] write + localStorage (velious default).
- BACKEND-05 (consumption): the client fetches the read endpoints Plan 03 pinned.
- D-03 (client search/tooltip), D-05 (noindex inherited), D-08 (hover/tap tooltip), D-09 (inline did-you-mean) satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/14-web-frontend/14-04-SUMMARY.md` — note the build output location (`web/build/`), the local-deploy-proof command (`npm run preview`), any UI-SPEC deviations, and that the public Cloudflare Pages deploy + the on-box Caddy-CORS verification (Pitfall 5) remain operational steps (mirroring P11's manual-deploy posture). Flag whether the live `bank` view was empty (is_bank_toon unset until P16) so it isn't read as a bug.
</output>
