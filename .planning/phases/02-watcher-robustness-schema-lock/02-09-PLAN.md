---
phase: 02-watcher-robustness-schema-lock
plan: 09
type: execute
wave: 7
depends_on: ["02-08"]
files_modified:
  - docs/smartscreen-walkthrough.md
  - docs/signpath-application.md
  - README.md
autonomous: false
requirements:
  - INST-05
must_haves:
  truths:
    - "A new docs/smartscreen-walkthrough.md exists and documents the 'Windows protected your PC → More info → Run anyway' flow with screenshots for Edge AND Chrome (Pitfall E coverage)."
    - "The walkthrough completes in under 30 seconds for a guildie following the steps (success criterion 5 in ROADMAP)."
    - "README.md links to smartscreen-walkthrough.md and to the GitHub Releases page; documents the 'tray turned red, what now?' recovery flow (links to Plan 02-04 Reauthorize)."
    - "A SignPath Foundation OSS application is filed (or a docs/signpath-application.md tracker is created with the application URL + filing date if approval lags) — async tracked, NOT blocking Phase 2 completion."
    - "The walkthrough explicitly addresses Firefox (no MOTW = no SmartScreen prompt; Defender may still flag at runtime) and Chrome (Keep/Discard prompt different from Edge's blue panel) so guildies on any browser have a path."
  artifacts:
    - path: "docs/smartscreen-walkthrough.md"
      provides: "30-second walkthrough with browser-specific paths (Edge default, Chrome variant, Firefox no-prompt note); screenshots placeholder; manual recovery if SmartScreen mis-flags after install"
      min_lines: 100
    - path: "docs/signpath-application.md"
      provides: "SignPath OSS application tracking: filing date, application URL, status, expected approval timeline"
      min_lines: 30
    - path: "README.md"
      provides: "Links to walkthrough + Releases page; lists key tray menu items including Reauthorize and Check for updates; 'tray turned red' recovery section"
  key_links:
    - from: "GitHub Release notes (auto-generated by softprops/action-gh-release)"
      to: "docs/smartscreen-walkthrough.md"
      via: "Release body links to docs/build-and-install.md, which links to smartscreen-walkthrough.md"
      pattern: "smartscreen-walkthrough.md"
    - from: "README.md"
      to: "docs/smartscreen-walkthrough.md, docs/build-and-install.md"
      via: "Quick-start install section links to walkthrough; troubleshooting links"
      pattern: "smartscreen-walkthrough\\.md|build-and-install\\.md"
---

<objective>
Implement INST-05 — the documented SmartScreen walkthrough that completes in <30 seconds — and file the SignPath Foundation OSS application in parallel as an async track. Per CONTEXT.md, the unsigned-with-walkthrough path is the Phase 2 default; SignPath is a parallel async track that ships in a 1.x patch if approval lands. This plan does NOT block Phase 2 on signing — it produces the walkthrough doc + README updates + SignPath application filing as deliverables.

Purpose: ROADMAP success criterion 5 requires "either signed binary OR documented walkthrough <30s". Plan 02-08 leaves the binary unsigned by default (signs: block disabled). This plan delivers the walkthrough side of the OR-clause AND files the SignPath application that may eventually flip us to the signed side.

Output: docs/smartscreen-walkthrough.md (with browser-specific paths covering Pitfall E); docs/signpath-application.md (tracker); README.md update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-watcher-robustness-schema-lock/02-CONTEXT.md
@.planning/phases/02-watcher-robustness-schema-lock/02-RESEARCH.md
@CLAUDE.md
@docs/build-and-install.md
@docs/oauth-setup.md
@README.md

<interfaces>
<!-- Locked decisions for this plan -->

From 02-RESEARCH.md ## Common Pitfalls Pitfall E:
- Edge: full blue "Windows protected your PC" panel → click "More info" → "Run anyway"
- Chrome: smaller "Keep / Discard" prompt at the bottom of the browser → click "Keep"
- Firefox: NO MOTW tag → SmartScreen never engages → Defender may still flag at runtime
- macOS: out of scope (document the rejection)

From 02-CONTEXT.md Code Signing decisions:
- Default release path is UNSIGNED. Document SmartScreen walkthrough as the deliverable.
- Apply for SignPath Foundation OSS in parallel (free, eligibility-gated, ~1-4 weeks).
- Never buy EV. (March 2024: EV no longer grants instant SmartScreen reputation.)
- Paid fallback (only if SignPath denied AND user opts in later): Certum OSS €69 + €30/yr.

SignPath Foundation:
- URL: https://signpath.org
- Application: https://signpath.org/foundation (eligibility form on the Foundation page)
- Eligibility: public OSS GitHub repo (✓ we have one), real OSS license (✓ to be added or confirmed in repo), MFA on GitHub account (✓), code-review process.
- Approval timeline: ~1-4 weeks per community reports (Tertiary confidence in RESEARCH.md).
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/smartscreen-walkthrough.md with browser-specific paths</name>
  <read_first>
    - .planning/phases/02-watcher-robustness-schema-lock/02-RESEARCH.md (## Common Pitfalls Pitfall E — full UX divergence across Edge/Chrome/Firefox)
    - docs/build-and-install.md (existing reference to "More info → Run anyway" — we'll cross-link)
    - .planning/research/PITFALLS.md Pitfall #2 if accessible (note in the new doc that PITFALLS.md Pitfall #2 is OUTDATED on EV-cert reputation per 02-RESEARCH.md State of the Art)
  </read_first>
  <action>
    Create `docs/smartscreen-walkthrough.md`:

    ```markdown
    # SmartScreen Walkthrough — Installing SquireBot

    SquireBot ships unsigned in Phase 2. Windows Defender SmartScreen will show one of three prompts depending on your browser when you double-click `SquireBot-Setup-<version>.exe`. All three lead to the same install in **under 30 seconds**.

    > **Why unsigned?** Code-signing certificates no longer grant instant SmartScreen reputation as of March 2024. A 12-user app would never accumulate enough downloads to clear the reputation curve, regardless of cert type. We're applying for free SignPath Foundation OSS code signing in parallel; until that approves, the walkthrough below is the recommended path. See [signpath-application.md](signpath-application.md) for status.

    ## Path A: Microsoft Edge (default Windows browser)

    1. After download, click the "..." next to the file in the download bar and choose **Keep**. (Edge's first warning — about 5 seconds.)
    2. Double-click `SquireBot-Setup-<version>.exe`.
    3. Windows Defender SmartScreen shows a blue panel titled **"Windows protected your PC"**.
    4. Click the small **"More info"** link near the bottom-left. The panel expands, showing the publisher (likely **"Unknown publisher"**).
    5. Click the new **"Run anyway"** button that appeared.
    6. The installer launches. Total elapsed: **~25 seconds** for a first-time user.

    *Screenshot placeholder: edge-smartscreen-1.png (the blue panel with "More info" highlighted)*
    *Screenshot placeholder: edge-smartscreen-2.png (after clicking More info — "Run anyway" highlighted)*

    ## Path B: Google Chrome

    1. After download, Chrome's bottom bar may show a **"Keep / Discard"** prompt for the .exe. Click **"Keep"**. (Chrome's first warning — about 5 seconds.)
    2. Double-click `SquireBot-Setup-<version>.exe`.
    3. Windows Defender SmartScreen shows the same blue panel as Edge: **"Windows protected your PC"**.
    4. Click **"More info"** → **"Run anyway"**, identical to Path A.

    *Screenshot placeholder: chrome-keep-discard.png (the bottom-bar prompt)*

    ## Path C: Mozilla Firefox

    Firefox does NOT tag downloads with the Windows Mark-of-the-Web by default, so SmartScreen often does not engage at all on Firefox-downloaded installers. You should be able to double-click and run with no Windows-level prompt.

    However, Defender (the runtime AV layer) may still scan the binary and flag it during execution. If you see a **Windows Defender SmartScreen** prompt at any point, follow the same "More info → Run anyway" path from Path A.

    > **Caveat:** if your guild standardizes on Firefox + an aggressive security suite, ask your guild leader before clicking through any prompts.

    ## What if Windows Defender quarantines SquireBot AFTER install?

    Sometimes Defender catches an unknown publisher binary post-install and silently moves it to quarantine. Symptoms:
    - The tray icon disappears.
    - `%LOCALAPPDATA%\Programs\SquireBot\squirebot.exe` no longer exists.
    - The Run-key autostart entry exists but points at nothing.

    Recovery:
    1. Open **Windows Security** → **Virus & threat protection** → **Protection history**.
    2. Find the most recent quarantine of `squirebot.exe`.
    3. Click **Actions** → **Restore** + **Allow on device**.
    4. SquireBot reappears at next logon (autostart fires).
    5. If Defender quarantines again on the next launch, file a false-positive report at https://www.microsoft.com/en-us/wdsi/filesubmission so SquireBot's hash gets whitelisted globally for future guildies.

    ## Why this is "good enough" at our scale

    | Bar | Met by SquireBot |
    |-----|------------------|
    | Install completes in <30s without admin rights | Yes (NSIS per-user, no UAC; SmartScreen "Run anyway" in 5s) |
    | Documented for non-technical users | Yes (this doc + screenshots + browser-specific paths) |
    | False-positive recovery procedure exists | Yes (Defender quarantine restore steps above) |
    | Path to signed binary if needed later | Yes ([signpath-application.md](signpath-application.md) tracks the OSS application) |

    For a 12-guildie audience installing once and forgetting, this is the right ergonomics-vs-cost tradeoff. See [PROJECT.md](../.planning/PROJECT.md) for the cost-of-signing analysis that drove this decision.

    ## macOS / Linux

    Out of scope. SquireBot is Windows-only because EverQuest's `/outputfile inventory` writes to a fixed Windows path; the watcher's value is its location next to the EQ install.

    ---

    *Last updated: 2026-05 (Phase 2). PITFALLS.md Pitfall #2 contains outdated guidance on EV-cert reputation; defer to this doc.*
    ```

    NOTE: actual screenshots are deferred to Phase 5 polish (per ROADMAP — Phase 5 owns onboarding screenshots/video for DOC-03). For Phase 2, placeholder text is acceptable. The walkthrough's TEXT is the Phase 2 deliverable.
  </action>
  <verify>
    <automated>cd C:\Users\Virus Canary\Desktop\Claude\SquireBot && grep -c 'Microsoft Edge\|Google Chrome\|Mozilla Firefox' docs/smartscreen-walkthrough.md && grep -c 'More info\|Run anyway\|Keep' docs/smartscreen-walkthrough.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/smartscreen-walkthrough.md` exists.
    - `grep -c '## Path A\|## Path B\|## Path C' docs/smartscreen-walkthrough.md` returns 3 (Edge + Chrome + Firefox sections).
    - `grep -c 'Run anyway' docs/smartscreen-walkthrough.md` returns at least 2 (Edge path + cross-reference for Firefox runtime).
    - `grep -c 'Keep' docs/smartscreen-walkthrough.md` returns at least 2 (Edge first prompt + Chrome first prompt).
    - `grep -c 'Mark-of-the-Web\|MOTW' docs/smartscreen-walkthrough.md` returns at least 1 (Pitfall E technical note).
    - `grep -c '<30 seconds\|under 30 seconds' docs/smartscreen-walkthrough.md` returns at least 1 (success criterion 5 explicit).
    - `grep -c 'signpath-application.md' docs/smartscreen-walkthrough.md` returns at least 1 (cross-link).
    - `wc -l docs/smartscreen-walkthrough.md` returns at least 60 lines (substantive doc, not a stub).
  </acceptance_criteria>
  <done>SmartScreen walkthrough documents all three browser paths + Defender quarantine recovery + cross-links to SignPath tracker.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: File the SignPath Foundation OSS application</name>
  <what-built>
    - Phase 2 has decided to ship unsigned with the walkthrough as the default (Plan 02-08 leaves signs: block disabled in .goreleaser.yaml).
    - Plan 02-09 Task 1 produced docs/smartscreen-walkthrough.md.
    - Plan 02-09 Task 3 will create docs/signpath-application.md to track the application status.
    - SignPath Foundation OSS application is the parallel async track. Approval timeline ~1-4 weeks per community reports.
  </what-built>
  <how-to-verify>
    This task requires a human action that Claude cannot perform: filing a real OSS sponsorship application with a real GitHub identity + email + MFA confirmation.

    **Steps:**

    1. Visit https://signpath.org/foundation (the SignPath Foundation OSS sponsorship page).

    2. Confirm prerequisites for SquireBot eligibility:
       - **Public GitHub repo:** ✓ https://github.com/boejowen/SquireBot is public.
       - **Real OSS license:** Verify `LICENSE` file exists at repo root. If missing, add one (MIT or Apache-2.0 are conventional Go choices) BEFORE filing the application — SignPath checks for an OSI-approved license. If you already have one, confirm it's valid.
       - **MFA on GitHub account:** ✓ user has 2FA enabled (verify at https://github.com/settings/security).
       - **Active project:** ✓ Phase 1 shipped 2026-05-02, multiple recent commits.

    3. Submit the application form on SignPath Foundation's site. Required fields typically include:
       - Project name: `SquireBot`
       - Repo URL: `https://github.com/boejowen/SquireBot`
       - License: (whatever is in repo's LICENSE file)
       - Brief description: "Per-guildie Windows watcher for an EverQuest Project 1999 guild's shared Google Sheets inventory tracker. ~12 users, single-binary install, OAuth + Sheets API."
       - Code review / build process description: "Public CI on GitHub Actions, NSIS installer, hand-author .nsi + .goreleaser.yaml, branch protection on main."

    4. Save the application confirmation email (with submission date + reference number if any).

    5. Capture the relevant data for Task 3's tracker doc:
       - Application URL or reference number.
       - Submission date (today).
       - Expected response timeline (whatever SignPath communicates).
       - Email / contact thread the response will arrive at.
  </how-to-verify>
  <resume-signal>
    Type "approved" if the SignPath application has been filed (or alternatively, type "skipped" if the user has decided NOT to apply at this time — which keeps Phase 2 valid since the walkthrough is the default path; just note the decision in Task 3's tracker doc).

    If filed, share the submission date + any reference number — Task 3 will record them.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create docs/signpath-application.md tracker doc</name>
  <read_first>
    - Task 2 checkpoint output (submission date, reference number, contact thread)
    - .planning/phases/02-watcher-robustness-schema-lock/02-RESEARCH.md (## Code-Signing Recommendation Matrix path B — SignPath details + assumption A2 about aggregate reputation)
    - .planning/phases/02-watcher-robustness-schema-lock/02-CONTEXT.md (Code Signing Q1 — paid fallback chain: SignPath → Certum OSS only if SignPath denied)
  </read_first>
  <action>
    Create `docs/signpath-application.md`. The file is a living tracker; populate fields from Task 2 checkpoint output (or note "skipped" if user opted out):

    ```markdown
    # SignPath Foundation OSS Application — Tracker

    **Status:** {FILED | SKIPPED | APPROVED | DENIED} — {date}
    **Application URL / reference:** {paste from Task 2}
    **Submission date:** {YYYY-MM-DD from Task 2}
    **Expected response timeline:** ~1-4 weeks per community reports (no official SLA).
    **Project repo:** https://github.com/boejowen/SquireBot
    **Contact thread:** {email or however SignPath communicates}

    ## Why this exists

    SquireBot ships **unsigned by default** in Phase 2. Per [02-RESEARCH.md ## Code-Signing Recommendation Matrix](../.planning/phases/02-watcher-robustness-schema-lock/02-RESEARCH.md), the 2024–2026 code-signing landscape changed materially: EV no longer grants instant SmartScreen reputation, and a 12-user app cannot accumulate the downloads needed to clear the reputation curve regardless of cert type. So:

    - **Default path (Phase 2 ships):** unsigned + [docs/smartscreen-walkthrough.md](smartscreen-walkthrough.md) (under 30 seconds for a guildie).
    - **Parallel async track:** SignPath Foundation OSS sponsorship — free, cloud-HSM-backed, eligibility-gated.
    - **Fallback (only if SignPath denied AND user opts in):** Certum OSS Code Signing, €69 one-time + €30/yr smartcard renewal.

    ## What approval would change

    Once approved, [.goreleaser.yaml](../.goreleaser.yaml)'s `signs:` block (currently `disable: true`) gets enabled and configured to call SignPath's GitHub Action. This is a single config-block change, not a re-architecture (Phase 2's release pipeline is signing-aware).

    No watcher code changes. The auto-updater (Plan 02-06's `internal/update`) is signing-agnostic — it just consumes the manifest and verifies SHA-256.

    ## Eligibility checklist

    - [x] Public OSS GitHub repo (https://github.com/boejowen/SquireBot)
    - [{LICENSE}] OSI-approved license — confirm by checking `LICENSE` file at repo root
    - [x] MFA on GitHub account
    - [x] Active project (Phase 1 shipped, Phase 2 in progress)
    - [{x|?}] No prior SignPath sponsorship (single-project rule)

    ## Approval workflow

    Once SignPath approves:

    1. Receive shared certificate config / GitHub Action token.
    2. Add `SIGNPATH_API_TOKEN` secret to repo settings.
    3. Add `signpath/signpath-action` to release workflow OR enable goreleaser's signs: block by removing `disable: true` and configuring the cmd/args (per https://about.signpath.io/documentation/build-systems/goreleaser/).
    4. Tag a real release; verify the GitHub Release's installer .exe shows publisher info in the SmartScreen panel.
    5. Update [docs/smartscreen-walkthrough.md](smartscreen-walkthrough.md) to mention "if you see a 'Verified publisher: SignPath Foundation' line, you're on the signed-binary path."
    6. Update this file's Status field to `APPROVED — {date}`.

    If denied:

    1. Update Status to `DENIED — {date}`.
    2. Per [02-CONTEXT.md Code Signing Q1](../.planning/phases/02-watcher-robustness-schema-lock/02-CONTEXT.md), surface to user: option to fall back to Certum OSS (€69 + €30/yr) or stay unsigned indefinitely.

    ## Assumption to validate

    Per [02-RESEARCH.md Assumptions Log A2](../.planning/phases/02-watcher-robustness-schema-lock/02-RESEARCH.md), the claim "SignPath's aggregate reputation builds SmartScreen trust faster than a single 12-user project's would" is plausible but unverified. Once approved AND the first signed release is downloaded by a few guildies, observe whether the SmartScreen UX is noticeably different from the unsigned baseline. Update this section with empirical findings.

    ---

    *Tracker created: 2026-05 (Phase 2 Plan 09). Update this file whenever SignPath status changes.*
    ```

    Replace the `{FILED | SKIPPED | ...}` and `{paste from Task 2}` placeholders with the actual values from the checkpoint outcome before saving.
  </action>
  <verify>
    <automated>cd C:\Users\Virus Canary\Desktop\Claude\SquireBot && grep -c 'Status:' docs/signpath-application.md && grep -c 'smartscreen-walkthrough.md\|.goreleaser.yaml' docs/signpath-application.md && grep -c 'Certum OSS' docs/signpath-application.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/signpath-application.md` exists.
    - `grep -c '\*\*Status:\*\*' docs/signpath-application.md` returns 1.
    - `grep -c 'smartscreen-walkthrough.md' docs/signpath-application.md` returns at least 2 (cross-links).
    - `grep -c '.goreleaser.yaml' docs/signpath-application.md` returns at least 1 (the disable: true → enabled config-block change).
    - `grep -c 'Certum OSS' docs/signpath-application.md` returns at least 1 (fallback path documented).
    - `grep -c 'disable: true' docs/signpath-application.md` returns at least 1 (Plan 02-08 wiring referenced).
    - `wc -l docs/signpath-application.md` returns at least 30 lines.
  </acceptance_criteria>
  <done>Tracker doc exists with status (FILED or SKIPPED), cross-links to walkthrough + goreleaser config, fallback path to Certum OSS documented.</done>
</task>

<task type="auto">
  <name>Task 4: Update README.md with install + tray + recovery sections</name>
  <read_first>
    - README.md (current contents — likely thin since Phase 1 was internal-focused; Phase 2 starts polishing for guildie-facing onboarding)
    - docs/build-and-install.md (already covers detailed install/uninstall instructions; README will link to it)
    - docs/smartscreen-walkthrough.md (Task 1 output)
    - docs/oauth-setup.md (existing OAuth setup runbook)
    - .planning/PROJECT.md (Core Value statement — copy verbatim into README's lead)
  </read_first>
  <action>
    Replace or substantially update `README.md` with this structure (preserve any existing sections that already cover these areas; otherwise create them):

    ```markdown
    # SquireBot

    > Every guildie can answer "what does my character still need, and where in the guild is it?" without leaving the spreadsheet.

    A small Windows app that every member of a ~12-person Project 1999 guild installs on their PC. SquireBot watches the EverQuest folder for tab-separated text files produced by `/outputfile inventory` and `/outputfile spellbook` and pushes their contents into a shared Google Sheet. The sheet (Apps Script + TypeScript) joins each guildie's data with the P1999 wiki and the PigParse REST API to produce per-character inventory views, gear/spell progression checklists vs. Velious tiers, a shared bank with cross-character search, and item tooltips.

    See [.planning/PROJECT.md](.planning/PROJECT.md) for the full project context.

    ## Install

    1. Download `SquireBot-Setup-<version>.exe` from the [latest GitHub Release](https://github.com/boejowen/SquireBot/releases/latest).
    2. Walk through the SmartScreen "More info → Run anyway" prompt. **Don't skip this — your browser may show different prompts.** See [docs/smartscreen-walkthrough.md](docs/smartscreen-walkthrough.md) for browser-specific paths (Edge, Chrome, Firefox).
    3. The installer runs without admin rights (per-user only).
    4. SquireBot launches automatically after install. Complete the wizard:
       - Authorize Google (browser opens for OAuth — `drive.file` scope only).
       - Pick the shared guild workbook in the Drive Picker.
       - Confirm the EverQuest install folder (auto-detected from registry / heuristics).
    5. SquireBot now lives in the system tray, watching for `*-Inventory.txt` and `*-Spellbook.txt` files.

    For local building from source, see [docs/build-and-install.md](docs/build-and-install.md). For the OAuth setup runbook (Cloud Console steps), see [docs/oauth-setup.md](docs/oauth-setup.md).

    ## Tray menu

    Right-click the tray icon for these options:

    | Item | Purpose |
    |------|---------|
    | Status (top, disabled) | Current state, e.g. "Last upload: Foo at 14:32" |
    | Open Workbook | Opens the configured Google Sheet in your browser |
    | Open log folder | Opens `%LOCALAPPDATA%\SquireBot\` in Explorer |
    | Check for updates | Manually triggers an update check (auto-checks every 24h) |
    | Change Workbook… | Re-runs the Drive Picker to switch to a different workbook |
    | Continue setup… | (hidden unless setup is incomplete) |
    | Reauthorize… | (hidden unless your Google refresh token died) |
    | Quit | Exit SquireBot |

    ## Tray turned red — what now?

    A red tray icon means one of:

    - **Setup needed** — wizard didn't finish. Click **Continue setup…** to resume.
    - **Refresh token died** — Google's refresh token has expired or been revoked (e.g., you changed your password, or the Testing-mode 7-day expiry hit). Click **Reauthorize…** to redo OAuth. The watcher resumes immediately on success.
    - **Watcher error** — see `%LOCALAPPDATA%\SquireBot\squirebot.log` for details. Common causes: workbook deleted, sheet schema mismatch (run a fresh install if so).

    Most red states are recoverable in 30 seconds via the tray menu. If you're stuck, share the relevant lines from the log file.

    ## Auto-update

    SquireBot auto-updates daily via a `latest.json` manifest fetched from GitHub Releases. SHA-256 verification ensures binary integrity. The swap happens at next launch (Windows file-locking forbids in-process replacement). Manual trigger: tray's **Check for updates**.

    ## Uninstalling

    See [docs/build-and-install.md "Uninstalling"](docs/build-and-install.md#uninstalling) for the full uninstall flow including the "preserve config / wipe everything" prompt.

    ## Project status

    Phase: 2 of 5 (Watcher Robustness + Schema Lock). See [.planning/ROADMAP.md](.planning/ROADMAP.md) for the full phased plan.

    ## Code signing

    SquireBot ships unsigned. We've applied for free SignPath Foundation OSS code signing — see [docs/signpath-application.md](docs/signpath-application.md) for status. The unsigned + walkthrough path will remain the default until/unless SignPath approves.

    ## License

    See [LICENSE](LICENSE).
    ```

    Preserve any existing README content that this rewrite doesn't cover (e.g., contributor sections, build badges).
  </action>
  <verify>
    <automated>cd C:\Users\Virus Canary\Desktop\Claude\SquireBot && grep -c 'smartscreen-walkthrough.md\|build-and-install.md\|oauth-setup.md\|signpath-application.md' README.md && grep -c 'tray turned red\|Reauthorize\|Check for updates' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'smartscreen-walkthrough.md' README.md` returns at least 1.
    - `grep -c 'build-and-install.md' README.md` returns at least 1.
    - `grep -c 'oauth-setup.md' README.md` returns at least 1.
    - `grep -c 'signpath-application.md' README.md` returns at least 1.
    - `grep -ic 'tray turned red\|Tray turned red' README.md` returns at least 1 (recovery section).
    - `grep -c 'Reauthorize' README.md` returns at least 2 (tray menu table + recovery section).
    - `grep -c 'Check for updates' README.md` returns at least 2.
    - `grep -c 'releases/latest' README.md` returns at least 1 (download link).
    - `wc -l README.md` returns at least 50 lines (substantive, not stub).
  </acceptance_criteria>
  <done>README.md links to walkthrough + uninstall + oauth + signpath docs; tray menu documented; "tray turned red" recovery section explains all three failure modes.</done>
</task>

</tasks>

<verification>
- `docs/smartscreen-walkthrough.md` covers Edge + Chrome + Firefox paths (Pitfall E coverage).
- `docs/signpath-application.md` records the application status (FILED / SKIPPED / APPROVED / DENIED).
- `README.md` links to all three new/updated docs and documents the tray red-icon recovery flow.
- All acceptance grep checks return their expected counts.
</verification>

<success_criteria>
- INST-05 met: documented walkthrough completes in &lt;30 seconds; covers all three browsers per Pitfall E.
- SignPath OSS application is filed (or formally skipped) per CONTEXT.md decision.
- README.md is now guildie-readable for onboarding (Phase 5 will add screenshots/video; Phase 2 ships the prose).
- The unsigned-default decision is documented and self-explanatory; no future maintainer will be confused about why we didn't buy EV.
</success_criteria>

<output>
After completion, create `.planning/phases/02-watcher-robustness-schema-lock/02-09-SUMMARY.md` documenting:
- Files created (`docs/smartscreen-walkthrough.md`, `docs/signpath-application.md`)
- Files modified (`README.md`)
- SignPath application status: FILED on {date} OR SKIPPED per user decision
- Walkthrough timing measurement (if attempted on a fresh VM): elapsed seconds for each browser path
</output>
