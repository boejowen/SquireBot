---
phase: 38-catalog-wide-enrichment-icon-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/backendsrv/store/itemids.go
  - internal/backendsrv/store/itemids_test.go
  - internal/backendsrv/enrich/jobs/wiki.go
  - internal/backendsrv/enrich/jobs/wiki_test.go
autonomous: true
requirements: [ENRICH-14, ENRICH-15]

must_haves:
  truths:
    - "The weekly wiki items pass fetches a wiki page for every item in the full PigParse Blue catalog (~4,341 rows), not only items some character currently holds (ENRICH-14)."
    - "A catalog-only (unheld) item gets an item_master row with a non-zero icon_id when its wiki page provides a lucy_img_ID (ENRICH-15 icon backfill)."
    - "Each distinct item (by normalized name) is fetched exactly once — a held item and its catalog row never both appear in the crawl set (politeness preserved; D-04 dedup)."
    - "A catalog PigParse item_id that numerically equals an existing item_master row's id is EXCLUDED from the catalog arm, so the held row's enrichment is never overwritten (D-04 collision guard)."
    - "After each items pass the job logs ONE structured slog summary line with total / enriched / icon-covered / icon-less counts + a bounded residue name sample (ENRICH-15 / D-03)."
  artifacts:
    - path: "internal/backendsrv/store/itemids.go"
      provides: "DistinctEnrichmentRefs union read (held EQ ids ∪ catalog-only PigParse ids, deduped by lower(trim(name)), held wins, EQ-id-collision exclusion)"
      contains: "func (s *Store) DistinctEnrichmentRefs"
    - path: "internal/backendsrv/store/itemids_test.go"
      provides: "Union test mirroring TestDistinctInventoryItemIDs incl. the unheld-catalog, held-wins, and id-collision-exclusion cases"
      contains: "func TestDistinctEnrichmentRefs"
    - path: "internal/backendsrv/enrich/jobs/wiki.go"
      provides: "runWikiItems iterating DistinctEnrichmentRefs + the D-03 coverage slog summary line"
      contains: "DistinctEnrichmentRefs"
    - path: "internal/backendsrv/enrich/jobs/wiki_test.go"
      provides: "Widened seeding (a pigparse_price catalog-only item with a wiki fixture) asserting it gets enriched + iconned"
      contains: "item_master"
  key_links:
    - from: "internal/backendsrv/enrich/jobs/wiki.go::runWikiItems"
      to: "internal/backendsrv/store/itemids.go::DistinctEnrichmentRefs"
      via: "s.DistinctEnrichmentRefs(ctx) replaces s.DistinctInventoryItemIDs(ctx) as the loop input"
      pattern: "DistinctEnrichmentRefs"
    - from: "internal/backendsrv/store/itemids.go::DistinctEnrichmentRefs catalog arm"
      to: "item_master.item_id"
      via: "AND item_id NOT IN (SELECT item_id FROM item_master) collision-exclusion subquery"
      pattern: "NOT IN \\(SELECT item_id FROM item_master\\)"
---

<objective>
Widen item enrichment from "only items some character currently HOLDS" (today's
`store.DistinctInventoryItemIDs` gate, ~hundreds of pages) to the **full PigParse Blue
catalog** (`pigparse_price`, ~4,341 rows), and backfill the wiki icon for every catalog
item whose wiki page provides one — with a maintainer-visible structured `slog` diagnostic
for the items that remain icon-less. This is the "not all items have images" bug AND the
full-catalog data the Phase 39 search-scope toggle (SEARCH-06, "what exists") reads.

This is the milestone's biggest/riskiest piece, but per RESEARCH it is almost entirely
*reuse + one store-method addition + one loop-input swap + one log line*. The wiki parser,
the per-item upsert, the freshness short-circuit (which ALREADY compares `icon_id`), the 1s
courtesy sleep, the ETag 304 short-circuit, and the log-and-skip-one-bad-page resilience all
already exist and already do everything per item. The ONLY behavioral change ENRICH-14 needs
is the SET of refs `runWikiItems` iterates; ENRICH-15 (icon backfill) then falls out for free
because widening the set is what newly fetches an unheld item's page and populates its icon.

Implements: ENRICH-14 (full-catalog enrichment, politefetch-paced) + ENRICH-15 (icon
backfill + a maintainer can see the icon-less residue).

Honors locked decisions:
- **D-01 / D-01a:** seed-once-then-weekly-re-validate-all-via-ETag, running AUTOMATICALLY
  inside the existing weekly wiki job — NO new admin trigger, NO execution-cap bookkeeping.
  The job already has no execution cap (`wiki.go` header), so the ~72-min first Sunday run
  is fine; later weeks 304 cheaply on unchanged pages.
- **D-02:** "full catalog" = `pigparse_price` ONLY (~4,341 auctioned rows); do NOT union
  never-auctioned `wiki_gear_tier` items into the crawl.
- **D-03:** the coverage diagnostic = a per-run structured `slog` summary line (no new UI).
- **D-04 → RESEARCH Option A:** admit catalog-only rows into `item_master` keyed by the
  PigParse `item_id`, but ONLY for normalized names with no held EQ-id row, with an EQ-id
  collision exclusion. NO new table, NO new migration (00016 stays the last).

Purpose: Turn the half-captured catalog into usable intelligence — every searchable item
shows its real wiki icon where the wiki provides one, and the residue is visible to the
maintainer. Unblocks Phase 39's full-catalog search scope.
Output: a new `store.DistinctEnrichmentRefs` union read (+ test), the `runWikiItems` input
swap + D-03 slog line (+ widened test). No migration, no watcher change, no `v*` tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase artifacts — READ THESE FIRST (the implementation is fully specified here)
@.planning/phases/38-catalog-wide-enrichment-icon-coverage/38-CONTEXT.md
@.planning/phases/38-catalog-wide-enrichment-icon-coverage/38-RESEARCH.md
@.planning/phases/38-catalog-wide-enrichment-icon-coverage/38-PATTERNS.md

# The prior phase whose backfill+freshness pattern this extends (same item_master row)
@.planning/phases/37-item-enrichment-backbone-flags-effects/37-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase 2026-06-25. Use directly — no exploration needed. -->

The ItemRef return shape is UNCHANGED — the new union method returns the same struct
(from internal/backendsrv/store/itemids.go:25-28):
```go
type ItemRef struct {
	ItemID int64
	Name   string
}
```

The existing held-arm read to mirror (internal/backendsrv/store/itemids.go:40-64) — note
GROUP BY item_id + MIN(name) is the "fetch each page once" politeness rule:
```go
func (s *Store) DistinctInventoryItemIDs(ctx context.Context) ([]ItemRef, error) {
	rows, err := s.db.QueryContext(ctx,
		`SELECT item_id, MIN(name) AS name
		 FROM inventory_item
		 WHERE item_id IS NOT NULL AND item_id > 0
		 GROUP BY item_id
		 ORDER BY item_id`)
	if err != nil {
		return nil, fmt.Errorf("query distinct inventory item ids: %w", err)
	}
	defer rows.Close()
	var refs []ItemRef
	for rows.Next() {
		var ref ItemRef
		if err := rows.Scan(&ref.ItemID, &ref.Name); err != nil {
			return nil, fmt.Errorf("scan inventory item ref: %w", err)
		}
		refs = append(refs, ref)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("iterate inventory item refs: %w", err)
	}
	return refs, nil
}
```

The freshness getter that ALREADY carries the icon — DO NOT MODIFY
(internal/backendsrv/store/enrich.go:282-294):
```go
func GetItemMasterFreshnessTx(ctx context.Context, tx *sql.Tx, itemID int64) (sha string, iconID int64, statsblock, flagsJSON string, err error) {
	// SELECT wikitext_sha1, icon_id, statsblock, flags_json FROM item_master WHERE item_id = ?
	// returns "",0,"","" on sql.ErrNoRows
}
```

The runWikiItems loop input — the ONE line that changes
(internal/backendsrv/enrich/jobs/wiki.go:161):
```go
refs, rerr := s.DistinctInventoryItemIDs(ctx)   // today
refs, rerr := s.DistinctEnrichmentRefs(ctx)     // Phase 38 — the entire loop body below is UNCHANGED
```

The per-item freshness short-circuit already compares icon_id (DO NOT touch)
(internal/backendsrv/enrich/jobs/wiki.go:227-245): widening the ref set is the WHOLE
icon-backfill mechanism — no new icon code.

The upsert is namespace-agnostic on item_id (ON CONFLICT(item_id) DO UPDATE,
internal/backendsrv/store/enrich.go:204-216): a catalog-only ref's PigParse id flows through
`ItemID: int(ref.ItemID)` exactly like a held EQ id — no branch.

The existing end-of-job summary slog line to mirror (internal/backendsrv/enrich/jobs/wiki.go:149-153):
```go
detail := fmt.Sprintf("items_ok=%d items_unchanged=%d items_fail=%d ...", ...)
slog.Info(wikiJobName+": ok", "detail", detail)
```

Test helpers available IN-PACKAGE (no imports needed):
- store package: `NewTestDB(t)`, `seedOwnerChar(t, db, label, name)`, `seedRaw(t, db, charID, location, name, *int64 itemID, ordinal)` + `i64ptr(v)`, `seedItemMaster(t, db, itemID, name, summary, url, isQuest)`, `seedPigparse(t, db, itemID, name, direction, a30, t30)` (its doc notes the name-bridge / id-namespace rule).
- jobs package: `setWikiSleepNoop()`, `newWikiFixtureServer(t, wikiServerOpts{})` (serves ../testdata/<fixture>.json per ?page=; unknown page → MediaWiki error envelope), `serverFetcher(srv)`, `seedInvFor(t, db, charID, name, itemID, ord)`, `seedOwnerChar`. Existing item fixtures with a lucy_img_ID: `Cloak of Flames` (id 18950) → `wiki-parse-cloak-of-flames`, `Pearl` (11000), `Cloth Cap` (1001), `Fungus Covered Scale Tunic` (13128). `seedPigparse` is a `store`-package helper; in the jobs test seed via a raw `db.Exec` INSERT into pigparse_price (the jobs package cannot call the unexported store helper).
</interfaces>
</context>

<pre_execution_check>
## D-04 prod collision probe (READ-ONLY — run BEFORE the code tasks)

RESEARCH Assumption A2 / Open Question 2: the catalog arm's EQ-id-collision exclusion
(`AND item_id NOT IN (SELECT item_id FROM item_master)`) DROPS a catalog name whose PigParse
id numerically equals an existing `item_master` row id for a DIFFERENT item. Expected count
≈ 0. Confirm this is harmless before relying on the exclusion.

Run this READ-ONLY probe against the **prod** SQLite (Hetzner VPS 5.78.232.85 — Windows
ssh-agent SERVICE key via PowerShell `ssh.exe`, per the resume-point memory; the on-box DB
path is the one the backend opens). It is a SELECT — no write, no migration, no backup needed:

```sql
SELECT count(*) AS colliding
FROM pigparse_price p
JOIN item_master m ON p.item_id = m.item_id
WHERE lower(trim(p.name)) <> lower(trim(m.name));
```

Disposition:
- **count ≈ 0 (EXPECTED):** the exclusion is inert — those few (if any) names simply stay
  icon-less and surface in the D-03 residue sample. Proceed with Option A as written. Record
  the count in the SUMMARY.
- **count is material (>~20):** do NOT silently drop that many items. STOP and surface to the
  user: escalate to a name-keyed fallback for just the excluded set (still no new table — a
  second catalog arm keyed by name for ids that collide). Note it in the SUMMARY and request a
  decision before shipping.

This probe is informational — it does NOT block writing the code (the exclusion is correct
regardless; the probe only sizes how much residue it produces). If prod SSH is unavailable at
plan-execution time, proceed with the code (the exclusion is the safe default) and record
"probe deferred — exclusion applied as the safe default" in the SUMMARY.
</pre_execution_check>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add store.DistinctEnrichmentRefs (held ∪ catalog union, deduped by name, held wins, EQ-id-collision guard) + its test</name>
  <read_first>
    - internal/backendsrv/store/itemids.go (the file being modified — `DistinctInventoryItemIDs` is the exact analog to copy: same file, same `ItemRef` shape, same scan/error-wrap idiom)
    - internal/backendsrv/store/itemids_test.go (the test file being modified — `TestDistinctInventoryItemIDs` + the `seedRaw`/`i64ptr` helpers are the analog)
    - internal/backendsrv/store/readviews.go around the `pp_rep` CTE (the `lower(trim(name))` normalized-name dedup idiom the catalog arm mirrors — search for `pp_rep`)
    - internal/backendsrv/store/readviews_test.go:30-57 (the `seedItemMaster` + `seedPigparse` helpers, with `seedPigparse`'s doc noting the name-bridge rule)
    - .planning/phases/38-catalog-wide-enrichment-icon-coverage/38-PATTERNS.md (the "store.DistinctEnrichmentRefs" section — the exact union SQL sketch + the 4 mandatory test cases)
    - .planning/phases/38-catalog-wide-enrichment-icon-coverage/38-RESEARCH.md (§D-04 RESOLUTION — the union read shape + the collision guard rationale)
  </read_first>
  <behavior>
    Test (TestDistinctEnrichmentRefs, mirroring TestDistinctInventoryItemIDs in the same file):
    - Held items still appear keyed by their EQ id, MIN(name) representative, ordered, dedup-by-id (re-prove the existing held-arm behavior: duplicate ids across chars → one ref; item_id=0 and NULL excluded).
    - Case A — unheld catalog item: a `pigparse_price` row whose normalized name is NOT held → appears in the union keyed by its PigParse id.
    - Case B — held name wins: a `pigparse_price` row whose `lower(trim(name))` IS held (same item, different/equal id) → appears EXACTLY ONCE, keyed by the held EQ id (NOT the PigParse id); no duplicate row for that name.
    - Case C — EQ-id collision exclusion: a `pigparse_price.item_id` that numerically equals an existing `item_master` row id for a DIFFERENT (unheld) name → that catalog row is EXCLUDED from the union (the `NOT IN (SELECT item_id FROM item_master)` guard), so it never reaches the upsert and cannot overwrite the held row.
    - Case D — one ref per normalized name: after the full union, assert there is exactly one ref per `lower(trim(name))` (Pitfall 1 regression).
  </behavior>
  <action>
    Add `func (s *Store) DistinctEnrichmentRefs(ctx context.Context) ([]ItemRef, error)` to
    internal/backendsrv/store/itemids.go, directly below `DistinctInventoryItemIDs`. Reuse the
    EXACT scan/error-wrap idiom from `DistinctInventoryItemIDs` (`s.db.QueryContext`, `defer
    rows.Close()`, `rows.Scan(&ref.ItemID, &ref.Name)`, `rows.Err()`, each error wrapped
    `fmt.Errorf("...: %w", err)`). No new imports. The method is a plain `(*Store)` read method
    (no Tx variant — read side). The job authors ZERO inline SQL (11-05 single-tested-SQL-path
    rule) — this SELECT lives HERE.

    Use this exact union SQL (from PATTERNS / RESEARCH §D-04 — the held arm is byte-identical to
    `DistinctInventoryItemIDs`; the catalog arm mirrors the `pp_rep` `lower(trim(name))` dedup
    with the TWO load-bearing exclusions):

    ```sql
    WITH held AS (
      SELECT item_id, MIN(name) AS name
      FROM inventory_item
      WHERE item_id IS NOT NULL AND item_id > 0
      GROUP BY item_id
    ),
    held_names AS (
      SELECT DISTINCT lower(trim(name)) AS norm
      FROM inventory_item
      WHERE item_id IS NOT NULL AND item_id > 0
    ),
    catalog AS (
      SELECT MIN(item_id) AS item_id, MIN(name) AS name
      FROM pigparse_price
      WHERE name IS NOT NULL AND trim(name) <> ''
        AND lower(trim(name)) NOT IN (SELECT norm FROM held_names)
        AND item_id NOT IN (SELECT item_id FROM item_master)
      GROUP BY lower(trim(name))
    )
    SELECT item_id, name FROM held
    UNION ALL
    SELECT item_id, name FROM catalog
    ORDER BY item_id
    ```

    The two exclusions are NON-NEGOTIABLE (D-04 collision guard — Pitfall 2/4 in RESEARCH):
    1. `lower(trim(name)) NOT IN (SELECT norm FROM held_names)` — held names win (no duplicate
       row for an item that is both held and in the catalog; preserves "fetch each page once").
    2. `item_id NOT IN (SELECT item_id FROM item_master)` — a PigParse id equal to an existing
       `item_master` row id is excluded, so `ON CONFLICT(item_id) DO UPDATE` can never overwrite
       the wrong row.
    Dedup the catalog arm by `lower(trim(name))` (NOT by id) — an id-keyed or (id,name)-keyed
    dedup would refetch the same wiki page under multiple casings = a politeness regression.

    Write a doc comment on the method matching the file's style: explain it returns held EQ ids
    ∪ catalog-only PigParse ids deduped by normalized name (held wins), the two exclusions and
    WHY (the EQ-id namespace difference + the ON CONFLICT overwrite hazard), and that it
    preserves the per-name "fetch each page once" politeness rule.

    Then add `TestDistinctEnrichmentRefs` to itemids_test.go per the <behavior> block above.
    Reuse `NewTestDB`, `seedOwnerChar`, `seedRaw`/`i64ptr` for held rows. For catalog rows and
    the held-name collision row, INSERT directly into `pigparse_price` (mirror the
    `seedPigparse` shape from readviews_test.go — the columns are
    `item_id, name, current_avg, blue_volume, last_seen, direction, t30, a30, last_refreshed`);
    `seedPigparse` and `seedItemMaster` are available in the `store` test package, so call them
    directly rather than re-inserting by hand. For Case C, `seedItemMaster` a row at id X for an
    unheld name, then `seedPigparse` a DIFFERENT unheld name at the same id X and assert that
    catalog row is excluded.
  </action>
  <verify>
    <automated>cd internal/backendsrv && go test ./store/ -run TestDistinctEnrichmentRefs -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'func (s \*Store) DistinctEnrichmentRefs' internal/backendsrv/store/itemids.go` returns exactly one match.
    - `grep -c 'NOT IN (SELECT item_id FROM item_master)' internal/backendsrv/store/itemids.go` returns at least 1 (the EQ-id-collision exclusion is present).
    - `grep -c 'NOT IN (SELECT norm FROM held_names)' internal/backendsrv/store/itemids.go` returns at least 1 (the held-name exclusion is present).
    - `grep -n 'func TestDistinctEnrichmentRefs' internal/backendsrv/store/itemids_test.go` returns exactly one match.
    - `cd internal/backendsrv && go test ./store/ -run TestDistinctEnrichmentRefs` exits 0.
    - The job file authors NO inline union SQL: `grep -c 'pigparse_price' internal/backendsrv/enrich/jobs/wiki.go` returns 0.
  </acceptance_criteria>
  <done>
    `DistinctEnrichmentRefs` exists in store/itemids.go returning held ∪ catalog-only refs
    deduped by normalized name with BOTH exclusions; `TestDistinctEnrichmentRefs` covers the
    held-dedup, unheld-catalog, held-wins, id-collision-exclusion, and one-ref-per-name cases
    and passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap runWikiItems input to DistinctEnrichmentRefs + emit the D-03 coverage slog line + widen the job test</name>
  <read_first>
    - internal/backendsrv/enrich/jobs/wiki.go:128-291 (the file being modified — `runWikiItems` is the loop, `upsertItemAndQuests` is the per-item body; the freshness short-circuit at :227-245 already compares icon_id; the existing summary slog at :149-153 is the analog for the D-03 line)
    - internal/backendsrv/enrich/jobs/wiki_test.go:1-238 (the test file being modified — `seedAllItemRefs`, `newWikiFixtureServer`, `serverFetcher`, `setWikiSleepNoop`, `seedInvFor`, and especially `TestRunWiki_BackfillsStaleIcon` at :190-238 whose `SELECT icon_id FROM item_master WHERE item_id=?` read-back is the icon-assert pattern)
    - internal/backendsrv/store/itemids.go (the new `DistinctEnrichmentRefs` from Task 1 — the loop input)
    - .planning/phases/38-catalog-wide-enrichment-icon-coverage/38-PATTERNS.md (the "runWikiItems input swap + D-03 coverage slog" section — the swap, the resilience to inherit unchanged, the slog field shape, and the wiki_test additions)
    - .planning/phases/38-catalog-wide-enrichment-icon-coverage/38-RESEARCH.md (§"D-03 slog Summary Line" + §"Icon-Backfill Mechanics" — what to count, why no BackfillItemIcon boot pass, the bounded-residue requirement)
  </read_first>
  <behavior>
    Test additions to wiki_test.go (mirroring TestRunWiki_PopulatesAllTables / TestRunWiki_BackfillsStaleIcon):
    - TestRunWiki_EnrichesUnheldCatalogItem: seed an UNHELD pigparse_price catalog item whose name matches an existing wiki fixture that carries a lucy_img_ID (e.g. "Cloak of Flames", id arbitrary in the PigParse namespace — NOT held by any character), run RunWiki, then assert (a) an item_master row exists for that item (looked up by name, since the keyed id is the PigParse id), and (b) its icon_id > 0 (the ENRICH-15 win condition — an unheld item now resolves a real icon).
    - The same test (or a sibling) seeds a junk/no-page catalog name (no wiki fixture) and asserts the run still completes (job status "ok") and does NOT abort — the junk name lands in the icon-less residue (Pitfall 3).
    - Existing TestRunWiki_PopulatesAllTables / _SHA1ShortCircuit / _BackfillsStaleIcon / _BackfillsStaleFlags MUST still pass unchanged (the held path is byte-for-byte preserved).
  </behavior>
  <action>
    PART A — the input swap (the ONE behavioral change). In `runWikiItems`
    (internal/backendsrv/enrich/jobs/wiki.go:161), change:
    ```go
    refs, rerr := s.DistinctInventoryItemIDs(ctx)
    ```
    to:
    ```go
    refs, rerr := s.DistinctEnrichmentRefs(ctx)
    ```
    The ENTIRE rest of the loop body (the 1s `wikiSleepFn` courtesy sleep, `fetchWikiPage`,
    the ETag 304 → `unchanged++; continue`, the `ParseItempage` `!ok` → `failed++; continue`,
    `upsertItemAndQuests`, `SetETag`) is UNCHANGED. Do NOT touch `upsertItemAndQuests` or the
    freshness short-circuit — the icon already participates in `GetItemMasterFreshnessTx`'s
    comparison, so widening the ref set is the entire icon-backfill mechanism (ENRICH-15). Do
    NOT add a `BackfillItemIcon` boot pass (an unheld item has no local statsblock to re-parse —
    the crawl is its only icon source; contrast Phase 37's `BackfillItemFlags`). Do NOT touch
    the gear-tier pass (it is a deliberate unconditional full-replace).

    PART B — the D-03 coverage slog line (ENRICH-15 maintainer diagnostic). At the END of
    `runWikiItems`, immediately BEFORE its `return written, unchanged, failed, nil`, emit ONE
    structured `slog.Info` line summarizing coverage over the iterated union. Compute the counts
    cheaply with a single post-loop store read over the union names (or accumulate during the
    loop) — minimum fields per D-03:
    - `total`     — the union size iterated (`len(refs)`).
    - `enriched`  — items that have an `item_master` row after the pass.
    - `icon_covered` — items whose `item_master.icon_id > 0`.
    - `icon_less` — items whose `item_master.icon_id` is 0/NULL OR that have no `item_master`
      row at all (the colored-tile residue + the un-enriched junk names).
    - `residue_sample` — a BOUNDED slice of the icon-less item NAMES (cap at 50 — write a tiny
      `boundedSample(names []string, max int) []string` helper, or inline the cap). This keeps a
      hundreds-long residue from flooding the log.
    Field/shape (refine names to match the file):
    ```go
    slog.Info(wikiJobName+": items coverage",
        "total", total, "enriched", enriched,
        "icon_covered", iconCovered, "icon_less", iconLess,
        "residue_sample", boundedSample(residueNames, 50))
    ```
    SECURITY (V7 — STRIDE T-38-03): log COUNTS, IDs, and PUBLIC wiki page NAMES only. NEVER log
    `statsblock` or `wikitext` bodies. Item names are public wiki page titles — acceptable.
    Bound the residue slice (resource-exhaustion / log-DoS guard, T-38-04).

    To compute `enriched` / `icon_covered` / `icon_less` / `residue_sample` from the store
    (keep ZERO inline SQL in the job per 11-05), add a small read helper to
    internal/backendsrv/store/itemids.go (next to `DistinctEnrichmentRefs`) — e.g.
    `IconCoverageForNames(ctx, names []string) (iconCovered, iconLess int, iconLessNames []string, err error)`
    OR a simpler `ItemMasterIconCount(ctx) (covered, zeroOrNull int, err error)` joined to the
    union — whichever the executor finds cleanest while satisfying the D-03 minimum fields. If a
    names-IN query is awkward (large IN-list), prefer accumulating `iconCovered`/`iconLess` +
    `residueNames` INSIDE the existing loop from each item's parsed `item.IconID` (0 = icon-less)
    and the `fetchSkip`/`!ok`/parse-fail branches (those names are icon-less residue too) — this
    needs NO new store read at all and is the recommended approach. Pick the loop-accumulation
    path unless it materially complicates the loop. Either way the job authors no new inline SQL
    beyond what already exists; any new SELECT lives in store/.

    PART C — the test. Add `TestRunWiki_EnrichesUnheldCatalogItem` to wiki_test.go per the
    <behavior> block. Reuse `setWikiSleepNoop`, `newWikiFixtureServer`, `serverFetcher`,
    `seedOwnerChar`. Seed a HELD item (so the held arm is non-empty) via `seedInvFor`, then seed
    an UNHELD `pigparse_price` catalog row whose `name` matches a fixture with a lucy_img_ID
    (e.g. "Cloak of Flames") via a raw `db.Exec` INSERT into `pigparse_price` (the jobs package
    cannot call the unexported `store.seedPigparse`; insert the same columns
    `item_id, name, current_avg, blue_volume, last_seen, direction, t30, a30, last_refreshed`).
    Use a PigParse `item_id` that does NOT collide with any held id and is NOT already in
    `item_master`. Run `RunWiki`; assert the catalog item now has an `item_master` row (look it
    up by `lower(trim(name)) = lower(trim('Cloak of Flames'))` since its keyed id is the PigParse
    id) with `icon_id > 0`. Also seed a junk catalog name with no fixture and assert the job
    still reaches status "ok" (read-back the existing `assertJobStatus(t, db, "wiki_weekly", "ok")`).
  </action>
  <verify>
    <automated>cd internal/backendsrv && go test ./enrich/jobs/ -run TestRunWiki -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DistinctEnrichmentRefs' internal/backendsrv/enrich/jobs/wiki.go` returns at least 1 (the loop input was swapped).
    - `grep -c 'DistinctInventoryItemIDs' internal/backendsrv/enrich/jobs/wiki.go` returns 0 (the old held-only gate is no longer referenced in the job).
    - `grep -v '^//' internal/backendsrv/enrich/jobs/wiki.go | grep -c 'items coverage'` returns at least 1 (the D-03 slog summary line is present in code, not just a comment).
    - `grep -c 'BackfillItemIcon' internal/backendsrv/enrich/jobs/wiki.go internal/backendsrv/store/*.go` returns 0 (NO icon boot-pass was added — RESEARCH anti-pattern).
    - No new migration file was added: `ls internal/backendsrv/migrations/ | grep -c '00017'` returns 0.
    - `grep -n 'func TestRunWiki_EnrichesUnheldCatalogItem' internal/backendsrv/enrich/jobs/wiki_test.go` returns exactly one match.
    - `cd internal/backendsrv && go test ./enrich/jobs/ -run TestRunWiki` exits 0 (the new test AND all existing TestRunWiki_* still pass).
  </acceptance_criteria>
  <done>
    `runWikiItems` iterates `DistinctEnrichmentRefs` (widened to the full catalog), emits one
    bounded D-03 coverage slog line per pass, adds NO icon boot pass and NO migration; the new
    `TestRunWiki_EnrichesUnheldCatalogItem` proves an unheld catalog item gets enriched with a
    non-zero icon and the run survives a junk catalog name; all prior TestRunWiki_* pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| P1999 MediaWiki API → backend parser | Untrusted wiki text (item names, statsblock, flags, lucy_img_ID) crosses into the enrichment pipeline. Already-existing boundary; this phase only widens the SET of pages crossing it. |
| `pigparse_price` table → the union read | The catalog names/ids that seed the widened crawl. PigParse is an auction feed, not a curated item list (junk/spell names possible). |
| backend `item_master` writer ↔ the two id namespaces (EQ-inventory vs PigParse) | The data-integrity boundary at the heart of D-04: a PigParse id reused as an `item_master` PK could collide with a real EQ id. |
| backend job → `slog` (VPS logs) | The D-03 coverage line writes parsed values to logs the maintainer greps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `DistinctEnrichmentRefs` SQL (`store/itemids.go`) | mitigate | Fixed, parameterless projection — the union SELECT interpolates NO untrusted input (mirrors `DistinctInventoryItemIDs`; the only literals are column names). No SQL-injection surface. The downstream upsert binds the parsed name/flags through `?` placeholders only (`store/enrich.go:234-247`, unchanged). |
| T-38-02 | Tampering | `item_master` write via the catalog arm (the EQ-id collision hazard) | mitigate | **The load-bearing mitigation:** the catalog arm excludes `item_id NOT IN (SELECT item_id FROM item_master)` AND `lower(trim(name)) NOT IN held_names`, so a PigParse id equal to a held EQ id never reaches `ON CONFLICT(item_id) DO UPDATE` and cannot overwrite the wrong row. Verified by Task-1 Case-C test + the pre-execution prod probe sizing the excluded set (expected ≈0). |
| T-38-03 | Information disclosure | D-03 coverage `slog` line | mitigate | Logs COUNTS, IDs, and PUBLIC wiki page NAMES only — NEVER `statsblock`/`wikitext` bodies (V7; the package convention, `store/enrich.go:22-23`). Item names are public wiki page titles (RESEARCH A4). |
| T-38-04 | Denial of service (self) | the residue sample in the D-03 line | mitigate | The residue name slice is BOUNDED (cap 50) so a hundreds-long icon-less residue never floods the log. |
| T-38-05 | Denial of service (third party — the wiki) | the widened ~4,341-page crawl | mitigate | The existing 1s ctx-aware courtesy sleep (`wikiSleepFn`) runs BEFORE every fetch unchanged on the widened set; ETag 304s keep steady-state cheap. The crawl is capless but NEVER faster than 1 req/s (RESEARCH DoS row). No change to the courtesy mechanism. |
| T-38-06 | Tampering | untrusted `lucy_img_ID` → `Item_<int>.png` URL | accept (already-mitigated) | `parseIconID` coerces the wiki value to a non-negative int (sentinel 0) before it reaches any URL; no untrusted string flows into the image path (`enrich/wikiitem.go:584-601`, unchanged). No new surface this phase. |
</threat_model>

<verification>
Phase-level checks (run from `internal/backendsrv/`):

1. **Build + vet clean:** `go build ./...` and `go vet ./...` both exit 0.
2. **Full backend suite green:** `go test ./...` exits 0 — the store + jobs packages plus every
   held-path reader (Phases 31/32/37 tests) still pass (Option A's zero-blast-radius claim:
   held readers join `item_master` by EQ id exactly as before; the new catalog rows are invisible
   to them).
3. **The union read is correct:** `go test ./store/ -run TestDistinctEnrichmentRefs` covers
   held-dedup, unheld-catalog, held-wins, id-collision-exclusion, and one-ref-per-name.
4. **The widened crawl enriches + icons an unheld item:**
   `go test ./enrich/jobs/ -run TestRunWiki` (incl. `TestRunWiki_EnrichesUnheldCatalogItem`).
5. **No migration, no boot pass, no watcher gate:** `ls internal/backendsrv/migrations/` shows
   `00016` as the last migration (NO `00017`); no `BackfillItemIcon` was added; no
   `WatcherMaxSchemaVersion` change (that gate no longer exists post-v2.0). No `v*` tag.
6. **D-03 line present in code:** the `"items coverage"` slog line is emitted at the end of
   `runWikiItems` (greppable, not commented out).
</verification>

<success_criteria>
Phase 38 is complete when:
- [ ] ENRICH-14: the weekly wiki items pass iterates the full PigParse Blue catalog (held ∪
      catalog by normalized name) via `store.DistinctEnrichmentRefs`, politefetch-paced at the
      existing 1 req/s — NOT only held items.
- [ ] ENRICH-15: an unheld catalog item whose wiki page provides a `lucy_img_ID` gets an
      `item_master` row with a non-zero `icon_id` after a crawl (icon backfill falls out of the
      widened set + the existing freshness comparison — no new icon code).
- [ ] ENRICH-15: a per-run structured `slog` "items coverage" line reports total / enriched /
      icon-covered / icon-less + a bounded residue name sample (D-03) — the maintainer can see
      which items are still icon-less by grepping the VPS logs.
- [ ] D-04 honored: catalog-only rows land in `item_master` keyed by PigParse id ONLY for
      names with no held EQ-id row, with the EQ-id-collision exclusion — held readers (P31/32/37)
      are byte-for-byte unaffected; NO new table, NO migration (00016 stays last).
- [ ] D-01/D-01a/D-02 honored: the crawl runs automatically inside the existing weekly job over
      `pigparse_price` only (no `wiki_gear_tier` union), no new admin trigger, no execution-cap
      bookkeeping.
- [ ] `go build ./...`, `go vet ./...`, and `go test ./...` (from `internal/backendsrv/`) all green.
- [ ] Watcher untouched; no `v*` tag; backend-only.
- [ ] The D-04 prod collision probe was run (or deferred with the exclusion applied as the safe
      default) and its outcome recorded in the SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/38-catalog-wide-enrichment-icon-coverage/38-01-SUMMARY.md`
(use the summary template). Record:
- The commits (store method + test, then the job swap + slog + test).
- The D-04 prod collision-probe result (the `colliding` count) or that it was deferred.
- That NO migration was added (00016 stays the last), NO `BackfillItemIcon` boot pass, watcher
  untouched, no `v*` tag.
- A note that this phase's enrichment rides the NEXT backend deploy (which also carries the
  not-yet-deployed Phase 37 migration 00016 / schema bump) — the first prod weekly wiki run
  after deploy does the ~72-min seed crawl.
- The downstream contract for Phase 39 SEARCH-06: held + catalog reconcile through the existing
  normalized-name dedup (`pp_rep` / the item rollup), one row per item.
</output>
