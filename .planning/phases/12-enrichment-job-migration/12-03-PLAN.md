---
phase: 12-enrichment-job-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/backendsrv/buildinfo/buildinfo.go
  - internal/backendsrv/enrich/politefetch/politefetch.go
  - internal/backendsrv/enrich/politefetch/politefetch_test.go
autonomous: true
requirements: [ENRICH-10, ENRICH-11]
user_setup: []

must_haves:
  truths:
    - "Every outbound request carries an identifying, contactable User-Agent: SquireBot/<ver> (+github url)"
    - "If-None-Match + If-Modified-Since are sent when cached values exist, and a 304 short-circuits to FromCache=true with an empty body (no re-parse, no re-write)"
    - "429/503/504 retry with exponential backoff [2s,4s,8s,16s,32s], honoring an integer Retry-After header (clamped 0-600s); a non-retriable status surfaces immediately with no sleep"
    - "The response body is bounded by io.LimitReader (~16MB) so a runaway response can't OOM the VPS"
    - "TLS verification is on (Go default) — InsecureSkipVerify is never set"
  artifacts:
    - path: "internal/backendsrv/enrich/politefetch/politefetch.go"
      provides: "Fetch(ctx, url, opts) FetchResult with all 12 politeness controls; a Fetcher seam"
      exports: ["Fetch", "FetchResult"]
    - path: "internal/backendsrv/buildinfo/buildinfo.go"
      provides: "var Version (settable via -ldflags -X) + UserAgent() helper"
      exports: ["Version", "UserAgent"]
  key_links:
    - from: "internal/backendsrv/enrich/politefetch/politefetch.go"
      to: "io.LimitReader(resp.Body, ~16MB)"
      via: "bounded read before io.ReadAll"
      pattern: "io\\.LimitReader"
    - from: "internal/backendsrv/enrich/politefetch/politefetch.go"
      to: "ctx-aware retry/backoff sleep"
      via: "time.NewTimer + select ctx.Done()/timer.C (NOT bare time.Sleep)"
      pattern: "time\\.NewTimer"
---

<objective>
Port `politeFetch.ts` verbatim to a Go `net/http` client at `internal/backendsrv/enrich/politefetch/`, carrying over EVERY politeness control (the controls are an explicit success criterion and good-external-citizen behavior toward the community-run PigParse + P1999 wiki services): identifying User-Agent, `If-None-Match`/`If-Modified-Since` → 304 short-circuit, exponential backoff `[2s,4s,8s,16s,32s]` honoring `Retry-After` on 429/503/504, plus the two REQUIRED Go-side additions — an `io.LimitReader` body cap (OOM guard) and a ctx-aware sleep seam (clean shutdown + fast tests). Also add a backend `var Version` (mirroring the watcher's `main.Version`) for the User-Agent.

Purpose: This is the reusable polite-client policy code (its own package with its own httptest surface, exactly as `politeFetch.ts` had its own test file). The jobs (Plan 04) compose it. It depends on nothing else in this phase (the ETag state it reads/writes lives in Plan 01's `etag_cache`, but the CLIENT takes etag/lastMod as parameters and returns the new ones — the DB read/write is the job's responsibility), so it runs in parallel with Plans 01 and 02.
Output: `buildinfo/buildinfo.go` (Version + UserAgent) + `enrich/politefetch/politefetch.go` (the client + Fetcher seam) + its httptest-driven test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-enrichment-job-migration/12-CONTEXT.md
@.planning/phases/12-enrichment-job-migration/12-RESEARCH.md
@.planning/phases/12-enrichment-job-migration/12-PATTERNS.md

<interfaces>
<!-- The watcher's net/http client analog + the bounded-read + ctx-sleep idioms. The TS source is the port spec (read it in full). -->

net/http request + UA + Timeout + status guard analog — internal/update/check.go (lines ~125-138): http.NewRequestWithContext + req.Header.Set("User-Agent", …) + &http.Client{Timeout:…} + resp.Body.Close() + a StatusCode guard.

ctx-aware sleep seam to mirror — internal/update/check.go:53-62 (realCheckSleep): time.NewTimer + select{ ctx.Done() → ctx.Err(); t.C → nil }. A package-level `var sleepFn = realSleep` seam lets tests inject a fast/no-op sleep. NEVER bare time.Sleep in a path that should unwind on SIGTERM.

Bounded-read discipline to mirror — internal/backendsrv/ingest/handler.go:50,79: `const maxBodyBytes = 1<<20` + `http.MaxBytesReader(w, r.Body, maxBodyBytes)`. The OUTBOUND equivalent (no ResponseWriter) is `io.ReadAll(io.LimitReader(resp.Body, maxResponseBytes))` with maxResponseBytes ~16MB (PigParse fixture is 1.27MB; wiki pages <100KB).

Watcher Version precedent — cmd/squirebot/build_constants.go has `var Version = "0.1.0-dev"` set via `-ldflags "-X main.Version=…"`. The backend has none yet; add one (D-11).

The FetchResult return shape (RESEARCH §"Return shape"):
```go
type FetchResult struct {
    OK           bool
    Status       int
    Body         []byte   // empty on 304
    ETag         string
    LastModified string
    FromCache    bool     // true on 304
    RetriesUsed  int
    Err          error    // non-nil when !OK
}
```
1-SECOND INTER-REQUEST WIKI SLEEP STAYS OUT OF THIS CLIENT (politeFetch.ts comment is explicit) — it lives in the wiki JOB (Plan 04) between page fetches. PigParse is a single request → no inter-request sleep.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildinfo.go — backend Version + UserAgent</name>
  <read_first>
  - cmd/squirebot/build_constants.go (the watcher's `var Version` + `-ldflags -X` precedent to mirror, ~line 29)
  - cmd/squirebot-server/main.go (the backend main that will eventually be built with `-ldflags -X`; this plan does NOT need to wire the ldflags into the build — Plan 05 / deploy owns that — just provide the var + helper)
  - 12-CONTEXT.md D-11 (add a backend Version for the politeFetch UA; hardcoded `SquireBot/dev (+https://github.com/boejowen/SquireBot)` fallback acceptable)
  - 12-RESEARCH.md §"politeFetch Go Port Spec" §"Version source for the User-Agent"
  - TS source: apps-script/src/lib/politeFetch.ts (DEFAULT_USER_AGENT shape `SquireBot/${VERSION} (+https://github.com/boejowen/SquireBot)`)
  - Go analog: cmd/squirebot/build_constants.go
  </read_first>
  <action>
  Create package `internal/backendsrv/buildinfo` at `internal/backendsrv/buildinfo/buildinfo.go`:
  - `var Version = "dev"` — a package-level string settable at build time via `-ldflags "-X github.com/boejowen/SquireBot/internal/backendsrv/buildinfo.Version=v2.0.0"`. Default `"dev"` so an un-stamped build still produces a valid identifying UA.
  - `func UserAgent() string { return "SquireBot/" + Version + " (+https://github.com/boejowen/SquireBot)" }` — the identifying, contactable User-Agent (the GitHub URL satisfies the "contactable" requirement). This mirrors the TS DEFAULT_USER_AGENT exactly.

  A separate `internal/backendsrv/buildinfo` package (rather than a var in `package main`) is chosen so the politefetch client — which is NOT in `package main` — can import the Version without an import cycle and without main depending on politefetch's internals. (Wiring `cmd/squirebot-server` to set this var via ldflags is deferred to the build/deploy step; the hardcoded "dev" fallback is acceptable for this phase per D-11.)

  Add `internal/backendsrv/buildinfo/buildinfo_test.go` with `TestUserAgent_Identifying`: assert `UserAgent()` contains `"SquireBot/"` and `"github.com/boejowen/SquireBot"` (so the UA is always identifying + contactable regardless of the Version value).
  </action>
  <verify>
    <automated>go test ./internal/backendsrv/buildinfo/... -v</automated>
  </verify>
  <acceptance_criteria>
  - `internal/backendsrv/buildinfo/buildinfo.go` exists, package `buildinfo`, exporting `var Version` and `func UserAgent() string`
  - `grep "SquireBot/" internal/backendsrv/buildinfo/buildinfo.go` and `grep "github.com/boejowen/SquireBot" internal/backendsrv/buildinfo/buildinfo.go` both match (UA is identifying + contactable)
  - `TestUserAgent_Identifying` asserts both substrings are present in `UserAgent()`
  - `go test ./internal/backendsrv/buildinfo/...` exits 0
  </acceptance_criteria>
  <done>The backend has a Version var (ldflags-settable, "dev" fallback) and a UserAgent() helper producing the exact identifying UA the polite client sends.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: politefetch.go — the polite net/http client (all 12 controls) + httptest tests</name>
  <read_first>
  - apps-script/src/lib/politeFetch.ts (TS source — politeFetch, FetchResult union, RETRY_DELAYS_MS=[2000,4000,8000,16000,32000], RETRY_STATUSES={429,503,504}, parseRetryAfterMs 0-600 clamp, the 304 short-circuit, the 200-captures-ETag path, validateHttpsCertificates:true, the comment that the 1s sleep is NOT in the client)
  - apps-script/src/__tests__/politeFetch.test.ts (the control-by-control test contract to transliterate: 200, 304, 429-with-Retry-After, 503, non-retriable 404, network-error retry)
  - internal/update/check.go (Go analog — net/http request+UA+Timeout+status guard lines ~125-138; the realCheckSleep ctx-aware seam lines 53-62; the package-level `var checkSleepFn` test-seam pattern)
  - internal/backendsrv/ingest/handler.go lines 45-50,78-79 (the MaxBytesReader bounded-read discipline this OUTBOUND client mirrors via io.LimitReader)
  - internal/backendsrv/buildinfo/buildinfo.go (Task 1 — UserAgent())
  - 12-RESEARCH.md §"politeFetch Go Port Spec" (the full 12-control table + the Return shape + §"Body-size hardening" + Pitfall 5 + Pitfall 6) + §"Code Examples" (the retry skeleton)
  - 12-PATTERNS.md §"enrich/politefetch/politefetch.go" (the port mapping + the REQUIRED Go-vs-TS deltas: io.LimitReader, TLS-on, 1s-sleep-stays-out)
  - Go analog: internal/update/check.go
  </read_first>
  <action>
  Create `internal/backendsrv/enrich/politefetch/politefetch.go` (package `politefetch`). Port all 12 controls from RESEARCH §"politeFetch Go Port Spec":

  - `type FetchResult struct { OK bool; Status int; Body []byte; ETag string; LastModified string; FromCache bool; RetriesUsed int; Err error }` (exact shape above).
  - `type Options struct { ETag string; LastModified string }` — the conditional-request inputs (read from etag_cache by the job and passed in).
  - `type Fetcher func(ctx context.Context, url string, opts Options) FetchResult` — the seam the jobs depend on (so jobs can be tested with a fake Fetcher).
  - `func Fetch(ctx context.Context, url string, opts Options) FetchResult` — the production Fetcher. Implementation:
    1. Build `http.NewRequestWithContext(ctx, GET, url, nil)`. Set `User-Agent` = `buildinfo.UserAgent()`. If `opts.ETag != ""` set `If-None-Match`; if `opts.LastModified != ""` set `If-Modified-Since` (the ADD beyond the TS — SC-3 lists If-Modified-Since; pulling both maximizes 304 hits).
    2. Use a package-level `var httpClient = &http.Client{ Timeout: 30*time.Second }` (TLS verification ON by Go default — do NOT set InsecureSkipVerify; followRedirects is the default 10-cap). Make it a `var` so a test could swap it if needed, but the httptest server is the primary seam.
    3. Retry loop: `retryDelays := []time.Duration{2*time.Second,4*time.Second,8*time.Second,16*time.Second,32*time.Second}`; `for attempt := 0; attempt <= len(retryDelays); attempt++`. On `client.Do` transport error → if `attempt < len(retryDelays)` sleep `retryDelays[attempt]` (via the ctx-aware seam) and continue, else return `FetchResult{OK:false, Err: ...}`. On response:
       - `200`: read the body with `io.ReadAll(io.LimitReader(resp.Body, maxResponseBytes))` (`const maxResponseBytes = 16 << 20`), capture `resp.Header.Get("ETag")` + `resp.Header.Get("Last-Modified")`, return `FetchResult{OK:true, Status:200, Body:..., ETag:..., LastModified:..., RetriesUsed:attempt}`.
       - `304`: return `FetchResult{OK:true, Status:304, Body:nil, FromCache:true, ETag:opts.ETag, LastModified:opts.LastModified, RetriesUsed:attempt}` (Pitfall 6: the job MUST NOT parse this empty body).
       - `429 || 503 || 504`: if `attempt >= len(retryDelays)` return an "exhausted" error; else `wait := retryDelays[attempt]`; parse `Retry-After` as integer seconds, clamp 0-600, and if `> 0` use it instead; sleep `wait` via the ctx-aware seam; continue. (Match the TS: integer delta-seconds only, NOT HTTP-date form.)
       - default (any other status): return `FetchResult{OK:false, Status:..., Err: ...}` IMMEDIATELY — no retry, no sleep (TS "non-retriable ${status}").
    Always `resp.Body.Close()` (defer inside the loop body or close before continue/return — be careful not to leak on retry).
  - `func parseRetryAfter(h string) time.Duration` — parse integer seconds; clamp 0-600; return 0 if absent/unparseable (so the caller falls back to the schedule delay).
  - A ctx-aware sleep seam mirroring check.go:53-62: `var sleepFn = realSleep` where `realSleep(ctx, d) error` uses `time.NewTimer`+`select ctx.Done()/t.C`. Tests override `sleepFn` to a no-op for fast retry tests.

  Do NOT add the 1-second inter-request sleep here (it lives in the wiki job — Plan 04). Do NOT add any response-body cache (CacheService is dropped, D-3).

  Create `internal/backendsrv/enrich/politefetch/politefetch_test.go` driving an `httptest.Server` (mirror update/check_test.go's httptest seam). Override `sleepFn` to a no-op so retries are instant. Cover every control:
  - `TestFetch_200_CapturesETag`: server returns 200 + body + `ETag: "abc"` + `Last-Modified: ...` → result OK, Body matches, ETag=="abc", LastModified set, RetriesUsed==0.
  - `TestFetch_SendsConditionalHeaders`: server asserts the request carried `If-None-Match: abc` and `If-Modified-Since` when opts set; respond 304 → result FromCache==true, Body empty, OK==true.
  - `TestFetch_304_ShortCircuits`: 304 → FromCache true, Body nil.
  - `TestFetch_RetriesOn503ThenSucceeds`: server returns 503 twice then 200 → OK==true, RetriesUsed==2.
  - `TestFetch_RetryAfterHonored`: server returns 429 with `Retry-After: 1` once then 200; with sleepFn capturing the requested duration, assert the wait passed to sleepFn was 1*time.Second (not retryDelays[0]==2s). (Clamp test: a 700 Retry-After clamps to 600.)
  - `TestFetch_NonRetriable404_Immediate`: 404 → OK==false, Status==404, RetriesUsed==0, and sleepFn was NEVER called (assert via a call counter).
  - `TestFetch_SendsUserAgent`: server asserts the inbound `User-Agent` contains "SquireBot/" and the github URL.
  - `TestFetch_BoundedRead`: server returns a body larger than a small test cap (temporarily lower maxResponseBytes via a test hook OR assert the LimitReader is present by serving exactly maxResponseBytes+extra and asserting the read is truncated to the cap) — at minimum assert `io.LimitReader` is in the source path. (If a runtime test is impractical, the grep acceptance below covers presence.)
  </action>
  <verify>
    <automated>go test ./internal/backendsrv/enrich/politefetch/... -v</automated>
  </verify>
  <acceptance_criteria>
  - `internal/backendsrv/enrich/politefetch/politefetch.go` exports `Fetch`, `FetchResult`, `Options`, `Fetcher`
  - `grep -c "io.LimitReader" internal/backendsrv/enrich/politefetch/politefetch.go` returns ≥1 (bounded read present)
  - `grep "16 << 20" internal/backendsrv/enrich/politefetch/politefetch.go` matches (the ~16MB cap)
  - `grep -E "InsecureSkipVerify" internal/backendsrv/enrich/politefetch/politefetch.go` returns 0 (TLS verification left ON)
  - `grep "2\\s*\\*\\s*time.Second" internal/backendsrv/enrich/politefetch/politefetch.go` AND the slice up to `32 * time.Second` are present (the [2s,4s,8s,16s,32s] backoff)
  - `grep "time.NewTimer" internal/backendsrv/enrich/politefetch/politefetch.go` matches (ctx-aware sleep, not bare time.Sleep) — `grep -c "time.Sleep(" internal/backendsrv/enrich/politefetch/politefetch.go` returns 0
  - `grep -E "If-None-Match|If-Modified-Since" internal/backendsrv/enrich/politefetch/politefetch.go` matches both
  - `TestFetch_NonRetriable404_Immediate` asserts sleepFn was never invoked (call-count == 0)
  - `TestFetch_RetryAfterHonored` asserts the honored wait was 1s (the Retry-After value), not the 2s schedule default
  - `go test ./internal/backendsrv/enrich/politefetch/...` exits 0
  </acceptance_criteria>
  <done>The polite client ports all 12 controls; 304 short-circuits, 429/503/504 back off honoring Retry-After, non-retriable surfaces immediately, the body is bounded, TLS stays on, and the 1s inter-request sleep is correctly absent (it lives in the wiki job).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend process → community-run external services (outbound) | The client makes OUTBOUND GET requests to two hardcoded public URLs (PigParse getall, P1999 wiki api.php) and reads UNTRUSTED responses into memory. There is NO inbound surface (no ResponseWriter, no auth), NO secrets (public APIs), and the URL is NEVER user-supplied. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.03-01 | Denial of Service (self) | Fetch response read | mitigate | `io.ReadAll(io.LimitReader(resp.Body, 16<<20))` bounds the read so a runaway/oversized response can't OOM the small VPS (Pitfall 5; mirrors the ingest handler's MaxBytesReader). Verified by the `io.LimitReader` + `16 << 20` acceptance greps. |
| T-12.03-02 | Tampering / Information Disclosure | outbound transport | mitigate | TLS verification stays ON (Go's default http.Client; `InsecureSkipVerify` is never set — verified by the grep returning 0). Matches the TS `validateHttpsCertificates:true`. |
| T-12.03-03 | Denial of Service (hot-loop / bad citizen) | retry loop | mitigate | Bounded retry (≤5 attempts) with exponential backoff `[2s,4s,8s,16s,32s]` honoring `Retry-After`; a non-retriable status surfaces immediately (no sleep, no hammering). This IS the SC-3 politeness control toward the community servers. The ctx-aware sleep means SIGTERM unwinds the wait promptly. |
| T-12.03-04 | Spoofing (unidentified bad citizen) | request headers | mitigate | Every request carries an identifying, contactable User-Agent (`SquireBot/<ver> (+github)`) so the community-server operators can attribute + contact — required by SC-3 and verified by the UA grep + test. |
| T-12.03-05 | Server-Side Request Forgery | request URL | accept | The URL is NEVER user-supplied — the jobs pass hardcoded public API URL constants (PigParse getall, wiki api.php). No SSRF vector exists (no user controls the destination). Go's default 10-redirect cap bounds redirect following. Disposition: accept (no input path to exploit). |
| T-12.03-06 | Information Disclosure (secret leak) | outbound request | accept | There are NO secrets in this path — PigParse + wiki are public unauthenticated APIs. The client attaches no token/credential to any outbound request (only UA + conditional headers). Confirmed: no auth header is ever set. Disposition: accept (nothing to leak). |

Repudiation / Elevation of Privilege: N/A — no identity, no privilege boundary, no audit-relevant action (the client only reads public data). SQL injection: N/A — no SQL in this plan. None of the above is severity `high`; none trips `security_block_on: high`.
</threat_model>

<verification>
- `go test ./internal/backendsrv/enrich/politefetch/... ./internal/backendsrv/buildinfo/...` exits 0 (every control httptest-proven; retries instant via the sleepFn seam).
- `go build ./...` and `go vet ./...` clean.
- `grep -rn "InsecureSkipVerify" internal/backendsrv/` returns 0 (TLS verification never disabled anywhere).
- `grep -n "time.Sleep(" internal/backendsrv/enrich/politefetch/politefetch.go` returns 0 (no bare sleep — the ctx-aware timer seam is used so SIGTERM unwinds cleanly).
- The 1-second inter-request sleep is NOT in this client (it is the wiki job's responsibility in Plan 04) — `grep -n "1.*time.Second" politefetch.go` shows no inter-request sleep constant.
</verification>

<success_criteria>
- The polite client ports all 12 controls from RESEARCH; httptest proves 200/304/429-with-Retry-After/503/404 behavior.
- The body read is bounded (io.LimitReader ~16MB); TLS verification is on; retries are bounded with the exact [2s,4s,8s,16s,32s] backoff and Retry-After honored.
- The backend has a Version var + UserAgent() helper producing the identifying, contactable UA.
- The 1s inter-request wiki sleep is correctly absent from the client (lives in the wiki job).
</success_criteria>

<output>
After completion, create `.planning/phases/12-enrichment-job-migration/12-03-SUMMARY.md`
</output>
