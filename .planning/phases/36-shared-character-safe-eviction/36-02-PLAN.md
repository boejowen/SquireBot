---
phase: 36-shared-character-safe-eviction
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/eviction.ts
  - web/src/lib/components/EvictionForm.svelte
  - web/src/lib/__tests__/eviction.test.ts
requirements: [OWN-03]
must_haves:
  truths:
    - "web/src/lib/api.ts EvictionPreview interface gains preserved_shared_count: number, mirroring the backend's additive snake_case field field-for-field (the 36-01 preview contract)."
    - "An ALL-SHARED owner (preview characters:[] BUT preserved_shared_count>0) keeps the Evict button ENABLED — the officer can revoke their guild code (code-only revoke), with explicit '0 characters removed; {N} shared characters preserved; guild code will be revoked' framing."
    - "A genuine zero-live-chars owner (characters:[] AND preserved_shared_count==0) keeps the Evict button DISABLED (unchanged behaviour) — there is nothing to evict and no code-only revoke to perform."
    - "A normal owner with sole-owned chars (characters non-empty) keeps the existing enabled-with-cascade-list behaviour."
    - "The new gating + framing logic lives in a PURE node-testable helper in web/src/lib/eviction.ts (no DOM); EvictionForm.svelte is a thin renderer over it."
    - "The eviction flow is browser-smoked on PROD after deploy (node vitest is DOM-blind): an all-shared owner can be evicted (code-only), a normal owner shows the cascade list, a zero-char owner stays disabled."
  artifacts:
    - path: "web/src/lib/api.ts"
      provides: "EvictionPreview.preserved_shared_count field mirroring the backend"
      contains: "preserved_shared_count"
    - path: "web/src/lib/eviction.ts"
      provides: "pure evictability/framing helpers (canEvictPreview / evictPreviewSummary)"
      contains: "preserved_shared_count"
    - path: "web/src/lib/components/EvictionForm.svelte"
      provides: "all-shared-owner-evictable gating + code-only-revoke framing"
    - path: "web/src/lib/__tests__/eviction.test.ts"
      provides: "node tests for the new pure helpers + source-inspection of the form wiring"
  key_links:
    - from: "EvictionForm.svelte canEvict gate"
      to: "the pure helper in eviction.ts"
      via: "canEvictPreview reading preview.preserved_shared_count"
      pattern: "preserved_shared_count"
    - from: "previewEviction() reply"
      to: "EvictionPreview.preserved_shared_count"
      via: "the snake_case field mirrored in api.ts"
      pattern: "preserved_shared_count"
autonomous: false
---

<objective>
Close the plan-checker BLOCKER on the web side: after 36-01 narrows the eviction preview, an ALL-SHARED owner (a departing member whose every live char is shared) produces an empty removed-set (`characters:[]`). `EvictionForm.svelte` today disables the Evict button when `preview.characters.length === 0` (`cascadeEmpty`), so that owner could no longer be evicted → their guild code would never be revoked. The backend (36-01) already does the right thing (removedCount=0 but the code is still revoked) and now ships a preview that carries `preserved_shared_count` — this plan makes the web read it so the officer can still perform the code-only revoke.

The fix (per CONTEXT D-06): the Evict button is ENABLED when EITHER the removed-set is non-empty OR the owner still has live preserved-shared chars (`preserved_shared_count > 0`). In the all-shared case the form renders the explicit "0 characters removed; {N} shared characters preserved; guild code will be revoked" framing instead of the dead "No characters found" copy. The genuine "owner has zero live chars at all" case (`characters:[]` AND `preserved_shared_count == 0`) stays DISABLED — unchanged. The new gating + framing logic is extracted into PURE node-testable helpers in `web/src/lib/eviction.ts` (the repo's established discipline — node vitest is DOM-blind, so logic that can be unit-tested lives outside the .svelte; [[web-tests-node-only-blind-to-dom]]), and the form becomes a thin renderer over them.

The new backend field is mirrored into `web/src/lib/api.ts`'s `EvictionPreview` interface field-for-field (snake_case `preserved_shared_count: number`), per the CLAUDE.md "JSON contract mirrored field-for-field" convention. No new `{@html}` sink, theme tokens only, every user/Discord string stays plain `{}` (T-15-28).

Purpose: an all-shared departing member stays evictable through the UI, so their code is revoked and their watcher stops uploading — the originating incident's core requirement, made reachable.
Output: the `preserved_shared_count` contract mirror in api.ts, pure evictability/framing helpers in eviction.ts (with node tests), the `EvictionForm.svelte` gating + framing change, and a deploy-then-browser-smoke verification on prod. Web only; NO migration; watcher UNTOUCHED; deploy is the existing web atomic-swap (Claude drives via the ssh-agent workaround); NO `v*` tag.

This is the web half of Phase 36 (wave 2). It genuinely DEPENDS on the 36-01 backend preview contract (`preserved_shared_count`), so it is a separate plan in a later wave — the wave boundary is correct. 3 tasks (mirror+helpers+tests / form wiring / deploy+browser-smoke); within the context budget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-shared-character-safe-eviction/36-CONTEXT.md

# The 36-01 backend contract this plan consumes:
@.planning/phases/36-shared-character-safe-eviction/36-01-PLAN.md

# The web files this plan modifies + their pattern (read these BEFORE editing):
@web/src/lib/api.ts
@web/src/lib/eviction.ts
@web/src/lib/components/EvictionForm.svelte
@web/src/lib/__tests__/eviction.test.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. Use these directly — no exploration needed. -->

The 36-01 backend preview reply (webadmin/eviction.go EvictionPreviewHandler — what previewEviction() receives):
  {
    "owner_id":               <int>,
    "characters":             [<string>, …],   // the NARROWED remove-set (sole-owned live chars)
    "grace_until":            <int>,            // unix epoch SECONDS
    "preserved_shared_count": <int>             // NEW (36-01): count of live chars that SURVIVE because shared
  }
  // ALL-SHARED owner: characters:[] AND preserved_shared_count>0.
  // ZERO-live-chars owner: characters:[] AND preserved_shared_count==0.
  // NORMAL owner: characters non-empty (preserved_shared_count may be 0 or >0).

web/src/lib/api.ts EvictionPreview (api.ts:540) — the interface to extend field-for-field:
  export interface EvictionPreview {
    owner_id: number;
    characters: string[];
    grace_until: number;            // unix epoch SECONDS (CR-02 lesson)
    // ADD: preserved_shared_count: number;   // unix-int count, snake_case mirror of the Go field
  }

web/src/lib/eviction.ts (the PURE helpers home — node-testable, DOM-free):
  graceDate(epochSeconds: number): string                    // existing (CR-02)
  restoreResultMessage(res: RestoreResult, label: string)    // existing (WR-01/WR-02)
  // ADD pure helpers here: canEvictPreview(preview) + evictPreviewSummary(preview).

web/src/lib/components/EvictionForm.svelte — the officer form (Settings → Admin, officer-gated). Current gate (lines 68-73):
  let selectedOwner = $derived(owners.find((o) => String(o.owner_id) === selectedId) ?? null);
  let cascadeEmpty = $derived(!!preview && preview.characters.length === 0);
  let canEvict = $derived(!!selectedOwner && !!preview && !cascadeEmpty && !floorBlocked && !evicting);
  // The preview block (lines 235-253): renders "No characters found for this guildie." when cascadeEmpty,
  // else the "Characters affected (N):" list + the grace line + the CONSEQUENCE callout.
  // doEvict success copy (line 126): `Marked ${res.removed_count} character(s) as removed and revoked the guild code…`
  // ConfirmDialog body (line 277): `This marks ${preview?.characters.length ?? 0} character(s) as removed and revokes …'s guild code …`

Deploy (existing path — NO migration, web atomic-swap; Claude drives via the ssh-agent workaround):
  - Build: cd web && npm run build (vite build).
  - Web atomic-swap onto root@5.78.232.85 (the runbook §7 path; PowerShell ssh.exe/scp.exe; the §7.5 200.html guard is in place).
  - Backend binary swap is the 36-01 deliverable; 36-02 deploys ONLY the web build. Both must be live before the browser-smoke.
  - NO goose-run (no migration), NO `v*` tag, watcher untouched.
  - Browser-smoke requires OFFICER auth on PROD (localhost dev can't auth against prod — [[web-local-dev-cant-auth-against-prod]]).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror preserved_shared_count in api.ts + add the pure evictability/framing helpers in eviction.ts (with node tests)</name>
  <files>web/src/lib/api.ts, web/src/lib/eviction.ts, web/src/lib/__tests__/eviction.test.ts</files>
  <read_first>
    - web/src/lib/api.ts lines 539-545 (the EvictionPreview interface — extend it field-for-field) and lines 547-552 (EvictResult, for the snake_case + epoch-seconds-number convention to follow)
    - web/src/lib/eviction.ts (the WHOLE file — the pure-helper home + the CR-02 / WR-01/WR-02 doc style to match for the two new helpers)
    - web/src/lib/__tests__/eviction.test.ts (the WHOLE file — the node-only test shape: describe/it, the source-inspection idiom for the form, no DOM)
    - .planning/phases/36-shared-character-safe-eviction/36-01-PLAN.md <interfaces> (the exact backend reply shape this mirrors)
  </read_first>
  <behavior>
    - EvictionPreview gains preserved_shared_count: number — a snake_case mirror of the Go field; no other field changes.
    - canEvictPreview(preview) returns true iff the preview indicates there is SOMETHING to do: characters.length>0 (cascade) OR preserved_shared_count>0 (code-only revoke of an all-shared owner). It returns false ONLY for a genuine zero-live-chars owner (characters:[] AND preserved_shared_count==0). (It does NOT know about floorBlocked/evicting/selectedOwner — those stay in the .svelte; this helper is the preview-shape decision only.)
    - evictPreviewSummary(preview) returns a small discriminated shape the form renders, distinguishing three cases: (a) cascade (characters non-empty) → kind:'cascade' (the form renders the existing "Characters affected (N):" list); (b) all-shared (characters:[] AND preserved_shared_count>0) → kind:'code-only' + the message "0 characters removed; {N} shared character(s) preserved; guild code will be revoked."; (c) empty (characters:[] AND preserved_shared_count==0) → kind:'empty' + the existing "No characters found for this guildie." Keep the helper pure (no DOM), label-free (it only formats counts), returning a discriminated shape the form switches on.
  </behavior>
  <action>
    1. Edit `web/src/lib/api.ts` EvictionPreview: add `preserved_shared_count: number;` with a one-line comment mirroring the backend (snake_case, the count of live chars preserved because shared — drives the all-shared-owner Evict gate). Field-for-field with the 36-01 reply; do NOT rename or reorder the existing fields.

    2. Edit `web/src/lib/eviction.ts`: add two pure exported helpers (match the file's existing doc-comment style):
       ```ts
       /**
        * Decide whether an eviction preview has anything to act on — the Evict-button
        * gate's PREVIEW-shape half (D-06, the BLOCKER fix). True iff there is a cascade
        * (characters to remove) OR the owner still has live PRESERVED-SHARED chars (a
        * code-only revoke of an all-shared departing member). False ONLY for a genuine
        * zero-live-chars owner. The form ANDs this with !floorBlocked/!evicting/selectedOwner.
        */
       export function canEvictPreview(preview: EvictionPreview): boolean {
           return preview.characters.length > 0 || preview.preserved_shared_count > 0;
       }

       type EvictPreviewSummary =
           | { kind: 'cascade' }
           | { kind: 'code-only'; message: string }
           | { kind: 'empty'; message: string };

       /**
        * Classify an eviction preview into the three render cases (D-06). 'cascade' →
        * the form renders its "Characters affected (N)" list. 'code-only' (all-shared
        * owner: nothing removed but live shared chars remain) → the explicit "0
        * characters removed; {N} shared character(s) preserved; guild code will be
        * revoked" framing so the officer understands the code-only revoke. 'empty' (no
        * live chars at all) → the existing "No characters found" copy; the button stays
        * disabled. Pure / DOM-free / label-free (formats counts only) — node-testable.
        */
       export function evictPreviewSummary(preview: EvictionPreview): EvictPreviewSummary {
           if (preview.characters.length > 0) return { kind: 'cascade' };
           if (preview.preserved_shared_count > 0) {
               return {
                   kind: 'code-only',
                   message: `0 characters removed; ${preview.preserved_shared_count} shared character(s) preserved; guild code will be revoked.`
               };
           }
           return { kind: 'empty', message: 'No characters found for this guildie.' };
       }
       ```
       Import `EvictionPreview` from `$lib/api` at the top (the file already imports `RestoreResult` from there — extend that import to include `EvictionPreview`).

    3. Extend `web/src/lib/__tests__/eviction.test.ts` with a new `describe('eviction preview gating + framing (D-06)')`:
       - `canEvictPreview`: a cascade preview (characters:['Soletoon'], preserved_shared_count:0) → true; an all-shared preview (characters:[], preserved_shared_count:2) → true; a zero-live preview (characters:[], preserved_shared_count:0) → false.
       - `evictPreviewSummary`: cascade → {kind:'cascade'}; all-shared → kind:'code-only' AND message contains '0 characters removed' AND contains the count (e.g. '2') AND contains 'guild code will be revoked'; empty → kind:'empty' AND message contains 'No characters found'.
       (Construct EvictionPreview literals inline with owner_id/grace_until placeholders — these helpers only read characters + preserved_shared_count.)
  </action>
  <verify>
    <automated>cd web && npm run test:unit -- --run src/lib/__tests__/eviction.test.ts && npm run check 2>&1 | tail -20</automated>
  </verify>
  <done>EvictionPreview in api.ts has preserved_shared_count: number (snake_case mirror); canEvictPreview + evictPreviewSummary exist as pure exports in eviction.ts; the new node tests cover the cascade / all-shared / empty cases for both helpers; `npm run test:unit -- --run src/lib/__tests__/eviction.test.ts` green and `npm run check` (svelte-check/typecheck) clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire EvictionForm.svelte to the new helpers — all-shared owner stays evictable with code-only framing</name>
  <files>web/src/lib/components/EvictionForm.svelte, web/src/lib/__tests__/eviction.test.ts</files>
  <read_first>
    - web/src/lib/components/EvictionForm.svelte lines 25-36 (the $lib imports — add canEvictPreview/evictPreviewSummary), 68-73 (the cascadeEmpty/canEvict derivations to change), 113-137 (openConfirm/doEvict — the success copy), 233-281 (the preview render block + the ConfirmDialog body)
    - web/src/lib/eviction.ts (the helpers added in Task 1)
    - web/src/lib/__tests__/eviction.test.ts (the source-inspection describe block at the bottom — extend it to assert the form is wired to the new helpers)
  </read_first>
  <behavior>
    - The Evict button is ENABLED when a preview exists, the owner is selected, not floor-blocked, not mid-evict, AND canEvictPreview(preview) is true (cascade OR all-shared). It is DISABLED for a genuine zero-live-chars owner (the existing empty case).
    - The preview block renders by evictPreviewSummary(preview).kind: 'cascade' → the existing "Characters affected (N):" list + grace + CONSEQUENCE; 'code-only' → the new "0 characters removed; {N} shared character(s) preserved; guild code will be revoked." line + grace + CONSEQUENCE (the button stays enabled); 'empty' → the existing "No characters found for this guildie." (button disabled). Every interpolation stays plain {} (no {@html}).
    - The doEvict success copy already interpolates res.removed_count (so "Marked 0 character(s) as removed and revoked the guild code" is accurate for an all-shared owner — keep it, it is already count-driven). The ConfirmDialog body already says it "revokes …'s guild code right away", so it stays accurate at removed_count 0 — no misleading copy, leave it.
  </behavior>
  <action>
    Edit `web/src/lib/components/EvictionForm.svelte`:

    1. Import the new helpers: add `canEvictPreview, evictPreviewSummary` to the existing `import { graceDate, restoreResultMessage } from '$lib/eviction';` line.

    2. Replace the gate derivations (lines 69-73). Drop the standalone `cascadeEmpty` and gate on the helper:
       ```ts
       let selectedOwner = $derived(owners.find((o) => String(o.owner_id) === selectedId) ?? null);
       let previewSummary = $derived(preview ? evictPreviewSummary(preview) : null);
       // Evict is enabled when the preview has something to act on (cascade OR an
       // all-shared code-only revoke — D-06), and not floor-blocked / mid-evict.
       let canEvict = $derived(
           !!selectedOwner && !!preview && canEvictPreview(preview) && !floorBlocked && !evicting
       );
       ```

    3. Update the preview render block (lines 235-253) to switch on `previewSummary.kind` (the cascade branch keeps the existing list; the code-only + empty branches render `previewSummary.message`; the grace line + CONSEQUENCE callout stay for all kinds):
       ```svelte
       {:else if preview && previewSummary}
           <div class="preview">
               {#if previewSummary.kind === 'cascade'}
                   <p class="preview-heading">Characters affected ({preview.characters.length}):</p>
                   <ul class="char-list">
                       {#each preview.characters as c (c)}
                           <li class="char">{c}</li>
                       {/each}
                   </ul>
               {:else}
                   <p class="cascade-empty">{previewSummary.message}</p>
               {/if}
               <p class="grace">Grace expires: {graceDate(preview.grace_until)} (30 days from today).</p>
               <div class="consequence" role="note">
                   <TriangleAlert size={18} aria-hidden="true" class="consequence-icon" />
                   <span>{CONSEQUENCE}</span>
               </div>
           </div>
       ```
       (Both the 'code-only' and 'empty' branches render `previewSummary.message` via plain {} — the only difference between them is whether canEvict is true, which the button already reflects. Theme tokens only — reuse the existing `.cascade-empty` / `.preview` styles; add NO new color literals.)

    4. Leave doEvict's success copy as-is (it already interpolates `res.removed_count`). Leave the ConfirmDialog body as-is. Do NOT introduce any `{@html}` (T-15-28).

    5. Extend the source-inspection describe block in `web/src/lib/__tests__/eviction.test.ts`:
       - assert `EVICTION_FORM_SOURCE` contains `canEvictPreview` and `evictPreviewSummary` (the form is wired to the helpers, not re-deriving the gate inline).
       - assert it still contains NO `{@html` (the existing T-15-28 check stays green).
  </action>
  <verify>
    <automated>cd web && npm run check 2>&1 | tail -20 && npm run test:unit -- --run src/lib/__tests__/eviction.test.ts && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>EvictionForm imports + uses canEvictPreview/evictPreviewSummary; canEvict is true for cascade AND all-shared previews, false for the zero-live empty case; the preview block renders the code-only "0 characters removed; {N} shared character(s) preserved; guild code will be revoked" framing for an all-shared owner via plain {}; no {@html} added; the source-inspection tests assert the wiring; `npm run check` clean, the eviction unit tests green, and `npm run build` (vite) succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy (backend binary + web atomic-swap) and browser-smoke the eviction flow on prod</name>
  <what-built>
    The backend (36-01) narrowed the eviction cascade + added the preserved_shared_count preview field; this web plan (36-02) made an all-shared owner stay evictable with code-only-revoke framing. Claude will deploy BOTH (the 36-01 backend binary swap + the 36-02 web atomic-swap onto root@5.78.232.85 via the ssh-agent workaround — NO goose-run since there is no migration, NO `v*` tag, watcher untouched), then this checkpoint asks you to browser-smoke the eviction flow on https://squirebot.quest because node vitest is DOM-blind (it cannot prove the live officer UI behaves — [[web-tests-node-only-blind-to-dom]]). Officer auth on prod is required (localhost dev can't auth against prod — [[web-local-dev-cant-auth-against-prod]]).

    Claude completes the automated deploy FIRST (build → web atomic-swap → backend binary swap → R2 backup → proof a removed/protected route returns 401-not-404), then hands off the manual verification below.
  </what-built>
  <how-to-verify>
    On https://squirebot.quest, logged in as an OFFICER, open Settings → Admin → the Evict guildie form. Confirm:

    1. **Normal owner (sole-owned chars):** pick a guildie with at least one non-shared character. The preview shows "Characters affected (N):" with the char list, the grace line, and the consequence callout. The Evict button is ENABLED. (Do NOT actually evict a real guildie unless you intend to — verifying the preview + button state is enough; if you want a live commit, use a throwaway/test owner and restore them after.)

    2. **All-shared owner (the BLOCKER case — the load-bearing check):** pick a guildie whose every live character is shared (one that other guildies also upload — e.g. a known shared-login case). The preview shows "0 characters removed; {N} shared character(s) preserved; guild code will be revoked." and the Evict button is **ENABLED** (the fix — before this change it would have been disabled). If you commit the evict on a test all-shared owner: it succeeds, the success copy reads "Marked 0 character(s) as removed and revoked the guild code…", and the owner's guild code is revoked (their watcher stops uploading). The shared characters stay live (visible/un-removed in the rest of the app).

    3. **Empty owner (zero live chars):** if such an owner is pickable, the preview shows "No characters found for this guildie." and the Evict button stays DISABLED (unchanged).

    4. **No layout/theme regression:** the form renders cleanly under at least 2 themes (theme tokens only — no hard-coded colors), and there is no console error on the eviction page.

    Report PASS, or describe what you saw (which case misbehaved).
  </how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| previewEviction() reply → EvictionForm Evict-button gate | The web reads preserved_shared_count (a server-supplied int) to decide whether to enable a DESTRUCTIVE action (evict + code-revoke). The gate must enable EXACTLY the set the backend honors — never enable an evict the backend no-ops, never hide one it performs. |
| officer → the live eviction UI on prod | The form is officer-gated in Settings→Admin ({#if isOfficer}); the backend (webadmin) re-checks officer on every write (server-truth, T-30-05). The web gate is UX, not security — a non-officer cannot evict regardless of the button state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-W1 | Tampering / availability gap (the BLOCKER) | EvictionForm disabling Evict for an all-shared owner → their code never revoked | mitigate | canEvictPreview enables Evict when preserved_shared_count>0 even with characters:[] — the all-shared departing member stays evictable (code-only revoke). The framing tells the officer exactly what happens ("0 removed; {N} preserved; code revoked"). Backed by the honest preserved_shared_count from 36-01 (computed off the same predicate as the cascade). (Tests: canEvictPreview all-shared → true; browser-smoke case 2.) |
| T-36-W2 | Spoofing / over-enable | the web enabling an evict on a genuinely-empty owner | mitigate | canEvictPreview returns false for characters:[] AND preserved_shared_count==0 — a zero-live-chars owner stays disabled (nothing to remove, no code-only revoke). The three-case discrimination is a pure, node-tested helper, not inline boolean drift. (Test: canEvictPreview empty → false; browser-smoke case 3.) |
| T-36-W3 | Information disclosure / XSS | guildie labels + char names + the count in the new framing | mitigate | The new framing renders preserved_shared_count (a number) and uses plain Svelte {} interpolation everywhere — NO {@html} sink is added (the existing T-15-28 source-inspection test asserts the form has no {@html}). Char names + labels continue to render via auto-escaping {}. |
| T-36-W4 | Tampering / server-truth bypass | a non-officer manipulating the client to evict | accept (already mitigated) | The web gate is UX only; the backend webadmin re-checks officer status inside the write tx on every evict (T-30-05 / WR-04) and a 403 collapses the admin UI via authGuard. Enabling the button client-side cannot bypass the server gate. No change to that posture. |
</threat_model>

<verification>

- `cd web && npm run check` clean (svelte-check + typecheck — the new EvictionPreview field + the helper types resolve).
- `cd web && npm run test:unit -- --run src/lib/__tests__/eviction.test.ts` green (the new canEvictPreview/evictPreviewSummary cases + the existing CR-02/WR-01/WR-02 + source-inspection tests).
- `cd web && npm run test -- --run` (the full web suite) green — no regression.
- `cd web && npm run build` (vite) succeeds — the form compiles.
- Contract mirror present: `grep -c "preserved_shared_count" web/src/lib/api.ts` >= 1 (the snake_case field) AND `grep -c "preserved_shared_count" web/src/lib/eviction.ts` >= 1 (the helpers read it).
- No raw-HTML sink added: `grep -c "{@html" web/src/lib/components/EvictionForm.svelte` == 0 (T-15-28).
- Deploy: backend binary swap + web atomic-swap live on prod (NO goose-run — schema v15, no migration; NO `v*` tag; watcher untouched). A removed/protected route proof returns 401-not-404 (the deploy-correctness smoke).
- Browser-smoke (Task 3 checkpoint): the all-shared owner is evictable with code-only framing; the normal owner shows the cascade list; the empty owner stays disabled; no console error; clean under 2+ themes.

</verification>

<success_criteria>

1. The EvictionPreview contract mirrors the backend field-for-field: `preserved_shared_count: number` (snake_case) in api.ts. ✔ when `npm run check` is clean and `grep preserved_shared_count web/src/lib/api.ts` matches.
2. An all-shared owner stays EVICTABLE through the UI (the BLOCKER fix) — the Evict button is enabled with characters:[] when preserved_shared_count>0, rendering "0 characters removed; {N} shared character(s) preserved; guild code will be revoked." ✔ when canEvictPreview/evictPreviewSummary tests pass AND browser-smoke case 2 confirms it live.
3. A genuine zero-live-chars owner stays DISABLED (unchanged). ✔ when canEvictPreview empty→false passes AND browser-smoke case 3 confirms.
4. A normal owner with sole-owned chars keeps the existing enabled cascade-list behaviour. ✔ when browser-smoke case 1 confirms.
5. The gating + framing is a pure node-testable helper (no DOM); the form is a thin renderer; no {@html} added; theme tokens only. ✔ when the eviction unit tests + the no-{@html} grep + `npm run check`/`build` are green.
6. Deployed to prod via the existing web atomic-swap (+ the 36-01 backend binary swap); NO migration (schema v15); NO `v*` tag; watcher untouched. ✔ when the deploy proof + browser-smoke PASS.

**Milestone close-out:** With 36-01 (backend) this completes OWN-03 — the last open v2.5 requirement. After both land + the deploy + browser-smoke PASS, v2.5 "Ownership Cleanup" is feature-complete and ready for milestone audit/close. No watcher change → no `v*` tag for v2.5.
</success_criteria>

<output>
After completion, create `.planning/phases/36-shared-character-safe-eviction/36-02-SUMMARY.md` (use the summary template). Note: the web half of OWN-03 / the D-06 BLOCKER fix; the preserved_shared_count contract mirror in api.ts; the pure canEvictPreview/evictPreviewSummary helpers + their node tests; the EvictionForm gating + code-only framing change; the browser-smoke verdict (which of the 4 cases passed); that the deploy was backend-binary-swap + web-atomic-swap with NO migration (schema v15), NO `v*` tag, watcher untouched. With 36-01 this closes OWN-03 → v2.5 ready for milestone audit/close.
</output>
