---
phase: 13-watcher-re-target-onboarding
plan: 04
type: execute
wave: 3
depends_on: [13-03]
files_modified:
  - cmd/squirebot/console_windows.go
  - internal/update/manifest.go
  - internal/update/manifest_test.go
autonomous: true
requirements: [WATCH-09, WATCH-11]
user_setup: []

must_haves:
  truths:
    - "console_windows.go is gofmt-clean and the freeConsole() doc matches its implementation (999.20 + 999.21)"
    - "The watcher's auto-update version comparison is SemVer-pre-release-aware: a pre-release tag sorts below its final release so the P16 coordinated self-update flip is not surprised by a pre-release manifest (999.22)"
    - "The re-targeted watcher binary is materially smaller than the v1 binary and carries no Google client secret (the SC-2 confirmation)"
  artifacts:
    - path: "internal/update/manifest.go"
      provides: "SemVer-pre-release-aware IsNewer/parseVersion (999.22 — the watcher-side version-compare truth)"
      contains: "func IsNewer"
    - path: "cmd/squirebot/console_windows.go"
      provides: "gofmt-clean console detach with a doc matching freeConsole's BOOL contract"
      contains: "func freeConsole"
  key_links:
    - from: "internal/update/manifest.go"
      to: "the P16 coordinated self-update flip"
      via: "IsNewer ranks a final v2.x.0 above the running version and pre-releases below their final"
      pattern: "func IsNewer"
---

<objective>
The polish + ship-prep plan: land the three deferred watcher nits that ride along with the re-target, and confirm the deletion produced a materially-smaller, Google-secret-free binary (CONTEXT D-9; SC-2 confirmation). Runs in Wave 3 because the binary-size / no-secret confirmation requires Plan 03's deletion to have landed.

1. **999.20** — `cmd/squirebot/console_windows.go` is not `gofmt -l` clean (the `var ( kernel32 ... procFreeConsole ... )` block has misaligned spacing). Fix the formatting.
2. **999.21** — `freeConsole()`'s doc claims a behavior its implementation does not match (the doc-vs-impl mismatch around the BOOL/LastError handling that produces log noise). Reconcile the doc to the implementation so they agree and the benign no-console case stops logging a Warn.
3. **999.22** — `internal/update/manifest.go`'s `IsNewer`/`parseVersion` does a strict 3-part numeric compare with NO pre-release handling (`v2.0.0-rc1` fails `parseVersion`'s `len(parts)!=3` then "never newer"). LOAD-BEARING for the P16 coordinated self-update flip (CUTOVER-03): make the compare SemVer-pre-release-aware so a pre-release manifest ranks correctly. This is the watcher-side twin of the server-side `IsOlder` built in Plan 01 — deliberately separate copies (watcher and server must not import each other) that must behave identically; document the "one version-compare doctrine, per side" rule.

Purpose: De-risk the P16 coordinated flip (the updater must compare versions correctly including any pre-release safety tags) and clear the two console nits that have ridden the backlog since v1.0.2. Confirm the milestone's SC-2 "materially smaller, no Google secret" claim holds after the deletion.
Output: a gofmt-clean + doc-correct console_windows.go, a SemVer-aware manifest.go (+ tests), and a recorded binary-size + no-secret confirmation in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-watcher-re-target-onboarding/13-CONTEXT.md
@.planning/phases/13-watcher-re-target-onboarding/13-03-SUMMARY.md

<interfaces>
<!-- The two files this plan touches. Extracted from the codebase. -->

From cmd/squirebot/console_windows.go (999.20 gofmt + 999.21 doc — the current state):
```go
//go:build windows
var (
	kernel32         = windows.NewLazySystemDLL("kernel32.dll")
	procFreeConsole  = kernel32.NewProc("FreeConsole")   // misaligned: gofmt re-spaces this block
)
// freeConsole() doc currently says it "Returns nil if the process had no console attached"
// but the impl returns a non-nil error whenever ret==0 (which INCLUDES the no-console case on
// some Windows builds) and logs a Warn — that is the 999.21 doc-vs-impl mismatch + log noise.
func freeConsole() error {
	ret, _, err := procFreeConsole.Call()
	if ret == 0 { slog.Warn("FreeConsole failed", "err", err); return fmt.Errorf("FreeConsole: %w", err) }
	return nil
}
```

From internal/update/manifest.go (999.22 — the current strict 3-part compare):
```go
func IsNewer(current, manifest string) bool {
	cParts, ok := parseVersion(current); if !ok { return false }
	mParts, ok := parseVersion(manifest); if !ok { return false }
	for i := 0; i < 3; i++ { if mParts[i] > cParts[i] { return true }; if mParts[i] < cParts[i] { return false } }
	return false // equal
}
func parseVersion(v string) ([3]int, bool) {
	v = strings.TrimPrefix(v, "v"); parts := strings.Split(v, "."); if len(parts) != 3 { return [3]int{}, false }
	// strconv.Atoi each part; any failure → false
}
// IsNewer is consumed by internal/update/check.go (the 24h goroutine + manual check) to decide
// whether to download+stage the manifest's binary. The defensive "false on parse failure" must
// be preserved for a CORRUPT manifest; only the pre-release case changes.
```

The server-side twin (Plan 01 — internal/backendsrv/ingest/version.go::IsOlder): SemVer-aware, pre-release sorts below final. manifest.go's pre-release handling must agree in DIRECTION (a pre-release of the same core is older than the final). Do NOT import it — recreate the logic locally (client/server decoupling).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 999.20 gofmt + 999.21 freeConsole doc-vs-impl reconciliation (console_windows.go)</name>
  <read_first>
    - MODIFY: cmd/squirebot/console_windows.go (the var-block alignment + the freeConsole doc)
    - CONTRACT to honor: CONTEXT D-9 (999.20 + 999.21, both in this file); the file is `//go:build windows`, so `go build` of it only runs on Windows (the dev box) — gofmt is platform-independent. The freeConsole ORDERING + behavior (called after --quit/update.Apply, before logging.Setup; safe no-op when no console) must be preserved — only the doc/log-noise is reconciled.
  </read_first>
  <action>
**999.20 (gofmt):** Run `gofmt -w cmd/squirebot/console_windows.go` to re-space the `var ( kernel32 ... procFreeConsole ... )` block (the extra space before `=` on `procFreeConsole` is the offending misalignment). After this, `gofmt -l cmd/squirebot/console_windows.go` must print nothing.

**999.21 (doc-vs-impl + log noise):** The doc claims "Returns nil if the process had no console attached (safe to call unconditionally)", but the impl returns a non-nil error AND logs `slog.Warn("FreeConsole failed", ...)` whenever `ret==0` — which on a no-console process is the EXPECTED benign case, producing spurious Warn noise on every GUI/Explorer launch. Reconcile so doc and impl agree AND the benign no-console case is not logged as a failure:
- Tighten the impl to the doc: when `procFreeConsole.Call()` returns `ret==0`, treat it as the benign no-console case — log at `Debug` (not `Warn`) and return `nil` (matching the doc's "returns nil if no console attached / safe to call unconditionally"). FreeConsole's only failure mode that matters is "couldn't detach a console that WAS attached", which is non-fatal anyway (the watcher continues per the existing doc). So: drop the `fmt.Errorf` return on `ret==0`; do `slog.Debug("FreeConsole returned 0 (likely no console attached); continuing", "err", err)` then `return nil`. This removes the Warn noise AND makes the impl match the doc.
- Update the doc comment to the reconciled behavior: "Returns nil unconditionally — a no-console process (GUI/Explorer launch) is the common benign case and is logged at Debug, not Warn; a genuine detach failure is non-fatal and the watcher continues." Remove the stale "On any FreeConsole failure, log at Warn level" sentence.
- If `fmt` is no longer used after dropping `fmt.Errorf`, remove the `"fmt"` import.
  </action>
  <verify>
    <automated>cd "C:/Users/Virus Canary/Desktop/Claude/SquireBot" && gofmt -l cmd/squirebot/console_windows.go; go vet ./cmd/squirebot/</automated>
  </verify>
  <acceptance_criteria>
    - `gofmt -l cmd/squirebot/console_windows.go` prints NOTHING (999.20 clean — empty output is the pass)
    - `cmd/squirebot/console_windows.go` contains `func freeConsole`
    - `grep -n "slog.Warn" cmd/squirebot/console_windows.go` finds NOTHING (the benign no-console Warn noise is gone — 999.21)
    - the freeConsole doc states "returns nil unconditionally" / "Debug" (no longer claims a Warn-on-failure behavior the impl contradicts)
    - `go build ./cmd/squirebot` exits 0 (compiles on the Windows dev box; no unused-import error if `fmt` was dropped)
  </acceptance_criteria>
  <done>console_windows.go is gofmt-clean and freeConsole's doc matches its (Debug-not-Warn, returns-nil-unconditionally) implementation — no spurious detach Warn on GUI launches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 999.22 SemVer-pre-release-aware auto-update compare (manifest.go)</name>
  <read_first>
    - MODIFY: internal/update/manifest.go (IsNewer + parseVersion) + manifest_test.go
    - MIRROR (direction only, do NOT import): internal/backendsrv/ingest/version.go::IsOlder (Plan 01 — pre-release sorts below final). The two are deliberately separate copies; keep them behaviorally consistent.
    - CONTRACT to honor: CONTEXT D-9 (999.22, load-bearing for the P16 CUTOVER-03 coordinated self-update flip); the existing "false on a CORRUPT manifest" defensive contract MUST be preserved (a malformed manifest must never trigger an update); IsNewer is consumed by internal/update/check.go to gate the download+stage. Document the "ONE version-compare doctrine, per side" rule (the server-side IsOlder is the twin).
  </read_first>
  <behavior>
    - IsNewer("1.0.0", "2.0.0") == true; IsNewer("2.0.0", "2.0.0") == false; IsNewer("2.0.1", "2.0.0") == false (unchanged from today)
    - Leading "v" stripped both sides: IsNewer("v1.0.0", "v2.0.0") == true (unchanged)
    - Pre-release ranks BELOW its final: IsNewer("2.0.0-rc1", "2.0.0") == true (a final IS newer than its rc — the rc watcher should update to the final); IsNewer("2.0.0", "2.0.0-rc1") == false (a final must NOT "downgrade" to an rc)
    - Pre-release vs pre-release: IsNewer("2.0.0-rc1", "2.0.0-rc2") == true; IsNewer("2.0.0-rc2", "2.0.0-rc1") == false
    - Same version both with no pre-release: IsNewer("2.0.0", "2.0.0") == false (equal)
    - A CORRUPT manifest version still yields false (defensive): IsNewer("2.0.0", "garbage") == false; IsNewer("2.0.0", "") == false; IsNewer("2.0.0", "2.0") == false (a manifest that fails to parse must NOT trigger an update)
    - A corrupt CURRENT version is conservative: IsNewer("garbage", "2.0.0") == false (do not auto-update off an unparseable running version). Document the choice.
  </behavior>
  <action>
Rework `internal/update/manifest.go`:
- Change `parseVersion` to return the 3-part core AND the optional pre-release tail: `func parseVersion(v string) (core [3]int, prerelease string, ok bool)`. Implementation: strip leading `v`; `coreStr, prerelease, _ := strings.Cut(v, "-")` (splits on the FIRST `-`); split `coreStr` on `.`; require exactly 3 numeric parts (else `ok=false` — preserves the defensive contract); `strconv.Atoi` each. Return the parsed core, the tail (may be ""), and ok.
- Rework `IsNewer(current, manifest string) bool`:
  - `cCore, cPre, cOk := parseVersion(current); if !cOk { return false }` (conservative: an unparseable running version does not auto-update).
  - `mCore, mPre, mOk := parseVersion(manifest); if !mOk { return false }` (defensive: a corrupt manifest never triggers an update — UNCHANGED contract).
  - Compare the cores in order; first difference: `mCore[i] > cCore[i]` ⇒ true, `<` ⇒ false.
  - If cores are EQUAL, apply pre-release precedence (SemVer §11 — a version WITHOUT a pre-release outranks the same version WITH one):
    - `cPre == "" && mPre == ""` ⇒ false (truly equal).
    - `cPre != "" && mPre == ""` ⇒ true (manifest final is newer than the running rc → update).
    - `cPre == "" && mPre != ""` ⇒ false (running final must not downgrade to a manifest rc).
    - `cPre != "" && mPre != ""` ⇒ `strings.Compare(mPre, cPre) > 0` (manifest rc lexically after the running rc → newer; sufficient for our `rcN`/`betaN` scheme — document that pre-release comparison is intentionally a lexical compare because we only ever publish `MAJOR.MINOR.PATCH` finals, with pre-releases a dev-only safety rail).
- Update the IsNewer + parseVersion doc comments: state the SemVer-pre-release-aware behavior, the preserved "false on corrupt manifest" defensive contract, and the "ONE version-compare doctrine, per side" rule (this watcher-side IsNewer and the server-side `internal/backendsrv/ingest/version.go::IsOlder` from Plan 01 are deliberately separate, behaviorally-consistent copies — watcher and server must not import each other's internals; only `MAJOR.MINOR.PATCH` final tags are ever published, so the pre-release path is a defensive rail for the P16 flip).
- Update manifest_test.go: add cases for every `<behavior>` bullet (the pre-release ranking + the corrupt-manifest defensiveness). KEEP any existing IsNewer cases passing.
  </action>
  <verify>
    <automated>cd "C:/Users/Virus Canary/Desktop/Claude/SquireBot" && go test ./internal/update/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/update/ -count=1` exits 0 (the new pre-release cases + the preserved corrupt-manifest cases + any pre-existing IsNewer/check tests all green)
    - `internal/update/manifest.go` contains `func IsNewer`
    - manifest_test.go asserts `IsNewer("2.0.0-rc1", "2.0.0") == true` AND `IsNewer("2.0.0", "garbage") == false` (pre-release ranking + defensive contract both proven)
    - `go vet ./internal/update/` exits 0
  </acceptance_criteria>
  <done>The watcher's auto-update compare ranks pre-releases below their final, keeps the corrupt-manifest defensive contract, and is the documented watcher-side twin of the server's IsOlder — de-risking the P16 coordinated flip.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the re-targeted binary is materially smaller + carries no Google secret (SC-2 confirmation)</name>
  <read_first>
    - READ-ONLY confirmation (no production code change): the deletion landed in Plan 03; this task PROVES the SC-2 outcome and records it.
    - CONTRACT to honor: WATCH-09 / SC-2 ("the re-targeted binary contains no Google OAuth/PKCE/Sheets/Drive-Picker code; the build bakes in no Google secret; the binary is materially smaller"). The dependency tree already dropped in Plan 03 (`go mod tidy`); this task quantifies the binary delta + greps the binary for any baked Google secret string.
    - PRECONDITION: Plan 03 complete (the six packages deleted, `go mod tidy` done, build green).
  </read_first>
  <action>
This is a confirmation task — it builds the re-targeted watcher and records the SC-2 evidence in the SUMMARY (no production code edits; if any check FAILS, that is a regression to fix back in Plan 03, not here).

1. Build the re-targeted watcher the way CI does (so the size is representative): on the Windows dev box, `go build -ldflags="-H=windowsgui -s -w -X main.Version=2.0.0-dev" -o dist/squirebot-v2.exe ./cmd/squirebot`. Record the byte size. The v1 bare binary was ~17 MB (per release.yml's Release body text); the re-targeted binary should be MATERIALLY smaller (the dropped Google tree — oauth2 + google.golang.org/api + grpc + genproto + cloud.google.com/go/auth — is multiple MB). Record old (~17 MB, documented) + new sizes + the delta in the SUMMARY.
2. Grep the built binary for any baked Google secret / OAuth string residue. Use a string scan for the patterns `oauth2.googleapis.com`, `accounts.google.com`, `client_secret`, `spreadsheets.googleapis.com` (PowerShell `Select-String -SimpleMatch` or the `strings`-equivalent). Expect ZERO matches — no Google OAuth endpoint, no client_secret, no Sheets API host baked in. (A statically-linked Go binary embeds the URLs/strings of any linked Google client; their ABSENCE confirms the dependency tree is truly gone, not just unused.)
3. Confirm the dependency-tree drop once more: `go list -deps ./cmd/squirebot` filtered for `oauth2`, `google.golang.org/api`, `cloud.google.com` returns nothing (the `database/sql/driver` substring-match for "drive" is the known benign exception — exclude it).
4. Record in the SUMMARY: old size, new size, delta (absolute + percent), the zero-Google-string grep result, and the clean `go list -deps`. This IS the SC-2 evidence the verifier reads.

If the binary still contains a Google string OR is not materially smaller, STOP — that means a Google package survived the deletion (a missed importer or a `go mod tidy` that did not run); fix it back in Plan 03 (re-open the deletion); do not paper over it here.
  </action>
  <verify>
    <automated>cd "C:/Users/Virus Canary/Desktop/Claude/SquireBot" && go build -ldflags="-s -w -X main.Version=2.0.0-dev" -o dist/squirebot-v2-check ./cmd/squirebot && go list -deps ./cmd/squirebot</automated>
  </verify>
  <acceptance_criteria>
    - `go build -ldflags="-s -w -X main.Version=2.0.0-dev" -o dist/squirebot-v2-check ./cmd/squirebot` exits 0
    - `go list -deps ./cmd/squirebot` output contains NO `golang.org/x/oauth2`, NO `google.golang.org/api`, NO `cloud.google.com` (Google-free; the benign `database/sql/driver` "drive" substring is excluded)
    - the SUMMARY records the new binary size + the ~17 MB v1 baseline + the delta (the materially-smaller evidence)
    - a string scan of the built binary for `client_secret` / `accounts.google.com` / `oauth2.googleapis.com` / `spreadsheets.googleapis.com` finds NOTHING (no baked Google secret — recorded in the SUMMARY)
  </acceptance_criteria>
  <done>The re-targeted binary is measured materially smaller than the ~17 MB v1 baseline, carries no Google OAuth/Sheets string, and `go list -deps` is Google-free — the SC-2 evidence is recorded for the verifier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| watcher → GitHub Releases (auto-update) | The version compare gates which manifest binary the watcher downloads + stages (the P16 flip relies on it) |
| build artifact → guildie machines | The shipped binary must carry no residual Google secret (SC-2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.04-01 | Tampering | A corrupt/forged manifest version tricking the updater into a bad swap | mitigate | IsNewer preserves the "false on any parse failure" defensive contract (proven by `IsNewer("2.0.0","garbage")==false`); only a well-formed, strictly-newer version triggers a download. The SHA-256 verify in check.go/swap.go (unchanged) is the second gate. |
| T-13.04-02 | Tampering | A pre-release manifest mis-ranked, causing the P16 flip to skip or downgrade watchers | mitigate | 999.22: pre-release now ranks below its final (a final updates an rc; a final never downgrades to an rc), so the coordinated final `v2.x.0` tag is correctly seen as newer by every watcher (including any on a pre-release). De-risks CUTOVER-03. |
| T-13.04-03 | Information Disclosure | A residual Google client secret baked into the shipped binary | mitigate | Task 3 string-scans the built binary for `client_secret`/Google hosts and asserts zero matches; `go list -deps` is Google-free. The secret died with the stripped ldflags (Plan 03) — Task 3 is the proof. |
| T-13.04-04 | Repudiation | Spurious FreeConsole Warn noise obscuring real log signal | accept | 999.21 drops the benign no-console case to Debug, reducing noise; the genuine-failure path stays observable. Low severity (log hygiene); accepted as resolved by the doc/impl reconciliation. |

Severity: all LOW. This plan is hygiene + a P16-de-risking version-compare fix + an SC-2 confirmation; it adds no new attack surface. None trips block_on:high.
</threat_model>

<verification>
- `gofmt -l cmd/squirebot/console_windows.go` prints nothing; `go test ./internal/update/ -count=1` green.
- `go build ./... && go vet ./... && go test ./... -count=1` still green across the whole repo (no regression from the nits).
- `go list -deps ./cmd/squirebot` Google-free; the built binary carries no Google OAuth/Sheets string (Task 3 evidence in the SUMMARY).
</verification>

<success_criteria>
- console_windows.go is gofmt-clean and freeConsole's doc matches its implementation (no spurious Warn).
- The watcher's auto-update compare is SemVer-pre-release-aware (pre-release below final; corrupt-manifest defensive contract preserved) — the P16 flip is de-risked.
- The re-targeted binary is recorded materially smaller than the ~17 MB v1 baseline and carries no Google secret (SC-2 confirmed).
</success_criteria>

<output>
After completion, create `.planning/phases/13-watcher-re-target-onboarding/13-04-SUMMARY.md`.
</output>
