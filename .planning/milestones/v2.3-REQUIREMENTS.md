# v2.3 Requirements (ARCHIVED — shipped 2026-06-09)

**Status:** ✅ SHIPPED — opened 2026-06-08, shipped 2026-06-09. Phases 26–28. Milestone audit PASSED 2026-06-09 (14/14 requirements satisfied + UAT-verified).

**Milestone goal:** Associate SquireBot users with specific characters, let them view those characters' inventory, and create character-tagged wantlist items that roll up to the guildwide wantlist.

**Builds on (verified in code, did not rebuild):** `character.owner_id → owner.discord_user_id → web_user` already links each character to one user (bound by whoever's watcher code uploaded it). Inventory is per-character; read views are all-members consolidated (Char column). `wantlist_item` (00006) is per-user with NO character_id; the EC-tunnel monitor (`internal/backendsrv/ec`) DMs the user via `wantmatch.ForItem`. Schema started at v8 (`00008_ec_cursor`); this milestone added `00009` → schema v9 (P26 `character_assignment`) and `00010` → schema **v10** (P28 wantlist `character_id`).

**Locked decisions (2026-06-08):**
- **Assignment = BOTH** — members self-claim (incl. characters under an unlinked/legacy owner) AND officers assign/override/reassign. Single-assignee PK; bank/bot designation officer-only.
- **Inventory = ALL-MEMBERS + FILTER** — keep the all-members consolidated views (CLAUDE.md LOCKED), ADD a "my characters" filter/drill-down. ADDITIVE — not an access-control restriction.
- **Wantlist = CHARACTER-TAGGED** — wants gain an optional character dimension and aggregate into the guildwide wantlist; the EC-monitor DM still targets the character's OWNER (notifications key on `discord_user_id`).

**Scope:** backend (`internal/backendsrv`) + web (`web/`). The Go **watcher is untouched** (backend-only schema; no `WatcherMaxSchemaVersion` concern).

**Resolved sub-decisions (locked 2026-06-08):** ASSIGN-05 → single-assignee model (shared bank toons handled via officer-only bank/bot designation, not multi-owner rows); CWANT-04 → the guildwide wantlist SHOWS per-want character + owner attribution (private `note` excluded); CWANT-05 → the EC-monitor embed NAMES the character via a display-only "For <char>" field (DM target unchanged).

---

## v2.3 Requirements

### Character Assignment (ASSIGN)

- [x] **ASSIGN-01**: A signed-in member can see "My characters" — the list of characters currently assigned to them.
- [x] **ASSIGN-02**: A signed-in member can self-claim a character (including one uploaded under an unlinked/legacy owner, or currently unassigned) so it appears under "My characters."
- [x] **ASSIGN-03**: A member can release/unclaim a character they hold, returning it to unassigned so it can be reassigned.
- [x] **ASSIGN-04**: An officer can assign any character to any member, and reassign/override an existing assignment, from the admin panel.
- [x] **ASSIGN-05**: A character's shared-bank case is resolved via a **single-assignee** model (`character_assignment` single-assignee PK + officer-only bank/bot designation) rather than a many-to-many layer — no schema rework needed to support shared bank toons.
- [x] **ASSIGN-06**: Every assignment change (self-claim, release, officer assign/reassign) is recorded in the audit log (actor, character, action, time).

### My-Characters Inventory Filter (MYVIEW)

- [x] **MYVIEW-01**: A member can filter the consolidated views (inventory/bank/gear/spell) to just their assigned characters — a "my characters" quick-filter — WITHOUT changing the existing all-members visibility.
- [x] **MYVIEW-02**: A member can drill into a single specific assigned character's inventory.

### Character-Tagged Wantlist (CWANT)

- [x] **CWANT-01**: When adding a wantlist item, a member can optionally tag it to one of their assigned characters.
- [x] **CWANT-02**: Wants without a character (account-level, and all pre-existing wants) remain valid — the character tag is OPTIONAL; the `00010` migration backfills existing wants to no-character with no data loss (schema → v10). *(00010/v10, NOT 00009 — 00009 shipped as the P26 `character_assignment` migration.)*
- [x] **CWANT-03**: Character-tagged wants aggregate ("filter up") into the guildwide wantlist alongside untagged wants.
- [x] **CWANT-04**: The guildwide wantlist surfaces character/owner attribution per want (who / which character wants it), EXCLUDING the private `note`.
- [x] **CWANT-05**: The EC-tunnel monitor DM for a character-tagged want still targets the character's OWNER (keys on `discord_user_id`); the embed NAMES the character via a display-only "For <char>" field.
- [x] **CWANT-06**: A member can filter/group their own wantlist by character.

---

## v2.3 Traceability

_Maps each v2.3 REQ-ID to exactly one phase. All 14 v2.3 requirements satisfied; no orphans, no duplicates._

| REQ-ID | Phase | Status |
|--------|-------|--------|
| ASSIGN-01 | Phase 26 — Character Assignment | Complete / shipped |
| ASSIGN-02 | Phase 26 — Character Assignment | Complete / shipped |
| ASSIGN-03 | Phase 26 — Character Assignment | Complete / shipped |
| ASSIGN-04 | Phase 26 — Character Assignment | Complete / shipped |
| ASSIGN-05 | Phase 26 — Character Assignment *(resolved: single-assignee)* | Complete / shipped |
| ASSIGN-06 | Phase 26 — Character Assignment | Complete / shipped |
| MYVIEW-01 | Phase 27 — My-Characters Inventory Filter | Complete / shipped |
| MYVIEW-02 | Phase 27 — My-Characters Inventory Filter | Complete / shipped |
| CWANT-01 | Phase 28 — Character-Tagged Wantlist | Complete / shipped |
| CWANT-02 | Phase 28 — Character-Tagged Wantlist *(00010/v10 correction)* | Complete / shipped |
| CWANT-03 | Phase 28 — Character-Tagged Wantlist | Complete / shipped |
| CWANT-04 | Phase 28 — Character-Tagged Wantlist *(resolved: shows attribution)* | Complete / shipped |
| CWANT-05 | Phase 28 — Character-Tagged Wantlist *(resolved: embed names character)* | Complete / shipped |
| CWANT-06 | Phase 28 — Character-Tagged Wantlist | Complete / shipped |

**Coverage:** 14/14 v2.3 requirements satisfied across Phases 26–28 (ASSIGN-01..06 → P26 · MYVIEW-01/02 → P27 · CWANT-01..06 → P28). Strict dependency order 26 → 27 → 28. Backend (`internal/backendsrv`) + web (`web/`) only; the Go watcher was untouched; `00009` migration → schema v9, `00010` → schema **v10**.

> **CWANT-02 correction note:** the original v2.3 requirements named `00009` as the wantlist `character_id` migration. During execution `00009` shipped as the P26 `character_assignment` migration (schema v9), so the wantlist `character_id` touch landed in `00010` (schema v10) in P28. The requirement text above reflects the corrected `00010`/v10 numbering.

---

*Requirements defined 2026-06-08 for v2.3 "Character Assignment & Per-Character Wantlists". Shipped 2026-06-09 (Phases 26–28; 14/14 satisfied; milestone audit PASSED). Prior milestones: v1.0, v1.0.1, v2.0, v2.1, v2.2 (see `milestones/` and the parked-v2.2 record).*
