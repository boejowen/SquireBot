---
milestone: v2.4
name: "Web UI Revamp (5-Tab Restructure)"
audited: 2026-06-21
status: passed
scores:
  requirements: 27/27
  phases: 6/6
  integration: wired
  flows: 4/4
gaps:
  requirements: []   # none — all 27 satisfied (3-source cross-reference: VERIFICATION passed + SUMMARY frontmatter + traceability ✅)
  integration: []    # the one WARNING (stale apex redirect) was FIXED during the audit — see resolved_during_audit
  flows: []   # all 4 E2E user flows wire end-to-end
resolved_during_audit:
  - id: "apex-redirect-stale"
    fix: "web/src/routes/+page.ts now redirect(307, '/characters') — the documented post-Phase-31 flip. Committed b92b71b, web-only deploy verified live (apex 200 → client-redirects to /characters; /guild-views still 200). The front door now opens onto the 5-tab UI."
tech_debt:
  - phase: 34-wishlist-rework-per-character-per-slot-upgrades
    items:
      - "MD-01 (code-review): web/src/lib/api.ts retains 5 orphaned wantlist fetch wrappers (fetchOwnWants/fetchGuildWants/addWant/removeWant/muteWant) → the deleted /api/v1/wantlist* routes, imported only by the now-orphaned WantAddForm/WantMuteCell/WantItemCell/WantRemoveCell cluster + a stale api.test.ts. Latent/unreachable (/wantlist is a redirect stub) — verified no live tab reaches them."
      - "Backend twin of MD-01 (integration-check): internal/backendsrv/webadmin/wantlist.go + store/wantlist.go still contain live SQL against the dropped wantlist_item table (INSERT/UPDATE/SELECT). Compile-clean but would error at runtime IF called — and they are NOT registered in main.go, so dead/unreachable. Delete both to close the loop."
  - phase: 31-characters-tab-in-game-inventory-window
    items:
      - "4 code-review WARNINGs (WR-01..04, advisory, shipped 0 BLOCKER/0 HIGH): empty wiki row on non-http(s) url; missing 00013 migration test; untested DROP COLUMN rollback; two-word-race roster ambiguity. Optional /gsd-code-review-fix 31."
      - "21-of-23 paperdoll slots rendered (Charm/Power Source omitted — post-Velious; backend keeps all 23). Accepted deviation."
nyquist:
  overall: skipped   # workflow.nyquist_validation = false
---

# v2.4 "Web UI Revamp (5-Tab Restructure)" — Milestone Audit

**Audited:** 2026-06-21 · **Status:** `passed` (the one WARNING fixed during the audit; remaining tech debt is non-blocking dead code) · **Requirements:** 27/27 satisfied

## Verdict

All six phases shipped, deployed live to squirebot.quest, browser-smoke approved across 5 themes, and **wire together cleanly** — the integration checker traced all 5 backend routes (session-gated), all 5 tabs, 2 single-definition shared components (InventoryWindow, ExaminePanel — many consumers, no fork), the P29 data layer feeding all 3 new rollups, the cross-tab deep-links, and the EC/notify spine surviving the P34 wantmatch repoint. All 4 headline E2E flows complete end-to-end. There are **no unsatisfied requirements and no critical blockers** — the milestone delivers its definition of done. The audit's one WARNING (the stale apex `/` → /guild-views redirect) was **fixed during the audit** (`b92b71b`, flipped to /characters + deployed). The remaining tech debt is non-blocking dead wantlist code, deferred to a `/gsd-quick` cleanup.

## Requirements Coverage — 27/27 satisfied (3-source cross-reference)

Every REQ-ID cross-references cleanly across all three independent sources (phase VERIFICATION.md `passed` + phase SUMMARY `requirements-completed` frontmatter + REQUIREMENTS.md traceability `✅ Done`):

| Phase | Requirements | VERIFICATION | Status |
|-------|-------------|--------------|--------|
| 29 Data Foundation | INV-05, DATA-01, DATA-02 | passed | ✅ satisfied |
| 30 App Shell + 5-Tab Nav | NAV-01, NAV-02, NAV-03, NAV-04 | passed | ✅ satisfied |
| 31 Characters Tab + Window | CHAR-01..03, INV-01..04 | passed | ✅ satisfied |
| 32 Inventory Tab (item-centric) | ITEM-01, ITEM-02, ITEM-03 | passed | ✅ satisfied |
| 33 Banks Tab + Valuation | BANK-01, BANK-02, BANK-03 | passed | ✅ satisfied |
| 34 Wishlist Rework | WISH-01..07 | passed | ✅ satisfied |

No orphaned requirements (every traceability entry maps to a passing VERIFICATION). No partial/unsatisfied. **Total: 27/27.**

## Cross-Phase Integration — WIRED

| E2E Flow | Status | Trace |
|----------|--------|-------|
| "What does my character have?" | ✅ wired | /characters → /api/v1/characters → select → /api/v1/inventory/{char} → InventoryWindow → ExaminePanel |
| "Which characters have item X?" (cross-tab) | ✅ wired | /inventory → /api/v1/items → select → holders → /characters?c=<name> → characters reads ?c= + pre-selects |
| "What's in the banks & worth?" | ✅ wired | /banks → /api/v1/banks → guild summary + per-bank header → InventoryWindow; search reuses /api/v1/items filtered is_bank, holder-click in-tab |
| "What can I get to improve my character?" | ✅ wired | /wishlist → /api/v1/wishlist/{char} → per-slot accordion + suggestions + add + ping toggle → EC hit → "Seen in EC" badge |

Shared components: `InventoryWindow.svelte` (P31 + P33), `ExaminePanel.svelte` (P31 + P32 + P34) — one definition each, no copy-paste divergence. Backend: all 5 routes registered under `RequireSession` in main.go; the 5 retired `/api/v1/wantlist*` routes removed. Schema chain 00012 → 00013 → 00014 → **v14**, no orphaned FK to the dropped wantlist_item (alert_log rebuilt before the drop).

## Fixed during the audit

- **[user-facing, one-liner] Apex redirect** — `web/src/routes/+page.ts` flipped `/` → `/characters` (was `/guild-views`, the leftover P30 stopgap). Committed `b92b71b`, web-only deploy verified live (apex 200 → client-redirects to /characters; the legacy grids remain at /guild-views). The front door now opens onto the 5-tab UI.

## Tech Debt (deferred — none blocking)

1. **[dead code] Wantlist remnants (frontend + backend)** — `api.ts`'s 5 orphaned wantlist wrappers + the `Want*` component cluster + the stale `api.test.ts` (MD-01), and the backend twin `webadmin/wantlist.go` + `store/wantlist.go` (live SQL against the dropped table, unregistered). All verified unreachable. Worth a `/gsd-quick` cleanup to close the loop after the clean break.
2. **[advisory] Phase 31 carry-overs** — 4 code-review WARNINGs (WR-01..04) + the accepted 21-of-23 paperdoll-slot deviation.

## Phase Quality Snapshot

| Phase | Verifier | Code review | Deploy + smoke |
|-------|----------|-------------|----------------|
| 29 | 4/4 | clean (findings fixed) | n/a (compute-on-read) |
| 30 | 12/12 | — | live + 8/8 smoke |
| 31 | 4/4 + 7/7 reqs | 0 BLOCKER/0 HIGH (4 advisory WARN) | live + smoke × 5 themes |
| 32 | 6/6 | resolved (1 BLOCKER fixed-forward `2637db9`) | live + 7-pt smoke × 5 themes |
| 33 | 12/12 | clean (0/0; 1 MED + 3 WARN advisory) | live + 10-pt smoke × 5 themes |
| 34 | 7/7 | clean (0/0; 1 MED + 2 WARN advisory) | live + 10-pt smoke × 5 themes |

---

*Milestone audit generated 2026-06-21. v2.4 = 6 phases, 27 requirements, schema v14. Watcher untouched throughout. All prod deploys driven via the ssh-agent workaround.*
