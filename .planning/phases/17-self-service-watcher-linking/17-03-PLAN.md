---
phase: 17-self-service-watcher-linking
plan: 03
type: execute
wave: 3
depends_on: [17-02]
files_modified:
  - web/src/lib/api.ts
  - web/src/routes/account/+page.svelte
  - web/src/lib/components/WatcherCodesPanel.svelte
  - web/src/lib/components/SiteShell.svelte
  - web/src/lib/components/StateBlock.svelte
  - web/src/lib/__tests__/api.test.ts
autonomous: false
requirements: [LINK-01, LINK-03, LINK-04, LINK-05]
must_haves:
  truths:
    - "A signed-in member sees an 'Account' nav entry (every logged-in member, NOT officer-gated) leading to /account"
    - "Clicking 'Generate a new code' mints a code and shows the plaintext exactly once in a prominent copy-to-clipboard panel with paste-into-watcher instructions and a 'you won't see this again' warning"
    - "Reloading or revisiting /account never re-reveals a plaintext code (the list shows only #N / created / last-seen)"
    - "Minting again adds a new #N row without removing existing ones (additive)"
    - "A member can revoke any single one of their own codes via a confirm-before-commit dialog; the row collapses on success"
  artifacts:
    - path: "web/src/routes/account/+page.svelte"
      provides: "the /account member page (.form-card shell, mirrors /char-meta)"
      contains: "WatcherCodesPanel"
    - path: "web/src/lib/components/WatcherCodesPanel.svelte"
      provides: "generate → show-once panel → codes list → confirm-revoke"
      min_lines: 80
    - path: "web/src/lib/api.ts"
      provides: "fetchOwnCodes / mintOwnCode / revokeOwnCode + OwnCode type"
      contains: "mintOwnCode"
  key_links:
    - from: "web/src/lib/components/WatcherCodesPanel.svelte"
      to: "/api/v1/account/codes"
      via: "mintOwnCode/fetchOwnCodes/revokeOwnCode in $lib/api"
      pattern: "mintOwnCode|fetchOwnCodes|revokeOwnCode"
    - from: "web/src/lib/components/SiteShell.svelte"
      to: "/account"
      via: "member-visible nav under session?.authenticated"
      pattern: "href=\"/account\""
---

<objective>
Build the `/account` member page: the SvelteKit route + the `WatcherCodesPanel` (generate → show-once plaintext reveal with copy-to-clipboard → active-codes list with #N/created/last-seen → confirm-before-commit revoke), the `api.ts` wrappers, and the member-visible "Account" nav entry. This consumes the three Wave-2 endpoints.

Purpose: The user-facing surface of self-service linking (LINK-01/03/04/05). The show-once + clipboard panel is the one genuinely-new frontend surface and the most security-sensitive — it must never persist/re-reveal the plaintext.
Output: `api.ts` (+3 wrappers + OwnCode type), `routes/account/+page.svelte`, `WatcherCodesPanel.svelte`, `SiteShell.svelte` (Account nav), `StateBlock.svelte` (no-codes kind), api.test.ts.

NOTE: this plan is NON-AUTONOMOUS — it ends in a human browser-smoke checkpoint because `web/` vitest is node-only and cannot see the DOM/clipboard (the P15 precedent shipped 2 crashing BLOCKERs behind 165 green tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-self-service-watcher-linking/17-CONTEXT.md
@.planning/phases/17-self-service-watcher-linking/17-RESEARCH.md
@.planning/phases/17-self-service-watcher-linking/17-PATTERNS.md
@.planning/phases/17-self-service-watcher-linking/17-UI-SPEC.md

<interfaces>
<!-- Wave-2 endpoints this page consumes (login-only, cookie-credentialed): -->
POST /api/v1/account/codes          → { "code": "<plaintext>" }   (owner session-derived; body is {})
GET  /api/v1/account/codes          → [ { id, ordinal, created_at, last_seen|null }, ... ]  (or [] if never minted)
POST /api/v1/account/codes/revoke   → { "revoked": bool }          (body { id })  ; 409 {"error":"ambiguous_owner"} on mint
<!-- Reuse from web/src/lib/api.ts (DO NOT re-author fetch): -->
getJSON<T>(path, f)   // credentials:'include', throws Unauthenticated(401)/Forbidden(403)
postJSON<T>(path, body, f)
classifyAdminError(err) // api.ts:517 — branch the {"error":"code"} shape if needed
<!-- Reuse components: EvictionForm.svelte (load→confirm→commit pattern), ConfirmDialog, StateBlock, AuthGate (AUTH_GUARD_KEY / SESSION_KEY). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add api.ts wrappers + types; add the StateBlock no-codes kind</name>
  <read_first>
    - web/src/lib/api.ts (getJSON/postJSON cores + credentials:'include' :163/247; the one-liner GET-wrapper convention e.g. fetchMeta :221; Unauthenticated/Forbidden subclasses :46/54; classifyAdminError :517)
    - web/src/lib/components/StateBlock.svelte (the existing kind switch — add a no-codes kind)
    - web/src/lib/__tests__/api.test.ts (the existing wrapper-test convention to mirror)
    - .planning/phases/17-self-service-watcher-linking/17-PATTERNS.md (api.ts section — the exact wrapper shapes)
    - .planning/phases/17-self-service-watcher-linking/17-UI-SPEC.md (Copywriting Contract — no-codes empty-state copy)
  </read_first>
  <action>
    In `web/src/lib/api.ts`, add (mirroring the existing one-liner wrappers — they already carry credentials + typed errors):
    ```ts
    export interface OwnCode { id: number; ordinal: number; created_at: string; last_seen: string | null; }
    export function fetchOwnCodes(f: typeof fetch = fetch): Promise<OwnCode[]> {
        return getJSON<OwnCode[]>('/api/v1/account/codes', f);
    }
    export function mintOwnCode(f: typeof fetch = fetch): Promise<{ code: string }> {
        return postJSON<{ code: string }>('/api/v1/account/codes', {}, f);
    }
    export function revokeOwnCode(id: number, f: typeof fetch = fetch): Promise<{ revoked: boolean }> {
        return postJSON<{ revoked: boolean }>('/api/v1/account/codes/revoke', { id }, f);
    }
    ```
    Do NOT write raw `fetch` — reuse `getJSON`/`postJSON`. In `StateBlock.svelte`, add a `no-codes` kind: heading "No watcher codes yet", body "You haven't linked any PCs yet. Generate a code above, then paste it into the SquireBot watcher on the PC you want to link." (17-UI-SPEC copy), reusing the existing `circle-alert` glyph + the existing StateBlock layout (no new styling). In `api.test.ts`, add unit tests for the three wrappers asserting the correct path + method + that the body for mint is `{}` (use the existing mocked-fetch harness).
  </action>
  <verify>
    <automated>cd web && npm run check && npm test -- api</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function mintOwnCode" web/src/lib/api.ts` AND `grep -q "export function fetchOwnCodes" web/src/lib/api.ts` AND `grep -q "export function revokeOwnCode" web/src/lib/api.ts` succeed
    - `grep -q "export interface OwnCode" web/src/lib/api.ts` succeeds
    - The three wrappers call `getJSON`/`postJSON` (not a raw `fetch(` — grep the new lines; no bare `fetch(` in them)
    - `grep -q "no-codes" web/src/lib/components/StateBlock.svelte` succeeds
    - `npm run check` (svelte-check/tsc) passes; `npm test -- api` passes the new wrapper tests
  </acceptance_criteria>
  <done>Three typed, credentialed api wrappers exist and reuse getJSON/postJSON; OwnCode type exported; StateBlock has a no-codes kind; wrapper unit tests green; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build WatcherCodesPanel + the /account page + the Account nav entry</name>
  <read_first>
    - web/src/lib/components/EvictionForm.svelte (the richest analog: Svelte 5 runes, getContext AUTH_GUARD_KEY, load→confirm→commit lifecycle, ConfirmDialog reuse, optimistic-collapse list-filter, {} auto-escape — never {@html})
    - web/src/routes/char-meta/+page.svelte (the .form-card member-page shell to copy verbatim; no +page.ts)
    - web/src/lib/components/SiteShell.svelte (the .char-meta-nav block under session?.authenticated — :54-62; the officer Admin block :47-52 to AVOID)
    - web/src/lib/components/ConfirmDialog.svelte (reuse as-is for the destructive revoke confirm)
    - web/src/lib/components/AuthGate.svelte (AUTH_GUARD_KEY / SESSION_KEY context + the 401→LoginScreen route)
    - web/src/lib/api.ts (the Task-1 wrappers)
    - .planning/phases/17-self-service-watcher-linking/17-UI-SPEC.md (Show-Once Panel, Codes List, Revoke Interaction, Copywriting Contract — the full visual/copy contract)
    - .planning/phases/17-self-service-watcher-linking/17-PATTERNS.md (WatcherCodesPanel + /account page + SiteShell sections)
  </read_first>
  <action>
    Create `web/src/routes/account/+page.svelte` mirroring `char-meta/+page.svelte`: the same `<section class="form-card">` shell (max-width:720px, 24px padding, --panel/--border, 6px radius, flex column gap:24px) + `.form-title` (20px --font-display). Title: "Your watcher codes" + the purpose line "A watcher code links a PC to your account. Paste one into the SquireBot watcher on each PC you run — generate a separate code per PC." Render `<WatcherCodesPanel />`. No `+page.ts` (inherits SPA fallback). AuthGate-gated at the layout (no page-level officer check).

    Create `web/src/lib/components/WatcherCodesPanel.svelte` (Svelte 5 runes; copy EvictionForm's structure):
    - Imports + `const authGuard = getContext<AuthGuard>(AUTH_GUARD_KEY)`; lucide `copy`/`check`/`triangle-alert`/`trash-2`; StateBlock, ConfirmDialog; the api wrappers.
    - State runes: `phase = $state<'loading'|'error'|'ready'>('loading')`, `codes = $state<OwnCode[]>([])`, `minting = $state(false)`, `mintedPlaintext = $state<string | null>(null)` (the show-once panel state), `copied = $state(false)`, `revokeTarget = $state<OwnCode|null>(null)`, `revokeDialogOpen = $state(false)`, `revoking = $state(false)`, `successMsg`/`errorMsg`.
    - `onMount` → `load()`: `fetchOwnCodes()` → `codes`; `phase='ready'`. On error: if it's an Unauthenticated, route to AuthGate (the EvictionForm `if (route(err)) return` pattern); else `phase='error'`.
    - Generate: `minting=true`, button label "Generating…" disabled; `const { code } = await mintOwnCode()`; set `mintedPlaintext = code`; re-`fetchOwnCodes()` to refresh the list (so the new #N appears below); `aria-live="polite"` on the panel. On error → inline `--status-missing` "Couldn't generate a code…".
    - SHOW-ONCE PANEL (LINK-04 — the genuinely-new surface, no in-repo clipboard analog): only renders `{#if mintedPlaintext}`. Highlighted callout (2px --accent left-border + ~8% accent tint), heading "Copy your new watcher code now", the token on its own line in --text, `font-variant-numeric: tabular-nums`, ~0.04em letter-spacing, `user-select: all`, word-break (NOT accent-colored, render via `{mintedPlaintext}` auto-escape — NEVER {@html}). A Copy button: `navigator.clipboard.writeText(mintedPlaintext)` → swap to check+"Copied!" (--status-ok) for ~2s; if the Clipboard API throws/denied, the token's `user-select:all` is the manual fallback (never block). Paste instructions: "Open the SquireBot watcher, paste this where it asks for your guild code, and you're linked. The same code works on this PC forever — no need to re-link." A `triangle-alert` warning in a --destructive consequence callout: "This is the only time you'll see this code. Copy it now — you can't view it again. (Lost it? Just generate a new one.)" A "Done" dismiss that sets `mintedPlaintext = null` (cosmetic only — the code is already in the list as #N; dismiss does NOT revoke). The plaintext lives ONLY in `mintedPlaintext` state — NEVER localStorage, NEVER re-fetched, NEVER logged (LINK-04 / RESEARCH Pitfall 4).
    - CODES LIST (LINK-05): section heading "Your active codes (N)"; a real `<ul>`/`<li>`; per row (flex space-between, mirror EvictionForm .restore-row): left = `#{ordinal}` chip + "Created {created_at human}" + last-seen ("last used {relative}" / "never used yet" when last_seen is null — never an alarm color); right = a quiet destructive-text "Revoke" button. Empty (`codes.length === 0`) → `<StateBlock kind="no-codes" />` with Generate still visible above.
    - REVOKE (confirm-before-commit, mirror EvictionForm openRestoreConfirm→ConfirmDialog→doRevoke): Revoke opens ConfirmDialog (destructive variant) for that code, heading "Revoke this code?", body "Revoking code #{N} stops that PC from uploading. Your other codes keep working, and you can always generate a fresh one.", destructive confirm "Revoke code #{N}", neutral Cancel, default-focus Cancel. On confirm: `await revokeOwnCode(id)`; on `revoked` → optimistic-collapse (`codes = codes.filter(c => c.id !== id)`) + `--status-ok` "Code #{N} revoked. That watcher will stop uploading on its next attempt." On failure → keep the row + `--status-missing` "Couldn't revoke code #{N}. No change was made." Esc/backdrop/Cancel dismiss with no side effect.
    - All interpolation via `{}` (auto-escape); NEVER `{@html}`. 44px min touch targets; honor prefers-reduced-motion (inherited from app.css). aria-live="polite" on the panel, the Copied! swap, and the success/error lines.

    In `SiteShell.svelte`: add `<a href="/account" class="char-meta-nav">Account</a>` INSIDE the `{#if session?.authenticated}` block, alongside the existing "Character details" link — NOT inside the `{#if session?.isOfficer}` Admin block. Reuse `.char-meta-nav` styling verbatim (no new styling).
  </action>
  <verify>
    <automated>cd web && npm run check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/routes/account/+page.svelte` exists and `grep -q "WatcherCodesPanel" web/src/routes/account/+page.svelte` succeeds
    - `web/src/routes/account/+page.svelte` does NOT contain a `RequireOfficer`/`isOfficer` gate (member page, not officer)
    - `grep -q "navigator.clipboard.writeText" web/src/lib/components/WatcherCodesPanel.svelte` succeeds
    - `grep -q "user-select" web/src/lib/components/WatcherCodesPanel.svelte` succeeds (the manual-copy fallback)
    - `WatcherCodesPanel.svelte` contains NO `{@html}` and NO `localStorage` (grep both return nothing)
    - `grep -q "mintOwnCode" web/src/lib/components/WatcherCodesPanel.svelte` AND `grep -q "revokeOwnCode" web/src/lib/components/WatcherCodesPanel.svelte` AND `grep -q "ConfirmDialog" web/src/lib/components/WatcherCodesPanel.svelte` succeed
    - `grep -q "href=\"/account\"" web/src/lib/components/SiteShell.svelte` succeeds, AND that line sits inside the `session?.authenticated` block, NOT the `session?.isOfficer` block (read the file to confirm placement)
    - `npm run check` (svelte-check/tsc) passes; `npm run build` succeeds
  </acceptance_criteria>
  <done>/account renders the form-card shell with WatcherCodesPanel; generate shows the plaintext once with working copy + a user-select fallback + the "won't see again" warning; the list shows #N/created/last-seen with confirm-before-commit revoke; the Account nav entry is member-visible (not officer-gated); plaintext never persisted/re-revealed; typecheck + build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser-smoke the /account page (vitest is DOM-blind)</name>
  <what-built>
    The /account member page: Generate-a-code → show-once plaintext panel with copy-to-clipboard + paste instructions + "won't see again" warning, the active-codes list (#N / created / last-seen), and the confirm-before-commit Revoke flow. The member-visible "Account" nav entry. (`web/` vitest is node-only — it cannot verify the clipboard, the show-once reveal, or the DOM, so this MUST be smoked in a real browser — the P15 precedent shipped 2 crashing BLOCKERs behind 165 green tests.)
  </what-built>
  <how-to-verify>
    1. Run the site locally (`cd web && npm run dev`) signed in as a member (or verify on the deployed site after backend deploy). Confirm the "Account" nav entry appears for a logged-in member and is ABSENT for a logged-out visitor.
    2. Visit /account. Click "Generate a new code". CONFIRM: a freshly minted plaintext code appears in the highlighted show-once panel, with the "you won't see this again" warning and paste instructions.
    3. Click "Copy" → CONFIRM it swaps to "Copied!" and the clipboard actually holds the code (paste it somewhere). If clipboard is denied, CONFIRM the token is still manually selectable (user-select:all).
    4. Dismiss the panel ("Done"), then RELOAD /account. CONFIRM the plaintext is GONE and the new code now shows only as a #N row (created + "never used yet" / last-seen). It must NOT re-reveal the plaintext.
    5. Generate a SECOND code → CONFIRM both #N rows are present (additive — LINK-03), neither removed.
    6. Click Revoke on one row → CONFIRM the ConfirmDialog opens with default focus on Cancel, Esc/backdrop dismiss with no change; confirm the destructive "Revoke code #N" → CONFIRM the row collapses + the success line shows, and the OTHER code's row remains.
    7. Spot-check the "Heavy" (parchment/light) theme for the show-once panel contrast and the destructive Revoke styling (17-UI-SPEC budgeted Heavy QA).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (broken clipboard, plaintext re-revealed on reload, revoke affecting the wrong code, nav visible when logged out, Heavy-theme contrast).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → account API | Cookie-credentialed fetch (apex→api subdomain); the backend RequireSession + owner-scoping is the real boundary — the hidden nav is UX only. |
| minted plaintext → DOM | The one-time plaintext lives only in component state; must never be persisted/logged/re-revealed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.03-01 | Information disclosure | show-once plaintext | mitigate | Plaintext held ONLY in `mintedPlaintext` Svelte state for the panel lifetime; never localStorage, never re-fetched, never logged; the list endpoint returns #N/created/last-seen, never a code (LINK-04 / RESEARCH Pitfall 4). Rendered via `{}` auto-escape, never `{@html}`. |
| T-17.03-02 | Tampering (XSS) | code/date/error interpolation | mitigate | Every interpolation (plaintext, #N, dates, error reason) via Svelte `{}` auto-escape; no `{@html}`. |
| T-17.03-03 | Elevation | nav vs. real gate | mitigate | The member-visible nav is UX; auth is server-truth — a 401 bubbles to AuthGate→LoginScreen (reuse the EvictionForm route pattern); never treat the hidden nav as the boundary (D-02/D-08). |
| T-17.03-04 | Repudiation | revoke | accept | Audit happens server-side (Plan 02); the UI is confirm-before-commit + optimistic-collapse only. |
| T-17.03-05 | DOM-blind test gap | clipboard/show-once | mitigate | NON-AUTONOMOUS browser-smoke checkpoint (Task 3) — vitest cannot see the clipboard/DOM (MEMORY: web-tests-node-only); do not call verified on unit tests alone. |
</threat_model>

<verification>
- `npm run check` + `npm run build` pass (typecheck + production build clean).
- `npm test -- api` passes the wrapper unit tests.
- Browser-smoke checkpoint (Task 3) approved: copy works, plaintext never re-revealed on reload, additive mint, scoped revoke, member-only nav, Heavy-theme contrast.
</verification>

<success_criteria>
- A member sees the "Account" nav (not officer-gated) and reaches /account.
- Generate shows the plaintext exactly once with working copy-to-clipboard + paste instructions + the irreversibility warning.
- Reload/revisit never re-reveals the plaintext; the list shows only #N/created/last-seen.
- Minting is additive in the UI; revoke is confirm-before-commit, per-code, optimistic-collapse.
- Plaintext is never persisted, logged, or re-fetched.
</success_criteria>

<output>
After completion, create `.planning/phases/17-self-service-watcher-linking/17-03-SUMMARY.md`
</output>
