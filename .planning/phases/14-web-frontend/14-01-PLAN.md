---
phase: 14-web-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/backendsrv/store/readviews.go
  - internal/backendsrv/store/readviews_test.go
  - internal/backendsrv/compute/types.go
  - internal/backendsrv/compute/view.go
  - internal/backendsrv/compute/view_test.go
  - internal/backendsrv/compute/bank.go
  - internal/backendsrv/compute/bank_test.go
  - internal/backendsrv/compute/gearcheck.go
  - internal/backendsrv/compute/gearcheck_test.go
  - internal/backendsrv/compute/spellcheck.go
  - internal/backendsrv/compute/spellcheck_test.go
  - internal/backendsrv/compute/eqconst.go
autonomous: true
requirements: [BACKEND-05, WEB-02]
must_haves:
  truths:
    - "Go compute produces view/bank/gear_check/spell_check rows matching v1 builder semantics for the same input"
    - "gear_check yields OK/MISSING/OTHER per (char,tier,slot,recommendation) exactly as buildGearCheck.ts"
    - "spell_check yields KNOWN/MISSING per (char,level,spell) exactly as buildSpellCheck.ts"
    - "view/bank rows carry enrichment inline (wiki_summary, WTS/WTB a30+t30, is_quest_item, quest links) so the client tooltip needs no second fetch"
  artifacts:
    - path: "internal/backendsrv/store/readviews.go"
      provides: "parameterized SELECT...JOIN read methods + raw-input reads for gear/spell"
      contains: "func (s *Store)"
    - path: "internal/backendsrv/compute/types.go"
      provides: "the four exported row structs the read API JSON-encodes"
      contains: "type ViewRow struct"
    - path: "internal/backendsrv/compute/gearcheck.go"
      provides: "Go reimpl of buildGearCheck slot-pair match + OK/OTHER/MISSING ordering"
      contains: "func GearCheck"
    - path: "internal/backendsrv/compute/spellcheck.go"
      provides: "Go reimpl of buildSpellCheck normalized_name KNOWN/MISSING join"
      contains: "func SpellCheck"
  key_links:
    - from: "internal/backendsrv/compute/view.go"
      to: "internal/backendsrv/store/readviews.go"
      via: "Store read method call (compute imports store, never the reverse)"
      pattern: "store\\."
    - from: "internal/backendsrv/compute/gearcheck.go"
      to: "internal/backendsrv/enrich/eqconst.go"
      via: "reuse WIKI_SLOT_TO_INV_SLOTS (do not re-type)"
      pattern: "WIKI_SLOT_TO_INV_SLOTS"
---

<objective>
Reimplement the four v1 Apps Script view builders (`buildView`, `buildBank`, `buildGearCheck`, `buildSpellCheck`) in Go over the live SQLite store, with parity proven against the v1 vitest expectations translated into Go table-tests (D-02 / WEB-02). Add the `store` read-query methods these builders consume — the relational/data layer of the read API (D-01). This is the **compute/data half** of BACKEND-05; the HTTP handler surface is Plan 03 (which calls these).

Purpose: The Go output MUST match v1 semantics exactly (gear OK/MISSING/OTHER vs Velious tiers; spell KNOWN/MISSING) so the website is a faithful replacement for the Sheet's view tabs, with parity provable as a `go test` acceptance criterion rather than asserted in prose.

Output: `internal/backendsrv/compute/` (4 builders + shared row structs + parity tests) and `internal/backendsrv/store/readviews.go` (parameterized read methods + parity test). No HTTP, no handlers — those compose this in Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-web-frontend/14-CONTEXT.md
@.planning/phases/14-web-frontend/14-RESEARCH.md
@.planning/phases/14-web-frontend/14-PATTERNS.md

<interfaces>
<!-- Schema (verified, migrations/00001_init.sql + 00003_enrich_columns.sql). The reads run over these tables. -->

character:        id, owner_id, name, class, level, race, is_bank_toon, is_hidden, is_removed, last_seen, watcher_version
inventory_item:   id, character_id, location, name, item_id (0/NULL=empty slot), count, slots, row_ordinal, uploaded_at
spellbook_entry:  id, character_id, level, name, normalized_name (= lower(trim(name)), already materialized), uploaded_at
item_master:      item_id (PK), name, wiki_summary, wiki_url, slot, is_quest_item (INTEGER 0/1), wikitext_sha1, last_refreshed
pigparse_price:   item_id (PK — ONE row per item), name, current_avg (REAL), blue_volume, last_seen, direction (TEXT!), last_refreshed, t30, a30, t60, a60, t6m, a6m, ty, ay
wiki_spells:      id, class, level, spell_name, normalized_name (= lower(trim(name)), already materialized), last_refreshed
wiki_gear_tier:   id, tier, class, slot, item_id (always NULL), item_name, rank, last_refreshed
quest_items:      id, item_id, quest_name, source_url, source, last_refreshed

<!-- Store constructor + read-method analog (verified, internal/backendsrv/store/) -->
func NewStore(db *sql.DB) *Store            // replace.go:45 — already exists
func (s *Store) DB() *sql.DB                // replace.go:51
func (s *Store) DistinctInventoryItemIDs(ctx context.Context) ([]ItemRef, error)  // itemids.go:40 — the ONLY existing read-side method; copy its shape exactly
func NewTestDB(t *testing.T) *sql.DB        // testhelper.go:23 — package store, importable from compute tests; Open(temp)+RunMigrations+Cleanup

<!-- Reusable Go constant (verified, internal/backendsrv/enrich/eqconst.go) — REUSE, do not re-type -->
var WIKI_SLOT_TO_INV_SLOTS = map[string][]string{ "Ears":{"EAR1","EAR2"}, "Fingers":{"FINGER1","FINGER2"}, "Wrists":{"WRIST1","WRIST2"}, "Neck":{"NECK"}, ... }  // eqconst.go:56 — 17 entries, ported verbatim from eq-constants.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store read methods (readviews.go) + parity test over a seeded temp DB</name>
  <files>internal/backendsrv/store/readviews.go, internal/backendsrv/store/readviews_test.go</files>
  <read_first>
    - internal/backendsrv/store/itemids.go (THE analog — the repo's only existing read-side `(*Store)` method; copy its QueryContext→rows.Next()/Scan→rows.Err() skeleton + `%w`-wrapped errors VERBATIM)
    - internal/backendsrv/store/replace.go:40-51 (Store struct + NewStore + DB() — already exist)
    - internal/backendsrv/store/testhelper.go (NewTestDB(t) — the migrated temp-DB fixture compute/store tests both use)
    - internal/backendsrv/store/enrich.go:38-103 (store-local input-struct convention — define read outputs the same way: store-local, NOT imported from compute/, so dependency runs compute→store)
    - internal/backendsrv/migrations/00001_init.sql + 00003_enrich_columns.sql (the verified column names the SELECTs project)
    - .planning/phases/14-web-frontend/14-PATTERNS.md § "internal/backendsrv/store/readviews.go" (the JOIN + the store-local-struct rule)
  </read_first>
  <action>
Create `internal/backendsrv/store/readviews.go` (package store) with parameterized read methods feeding the four builders. Mirror `itemids.go` exactly: plain `(*Store)` methods (reads need no tx-composing variant), `s.db.QueryContext(ctx, ...)` → `rows.Next()`/`Scan` loop → `rows.Err()` check, `fmt.Errorf("...: %w", err)` wrapping. Result structs are store-local (mirror enrich.go), e.g. `InventoryJoinRow`, `WikiGearTierRow`, `WikiSpellRow`, `CharMeta`, `QuestLinkRow`, `CharFreshness`. Use `?` placeholders only (V5) — never string-concat (even though P14 reads take no user input, keep the parameterized habit). slog silent on the happy path; on error log op + err only, NEVER row content (V7).

Methods to author:

1. `InventoryJoin(ctx, bankOnly bool) ([]InventoryJoinRow, error)` — the view/bank join. SQL:
```sql
SELECT c.name, ii.location, ii.name, ii.item_id, ii.count,
       im.wiki_url, im.wiki_summary, im.is_quest_item,
       pp.direction, pp.a30, pp.t30,
       c.last_seen, ii.row_ordinal
FROM inventory_item ii
JOIN character c            ON c.id = ii.character_id
LEFT JOIN item_master im     ON im.item_id = ii.item_id
LEFT JOIN pigparse_price pp  ON pp.item_id = ii.item_id
WHERE c.is_removed = 0
ORDER BY c.name, ii.name, ii.location;
```
When `bankOnly` is true, append `AND c.is_bank_toon = 1` to the WHERE (build the WHERE as a fixed string switch on the bool — NOT by interpolating a value). `item_id`, `wiki_url`, `wiki_summary`, `is_quest_item`, `direction`, `a30`, `t30`, `last_seen` are all nullable in the join → Scan into `sql.NullInt64`/`sql.NullString`/`sql.NullFloat64` and resolve to zero-values on the struct. `pigparse_price.item_id` is the PRIMARY KEY so the join yields at most ONE price row per item (no fan-out). `direction` is **TEXT** (see Task 2 type-drift note) — Scan as string.

2. `QuestLinksByItem(ctx) (map[int64][]QuestLinkRow, error)` — `SELECT item_id, quest_name, source FROM quest_items ORDER BY item_id, quest_name`; group in Go into a `map[int64][]QuestLinkRow` (mirrors v1's `Map<number, QuestLinkRow[]>`, avoids the view JOIN fanning out). `QuestLinkRow{QuestName string; Source string}`.

3. `WikiGearTiers(ctx) ([]WikiGearTierRow, error)` — `SELECT tier, class, slot, item_name, rank FROM wiki_gear_tier ORDER BY tier, class, slot, rank`. `WikiGearTierRow{Tier, Class, Slot, ItemName string; Rank int64}`.

4. `WikiSpells(ctx) ([]WikiSpellRow, error)` — `SELECT class, level, spell_name, normalized_name FROM wiki_spells ORDER BY class, level, spell_name`. `WikiSpellRow{Class string; Level int64; SpellName, NormalizedName string}`.

5. `CharsWithMeta(ctx) ([]CharMeta, error)` — `SELECT id, name, class, level, race FROM character WHERE is_removed = 0 ORDER BY name`. `class`/`level`/`race` nullable → `sql.NullString`/`sql.NullInt64`. `CharMeta{ID int64; Name string; Class string; Level int64; Race string}`.

6. `InventoryByChar(ctx) (map[string][]InvSlotItem, error)` — `SELECT c.name, ii.location, ii.name, ii.item_id FROM inventory_item ii JOIN character c ON c.id = ii.character_id WHERE c.is_removed = 0`; group by char name into `map[string][]InvSlotItem{Location, ItemName string; ItemID int64}`. (gear_check's slot-pair match consumes this per-char.)

7. `SpellbookNormalizedByChar(ctx) (map[string]map[string]bool, error)` — `SELECT c.name, se.normalized_name FROM spellbook_entry se JOIN character c ON c.id = se.character_id WHERE c.is_removed = 0`; group into char→set-of-normalized-names. (spell_check's KNOWN/MISSING join.)

8. `CharFreshness(ctx) ([]CharFreshness, error)` — `SELECT name, last_seen FROM character WHERE is_removed = 0 ORDER BY name`. `CharFreshness{Name string; LastSeen string}` (`last_seen` nullable → NullString). (Plan 03's /api/v1/meta consumes this.)

Write `internal/backendsrv/store/readviews_test.go` (package store) that, using `NewTestDB(t)`, INSERTs a small fixture (2 characters, a few inventory_item rows, one item_master + pigparse_price row, two quest_items rows) and asserts each method returns the expected rows/grouping (e.g. `InventoryJoin(ctx, false)` returns both chars' rows ordered by name/item/location; `bankOnly=true` returns only the `is_bank_toon=1` char; `QuestLinksByItem` groups the two rows under one item_id). This proves the SQL + grouping before compute consumes it.
  </action>
  <verify>
    <automated>cd internal/backendsrv && go test ./store/ -run 'ReadViews|InventoryJoin|QuestLinks|CharsWithMeta|WikiGear|WikiSpells|Spellbook|CharFreshness' -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/backendsrv/store/` passes (including the new readviews_test.go).
    - `grep -n "func (s \*Store)" internal/backendsrv/store/readviews.go` lists InventoryJoin, QuestLinksByItem, WikiGearTiers, WikiSpells, CharsWithMeta, InventoryByChar, SpellbookNormalizedByChar, CharFreshness (8 methods).
    - `grep -c '\bfmt.Sprintf\|"+\|" +' internal/backendsrv/store/readviews.go` returns 0 (no string-concatenated SQL; only `?` placeholders and a fixed WHERE-string switch).
    - `grep -n "bankOnly" internal/backendsrv/store/readviews.go` shows the bank scoping is a fixed-string branch, not a value interpolation.
    - `go vet ./internal/backendsrv/store/` exits 0.
  </acceptance_criteria>
  <done>The 8 store read methods exist, are parameterized, and a seeded-temp-DB test proves their rows + grouping match expectation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Go compute reimpl of view + bank (with pickPrice + enrichment inline) + parity tests</name>
  <files>internal/backendsrv/compute/types.go, internal/backendsrv/compute/view.go, internal/backendsrv/compute/view_test.go, internal/backendsrv/compute/bank.go, internal/backendsrv/compute/bank_test.go</files>
  <read_first>
    - apps-script/src/tabs/buildView.ts (the behavioral oracle — pickPrice at :259-265, sort at :95-99, the join shape; DROP the `=HYPERLINK` formula at :107-109 and the parseToDate/conditional-format machinery at :267-304)
    - apps-script/src/tabs/buildBank.ts (bank oracle — note bank-toon identity changed: v1 `_meta.bank_toon_name` at :54-55 → DB `character.is_bank_toon = 1`; DROP composeCoinRow/writeCoinRow at :135-163)
    - apps-script/src/tabs/composeNotes.ts (the enrichment fields the tooltip needs — these must be carried INLINE on the row so the client composes the tooltip without a second fetch, D-03)
    - internal/backendsrv/store/readviews.go (Task 1 — the InventoryJoinRow + QuestLinksByItem the builders consume)
    - .planning/phases/14-web-frontend/14-PATTERNS.md § "compute/{view,bank,...}.go" (the pickPrice excerpt + the Pitfall-6 direction type-drift warning)
    - .planning/phases/14-web-frontend/14-RESEARCH.md § "Per-View SQL Read Queries" + Pitfall 6 (direction is TEXT)
  </read_first>
  <behavior>
    - pickPrice prefers WTS then WTB: given a row whose direction is the WTS value with a30>0 → returns that a30; given only a WTB value with a30>0 → returns the WTB a30; given neither (a30<=0 or absent) → returns empty/zero sentinel.
    - view rows are sorted Char asc → item asc → location asc (the store already ORDERs this; assert the compute preserves it).
    - each ViewRow carries inline: Char, Slot(location), Item(name), ID(item_id), Count, WikiURL (plain url, NOT =HYPERLINK), Price, LastSynced(ISO string), plus the tooltip payload — WikiSummary, IsQuestItem, the WTS/WTB a30+t30 detail, and the grouped QuestLinks.
    - bank rows are the same shape but only for the `is_bank_toon=1` character; Coin is null/absent (NOT fabricated zeros).
  </behavior>
  <action>
Create package `compute` in `internal/backendsrv/compute/`.

`types.go` — the exported row structs the read API will JSON-encode (Plan 03 encodes these directly):
- `type PriceDetail struct { Direction string; A30 float64; T30 int64 }` — one per direction present.
- `type QuestLink struct { QuestName string; Source string }`
- `type ViewRow struct { Char, Slot, Item string; ID int64; Count int64; WikiURL string; Price *float64; LastSynced string; WikiSummary string; IsQuestItem bool; Prices []PriceDetail; QuestLinks []QuestLink }` — `Price` is the pickPrice result (nil when neither WTS nor WTB has a30>0; the client renders blank). `Prices` carries the raw WTS/WTB detail for the tooltip (D-03). **JSON tags are snake_case — MANDATORY, not optional.** This is a FIXED cross-plan contract: Plan 02's `composeNotes.ts`, Plan 03's read handlers, and Plan 04's client all consume these exact names — do NOT use lowerCamel. Apply `json:"..."` snake_case tags across all four row structs (`json:"char"`, `json:"slot"`, `json:"item"`, `json:"id"`, `json:"count"`, `json:"wiki_url"`, `json:"price"`, `json:"last_synced"`, `json:"wiki_summary"`, `json:"is_quest_item"`, `json:"prices"`, `json:"quest_links"`, etc.) to match the existing API's JSON style, and document the full field list in the file header. **`PriceDetail` JSON is also fixed:** `json:"direction"` (string; `"0"`=WTS / `"1"`=WTB — the exact encoding Plans 02/04 assume), `json:"a30"`, `json:"t30"`.
- `BankRow` = alias/struct identical to ViewRow (bank uses the same columns); add a `BankView struct { Rows []ViewRow; Coin *CoinTotals }` where `Coin` is `nil` in P14 (ADMIN-05 fills it in P15) and `CoinTotals struct { PP,GP,SP,CP int64 }` is defined but unused-as-nil so the JSON shape is stable for P15.

`view.go` — `func View(ctx context.Context, s *store.Store) ([]ViewRow, error)`:
1. `rows, _ := s.InventoryJoin(ctx, false)`; `links, _ := s.QuestLinksByItem(ctx)`.
2. For each join row, build a ViewRow: copy Char/Slot/Item/ID/Count; `WikiURL` = the plain url (NO `=HYPERLINK(...)` — emit the bare URL string, the web renders a real `<a>`); attach `links[item_id]` as QuestLinks; set WikiSummary + IsQuestItem; build `Prices` from the single price row's direction/a30/t30 (the join is one-row-per-item, so 0-or-1 PriceDetail).
3. `pickPrice` — port `buildView.ts:259-265` BUT with the **TEXT direction** drift fix: the SQLite `pigparse_price.direction` is TEXT (v1 was numeric 0=WTS/1=WTB; the P12 job stores `strconv.Itoa(t)`). Implement `pickPrice` to prefer the WTS direction then WTB, comparing the **stringified** direction. Define two package consts `directionWTS = "0"` and `directionWTB = "1"` as the default mapping, AND add a code comment + the Task-4 verification step that the actual stored values are confirmed on the box (`SELECT DISTINCT direction FROM pigparse_price`). pickPrice returns `*float64` (nil sentinel when neither has a30>0).
4. Sort is already applied by the store ORDER BY; do not re-sort (assert order in the test).

`bank.go` — `func Bank(ctx context.Context, s *store.Store) (BankView, error)`: same as View but `s.InventoryJoin(ctx, true)` (bankOnly), and return `BankView{Rows: ..., Coin: nil}`. Do NOT port composeCoinRow/writeCoinRow.

Parity tests (`view_test.go`, `bank_test.go`) using `store.NewTestDB(t)`: INSERT a fixture mirroring a v1 `buildView.test.ts` scenario — a character with 2-3 inventory items, one with a matching item_master (wiki_url + summary + is_quest_item=1) and a pigparse_price row (direction "0", a30=4500, t30=75), and a quest_items row. Assert: the ViewRow for that item has `*Price == 4500`, `WikiURL` is the plain url (no "=HYPERLINK"), `IsQuestItem == true`, `len(QuestLinks) == 1`, `WikiSummary` populated, and rows are ordered Char→item→location. Add a pickPrice unit test: WTS-present→WTS a30; WTB-only→WTB a30; neither→nil. For bank: assert only the `is_bank_toon=1` char's rows return and `Coin == nil`.
  </action>
  <verify>
    <automated>cd internal/backendsrv && go test ./compute/ -run 'View|Bank|PickPrice' -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/backendsrv/compute/ -run 'View|Bank|PickPrice'` passes.
    - `grep -c "HYPERLINK" internal/backendsrv/compute/view.go internal/backendsrv/compute/bank.go` returns 0 (the Sheet formula machinery is dropped; plain URLs only).
    - `grep -n "directionWTS\|directionWTB" internal/backendsrv/compute/view.go` shows the stringified-direction comparison (TEXT, not numeric).
    - `grep -n "Coin" internal/backendsrv/compute/bank.go` shows Coin is returned as nil (no fabricated zeros).
    - `grep -n "func pickPrice" internal/backendsrv/compute/view.go` exists and returns `*float64`.
    - The view_test.go asserts `len(QuestLinks)` and `WikiSummary`/`IsQuestItem` inline on the row (proves D-03 enrichment-inline).
  </acceptance_criteria>
  <done>view + bank compute exist, port pickPrice with the TEXT-direction fix, carry enrichment inline per D-03, render bank coin as nil, and pass parity tests over a seeded temp DB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Go compute reimpl of gear_check + spell_check (the WEB-02 parity heart) + table-tests from v1 fixtures</name>
  <files>internal/backendsrv/compute/eqconst.go, internal/backendsrv/compute/gearcheck.go, internal/backendsrv/compute/gearcheck_test.go, internal/backendsrv/compute/spellcheck.go, internal/backendsrv/compute/spellcheck_test.go</files>
  <read_first>
    - apps-script/src/tabs/buildGearCheck.ts (THE oracle — tiers shown :87-89, slot-pair match + OK/OTHER/MISSING ordering :101-125, TIER_SORT :29-33, sort :131-141)
    - apps-script/src/tabs/buildSpellCheck.ts (THE oracle — class+level>=1 filter + level<=char.level + KNOWN/MISSING :76-86, sort :89-95)
    - apps-script/src/__tests__/buildGearCheck.test.ts (the parity oracle — its seed arrays + expected (status,have) tuples become the Go table-test expectations)
    - apps-script/src/__tests__/buildSpellCheck.test.ts (the parity oracle — seed arrays + expected (level,spell,status) tuples)
    - internal/backendsrv/enrich/eqconst.go (WIKI_SLOT_TO_INV_SLOTS ALREADY EXISTS here, ported verbatim — REUSE it; only the 3-entry tier-rank map is new)
    - internal/backendsrv/store/readviews.go (Task 1 — CharsWithMeta, WikiGearTiers, WikiSpells, InventoryByChar, SpellbookNormalizedByChar)
    - .planning/phases/14-web-frontend/14-PATTERNS.md § "compute/...gearcheck.go" + § "compute/*_test.go" (the slot-pair excerpt + the fixture-port column mapping)
  </read_first>
  <behavior>
    - gear_check tiers: 'Velious Pre-Raid/Group' + 'Velious Raiding' always; add 'Iksar' iff char.race == 'IKS'.
    - per (char,tier,slot,recommendation): matched (case-insensitive name equals a recommended item in EITHER inv slot of the pair) → OK, Have=matched name; else slot non-empty → OTHER, Have=first item in slot; else → MISSING, Have="". Order is load-bearing (matched wins over non-empty).
    - gear sort: Char asc → tier rank (Pre-Raid=1,Raiding=2,Iksar=3) → slot asc → recommended asc.
    - spell_check: skip chars with no class or level<1; for each wiki_spells row of that class with level<=char.level, KNOWN iff the char's spellbook (normalized_name set) contains it, else MISSING.
    - spell sort: Char asc → level asc → spell asc.
  </behavior>
  <action>
`eqconst.go` (package compute) — add ONLY the tier-rank map (the 3-entry `buildGearCheck.ts:29-33` `TIER_SORT`): `var tierRank = map[string]int{"Velious Pre-Raid/Group":1, "Velious Raiding":2, "Iksar":3}`. Do NOT re-type `WIKI_SLOT_TO_INV_SLOTS` — it already exists in `internal/backendsrv/enrich/eqconst.go`. Reference it: import the enrich package and use `enrich.WIKI_SLOT_TO_INV_SLOTS` (verify no import cycle — enrich imports nothing from compute/store; if the import is awkward, the alternative is to move the map to a shared spot, but the simplest correct path is `compute` importing `enrich` for the const).

`gearcheck.go` — `func GearCheck(ctx context.Context, s *store.Store) ([]GearCheckRow, error)` where `GearCheckRow struct { Char, Class, Tier, Slot, Have, Recommended, Status string }` (JSON snake_case tags consistent with types.go):
1. `chars, _ := s.CharsWithMeta(ctx)`; `tiers, _ := s.WikiGearTiers(ctx)`; `invByChar, _ := s.InventoryByChar(ctx)`.
2. Group `tiers` by (tier, class). For each char with non-empty class: tiersToShow = ["Velious Pre-Raid/Group","Velious Raiding"], append "Iksar" iff race=="IKS".
3. For each tier → each class's recommendations grouped by slot → for each slot, `invSlots := enrich.WIKI_SLOT_TO_INV_SLOTS[slot]`, `charItemsInSlots := items where location ∈ invSlots`. For each recommendation: matched = first charItemsInSlots whose `strings.EqualFold(itemName, rec.ItemName)`. matched→OK/Have=matched; else len(charItemsInSlots)>0→OTHER/Have=charItemsInSlots[0]; else→MISSING/Have="". (Port buildGearCheck.ts:101-125 exactly; the ORDER of the three branches is load-bearing.)
4. Sort: Char asc → tierRank[tier] → slot asc → recommended asc (port :131-141).

`spellcheck.go` — `func SpellCheck(ctx context.Context, s *store.Store) ([]SpellCheckRow, error)` where `SpellCheckRow struct { Char, Class string; Level int64; Spell, Status string }`:
1. `chars, _ := s.CharsWithMeta(ctx)`; `wiki, _ := s.WikiSpells(ctx)` grouped by class; `known, _ := s.SpellbookNormalizedByChar(ctx)`.
2. For each char: skip if class=="" or level<1. For each wiki spell of that class: skip if `w.Level > char.Level`; status = KNOWN if `known[char][w.NormalizedName]` else MISSING. (The normalized_name join needs NO recompute — both `spellbook_entry.normalized_name` and `wiki_spells.normalized_name` are materialized with the identical `lower(trim(...))` expression.)
3. Sort: Char asc → level asc → spell asc.

Parity table-tests — translate the v1 vitest seed arrays into Go (RESEARCH approach 1). For `gearcheck_test.go`: a table of cases, each INSERTing into `store.NewTestDB(t)` the same char_owner metadata + wiki_gear_tier rows + inventory rows the corresponding `buildGearCheck.test.ts` case seeds, asserting the same `(Char,Tier,Slot,Have,Recommended,Status)` tuples in the same order. Cover at minimum: an OK case (recommended item equipped, incl. a pair-slot like Ears/EAR2), an OTHER case (different item in slot), a MISSING case (empty slot), and the Iksar-tier-only-for-IKS case. For `spellcheck_test.go`: KNOWN (spellbook has the normalized name), MISSING (doesn't), level-gating (a high-level spell excluded for a low-level char), and the skip-char-without-class case. The v1 expected tuples are the parity bar — same input → same status/order.
  </action>
  <verify>
    <automated>cd internal/backendsrv && go test ./compute/ -run 'GearCheck|SpellCheck' -v && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/backendsrv/compute/` passes (all four builders + their parity tests).
    - `grep -c "EAR1\|EAR2\|FINGER1" internal/backendsrv/compute/eqconst.go` returns 0 (the slot-pair map is NOT re-typed; it's reused from enrich).
    - `grep -n "enrich.WIKI_SLOT_TO_INV_SLOTS" internal/backendsrv/compute/gearcheck.go` shows the reuse.
    - `grep -n "tierRank" internal/backendsrv/compute/gearcheck.go` shows the 3-entry tier-rank map (Pre-Raid=1/Raiding=2/Iksar=3).
    - `grep -n "EqualFold\|ToLower" internal/backendsrv/compute/gearcheck.go` shows case-insensitive name matching.
    - `grep -n "NormalizedName\|normalized" internal/backendsrv/compute/spellcheck.go` shows the join is on the materialized normalized_name (no recompute).
    - gearcheck_test.go contains an Iksar case asserting the tier appears only for race=="IKS".
    - `go build ./...` (whole repo) exits 0.
  </acceptance_criteria>
  <done>gear_check + spell_check compute reimpl exists, reuses the existing slot-pair map, and passes Go table-tests whose expectations are translated from the v1 vitest fixtures — WEB-02 parity is test-provable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store SQL ↔ SQLite | The read methods author all SQL; compute composes typed structs. No user input crosses into SQL in P14 (no server-side filters). |
| compute output → (Plan 03) JSON → browser | The row structs become the public JSON payload; they carry wiki/user-controlled strings (item/char/quest names, summary) that the CLIENT must escape (Plan 02/04). compute does NOT escape — it returns raw data (mirrors v1 searchIndex.ts trust-boundary note). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.01-01 | Tampering | store/readviews.go SQL | mitigate | Parameterized `?` placeholders exclusively; the only dynamic SQL is a fixed-string WHERE switch on the `bankOnly` bool (never a value interpolation) — grep-verified 0 string-concat. Matches the existing store discipline (binding.go/enrich.go/itemids.go). |
| T-14.01-02 | Information disclosure | store read methods (logs) | mitigate | slog logs op + err only, NEVER row content or query values (V7) — carries the handler.go/whoami.go logging discipline forward. |
| T-14.01-03 | Tampering (downstream XSS) | compute row structs | accept (boundary owned downstream) | compute returns RAW wiki/user strings by design (the data layer is not the escaping layer); the escaping obligation is the client's `composeNotes.ts` + Svelte `{}` interpolation (Plan 02/04, where T-14.02-01 mitigates it). Documented as the deliberate trust-boundary split, not an oversight. |
| T-14.01-04 | Elevation | read methods are read-only | mitigate | Every method is a pure SELECT — zero DELETE/INSERT/UPDATE (V4 read-only contract, mirrors whoami.go). No write surface introduced. |
</threat_model>

<verification>
- `go test ./internal/backendsrv/...` green (store + compute, incl. all parity tests).
- `go vet ./internal/backendsrv/...` exits 0; `go build ./...` exits 0.
- Parity is provable: gear/spell table-tests assert the v1 expected tuples for the same seeded input.
- No `=HYPERLINK` and no re-typed slot-pair map (grep-verified).
</verification>

<success_criteria>
- BACKEND-05 (data/compute half): the four view builders compute correctly over the SQLite store via parameterized read methods.
- WEB-02: gear OK/MISSING/OTHER + spell KNOWN/MISSING match v1 semantics, proven by Go table-tests translated from the v1 vitest fixtures.
- D-01 (server is the compute layer) + D-02 (Go reimpl with parity oracle) + D-03 (enrichment inline on view/bank rows) satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/14-web-frontend/14-01-SUMMARY.md` — note the JSON tag convention chosen (it is the contract Plan 03 encodes and Plan 02/04 consume), the confirmed `pigparse_price.direction` stored values if checked, and any fixture-translation deviations from the v1 expected tuples.
</output>
